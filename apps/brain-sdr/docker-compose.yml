services:
  brain-sdr:
    build:
      context: ../..
      dockerfile: apps/brain-sdr/Dockerfile
    image: brain-sdr:latest
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    env_file: .env
    restart: unless-stopped
