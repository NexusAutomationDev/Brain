services:
  brain-echo:
    build:
      context: ../..
      dockerfile: apps/brain-echo/Dockerfile
    image: brain-echo:latest
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file: .env
    restart: unless-stopped
