---
phase: 21-rag
plan: 03
type: tdd
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - packages/core/src/tools/search-knowledge.ts
  - packages/core/src/index.ts
  - apps/brain-sdr/src/server.ts
  - apps/brain-sdr/src/index.ts
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04

must_haves:
  truths:
    - "LLM pode chamar search_knowledge(query, collections[]) e receber texto formatado em blocos (RAG-02, D-10)"
    - "search_knowledge com collections vazias retorna string de 'Nenhum resultado' sem chamar o banco (D-11, Pitfall 3)"
    - "brain-sdr monta createIngestApp(sql) e registra search_knowledge no ToolsRegistry (D-05, D-12)"
    - "imports de createIngestApp e createSearchKnowledgeTool funcionam via @brain-pkg/core (D-04)"
  artifacts:
    - path: "packages/core/src/tools/search-knowledge.ts"
      provides: "createSearchKnowledgeTool(sql) — factory de tool LangChain (D-06)"
      exports: ["createSearchKnowledgeTool"]
    - path: "packages/core/src/index.ts"
      provides: "barrel export de createIngestApp e createSearchKnowledgeTool"
      contains: "createIngestApp"
    - path: "apps/brain-sdr/src/server.ts"
      provides: "brain-sdr monta createIngestApp(sql)"
      contains: "createIngestApp"
    - path: "apps/brain-sdr/src/index.ts"
      provides: "brain-sdr habilita search_knowledge no ToolsRegistry (D-12)"
      contains: "search_knowledge"
  key_links:
    - from: "packages/core/src/tools/search-knowledge.ts"
      to: "packages/core/src/rag/search.ts"
      via: "import searchKnowledge + resolveEmbeddingModel"
      pattern: "searchKnowledge"
    - from: "packages/core/src/tools/search-knowledge.ts"
      to: "packages/ai/src/embeddings/factory.ts"
      via: "import createEmbeddings — embedder.embedQuery(query)"
      pattern: "createEmbeddings"
    - from: "apps/brain-sdr/src/server.ts"
      to: "packages/core/src/rag/ingest.ts"
      via: "app.route('/', createIngestApp(sql))"
      pattern: "createIngestApp"
    - from: "apps/brain-sdr/src/index.ts"
      to: "packages/core/src/tools/search-knowledge.ts"
      via: "toolsRegistry.enableTool('sdr', 'search_knowledge')"
      pattern: "search_knowledge"
---

<objective>
Implementar `createSearchKnowledgeTool`, atualizar o barrel export de `packages/core`, e integrar RAG no `brain-sdr` — tornando GREEN os últimos testes RED e entregando a fase completa.

Purpose: RAG-02 e RAG-03 requerem que o LLM possa chamar `search_knowledge(query, collections[])` e receber o contexto formatado em blocos (D-10). RAG-01 requer que o endpoint `/api/v1/ingest` esteja montado no `brain-sdr`. D-12 requer habilitação explícita via `toolsRegistry.enableTool`. Este plano fecha todas as 4 integrações: tool factory, barrel export, server mount, registry enable.

Output: Fase 21 completa — 4 requirements RAG-01 a RAG-04 implementados e testados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-rag/21-CONTEXT.md
@.planning/phases/21-rag/21-RESEARCH.md
@.planning/phases/21-rag/21-01-SUMMARY.md
@.planning/phases/21-rag/21-02-SUMMARY.md

<interfaces>
<!-- Padrão de tool factory — packages/core/src/tools/pause-session.ts -->
```typescript
import { tool } from "@langchain/core/tools";
import { drizzle } from "drizzle-orm/postgres-js";
import type { Sql } from "postgres";
import { z } from "zod";

export function createPauseSessionTool(sql: Sql) {
  const db = drizzle(sql);
  return tool(
    async (_args, config?) => { /* implementação */ return "string result"; },
    {
      name: "pause_session",
      description: "...",
      schema: z.object({}),
    }
  );
}
```

<!-- searchKnowledge e ChunkResult (criados no Plano 02) -->
```typescript
// De packages/core/src/rag/search.ts:
export interface ChunkResult {
  id: string;
  content: string;
  collection: string;
  chunkIndex: number;
  totalChunks: number;
  similarity: number;
}
export function resolveEmbeddingModel(): string { ... }
export async function searchKnowledge(
  db: PostgresJsDatabase,
  queryVector: number[],
  collections: string[],
  embeddingModel: string,
  topK?: number,
  threshold?: number
): Promise<ChunkResult[]>
```

<!-- createEmbeddings após D-17 (Plano 01) -->
```typescript
// Sem throw — EMBEDDING_MODEL opcional
const embedder = await createEmbeddings();
const queryVector = await embedder.embedQuery(query);  // single query → number[]
```

<!-- Formato de bloco de resultado (D-10) -->
```typescript
// D-10: cada resultado = "[Coleção: {collection}] chunk {chunkIndex+1}/{totalChunks}\n{content}"
// Separados por "\n---\n"
function formatResults(results: ChunkResult[]): string {
  return results
    .map((r) => `[Coleção: ${r.collection}] chunk ${r.chunkIndex + 1}/${r.totalChunks}\n${r.content}`)
    .join("\n---\n");
}
```

<!-- D-11: string de "sem resultados" -->
```typescript
const NO_RESULTS = "Nenhum resultado encontrado para a consulta nas coleções informadas.";
```

<!-- Barrel export atual packages/core/src/index.ts -->
```typescript
// Últimas linhas relevantes para adicionar após:
export { createPauseSessionTool } from "./tools/pause-session.js";
export { createFinishConversationTool } from "./tools/finish-conversation.js";
export { createRespondTool } from "./tools/respond.js";
// ...
export { FupScheduler, getNextValidSlot } from "./fup/fup-scheduler.js";
// ADICIONAR:
// export { createSearchKnowledgeTool } from "./tools/search-knowledge.js";
// export { createIngestApp } from "./rag/index.js";
```

<!-- apps/brain-sdr/src/server.ts atual -->
```typescript
export function createServer(sql: Sql, runner: BrainRunner): Hono {
  const app = new Hono();
  app.route("/", createHealthApp(sql));
  app.route("/", createWebhookApp(runner));
  app.route("/", createCoreApp(runner));
  // ADICIONAR: app.route("/", createIngestApp(sql));
  return app;
}
```

<!-- apps/brain-sdr/src/index.ts — toolsRegistry registros existentes -->
```typescript
toolsRegistry.enableTool("sdr", "qualify_lead");
toolsRegistry.enableTool("sdr", "pause_session");
toolsRegistry.enableTool("sdr", "finish_conversation");
// ADICIONAR: toolsRegistry.enableTool("sdr", "search_knowledge");  // D-12
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar createSearchKnowledgeTool — RED→GREEN</name>
  <files>
    packages/core/src/tools/search-knowledge.ts
  </files>
  <read_first>
    - packages/core/src/tools/__tests__/search-knowledge.test.ts (testes RED a tornar GREEN — criados no Plano 01)
    - packages/core/src/tools/pause-session.ts (padrão EXATO de tool factory com closure sobre sql — replicar)
    - packages/core/src/rag/search.ts (imports de searchKnowledge, resolveEmbeddingModel, ChunkResult)
    - .planning/phases/21-rag/21-CONTEXT.md §D-06, D-07, D-08, D-09, D-10, D-11 (spec da tool)
    - .planning/phases/21-rag/21-RESEARCH.md §Pattern 2 (código de referência da tool factory)
    - .planning/phases/21-rag/21-RESEARCH.md §Anti-Patterns (embedding NÃO ocorre dentro de search.ts)
  </read_first>
  <behavior>
    search-knowledge.test.ts — deve tornar GREEN:
    - tool.name === "search_knowledge"
    - tool.invoke({ query: "...", collections: ["faq"] }) retorna string "[Coleção: faq] chunk 1/3\n..." (D-10)
    - tool.invoke com resultados vazios retorna "Nenhum resultado encontrado para a consulta nas coleções informadas." (D-11)
    - tool.invoke com múltiplas collections chama searchKnowledge com array multi-elemento (RAG-03)
    - Zod schema rejeita collections: [] (min(1))
    - tool não recebe topK nem threshold como argumento — hardcoded (D-09)
  </behavior>
  <action>
    ARQUIVO: packages/core/src/tools/search-knowledge.ts

    ```typescript
    // RAG-02/RAG-03: search_knowledge tool — factory seguindo padrão de createPauseSessionTool
    // D-06: Factory function com closure sobre sql — compatibilidade multi-tenant
    // D-07: Top 5 chunks no total (hardcoded — D-09: LLM não configura)
    // D-08: Threshold cosine 0.5 (hardcoded — D-09)
    // D-09: topK e threshold não expostos no schema Zod da tool
    // D-10: Resultado formatado em blocos "[Coleção: X] chunk N/M\nconteúdo"
    // D-11: Sem resultados → string fixa, sem throw
    import { tool } from "@langchain/core/tools";
    import { drizzle } from "drizzle-orm/postgres-js";
    import { createEmbeddings } from "@brain-pkg/ai";
    import type { Sql } from "postgres";
    import { z } from "zod";
    import { searchKnowledge, resolveEmbeddingModel } from "../rag/search.js";
    import type { ChunkResult } from "../rag/search.js";

    // D-11: String constante para "sem resultados" — testável por igualdade exata
    const NO_RESULTS_MSG = "Nenhum resultado encontrado para a consulta nas coleções informadas.";

    /**
     * D-10: Formata resultados de chunks em blocos legíveis pelo LLM.
     * Formato: "[Coleção: {collection}] chunk {chunkIndex+1}/{totalChunks}\n{content}"
     * Separador entre chunks: "\n---\n"
     */
    function formatResults(results: ChunkResult[]): string {
      return results
        .map(
          (r) =>
            `[Coleção: ${r.collection}] chunk ${r.chunkIndex + 1}/${r.totalChunks}\n${r.content}`
        )
        .join("\n---\n");
    }

    /**
     * D-06: Cria a tool search_knowledge bound ao sql do tenant.
     *
     * RAG-02: Gera embedding da query e busca chunks similares no pgvector.
     * RAG-03: Aceita array de coleções e busca em todas simultaneamente.
     * Anti-pattern evitado: embedding da query ocorre AQUI (não em search.ts) — separação de concerns.
     *
     * @param sql - postgres.js Sql instance do tenant (de BrainBuildContext.sql)
     */
    export function createSearchKnowledgeTool(sql: Sql) {
      const db = drizzle(sql);
      return tool(
        async (args: { query: string; collections: string[] }) => {
          // Pitfall 3: guard collections vazio (schema Zod garante min(1), mas guard defensivo)
          if (!args.collections || args.collections.length === 0) {
            return NO_RESULTS_MSG;
          }

          // D-14/D-15: resolver modelo atual para filtrar por embedding_model (D-03a)
          const embeddingModel = resolveEmbeddingModel();

          // Gerar embedding da query (Pitfall 1: usar embedQuery, não embedDocuments)
          const embedder = await createEmbeddings();
          const queryVector = await embedder.embedQuery(args.query);

          // D-07/D-08/D-09: topK=5 e threshold=0.5 hardcoded — LLM não controla
          const results = await searchKnowledge(db, queryVector, args.collections, embeddingModel);

          if (results.length === 0) {
            return NO_RESULTS_MSG;  // D-11: sem throw, retorna string fixa
          }

          return formatResults(results);  // D-10: blocos formatados
        },
        {
          name: "search_knowledge",
          description:
            "Busca contexto relevante na base de conhecimento. Use quando precisar de informações sobre produtos, FAQs, manuais ou qualquer conteúdo ingerido nas coleções disponíveis.",
          schema: z.object({
            query: z.string().min(1).describe("Texto da busca semântica"),
            // D-09: collections é o único parâmetro configurável pelo LLM
            collections: z
              .array(z.string().min(1))
              .min(1)  // Pitfall 3: min(1) — rejeita array vazio
              .describe("Lista de coleções para buscar (mínimo 1)"),
          }),
        }
      );
    }
    ```
  </action>
  <verify>
    <automated>bun test packages/core/src/tools/__tests__/search-knowledge.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `bun test packages/core/src/tools/__tests__/search-knowledge.test.ts` passa com 0 falhas (GREEN)
    - `grep -n "search_knowledge" packages/core/src/tools/search-knowledge.ts` retorna linha com name
    - `grep -n "NO_RESULTS_MSG\|Nenhum resultado" packages/core/src/tools/search-knowledge.ts` retorna linha (D-11)
    - `grep -n "formatResults" packages/core/src/tools/search-knowledge.ts` retorna linha no handler (D-10)
    - `grep -n "min(1)" packages/core/src/tools/search-knowledge.ts` retorna linha no schema Zod (Pitfall 3)
    - `grep -n "embedQuery" packages/core/src/tools/search-knowledge.ts` retorna linha (não embedDocuments — Pitfall 1)
  </done>
</task>

<task type="auto">
  <name>Task 2: Barrel export + integração brain-sdr</name>
  <files>
    packages/core/src/index.ts
    apps/brain-sdr/src/server.ts
    apps/brain-sdr/src/index.ts
  </files>
  <read_first>
    - packages/core/src/index.ts (barrel export atual — adicionar exports RAG sem remover existentes)
    - apps/brain-sdr/src/server.ts (adicionar app.route createIngestApp)
    - apps/brain-sdr/src/index.ts (adicionar toolsRegistry.enableTool search_knowledge)
    - packages/core/src/runner/runner.ts (verificar se buildGraph() já passa sql para tool factories — confirmar que createSearchKnowledgeTool(sql) segue o mesmo wiring sem alterações no runner)
    - .planning/phases/21-rag/21-CONTEXT.md §D-05, D-12 (decisões de integração)
    - .planning/phases/21-rag/21-RESEARCH.md §Pitfall 6 (barrel export crítico — sem ele import de @brain-pkg/core falha)
  </read_first>
  <action>
    ARQUIVO 1: packages/core/src/index.ts

    Adicionar no bloco de SDK-07 (após createRespondTool), antes do bloco EVT-01:

    ```typescript
    // RAG-01/RAG-02: RAG tools and ingest endpoint
    // D-05: createIngestApp exportado para Brain apps montarem explicitamente no server.ts
    // D-06: createSearchKnowledgeTool exportado para BrainRunner injetar via buildGraph()
    export { createSearchKnowledgeTool } from "./tools/search-knowledge.js";
    export { createIngestApp } from "./rag/index.js";
    ```

    ARQUIVO 2: apps/brain-sdr/src/server.ts

    Adicionar import de `createIngestApp`:
    ```typescript
    import { createCoreApp, createIngestApp, type BrainRunner } from "@brain-pkg/core";
    ```

    Adicionar `app.route("/", createIngestApp(sql))` APÓS createCoreApp e ANTES do return:
    ```typescript
    export function createServer(sql: Sql, runner: BrainRunner): Hono {
      const app = new Hono();
      app.route("/", createHealthApp(sql));
      app.route("/", createWebhookApp(runner));
      app.route("/", createCoreApp(runner));
      app.route("/", createIngestApp(sql));  // RAG-01/D-05: ingest endpoint
      return app;
    }
    ```

    Também adicionar `sql` ao parâmetro se não estiver na assinatura — verificar que `createServer(sql, runner)` já recebe `sql: Sql` (está no código atual — não mudar assinatura).

    ARQUIVO 3: apps/brain-sdr/src/index.ts

    Adicionar `toolsRegistry.enableTool("sdr", "search_knowledge")` após os outros enableTool calls:
    ```typescript
    toolsRegistry.enableTool("sdr", "qualify_lead");
    toolsRegistry.enableTool("sdr", "pause_session");
    toolsRegistry.enableTool("sdr", "finish_conversation");
    toolsRegistry.enableTool("sdr", "search_knowledge");  // D-12/RAG-02: habilita tool RAG para brain-sdr
    ```

    Verificar que NÃO é necessário adicionar `createIngestApp` ao index.ts (é montado em server.ts, não em index.ts).
  </action>
  <verify>
    <automated>cd /root/Brain && bun run typecheck 2>&1 | grep -E "(error|Error|cannot find|Cannot find)" | grep -v "node_modules" | head -20; echo "TYPECHECK_EXIT: $?"; bun test packages/core/src/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `grep -n "createIngestApp" packages/core/src/index.ts` retorna linha de export (Pitfall 6)
    - `grep -n "createSearchKnowledgeTool" packages/core/src/index.ts` retorna linha de export
    - `grep -n "createIngestApp" apps/brain-sdr/src/server.ts` retorna linha de import E linha de app.route (D-05)
    - `grep -n "search_knowledge" apps/brain-sdr/src/index.ts` retorna linha de toolsRegistry.enableTool (D-12)
    - typecheck sem erros nos arquivos modificados (bun run typecheck ou tsc --noEmit)
    - Suite completa final: `bun test packages/core/src/` — todos os testes GREEN, sem regressão
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → search_knowledge tool args | LLM fornece `query` e `collections[]` — validados por Zod antes de ir ao banco |
| @brain-pkg/core barrel → brain-sdr | Exportações públicas do SDK — apenas factories, não instâncias |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | LLM injeta collections: [] para forçar inArray vazio | mitigate | Zod schema `z.array(z.string().min(1)).min(1)` + guard defensivo em search.ts (Pitfall 3) |
| T-21-03-02 | Elevation of Privilege | search_knowledge acessa coleções de outros tenants | accept | Isolamento por banco (1 banco por cliente — arquitetura TenantPoolManager); não há separação por collection dentro do banco (v1) |
| T-21-03-03 | Information Disclosure | resultado formatado expõe chunks de documentos confidenciais ao LLM | accept | Documentos ingeridos são intencionalmente disponibilizados ao LLM — operador controla o que ingere |
| T-21-03-04 | Denial of Service | query muito longa para embedQuery → latência alta ou rate limit | accept | Rate limiting é responsabilidade do provider de embedding; sem mitigação adicional em v1 |
</threat_model>

<verification>
Verificação final de fase completa:

```bash
# Tool search_knowledge — GREEN
bun test packages/core/src/tools/__tests__/search-knowledge.test.ts

# Barrel exports presentes
grep -n "createIngestApp\|createSearchKnowledgeTool" packages/core/src/index.ts

# Integração brain-sdr
grep -n "createIngestApp" apps/brain-sdr/src/server.ts
grep -n "search_knowledge" apps/brain-sdr/src/index.ts

# Suite completa sem regressão
bun test packages/core/src/
bun test packages/ai/src/embeddings/

# Typecheck clean
bun run typecheck 2>&1 | grep -v node_modules | grep -iE "error" | wc -l
# deve retornar 0
```
</verification>

<success_criteria>
- `bun test packages/core/src/tools/__tests__/search-knowledge.test.ts` — GREEN
- `packages/core/src/index.ts` exporta `createIngestApp` e `createSearchKnowledgeTool`
- `apps/brain-sdr/src/server.ts` monta `createIngestApp(sql)` via `app.route`
- `apps/brain-sdr/src/index.ts` chama `toolsRegistry.enableTool("sdr", "search_knowledge")`
- `bun test packages/core/src/` passa sem regressões — todos os 57+ testes existentes mantidos
- typecheck sem erros nos arquivos modificados
- Requirements RAG-01, RAG-02, RAG-03, RAG-04 todos implementados e verificáveis via testes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-rag/21-03-SUMMARY.md`
</output>
