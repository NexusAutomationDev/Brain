---
phase: 24-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/runner/runner.ts
  - packages/core/src/leads/lead-service.ts
  - packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "BrainRunner.init() loga warning quando FUP_WEBHOOK_URL configurado mas checkpointer null (WR-01)"
    - "LeadService.resetFup() inclui updatedAt: new Date() no set() — consistente com setFullpp/setIaAtivada (WR-02)"
    - "BrainRunner guarda SIGTERM handler como campo privado e o remove em close() — sem acúmulo de listeners (WR-03)"
  artifacts:
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner com WR-01 (else-if warning) e WR-03 (campo privado + process.off)"
      contains: "_sigtermHandler"
    - path: "packages/core/src/leads/lead-service.ts"
      provides: "LeadService.resetFup() com updatedAt"
      contains: "updatedAt: new Date()"
    - path: "packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts"
      provides: "Teste confirmando updatedAt presente no set() de resetFup()"
      contains: "updatedAt"
  key_links:
    - from: "runner.ts init()"
      to: "logger.warn"
      via: "else if (fupWebhookUrl && !this.checkpointer)"
      pattern: "!this\\.checkpointer"
    - from: "runner.ts constructor"
      to: "_sigtermHandler field"
      via: "private readonly _sigtermHandler: () => Promise<void>"
      pattern: "_sigtermHandler"
    - from: "runner.ts close()"
      to: "process.off"
      via: "process.off('SIGTERM', this._sigtermHandler)"
      pattern: "process\\.off"
---

<objective>
Corrigir WR-01, WR-02 e WR-03 — três itens de tech debt identificados na auditoria v1.4.

Purpose: Aumentar visibilidade operacional (WR-01), consistência de dados (WR-02) e estabilidade de memória (WR-03 — sem EventEmitter leak em reinicializações).
Output: runner.ts e lead-service.ts corrigidos; teste de WR-02 atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-tech-debt-cleanup/24-CONTEXT.md

<!-- Arquivos a modificar — lidos obrigatoriamente antes de editar -->
@packages/core/src/runner/runner.ts
@packages/core/src/leads/lead-service.ts
@packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 + WR-03 — BrainRunner SIGTERM handler e warning de checkpointer</name>
  <read_first>
    - packages/core/src/runner/runner.ts (estado atual completo — ver linhas 65-188 e 382-398)
  </read_first>
  <files>packages/core/src/runner/runner.ts</files>
  <action>
Duas mudanças cirúrgicas em runner.ts:

**WR-01 — Warning quando FUP_WEBHOOK_URL configurado mas checkpointer null (linha ~163):**

Após o bloco `if (fupWebhookUrl && this.checkpointer) { ... }`, adicionar else-if:

```typescript
} else if (fupWebhookUrl && !this.checkpointer) {
  // WR-01: Visibilidade operacional — checkpointer null impede FupScheduler de iniciar.
  // Sem esse log, operador configura FUP_WEBHOOK_URL e não recebe nenhum feedback.
  this.logger.warn(
    { brainType: this.brain.brainType, hasFupUrl: true },
    "FupScheduler not started — checkpointer unavailable"
  );
}
```

Inserir IMEDIATAMENTE após o fechamento do bloco `if (fupWebhookUrl && this.checkpointer) { ... await this.fupScheduler.start(); ... this.logger.info(...) }` — antes do registro do SIGTERM handler.

**WR-03 — SIGTERM handler como campo privado com cleanup em close():**

1. Adicionar campo privado na classe, APÓS o campo `private readonly mcpSessionTtlMs` (linha ~89):

```typescript
// WR-03: Salvo como campo para remoção em close() — evita acúmulo de listeners em
// chamadas múltiplas de init() (ex: testes ou reinicializações).
private _sigtermHandler: (() => Promise<void>) | null = null;
```

2. Em `init()`, substituir:
```typescript
// ANTES (código atual, linha ~182):
process.on('SIGTERM', async () => {
  this.logger.info({ brainId: this.brain.id }, 'SIGTERM received — shutting down cleanly');
  await this.close();
  process.exit(0);
});
```
Por:
```typescript
// DEPOIS (WR-03):
this._sigtermHandler = async () => {
  this.logger.info({ brainId: this.brain.id }, 'SIGTERM received — shutting down cleanly');
  await this.close();
  process.exit(0);
};
process.on('SIGTERM', this._sigtermHandler);
```

3. Em `close()`, adicionar ao início do método (antes de `if (this.mcpClient)`):
```typescript
// WR-03: Remover SIGTERM handler — evita acúmulo de listeners em reinicializações
if (this._sigtermHandler) {
  process.off('SIGTERM', this._sigtermHandler);
  this._sigtermHandler = null;
}
```

Nenhuma outra mudança em runner.ts.
  </action>
  <verify>
    <automated>cd /root/Brain && grep -n "_sigtermHandler" packages/core/src/runner/runner.ts | head -10</automated>
    <automated>cd /root/Brain && grep -n "process.off" packages/core/src/runner/runner.ts</automated>
    <automated>cd /root/Brain && grep -n "checkpointer unavailable" packages/core/src/runner/runner.ts</automated>
    <automated>cd /root/Brain && bun tsc --noEmit -p packages/core/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - runner.ts contém a linha `private _sigtermHandler: (() => Promise<void>) | null = null;`
    - runner.ts contém `process.off('SIGTERM', this._sigtermHandler)`
    - runner.ts contém `"FupScheduler not started — checkpointer unavailable"`
    - runner.ts contém `else if (fupWebhookUrl && !this.checkpointer)`
    - `bun tsc --noEmit -p packages/core/tsconfig.json` retorna 0 erros
  </acceptance_criteria>
  <done>WR-01 e WR-03 implementados em runner.ts sem erros TypeScript</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 — updatedAt em resetFup() e teste atualizado</name>
  <read_first>
    - packages/core/src/leads/lead-service.ts (método resetFup() linha 132-137)
    - packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts (teste existente que verifica set())
  </read_first>
  <files>packages/core/src/leads/lead-service.ts, packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts</files>
  <behavior>
    - Teste existente "set() recebe { fupNextAt: null, fupStep: 0 } — sem fupEnabled" deve ser atualizado para também verificar que `updatedAt` está presente no set payload e é instância de Date
    - Após adição de updatedAt ao código, `setPayload.updatedAt instanceof Date` deve ser `true`
    - A verificação de ausência de `fupEnabled` e `iaAtivada` deve permanecer
  </behavior>
  <action>
**Em lead-service.ts — método resetFup() (linha ~132):**

Substituir:
```typescript
async resetFup(uniqueId: string): Promise<void> {
  await this.db
    .update(leads)
    .set({ fupNextAt: null, fupStep: 0 })  // D-19: fupEnabled intencionalmente ausente
    .where(eq(leads.uniqueId, uniqueId));
}
```

Por:
```typescript
async resetFup(uniqueId: string): Promise<void> {
  await this.db
    .update(leads)
    // WR-02: updatedAt incluído para consistência com setFullpp() e setIaAtivada().
    // fupEnabled intencionalmente ausente — D-19: lead permanece elegível para novo ciclo.
    .set({ fupNextAt: null, fupStep: 0, updatedAt: new Date() })
    .where(eq(leads.uniqueId, uniqueId));
}
```

**Em lead-service-fup.test.ts — teste "set() recebe { fupNextAt: null, fupStep: 0 }":**

Adicionar ao bloco de expects existente, após `expect(setPayload!.fupStep).toBe(0)`:
```typescript
// WR-02: updatedAt deve estar presente e ser Date
expect(setPayload!.updatedAt).toBeInstanceOf(Date);
```

A verificação de ausência de `fupEnabled` e `iaAtivada` DEVE permanecer intacta.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - lead-service.ts método resetFup() contém `updatedAt: new Date()`
    - lead-service-fup.test.ts contém `expect(setPayload!.updatedAt).toBeInstanceOf(Date)`
    - `bun test packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts` passa com 3 testes GREEN
  </acceptance_criteria>
  <done>WR-02 implementado; test suite lead-service-fup.test.ts passa com updatedAt verificado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SIGTERM handler | Evento de OS — confiável; não há dados externos envolvidos |
| Logger output | Campos estruturados loggados — risco de info disclosure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | logger.warn WR-01 | mitigate | Log contém apenas `brainType` e `hasFupUrl: true` — nunca a URL em si (per D-01/T-22-04 pattern já estabelecido) |
| T-24-02 | Denial of Service | SIGTERM handler leak (WR-03) | mitigate | WR-03 elimina acúmulo de listeners com `process.off()` — resolve potencial MaxListenersExceededWarning em long-running processes |
| T-24-03 | Tampering | resetFup updatedAt | accept | updatedAt é timestamp do servidor (new Date()), não derivado de input externo — sem risco de tampering |
</threat_model>

<verification>
```bash
# 1. Verificar WR-01 presente
grep -n "checkpointer unavailable" packages/core/src/runner/runner.ts

# 2. Verificar WR-02 presente
grep -n "updatedAt: new Date" packages/core/src/leads/lead-service.ts

# 3. Verificar WR-03 campos presentes
grep -n "_sigtermHandler\|process\.off" packages/core/src/runner/runner.ts

# 4. Testes passando
bun test packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts

# 5. TypeScript limpo
bun tsc --noEmit -p packages/core/tsconfig.json
```
</verification>

<success_criteria>
- runner.ts contém `else if (fupWebhookUrl && !this.checkpointer)` com logger.warn (WR-01)
- runner.ts contém `private _sigtermHandler` + `process.off('SIGTERM', this._sigtermHandler)` em close() (WR-03)
- lead-service.ts resetFup() contém `updatedAt: new Date()` (WR-02)
- lead-service-fup.test.ts verifica `updatedAt instanceof Date` — 3 testes GREEN
- `bun tsc --noEmit -p packages/core/tsconfig.json` = 0 erros
</success_criteria>

<output>
After completion, create `.planning/phases/24-tech-debt-cleanup/24-01-SUMMARY.md`
</output>
