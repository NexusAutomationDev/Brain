---
phase: 24-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/fup/fup-scheduler.ts
  - packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Loop de retry em _processFupForLead aguarda 1 segundo entre tentativas fracassadas (WR-04)"
    - "Delay não ocorre após a última tentativa — apenas entre tentativas intermediárias"
    - "Testes existentes ainda passam após adição do delay"
  artifacts:
    - path: "packages/core/src/fup/fup-scheduler.ts"
      provides: "_processFupForLead() com delay de 1s entre retries"
      contains: "setTimeout(r, 1000)"
    - path: "packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts"
      provides: "Teste WR-04 verificando que delay é inserido entre tentativas"
      contains: "WR-04"
  key_links:
    - from: "fup-scheduler.ts _processFupForLead"
      to: "setTimeout delay"
      via: "await new Promise((r) => setTimeout(r, 1000))"
      pattern: "setTimeout.*1000"
---

<objective>
Corrigir WR-04 — adicionar delay de 1 segundo fixo entre tentativas de retry no loop de _processFupForLead() do FupScheduler.

Purpose: Evitar thundering herd de até 30 calls simultâneos ao LLM/webhook (BATCH_SIZE=10 × 3 retries) em cenário de falha. Delay simples de 1s fixo é suficiente para v1.4 (backoff exponencial adiado para FUP-F01).
Output: fup-scheduler.ts com delay entre retries; teste WR-04 adicionado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-tech-debt-cleanup/24-CONTEXT.md

<!-- Arquivos a modificar — lidos obrigatoriamente antes de editar -->
@packages/core/src/fup/fup-scheduler.ts
@packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 — Delay de 1s entre retries em _processFupForLead()</name>
  <read_first>
    - packages/core/src/fup/fup-scheduler.ts (loop de retry em _processFupForLead(), linhas 168-247)
    - packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts (testes existentes — todos devem continuar passando)
  </read_first>
  <files>packages/core/src/fup/fup-scheduler.ts, packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts</files>
  <behavior>
    - Novo teste "WR-04: delay de 1s inserido entre retries": mock de Date.now() ou verificação de chamadas não é necessário — verificar comportamento: com 2 falhas e 1 sucesso, o scheduler deve completar e o resultado final deve ser o de sucesso (não de falha)
    - Teste de regressão: "FUP-08/D-14: após 3 falhas acumuladas, fup_enabled=false" deve continuar passando sem timeout (delay inserido apenas entre tentativas, não após a última)
    - Testes existentes que mockam _generateFupMessage com sucesso na primeira tentativa NÃO devem ser afetados (delay só ocorre em catch)
  </behavior>
  <action>
**Em fup-scheduler.ts — loop de retry em _processFupForLead() (linha ~238-246):**

O bloco `catch` atual é:
```typescript
      } catch (err) {
        lastErr = err;
        // T-22-03: logar apenas uniqueId, nunca conteúdo da mensagem gerada
        this.logger.warn(
          { err, uniqueId: lead.uniqueId, attempt },
          "FupScheduler: tentativa de FUP falhou"
        );
        // D-15: retry simples sem delay (backoff exponencial é FUP-F01)
      }
```

Substituir por:
```typescript
      } catch (err) {
        lastErr = err;
        // T-22-03: logar apenas uniqueId, nunca conteúdo da mensagem gerada
        this.logger.warn(
          { err, uniqueId: lead.uniqueId, attempt },
          "FupScheduler: tentativa de FUP falhou"
        );
        // WR-04: delay de 1s fixo entre tentativas — evita thundering herd de 30 calls
        // simultâneos ao LLM em cenário de falha (BATCH_SIZE=10 × 3 retries).
        // Backoff exponencial adiado para FUP-F01.
        if (attempt < MAX_FUP_ATTEMPTS) {
          await new Promise((r) => setTimeout(r, 1000));
        }
      }
```

A condição `attempt < MAX_FUP_ATTEMPTS` garante que o delay NÃO ocorre após a última tentativa — evita espera desnecessária quando o resultado final é falha.

**Em fup-scheduler.test.ts — adicionar teste WR-04 ao describe "_processFupForLead()":**

Localizar o describe `"FupScheduler._processFupForLead()"` e adicionar ao final:

```typescript
  test("WR-04: com 2 falhas seguidas de sucesso na 3ª tentativa, scheduler completa e chama fetch 3x", async () => {
    const lead = makeLead({ fupStep: 0, intervalsSeconds: [3600, 86400] });
    let callCount = 0;
    const generateSpy = mock(() => {
      callCount++;
      if (callCount < 3) return Promise.reject(new Error(`Falha tentativa ${callCount}`));
      return Promise.resolve("Mensagem de FUP no 3º attempt");
    });

    const fetchMock = mock(async () => new Response(null, { status: 200 }));
    const { scheduler } = makeScheduler({ leads: [lead], fetchMock });

    (scheduler as unknown as { _generateFupMessage: typeof generateSpy })._generateFupMessage = generateSpy;

    // Usar fake timer seria ideal, mas dado o timeout de 2s total, configurar timeout maior:
    // O delay real de 1s × 2 = 2s, por isso o timeout do teste é 5000ms
    await scheduler._processFupForLead(lead);

    // Geração chamada 3x (2 falhas + 1 sucesso)
    expect(generateSpy).toHaveBeenCalledTimes(3);
    // Fetch chamado apenas 1x (somente na tentativa bem-sucedida)
    expect(fetchMock).toHaveBeenCalledTimes(1);
  }, 5000); // timeout explícito para cobrir 2x delay de 1s
```

ATENÇÃO: O timeout de `5000ms` é necessário porque o delay real de 1s × 2 retries = 2s de espera total. O timeout padrão do bun test é 5s, mas especificar explicitamente garante que futuras mudanças no delay não quebrem silenciosamente.
  </action>
  <verify>
    <automated>cd /root/Brain && grep -n "setTimeout.*1000" packages/core/src/fup/fup-scheduler.ts</automated>
    <automated>cd /root/Brain && grep -n "attempt < MAX_FUP_ATTEMPTS" packages/core/src/fup/fup-scheduler.ts</automated>
    <automated>cd /root/Brain && bun test packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - fup-scheduler.ts contém `await new Promise((r) => setTimeout(r, 1000))`
    - fup-scheduler.ts contém `if (attempt < MAX_FUP_ATTEMPTS)` antes do setTimeout
    - fup-scheduler.test.ts contém um teste com "WR-04" no nome
    - `bun test packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts` passa com todos os testes GREEN (incluindo novo WR-04)
    - O teste WR-04 verifica `generateSpy` chamado 3 vezes e `fetchMock` chamado 1 vez
  </acceptance_criteria>
  <done>WR-04 implementado; delay de 1s presente no catch do retry loop; suite de testes completa GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setTimeout delay | Controle de fluxo interno — sem dados externos |
| LLM retry loop | Chamadas externas ao LLM/webhook no retry — risco de flooding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Denial of Service | _processFupForLead retry loop sem delay | mitigate | WR-04 adiciona delay de 1s entre retries — reduz de até 30 calls simultâneos para calls distribuídos no tempo |
| T-24-05 | Denial of Service | Delay fixo 1s pode prolongar processamento do batch | accept | Em pior caso (BATCH_SIZE=10, 3 retries cada) = 20s adicionais por batch; poll interval é 30s — ainda < poll_interval; aceitável para v1.4 |
</threat_model>

<verification>
```bash
# 1. Verificar delay presente
grep -n "setTimeout.*1000" packages/core/src/fup/fup-scheduler.ts

# 2. Verificar guard de última tentativa
grep -n "attempt < MAX_FUP_ATTEMPTS" packages/core/src/fup/fup-scheduler.ts

# 3. Testes passando (incluindo WR-04)
bun test packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts --reporter=verbose

# 4. TypeScript limpo
bun tsc --noEmit -p packages/core/tsconfig.json
```
</verification>

<success_criteria>
- fup-scheduler.ts tem `await new Promise((r) => setTimeout(r, 1000))` no catch do retry, protegido por `if (attempt < MAX_FUP_ATTEMPTS)`
- Teste WR-04 passa no fup-scheduler.test.ts (verifica 2 falhas + 1 sucesso na 3ª tentativa)
- Todos os testes pré-existentes do fup-scheduler continuam GREEN
- `bun tsc --noEmit -p packages/core/tsconfig.json` = 0 erros
</success_criteria>

<output>
After completion, create `.planning/phases/24-tech-debt-cleanup/24-02-SUMMARY.md`
</output>
