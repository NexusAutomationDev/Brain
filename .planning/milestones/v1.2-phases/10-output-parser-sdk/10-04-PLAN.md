---
phase: 10-output-parser-sdk
plan: 04
type: execute
wave: 4
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
  - 10-03-PLAN.md
files_modified:
  - packages/shared/src/errors/index.js
  - packages/shared/src/errors/index.js.map
  - packages/shared/src/types/index.js
  - packages/shared/src/types/index.js.map
  - packages/shared/src/utils/index.js
  - packages/shared/src/utils/index.js.map
  - packages/shared/src/index.js
  - packages/shared/src/index.js.map
  - packages/shared/dist/errors/index.js
  - packages/shared/dist/types/index.js
autonomous: true
requirements:
  - PARSER-01
  - PARSER-02
gap_closure: true

must_haves:
  truths:
    - "BrainOutputValidationError é encontrado pelo bun ao importar @brain-pkg/shared"
    - "bun test packages/core/src/runner/__tests__/brain-runner.test.ts passa com 17 testes (0 falhas)"
  artifacts:
    - path: "packages/shared/src/errors/index.js"
      provides: "AUSENTE — arquivo obsoleto removido"
      contains: "DEVE SER REMOVIDO"
    - path: "packages/shared/dist/errors/index.js"
      provides: "BrainOutputValidationError compiled no dist"
      contains: "BrainOutputValidationError"
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/errors/index.ts"
      via: "export * from './errors/index.js' — bun resolve .js para .ts quando não existe .js"
      pattern: "export.*from.*errors/index"
---

<objective>
Remove os arquivos `.js` e `.js.map` obsoletos de `packages/shared/src/` que foram gerados na Fase 1 e nunca atualizados. Esses arquivos sombreiam os arquivos `.ts` atualizados (Phase 10 Plan 01) quando o bun resolve imports com extensão `.js` no barrel `src/index.ts`. A remoção faz o bun resolver corretamente `./errors/index.js` para `./errors/index.ts`, expondo `BrainOutputValidationError` aos importadores.

Purpose: Corrigir a raiz do gap de resolução de módulo que impede os 17 testes do BrainRunner de passarem. O código de produção está correto — apenas artefatos de build obsoletos bloqueiam a execução.
Output: `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` passa com 17 testes; dist/ do shared reconstruída com o conteúdo correto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-output-parser-sdk/10-VERIFICATION.md

<interfaces>
<!-- Estado atual dos arquivos que causam o gap. -->

packages/shared/src/index.ts (barrel — NÃO modificar):
```typescript
// Barrel export for shared package
export * from './types/index.js';
export * from './utils/index.js';
export * from './errors/index.js';
```

Problema: quando existe `src/errors/index.js`, o bun o carrega no lugar de `src/errors/index.ts`.

packages/shared/src/errors/index.js (STALE — gerado Fase 1, SEM BrainOutputValidationError):
```javascript
export class BrainError extends Error { ... }
export class ConfigurationError extends BrainError { ... }
// BrainOutputValidationError AUSENTE — adicionado ao .ts na Phase 10 Plan 01, mas .js nunca regenerado
```

packages/shared/src/types/index.js (STALE — contém apenas `export {}`):
```javascript
export {};
```

packages/shared/src/utils/index.js (STALE — contém apenas `export {}`):
```javascript
export {};
```

packages/shared/src/index.js (STALE — gerado Fase 1):
```javascript
export * from './types/index.js';
export * from './utils/index.js';
export * from './errors/index.js';
```

packages/shared/src/errors/index.ts (CORRETO — Phase 10 Plan 01 — manter):
```typescript
export class BrainError extends Error { ... }
export class ConfigurationError extends BrainError { ... }
export class BrainOutputValidationError extends BrainError {
  constructor(message: string, context?: Record<string, unknown>) {
    super(message, 'BRAIN_OUTPUT_VALIDATION_ERROR', context);
    this.name = 'BrainOutputValidationError';
  }
}
```

packages/shared/tsconfig.json (build config):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

packages/shared/package.json scripts:
```json
{
  "scripts": {
    "build": "tsc"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover arquivos .js e .js.map obsoletos de packages/shared/src/</name>
  <files>
    packages/shared/src/errors/index.js
    packages/shared/src/errors/index.js.map
    packages/shared/src/types/index.js
    packages/shared/src/types/index.js.map
    packages/shared/src/utils/index.js
    packages/shared/src/utils/index.js.map
    packages/shared/src/index.js
    packages/shared/src/index.js.map
  </files>
  <read_first>
    - packages/shared/src/index.ts (confirmar que usa extensão .js nos exports — não modificar)
    - packages/shared/src/errors/index.ts (confirmar que BrainOutputValidationError está presente no .ts)
    - packages/shared/src/errors/index.js (confirmar conteúdo stale antes de deletar)
  </read_first>

  <action>
Remover todos os arquivos `.js` e `.js.map` dentro de `packages/shared/src/` (não em `dist/`).

Esses arquivos são artefatos de build gerados pela Fase 1 (tsc rodou com outDir incorreto ou sem configuração de rootDir). O `tsconfig.json` do shared usa `"outDir": "./dist"`, então nenhum arquivo compilado deve existir em `src/`.

Executar os seguintes comandos:

```bash
rm /root/Brain/packages/shared/src/errors/index.js
rm /root/Brain/packages/shared/src/errors/index.js.map
rm /root/Brain/packages/shared/src/types/index.js
rm /root/Brain/packages/shared/src/types/index.js.map
rm /root/Brain/packages/shared/src/utils/index.js
rm /root/Brain/packages/shared/src/utils/index.js.map
rm /root/Brain/packages/shared/src/index.js
rm /root/Brain/packages/shared/src/index.js.map
```

Verificar que os arquivos `.ts` correspondentes ainda existem (NÃO remover os .ts):

```bash
ls /root/Brain/packages/shared/src/errors/
ls /root/Brain/packages/shared/src/types/
ls /root/Brain/packages/shared/src/utils/
```

Cada diretório deve conter APENAS `index.ts` (e possivelmente `index.d.ts` / `index.d.ts.map` se foram gerados — esses podem permanecer, são arquivos de declaração TypeScript, não JavaScript compilado).

IMPORTANTE: NÃO modificar `src/index.ts`. O barrel mantém `export * from './errors/index.js'` — quando não existe `index.js`, o bun resolve automaticamente para `index.ts` (comportamento padrão do bun com TypeScript).
  </action>

  <verify>
    <automated>ls /root/Brain/packages/shared/src/errors/ &amp;&amp; ls /root/Brain/packages/shared/src/types/ &amp;&amp; ls /root/Brain/packages/shared/src/utils/ &amp;&amp; ! test -f /root/Brain/packages/shared/src/errors/index.js &amp;&amp; echo "OK: nenhum .js stale encontrado"</automated>
  </verify>

  <acceptance_criteria>
    - `packages/shared/src/errors/index.js` NÃO existe (removido)
    - `packages/shared/src/errors/index.js.map` NÃO existe (removido)
    - `packages/shared/src/types/index.js` NÃO existe (removido)
    - `packages/shared/src/types/index.js.map` NÃO existe (removido)
    - `packages/shared/src/utils/index.js` NÃO existe (removido)
    - `packages/shared/src/utils/index.js.map` NÃO existe (removido)
    - `packages/shared/src/index.js` NÃO existe (removido)
    - `packages/shared/src/index.js.map` NÃO existe (removido)
    - `packages/shared/src/errors/index.ts` EXISTE (não removido)
    - `packages/shared/src/types/index.ts` EXISTE (não removido)
    - `packages/shared/src/utils/index.ts` EXISTE (não removido)
    - `packages/shared/src/index.ts` EXISTE e NÃO foi modificado (ainda tem `export * from './errors/index.js'`)
  </acceptance_criteria>

  <done>Nenhum arquivo .js ou .js.map existe em packages/shared/src/ — apenas arquivos .ts (e opcionalmente .d.ts) permanecem.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruir dist/ do shared e verificar que os 17 testes do runner passam</name>
  <files>
    packages/shared/dist/errors/index.js
    packages/shared/dist/types/index.js
    packages/shared/dist/index.js
  </files>
  <read_first>
    - packages/shared/src/errors/index.ts (confirmar BrainOutputValidationError presente antes de buildar)
    - packages/shared/tsconfig.json (confirmar outDir: ./dist)
    - packages/shared/dist/errors/index.js (ler ANTES do rebuild para comparar depois)
  </read_first>

  <action>
**PASSO 1 — Reconstruir o pacote shared:**

```bash
cd /root/Brain/packages/shared && bun run build
```

O `bun run build` executa `tsc` com `outDir: ./dist`. Isso regenera `dist/errors/index.js` com `BrainOutputValidationError`, `dist/types/index.js` com as interfaces `BrainOutput` e `ResponseMode`, e `dist/index.js` como barrel.

**PASSO 2 — Confirmar que dist/errors/index.js contém BrainOutputValidationError:**

```bash
grep "BrainOutputValidationError" /root/Brain/packages/shared/dist/errors/index.js
```

Deve retornar pelo menos uma linha com `BrainOutputValidationError`.

**PASSO 3 — Confirmar que dist/types/index.js não é mais um `export {}` placeholder:**

```bash
cat /root/Brain/packages/shared/dist/types/index.js
```

Não deve conter apenas `export {}` — deve ter as exportações de `BrainOutput`, `ResponseMode`, etc. (ou ser vazio se tudo for `export type` puro, que é eliminado por tsc mas não é um blocker).

**PASSO 4 — Rodar bun install na raiz para atualizar symlinks (se necessário):**

```bash
cd /root/Brain && bun install
```

Isso garante que os symlinks de workspace apontem para os artefatos corretos.

**PASSO 5 — Rodar os 17 testes do BrainRunner:**

```bash
bun test /root/Brain/packages/core/src/runner/__tests__/brain-runner.test.ts
```

TODOS os 17 testes devem passar. Se algum falhar com erro diferente de `SyntaxError: Export named 'BrainOutputValidationError' not found`, investigar e corrigir antes de prosseguir.

**PASSO 6 — Rodar a suite completa de testes do projeto para detectar regressões:**

```bash
bun test /root/Brain/packages/core/src /root/Brain/packages/shared/src /root/Brain/packages/transport/src /root/Brain/apps/brain-echo/src/__tests__/unit
```

Nenhum teste que estava passando antes (Truths 1-6 do VERIFICATION.md) deve regredir.
  </action>

  <verify>
    <automated>bun test /root/Brain/packages/core/src/runner/__tests__/brain-runner.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `bun run build` em `packages/shared` completa sem erros de TypeScript
    - `grep "BrainOutputValidationError" /root/Brain/packages/shared/dist/errors/index.js` retorna resultado não vazio
    - `bun test /root/Brain/packages/core/src/runner/__tests__/brain-runner.test.ts` reporta `17 pass, 0 fail`
    - Nenhum teste em `packages/transport/src` regride (esperado: 32 pass, 0 fail)
    - Nenhum teste em `apps/brain-echo/src/__tests__/unit` regride (esperado: 10 pass, 0 fail)
    - Nenhum teste em `packages/core/src/__tests__/unit/output` regride (esperado: 9 pass, 0 fail)
    - O arquivo `packages/shared/src/errors/index.js` NÃO foi recriado pelo tsc (tsc usa outDir: ./dist, não ./src)
  </acceptance_criteria>

  <done>dist/ do shared reconstruída com BrainOutputValidationError; todos os 17 testes do BrainRunner passam; nenhuma regressão nos outros 51 testes do projeto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/shared/src/ → bun module resolver | Bun resolve imports .js para .ts apenas quando nenhum .js existe no disco |
| packages/shared/dist/ → workspace consumers | packages/core e transport importam via @brain-pkg/shared que aponta para dist/ em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | tsc build em packages/shared | accept | tsc com outDir: ./dist garante que output vai para dist/ — não pode recriar arquivos em src/. Risco de recriação é zero dado o tsconfig correto |
| T-10-11 | Denial of Service | bun test suite após remoção de .js | mitigate | Task 2 Passo 6 roda suite completa cruzada antes de concluir — qualquer regressão é detectada antes do commit |
</threat_model>

<verification>
Verificação final do gap closure:

1. `! test -f packages/shared/src/errors/index.js && echo "PASS"` — arquivo stale removido
2. `grep "BrainOutputValidationError" packages/shared/dist/errors/index.js` — dist tem o export correto
3. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` — 17 pass, 0 fail (Truths 7 e 8 do VERIFICATION.md)
4. `bun test packages/core/src/__tests__/unit/output` — 9 pass, 0 fail (Truth 1 não regrediu)
5. `bun test packages/transport/src` — 32 pass, 0 fail (Truths 4-6 não regrediram)
6. `bun test apps/brain-echo/src/__tests__/unit` — 10 pass, 0 fail (Truth 4 não regrediu)

**Phase 10 Gate:** Após esse plano, todas as 8 truths do VERIFICATION.md devem estar VERIFIED. Score esperado: 8/8.
</verification>

<success_criteria>
1. `packages/shared/src/` não contém nenhum arquivo `.js` ou `.js.map`
2. `packages/shared/dist/errors/index.js` contém `BrainOutputValidationError`
3. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` passa com 17 testes
4. Nenhuma regressão nos testes de output schema (9), transport (32), brain-echo unit (10)
5. PARSER-02 satisfeito completamente: BrainRunner.run() lança BrainOutputValidationError e os testes provam isso
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-output-parser-sdk/10-04-SUMMARY.md`
</output>
