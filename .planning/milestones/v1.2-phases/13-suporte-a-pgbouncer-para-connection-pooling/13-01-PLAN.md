---
phase: 13-suporte-a-pgbouncer-para-connection-pooling
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/database/src/pool-manager.ts
  - packages/database/src/migrate.ts
  - packages/database/src/migrate.test.ts
  - packages/database/src/pool-manager.test.ts
  - packages/ai/src/graph/checkpointer.ts
autonomous: true
requirements:
  - PGB-01
  - PGB-02
  - PGB-03
  - PGB-05

must_haves:
  truths:
    - "TenantPoolManager.getPool() cria pools com `prepare: false` em todas as instâncias postgres()"
    - "runMigrations() serializa execuções concorrentes via row-lock em _schema_lock (não pg_advisory_lock)"
    - "runMigrations() cria a tabela _schema_lock idempotentemente via DDL direto antes de adquirir lock"
    - "runMigrations() comportamento observável idêntico: CREATE EXTENSION vector + migrate() continuam sendo chamados"
    - "O bloco CLI de migrate.ts (import.meta.main) usa `prepare: false` no postgres()"
    - "createCheckpointer() documenta limitação do PostgresSaver com PgBouncer transaction mode"
  artifacts:
    - path: "packages/database/src/pool-manager.ts"
      provides: "TenantPoolManager com prepare: false"
      contains: "prepare: false"
    - path: "packages/database/src/migrate.ts"
      provides: "runMigrations() com row-lock"
      contains: "_schema_lock"
    - path: "packages/database/src/migrate.test.ts"
      provides: "testes adaptados para row-lock"
      contains: "_schema_lock"
    - path: "packages/database/src/pool-manager.test.ts"
      provides: "testes implementados para prepare: false"
      contains: "prepare"
    - path: "packages/ai/src/graph/checkpointer.ts"
      provides: "documentação de compatibilidade PgBouncer"
      contains: "PgBouncer"
  key_links:
    - from: "packages/database/src/migrate.ts"
      to: "_schema_lock table"
      via: "CREATE TABLE IF NOT EXISTS + FOR UPDATE NOWAIT"
      pattern: "_schema_lock"
    - from: "packages/database/src/pool-manager.ts"
      to: "postgres()"
      via: "prepare: false option"
      pattern: "prepare: false"
---

<objective>
Tornar o `packages/database` e `packages/ai` totalmente compatíveis com PgBouncer:
(1) adicionar `prepare: false` ao TenantPoolManager e ao bloco CLI de migrate.ts,
(2) substituir `pg_advisory_lock` por row-lock transacional em `_schema_lock`,
(3) documentar a limitação do PostgresSaver com PgBouncer transaction mode.

Purpose: Eliminar os dois bloqueios para PgBouncer: prepared statements no postgres.js (incompatíveis com qualquer modo) e advisory lock em runMigrations() (incompatível com transaction mode).
Output: pool-manager.ts, migrate.ts e checkpointer.ts modificados; testes adaptados e implementados passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-CONTEXT.md
@.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces extraídas do codebase para o executor — sem necessidade de explorar. -->

From packages/database/src/pool-manager.ts (arquivo a modificar):
```typescript
// Linha 36-43: bloco a modificar — adicionar prepare: false
pool = postgres({
  ...this.baseConfig,
  database: databaseName,
  max: this.baseConfig.max,
  idle_timeout: this.baseConfig.idle_timeout,
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  onnotice: () => {},
});
```

From packages/database/src/migrate.ts (arquivo a substituir):
```typescript
// Padrão atual (a ser removido):
const MIGRATION_LOCK_KEY = 7316882;
await sql`SELECT pg_advisory_lock(${MIGRATION_LOCK_KEY})`;
// ...
await sql`SELECT pg_advisory_unlock(${MIGRATION_LOCK_KEY})`;

// Bloco CLI (linha 38 — adicionar prepare: false):
const sql = postgres(connectionString, { max: 1 });  // ← adicionar prepare: false
```

From packages/ai/src/graph/checkpointer.ts (apenas doc):
```typescript
export async function createCheckpointer(connectionString: string): Promise<PostgresSaver>
```

From packages/database/src/migrate.test.ts (testes a adaptar):
// Testa pg_advisory_lock — todas as 7 asserções referentes a advisory lock
// precisam ser substituídas por asserções de row-lock (_schema_lock)
// O padrão de mock (mockSql, mockDrizzle, mockMigrate) deve ser MANTIDO

From packages/database/src/pool-manager.test.ts (testes a implementar):
// Tem apenas it.todo() — 7 tests stub sem implementação
// Precisam verificar prepare: false e comportamento do LRU
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: [RED] Adaptar migrate.test.ts e implementar pool-manager.test.ts (Wave 0)</name>
  <files>
    packages/database/src/migrate.test.ts
    packages/database/src/pool-manager.test.ts
  </files>
  <read_first>
    - packages/database/src/migrate.test.ts (estado atual com mocks de pg_advisory_lock — entender estrutura antes de modificar)
    - packages/database/src/pool-manager.test.ts (stubs it.todo() — entender estrutura atual)
    - packages/database/src/pool-manager.ts (interface TenantPoolManager, getPool() — entender o que testar)
    - packages/database/src/migrate.ts (runMigrations() atual — entender o que o teste deve verificar após a mudança)
  </read_first>
  <behavior>
    migrate.test.ts — substituir 7 testes de pg_advisory_lock por testes de row-lock:
    - Test: cria _schema_lock via CREATE TABLE IF NOT EXISTS antes de tentar adquirir lock
    - Test: cria linha id=1 via INSERT ... ON CONFLICT DO NOTHING após CREATE TABLE
    - Test: executa queries DDL (_schema_lock setup) FORA de sql.begin() (não dentro de transação)
    - Test: adquire lock via SELECT id FROM _schema_lock WHERE id = 1 FOR UPDATE NOWAIT dentro de sql.begin()
    - Test: chama migrate() após adquirir o lock (comportamento observável intacto — D-08)
    - Test: cria CREATE EXTENSION IF NOT EXISTS vector (comportamento observável intacto — D-08)
    - Test: em erro de lock (código 55P03), retorna fallback descritivo (não quebra processo)
    - Test: quando migrate() lança erro, o erro é propagado (pg rollback implícito libera lock)

    pool-manager.test.ts — implementar os stubs existentes + adicionar teste PGB-01:
    - Test PGB-01: getPool() cria instância postgres() com opção prepare === false
      (mockar o módulo 'postgres' para capturar o config object passado)
    - Test: cria pool separado por DATABASE_NAME diferente
    - Test: reutiliza pool existente para mesmo DATABASE_NAME (mesma referência)
    - Test: LRU evict: quando maxTenants=1 e segundo tenant é criado, primeiro é evictado

    IMPORTANTE: mock do postgres deve ser declarado ANTES do import (bun hoisting requirement)
    Padrão: mock.module('postgres', () => ({ default: mockPostgresFactory }))
    onde mockPostgresFactory captura o config em um array para asserção posterior.
  </behavior>
  <action>
    PASSO 1 — Adaptar migrate.test.ts:

    Manter a estrutura de mocks (mockSql como tagged template literal mock, mockDrizzle, mockMigrate).
    Adicionar ao mockSql o suporte a sql.begin() — sql.begin() recebe uma callback async e a executa
    passando o próprio mockSql como argumento de transação:

    ```typescript
    // Adicionar ao mockSql mock:
    (mockSql as any).begin = mock(async (fn: (tx: typeof mockSql) => Promise<unknown>) => {
      return fn(mockSql);
    });
    ```

    Substituir o bloco describe 'LEAD-04: pg_advisory_lock advisory locking' por novo describe
    'PGB-02/PGB-03: row-lock via _schema_lock':

    Testes a incluir:
    1. 'cria _schema_lock com CREATE TABLE IF NOT EXISTS antes do sql.begin()' —
       executar runMigrations(), verificar que sqlCalls contém 'CREATE TABLE IF NOT EXISTS _schema_lock'
       antes de qualquer chamada a sql.begin
    2. 'insere row id=1 com ON CONFLICT DO NOTHING antes do sql.begin()' —
       verificar que sqlCalls contém 'INSERT INTO _schema_lock' com 'ON CONFLICT'
    3. 'adquire lock com FOR UPDATE NOWAIT dentro de sql.begin()' —
       verificar que sqlCalls contém 'FOR UPDATE NOWAIT' e que veio de dentro do begin()
    4. 'chama migrate() após adquirir o lock' —
       verificar mockMigrate.mock.calls.length === 1
    5. 'cria CREATE EXTENSION IF NOT EXISTS vector (D-08: comportamento intacto)' —
       verificar sqlCalls contém 'CREATE EXTENSION'
    6. 'propaga erro de migrate() (lock liberado por rollback implícito)' —
       mockMigrate throw → runMigrations() rejects com mesmo erro
    7. 'não usa pg_advisory_lock em nenhum momento' —
       verificar que nenhum sqlCall contém 'pg_advisory_lock'

    Adicionar describe 'PGB-05: prepare: false no bloco CLI' com teste de análise estática:
    8. 'bloco import.meta.main usa postgres() com prepare: false' —
       ler src com readFileSync, verificar regex /postgres\([^)]*prepare:\s*false/

    PASSO 2 — Implementar pool-manager.test.ts:

    Adicionar no topo (antes de qualquer import):
    ```typescript
    import { describe, it, expect, mock, beforeEach } from 'bun:test';

    // Captura o config passado para postgres() factory
    const capturedConfigs: Record<string, unknown>[] = [];
    const mockPostgresFactory = mock((config: Record<string, unknown>) => {
      capturedConfigs.push({ ...config });
      return {
        end: mock(async () => {}),
      };
    });
    mock.module('postgres', () => ({ default: mockPostgresFactory }));

    import { TenantPoolManager } from './pool-manager.js';
    ```

    Substituir os it.todo() por implementações reais:

    describe 'DB-03: Multi-tenant connection pooling' → 3 tests:
    1. 'creates separate pool per DATABASE_NAME' — mgr.getPool('db1'), mgr.getPool('db2'),
       verificar mockPostgresFactory.mock.calls.length === 2
    2. 'reuses existing pool for same DATABASE_NAME' — mgr.getPool('db1'), mgr.getPool('db1'),
       verificar mockPostgresFactory.mock.calls.length === 1
    3. 'isolates queries between different tenant databases' — mgr.getPool('db1') !== mgr.getPool('db2')
       (referências diferentes)

    describe 'DB-04: LRU cache eviction' → 1 test real implementado:
    4. 'evicts least-recently-used pool when exceeding maxTenants (PGB-01 verify)' —
       const mgr = new TenantPoolManager(baseConfig, 1); mgr.getPool('db1'); mgr.getPool('db2');
       verificar que mockPostgresFactory foi chamado 2 vezes (db2 forçou evict de db1)

    Adicionar describe 'PGB-01: prepare: false sempre ativo':
    5. 'getPool() sempre passa prepare: false para postgres()' —
       new TenantPoolManager(baseConfig); manager.getPool('testdb');
       verificar capturedConfigs[ultimo].prepare === false
    6. 'prepare: false presente mesmo sem PgBouncer configurado' —
       same check — garante que não é condicional

    beforeEach: resetar capturedConfigs e mockPostgresFactory.mockClear()
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/migrate.test.ts packages/database/src/pool-manager.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Os testes falham com mensagens como "expected ... to contain '_schema_lock'" e
    "expected prepare to be false" (RED phase confirmada — testes escritos contra comportamento
    ainda não implementado). Nenhum syntax error nos arquivos de teste.
  </done>
  <acceptance_criteria>
    - packages/database/src/migrate.test.ts NÃO contém 'pg_advisory_lock' (grep -c 'pg_advisory_lock' packages/database/src/migrate.test.ts retorna 0)
    - packages/database/src/migrate.test.ts contém '_schema_lock' (grep -c '_schema_lock' packages/database/src/migrate.test.ts retorna >= 3)
    - packages/database/src/migrate.test.ts contém 'FOR UPDATE NOWAIT' (grep retorna >= 1)
    - packages/database/src/pool-manager.test.ts contém 'prepare' (grep retorna >= 2)
    - packages/database/src/pool-manager.test.ts NÃO contém 'it.todo' (todos os stubs implementados)
    - bun test retorna falhas (não syntax errors) — confirma RED phase
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: [GREEN] Implementar prepare:false + row-lock + doc checkpointer</name>
  <files>
    packages/database/src/pool-manager.ts
    packages/database/src/migrate.ts
    packages/ai/src/graph/checkpointer.ts
  </files>
  <read_first>
    - packages/database/src/pool-manager.ts (estado atual — adicionar prepare: false no getPool())
    - packages/database/src/migrate.ts (estado atual — substituir advisory lock por row-lock)
    - packages/ai/src/graph/checkpointer.ts (estado atual — adicionar doc de compatibilidade PgBouncer)
    - packages/database/src/migrate.test.ts (testes recém-escritos — guia do comportamento esperado)
    - packages/database/src/pool-manager.test.ts (testes recém-escritos — guia do comportamento esperado)
    - .planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-RESEARCH.md (padrões e pitfalls)
  </read_first>
  <behavior>
    pool-manager.ts:
    - getPool() deve criar postgres() com prepare: false — sempre, sem condicional

    migrate.ts:
    - runMigrations() deve criar _schema_lock com DDL idempotente fora de begin()
    - runMigrations() deve adquirir row-lock com FOR UPDATE NOWAIT dentro de sql.begin()
    - migrate() e CREATE EXTENSION devem ser chamados dentro do begin() usando o tx
    - Em erro de lock (code 55P03), deve fazer retry até 3 vezes com sleep de 200ms; após 3 falhas, lança erro descritivo
    - Bloco import.meta.main: postgres() com prepare: false

    checkpointer.ts:
    - Adicionar JSDoc documentando incompatibilidade com PgBouncer transaction mode
  </behavior>
  <action>
    ARQUIVO 1 — packages/database/src/pool-manager.ts:

    No método getPool(), no bloco `if (!pool)`, modificar a chamada postgres() adicionando `prepare: false`:

    ```typescript
    pool = postgres({
      ...this.baseConfig,
      database: databaseName,
      max: this.baseConfig.max,
      idle_timeout: this.baseConfig.idle_timeout,
      prepare: false,  // D-01, D-12: sempre ativo — compatível com PgBouncer em qualquer modo
      // eslint-disable-next-line @typescript-eslint/no-empty-function
      onnotice: () => {},
    });
    ```

    ARQUIVO 2 — packages/database/src/migrate.ts:

    Remover a constante MIGRATION_LOCK_KEY e toda a lógica de pg_advisory_lock.

    Adicionar função helper `isLockNotAvailable`:
    ```typescript
    function isLockNotAvailable(err: unknown): boolean {
      return (
        typeof err === 'object' &&
        err !== null &&
        'code' in err &&
        (err as { code: string }).code === '55P03' // lock_not_available (PostgreSQL)
      );
    }
    ```

    Adicionar função helper `sleep`:
    ```typescript
    function sleep(ms: number): Promise<void> {
      return new Promise(resolve => setTimeout(resolve, ms));
    }
    ```

    Substituir runMigrations() completo por:
    ```typescript
    export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void> {
      // D-07: Criar tabela de lock idempotentemente — DDL fora de transação (Pitfall 4 do RESEARCH.md)
      // CREATE TABLE IF NOT EXISTS é idempotente e seguro sem transação
      await sql`CREATE TABLE IF NOT EXISTS _schema_lock (id INTEGER PRIMARY KEY, locked_at TIMESTAMPTZ)`;
      await sql`INSERT INTO _schema_lock (id, locked_at) VALUES (1, NOW()) ON CONFLICT (id) DO NOTHING`;

      // D-06: Row-lock transaction-scoped — compatível com PgBouncer transaction mode
      // Retry até 3 tentativas com 200ms de sleep entre elas (D-14: comportamento desejável)
      const MAX_RETRIES = 3;
      let attempt = 0;

      while (attempt < MAX_RETRIES) {
        try {
          await sql.begin(async (tx) => {
            // FOR UPDATE NOWAIT: lança erro código 55P03 se outra instância tem o lock
            await tx`SELECT id FROM _schema_lock WHERE id = 1 FOR UPDATE NOWAIT`;

            console.log('[migrate] Row-lock adquirido — iniciando migrations');

            // D-08: Comportamento observável idêntico — CREATE EXTENSION + migrate()
            const db = drizzle(tx as unknown as Sql);
            await tx`CREATE EXTENSION IF NOT EXISTS vector`;
            await migrate(db, { migrationsFolder });

            console.log('[migrate] Migrations concluídas com sucesso');
            // Lock liberado automaticamente ao fim da transação
          });
          return; // Sucesso — sair do loop
        } catch (err: unknown) {
          if (isLockNotAvailable(err) && attempt < MAX_RETRIES - 1) {
            attempt++;
            console.log(`[migrate] Lock não disponível — tentativa ${attempt}/${MAX_RETRIES}, aguardando 200ms`);
            await sleep(200);
            continue;
          }
          if (isLockNotAvailable(err)) {
            throw new Error(
              `[migrate] Não foi possível adquirir lock de migrations após ${MAX_RETRIES} tentativas. ` +
              'Outra instância pode estar executando migrations. Reinicie a aplicação.'
            );
          }
          throw err; // Outros erros (ex: migrate() falhou) — propagar imediatamente
        }
      }
    }
    ```

    No bloco `if (import.meta.main)`, modificar a linha de criação do sql:
    ```typescript
    const sql = postgres(connectionString, { max: 1, prepare: false }); // D-01: PgBouncer-compatible
    ```

    Manter o resto do bloco CLI inalterado.

    ARQUIVO 3 — packages/ai/src/graph/checkpointer.ts:

    Substituir o JSDoc existente da função createCheckpointer() por:
    ```typescript
    /**
     * AI-01: Creates and configures a PostgresSaver checkpointer.
     *
     * PostgresSaver uses `pg` (node-postgres) internally — NOT postgres.js.
     * This is intentional: do not attempt to share the postgres.js Sql instance.
     * Both drivers coexist against the same database without conflict.
     *
     * IMPORTANT: setup() is called here so callers do not need to call it separately.
     * Call this once at application startup, not per-request.
     *
     * AI-01 constraint: MemorySaver is NEVER used in production code.
     * MemorySaver is only permitted in *.test.ts files for unit tests without PostgreSQL.
     *
     * PgBouncer compatibility (Phase 13 — D-05):
     * PostgresSaver uses the `pg` (node-postgres v8.21) driver internally, which uses
     * the extended query protocol (prepared statements) for all parameterized queries.
     * There is no pool-level option to disable this in pg v8.21.
     *
     * Supported PgBouncer configurations:
     * - Session mode: fully compatible (recommended)
     * - Transaction mode + PgBouncer >= 1.21 with max_prepared_statements > 0: compatible
     * - Transaction mode + PgBouncer < 1.21: NOT supported — use session mode instead
     * - Direct PostgreSQL (no PgBouncer): always compatible
     *
     * @param connectionString - PostgreSQL connection string (use TEST_DATABASE_URL for tests)
     */
    ```

    NOTA sobre drizzle(tx): O `tx` passado pelo `sql.begin()` no postgres.js é do tipo `TransactionSql`
    (subset de `Sql`) — compatível com `drizzle()` do `drizzle-orm/postgres-js`. O cast
    `tx as unknown as Sql` é necessário pois os tipos diferem em propriedades internas,
    mas a interface de execução de queries é idêntica. Verificar se o TypeScript aceita; se não,
    manter o cast.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/migrate.test.ts packages/database/src/pool-manager.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Todos os testes em migrate.test.ts e pool-manager.test.ts passam (GREEN phase).
    Nenhum teste em packages/database/ regride.
    checkpointer.ts contém o bloco de documentação PgBouncer.
  </done>
  <acceptance_criteria>
    - bun test packages/database/src/ retorna 0 falhas (todos os testes passam)
    - grep -c 'prepare: false' packages/database/src/pool-manager.ts retorna >= 1
    - grep -c 'prepare: false' packages/database/src/migrate.ts retorna >= 1 (bloco CLI)
    - grep -c '_schema_lock' packages/database/src/migrate.ts retorna >= 3
    - grep -c 'FOR UPDATE NOWAIT' packages/database/src/migrate.ts retorna >= 1
    - grep -c 'pg_advisory_lock' packages/database/src/migrate.ts retorna 0
    - grep -c 'PgBouncer' packages/ai/src/graph/checkpointer.ts retorna >= 3
    - grep -c 'session mode' packages/ai/src/graph/checkpointer.ts retorna >= 1
    - bun test packages/database/src/ packages/ai/src/ retorna 0 falhas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → PgBouncer → PostgreSQL | Conexões de banco passam por proxy — lock mechanisms devem ser proxy-aware |
| runMigrations() → _schema_lock table | DDL e row-lock em tabela de controle — integridade crítica para safety das migrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | runMigrations() | mitigate | Row-lock `FOR UPDATE NOWAIT` dentro de `sql.begin()` garante exclusão mútua — uma única instância executa migrations por vez; rollback implícito no erro libera o lock |
| T-13-01-02 | Information Disclosure | migrate.ts / pool-manager.ts | mitigate | `DATABASE_URL` nunca é logada — apenas mensagens de status sem credenciais; padrão já existente mantido |
| T-13-01-03 | Denial of Service | runMigrations() retry loop | accept | Retry máximo de 3 tentativas com 200ms sleep — loop encerra com erro descritivo após 3 falhas; risco de lock starvation é mínimo em cenários reais de deploy |
| T-13-01-04 | Tampering | _schema_lock DDL | mitigate | `CREATE TABLE IF NOT EXISTS` executado fora de transação (Pitfall 4 do RESEARCH.md) — DDL autocommit garante que a tabela existe antes de qualquer lock attempt; idempotente em todos os cenários |
</threat_model>

<verification>
Verificação geral do Plan 01:

```bash
# Todos os testes de database passam
cd /root/Brain && bun test packages/database/src/

# pg_advisory_lock foi completamente removido de migrate.ts
grep -c 'pg_advisory_lock' packages/database/src/migrate.ts  # deve retornar 0

# prepare: false presente em ambos os locais
grep -n 'prepare: false' packages/database/src/pool-manager.ts packages/database/src/migrate.ts

# _schema_lock presente em migrate.ts e no teste
grep -c '_schema_lock' packages/database/src/migrate.ts packages/database/src/migrate.test.ts

# Documentação PgBouncer presente em checkpointer.ts
grep -c 'PgBouncer' packages/ai/src/graph/checkpointer.ts
```
</verification>

<success_criteria>
- bun test packages/database/src/ — 0 falhas, todos os testes passam (incluindo 8+ novos testes de row-lock e 6+ testes de pool-manager implementados)
- migrate.ts: zero ocorrências de pg_advisory_lock; _schema_lock presente; FOR UPDATE NOWAIT presente; prepare: false no bloco CLI
- pool-manager.ts: prepare: false presente no getPool()
- checkpointer.ts: bloco de documentação PgBouncer com session mode vs transaction mode documentado
- migrate.test.ts: zero referências a pg_advisory_lock; testes de _schema_lock e FOR UPDATE NOWAIT presentes
- pool-manager.test.ts: todos os it.todo() substituídos por implementações reais; teste PGB-01 (prepare: false) presente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-01-SUMMARY.md`
</output>
