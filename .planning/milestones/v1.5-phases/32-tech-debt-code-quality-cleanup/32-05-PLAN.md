---
phase: 32-tech-debt-code-quality-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md
  - .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md
  - .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md
  - .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md
autonomous: true
requirements: [TECH-06]
must_haves:
  truths:
    - "An automated frontmatter-only scan across all v1.5 SUMMARY.md files can discover which plan satisfied every TECH-* and SUP-* requirement, without needing to read VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md"
      provides: "requirements-completed: [TECH-02] in frontmatter"
    - path: ".planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md"
      provides: "requirements-completed: [TECH-03] in frontmatter"
    - path: ".planning/phases/29-brain-suporte-core/29-01-SUMMARY.md"
      provides: "requirements-completed: [SUP-01, SUP-03, SUP-04, SUP-05, SUP-07] in frontmatter"
    - path: ".planning/phases/29-brain-suporte-core/29-02-SUMMARY.md"
      provides: "requirements-completed: [SUP-01, SUP-08] in frontmatter"
  key_links:
    - from: ".planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md frontmatter"
      to: ".planning/v1.5-MILESTONE-AUDIT.md requirements coverage table"
      via: "requirements-completed field cross-referenced against the audit's TECH-02 row"
      pattern: "requirements-completed"
---

<objective>
Backfill the missing `requirements-completed` frontmatter field on the 4 v1.5 SUMMARY.md files the milestone audit flagged as undiscoverable via frontmatter-only scan (`27-02-SUMMARY.md`, `27-03-SUMMARY.md`, `29-01-SUMMARY.md`, `29-02-SUMMARY.md`) — a purely mechanical, no-ambiguity fix per CONTEXT.md D-14.

Purpose: `.planning/v1.5-MILESTONE-AUDIT.md`'s requirements coverage table (3-source cross-reference: `REQUIREMENTS.md`, phase `VERIFICATION.md`, plan `SUMMARY.md` frontmatter) found that TECH-02, TECH-03, SUP-01, SUP-03, SUP-04, SUP-05, SUP-07, SUP-08 all show "satisfied" only via `VERIFICATION.md`'s direct evidence — none of them are discoverable by scanning `SUMMARY.md` frontmatter alone, because these 4 plans never populated the `requirements-completed` field other plans in the milestone consistently used. This is purely a documentation-completeness fix (the requirements were already verified as satisfied by the audit — this task does not re-verify or change that conclusion), closing the gap so a future automated frontmatter scan produces accurate coverage numbers without needing to fall back to VERIFICATION.md.
Output: All 4 SUMMARY.md files gain a `requirements-completed` frontmatter field listing the correct requirement IDs, cross-referenced against `.planning/v1.5-MILESTONE-AUDIT.md`'s requirements coverage table and each plan's actual delivered scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tech-debt-code-quality-cleanup/32-CONTEXT.md
@.planning/v1.5-MILESTONE-AUDIT.md
</context>

<interfaces>
<!-- Audit's requirements coverage table (.planning/v1.5-MILESTONE-AUDIT.md) — the authoritative
     source for WHICH requirement IDs map to WHICH phase (not which specific plan within the
     phase — that attribution is derived below from each plan's actual delivered scope, since
     the audit table is phase-level, not plan-level): -->
```
| Requirement | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
| TECH-02 | 27 | passed/SATISFIED | missing (27-02) | [x] | satisfied (doc gap) |
| TECH-03 | 27 | passed/SATISFIED | missing (27-03) | [x] | satisfied (doc gap) |
| SUP-01  | 29 | passed/SATISFIED | missing (29-01/02) | [x] | satisfied (doc gap) |
| SUP-03  | 29 | passed/SATISFIED (override D-01/D-02) | missing | [x] (override) | satisfied |
| SUP-04  | 29 | passed/SATISFIED | missing | [x] | satisfied (doc gap) |
| SUP-05  | 29 | passed/SATISFIED | missing | [x] | satisfied (doc gap) |
| SUP-07  | 29 | passed/SATISFIED | missing | [x] | satisfied (doc gap) |
| SUP-08  | 29 | passed/SATISFIED | missing | [x] | satisfied (doc gap; also cosmetic naming — see tech debt) |
```

<!-- Per-plan attribution derived from each plan's actual delivered scope (read during phase
     32 planning from 27-02-SUMMARY.md, 27-03-SUMMARY.md, 29-01-SUMMARY.md, 29-02-SUMMARY.md,
     and apps/brain-support/src/{brain.ts,index.ts} source): -->

**27-02-SUMMARY.md** — created `packages/core/src/__tests__/integration/fup-e2e.test.ts`,
the FupScheduler E2E integration test against real PostgreSQL. This is the entire deliverable
for **TECH-02** ("Existe teste de integração E2E do FUP rodando contra banco PostgreSQL real").
No other requirement touched by this plan.

**27-03-SUMMARY.md** — added `TransportStatus`/`ITransport.getStatus()`, expanded
`HealthCheckResult`/`performHealthCheck()`, wired transport status into `GET /health`. This is
the entire deliverable for **TECH-03** ("GET /health retorna status do transport"). No other
requirement touched by this plan.

**29-01-SUMMARY.md** — created `apps/brain-support` scaffold: `brain.ts` (supportBrain IBrain,
`pause_session`/`finish_conversation` as native closures per D-01/D-02, `search_knowledge`
always-on via post-filter append, `BrainOutput` structured return via the `respond` node),
`index.ts` (wires `createTransport()` — webhook/RabbitMQ selectable via `TRANSPORT` ENV,
instantiates `BrainRunner` with `supportBrain` — `BrainRunner.run()`'s `ia_ativada` gate is
inherited core behavior, but this plan is what FIRST WIRES it for brain-support), `server.ts`.
Delivers: **SUP-01** (webhook/RabbitMQ transport wiring via index.ts), **SUP-03** (native
closures for pause_session/finish_conversation, no qualify_lead equivalent — the D-01/D-02
reinterpretation this plan implements), **SUP-04** (embeddingProvider instantiation in
index.ts, independent ENV-driven config), **SUP-05** (BrainOutput structured output via the
`respond` node in brain.ts), **SUP-07** (ia_ativada gate + PostgresSaver history — wired via
BrainRunner instantiation in index.ts, first activation for brain-support).

**29-02-SUMMARY.md** — created migration 0010 (seed prompt), `.env.example`,
`toolsregistry-support.test.ts` (proves `ToolsRegistry` correctly configured for `"support"`
brainType via `enableTool()`), `server.test.ts` (proves transport mounting). Delivers:
**SUP-01** (server.test.ts proves webhook mounting works — joint contribution with 29-01 per
the audit's "missing (29-01/02)" notation), **SUP-08** (`toolsregistry-support.test.ts` is the
concrete test evidence that `ToolsRegistry` registration works for brainType `"support"` —
this plan's test suite is what proves SUP-08, even though the `enableTool()` CALL SITE lives
in 29-01's `index.ts`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backfill requirements-completed frontmatter on all 4 SUMMARY.md files</name>
  <files>.planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md, .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md, .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md, .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md</files>
  <read_first>
    .planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md
    .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md
    .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md
    .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md
    .planning/v1.5-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Add a `requirements-completed` field to each of the 4 files' YAML frontmatter, inserted
    immediately after the `phase`/`plan` fields (before `subsystem`) to match the field
    ordering convention already used in later v1.5 plans that DO have this field populated
    (check any Phase 28 or Phase 31 SUMMARY.md for the exact key placement/format precedent
    if one exists — otherwise insert directly after `plan:`).

    **27-02-SUMMARY.md** — current frontmatter starts:
    ```yaml
    ---
    phase: 27-tech-debt-fixes
    plan: "02"
    subsystem: packages/core
    ```
    Insert `requirements-completed: [TECH-02]` after `plan: "02"`:
    ```yaml
    ---
    phase: 27-tech-debt-fixes
    plan: "02"
    requirements-completed: [TECH-02]
    subsystem: packages/core
    ```

    **27-03-SUMMARY.md** — current frontmatter starts:
    ```yaml
    ---
    phase: 27-tech-debt-fixes
    plan: "03"
    subsystem: transport,observability,brain-sdr
    ```
    Insert `requirements-completed: [TECH-03]` after `plan: "03"`:
    ```yaml
    ---
    phase: 27-tech-debt-fixes
    plan: "03"
    requirements-completed: [TECH-03]
    subsystem: transport,observability,brain-sdr
    ```

    **29-01-SUMMARY.md** — current frontmatter starts:
    ```yaml
    ---
    phase: 29-brain-suporte-core
    plan: 01
    subsystem: brain-support
    ```
    Insert `requirements-completed: [SUP-01, SUP-03, SUP-04, SUP-05, SUP-07]` after `plan: 01`:
    ```yaml
    ---
    phase: 29-brain-suporte-core
    plan: 01
    requirements-completed: [SUP-01, SUP-03, SUP-04, SUP-05, SUP-07]
    subsystem: brain-support
    ```

    **29-02-SUMMARY.md** — current frontmatter starts:
    ```yaml
    ---
    phase: 29-brain-suporte-core
    plan: 02
    subsystem: brain-support
    ```
    Insert `requirements-completed: [SUP-01, SUP-08]` after `plan: 02`:
    ```yaml
    ---
    phase: 29-brain-suporte-core
    plan: 02
    requirements-completed: [SUP-01, SUP-08]
    subsystem: brain-support
    ```

    Do not modify any other frontmatter field or any body content in these 4 files — this is
    an additive, frontmatter-only change. Preserve exact existing YAML formatting/quoting style
    per file (e.g. `27-02-SUMMARY.md` quotes `plan: "02"` with double quotes — match that file's
    own quoting convention for consistency, don't impose a different style).
  </action>
  <verify>
    <automated>grep -rn "requirements-completed" .planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "requirements-completed: \[TECH-02\]" .planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md` returns a match
    - `grep -n "requirements-completed: \[TECH-03\]" .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md` returns a match
    - `grep -n "requirements-completed: \[SUP-01, SUP-03, SUP-04, SUP-05, SUP-07\]" .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md` returns a match
    - `grep -n "requirements-completed: \[SUP-01, SUP-08\]" .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md` returns a match
    - All 4 files remain valid YAML frontmatter (parseable — no broken syntax introduced)
    - No body content (anything after the closing `---`) differs from the pre-task version of each file
  </acceptance_criteria>
  <done>All 4 SUMMARY.md files have a requirements-completed frontmatter field populated with the correct requirement IDs, closing the milestone audit's "doc gap" finding for TECH-02, TECH-03, SUP-01, SUP-03, SUP-04, SUP-05, SUP-07, SUP-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new attack surface — this plan modifies only planning documentation frontmatter (`.planning/*.md` files), which are never read by production code, never served over HTTP, and never processed by the LLM at runtime.

## STRIDE Threat Register

Not applicable — documentation-only change with no code path, no external input, no runtime reachability.
</threat_model>

<verification>
```bash
grep -n "requirements-completed" .planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md .planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md .planning/phases/29-brain-suporte-core/29-01-SUMMARY.md .planning/phases/29-brain-suporte-core/29-02-SUMMARY.md
```
All 4 files must show a match.
</verification>

<success_criteria>
- All 4 SUMMARY.md files have requirements-completed populated
- Values match the audit's coverage table and each plan's actual delivered scope
- No other frontmatter or body content changed
</success_criteria>

<output>
After completion, create `.planning/phases/32-tech-debt-code-quality-cleanup/32-05-SUMMARY.md`
</output>
