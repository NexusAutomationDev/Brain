---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/database/src/schema/tables.test.ts
  - packages/database/src/pool-manager.test.ts
  - packages/database/src/migrate.test.ts
  - packages/observability/src/logger.test.ts
  - packages/observability/src/health.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test files exist for all database and observability modules"
    - "Each test file contains stubs/scaffolds for requirement verification"
    - "Tests can be executed via bun test (even if not implemented yet)"
  artifacts:
    - path: "packages/database/src/schema/tables.test.ts"
      provides: "Test stubs for schema validation (DB-01, DB-02)"
      min_lines: 20
    - path: "packages/database/src/pool-manager.test.ts"
      provides: "Test stubs for pool manager (DB-03, DB-04)"
      min_lines: 20
    - path: "packages/database/src/migrate.test.ts"
      provides: "Test stubs for migration script (DB-05)"
      min_lines: 15
    - path: "packages/observability/src/logger.test.ts"
      provides: "Test stubs for logger (OBS-01)"
      min_lines: 15
    - path: "packages/observability/src/health.test.ts"
      provides: "Test stubs for health check (OBS-02)"
      min_lines: 15
  key_links:
    - from: "test files"
      to: "bun test"
      via: "describe/it/expect syntax"
      pattern: "describe\\(.*it\\(.*expect\\("
---

<objective>
Create test file scaffolds for all database and observability modules to satisfy Nyquist compliance. These stubs allow automated verification commands to execute without errors before implementations exist.

Purpose: Enable continuous feedback loops during Phase 1 execution by ensuring test infrastructure exists from Wave 0.

Output: Executable test files with pending stubs for all requirements (DB-01 through DB-06, OBS-01, OBS-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database schema test stubs (DB-01, DB-02)</name>
  <files>
    packages/database/src/schema/tables.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
    .planning/REQUIREMENTS.md (DB-01, DB-02)
  </read_first>
  <action>
Create test scaffold for schema validation:

**packages/database/src/schema/tables.test.ts:**
```typescript
import { describe, it, expect } from 'bun:test';

describe('Schema Tables (DB-01, DB-02)', () => {
  describe('DB-01: Schema defines all required tables', () => {
    it.todo('exports users table with uuid id and externalId unique constraint');
    it.todo('exports memories table with userId and key index');
    it.todo('exports agentState table with threadId unique constraint');
    it.todo('exports embeddings table with vector column');
  });

  describe('DB-02: PGVector configuration', () => {
    it.todo('embeddings table reads EMBEDDING_DIMENSIONS from env');
    it.todo('embeddings table defaults to 1536 if env not set');
    it.todo('embeddings table has HNSW index with m=16, ef_construction=64');
  });
});
```

This creates executable stubs that can be run with `bun test` — they will show as "pending" until implemented.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/schema/tables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at packages/database/src/schema/tables.test.ts
    - Contains describe blocks for DB-01 and DB-02
    - Uses it.todo() for pending test stubs
    - Executes without errors via bun test
    - Shows tests as "pending" (not failed)
  </acceptance_criteria>
  <done>Schema test stubs created; ready for implementation in later plans</done>
</task>

<task type="auto">
  <name>Task 2: Create pool manager test stubs (DB-03, DB-04)</name>
  <files>
    packages/database/src/pool-manager.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
    .planning/REQUIREMENTS.md (DB-03, DB-04)
  </read_first>
  <action>
Create test scaffold for pool manager:

**packages/database/src/pool-manager.test.ts:**
```typescript
import { describe, it, expect } from 'bun:test';

describe('TenantPoolManager (DB-03, DB-04)', () => {
  describe('DB-03: Multi-tenant connection pooling', () => {
    it.todo('creates separate pool per DATABASE_NAME');
    it.todo('reuses existing pool for same DATABASE_NAME');
    it.todo('isolates queries between different tenant databases');
  });

  describe('DB-04: LRU cache eviction', () => {
    it.todo('maintains up to 20 tenant pools in cache');
    it.todo('evicts least-recently-used pool when exceeding maxTenants');
    it.todo('calls dispose callback with pool cleanup on eviction');
    it.todo('recreates pool for evicted tenant on next access');
  });
});
```

This creates executable stubs for pool manager verification.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/pool-manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at packages/database/src/pool-manager.test.ts
    - Contains describe blocks for DB-03 and DB-04
    - Uses it.todo() for pending test stubs
    - Executes without errors via bun test
    - Shows tests as "pending" (not failed)
  </acceptance_criteria>
  <done>Pool manager test stubs created; ready for implementation in 01-02b</done>
</task>

<task type="auto">
  <name>Task 3: Create migration script test stubs (DB-05)</name>
  <files>
    packages/database/src/migrate.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
    .planning/REQUIREMENTS.md (DB-05)
  </read_first>
  <action>
Create test scaffold for migration script:

**packages/database/src/migrate.test.ts:**
```typescript
import { describe, it, expect } from 'bun:test';

describe('Migration Script (DB-05)', () => {
  describe('DB-05: Migration behavior', () => {
    it.todo('creates PGVector extension before migrations');
    it.todo('applies migrations from src/migrations folder');
    it.todo('exits 0 on successful migration');
    it.todo('exits 1 if DATABASE_URL not set');
    it.todo('exits 1 on migration failure');
  });
});
```

This creates executable stubs for migration verification.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/migrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at packages/database/src/migrate.test.ts
    - Contains describe block for DB-05
    - Uses it.todo() for pending test stubs
    - Executes without errors via bun test
    - Shows tests as "pending" (not failed)
  </acceptance_criteria>
  <done>Migration script test stubs created; ready for implementation in 01-02b</done>
</task>

<task type="auto">
  <name>Task 4: Create logger test stubs (OBS-01)</name>
  <files>
    packages/observability/src/logger.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
    .planning/REQUIREMENTS.md (OBS-01)
  </read_first>
  <action>
Create test scaffold for logger:

**packages/observability/src/logger.test.ts:**
```typescript
import { describe, it, expect } from 'bun:test';

describe('Logger (OBS-01)', () => {
  describe('OBS-01: Structured logging with context', () => {
    it.todo('emits JSON with timestamp, level, and message');
    it.todo('injects tenantId from LogContext into all log lines');
    it.todo('injects brainId from LogContext into all log lines');
    it.todo('reads LOG_LEVEL from env (defaults to info)');
    it.todo('includes env (NODE_ENV) in base field');
  });
});
```

This creates executable stubs for logger verification.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/observability/src/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at packages/observability/src/logger.test.ts
    - Contains describe block for OBS-01
    - Uses it.todo() for pending test stubs
    - Executes without errors via bun test
    - Shows tests as "pending" (not failed)
  </acceptance_criteria>
  <done>Logger test stubs created; ready for implementation in 01-03</done>
</task>

<task type="auto">
  <name>Task 5: Create health check test stubs (OBS-02)</name>
  <files>
    packages/observability/src/health.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
    .planning/REQUIREMENTS.md (OBS-02)
  </read_first>
  <action>
Create test scaffold for health check:

**packages/observability/src/health.test.ts:**
```typescript
import { describe, it, expect } from 'bun:test';

describe('Health Check (OBS-02)', () => {
  describe('OBS-02: Health check endpoint behavior', () => {
    it.todo('returns status "ok" when database is connected');
    it.todo('returns status "error" when database is failed');
    it.todo('includes checks.db: "connected" when SELECT 1 succeeds');
    it.todo('includes checks.db: "failed" when SELECT 1 fails');
    it.todo('includes version field (GIT_COMMIT or "unknown")');
    it.todo('includes ISO timestamp');
  });
});
```

This creates executable stubs for health check verification.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/observability/src/health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists at packages/observability/src/health.test.ts
    - Contains describe block for OBS-02
    - Uses it.todo() for pending test stubs
    - Executes without errors via bun test
    - Shows tests as "pending" (not failed)
  </acceptance_criteria>
  <done>Health check test stubs created; ready for implementation in 01-03</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Test files import production modules; no security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | N/A | Test scaffolds | accept | Wave 0 creates only test stubs with no production impact; no threats apply |

</threat_model>

<verification>
After completing all tasks:
1. Run `bun test` in packages/database — should show pending tests
2. Run `bun test` in packages/observability — should show pending tests
3. Verify no test failures (only pending)
4. Verify all test files are executable
</verification>

<success_criteria>
- ✅ All 5 test files created (schema, pool-manager, migrate, logger, health)
- ✅ Each test file uses it.todo() for pending stubs
- ✅ All tests execute without errors via bun test
- ✅ VALIDATION.md Wave 0 gaps resolved
- ✅ Nyquist compliance achieved (automated verify commands can run)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md` with:
- Test files created and their purpose
- Coverage of requirements (DB-01 through DB-06, OBS-01, OBS-02)
- Confirmation that bun test runs successfully
- Ready state for implementation plans (01-01, 01-02, 01-02b, 01-03)
</output>
