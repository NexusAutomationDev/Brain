---
phase: 04-validation-brain
plan: "01"
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - apps/brain-echo/src/brain.ts
  - apps/brain-echo/src/server.ts
  - apps/brain-echo/src/index.ts
  - apps/brain-echo/src/__tests__/unit/brain.test.ts
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "EchoBrain implementa IBrain com id='brain-echo', brainType='echo', promptKeys=['system'], tools=[]"
    - "buildGraph() cria nó 'llm' que usa ctx.prompts['system'] como system prompt e chama ctx.llm.invoke()"
    - "buildGraph() retorna StateGraph NÃO compilado"
    - "server.ts monta 3 sub-apps Hono (health, webhook, core) em único app"
    - "index.ts executa startup sequencial: runMigrations → runner.init() → Bun.serve"
    - "Qualquer falha antes do Bun.serve causa process.exit(1)"
    - "Testes unitários do EchoBrain passam (IBrain contract verificado)"
  artifacts:
    - path: "apps/brain-echo/src/brain.ts"
      provides: "echoBrain: IBrain implementation"
      exports: ["echoBrain"]
    - path: "apps/brain-echo/src/server.ts"
      provides: "createServer(sql, runner): Hono"
      exports: ["createServer"]
    - path: "apps/brain-echo/src/index.ts"
      provides: "main() entrypoint sequencial"
    - path: "apps/brain-echo/src/__tests__/unit/brain.test.ts"
      provides: "testes completos do contrato IBrain"
  key_links:
    - from: "apps/brain-echo/src/index.ts"
      to: "packages/database/src/migrate.ts"
      via: "import { runMigrations } from '@brain-pkg/database'"
      pattern: "runMigrations"
    - from: "apps/brain-echo/src/index.ts"
      to: "packages/core/src/runner/runner.ts"
      via: "import { BrainRunner } from '@brain-pkg/core'"
      pattern: "runner.init"
    - from: "apps/brain-echo/src/brain.ts"
      to: "packages/core/src/brain/interface.ts"
      via: "implements IBrain"
      pattern: "IBrain"
    - from: "apps/brain-echo/src/server.ts"
      to: "packages/observability/src/server.ts"
      via: "createHealthApp(sql)"
      pattern: "createHealthApp"
---

<objective>
Implementar o Echo Brain completo: a implementação IBrain (brain.ts), o servidor Hono
com 3 sub-apps compostos (server.ts), e o entrypoint sequencial com startup fail-fast (index.ts).
Completar os testes unitários do EchoBrain.

Purpose: Este é o coração da Phase 4 — o EchoBrain exercita o SDK end-to-end e prova
que o contrato IBrain funciona com uma implementação real que usa LLM real + prompts do DB.
Output: Brain implementado + servidor montado + entrypoint pronto para o Dockerfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation-brain/04-CONTEXT.md
@.planning/phases/04-validation-brain/04-RESEARCH.md
@.planning/phases/04-validation-brain/04-00-SUMMARY.md

<interfaces>
<!-- Interfaces extraídas do codebase — executor usa diretamente sem explorar -->

De packages/core/src/brain/interface.ts:
```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
}

export interface IBrain {
  id: string;
  brainType: string;
  promptKeys: string[];
  tools: StructuredTool[];
  buildGraph(ctx: BrainBuildContext): StateGraph<typeof BrainStateAnnotation>;
}
```

De packages/core/src/index.ts:
```typescript
export type { IBrain, BrainBuildContext } from "./brain/interface.js";
export { BrainRegistry } from "./brain/registry.js";
export { BrainRunner } from "./runner/runner.js";
export type { BrainRunnerOptions, BrainRunResult } from "./runner/runner.js";
export { ToolsRegistry } from "./tools/registry.js";
export { loadPrompts } from "./prompts/loader.js";
```

De packages/core/src/runner/runner.ts:
```typescript
export interface BrainRunnerOptions {
  brain: IBrain;
  sql: Sql;
  toolsRegistry: ToolsRegistry;
  llmOptions?: LLMOptions;
}
// Lifecycle: new BrainRunner(options) → await runner.init() → runner.run(event)
// runner.init() chama process.exit(1) se promptKey faltando
```

De packages/observability/src/server.ts:
```typescript
export function createHealthApp(sql: Sql): Hono
```

De packages/transport/src/webhook/handler.ts:
```typescript
// createWebhookApp aceita runner opcional via duck typing IBrainRunnerLike
export function createWebhookApp(runner?: IBrainRunnerLike): Hono
// onde IBrainRunnerLike = { run(event: BrainEvent): Promise<{ reply: string }> }
```

De packages/core/src/server.ts:
```typescript
export function createCoreApp(runner: BrainRunner): Hono
// POST /reload-prompts — requer X-Admin-Token header
```

De packages/database/src/index.ts (após plano 04-00):
```typescript
export { runMigrations } from './migrate.js';
// runMigrations(sql: Sql, migrationsFolder: string): Promise<void>
```

De packages/observability/src/index.ts:
```typescript
export { createLogger } from './logger.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar EchoBrain (brain.ts) + completar testes unitários</name>
  <read_first>
    - apps/brain-echo/src/__tests__/unit/brain.test.ts (stub atual — ver o que precisa ser implementado)
    - packages/core/src/brain/interface.ts (IBrain e BrainBuildContext — contrato a implementar)
    - packages/ai/src/graph/state.ts (BrainStateAnnotation — tipo do state)
    - .planning/phases/04-validation-brain/04-CONTEXT.md (D-01, D-02, D-03 — decisões locked)
    - .planning/phases/04-validation-brain/04-RESEARCH.md (Pattern 2: EchoBrain IBrain implementation)
  </read_first>
  <files>
    apps/brain-echo/src/brain.ts,
    apps/brain-echo/src/__tests__/unit/brain.test.ts
  </files>
  <behavior>
    - Test 1: echoBrain.id === "brain-echo"
    - Test 2: echoBrain.brainType === "echo"
    - Test 3: echoBrain.promptKeys é igual a ["system"] (exatamente um elemento)
    - Test 4: echoBrain.tools é array vazio (length === 0)
    - Test 5: echoBrain.buildGraph(ctx) retorna objeto com propriedade addNode (é um StateGraph)
    - Test 6: buildGraph NÃO chama .compile() — retorna StateGraph não compilado
  </behavior>
  <action>
**Passo RED — Atualizar apps/brain-echo/src/__tests__/unit/brain.test.ts:**

Substituir o stub pelos testes reais que vão falhar antes da implementação:

```typescript
import { describe, test, expect, mock } from "bun:test";

// Importar echoBrain — vai falhar até brain.ts existir
// (importação dinâmica para evitar erro de parse antes do teste rodar)
let echoBrain: any;

describe("EchoBrain — IBrain contract", () => {
  test("placeholder: arquivo existe e é parseável", () => {
    expect(true).toBe(true);
  });

  test("echoBrain.id é 'brain-echo'", async () => {
    const mod = await import("../../brain.js");
    echoBrain = mod.echoBrain;
    expect(echoBrain.id).toBe("brain-echo");
  });

  test("echoBrain.brainType é 'echo'", async () => {
    const mod = await import("../../brain.js");
    expect(mod.echoBrain.brainType).toBe("echo");
  });

  test("echoBrain.promptKeys contém ['system']", async () => {
    const mod = await import("../../brain.js");
    expect(mod.echoBrain.promptKeys).toEqual(["system"]);
  });

  test("echoBrain.tools é array vazio", async () => {
    const mod = await import("../../brain.js");
    expect(mod.echoBrain.tools).toEqual([]);
  });

  test("buildGraph(ctx) retorna StateGraph (tem método addNode)", async () => {
    const mod = await import("../../brain.js");
    // Mock mínimo de BrainBuildContext
    const ctx = {
      llm: { invoke: mock(async () => ({ content: "ok" })) },
      prompts: { system: "Você é um assistente útil." },
      tools: [],
    };
    const graph = mod.echoBrain.buildGraph(ctx as any);
    // StateGraph retorna objeto com métodos de grafo — não deve ser null
    expect(graph).toBeTruthy();
    expect(typeof graph.addNode).toBe("function");
    expect(typeof graph.compile).toBe("function");
  });
});
```

**Passo GREEN — Criar apps/brain-echo/src/brain.ts:**

Implementar o EchoBrain exatamente conforme D-01, D-02, D-03 do CONTEXT.md e Pattern 2 do RESEARCH.md:

```typescript
// D-01: EchoBrain usa LLM real + system prompt carregado da tabela `prompts`
// D-02: tools = [] — sem tools na fase de validação
// D-03: buildGraph() retorna StateGraph NÃO compilado — BrainRunner chama .compile({ checkpointer })

import { StateGraph } from "@langchain/langgraph";
import { BrainStateAnnotation } from "@brain-pkg/ai";
import type { IBrain, BrainBuildContext } from "@brain-pkg/core";

export const echoBrain: IBrain = {
  id: "brain-echo",
  brainType: "echo",
  promptKeys: ["system"],
  tools: [],
  buildGraph(ctx: BrainBuildContext) {
    const graph = new StateGraph(BrainStateAnnotation);

    graph.addNode("llm", async (state) => {
      // D-01: system prompt carregado de ctx.prompts['system'] (nunca hardcoded)
      // D-03: ctx.llm já configurado pelo BrainRunner — não criar LLM aqui
      const response = await ctx.llm.invoke([
        { role: "system", content: ctx.prompts["system"] },
        ...state.messages,
      ]);
      return { messages: [...state.messages, response] };
    });

    graph.addEdge("__start__", "llm");
    graph.addEdge("llm", "__end__");

    // D-03: NUNCA chamar .compile() aqui — BrainRunner é o responsável
    return graph;
  },
};
```
  </action>
  <verify>
    bun test /root/Brain/apps/brain-echo/src/__tests__/unit/brain.test.ts
  </verify>
  <acceptance_criteria>
    - apps/brain-echo/src/brain.ts existe e contém `export const echoBrain: IBrain`
    - apps/brain-echo/src/brain.ts contém `id: "brain-echo"` e `brainType: "echo"`
    - apps/brain-echo/src/brain.ts contém `promptKeys: ["system"]` e `tools: []`
    - apps/brain-echo/src/brain.ts contém `ctx.prompts["system"]` (nunca string hardcoded de prompt)
    - apps/brain-echo/src/brain.ts NÃO contém `.compile(` (compilação é do BrainRunner)
    - `bun test apps/brain-echo/src/__tests__/unit/brain.test.ts` sai com código 0
    - Todos os 6 testes passam (incluindo buildGraph retorna StateGraph com addNode)
  </acceptance_criteria>
  <done>echoBrain implementado e todos os testes unitários de contrato IBrain passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar server.ts + index.ts (entrypoint sequencial)</name>
  <read_first>
    - apps/brain-echo/src/brain.ts (criado na task anterior — exporta echoBrain)
    - packages/observability/src/server.ts (createHealthApp signature)
    - packages/transport/src/webhook/handler.ts (createWebhookApp signature)
    - packages/core/src/server.ts (createCoreApp signature)
    - .planning/phases/04-validation-brain/04-CONTEXT.md (D-04, D-05 — assembly e startup)
    - .planning/phases/04-validation-brain/04-RESEARCH.md (Pattern 3: entrypoint + Pattern 4 nota sobre migrationsDir)
  </read_first>
  <files>
    apps/brain-echo/src/server.ts,
    apps/brain-echo/src/index.ts
  </files>
  <action>
**1. Criar apps/brain-echo/src/server.ts**

Montar o Hono app composto com os 3 sub-apps (D-04). A função recebe sql e runner como parâmetros para injeção de dependência (testabilidade):

```typescript
// D-04: Único Hono app montando 3 sub-apps via app.route('/')
// - createHealthApp(sql)  → GET /health
// - createWebhookApp(runner) → POST /api/v1/webhook
// - createCoreApp(runner)   → POST /reload-prompts

import { Hono } from "hono";
import { createHealthApp } from "@brain-pkg/observability";
import { createWebhookApp } from "@brain-pkg/transport";
import { createCoreApp, type BrainRunner } from "@brain-pkg/core";
import type { Sql } from "postgres";

/**
 * Cria o Hono app principal do Echo Brain.
 * Monta os 3 sub-apps em um único app via app.route('/').
 *
 * @param sql - postgres.js Sql instance para health check
 * @param runner - BrainRunner já inicializado para processar eventos
 */
export function createServer(sql: Sql, runner: BrainRunner): Hono {
  const app = new Hono();

  // Montar sub-apps — todos na rota raiz '/'
  app.route("/", createHealthApp(sql));
  app.route("/", createWebhookApp(runner));
  app.route("/", createCoreApp(runner));

  return app;
}
```

**2. Criar apps/brain-echo/src/index.ts**

Entrypoint com startup sequencial (D-05). O path das migrations é resolvido via import.meta.url
para funcionar corretamente tanto em desenvolvimento quanto no container Docker:

```typescript
// D-05: Startup sequencial:
// 1. runMigrations(sql) — aplica migrations + seed (exit 1 se falhar)
// 2. runner.init()     — carrega prompts + compila graph (exit 1 se prompt faltando)
// 3. Bun.serve(...)    — só sobe após os passos anteriores com sucesso

import postgres from "postgres";
import { runMigrations } from "@brain-pkg/database";
import { BrainRunner, ToolsRegistry } from "@brain-pkg/core";
import { createLogger } from "@brain-pkg/observability";
import { fileURLToPath } from "url";
import { join, dirname } from "path";
import { createServer } from "./server.js";
import { echoBrain } from "./brain.js";

const logger = createLogger();

async function main() {
  const connectionString = process.env.DATABASE_URL;
  if (!connectionString) {
    logger.error({}, "DATABASE_URL not set");
    process.exit(1);
  }

  const sql = postgres(connectionString, { max: 10, idle_timeout: 300 });

  // Passo 1: Migrations + seed de prompts
  // Path resolvido em relação ao arquivo compilado para funcionar no Docker
  // O Dockerfile copia migrations para /app/migrations/ (ver plano 04-02)
  // Em desenvolvimento, as migrations ficam em packages/database/src/migrations/
  const migrationsDir = process.env.MIGRATIONS_DIR
    ?? join(dirname(fileURLToPath(import.meta.url)), "../../migrations");

  await runMigrations(sql, migrationsDir).catch((err) => {
    logger.error({ err }, "Migrations failed — aborting startup");
    process.exit(1);
  });
  logger.info({}, "Migrations completed");

  // Passo 2: Inicializa BrainRunner
  // runner.init() chama process.exit(1) internamente se promptKey 'system' não existir no DB
  const toolsRegistry = new ToolsRegistry();
  const runner = new BrainRunner({ brain: echoBrain, sql, toolsRegistry });
  await runner.init();
  logger.info({}, "BrainRunner initialized");

  // Passo 3: Montar Hono app + iniciar servidor
  const app = createServer(sql, runner);
  const port = parseInt(process.env.PORT || "3000", 10);

  Bun.serve({ port, fetch: app.fetch });
  logger.info({ port }, "brain-echo server listening");
}

main();
```

**Nota sobre MIGRATIONS_DIR:** A env var `MIGRATIONS_DIR` serve como escape hatch para o Dockerfile
especificar o path absoluto `/app/migrations` sem depender de `import.meta.url` funcionando
corretamente com `fileURLToPath` no container. O Dockerfile (plano 04-02) deve incluir:
`ENV MIGRATIONS_DIR=/app/migrations`
  </action>
  <verify>
    grep -n "createServer" /root/Brain/apps/brain-echo/src/server.ts
    grep -n "createHealthApp\|createWebhookApp\|createCoreApp" /root/Brain/apps/brain-echo/src/server.ts
    grep -n "runMigrations\|runner.init\|Bun.serve" /root/Brain/apps/brain-echo/src/index.ts
    grep -n "process.exit(1)" /root/Brain/apps/brain-echo/src/index.ts
  </verify>
  <acceptance_criteria>
    - apps/brain-echo/src/server.ts contém `export function createServer(sql: Sql, runner: BrainRunner): Hono`
    - apps/brain-echo/src/server.ts contém `app.route("/", createHealthApp(sql))`
    - apps/brain-echo/src/server.ts contém `app.route("/", createWebhookApp(runner))`
    - apps/brain-echo/src/server.ts contém `app.route("/", createCoreApp(runner))`
    - apps/brain-echo/src/index.ts contém `await runMigrations(sql, migrationsDir)`
    - apps/brain-echo/src/index.ts contém `await runner.init()`
    - apps/brain-echo/src/index.ts contém `Bun.serve(`
    - apps/brain-echo/src/index.ts contém `process.exit(1)` no bloco catch de migrations
    - apps/brain-echo/src/index.ts NÃO cria conexão com `bun:sql` (usa postgres(connectionString))
    - `bun test apps/brain-echo/src/__tests__/unit` sai 0 (testes anteriores continuam passando)
  </acceptance_criteria>
  <done>server.ts monta 3 sub-apps corretamente; index.ts executa startup sequencial com exit 1 em falha de migrations; estrutura da app completa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → createWebhookApp | Entrada não confiável; Zod valida BrainEvent antes de processar |
| LLM response → state.messages | Resposta do LLM adicionada ao estado sem execução de código |
| prompts DB → ctx.prompts["system"] | Prompt vem do banco — não executa código, usado só como string de contexto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01-01 | Tampering | createWebhookApp (handler.ts) | mitigate | BrainEventSchema.safeParse() já implementado em packages/transport; apps/brain-echo usa createWebhookApp diretamente — proteção herdada sem código adicional |
| T-4-01-02 | Information Disclosure | index.ts startup logs | mitigate | createLogger (Pino) — index.ts NUNCA loga o valor de DATABASE_URL, apenas loga `{}`  ou `{ err }` sem conexão string; verificado na acceptance_criteria |
| T-4-01-03 | Denial of Service | Bun.serve startup | accept | Container reiniciará via orchestrador (Docker); sem rate limiting nesta fase (v1 validation only) |
| T-4-01-04 | Elevation of Privilege | POST /reload-prompts | mitigate | createCoreApp já implementa X-Admin-Token auth com fail-closed (503 se ADMIN_TOKEN não configurado); EchoBrain herda essa proteção via createCoreApp |
</threat_model>

<verification>
- `bun test apps/brain-echo/src/__tests__/unit` sai 0
- `grep "IBrain" apps/brain-echo/src/brain.ts` retorna a importação do tipo
- `grep "\.compile(" apps/brain-echo/src/brain.ts` NÃO retorna nada (proibido no buildGraph)
- `grep "process.exit(1)" apps/brain-echo/src/index.ts` retorna pelo menos 2 ocorrências (DB_URL check + migration catch)
- `grep "createHealthApp\|createWebhookApp\|createCoreApp" apps/brain-echo/src/server.ts` retorna 3 linhas
</verification>

<success_criteria>
- echoBrain implementa IBrain com todos os campos corretos
- buildGraph retorna StateGraph não compilado (testado em unit test)
- server.ts monta 3 sub-apps em único Hono app
- index.ts sequencia migrations → runner.init → Bun.serve com exit 1 em qualquer falha
- Testes unitários passam: `bun test apps/brain-echo/src/__tests__/unit` sai 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-validation-brain/04-01-SUMMARY.md` com:
- Arquivos criados
- Resultado do bun test unit (saída)
- Confirmação de que brain.ts não chama .compile()
- Quaisquer ajustes feitos ao padrão do RESEARCH.md
</output>
