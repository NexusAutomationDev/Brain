---
phase: 07-leadservice-rabbitmq-transport
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/transport/src/rabbitmq/consumer.ts
  - packages/transport/src/rabbitmq/consumer.test.ts
  - packages/transport/src/factory.ts
  - packages/transport/src/factory.test.ts
  - packages/transport/src/index.ts
  - packages/transport/package.json
  - apps/brain-echo/.env.example
autonomous: true
requirements:
  - TRP-03
  - TRP-04
  - TRP-05
  - TRP-06

must_haves:
  truths:
    - "TRANSPORT=rabbitmq inicia consumer que processa mensagens {Name, Message, Numero, IDLead}"
    - "Mensagem processada com sucesso recebe ACK manual"
    - "Após 3 falhas consecutivas, mensagem é publicada na DLQ e ACK enviado (sem loop infinito)"
    - "Payload inválido (campo faltando) vai direto para DLQ sem retry"
    - "RabbitMQTransport.start() lança ConfigurationError se RABBITMQ_URL, RABBITMQ_QUEUE ou RABBITMQ_DLQ ausentes"
    - "Reconexão automática gerenciada pela biblioteca rabbitmq-client (auto-reconnect built-in)"
    - "createTransport('rabbitmq') retorna instância de RabbitMQTransport"
  artifacts:
    - path: "packages/transport/src/rabbitmq/consumer.ts"
      provides: "RabbitMQTransport class implementando ITransport"
      exports: ["RabbitMQTransport"]
    - path: "packages/transport/src/rabbitmq/consumer.test.ts"
      provides: "Testes unitários TRP-03, TRP-04, TRP-05 com mock rabbitmq-client"
      contains: "RabbitMQTransport"
    - path: "packages/transport/src/factory.ts"
      provides: "case 'rabbitmq' no switch de TRANSPORT ENV"
      contains: "case \"rabbitmq\""
    - path: "packages/transport/src/factory.test.ts"
      provides: "Teste TRP-06: createTransport('rabbitmq') retorna RabbitMQTransport"
      contains: "rabbitmq"
    - path: "apps/brain-echo/.env.example"
      provides: "ENVs RabbitMQ documentadas"
      contains: "RABBITMQ_URL"
  key_links:
    - from: "packages/transport/src/factory.ts createTransport()"
      to: "packages/transport/src/rabbitmq/consumer.ts RabbitMQTransport"
      via: "case 'rabbitmq': return new RabbitMQTransport(runner)"
      pattern: "case \"rabbitmq\""
    - from: "packages/transport/src/rabbitmq/consumer.ts handler callback"
      to: "runner.run(parsed.data)"
      via: "ConsumerStatus.ACK após sucesso"
      pattern: "ConsumerStatus\\.ACK"
    - from: "packages/transport/src/rabbitmq/consumer.ts após 3 falhas"
      to: "DLQ via this.pub.send(dlq, msg.body)"
      via: "attempt >= MAX_ATTEMPTS → publish + ACK"
      pattern: "MAX_ATTEMPTS"
---

<objective>
Implementa o RabbitMQTransport com consumer ack manual, retry com backoff, publicação explícita na DLQ após 3 falhas e reconexão automática (TRP-03, TRP-04, TRP-05). Atualiza factory.ts com case "rabbitmq" (TRP-06).

Purpose: Permitir que o Brain receba mensagens via RabbitMQ além do webhook existente — seleção por ENV sem recompilação. Consumer é resiliente: não perde mensagens em falhas transitórias e não entra em loop infinito em falhas permanentes.

Output: RabbitMQTransport em packages/transport/src/rabbitmq/consumer.ts, factory.ts atualizado, testes unitários cobrindo TRP-03/TRP-04/TRP-05/TRP-06, rabbitmq-client instalado em packages/transport, .env.example do brain-echo atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-leadservice-rabbitmq-transport/07-CONTEXT.md
@.planning/phases/07-leadservice-rabbitmq-transport/07-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces e contratos que o executor precisa. Extraídos do codebase. -->

De packages/transport/src/interface.ts (ITransport — NÃO muda):
```typescript
export interface ITransport {
  start(port?: number): Promise<void>;
  stop(): Promise<void>;
}
```

De packages/transport/src/webhook/handler.ts (IBrainRunnerLike — já atualizada em Plan 01):
```typescript
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{ reply: string } | null>;
}
```

De packages/transport/src/webhook/events.ts (BrainEventSchema — reutilizar):
```typescript
export const BrainEventSchema = z.object({
  Name: z.string().min(1, "Name is required"),
  Message: z.string().min(1, "Message is required"),
  Numero: z.string().min(1, "Numero is required"),
  IDLead: z.string().min(1, "IDLead is required"),
});
export type BrainEvent = z.infer<typeof BrainEventSchema>;
```

De packages/transport/src/factory.ts (padrão existente a estender):
```typescript
import { ConfigurationError } from "@brain-pkg/shared";
import { WebhookTransport } from "./webhook/handler.js";
import type { IBrainRunnerLike } from "./webhook/handler.js";
import type { ITransport } from "./interface.js";

export function createTransport(runner?: IBrainRunnerLike): ITransport {
  const type = process.env.TRANSPORT ?? "webhook";
  switch (type) {
    case "webhook":
      return new WebhookTransport(runner);
    default:
      throw new ConfigurationError(`Unknown TRANSPORT: ${type}`, { transport: type });
  }
}
```

API rabbitmq-client@5.0.8 (verificada via GitHub source):
```typescript
import { Connection, ConsumerStatus } from "rabbitmq-client";

// ConsumerStatus enum:
// ACK = 0   → BasicAck (mensagem processada)
// REQUEUE = 1 → BasicNack(requeue=true) — coloca de volta na fila
// DROP = 2  → BasicNack(requeue=false) — descarta sem DLQ

const rabbit = new Connection("amqp://user:pass@host:5672");
rabbit.on("error", (err) => {});   // connection-level error
rabbit.on("connection", () => {}); // connected/reconnected

// Publisher para DLQ (confirm:true = aguarda confirmação do broker)
const pub = rabbit.createPublisher({ confirm: true });
await pub.send("fila-dlq", { payload: "objeto js" });

// Consumer com prefetch=1 e sem requeue automático
const sub = rabbit.createConsumer(
  {
    queue: "brain-events",
    qos: { prefetchCount: 1 },
    requeue: false,  // CRÍTICO: false previne requeue automático em exceção
    // NÃO passar queueOptions — não declarar fila (D-14)
  },
  async (msg) => {
    // msg.body: payload desserializado (objeto JS, não Buffer)
    // msg.deliveryTag: bigint — muda após REQUEUE
    return ConsumerStatus.ACK; // ou REQUEUE ou DROP
  }
);
sub.on("error", (err) => {});

await sub.close();
await pub.close();
await rabbit.close();
```

PITFALL CRÍTICO (RESEARCH.md): deliveryTag muda após REQUEUE.
Solução adotada (Opção A): usar `${IDLead}:${Numero}` como chave do retry counter.
Isso preserva o contador mesmo após REQUEUE e reconexão.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task W0: Instalar rabbitmq-client e criar stubs de teste para RabbitMQTransport</name>
  <files>
    packages/transport/package.json
    packages/transport/src/rabbitmq/consumer.test.ts
    packages/transport/src/factory.test.ts
  </files>
  <read_first>
    - packages/transport/package.json (versão atual — adicionar rabbitmq-client)
    - packages/transport/src/factory.test.ts (arquivo existente — adicionar case "rabbitmq" sem remover testes existentes)
    - packages/transport/src/webhook/handler.ts (IBrainRunnerLike — mockRunner deve satisfazer a interface atualizada)
  </read_first>
  <behavior>
    consumer.test.ts (stubs RED — falham porque consumer.ts não existe ainda):
    - Test TRP-04: start() lança ConfigurationError quando RABBITMQ_URL ausente
    - Test TRP-04: start() lança ConfigurationError quando RABBITMQ_QUEUE ausente
    - Test TRP-04: start() lança ConfigurationError quando RABBITMQ_DLQ ausente
    - Test TRP-03: handler processa mensagem válida e chama runner.run()
    - Test TRP-03: payload inválido (sem IDLead) vai para DLQ sem chamar runner.run()
    - Test TRP-05: após 3 falhas, mensagem é publicada na DLQ (pub.send chamado)
    - Test TRP-05: após 3 falhas, runner.run() não é chamado uma 4ª vez

    factory.test.ts (adicionar ao describe existente):
    - Test TRP-06: createTransport() com TRANSPORT=rabbitmq retorna instância de RabbitMQTransport
    (Atualizar também o teste existente "createTransport with unknown TRANSPORT env throws ConfigurationError" — remover "rabbitmq" da expectativa pois agora é válido)
  </behavior>
  <action>
    1. Instalar dependência em packages/transport:
    ```bash
    cd /root/Brain/packages/transport && bun add rabbitmq-client@^5.0.8
    ```

    2. Criar diretório packages/transport/src/rabbitmq/ se não existir.

    3. Criar packages/transport/src/rabbitmq/consumer.test.ts:

    ```typescript
    // TRP-03, TRP-04, TRP-05: RabbitMQTransport — consumer com ack manual, retry e DLQ
    import { describe, it, expect, mock, beforeEach, afterEach } from "bun:test";
    import { ConfigurationError } from "@brain-pkg/shared";

    // Mock rabbitmq-client ANTES do import do consumer
    const mockHandlerFn = mock(async (_msg: unknown) => 0); // retorna ConsumerStatus.ACK=0
    const mockSubClose = mock(async () => {});
    const mockSubOn = mock((_event: string, _fn: unknown) => {});
    const mockSub = { close: mockSubClose, on: mockSubOn };

    const mockPubSend = mock(async (_queue: string, _body: unknown) => {});
    const mockPubClose = mock(async () => {});
    const mockPub = { send: mockPubSend, close: mockPubClose };

    const mockRabbitOn = mock((_event: string, _fn: unknown) => {});
    const mockRabbitClose = mock(async () => {});
    const mockCreateConsumer = mock((_opts: unknown, fn: unknown) => {
      // Guardar o handler para chamá-lo nos testes
      mockHandlerFn.mockImplementation(fn as typeof mockHandlerFn);
      return mockSub;
    });
    const mockCreatePublisher = mock((_opts?: unknown) => mockPub);

    const MockConnection = mock(function (_url: string) {
      return {
        on: mockRabbitOn,
        close: mockRabbitClose,
        createConsumer: mockCreateConsumer,
        createPublisher: mockCreatePublisher,
      };
    });

    mock.module("rabbitmq-client", () => ({
      Connection: MockConnection,
      ConsumerStatus: { ACK: 0, REQUEUE: 1, DROP: 2 },
    }));

    import { RabbitMQTransport } from "./consumer.js";

    const mockRunner = {
      run: mock(async (_event: unknown) => ({ reply: "ok" })),
    };

    function makeMsg(body: unknown, deliveryTag = BigInt(1)) {
      return { body, deliveryTag, redelivered: false };
    }

    const validBody = {
      Name: "João",
      Message: "Olá",
      Numero: "5511999990001",
      IDLead: "lead-abc",
    };

    describe("RabbitMQTransport (TRP-03, TRP-04, TRP-05)", () => {
      const originalEnv = { ...process.env };

      beforeEach(() => {
        MockConnection.mockClear();
        mockRabbitOn.mockClear();
        mockCreateConsumer.mockClear();
        mockCreatePublisher.mockClear();
        mockPubSend.mockClear();
        mockSubClose.mockClear();
        (mockRunner.run as ReturnType<typeof mock>).mockClear();

        // Ambiente padrão válido
        process.env.RABBITMQ_URL = "amqp://guest:guest@localhost:5672";
        process.env.RABBITMQ_QUEUE = "brain-events";
        process.env.RABBITMQ_DLQ = "brain-dlq";
        process.env.RABBITMQ_RETRY_DELAY_MS = "0"; // sem delay nos testes
      });

      afterEach(() => {
        // Restaurar env
        for (const key of ["RABBITMQ_URL", "RABBITMQ_QUEUE", "RABBITMQ_DLQ", "RABBITMQ_RETRY_DELAY_MS"]) {
          if (originalEnv[key] === undefined) {
            delete process.env[key];
          } else {
            process.env[key] = originalEnv[key];
          }
        }
      });

      // TRP-04: ENVs obrigatórias
      it("start() lança ConfigurationError quando RABBITMQ_URL ausente (TRP-04)", async () => {
        delete process.env.RABBITMQ_URL;
        const transport = new RabbitMQTransport(mockRunner);
        await expect(transport.start()).rejects.toThrow(ConfigurationError);
      });

      it("start() lança ConfigurationError quando RABBITMQ_QUEUE ausente (TRP-04)", async () => {
        delete process.env.RABBITMQ_QUEUE;
        const transport = new RabbitMQTransport(mockRunner);
        await expect(transport.start()).rejects.toThrow(ConfigurationError);
      });

      it("start() lança ConfigurationError quando RABBITMQ_DLQ ausente (TRP-04)", async () => {
        delete process.env.RABBITMQ_DLQ;
        const transport = new RabbitMQTransport(mockRunner);
        await expect(transport.start()).rejects.toThrow(ConfigurationError);
      });

      // TRP-03: processamento de mensagem válida
      it("mensagem válida chama runner.run() e retorna ACK (TRP-03)", async () => {
        const transport = new RabbitMQTransport(mockRunner);
        await transport.start();

        // Simular chegada de mensagem chamando o handler diretamente
        const handler = mockCreateConsumer.mock.calls[0]?.[1] as ((msg: unknown) => Promise<number>);
        const status = await handler(makeMsg(validBody));

        expect(mockRunner.run).toHaveBeenCalledTimes(1);
        expect(mockRunner.run).toHaveBeenCalledWith(validBody);
        expect(status).toBe(0); // ConsumerStatus.ACK
      });

      // TRP-03: payload inválido → DLQ imediato, sem runner
      it("payload inválido (sem IDLead) publica na DLQ e ACK sem chamar runner (TRP-03)", async () => {
        const transport = new RabbitMQTransport(mockRunner);
        await transport.start();

        const invalidBody = { Name: "João", Message: "Olá", Numero: "5511999990001" }; // IDLead ausente
        const handler = mockCreateConsumer.mock.calls[0]?.[1] as ((msg: unknown) => Promise<number>);
        const status = await handler(makeMsg(invalidBody));

        expect(mockRunner.run).not.toHaveBeenCalled();
        expect(mockPubSend).toHaveBeenCalledTimes(1);
        const [dlqName] = mockPubSend.mock.calls[0] as [string, unknown];
        expect(dlqName).toBe("brain-dlq");
        expect(status).toBe(0); // ConsumerStatus.ACK (mensagem inválida foi ackada + DLQ)
      });

      // TRP-05: após 3 falhas, publicar na DLQ e não chamar runner uma 4ª vez
      it("após 3 falhas runner.run() publica na DLQ e não chama run() uma 4ª vez (TRP-05)", async () => {
        (mockRunner.run as ReturnType<typeof mock>).mockRejectedValue(new Error("processing failed"));

        const transport = new RabbitMQTransport(mockRunner);
        await transport.start();

        const handler = mockCreateConsumer.mock.calls[0]?.[1] as ((msg: unknown) => Promise<number>);

        // 3 tentativas com a mesma mensagem (mesma chave IDLead:Numero)
        const msg = makeMsg(validBody);
        await handler(msg); // attempt 1 — falha, REQUEUE
        await handler(msg); // attempt 2 — falha, REQUEUE
        const statusFinal = await handler(msg); // attempt 3 — falha, → DLQ + ACK

        expect(mockRunner.run).toHaveBeenCalledTimes(3);
        expect(mockPubSend).toHaveBeenCalledTimes(1); // publicou na DLQ uma vez
        const [dlqName] = mockPubSend.mock.calls[0] as [string, unknown];
        expect(dlqName).toBe("brain-dlq");
        expect(statusFinal).toBe(0); // ConsumerStatus.ACK (mensagem removida da fila principal)
      });

      it("stop() fecha sub, pub e connection", async () => {
        const transport = new RabbitMQTransport(mockRunner);
        await transport.start();
        await transport.stop();

        expect(mockSubClose).toHaveBeenCalled();
        expect(mockPubClose).toHaveBeenCalled();
        expect(mockRabbitClose).toHaveBeenCalled();
      });
    });
    ```

    4. Atualizar packages/transport/src/factory.test.ts:

    a) Adicionar import de RabbitMQTransport:
    ```typescript
    import { RabbitMQTransport } from "./rabbitmq/consumer.js";
    ```

    b) Remover (ou atualizar) o teste que espera que "rabbitmq" lance ConfigurationError — pois agora é válido:
    ```typescript
    // REMOVER este teste (ou renomear para "unknown value"):
    it("createTransport with unknown TRANSPORT env throws ConfigurationError", () => {
      process.env.TRANSPORT = "rabbitmq";
      expect(() => createTransport()).toThrow(ConfigurationError);
    });

    // SUBSTITUIR por:
    it("createTransport with unknown TRANSPORT env throws ConfigurationError (ex: 'kafka')", () => {
      process.env.TRANSPORT = "kafka";
      expect(() => createTransport()).toThrow(ConfigurationError);
    });
    ```

    c) Adicionar teste TRP-06:
    ```typescript
    it("createTransport com TRANSPORT=rabbitmq retorna RabbitMQTransport (TRP-06)", () => {
      process.env.TRANSPORT = "rabbitmq";
      const mockRunner = { run: async () => ({ reply: "ok" }) };
      const transport = createTransport(mockRunner);
      expect(transport).toBeInstanceOf(RabbitMQTransport);
    });
    ```

    NOTA: O factory.test.ts testa createTransport(). Como RabbitMQTransport.start() exige ENVs e abre conexão, apenas testar `toBeInstanceOf` sem chamar .start() — o construtor apenas guarda o runner, não conecta.
  </action>
  <verify>
    <automated>cd /root/Brain/packages/transport && bun test src/rabbitmq/consumer.test.ts 2>&1 | tail -10</automated>
    Esperado: falha com "Cannot find module './consumer.js'" — confirmando RED state para TRP-03/04/05.
    <automated>cd /root/Brain/packages/transport && bun test src/factory.test.ts 2>&1 | tail -10</automated>
    Esperado: falha com "Cannot find module './rabbitmq/consumer.js'" — confirmando RED state para TRP-06.
  </verify>
  <acceptance_criteria>
    - packages/transport/package.json contém "rabbitmq-client" nas dependencies
    - packages/transport/node_modules/rabbitmq-client/ existe (bun add executado)
    - packages/transport/src/rabbitmq/consumer.test.ts existe
    - Arquivo contém "ConfigurationError"
    - Arquivo contém "TRP-03"
    - Arquivo contém "TRP-04"
    - Arquivo contém "TRP-05"
    - packages/transport/src/factory.test.ts contém "RabbitMQTransport"
    - packages/transport/src/factory.test.ts contém "TRP-06"
    - packages/transport/src/factory.test.ts contém "kafka" (novo teste de unknown TRANSPORT)
    - packages/transport/src/factory.test.ts NÃO contém teste que espera ConfigurationError para "rabbitmq"
  </acceptance_criteria>
  <done>rabbitmq-client instalado, stubs de teste em RED state, factory.test.ts atualizado com TRP-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implementar RabbitMQTransport (TRP-03, TRP-04, TRP-05)</name>
  <files>
    packages/transport/src/rabbitmq/consumer.ts
  </files>
  <read_first>
    - packages/transport/src/rabbitmq/consumer.test.ts (stubs criados em W0 — implementar contra esses testes)
    - packages/transport/src/webhook/handler.ts (template de padrão: construtor, ITransport, ConfigurationError pattern)
    - packages/transport/src/interface.ts (ITransport — start/stop assinatura)
    - packages/transport/src/webhook/events.ts (BrainEventSchema e BrainEvent — reutilizar aqui)
    - .planning/phases/07-leadservice-rabbitmq-transport/07-RESEARCH.md §Pattern 3 e §Pitfall Crítico (deliveryTag vs msgKey)
  </read_first>
  <behavior>
    - Classe RabbitMQTransport implements ITransport
    - Construtor: `constructor(private readonly runner: IBrainRunnerLike)`
    - start(): valida RABBITMQ_URL, RABBITMQ_QUEUE, RABBITMQ_DLQ — ConfigurationError se qualquer um ausente
    - start(): cria Connection, Publisher (confirm:true), Consumer (queue, qos.prefetchCount=1, requeue:false)
    - Consumer handler: parse BrainEventSchema.safeParse(msg.body)
      - Parse falha → pub.send(dlq, msg.body) + return ConsumerStatus.ACK (sem retry)
      - Parse ok → try runner.run(parsed.data) → return ACK
      - runner.run() lança erro → incrementa contador com chave `${IDLead}:${Numero}`
        - Se attempt < 3 → await Bun.sleep(retryDelayMs) + return ConsumerStatus.REQUEUE
        - Se attempt >= 3 → pub.send(dlq, msg.body) + limpa contador + return ConsumerStatus.ACK
    - stop(): fecha sub → pub → rabbit (nessa ordem), limpa retryMap
    - MAX_ATTEMPTS = 3 como constante do módulo
    - retryMap: Map<string, number> onde chave = `${IDLead}:${Numero}` (Opção A do RESEARCH.md — robusto contra deliveryTag reset)
  </behavior>
  <action>
    Criar packages/transport/src/rabbitmq/consumer.ts:

    ```typescript
    // TRP-03, TRP-04, TRP-05: RabbitMQTransport — consumer RabbitMQ com ack manual, retry e DLQ
    // D-07: biblioteca rabbitmq-client@^5.0.8 — zero deps, Bun-compatible, auto-reconnect built-in
    // D-08: new RabbitMQTransport(runner) implementa ITransport
    // D-14: NÃO declara filas — apenas conecta e consome (filas pré-configuradas por ops)
    // D-15: prefetch=1 — uma mensagem por consumer por vez
    // D-16: MAX_ATTEMPTS=3 — após 3 falhas, mensagem vai para DLQ
    // D-19: DLQ via Publisher explícito + ACK da mensagem original (sem dependência de DLX broker)
    // Pitfall RESEARCH.md: chave de retry = IDLead:Numero (não deliveryTag — muda após REQUEUE)

    import { Connection, ConsumerStatus } from "rabbitmq-client";
    import { ConfigurationError } from "@brain-pkg/shared";
    import { createLogger } from "@brain-pkg/observability";
    import { BrainEventSchema } from "../webhook/events.js";
    import type { ITransport } from "../interface.js";
    import type { IBrainRunnerLike } from "../webhook/handler.js";

    const MAX_ATTEMPTS = 3;

    /**
     * TRP-03, TRP-05: RabbitMQTransport — consumer com ack manual, retry e DLQ.
     *
     * Configuração via ENVs (TRP-04):
     *   RABBITMQ_URL            — connection string (obrigatório)
     *   RABBITMQ_QUEUE          — fila de entrada (obrigatório)
     *   RABBITMQ_DLQ            — fila de saída para falhas permanentes (obrigatório)
     *   RABBITMQ_RETRY_DELAY_MS — backoff entre retries em ms (default: 1000)
     *
     * Segurança:
     *   - BrainEventSchema.safeParse() valida payload antes de qualquer processamento (ASVS V5)
     *   - RABBITMQ_URL não é logado — apenas boolean de presença em ConfigurationError
     */
    export class RabbitMQTransport implements ITransport {
      private rabbit?: Connection;
      private sub?: ReturnType<InstanceType<typeof Connection>["createConsumer"]>;
      private pub?: ReturnType<InstanceType<typeof Connection>["createPublisher"]>;
      // Pitfall RESEARCH.md: chave por IDLead:Numero — sobrevive a deliveryTag reset após REQUEUE
      private readonly retryMap = new Map<string, number>();
      private readonly logger = createLogger();

      constructor(private readonly runner: IBrainRunnerLike) {}

      /**
       * D-08: start() abre conexão RabbitMQ e registra consumer.
       * Lança ConfigurationError se qualquer ENV obrigatória estiver ausente.
       * Auto-reconnect gerenciado pelo rabbitmq-client (TRP-05).
       */
      async start(): Promise<void> {
        const url = process.env.RABBITMQ_URL;
        const queue = process.env.RABBITMQ_QUEUE;
        const dlq = process.env.RABBITMQ_DLQ;
        const retryDelayMs = parseInt(process.env.RABBITMQ_RETRY_DELAY_MS ?? "1000", 10);

        // TRP-04: fail-fast — não iniciar sem as 3 ENVs obrigatórias
        // Segurança: não logar valores de env sensíveis (url contém credenciais)
        if (!url || !queue || !dlq) {
          throw new ConfigurationError(
            "RABBITMQ_URL, RABBITMQ_QUEUE e RABBITMQ_DLQ são obrigatórios para TRANSPORT=rabbitmq",
            { hasUrl: !!url, hasQueue: !!queue, hasDlq: !!dlq }
          );
        }

        // TRP-05: auto-reconnect gerenciado pela biblioteca
        this.rabbit = new Connection(url);
        this.rabbit.on("error", (err: unknown) =>
          this.logger.error({ err }, "RabbitMQ connection error")
        );
        this.rabbit.on("connection", () =>
          this.logger.info({}, "RabbitMQ connected")
        );

        // Publisher para DLQ — confirm:true aguarda confirmação do broker (Pitfall 5 do RESEARCH)
        this.pub = this.rabbit.createPublisher({ confirm: true });

        // D-15: prefetch=1 + requeue:false (sem requeue automático — controle manual via retryMap)
        // D-14: sem queueOptions — NÃO declarar fila (filas pré-configuradas por ops)
        this.sub = this.rabbit.createConsumer(
          {
            queue,
            qos: { prefetchCount: 1 },
            requeue: false,
          },
          async (msg) => {
            // ASVS V5: validar payload antes de qualquer processamento
            const parsed = BrainEventSchema.safeParse(msg.body);

            if (!parsed.success) {
              // Payload inválido — sem retry (schema não vai melhorar com retry)
              this.logger.error(
                { body: msg.body },
                "Invalid BrainEvent from RabbitMQ — sending to DLQ"
              );
              await this.pub!.send(dlq, msg.body);
              return ConsumerStatus.ACK; // ack mensagem original (já está na DLQ)
            }

            // Chave de retry por conteúdo (IDLead:Numero) — robusta contra deliveryTag reset
            const msgKey = `${parsed.data.IDLead}:${parsed.data.Numero}`;
            const attempt = (this.retryMap.get(msgKey) ?? 0) + 1;
            this.retryMap.set(msgKey, attempt);

            try {
              await this.runner.run(parsed.data);
              // Sucesso — limpar contador e ACK
              this.retryMap.delete(msgKey);
              return ConsumerStatus.ACK;
            } catch (err) {
              // D-20: todas as exceções tratadas igualmente
              this.logger.error(
                { err, msgKey, attempt },
                "RabbitMQ message processing failed"
              );

              if (attempt >= MAX_ATTEMPTS) {
                // D-16/D-19: após MAX_ATTEMPTS falhas — publicar na DLQ + ACK (sem nack)
                this.logger.error(
                  { msgKey, attempt },
                  "Max attempts reached — sending to DLQ"
                );
                this.retryMap.delete(msgKey);
                await this.pub!.send(dlq, msg.body);
                return ConsumerStatus.ACK;
              }

              // D-17: backoff fixo entre tentativas
              // Nota: prefetch=1 — durante o sleep nenhuma outra mensagem é processada (backpressure)
              await Bun.sleep(retryDelayMs);
              return ConsumerStatus.REQUEUE;
            }
          }
        );

        this.sub.on("error", (err: unknown) =>
          this.logger.error({ err }, "RabbitMQ consumer error")
        );

        this.logger.info({ queue, dlq }, "RabbitMQTransport started");
      }

      /** Encerra consumer, publisher e conexão graciosamente. */
      async stop(): Promise<void> {
        await this.sub?.close();
        await this.pub?.close();
        await this.rabbit?.close();
        this.retryMap.clear();
        this.logger.info({}, "RabbitMQTransport stopped");
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /root/Brain/packages/transport && bun test src/rabbitmq/consumer.test.ts 2>&1</automated>
    Esperado: todos os testes passam (GREEN) — TRP-03, TRP-04, TRP-05 cobertos.
  </verify>
  <acceptance_criteria>
    - packages/transport/src/rabbitmq/consumer.ts existe
    - Contém "export class RabbitMQTransport implements ITransport"
    - Contém "MAX_ATTEMPTS = 3"
    - Contém "IDLead}:${parsed.data.Numero}" (chave de retry por conteúdo — Opção A do pitfall)
    - Contém "ConfigurationError" (fail-fast nas ENVs)
    - Contém "prefetchCount: 1" (D-15)
    - Contém "requeue: false" (sem requeue automático — controle manual)
    - Contém "confirm: true" (Publisher DLQ)
    - NÃO contém "queueOptions" (D-14)
    - NÃO contém "hasUrl: url" ou "url: url" no ConfigurationError (não logar valor sensível — apenas boolean !!url)
    - cd /root/Brain/packages/transport && bun test src/rabbitmq/consumer.test.ts sai com código 0
  </acceptance_criteria>
  <done>RabbitMQTransport implementado, todos os testes TRP-03/04/05 passam, retry com chave IDLead:Numero sem loop infinito.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar RabbitMQTransport na factory e atualizar barrel + .env.example (TRP-06)</name>
  <files>
    packages/transport/src/factory.ts
    packages/transport/src/index.ts
    apps/brain-echo/.env.example
  </files>
  <read_first>
    - packages/transport/src/factory.ts (arquivo completo — adicionar case "rabbitmq" ao switch)
    - packages/transport/src/factory.test.ts (testes atualizados em W0 — verificar que o novo case vai passar)
    - packages/transport/src/index.ts (barrel existente — adicionar export de RabbitMQTransport)
    - packages/transport/src/rabbitmq/consumer.ts (implementado em Task 1 — path do import)
    - apps/brain-echo/src/index.ts (verificar ENVs já usadas para garantir compatibilidade no .env.example)
  </read_first>
  <action>
    1. Atualizar packages/transport/src/factory.ts:

    a) Adicionar import do RabbitMQTransport (após o import de WebhookTransport):
    ```typescript
    import { RabbitMQTransport } from "./rabbitmq/consumer.js";
    ```

    b) Adicionar case "rabbitmq" ao switch (antes do default):
    ```typescript
    case "rabbitmq":
      return new RabbitMQTransport(runner);
    ```

    O arquivo final deve ser:
    ```typescript
    import { ConfigurationError } from "@brain-pkg/shared";
    import { WebhookTransport } from "./webhook/handler.js";
    import { RabbitMQTransport } from "./rabbitmq/consumer.js";
    import type { IBrainRunnerLike } from "./webhook/handler.js";
    import type { ITransport } from "./interface.js";

    /**
     * TRANS-04, D-07, TRP-06: Factory para seleção de transport por ENV.
     *
     * TRANSPORT env var:
     *   "webhook"  — WebhookTransport (Hono HTTP, padrão)
     *   "rabbitmq" — RabbitMQTransport (rabbitmq-client, Phase 7)
     *
     * ConfigurationError se TRANSPORT tiver valor desconhecido.
     */
    export function createTransport(runner?: IBrainRunnerLike): ITransport {
      const type = process.env.TRANSPORT ?? "webhook";

      switch (type) {
        case "webhook":
          return new WebhookTransport(runner);
        case "rabbitmq":
          return new RabbitMQTransport(runner);
        default:
          throw new ConfigurationError(`Unknown TRANSPORT: ${type}`, { transport: type });
      }
    }
    ```

    2. Atualizar packages/transport/src/index.ts — adicionar export de RabbitMQTransport:

    Adicionar após a linha de export do WebhookTransport:
    ```typescript
    export { RabbitMQTransport } from "./rabbitmq/consumer.js";
    ```

    3. Criar apps/brain-echo/.env.example com todas as ENVs do projeto:

    ```
    # =============================================================================
    # brain-echo — Environment Variables
    # =============================================================================

    # --- Database ---
    DATABASE_URL=postgresql://brain:brain@localhost:5432/brain_dev
    EMBEDDING_DIMENSIONS=1536

    # --- Migrations ---
    # Path para a pasta de migrations (relativo ao CWD ou absoluto)
    # Em Docker: /app/migrations (via ENV no Dockerfile)
    MIGRATIONS_FOLDER=../../packages/database/src/migrations

    # --- Transport ---
    # Selecione o transport: webhook (HTTP) ou rabbitmq (fila)
    TRANSPORT=webhook

    # --- Transport: Webhook ---
    PORT=3000
    ADMIN_TOKEN=change-me-in-production

    # --- Transport: RabbitMQ (TRP-03, TRP-04, TRP-05) ---
    # Obrigatório quando TRANSPORT=rabbitmq
    RABBITMQ_URL=amqp://guest:guest@localhost:5672
    RABBITMQ_QUEUE=brain-events
    RABBITMQ_DLQ=brain-dlq
    # Backoff entre tentativas em ms (default: 1000)
    RABBITMQ_RETRY_DELAY_MS=1000

    # --- LLM ---
    OPENAI_API_KEY=sk-...
    LLM_PROVIDER=openai
    LLM_MODEL=gpt-4o-mini

    # --- Observability ---
    # LangSmith tracing (opcional)
    # LANGCHAIN_TRACING_V2=true
    # LANGCHAIN_API_KEY=ls__...
    # LANGCHAIN_PROJECT=brain-echo
    ```
  </action>
  <verify>
    <automated>cd /root/Brain/packages/transport && bun test src/factory.test.ts 2>&1</automated>
    Esperado: todos os testes passam — incluindo TRP-06 (createTransport("rabbitmq") retorna RabbitMQTransport) e o teste de unknown TRANSPORT com "kafka".
    <automated>grep "RabbitMQTransport" /root/Brain/packages/transport/src/index.ts</automated>
    Esperado: linha com export de RabbitMQTransport presente.
    <automated>grep "RABBITMQ_URL" /root/Brain/apps/brain-echo/.env.example</automated>
    Esperado: linha com RABBITMQ_URL= presente.
  </verify>
  <acceptance_criteria>
    - packages/transport/src/factory.ts contém "case \"rabbitmq\":"
    - packages/transport/src/factory.ts importa "from './rabbitmq/consumer.js'"
    - packages/transport/src/index.ts contém "export { RabbitMQTransport }"
    - apps/brain-echo/.env.example existe
    - apps/brain-echo/.env.example contém "RABBITMQ_URL="
    - apps/brain-echo/.env.example contém "RABBITMQ_QUEUE="
    - apps/brain-echo/.env.example contém "RABBITMQ_DLQ="
    - apps/brain-echo/.env.example contém "RABBITMQ_RETRY_DELAY_MS="
    - apps/brain-echo/.env.example contém "TRANSPORT=webhook"
    - cd /root/Brain/packages/transport && bun test src/factory.test.ts sai com código 0
    - cd /root/Brain/packages/transport && bun test sai com código 0 (suite completa do pacote)
  </acceptance_criteria>
  <done>factory.ts com case "rabbitmq", RabbitMQTransport exportado do barrel, .env.example criado com todas as ENVs documentadas, testes TRP-06 passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ broker → consumer handler | Payload da fila é input externo — pode conter dados malformados ou intencionalmente corrompidos |
| consumer handler → runner.run() | Apenas payload validado pelo BrainEventSchema chega ao runner |
| handler → DLQ | Mensagens inválidas ou com falha permanente são publicadas explicitamente na DLQ |
| ENV vars → Connection | RABBITMQ_URL contém credenciais — não deve ser logada em texto claro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | consumer handler msg.body | mitigate | BrainEventSchema.safeParse(msg.body) valida antes de qualquer processamento. Payload inválido → DLQ imediato sem passar pelo runner. |
| T-07-07 | DoS | retry loop infinito | mitigate | MAX_ATTEMPTS=3 com retryMap por IDLead:Numero. Após 3 falhas → DLQ + ACK. Elimina possibilidade de loop infinito mesmo com reconexões. |
| T-07-08 | Information Disclosure | ConfigurationError com RABBITMQ_URL | mitigate | ConfigurationError recebe `{ hasUrl: !!url }` (boolean) — nunca o valor da URL com credenciais. Confirmado no código: `hasUrl: !!url`. |
| T-07-09 | DoS | Bun.sleep bloqueando prefetch slot | accept | Com prefetch=1, sleep bloqueia intencionalmente o slot durante retry. Backpressure natural — comportamento documentado e aceito para v1.1. Não é explorável externamente. |
| T-07-10 | Spoofing | TRANSPORT ENV manipulado | accept | TRANSPORT é configuração de deployment (não input de runtime). Modificar ENV requer acesso ao container/servidor — controle de acesso de infra é out of scope. |
| T-07-11 | Elevation of Privilege | DLQ recebendo dados de processamento | accept | DLQ recebe msg.body original (validado ou não) — não contém dados internos do sistema. Operador que acessa DLQ já tem acesso ao broker. |
</threat_model>

<verification>
Após execução de todos os tasks:

```bash
# Verificar testes unitários do consumer
cd /root/Brain/packages/transport && bun test src/rabbitmq/consumer.test.ts

# Verificar testes da factory (TRP-06)
cd /root/Brain/packages/transport && bun test src/factory.test.ts

# Suite completa do pacote transport
cd /root/Brain/packages/transport && bun test

# Verificar que RabbitMQTransport é exportado
grep "RabbitMQTransport" /root/Brain/packages/transport/src/index.ts

# Verificar .env.example
grep -c "RABBITMQ" /root/Brain/apps/brain-echo/.env.example

# Suite completa do monorepo
cd /root/Brain && bun run test
```
</verification>

<success_criteria>
- TRANSPORT=rabbitmq inicia RabbitMQTransport via factory sem erro (createTransport retorna instância correta)
- Mensagem {Name, Message, Numero, IDLead} processada com sucesso recebe ConsumerStatus.ACK
- Payload sem IDLead vai direto para DLQ sem chamar runner.run()
- Após 3 falhas em runner.run(), mensagem publicada na DLQ e ConsumerStatus.ACK retornado
- runner.run() não é chamado uma 4ª vez após 3 falhas (retryMap limpo + DLQ acionado)
- start() lança ConfigurationError se RABBITMQ_URL, RABBITMQ_QUEUE ou RABBITMQ_DLQ ausentes
- cd /root/Brain/packages/transport && bun test sai com código 0
- apps/brain-echo/.env.example documenta RABBITMQ_URL, RABBITMQ_QUEUE, RABBITMQ_DLQ, RABBITMQ_RETRY_DELAY_MS
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-leadservice-rabbitmq-transport/07-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Versão instalada do rabbitmq-client (confirmar via package.json após bun add)
- Chave de retry usada (IDLead:Numero — Opção A do pitfall)
- Qualquer desvio de implementação em relação ao plano
</output>
