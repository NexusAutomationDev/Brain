---
phase: 05-transport-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/transport/src/webhook/events.ts
  - packages/transport/src/webhook/handler.ts
  - packages/transport/src/webhook/dedup.ts
  - packages/transport/src/webhook/dedup.test.ts
  - packages/transport/src/webhook/handler.test.ts
  - packages/transport/src/factory.ts
  - packages/transport/src/factory.test.ts
  - packages/transport/src/index.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
  - packages/core/src/runner/__tests__/brain-runner.integration.test.ts
  - apps/brain-echo/src/__tests__/integration/webhook.test.ts
  - apps/brain-echo/src/__tests__/integration/restart.test.ts
autonomous: true
requirements:
  - TRP-02
must_haves:
  truths:
    - "POST /api/v1/webhook com payload {Name, Message, Numero, IDLead} retorna 200 com { status: 'ok', reply: string } quando runner está injetado"
    - "POST /api/v1/webhook sem runner injetado (WebhookTransport.start() sem runner) lança ConfigurationError antes de aceitar qualquer request"
    - "POST /api/v1/webhook com payload antigo (conversationId, stepIndex, userId, content) retorna 400 — campos antigos rejeitados"
    - "Nenhum request é rejeitado por ausência de X-Request-Id — header não é mais obrigatório"
    - "DedupCache e dedup.ts não existem mais no pacote transport"
  artifacts:
    - path: "packages/transport/src/webhook/events.ts"
      provides: "BrainEvent Zod schema com {Name, Message, Numero, IDLead}"
      contains: "z.object({ Name, Message, Numero, IDLead })"
    - path: "packages/transport/src/webhook/handler.ts"
      provides: "WebhookTransport com runner injection no construtor + ConfigurationError em start()"
      contains: "class WebhookTransport"
    - path: "packages/transport/src/factory.ts"
      provides: "createTransport(runner?) passa runner ao WebhookTransport constructor"
      contains: "new WebhookTransport(runner)"
    - path: "packages/transport/src/webhook/handler.test.ts"
      provides: "Testes atualizados para novo schema e sem dedup"
      contains: "Name: "
  key_links:
    - from: "packages/transport/src/factory.ts"
      to: "packages/transport/src/webhook/handler.ts"
      via: "new WebhookTransport(runner)"
      pattern: "new WebhookTransport\\(runner"
    - from: "packages/transport/src/webhook/handler.ts"
      to: "packages/shared/src/errors/index.ts"
      via: "ConfigurationError import"
      pattern: "ConfigurationError"
    - from: "packages/core/src/runner/runner.ts"
      to: "packages/transport/src/webhook/events.ts"
      via: "BrainEvent type import"
      pattern: "event\\.Message"
---

<objective>
Corrige o BrainEvent schema para {Name, Message, Numero, IDLead}, remove o mecanismo de dedup (X-Request-Id), e implementa runner injection via construtor no WebhookTransport com fail-fast ConfigurationError em start(). Atualiza todos os consumidores: runner.ts, factory.ts e fixtures de teste em transport, core e brain-echo.

Purpose: GAP-1 — WebhookTransport.start() sem runner resulta em silent accept sem processamento. Após esta fase, start() sem runner lança ConfigurationError imediatamente.
Output: BrainEvent com campos padronizados para WhatsApp/CRM; WebhookTransport com runner injection; todos os testes passando com os novos nomes de campo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-transport-foundation/05-CONTEXT.md
</context>

<interfaces>
<!-- Interfaces e contratos relevantes extraídos do codebase para o executor. -->
<!-- Executor NÃO precisa explorar o codebase — esses são os contratos. -->

De packages/shared/src/errors/index.ts:
```typescript
export class ConfigurationError extends BrainError {
  constructor(message: string, context?: Record<string, unknown>)
  // Throws with code = 'CONFIGURATION_ERROR'
}
```

De packages/transport/src/interface.ts (NÃO muda):
```typescript
export interface ITransport {
  start(port?: number): Promise<void>;
  stop(): Promise<void>;
}
```

BrainEvent atual (A SER SUBSTITUÍDO):
```typescript
// ANTES — remover completamente
{ conversationId: string, stepIndex: number, userId: string, content: string, metadata?: Record<string, unknown> }

// DEPOIS — novo schema (D-01, D-04)
{ Name: string, Message: string, Numero: string, IDLead: string }
```

WebhookTransport atual (A SER ATUALIZADO):
```typescript
// ANTES — construtor sem args, start() sem runner
export class WebhookTransport implements ITransport {
  async start(port = 3000): Promise<void> {
    const app = createWebhookApp(); // runner NUNCA é passado
    ...
  }
}

// DEPOIS — runner via construtor (D-05, D-06)
export class WebhookTransport implements ITransport {
  constructor(private readonly runner?: IBrainRunnerLike) {}
  async start(port = 3000): Promise<void> {
    if (!this.runner) throw new ConfigurationError("WebhookTransport requires a runner — inject via constructor: new WebhookTransport(runner)", { transport: "webhook" });
    const app = createWebhookApp(this.runner);
    ...
  }
}
```

createTransport atual (A SER ATUALIZADO):
```typescript
// ANTES
export function createTransport(transport?: string): ITransport {
  ...
  case "webhook": return new WebhookTransport();
}

// DEPOIS (D-07)
export function createTransport(runner?: IBrainRunnerLike, transport?: string): ITransport
// OU manter assinatura atual e adicionar overload — ver D-07:
// "Factory createTransport(runner?) atualizada para passar runner ao construtor"
// Assinatura: createTransport(runner?: IBrainRunnerLike): ITransport
// type selection continua via process.env.TRANSPORT
```

BrainRunner.run() — campos a atualizar (runner.ts linhas 128-165):
```typescript
// ANTES
const threadId = event.conversationId;
await this.memoryManager.getContext(threadId, event.userId, []);
messages: [{ role: "human", content: event.content }],
userId: event.userId,
await this.memoryManager.saveContext({ userId: event.userId, ... });

// DEPOIS (novos campos de BrainEvent)
const threadId = event.Numero; // proxy temporário — Phase 8 troca por lead.unique_id
await this.memoryManager.getContext(threadId, event.IDLead, []);
messages: [{ role: "human", content: event.Message }],
userId: event.IDLead,
await this.memoryManager.saveContext({ userId: event.IDLead, ... });
```
NOTA: usar event.Numero como threadId e event.IDLead como userId é solução intermediária para Phase 5.
Phase 8 substituirá por lead.unique_id. Documentar com comentário "// Phase 8: substituir por lead.unique_id".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Substituir BrainEvent schema e remover DedupCache</name>
  <files>
    packages/transport/src/webhook/events.ts,
    packages/transport/src/webhook/handler.ts,
    packages/transport/src/webhook/dedup.ts,
    packages/transport/src/webhook/dedup.test.ts,
    packages/transport/src/webhook/handler.test.ts,
    packages/transport/src/index.ts
  </files>

  <read_first>
    - packages/transport/src/webhook/events.ts — schema atual a ser substituído
    - packages/transport/src/webhook/handler.ts — handler atual com DedupCache e X-Request-Id
    - packages/transport/src/webhook/dedup.ts — arquivo a ser removido (deletar)
    - packages/transport/src/webhook/dedup.test.ts — arquivo a ser removido (deletar)
    - packages/transport/src/webhook/handler.test.ts — testes a atualizar
    - packages/transport/src/index.ts — barrel export a atualizar (remover DedupCache)
  </read_first>

  <behavior>
    - events.ts: BrainEventSchema.parse({ Name: "A", Message: "B", Numero: "11", IDLead: "1" }) não lança
    - events.ts: BrainEventSchema.parse({ conversationId: "x", stepIndex: 0, userId: "u", content: "c" }) lança ZodError
    - events.ts: BrainEventSchema.parse({ Name: "A", Message: "B", Numero: "11" }) lança ZodError (IDLead faltando)
    - handler.ts: POST com payload {Name, Message, Numero, IDLead} válido retorna 200 { status: "accepted" } sem runner
    - handler.ts: POST com payload {Name, Message, Numero, IDLead} válido + runner retorna 200 { status: "ok", reply: "..." }
    - handler.ts: POST sem X-Request-Id retorna 200 (não 400 — header não é mais obrigatório)
    - handler.ts: POST com payload antigo {conversationId, stepIndex, userId, content} retorna 400 "Invalid BrainEvent"
    - handler.ts: POST com JSON malformado retorna 400
  </behavior>

  <action>
**1. Substituir packages/transport/src/webhook/events.ts** (D-01, D-02, D-04):
```typescript
import { z } from "zod";

/**
 * TRP-02, D-01, D-04: BrainEvent schema — campos padronizados para WhatsApp/CRM.
 *
 * Campos anteriores (conversationId, stepIndex, userId, content, metadata) REMOVIDOS (D-02).
 * Sem deprecation shim — quebra intencional para forçar atualização dos consumidores.
 *
 * ASVS V5 Input Validation: valida estrutura antes de qualquer processamento.
 */
export const BrainEventSchema = z.object({
  Name: z.string().min(1, "Name is required"),
  Message: z.string().min(1, "Message is required"),
  Numero: z.string().min(1, "Numero is required"),
  IDLead: z.string().min(1, "IDLead is required"),
});

export type BrainEvent = z.infer<typeof BrainEventSchema>;
```

**2. Reescrever packages/transport/src/webhook/handler.ts** (D-03, D-05, D-06, D-08):
- Remover imports de DedupCache e dedup.js
- Remover toda lógica de X-Request-Id (requestId, cache.claim)
- IBrainRunnerLike permanece local em handler.ts (D-08): `interface IBrainRunnerLike { run(event: BrainEvent): Promise<{ reply: string }> }`
- createWebhookApp(runner?: IBrainRunnerLike) — mantém assinatura existente, remove dedup
- WebhookTransport recebe runner no construtor (D-05): `constructor(private readonly runner?: IBrainRunnerLike) {}`
- start() lança ConfigurationError se runner ausente (D-06): `if (!this.runner) throw new ConfigurationError("WebhookTransport requires a runner — inject via constructor: new WebhookTransport(runner)", { transport: "webhook" })`
- start() passa `this.runner` para `createWebhookApp(this.runner)`
- Adicionar import de ConfigurationError: `import { ConfigurationError } from "@brain-pkg/shared"`

**3. Deletar packages/transport/src/webhook/dedup.ts** (D-02, D-16):
Arquivo completamente removido — DedupCache não existe mais.

**4. Deletar packages/transport/src/webhook/dedup.test.ts** (D-16):
Testes do DedupCache removidos junto com a feature.

**5. Reescrever packages/transport/src/webhook/handler.test.ts** (D-15):
- Remover `import` de DedupCache se houver
- Substituir `validEvent` de `{ conversationId, stepIndex, userId, content }` por `{ Name: "João Silva", Message: "Olá, quero saber mais sobre o produto", Numero: "5511999990001", IDLead: "lead-001" }`
- Remover teste "POST /api/v1/webhook without X-Request-Id returns 400" — header não é mais obrigatório
- Remover teste "duplicate X-Request-Id returns 409" — dedup removido (D-16)
- Atualizar teste de campo ausente: body `{ Name: "A" }` deve retornar 400 (faltam Message, Numero, IDLead)
- Adicionar teste: POST com runner injetado retorna 200 `{ status: "ok", reply: "..." }` usando mock de runner
- Adicionar teste: POST sem runner (app = createWebhookApp()) retorna 200 `{ status: "accepted" }` (fallback)
- Manter `beforeEach(() => { app = createWebhookApp() })` mas sem DedupCache nos comentários

**6. Atualizar packages/transport/src/index.ts** — remover export de DedupCache:
```typescript
// Remover esta linha:
export { DedupCache } from "./webhook/dedup.js";
```
Manter exports de ITransport, BrainEvent, BrainEventSchema, createWebhookApp, WebhookTransport, createTransport.
  </action>

  <verify>
    <automated>cd /root/Brain && bun test packages/transport/src/webhook/handler.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - packages/transport/src/webhook/events.ts contém `Name: z.string()` e `Message: z.string()` e `Numero: z.string()` e `IDLead: z.string()`
    - packages/transport/src/webhook/events.ts NÃO contém `conversationId` nem `stepIndex` nem `metadata`
    - packages/transport/src/webhook/dedup.ts NÃO existe: `test ! -f packages/transport/src/webhook/dedup.ts`
    - packages/transport/src/webhook/dedup.test.ts NÃO existe: `test ! -f packages/transport/src/webhook/dedup.test.ts`
    - packages/transport/src/webhook/handler.ts contém `ConfigurationError`
    - packages/transport/src/webhook/handler.ts contém `private readonly runner`
    - packages/transport/src/webhook/handler.ts NÃO contém `DedupCache` nem `X-Request-Id`
    - packages/transport/src/webhook/handler.test.ts contém `Name: "João Silva"` ou `Name: `
    - packages/transport/src/webhook/handler.test.ts NÃO contém `conversationId`
    - packages/transport/src/index.ts NÃO contém `DedupCache`
    - `bun test packages/transport/src/webhook/handler.test.ts` exits 0
  </acceptance_criteria>

  <done>BrainEvent tem 4 campos {Name, Message, Numero, IDLead}. DedupCache e dedup.ts removidos. handler.ts sem lógica X-Request-Id. Testes do handler passando com novo schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runner injection no WebhookTransport, factory e consumidores (runner.ts + tests)</name>
  <files>
    packages/transport/src/factory.ts,
    packages/transport/src/factory.test.ts,
    packages/core/src/runner/runner.ts,
    packages/core/src/runner/__tests__/brain-runner.test.ts,
    packages/core/src/runner/__tests__/brain-runner.integration.test.ts,
    apps/brain-echo/src/__tests__/integration/webhook.test.ts,
    apps/brain-echo/src/__tests__/integration/restart.test.ts
  </files>

  <read_first>
    - packages/transport/src/factory.ts — factory atual a atualizar (aceitar runner como arg)
    - packages/transport/src/factory.test.ts — testes do factory a atualizar
    - packages/core/src/runner/runner.ts — usa event.conversationId, event.userId, event.content — todos a renomear
    - packages/core/src/runner/__tests__/brain-runner.test.ts — makeEvent() com campos antigos
    - packages/core/src/runner/__tests__/brain-runner.integration.test.ts — BrainEvent literal com campos antigos
    - apps/brain-echo/src/__tests__/integration/webhook.test.ts — makeBrainEvent() com campos antigos
    - apps/brain-echo/src/__tests__/integration/restart.test.ts — sendMessage() com campos antigos
    - packages/transport/src/webhook/handler.ts — verificar WebhookTransport após Task 1 (confirmar construtor)
  </read_first>

  <behavior>
    - factory.ts: createTransport(runner) retorna WebhookTransport com runner injetado
    - factory.ts: createTransport() sem runner retorna WebhookTransport com runner = undefined
    - factory.ts: WebhookTransport.start() sem runner (runner=undefined) lança ConfigurationError
    - runner.ts: event.Message (não event.content) é passado para LangGraph como conteúdo da mensagem humana
    - runner.ts: event.IDLead (não event.userId) é usado como userId no MemoryManager
    - runner.ts: event.Numero (não event.conversationId) é usado como threadId temporário
    - brain-runner.test.ts: makeEvent() retorna { Name: "...", Message: "...", Numero: "...", IDLead: "..." }
  </behavior>

  <action>
**1. Atualizar packages/transport/src/factory.ts** (D-07):
- Mudar assinatura: `createTransport(runner?: IBrainRunnerLike, transportType?: string): ITransport`
- IBrainRunnerLike é importada de handler.ts OU redeclarada localmente para evitar circular dep
- Opção recomendada: re-exportar IBrainRunnerLike de handler.ts e importar em factory.ts
  - Alternativa: declarar interface mínima local em factory.ts (mesma duck typing approach)
- `case "webhook": return new WebhookTransport(runner)` — passa runner ao construtor
- `TRANSPORT` env var selection permanece: `const type = transportType ?? process.env.TRANSPORT ?? "webhook"`
- Assinatura final recomendada: `export function createTransport(runner?: IBrainRunnerLike): ITransport`
  (sem transportType arg — type vem de process.env.TRANSPORT, mantém compatibilidade com factory.test.ts)

**2. Atualizar packages/transport/src/factory.test.ts** (D-07):
- Verificar se testes existentes quebram com nova assinatura
- Testes de tipo "createTransport('webhook') retorna WebhookTransport" continuam válidos
- Adicionar teste: `createTransport(mockRunner)` retorna WebhookTransport (instanceof check)
- Adicionar teste: `createTransport()` retorna WebhookTransport com runner undefined
- NOTA: factory.test.ts usa `createTransport("webhook")` com string como primeiro arg — MUDA para runner
  - Atualizar testes para passar runner ou undefined como primeiro arg
  - Type selection agora via `process.env.TRANSPORT`

**3. Atualizar packages/core/src/runner/runner.ts** — renomear campos de BrainEvent (linhas ~128-165):
Substituir cada referência de campo antigo pelo novo:
- `event.conversationId` → `event.Numero` (proxy para Phase 8 — comentar "// Phase 8: substituir por lead.unique_id")
- `event.userId` → `event.IDLead` (em TODAS as 4 ocorrências nas linhas 132, 137, 144, 163)
- `event.content` → `event.Message`

Após substituição, runner.ts deve conter:
```typescript
const threadId = event.Numero; // Phase 8: substituir por lead.unique_id
await this.memoryManager.getContext(threadId, event.IDLead, []);
// ...
messages: [{ role: "human", content: event.Message }],
userId: event.IDLead,
// ...
await this.memoryManager.saveContext({
  userId: event.IDLead,
  profileKey: "context",
  profileValue: { lastReply: reply, conversationId: threadId },
});
```

**4. Atualizar packages/core/src/runner/__tests__/brain-runner.test.ts** (D-15):
- Substituir makeEvent() de campos antigos para novo schema:
```typescript
function makeEvent(): BrainEvent {
  return {
    Name: "Test User",
    Message: "hello",
    Numero: "5511999990001",
    IDLead: "lead-test-1",
  };
}
```

**5. Atualizar packages/core/src/runner/__tests__/brain-runner.integration.test.ts** (D-15):
- Substituir todos os literais BrainEvent:
```typescript
// event 1
const event: BrainEvent = {
  Name: "Test User Integration",
  Message: "hello",
  Numero: "5511999990001",
  IDLead: "lead-integration-1",
};
// event 2 (mesmo thread — Numero igual)
const event2: BrainEvent = {
  Name: "Test User Integration",
  Message: "remember that",
  Numero: "5511999990001", // mesmo Numero = mesmo thread
  IDLead: "lead-integration-1",
};
```

**6. Atualizar apps/brain-echo/src/__tests__/integration/webhook.test.ts** (D-15):
- Substituir makeBrainEvent():
```typescript
function makeBrainEvent(message: string, numero = `num-${Date.now()}`) {
  return {
    Name: "Test User SC2",
    Message: message,
    Numero: numero,
    IDLead: `lead-sc2-${Date.now()}`,
  };
}
```
- Remover teste "mesmo X-Request-Id em duas requests retorna 409 na segunda" — dedup removido (D-16)
- Remover teste "sem X-Request-Id retorna 400" — header não obrigatório (D-03)
- Atualizar comentário de corpo inválido: `{ Message: "hello" }` (falta Name, Numero, IDLead) → 400

**7. Atualizar apps/brain-echo/src/__tests__/integration/restart.test.ts** (D-15):
- Substituir sendMessage() para usar campos novos:
```typescript
async function sendMessage(message: string, stepIndex: number): Promise<string> {
  const requestId = `sc3-${CONVERSATION_ID}-step${stepIndex}`;
  // ...
  body: JSON.stringify({
    Name: "Test User SC3",
    Message: message,
    Numero: CONVERSATION_ID,
    IDLead: "lead-sc3-restart",
  }),
}
```
- Remover X-Request-Id do sendMessage() se houver (D-03) ou manter (header ignorado pelo server — inofensivo)
- Atualizar comentários: "O mesmo Numero faz o LangGraph carregar o checkpoint do turno 1"
  </action>

  <verify>
    <automated>cd /root/Brain && bun test packages/transport/src/factory.test.ts packages/core/src/runner/__tests__/brain-runner.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - packages/transport/src/factory.ts contém `new WebhookTransport(runner)`
    - packages/core/src/runner/runner.ts contém `event.Message` e `event.IDLead` e `event.Numero`
    - packages/core/src/runner/runner.ts NÃO contém `event.conversationId` nem `event.userId` nem `event.content`
    - packages/core/src/runner/runner.ts contém comentário `Phase 8` próximo ao threadId
    - packages/core/src/runner/__tests__/brain-runner.test.ts contém `Name: "` e `Message: "` e `Numero: "` e `IDLead: "`
    - packages/core/src/runner/__tests__/brain-runner.test.ts NÃO contém `conversationId`
    - apps/brain-echo/src/__tests__/integration/webhook.test.ts NÃO contém `conversationId`
    - `bun test packages/transport/src/factory.test.ts` exits 0
    - `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` exits 0
    - `bun test packages/transport` exits 0 (todos os testes do pacote transport passam)
  </acceptance_criteria>

  <done>factory.ts passa runner ao WebhookTransport. runner.ts usa {Message, IDLead, Numero}. Todos os testes de unit no transport e core passam com os novos nomes de campo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /api/v1/webhook | Input não confiável — corpo JSON de origem externa (WhatsApp/CRM) |
| factory.ts → WebhookTransport | Runner pode ser undefined — fail-fast deve ocorrer antes de aceitar requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | BrainEventSchema (events.ts) | mitigate | Zod safeParse valida {Name, Message, Numero, IDLead} todos como string.min(1) — campos ausentes ou tipo errado retornam 400 antes de qualquer processamento |
| T-05-02 | Elevation of Privilege | WebhookTransport.start() | mitigate | ConfigurationError lançada se runner ausente — impede servidor aceitar requests em estado não configurado (GAP-1 fix, D-06) |
| T-05-03 | Information Disclosure | createWebhookApp handler | accept | reply retornado ao cliente é apenas string de texto da LLM — estado interno do LangGraph (thread_id, checkpoint data) nunca exposto; padrão mantido do v1.0 |
| T-05-04 | Denial of Service | POST /api/v1/webhook | accept | Rate limiting e DDoS protection são responsabilidade do proxy/gateway upstream (ex: nginx, cloudflare); fora de escopo para v1.1 |
| T-05-05 | Spoofing | IDLead como string | accept | IDLead vem como string do WhatsApp/CRM sem verificação de autenticidade nesta fase — validação de identidade de lead será tratada em Phase 7 (LeadService) |
</threat_model>

<verification>
Verificação completa da Phase 5 Plan 01:

```bash
# 1. Schema novo está correto
cd /root/Brain
grep -n "Name\|Message\|Numero\|IDLead" packages/transport/src/webhook/events.ts

# 2. Campos antigos removidos do schema
! grep -n "conversationId\|stepIndex\|metadata" packages/transport/src/webhook/events.ts

# 3. DedupCache removido
test ! -f packages/transport/src/webhook/dedup.ts && echo "dedup.ts REMOVIDO OK"
test ! -f packages/transport/src/webhook/dedup.test.ts && echo "dedup.test.ts REMOVIDO OK"

# 4. Runner injection presente
grep -n "ConfigurationError\|private readonly runner" packages/transport/src/webhook/handler.ts

# 5. factory passa runner
grep -n "new WebhookTransport(runner)" packages/transport/src/factory.ts

# 6. runner.ts usa novos campos
grep -n "event.Message\|event.IDLead\|event.Numero" packages/core/src/runner/runner.ts

# 7. Todos os testes passam
bun test packages/transport packages/core/src/runner/__tests__/brain-runner.test.ts
```
</verification>

<success_criteria>
- BrainEvent schema tem exatamente 4 campos: `{Name: string, Message: string, Numero: string, IDLead: string}`
- `dedup.ts` e `dedup.test.ts` não existem no repo
- `WebhookTransport.start()` sem runner lança `ConfigurationError` (não silent accept)
- `createTransport(runner)` passa runner ao `new WebhookTransport(runner)`
- `runner.ts` usa `event.Message`, `event.IDLead`, `event.Numero` — zero referências aos campos antigos
- Todos os testes em `packages/transport` e `packages/core/src/runner/__tests__/brain-runner.test.ts` passam: `bun test packages/transport packages/core/src/runner/__tests__/brain-runner.test.ts` exits 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-transport-foundation/05-01-SUMMARY.md` com:
- Decisões implementadas (D-01 a D-09, D-15, D-16)
- Arquivos modificados/removidos
- Campos do BrainEvent antigos → novos (tabela de mapeamento)
- Padrão de runner injection estabelecido para Phase 7 (RabbitMQ)
</output>
