---
phase: 05-transport-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/shared/package.json
  - packages/database/package.json
  - packages/observability/package.json
  - packages/ai/package.json
  - packages/memory/package.json
  - packages/transport/package.json
  - packages/core/package.json
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "bun run lint em qualquer dos 7 pacotes do monorepo executa ESLint sem erro de 'lint script not found'"
    - "turbo run lint na raiz passa sem erros de configuração (scripts presentes em todos os pacotes)"
    - "@typescript-eslint/parser e @typescript-eslint/eslint-plugin estão declarados em devDependencies do root package.json"
    - "Os 7 pacotes herdam a config ESLint do root sem arquivo .eslintrc.js por pacote"
  artifacts:
    - path: "package.json"
      provides: "devDependencies com @typescript-eslint/parser e @typescript-eslint/eslint-plugin"
      contains: "@typescript-eslint/parser"
    - path: "packages/shared/package.json"
      provides: "script lint: eslint src/"
      contains: "\"lint\""
    - path: "packages/transport/package.json"
      provides: "script lint: eslint src/"
      contains: "\"lint\""
  key_links:
    - from: "turbo.json (lint task)"
      to: "packages/*/package.json (lint script)"
      via: "turbo run lint delegates to each package's npm script"
      pattern: "\"lint\": \"eslint src/\""
    - from: "packages/*/package.json (lint script)"
      to: ".eslintrc.js (root)"
      via: "ESLint config inheritance (root: true)"
      pattern: "root: true"
---

<objective>
Instala as dependências TypeScript faltantes do ESLint no root e adiciona o script `"lint": "eslint src/"` nos 7 pacotes do monorepo. Após este plano, `turbo run lint` na raiz funciona corretamente em todos os pacotes.

Purpose: INFRA-02 — Lint pipeline estava no-op (turbo task definida mas sem scripts nos pacotes). Após esta fase, erros TypeScript comuns são capturados em CI antes do merge.
Output: 7 package.json atualizados + root package.json com @typescript-eslint deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-transport-foundation/05-CONTEXT.md
</context>

<interfaces>
<!-- Estado atual dos package.json relevantes -->

root package.json — devDependencies atuais:
```json
{
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^8.0.0",
    "turbo": "^2.7.4",
    "typescript": "^5.0.0"
  }
}
```
FALTAM: `@typescript-eslint/parser` e `@typescript-eslint/eslint-plugin`

.eslintrc.js (root) — já referencia os plugins mas eles não estão instalados:
```javascript
module.exports = {
  root: true,
  extends: ['eslint:recommended'],
  parser: '@typescript-eslint/parser',
  parserOptions: { ecmaVersion: 2022, sourceType: 'module' },
  env: { es2022: true, node: true }
};
```

turbo.json — lint task já definida:
```json
"lint": {
  "cache": true,
  "inputs": ["src/**", ".eslintrc.js"]
}
```

Todos os 7 pacotes — scripts atuais (NENHUM tem lint):
shared: { "build", "test", "typecheck" }
database: { "build", "test", "typecheck", "db:push", "db:generate", "db:migrate" }
observability: { "build", "test", "typecheck" }
ai: { "build", "test", "test:integration", "typecheck" }
memory: { "build", "test", "typecheck" }
transport: { "build", "test", "typecheck" }
core: { "build", "test", "typecheck" }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @typescript-eslint deps no root e adicionar script lint nos 7 pacotes</name>
  <files>
    package.json,
    packages/shared/package.json,
    packages/database/package.json,
    packages/observability/package.json,
    packages/ai/package.json,
    packages/memory/package.json,
    packages/transport/package.json,
    packages/core/package.json
  </files>

  <read_first>
    - /root/Brain/package.json — devDependencies atuais (confirmar versão do eslint antes de adicionar peer deps)
    - /root/Brain/packages/shared/package.json — scripts atuais (não sobrescrever scripts existentes)
    - /root/Brain/packages/database/package.json — scripts atuais (db:push, db:generate, db:migrate devem ser mantidos)
    - /root/Brain/packages/ai/package.json — scripts atuais (test:integration deve ser mantido)
    - /root/Brain/.eslintrc.js — confirmar que já referencia @typescript-eslint/parser
  </read_first>

  <action>
**1. Instalar @typescript-eslint deps** (D-11):
Execute o comando de instalação no root do monorepo:
```bash
cd /root/Brain && pnpm add -D @typescript-eslint/parser @typescript-eslint/eslint-plugin --workspace-root
```

Isso adiciona ao root package.json devDependencies:
- `"@typescript-eslint/parser": "^5.0.0"` (peer de eslint ^8 — usar latest v5.x compatível com eslint 8)
- `"@typescript-eslint/eslint-plugin": "^5.0.0"`

Se pnpm não disponível, editar package.json diretamente adicionando ao bloco devDependencies:
```json
"@typescript-eslint/eslint-plugin": "^5.62.0",
"@typescript-eslint/parser": "^5.62.0"
```
Então executar `pnpm install` ou `bun install` para instalar.

NOTA: @typescript-eslint v5.x é compatível com eslint ^8. NÃO usar v6+ (requer eslint 9+).

**2. Adicionar script lint em cada um dos 7 pacotes** (D-12, D-13):
Para cada package.json abaixo, adicionar `"lint": "eslint src/"` ao objeto `"scripts"`.
NÃO remover scripts existentes. Adicionar o script lint em ordem alfabética ou após "build".

Pacotes a atualizar (adicionar `"lint": "eslint src/"` em scripts):
- packages/shared/package.json
- packages/database/package.json
- packages/observability/package.json
- packages/ai/package.json
- packages/memory/package.json
- packages/transport/package.json
- packages/core/package.json

Exemplo de resultado para packages/shared/package.json:
```json
{
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc --noEmit"
  }
}
```

Exemplo de resultado para packages/database/package.json (manter scripts existentes):
```json
{
  "scripts": {
    "build": "tsc",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun src/migrate.ts",
    "db:push": "drizzle-kit push",
    "lint": "eslint src/",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
```

Cobertura: apenas `src/` (D-13). Testes em `__tests__/` e `src/**/__tests__/` ficam fora do lint nesta fase.
Os pacotes herdam automaticamente o root `.eslintrc.js` (já tem `root: true`) — sem criar .eslintrc.js por pacote (D-12).
  </action>

  <verify>
    <automated>cd /root/Brain && for pkg in shared database observability ai memory transport core; do grep -q '"lint"' packages/$pkg/package.json && echo "$pkg: OK" || echo "$pkg: MISSING"; done</automated>
  </verify>

  <acceptance_criteria>
    - /root/Brain/package.json contém `"@typescript-eslint/parser"` em devDependencies
    - /root/Brain/package.json contém `"@typescript-eslint/eslint-plugin"` em devDependencies
    - Todos os 7 pacotes têm `"lint": "eslint src/"` em scripts: `grep -r '"lint": "eslint src/"' packages/*/package.json | wc -l` retorna 7
    - Scripts existentes preservados: packages/database/package.json contém `"db:push"` e `"db:migrate"` e `"lint"`
    - packages/ai/package.json contém `"test:integration"` e `"lint"` (ambos preservados)
    - Nenhum arquivo .eslintrc.js criado dentro de packages/: `find packages/ -name '.eslintrc.js' | wc -l` retorna 0
  </acceptance_criteria>

  <done>Todos os 7 pacotes têm script lint. root package.json tem @typescript-eslint deps declarados.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar turbo run lint e corrigir erros de lint em src/ dos pacotes</name>
  <files>
    packages/shared/src/errors/index.ts,
    packages/transport/src/index.ts
  </files>

  <read_first>
    - /root/Brain/.eslintrc.js — regras ativas (eslint:recommended + @typescript-eslint/parser)
    - packages/transport/src/index.ts — barrel com DedupCache que foi removido em Plan 01 (pode ter import quebrado dependendo da ordem de execução)
  </read_first>

  <action>
**1. Executar turbo run lint para descobrir erros**:
```bash
cd /root/Brain && turbo run lint 2>&1
```

Se `turbo` não disponível, executar por pacote:
```bash
cd /root/Brain && for pkg in shared database observability ai memory transport core; do
  echo "=== $pkg ===" && cd packages/$pkg && bun run lint 2>&1 | head -20 && cd /root/Brain
done
```

**2. Corrigir erros de lint encontrados**:

Erros esperados e correções:
- `no-unused-vars` em arquivos que importam algo apenas para tipagem → substituir `import X` por `import type X`
- `no-undef` para `Bun` global (bun runtime) → adicionar `/* global Bun */` no topo do arquivo ou verificar se `env: { node: true }` é suficiente
- Erros em arquivos relacionados a DedupCache removido no Plan 01 → já resolvidos na Task 1 do Plan 01
- Parser errors em `.ts` files com sintaxe TypeScript específica → @typescript-eslint/parser deve resolver

REGRA: corrigir APENAS erros de lint em `src/` (não em `__tests__/`). Se um erro em test file aparecer, ignorar (D-13 — testes fora de escopo).

Se `turbo run lint` passa sem erros: esta task não tem modificações de código, apenas confirmar o resultado.

**3. Confirmar resultado final**:
```bash
cd /root/Brain && turbo run lint
```
Deve terminar com "Tasks: N successful, N total" sem erros.

NOTA IMPORTANTE: Se Plan 01 não foi executado antes deste plan (execução paralela), pode haver
erros de lint em transport/src/index.ts relacionados à DedupCache. Nesses casos:
- Ignorar — Plan 01 vai resolver quando executado
- Esta task verifica apenas que o script lint EXISTE e EXECUTA (não que passa com zero erros em todos os pacotes)
- A verificação de zero erros acontece no final da Phase 5 após ambos os planos estarem concluídos
  </action>

  <verify>
    <automated>cd /root/Brain && turbo run lint --filter='./packages/*' 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `turbo run lint` executa sem erro "missing script: lint" em nenhum pacote
    - turbo output contém "Tasks: 7 successful" OU todos os 7 pacotes aparecem na saída (mesmo que com warnings)
    - Se houver erros de lint em src/ (não em __tests__/): foram corrigidos ou documentados na SUMMARY como débito técnico
    - `cd /root/Brain/packages/shared && bun run lint` executa sem "missing script" (exits com 0 ou 1, nunca com "not found")
    - `cd /root/Brain/packages/transport && bun run lint` executa sem "missing script"
  </acceptance_criteria>

  <done>turbo run lint executa em todos os 7 pacotes. Erros de lint em src/ corrigidos ou documentados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev tooling → codebase | ESLint lê e analisa o código fonte — ferramenta de build, não afeta runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | .eslintrc.js (root) | accept | ESLint config é dev tooling — não afeta runtime ou dados de produção; risco de supply chain via eslint plugins é baixo para pacotes @typescript-eslint estabelecidos |
| T-05-07 | Denial of Service | turbo run lint em CI | accept | Lint lento pode atrasar CI — aceitável; cache turbo mitiga runs repetidos; sem impacto em produção |
</threat_model>

<verification>
Verificação completa da Phase 5 Plan 02:

```bash
# 1. @typescript-eslint deps no root
grep "@typescript-eslint" /root/Brain/package.json

# 2. Lint script em todos os 7 pacotes
grep -r '"lint": "eslint src/"' /root/Brain/packages/*/package.json

# 3. Scripts existentes preservados em database
grep -E '"db:push"|"db:migrate"|"lint"' /root/Brain/packages/database/package.json

# 4. Nenhum .eslintrc.js por pacote
find /root/Brain/packages -name '.eslintrc.js'

# 5. turbo run lint executa nos 7 pacotes
cd /root/Brain && turbo run lint
```
</verification>

<success_criteria>
- `grep "@typescript-eslint/parser" /root/Brain/package.json` retorna match
- `grep -r '"lint"' /root/Brain/packages/*/package.json | wc -l` retorna 7
- `find /root/Brain/packages -name '.eslintrc.js'` retorna vazio
- `cd /root/Brain && turbo run lint` não exibe "missing script: lint" em nenhum pacote
- Scripts pré-existentes (db:push, db:migrate, test:integration) preservados em seus respectivos package.json
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-transport-foundation/05-02-SUMMARY.md` com:
- Decisões implementadas (D-10, D-11, D-12, D-13, D-14)
- Versões de @typescript-eslint instaladas
- Saída do `turbo run lint` (status final)
- Erros de lint encontrados e corrigidos (se houver)
</output>
