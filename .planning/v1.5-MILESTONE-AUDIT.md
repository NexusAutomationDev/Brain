---
milestone: v1.5
audited: 2026-07-01T22:30:00Z
status: tech_debt
scores:
  requirements: 16/16
  phases: 4/4
  integration: 9/9
  flows: 6/6
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 27-tech-debt-fixes
    items:
      - "WR-01: status='error' always maps to HTTP 503, never 500 — dead code branch"
      - "WR-02: SIGTERM handler leak when BrainRunner.init() called multiple times"
      - "WR-03: RabbitMQ retry map key collision risk between different message types"
      - "IN-01: 'as any' used for LangChain RunnableConfig — proper type exists"
      - "IN-02: fup-e2e.test.ts has implicit ordering dependency between test cases"
      - "IN-03: WebhookTransport.getStatus() always returns connected:true after stop()"
      - "SUMMARY.md frontmatter for plans 27-02/27-03 missing requirements-completed field (TECH-02/TECH-03 undiscoverable via frontmatter scan, only via VERIFICATION.md)"
  - phase: 28-embedding-sdk
    items:
      - "CR-01 (accepted override): migration 0009 hardcodes vector(1536) with no inline warning it's OpenAI-specific; regenerating for a new EMBEDDING_DIMENSIONS requires manually re-adding TRUNCATE — accepted as pre-production tradeoff by biellil 2026-07-01"
      - "WR-01: ingest.ts comment claims EMBEDDING_DIMENSIONS=768 while every other reference uses 1536/3072"
      - "WR-02: LazyEmbeddingProvider.dimensions/providerName return placeholder values before first resolution"
      - "WR-03: reembed.ts issues one UPDATE per row, no upper bound on pages processed"
      - "WR-04: runner.ts dimension-mismatch query has no defensive handling for zero rows/missing relation"
      - "IN-01..04: atttypmod cross-version assumption, search-knowledge.ts doesn't escape/truncate chunk content, EMBEDDING_DIMENSIONS range not cross-checked against Gemini's fixed 3072, duplicate DATABASE_URL check in _compileGraph()"
      - "apps/brain-sdr/.env.example missing EMBEDDING_PROVIDER/EMBEDDING_MODEL/EMBEDDING_DIMENSIONS documentation (brain-support's .env.example has it, brain-sdr's doesn't) — confirmed independently by integration checker"
      - "Pre-existing test-isolation issue: mock.module(\"@brain-pkg/embeddings\") in brain-runner.test.ts pollutes factory.test.ts when run in the same process (3 failures only when combined, 0 when isolated) — systemic project-wide pattern, not phase-28-specific"
      - "Deferred (documented in deferred-items.md): 14 pre-existing lead-service-fup.test.ts failures, confirmed unrelated to phase 28 by stash-and-rerun"
  - phase: 29-brain-suporte-core
    items:
      - "WR-01: 'respond' tool has no append-after-filter protection like search_knowledge — if BRAIN_TOOLS is set without 'respond', every turn silently degrades to fallback response. Inherited unchanged from brain-sdr, affects both apps equally, requires operator misconfiguration to trigger"
      - "IN-01: RESERVED_TOOL_NAMES is a hardcoded literal, not derived from actual tool instances — could go stale silently on a future core refactor"
      - "IN-02: getEmbeddingProvider() module-level memoization is a process-lifetime singleton with no invalidation path"
      - "IN-03: duplicate/inconsistent AI-message type-guard pattern between routeAfterLlm/llm node and respond node"
  - phase: 30-brain-suporte-docker
    items:
      - "WR-01 (in progress): apps/brain-sdr/docker-compose.yml hardcodes host port 3002, colliding with brain-support's own default 3002 if both run on the same host. A fix changing brain-support's default to 3003 is already staged in the working tree (apps/brain-support/docker-compose.yml) but NOT YET COMMITTED — commit this before considering the milestone closed"
      - "IN-01: unquoted $RESPONSE in jq pipe in publish-brain-support.yml (inherited from publish-brain-sdr.yml)"
      - "IN-02: no validation that 'jq -r .url' returned non-null before upload in publish-brain-support.yml (inherited)"
      - "Cosmetic: SUP-08 milestone/requirement text says ToolsRegistry.registerBrainType(...), actual code uses toolsRegistry.enableTool(...) which implicitly registers the brain type — functionally equivalent, naming imprecision only"
nyquist:
  compliant_phases: []
  partial_phases: ["28-embedding-sdk"]
  missing_phases: ["27-tech-debt-fixes", "29-brain-suporte-core", "30-brain-suporte-docker"]
  overall: "partial"
---

# Milestone v1.5 — Audit Report

**Milestone:** v1.5 Embedding SDK + Brain Suporte + Tech Debt (Phases 27-30)
**Audited:** 2026-07-01T22:30:00Z
**Status:** ⚡ tech_debt
**Score:** 16/16 requirements satisfied

---

## Executive Summary

All 4 phases (27, 28, 29, 30) achieved their stated goals and passed individual verification. Cross-phase integration was independently re-checked and confirmed clean: `apps/brain-support` correctly consumes `packages/embeddings` with full ENV isolation from `apps/brain-sdr`, inherits the `BRAIN_TOOLS` whitelist and `/health` transport-status mechanisms from phase 27 without any brain-specific code, and its Dockerfile correctly avoids the exact `packages/embeddings`-omission bug that phase 30 had to retroactively fix in `brain-sdr`'s Dockerfile. The full E2E flow (webhook → BrainRunner → buildGraph → search_knowledge (RAG) → LLM → validated BrainOutput) was traced end-to-end across all 4 phases with no broken links. 123 unit tests pass across the touched packages/apps, and no regressions were found in previously-shipped `brain-sdr`/`packages/core` behavior.

**No blockers.** Two requirements (EMBD-03, SUP-03) and one artifact (`brain-support`'s `docker-compose.yml` shape) carry **explicitly accepted overrides**, documented and approved by biellil in their respective phase VERIFICATION.md files — these are not gaps, they're conscious pre-production tradeoffs.

What remains is accumulated tech debt: 22 review findings (1 critical, accepted via override; 9 warnings; 12 info) across the 4 phases, none of which block any requirement or E2E flow; Nyquist validation coverage is incomplete (only phase 28 has a `VALIDATION.md`, and it's `nyquist_compliant: false`); and — most actionable — a docker-compose.yml port-collision fix for `brain-support` is **staged but not committed**, found live in the working tree during this audit.

---

## Scope

This audit covers Phases 27-30 (v1.5): Tech Debt Fixes, Embedding SDK, Brain Suporte Core, Brain Suporte Docker.

| Phase | Status | Verified |
|-------|--------|----------|
| 27 Tech Debt Fixes | ✓ passed (3rd pass, gaps closed) | 2026-06-30T23:14:32Z |
| 28 Embedding SDK | ✓ passed (1 accepted override) | 2026-07-01T16:07:34Z |
| 29 Brain Suporte Core | ✓ passed (2nd pass, gap closed) | 2026-07-01T21:05:00Z |
| 30 Brain Suporte Docker | ✓ passed (1 accepted override) | 2026-07-01T22:16:31Z |

---

## Requirements Coverage — 3-Source Cross-Reference

Sources: `REQUIREMENTS.md` traceability table, phase `VERIFICATION.md` requirements tables, plan `SUMMARY.md` `requirements-completed` frontmatter.

| Requirement | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|-------------|-------|---------------|----------------------|-------------------|---------------|
| TECH-01 | 27 | passed/SATISFIED | listed (27-01) | [x] | **satisfied** |
| TECH-02 | 27 | passed/SATISFIED | missing (27-02) | [x] | **satisfied** (doc gap — see tech debt) |
| TECH-03 | 27 | passed/SATISFIED | missing (27-03) | [x] | **satisfied** (doc gap) |
| EMBD-01 | 28 | passed/SATISFIED | listed (28-01,03,05) | [x] | **satisfied** |
| EMBD-02 | 28 | passed/SATISFIED | listed (28-01,03,05) | [x] | **satisfied** |
| EMBD-03 | 28 | passed/SATISFIED **(override)** | listed (28-02) | [x] (override) | **satisfied** — accepted override, biellil, 2026-07-01T16:11:14Z |
| EMBD-04 | 28 | passed/SATISFIED | listed (28-01) | [x] | **satisfied** |
| EMBD-05 | 28 | passed/SATISFIED | listed (28-04) | [x] | **satisfied** |
| SUP-01 | 29 | passed/SATISFIED | missing (29-01/02) | [x] | **satisfied** (doc gap) |
| SUP-02 | 29 | passed/SATISFIED (gap closed 29-03) | listed (29-03) | [x] | **satisfied** |
| SUP-03 | 29 | passed/SATISFIED **(override D-01/D-02)** | missing | [x] (override) | **satisfied** — accepted deviation, user-confirmed per 29-CONTEXT.md |
| SUP-04 | 29 | passed/SATISFIED | missing | [x] | **satisfied** (doc gap) |
| SUP-05 | 29 | passed/SATISFIED | missing | [x] | **satisfied** (doc gap) |
| SUP-06 | 30 | passed/SATISFIED **(override)** | listed (30-01,02,03) | [x] | **satisfied** — docker-compose.yml shape override accepted by biellil, 2026-07-01T21:50:51Z |
| SUP-07 | 29 | passed/SATISFIED | missing | [x] | **satisfied** (doc gap) |
| SUP-08 | 29 | passed/SATISFIED | missing | [x] | **satisfied** (doc gap; also cosmetic naming imprecision — see tech debt) |

**Coverage:** 16/16 requirements satisfied. **No unsatisfied requirements. No orphaned requirements** — every REQ-ID in `REQUIREMENTS.md`'s traceability table is backed by at least one phase `VERIFICATION.md` entry with concrete evidence.

**Doc-gap note:** Several `SUMMARY.md` files (27-02, 27-03, 29-01, 29-02) omit the `requirements-completed` frontmatter field that other plans in this milestone use consistently. This does not affect the audit's conclusion (VERIFICATION.md independently confirmed each requirement with direct evidence), but it means an automated frontmatter-only scan would under-count coverage — a future phase should backfill this field for consistency.

---

## Cross-Phase Integration

Independently re-verified by a dedicated integration-checker pass (not just inherited from individual phase verifications):

| Check | Result |
|-------|--------|
| `brain-support` consumes `packages/embeddings` independently of `brain-sdr`, full ENV isolation (`DATABASE_NAME`, `EMBEDDING_*`, `PORT`) | ✓ WIRED |
| `brain-support`'s Dockerfile builds+copies `packages/embeddings` (the exact class of bug phase 30 retroactively fixed in `brain-sdr`) | ✓ WIRED — no regression |
| Phase 27's `BRAIN_TOOLS`/`enabledTools` mechanism threads into `brain-support`'s `buildGraph()` | ✓ WIRED — brain-agnostic by design, `search_knowledge` additionally protected by MCP-collision guard |
| Phase 27's `/health` transport-status field works for `brain-support`, not just `brain-sdr` | ✓ WIRED — shared `@brain-pkg/observability` code, no per-app divergence possible |
| Full E2E flow: webhook → BrainRunner → buildGraph → search_knowledge (RAG) → IEmbeddingProvider → LLM → validated BrainOutput | ✓ COMPLETE — traced call-by-call across all 4 phases |
| Regressions in `brain-sdr`/`packages/core` from phases 28-30 | ✓ NONE FOUND — 123 unit tests pass, 0 fail; full turbo build 10/10 |

**9/9 expected cross-phase connections confirmed working. 0 orphaned exports. 0 unprotected routes** (webhook/ingest/reembed/reload-prompts all token-gated, distinct tokens per app).

### Integration finding requiring action

The integration checker found **`apps/brain-support/docker-compose.yml` staged but not committed** in the working tree: the default host port was changed from `3002` to `3003` to avoid colliding with `apps/brain-sdr/docker-compose.yml`'s hardcoded host port `3002` — a real collision risk under the project's own "N instances of a Brain, multiple Brains per client" deployment model (`CLAUDE.md`). **This fix should be committed before the milestone is considered closed.**

---

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 27-tech-debt-fixes | missing | — | `/gsd-validate-phase 27` if desired |
| 28-embedding-sdk | exists | `nyquist_compliant: false` | `/gsd-validate-phase 28` |
| 29-brain-suporte-core | missing | — | `/gsd-validate-phase 29` if desired |
| 30-brain-suporte-docker | missing | — | `/gsd-validate-phase 30` if desired |

Discovery only — not a blocker. Phases 27, 29, 30 achieved passing `VERIFICATION.md` status through direct evidence-based verification (unit tests, live Docker runs, grep/typecheck confirmation) rather than the formal Nyquist sampling protocol.

---

## Tech Debt Summary

**22 items** across 4 phases (1 critical — accepted via override; 9 warnings; 12 info-level), plus 1 actionable uncommitted fix. None block any requirement or E2E flow. Full detail in frontmatter `tech_debt` block above. Highlights:

- **Actionable now:** commit the staged `apps/brain-support/docker-compose.yml` port fix (3002→3003).
- **Accepted, tracked overrides** (no action needed unless revisited pre-production-client-onboarding): EMBD-03's manual-TRUNCATE-on-regenerate + hardcoded `vector(1536)`; SUP-03's native-closures-instead-of-full-MCP reinterpretation; SUP-06's docker-compose shape (external Postgres, no bundled pgvector service).
- **Worth a follow-up phase before onboarding a real client:** add `EMBEDDING_PROVIDER`/`EMBEDDING_MODEL`/`EMBEDDING_DIMENSIONS` to `apps/brain-sdr/.env.example`; add inline warning comment to `0009_embedding_dimensions_fix.sql`; give `respond` the same append-after-filter protection as `search_knowledge` in both `brain-sdr` and `brain-support` (WR-01/29-REVIEW) so a misconfigured `BRAIN_TOOLS` can't silently degrade every response.
- **Cosmetic/low-priority:** backfill missing `requirements-completed` SUMMARY frontmatter (27-02, 27-03, 29-01, 29-02); `RESERVED_TOOL_NAMES` hardcoded literal; jq shell-hygiene items in `publish-brain-support.yml` (inherited, pre-existing).

---

## Verdict

**Status: tech_debt.** All 16 requirements satisfied, all cross-phase wiring and E2E flows confirmed working, no critical blockers. Accumulated tech debt (mostly informational/warning-level, well-documented, several already accepted as deliberate pre-production tradeoffs) and incomplete Nyquist coverage warrant a review before shipping to a real client, but do not block milestone completion.

---

*Verified: 2026-07-01T22:30:00Z*
*Auditor: Claude (gsd-audit-milestone)*
