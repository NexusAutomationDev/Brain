---
phase: 17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra
plan: 03
type: execute
wave: 3
depends_on:
  - "17-01-PLAN.md"
  - "17-02-PLAN.md"
files_modified:
  - apps/brain-sdr/src/brain.ts
  - apps/brain-echo/src/brain.ts
autonomous: true
requirements:
  - D-06
  - D-07

must_haves:
  truths:
    - "Nó llm de brain-sdr retorna tokenUsage via extractTokenUsage(response) no delta do nó"
    - "Nó llm de brain-echo retorna tokenUsage via extractTokenUsage(response) no delta do nó"
    - "BrainStateAnnotation.tokenUsage acumula todos os LLM calls do turno (comportamento correto para ReAct do SDR)"
    - "Testes existentes de brain-sdr e brain-echo continuam verdes após a mudança"
  artifacts:
    - path: "apps/brain-sdr/src/brain.ts"
      provides: "nó llm retorna tokenUsage: extractTokenUsage(response)"
      contains: "extractTokenUsage"
    - path: "apps/brain-echo/src/brain.ts"
      provides: "nó llm retorna tokenUsage: extractTokenUsage(response)"
      contains: "extractTokenUsage"
  key_links:
    - from: "apps/brain-sdr/src/brain.ts"
      to: "packages/ai/src/utils/token.ts"
      via: "import { extractTokenUsage } from '@brain-pkg/ai'"
      pattern: "extractTokenUsage.*@brain-pkg/ai"
    - from: "apps/brain-echo/src/brain.ts"
      to: "packages/ai/src/utils/token.ts"
      via: "import { extractTokenUsage } from '@brain-pkg/ai'"
      pattern: "extractTokenUsage.*@brain-pkg/ai"
---

<objective>
Integrar extractTokenUsage() nos nós llm de brain-sdr e brain-echo para que cada LLM call contribua com seu delta de tokens ao acumulador do BrainState. Esta é a Wave 3 — conecta os Brains ao acumulador criado na Wave 1.

Purpose: Sem esta etapa, BrainStateAnnotation.tokenUsage permanece com zeros mesmo após invoke() — o acumulador existe mas nenhum nó o alimenta. Os nós llm de cada Brain são os únicos responsáveis por retornar tokenUsage delta (ToolNode só retorna ToolMessage, não AIMessage com usage_metadata — RESEARCH.md anti-pattern).

Output: brain-sdr e brain-echo com nó llm emitindo tokenUsage delta. O BrainRunner (Wave 2) já extrai state.tokenUsage corretamente — esta wave fecha o ciclo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-CONTEXT.md
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-RESEARCH.md
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-01-SUMMARY.md
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-02-SUMMARY.md

<interfaces>
<!-- Contratos criados nas Waves 1 e 2. Ler os arquivos reais antes de editar. -->

De packages/ai/src/index.ts (Wave 1 exportou extractTokenUsage):
```typescript
export { extractTokenUsage } from "./utils/token.js";
```

De packages/ai/src/utils/token.ts (Wave 1):
```typescript
export function extractTokenUsage(response: AIMessage): TokenUsage;
// response.usage_metadata undefined/null → { inputTokens: 0, outputTokens: 0, totalTokens: 0 }
// response.usage_metadata presente → converte snake_case para camelCase
```

De apps/brain-sdr/src/brain.ts — nó llm ATUAL (retorno sem tokenUsage):
```typescript
.addNode("llm", async (state) => {
  const messagesForLLM = state.messages.slice(-getContextWindow());
  const response = await llmWithTools.invoke([
    { role: "system", content: ctx.prompts["system"] },
    ...messagesForLLM,
  ]);
  const fullResponse = typeof response.content === "string" ? response.content : "";
  return {
    messages: [response],
    brainOutput: { fullResponse, responseMode: "text" as const },
    // ADICIONAR: tokenUsage: extractTokenUsage(response),
  };
})
```

De apps/brain-echo/src/brain.ts — nó llm ATUAL (retorno sem tokenUsage):
```typescript
.addNode("llm", async (state) => {
  // ...
  const response = await ctx.llm.invoke([...]);
  const fullResponse = typeof response.content === "string" ? response.content : "";
  return {
    messages: [...state.messages, response],
    brainOutput: { fullResponse, responseMode: "text" as const },
    // ADICIONAR: tokenUsage: extractTokenUsage(response),
  };
})
```

IMPORTANTE — anti-pattern documentado em RESEARCH.md:
- NÃO adicionar extractTokenUsage() ao nó "tools" (ToolNode) — ToolNode retorna ToolMessage, não AIMessage
- NÃO tentar capturar tokens do ToolNode — apenas o nó "llm" emite AIMessage com usage_metadata
- NÃO importar extractTokenUsage de outro lugar que não @brain-pkg/ai (CLAUDE.md: código reutilizável vai em packages/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar extractTokenUsage no nó llm de brain-sdr</name>
  <files>apps/brain-sdr/src/brain.ts</files>
  <read_first>
    - apps/brain-sdr/src/brain.ts (ler COMPLETO antes de editar — especialmente imports no topo e return do nó "llm")
    - packages/ai/src/index.ts (confirmar que extractTokenUsage está re-exportado após Wave 1)
  </read_first>
  <action>
**Editar `apps/brain-sdr/src/brain.ts`:**

1. Adicionar `extractTokenUsage` ao import de `@brain-pkg/ai` (linha que já importa `BrainStateAnnotation`):
```typescript
// ANTES:
import { BrainStateAnnotation } from "@brain-pkg/ai";
// DEPOIS:
import { BrainStateAnnotation, extractTokenUsage } from "@brain-pkg/ai";
```

2. No retorno do nó "llm", adicionar `tokenUsage` após `brainOutput`:
```typescript
// ANTES:
return {
  messages: [response],
  brainOutput: { fullResponse, responseMode: "text" as const },
};
// DEPOIS:
return {
  messages: [response],
  brainOutput: { fullResponse, responseMode: "text" as const },
  tokenUsage: extractTokenUsage(response),  // D-07: delta do LLM call atual
};
```

IMPORTANTE: Adicionar SOMENTE no nó "llm" — NÃO adicionar no ToolNode (anti-pattern documentado).

O nó "tools" (.addNode("tools", new ToolNode([...]))) não muda — ToolNode retorna ToolMessage, não tem usage_metadata.

Para o Brain SDR com ReAct (llm → tools → llm → __end__), o reducer de soma no BrainStateAnnotation acumulará automaticamente o tokenUsage de TODAS as passagens pelo nó "llm".
  </action>
  <verify>
    <automated>cd /root/Brain && grep -n "extractTokenUsage" apps/brain-sdr/src/brain.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/brain-sdr/src/brain.ts` importa `extractTokenUsage` de `@brain-pkg/ai`
    - `apps/brain-sdr/src/brain.ts` contém `tokenUsage: extractTokenUsage(response),` no retorno do nó "llm"
    - `apps/brain-sdr/src/brain.ts` NÃO tem `extractTokenUsage` associado ao ToolNode
    - O arquivo TypeScript compila sem erros: `cd /root/Brain/apps/brain-sdr && bun build src/brain.ts --target bun 2>&1 | grep -c "error"` retorna 0
  </acceptance_criteria>
  <done>brain-sdr nó llm emite tokenUsage delta em cada LLM call (D-06, D-07). ReAct multi-step acumula via reducer.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar extractTokenUsage no nó llm de brain-echo e verificar suite completa</name>
  <files>apps/brain-echo/src/brain.ts</files>
  <read_first>
    - apps/brain-echo/src/brain.ts (ler COMPLETO antes de editar — especialmente imports e return do nó "llm")
    - packages/ai/src/index.ts (confirmar que extractTokenUsage está re-exportado)
  </read_first>
  <action>
**Editar `apps/brain-echo/src/brain.ts`:**

1. Adicionar `extractTokenUsage` ao import de `@brain-pkg/ai` (linha que já importa `BrainStateAnnotation`):
```typescript
// ANTES:
import { BrainStateAnnotation } from "@brain-pkg/ai";
// DEPOIS:
import { BrainStateAnnotation, extractTokenUsage } from "@brain-pkg/ai";
```

2. No retorno do nó "llm", adicionar `tokenUsage` após `brainOutput`:
```typescript
// ANTES:
return {
  messages: [...state.messages, response],
  brainOutput: {
    fullResponse,
    responseMode: "text" as const,
  },
};
// DEPOIS:
return {
  messages: [...state.messages, response],
  brainOutput: {
    fullResponse,
    responseMode: "text" as const,
  },
  tokenUsage: extractTokenUsage(response),  // D-07: delta do LLM call atual
};
```

IMPORTANTE: brain-echo tem apenas 1 nó (llm) e 0 tools — sem ReAct. O tokenUsage de um único LLM call será o valor final em state.tokenUsage. Pitfall 2 do RESEARCH.md: o default de zeros no Annotation garante que se o provider não reportar usage_metadata, o campo ainda existe com zeros (não undefined).

Após editar brain-echo, executar a suite completa para verificar que nenhuma mudança das 3 waves introduziu regressão:
```bash
cd /root/Brain && bun test packages/shared packages/ai packages/core packages/transport
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/shared packages/ai packages/core packages/transport</automated>
  </verify>
  <acceptance_criteria>
    - `apps/brain-echo/src/brain.ts` importa `extractTokenUsage` de `@brain-pkg/ai`
    - `apps/brain-echo/src/brain.ts` contém `tokenUsage: extractTokenUsage(response),` no retorno do nó "llm"
    - `bun test packages/shared packages/ai packages/core packages/transport` exits 0 — ZERO falhas
    - `grep -rn "extractTokenUsage" apps/brain-sdr/src/brain.ts apps/brain-echo/src/brain.ts` retorna 2 linhas (uma por arquivo)
    - Nenhum arquivo contém `extractTokenUsage` associado a ToolNode ou nó "tools"
  </acceptance_criteria>
  <done>brain-echo nó llm emite tokenUsage delta (D-06, D-07). Suite completa de packages verdes — feature de contagem de tokens implementada end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM Provider → AIMessage.usage_metadata nos nós Brain | Dados de token são internos à resposta do LLM — não cruzam fronteira de confiança externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering | `extractTokenUsage(response)` em brain-sdr/brain-echo | accept | `response` é o objeto AIMessage retornado pelo LLM provider via @langchain/core — não modificável por input externo do usuário; `usage_metadata` é campo read-only do provider |
| T-17-08 | Denial of Service | ToolNode + extractTokenUsage misuse | mitigate | Anti-pattern documentado: extractTokenUsage NÃO deve ser chamado em ToolNode (retorna ToolMessage, não AIMessage — TypeScript error em tempo de compilação se tentado); instruções explícitas no plano |
</threat_model>

<verification>
Suite completa final — TODOS os pacotes modificados nas 3 waves:

```bash
cd /root/Brain && bun test packages/shared packages/ai packages/core packages/transport
```

Verificações adicionais de integridade estrutural:
```bash
# Confirmar extractTokenUsage em ambos os Brains
grep -rn "extractTokenUsage" /root/Brain/apps/brain-sdr/src/brain.ts /root/Brain/apps/brain-echo/src/brain.ts

# Confirmar que BrainOutput não foi modificado (D-01)
grep -n "export interface BrainOutput" /root/Brain/packages/shared/src/types/index.ts

# Confirmar que TokenUsage foi adicionado
grep -n "export interface TokenUsage" /root/Brain/packages/shared/src/types/index.ts

# Confirmar que BrainOutputSchema Zod NÃO foi modificado (D-01)
grep -n "tokenUsage" /root/Brain/packages/core/src/output/schema.ts
# Deve retornar VAZIO — tokenUsage não está no schema Zod
```
</verification>

<success_criteria>
- `apps/brain-sdr/src/brain.ts` nó llm retorna `tokenUsage: extractTokenUsage(response)`
- `apps/brain-echo/src/brain.ts` nó llm retorna `tokenUsage: extractTokenUsage(response)`
- `bun test packages/shared packages/ai packages/core packages/transport` exits 0 (zero falhas)
- `packages/core/src/output/schema.ts` NÃO contém `tokenUsage` (D-01 preservado)
- Ciclo completo implementado: LLM call → extractTokenUsage → BrainState.tokenUsage (reducer de soma) → BrainRunner.run() wrapper → HTTP response JSON / RabbitMQ log
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-03-SUMMARY.md`
</output>
