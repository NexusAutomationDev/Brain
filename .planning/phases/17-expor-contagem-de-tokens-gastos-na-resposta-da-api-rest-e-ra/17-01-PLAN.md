---
phase: 17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - packages/ai/src/utils/token.ts
  - packages/ai/src/graph/state.ts
  - packages/ai/src/index.ts
  - packages/ai/src/__tests__/unit/token.test.ts
  - packages/ai/src/__tests__/unit/state-token.test.ts
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07

must_haves:
  truths:
    - "TokenUsage type está exportado de @brain-pkg/shared com campos inputTokens, outputTokens, totalTokens"
    - "extractTokenUsage() converte AIMessage.usage_metadata snake_case para camelCase e retorna zeros quando usage_metadata é undefined"
    - "BrainStateAnnotation.tokenUsage usa reducer de soma — múltiplas passagens pelo nó llm acumulam corretamente"
    - "BrainState type inclui campo tokenUsage (type alias atualizado automaticamente)"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "export interface TokenUsage"
      contains: "TokenUsage"
    - path: "packages/ai/src/utils/token.ts"
      provides: "extractTokenUsage() helper"
      exports: ["extractTokenUsage"]
    - path: "packages/ai/src/graph/state.ts"
      provides: "BrainStateAnnotation com campo tokenUsage e reducer de soma"
      contains: "tokenUsage"
    - path: "packages/ai/src/index.ts"
      provides: "re-exporta extractTokenUsage"
      contains: "extractTokenUsage"
    - path: "packages/ai/src/__tests__/unit/token.test.ts"
      provides: "testes TOK-01 e TOK-02"
    - path: "packages/ai/src/__tests__/unit/state-token.test.ts"
      provides: "teste TOK-03 — reducer de soma"
  key_links:
    - from: "packages/ai/src/utils/token.ts"
      to: "packages/shared/src/types/index.ts"
      via: "import type { TokenUsage }"
      pattern: "import.*TokenUsage.*@brain-pkg/shared"
    - from: "packages/ai/src/graph/state.ts"
      to: "packages/shared/src/types/index.ts"
      via: "import type { TokenUsage }"
      pattern: "tokenUsage.*Annotation<TokenUsage>"
    - from: "packages/ai/src/index.ts"
      to: "packages/ai/src/utils/token.ts"
      via: "re-export extractTokenUsage"
      pattern: "extractTokenUsage.*from.*utils/token"
---

<objective>
Criar o tipo TokenUsage em packages/shared, implementar o helper extractTokenUsage em packages/ai, e adicionar o campo tokenUsage com reducer de soma ao BrainStateAnnotation. Esta é a Wave 1 — a base de dados que todas as outras waves consomem.

Purpose: Sem esses três artefatos (tipo, helper, acumulador no estado), nenhum nó de Brain pode capturar tokens e nenhum transport pode expô-los. D-01 é respeitado (BrainOutput não muda), D-03 a D-07 são implementados aqui.

Output: TokenUsage type, extractTokenUsage helper exportado de @brain-pkg/ai, BrainStateAnnotation.tokenUsage com reducer de soma, testes verdes para TOK-01/02/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-CONTEXT.md
@.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor DEVE respeitar. Extraídos do codebase. -->

De packages/shared/src/types/index.ts (NÃO modificar BrainOutput — D-01):
```typescript
export type ResponseMode = "text" | "image" | "audio" | "video" | "document";

export interface BrainOutput {
  fullResponse: string;
  responseMode: ResponseMode;
  mediaType?: string;
  mediaUrl?: string;
}
// ADICIONAR após BrainOutput: export interface TokenUsage { ... }
```

De packages/ai/src/graph/state.ts (padrão de Annotation — replicar para tokenUsage):
```typescript
import { Annotation, messagesStateReducer } from "@langchain/langgraph";
import type { BaseMessage } from "@langchain/core/messages";
import type { BrainOutput } from "@brain-pkg/shared";

export const BrainStateAnnotation = Annotation.Root({
  schema_version: Annotation<number>({
    default: () => 1,      // D-RESEARCH: incrementar para 2 com adição de tokenUsage
    reducer: (_, next) => next,
  }),
  // ... (outros campos existentes — NÃO remover)
  brainOutput: Annotation<BrainOutput | null>({
    default: () => null,
    reducer: (_, next) => next,
  }),
  // ADICIONAR: tokenUsage com reducer de soma (padrão diferente de last-write-wins)
});
```

De packages/ai/src/index.ts (barrel — adicionar re-export de extractTokenUsage):
```typescript
export { BrainStateAnnotation } from "./graph/state.js";
export type { BrainState } from "./graph/state.js";
// ... outros exports existentes
// ADICIONAR: export { extractTokenUsage } from "./utils/token.js";
```

Tipo LangChain disponível em @langchain/core 1.1.48 (já instalado):
```typescript
// AIMessage.usage_metadata (snake_case — deve ser convertido para camelCase D-04):
// meta.input_tokens  → inputTokens
// meta.output_tokens → outputTokens
// meta.total_tokens  → totalTokens
import type { AIMessage } from "@langchain/core/messages";
// usage_metadata pode ser undefined (D-05: retornar zeros nesse caso)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar TokenUsage type em packages/shared/src/types/index.ts</name>
  <files>packages/shared/src/types/index.ts</files>
  <read_first>
    - packages/shared/src/types/index.ts (ler estado atual ANTES de qualquer edição — nunca assumir conteúdo)
  </read_first>
  <behavior>
    - Test 1: `TokenUsage` é exportado do módulo `@brain-pkg/shared`
    - Test 2: `TokenUsage` tem exatamente os campos `inputTokens: number`, `outputTokens: number`, `totalTokens: number`
    - Test 3: `BrainOutput` continua exportado sem mudança (D-01)
  </behavior>
  <action>
Adicionar ao final de `packages/shared/src/types/index.ts`, após a interface `BrainOutput`:

```typescript
/**
 * Contagem de tokens consumidos em um turno de conversa.
 * Soma de todos os LLM calls no turno (D-06).
 * Zeros explícitos quando o provider não reporta tokens (D-05).
 */
export interface TokenUsage {
  inputTokens: number;
  outputTokens: number;
  totalTokens: number;
}
```

NÃO modificar `BrainOutput`, `ResponseMode`, nem qualquer outro export existente (D-01).

Nota: packages/shared não tem arquivo de teste próprio para tipos. A verificação de tipo é feita pelo teste em packages/ai/src/__tests__/unit/token.test.ts (Wave 0).
  </action>
  <verify>
    <automated>cd /root/Brain && grep -n "export interface TokenUsage" packages/shared/src/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/types/index.ts` contém a linha `export interface TokenUsage {`
    - `packages/shared/src/types/index.ts` contém `inputTokens: number;`
    - `packages/shared/src/types/index.ts` contém `outputTokens: number;`
    - `packages/shared/src/types/index.ts` contém `totalTokens: number;`
    - `packages/shared/src/types/index.ts` ainda contém `export interface BrainOutput {` (D-01 — não removida)
    - `cd /root/Brain/packages/shared && bun build src/types/index.ts --target bun` exits 0 (sem erros de compilação TypeScript)
  </acceptance_criteria>
  <done>Interface TokenUsage exportada de @brain-pkg/shared com campos camelCase corretos (D-03, D-04). BrainOutput inalterada (D-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar helper extractTokenUsage e testes TOK-01/TOK-02</name>
  <files>
    packages/ai/src/utils/token.ts
    packages/ai/src/__tests__/unit/token.test.ts
  </files>
  <read_first>
    - packages/shared/src/types/index.ts (confirmar que TokenUsage foi criada na Task 1)
    - packages/ai/src/graph/state.ts (verificar padrão de imports do pacote)
    - packages/ai/src/llm/factory.test.ts (ver padrão de testes unitários do pacote ai)
  </read_first>
  <behavior>
    - Test TOK-01: `extractTokenUsage` é exportada de `packages/ai/src/utils/token.ts`
    - Test TOK-02a: `extractTokenUsage` com `usage_metadata = undefined` retorna `{ inputTokens: 0, outputTokens: 0, totalTokens: 0 }`
    - Test TOK-02b: `extractTokenUsage` com `usage_metadata = { input_tokens: 10, output_tokens: 5, total_tokens: 15 }` retorna `{ inputTokens: 10, outputTokens: 5, totalTokens: 15 }` (conversão snake_case → camelCase D-04)
    - Test TOK-02c: `extractTokenUsage` com `usage_metadata = null` retorna zeros (D-05)
  </behavior>
  <action>
**Criar `packages/ai/src/utils/token.ts`:**

```typescript
import type { AIMessage } from "@langchain/core/messages";
import type { TokenUsage } from "@brain-pkg/shared";

/**
 * Extrai dados de consumo de tokens de um AIMessage.
 *
 * D-04: Converte snake_case do LangChain (input_tokens, output_tokens, total_tokens)
 * para camelCase do projeto (inputTokens, outputTokens, totalTokens).
 *
 * D-05: Retorna zeros explícitos quando o provider não reporta usage_metadata.
 * Nunca retorna undefined — contrato da resposta HTTP é previsível.
 *
 * Anti-pattern: NÃO usar mergeUsageMetadata() do LangChain no reducer —
 * essa função opera em snake_case; o projeto usa camelCase (RESEARCH.md).
 */
export function extractTokenUsage(response: AIMessage): TokenUsage {
  const meta = response.usage_metadata;
  if (!meta) {
    // D-05: zeros explícitos quando provider não reporta tokens
    return { inputTokens: 0, outputTokens: 0, totalTokens: 0 };
  }
  return {
    inputTokens: meta.input_tokens,
    outputTokens: meta.output_tokens,
    totalTokens: meta.total_tokens,
  };
}
```

**Criar `packages/ai/src/__tests__/unit/token.test.ts`:**

Verificar primeiro se o diretório `packages/ai/src/__tests__/unit/` existe; se não, criar com `mkdir -p`.

```typescript
import { describe, it, expect } from "bun:test";
import { extractTokenUsage } from "../../utils/token.js";
import type { AIMessage } from "@langchain/core/messages";

describe("extractTokenUsage (TOK-01, TOK-02)", () => {
  it("TOK-01: extractTokenUsage is exported from utils/token", () => {
    expect(typeof extractTokenUsage).toBe("function");
  });

  it("TOK-02a: returns zeros when usage_metadata is undefined (D-05)", () => {
    // AIMessage without usage_metadata — mock the shape
    const msg = { usage_metadata: undefined } as unknown as AIMessage;
    const result = extractTokenUsage(msg);
    expect(result).toEqual({ inputTokens: 0, outputTokens: 0, totalTokens: 0 });
  });

  it("TOK-02b: converts snake_case to camelCase (D-04)", () => {
    const msg = {
      usage_metadata: { input_tokens: 10, output_tokens: 5, total_tokens: 15 },
    } as unknown as AIMessage;
    const result = extractTokenUsage(msg);
    expect(result).toEqual({ inputTokens: 10, outputTokens: 5, totalTokens: 15 });
  });

  it("TOK-02c: returns zeros when usage_metadata is null (D-05)", () => {
    const msg = { usage_metadata: null } as unknown as AIMessage;
    const result = extractTokenUsage(msg);
    expect(result).toEqual({ inputTokens: 0, outputTokens: 0, totalTokens: 0 });
  });

  it("TOK-02d: sums correctly with non-zero values", () => {
    const msg = {
      usage_metadata: { input_tokens: 512, output_tokens: 128, total_tokens: 640 },
    } as unknown as AIMessage;
    const result = extractTokenUsage(msg);
    expect(result.inputTokens).toBe(512);
    expect(result.outputTokens).toBe(128);
    expect(result.totalTokens).toBe(640);
  });
});
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/ai/src/__tests__/unit/token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ai/src/utils/token.ts` existe e contém `export function extractTokenUsage`
    - `packages/ai/src/__tests__/unit/token.test.ts` existe com 4+ testes
    - `bun test packages/ai/src/__tests__/unit/token.test.ts` exits 0 — todos os testes passam
    - O arquivo token.ts importa `AIMessage` de `@langchain/core/messages` e `TokenUsage` de `@brain-pkg/shared`
    - O arquivo contém o guard `if (!meta)` retornando zeros (D-05)
  </acceptance_criteria>
  <done>Helper extractTokenUsage criado com testes verdes cobrindo TOK-01 e TOK-02. Converte snake_case para camelCase (D-04). Retorna zeros para undefined/null (D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar tokenUsage ao BrainStateAnnotation, exportar helper e atualizar state.test.ts</name>
  <files>
    packages/ai/src/graph/state.ts
    packages/ai/src/index.ts
    packages/ai/src/graph/state.test.ts
    packages/ai/src/__tests__/unit/state-token.test.ts
  </files>
  <read_first>
    - packages/ai/src/graph/state.ts (ler ANTES de editar — verificar campos existentes e schema_version)
    - packages/ai/src/graph/state.test.ts (ler ANTES de editar — entender o teste _typeCheck existente)
    - packages/ai/src/index.ts (ler ANTES de editar — barrel atual)
    - packages/shared/src/types/index.ts (confirmar que TokenUsage existe)
  </read_first>
  <behavior>
    - Test TOK-03a: `BrainStateAnnotation.spec` contém campo `tokenUsage`
    - Test TOK-03b: reducer de soma acumula tokens de dois nós llm distintos (ex: 10+5=15 totalTokens)
    - Test TOK-03c: default do campo tokenUsage é `{ inputTokens: 0, outputTokens: 0, totalTokens: 0 }` (não null)
    - Test BrainState _typeCheck: incluir `tokenUsage` no tipo (atualizar teste existente)
  </behavior>
  <action>
**Editar `packages/ai/src/graph/state.ts`:**

1. Adicionar import do TokenUsage no topo:
```typescript
import type { TokenUsage } from "@brain-pkg/shared";
```
(BrainOutput já é importado — adicionar TokenUsage ao mesmo import ou em import separado)

2. Incrementar `schema_version` default de 1 para 2 (AI-03: "Increment when shape changes"):
```typescript
schema_version: Annotation<number>({
  default: () => 2,  // incrementado: tokenUsage adicionado ao shape
  reducer: (_, next) => next,
}),
```

3. Adicionar campo `tokenUsage` ao `Annotation.Root({...})` APÓS o campo `brainOutput`:
```typescript
// D-06, D-07: tokenUsage — acumulador de tokens por turno
// Reducer de soma (diferente de last-write-wins): cada nó llm retorna delta,
// BrainStateAnnotation acumula todos os LLM calls do turno.
// Default: zeros (não null) — garante que state.tokenUsage nunca é undefined após invoke() (Pitfall 2)
tokenUsage: Annotation<TokenUsage>({
  default: () => ({ inputTokens: 0, outputTokens: 0, totalTokens: 0 }),
  reducer: (prev, next) => ({
    inputTokens: (prev?.inputTokens ?? 0) + (next?.inputTokens ?? 0),
    outputTokens: (prev?.outputTokens ?? 0) + (next?.outputTokens ?? 0),
    totalTokens: (prev?.totalTokens ?? 0) + (next?.totalTokens ?? 0),
  }),
}),
```

**Editar `packages/ai/src/index.ts`:**

Adicionar ao barrel, após os exports existentes de graph/state:
```typescript
// Token usage helper — D-07
export { extractTokenUsage } from "./utils/token.js";
```

**Editar `packages/ai/src/graph/state.test.ts`:**

Atualizar o teste `"BrainState type is exported"` para incluir `tokenUsage` no `_typeCheck`:
```typescript
it("BrainState type is exported", () => {
  const _typeCheck: BrainState = {
    schema_version: 1,
    messages: [],
    userId: "u1",
    sessionId: "s1",
    brainOutput: null,
    tokenUsage: { inputTokens: 0, outputTokens: 0, totalTokens: 0 },
  };
  expect(_typeCheck.schema_version).toBe(1);
});
```

Adicionar também teste do campo:
```typescript
it("state schema contains tokenUsage field", () => {
  const state = BrainStateAnnotation.spec;
  expect("tokenUsage" in state).toBe(true);
});
```

**Criar `packages/ai/src/__tests__/unit/state-token.test.ts`:**

```typescript
import { describe, it, expect } from "bun:test";
import { StateGraph } from "@langchain/langgraph";
import { BrainStateAnnotation } from "../../graph/state.js";

describe("BrainStateAnnotation.tokenUsage reducer (TOK-03)", () => {
  it("TOK-03a: tokenUsage field exists in spec", () => {
    expect("tokenUsage" in BrainStateAnnotation.spec).toBe(true);
  });

  it("TOK-03b: default tokenUsage is zeros (not null)", async () => {
    const graph = new StateGraph(BrainStateAnnotation)
      .addNode("noop", (_state) => ({}))
      .addEdge("__start__", "noop")
      .addEdge("noop", "__end__")
      .compile();

    const result = await graph.invoke({});
    expect(result.tokenUsage).toEqual({ inputTokens: 0, outputTokens: 0, totalTokens: 0 });
  });

  it("TOK-03c: reducer sums tokenUsage across two llm node passes (D-06)", async () => {
    let pass = 0;
    const graph = new StateGraph(BrainStateAnnotation)
      .addNode("llm", (_state) => {
        pass++;
        // Simula dois passes pelo nó llm (padrão ReAct do Brain SDR)
        if (pass === 1) {
          return { tokenUsage: { inputTokens: 100, outputTokens: 50, totalTokens: 150 } };
        }
        return { tokenUsage: { inputTokens: 200, outputTokens: 80, totalTokens: 280 } };
      })
      .addNode("noop", (_state) => ({}))
      .addEdge("__start__", "llm")
      .addEdge("llm", "noop")
      .addEdge("noop", "__end__")
      .compile();

    // Nota: Para testar 2 passes, criamos um grafo que vai por "llm" duas vezes
    // via loop manual simulado — mais simples que condicional
    const result1 = await graph.invoke({});
    // Verificar que acumulou o primeiro pass
    expect(result1.tokenUsage.inputTokens).toBe(100);

    // Teste de acumulação via reducer direto: simular dois updates
    const graph2 = new StateGraph(BrainStateAnnotation)
      .addNode("llm1", (_state) => ({
        tokenUsage: { inputTokens: 100, outputTokens: 50, totalTokens: 150 },
      }))
      .addNode("llm2", (_state) => ({
        tokenUsage: { inputTokens: 200, outputTokens: 80, totalTokens: 280 },
      }))
      .addEdge("__start__", "llm1")
      .addEdge("llm1", "llm2")
      .addEdge("llm2", "__end__")
      .compile();

    const result2 = await graph2.invoke({});
    expect(result2.tokenUsage.inputTokens).toBe(300);   // 100 + 200
    expect(result2.tokenUsage.outputTokens).toBe(130);  // 50 + 80
    expect(result2.tokenUsage.totalTokens).toBe(430);   // 150 + 280
  });
});
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/ai/src/graph/state.test.ts packages/ai/src/__tests__/unit/state-token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ai/src/graph/state.ts` contém `tokenUsage: Annotation<TokenUsage>({`
    - `packages/ai/src/graph/state.ts` contém `reducer: (prev, next) => ({` com soma explícita
    - `packages/ai/src/graph/state.ts` tem `default: () => ({ inputTokens: 0, outputTokens: 0, totalTokens: 0 })`
    - `packages/ai/src/graph/state.ts` incrementou schema_version default para 2
    - `packages/ai/src/index.ts` contém `export { extractTokenUsage } from "./utils/token.js"`
    - `bun test packages/ai/src/graph/state.test.ts packages/ai/src/__tests__/unit/state-token.test.ts` exits 0
    - Teste `"TOK-03c"` valida que dois nós llm acumulam 300 inputTokens
  </acceptance_criteria>
  <done>BrainStateAnnotation.tokenUsage com reducer de soma implementado (D-06, D-07). extractTokenUsage re-exportado de packages/ai. state.test.ts e state-token.test.ts verdes (TOK-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM Provider → AIMessage.usage_metadata | Dados de token vêm de resposta interna do LLM (confiável) — não de input do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `extractTokenUsage` | accept | `usage_metadata` é lido da resposta do LLM provider (resposta interna), não de input externo — sem vetor de tampering do usuário |
| T-17-02 | Information Disclosure | `token.test.ts` | accept | Testes expõem apenas números (sem PII, sem secrets) — risco nulo |
| T-17-03 | Tampering | `BrainStateAnnotation.tokenUsage` reducer | accept | Reducer opera sobre dados do estado interno do LangGraph — sem superfície de ataque externa. Default de zeros evita NaN ou undefined (Pitfall 2) |
</threat_model>

<verification>
Após todas as tasks desta wave:

```bash
cd /root/Brain && bun test packages/shared packages/ai
```

Deve retornar 0 falhas. Verificar especificamente:
- `state.test.ts` — _typeCheck inclui tokenUsage, campo presente no spec
- `state-token.test.ts` — reducer de soma com 300 inputTokens acumulados
- `token.test.ts` — TOK-01, TOK-02a/b/c/d verdes
</verification>

<success_criteria>
- `export interface TokenUsage` em packages/shared/src/types/index.ts
- `extractTokenUsage()` em packages/ai/src/utils/token.ts com guard de undefined/null
- `tokenUsage: Annotation<TokenUsage>` com reducer de soma em BrainStateAnnotation
- `extractTokenUsage` re-exportado de packages/ai/src/index.ts
- `bun test packages/shared packages/ai` exits 0
- BrainOutput NÃO modificado (D-01 respeitado)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/17-expor-contagem-de-tokens-gastos-na-resposta-da-api-rest-e-ra/17-01-SUMMARY.md`
</output>
