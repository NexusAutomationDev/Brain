---
phase: 03-brain-sdk
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/server.ts
  - packages/transport/src/webhook/handler.ts
autonomous: false
requirements:
  - SDK-01
  - SDK-02
  - SDK-03
  - SDK-04

must_haves:
  truths:
    - "packages/core/src/index.ts exporta IBrain, BrainBuildContext, BrainRegistry, BrainRunner, ToolsRegistry, loadPrompts"
    - "Endpoint POST /reload-prompts existe em packages/core/src/server.ts protegido por X-Admin-Token"
    - "POST /reload-prompts retorna 401 quando X-Admin-Token ausente ou incorreto"
    - "POST /reload-prompts retorna 200 e chama runner.refreshPrompts() quando autenticado"
    - "packages/transport/src/webhook/handler.ts faz wiring com BrainRunner e retorna { status: 'ok', reply: string }"
    - "Usuário confirma que o barrel export e endpoints funcionam corretamente via bun test"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Barrel export de packages/core"
      exports: ["IBrain", "BrainBuildContext", "BrainRegistry", "BrainRunner", "BrainRunnerOptions", "ToolsRegistry", "loadPrompts"]
    - path: "packages/core/src/server.ts"
      provides: "createCoreApp(runner) — Hono app com /reload-prompts"
      exports: ["createCoreApp"]
    - path: "packages/transport/src/webhook/handler.ts"
      provides: "WebhookHandler com wiring para BrainRunner"
  key_links:
    - from: "packages/core/src/server.ts"
      to: "packages/core/src/runner/runner.ts"
      via: "BrainRunner.refreshPrompts() chamado no handler"
      pattern: "runner.refreshPrompts()"
    - from: "packages/transport/src/webhook/handler.ts"
      to: "packages/core/src/runner/runner.ts"
      via: "runner.run(event) retorna { reply: string }"
      pattern: "runner.run"
---

<objective>
Wave 3: Criar barrel export de packages/core, endpoint /reload-prompts autenticado, e completar wiring do WebhookHandler com BrainRunner.

Purpose: Finalizar a entrega do SDK — packages/core exporta sua API pública, /reload-prompts permite hot-reload de prompts, e o WebhookHandler (que estava aguardando Phase 3 desde Phase 2) é conectado ao BrainRunner. Checkpoint humano verifica o estado final.

Output: packages/core pronto para uso por apps, /reload-prompts funcional, transport wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-brain-sdk/03-CONTEXT.md
@.planning/phases/03-brain-sdk/03-RESEARCH.md
@.planning/phases/03-brain-sdk/03-03-SUMMARY.md

<interfaces>
<!-- APIs a exportar e consumir -->

De packages/observability/src/server.ts (padrão de createHealthApp — não modificar):
```typescript
export function createHealthApp(sql: Sql): Hono;
export function startServer(sql: Sql, port?: number): void;
```

De packages/transport/src/webhook/handler.ts (wiring point de Phase 3 — comentário existente):
```
// Event dispatching will be wired in Phase 3 (BrainRunner)
const _event: BrainEvent = parsed.data;
```

De packages/core/src/runner/runner.ts (criado no Plano 03):
```typescript
export class BrainRunner {
  async run(event: BrainEvent): Promise<{ reply: string }>;
  async refreshPrompts(): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export packages/core e endpoint /reload-prompts</name>
  <files>
    packages/core/src/index.ts
    packages/core/src/server.ts
  </files>
  <read_first>
    - /root/Brain/packages/ai/src/index.ts (padrão de barrel export)
    - /root/Brain/packages/observability/src/server.ts (padrão de createHealthApp — NÃO modificar este arquivo)
    - /root/Brain/packages/core/src/runner/runner.ts (BrainRunner API — criado no Plano 03)
    - /root/Brain/packages/core/src/brain/interface.ts (IBrain, BrainBuildContext exports)
    - /root/Brain/packages/core/src/brain/registry.ts (BrainRegistry export)
    - /root/Brain/packages/core/src/tools/registry.ts (ToolsRegistry export)
    - /root/Brain/packages/core/src/prompts/loader.ts (loadPrompts export)
  </read_first>
  <action>
**1. Criar `packages/core/src/index.ts`** — barrel export seguindo padrão dos outros packages:

```typescript
// packages/core — public API barrel export
// SDK-01: IBrain contract
export type { IBrain, BrainBuildContext } from "./brain/interface.js";
export { BrainRegistry } from "./brain/registry.js";

// SDK-02: BrainRunner
export { BrainRunner } from "./runner/runner.js";
export type { BrainRunnerOptions, BrainRunResult } from "./runner/runner.js";

// SDK-03: ToolsRegistry
export { ToolsRegistry } from "./tools/registry.js";

// SDK-04: Prompts loader (used by BrainRunner internally, exposed for testing/advanced use)
export { loadPrompts } from "./prompts/loader.js";
```

**2. Criar `packages/core/src/server.ts`** — Hono app com POST /reload-prompts:

Recomendação (do RESEARCH.md, Pitfall 6 + Open Question 1): criar `createCoreApp(runner)` em `packages/core` que monta `/reload-prompts` separadamente. Não modificar `packages/observability/src/server.ts` — evitar dependência circular (observability → core → observability seria circular).

```typescript
// SDK-02: Core HTTP server — exposes POST /reload-prompts for hot-reload of prompts.
// D-07: POST /reload-prompts forces prompt reload without container restart.
// Authentication: X-Admin-Token header vs ADMIN_TOKEN env var (Claude's Discretion — simple for v1).
// Security T-3-04-01: Unauthorized access rejected with 401 before any action.

import { Hono } from "hono";
import { createLogger } from "@brain-pkg/observability";
import type { BrainRunner } from "./runner/runner.js";

const logger = createLogger();

/**
 * Creates a Hono application exposing the /reload-prompts management endpoint.
 *
 * Security: Requires X-Admin-Token header matching ADMIN_TOKEN env var.
 * If ADMIN_TOKEN env var is not set, the endpoint always returns 503 (misconfiguration).
 *
 * @param runner - BrainRunner instance to call refreshPrompts() on
 */
export function createCoreApp(runner: BrainRunner): Hono {
  const app = new Hono();

  app.post("/reload-prompts", async (c) => {
    const adminToken = process.env.ADMIN_TOKEN;

    // SECURITY T-3-04-01: Fail closed if ADMIN_TOKEN is not configured
    if (!adminToken) {
      logger.warn({}, "/reload-prompts called but ADMIN_TOKEN env var is not set");
      return c.json({ error: "Service unavailable — management endpoint not configured" }, 503);
    }

    const token = c.req.header("X-Admin-Token");

    // SECURITY T-3-04-01: Reject unauthorized requests
    if (!token || token !== adminToken) {
      logger.warn({}, "/reload-prompts unauthorized attempt");
      // Do NOT reveal whether token is missing vs incorrect (timing/info disclosure)
      return c.json({ error: "Unauthorized" }, 401);
    }

    await runner.refreshPrompts();
    logger.info({}, "/reload-prompts completed — prompts reloaded and graph recompiled");
    return c.json({ status: "ok" });
  });

  return app;
}
```

**Nota sobre ADMIN_TOKEN:** O env var `ADMIN_TOKEN` deve ser adicionado ao `.env.example` raiz do projeto se existir. Se não houver `.env.example`, não criar — documentar apenas nos planos.
  </action>
  <verify>
    <automated>
      grep 'export.*IBrain' /root/Brain/packages/core/src/index.ts &&
      grep 'export.*BrainRunner' /root/Brain/packages/core/src/index.ts &&
      grep 'export.*ToolsRegistry' /root/Brain/packages/core/src/index.ts &&
      grep 'export.*loadPrompts' /root/Brain/packages/core/src/index.ts &&
      grep 'createCoreApp' /root/Brain/packages/core/src/server.ts &&
      grep 'X-Admin-Token' /root/Brain/packages/core/src/server.ts &&
      grep 'ADMIN_TOKEN' /root/Brain/packages/core/src/server.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/index.ts` contém `export type { IBrain, BrainBuildContext }`
    - `packages/core/src/index.ts` contém `export { BrainRegistry }`
    - `packages/core/src/index.ts` contém `export { BrainRunner }`
    - `packages/core/src/index.ts` contém `export { ToolsRegistry }`
    - `packages/core/src/index.ts` contém `export { loadPrompts }`
    - `packages/core/src/server.ts` contém `export function createCoreApp(runner: BrainRunner): Hono`
    - `packages/core/src/server.ts` contém `c.req.header("X-Admin-Token")`
    - `packages/core/src/server.ts` contém `process.env.ADMIN_TOKEN`
    - `packages/core/src/server.ts` retorna 401 quando token incorreto (grep: `return c.json.*Unauthorized.*401`)
    - `packages/core/src/server.ts` retorna 503 quando `ADMIN_TOKEN` não configurado (grep: `503`)
    - `packages/core/src/server.ts` NÃO importa nada de `@brain-pkg/observability` além de `createLogger` (sem dependência circular)
  </acceptance_criteria>
  <done>Barrel export e endpoint /reload-prompts criados. packages/core expõe IBrain, BrainRegistry, BrainRunner, ToolsRegistry, loadPrompts. /reload-prompts autenticado via X-Admin-Token.</done>
</task>

<task type="auto">
  <name>Task 2: Completar wiring do WebhookHandler com BrainRunner</name>
  <files>
    packages/transport/src/webhook/handler.ts
  </files>
  <read_first>
    - /root/Brain/packages/transport/src/webhook/handler.ts (arquivo completo — comentário "Phase 3" identifica o ponto de wiring)
    - /root/Brain/packages/transport/src/webhook/events.ts (BrainEvent type)
    - /root/Brain/packages/core/src/runner/runner.ts (BrainRunner.run() retorna { reply: string })
  </read_first>
  <action>
Ler o arquivo `packages/transport/src/webhook/handler.ts` completo. O comentário `// Event dispatching will be wired in Phase 3 (BrainRunner)` identifica onde adicionar o wiring.

A modificação é **mínima** — não reescrever o handler, apenas completar o wiring:

1. Adicionar parâmetro opcional `runner?: BrainRunner` ao factory/constructor do handler (ou à função que cria o handler, dependendo de como está estruturado)
2. Substituir o código placeholder pela chamada real: `const result = await runner.run(event)`
3. Modificar a response para retornar `{ status: "ok", reply: result.reply }` em vez de `{ status: "accepted" }` (conforme RESEARCH.md State of the Art)

**IMPORTANTE:** Ler o arquivo ANTES de modificar para entender a estrutura atual. O padrão exato de como injetar `BrainRunner` depende de como o handler está estruturado (classe, função factory, Hono route). Adaptar o wiring à estrutura existente — não reescrever a estrutura.

**Se o handler usa uma função factory:**
```typescript
// Antes:
export function createWebhookHandler(): Hono { ... }

// Depois — adicionar runner opcional para não quebrar código existente sem runner:
export function createWebhookHandler(runner?: BrainRunner): Hono { ... }
// No route handler:
if (runner) {
  const result = await runner.run(event);
  return c.json({ status: "ok", reply: result.reply });
}
return c.json({ status: "accepted" }); // fallback sem runner (não deve ocorrer em produção)
```

**Import a adicionar:**
```typescript
import type { BrainRunner } from "@brain-pkg/core";
```

**CUIDADO com dependência circular:** `packages/transport` vai importar de `@brain-pkg/core`. `packages/core` importa de `@brain-pkg/transport` (para `BrainEvent`). Isso é uma dependência circular!

**Solução para evitar dependência circular:**
- Em vez de importar `BrainRunner` de `@brain-pkg/core`, definir um tipo local mínimo:
```typescript
// packages/transport/src/webhook/handler.ts
// Interface local para evitar dependência circular (packages/core → packages/transport → packages/core)
interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{ reply: string }>;
}
```
- Usar `IBrainRunnerLike` como tipo do parâmetro no lugar de `BrainRunner`
- `BrainRunner` implementa implicitamente `IBrainRunnerLike` (duck typing TypeScript)
  </action>
  <verify>
    <automated>
      grep -n 'runner' /root/Brain/packages/transport/src/webhook/handler.ts &&
      grep 'reply' /root/Brain/packages/transport/src/webhook/handler.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/transport/src/webhook/handler.ts` contém parâmetro ou opção para receber o runner (grep: `runner`)
    - `packages/transport/src/webhook/handler.ts` contém `runner.run(` (wiring com BrainRunner)
    - `packages/transport/src/webhook/handler.ts` contém `reply` na response (retorno `{ status: "ok", reply: ... }`)
    - `packages/transport/src/webhook/handler.ts` NÃO importa de `@brain-pkg/core` (sem import circular)
    - `bun test packages/transport` sai com código 0 (testes existentes continuam passando)
  </acceptance_criteria>
  <done>WebhookHandler faz wiring com BrainRunner via tipo local (sem dependência circular), retorna { status: "ok", reply: string }</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fase 3 completa:
    - Plano 00: packages/core scaffold + tabela prompts schema
    - Plano 01: IBrain interface + BrainRegistry + ToolsRegistry (SDK-01, SDK-03)
    - Plano 02: loadPrompts() + migration SQL (SDK-04)
    - Plano 03: BrainRunner lifecycle completo (SDK-02)
    - Plano 04: Barrel export + /reload-prompts + transport wiring
  </what-built>
  <how-to-verify>
    1. Executar suite completa de testes:
       ```bash
       cd /root/Brain && bun test packages/core
       ```
       Esperado: Todos os testes de BrainRegistry, ToolsRegistry, loadPrompts e BrainRunner passam.

    2. Verificar que migration foi gerada:
       ```bash
       ls /root/Brain/packages/database/src/migrations/0001_*.sql
       ```
       Esperado: arquivo existe e contém "prompts".

    3. Verificar ausência de MemorySaver em produção:
       ```bash
       grep -r "MemorySaver" /root/Brain/packages/core/src --include="*.ts" --exclude="*.test.ts"
       ```
       Esperado: ZERO resultados.

    4. Verificar barrel export:
       ```bash
       grep -E "IBrain|BrainRunner|BrainRegistry|ToolsRegistry|loadPrompts" /root/Brain/packages/core/src/index.ts
       ```
       Esperado: Todos os 5 símbolos presentes.

    5. Verificar segurança do /reload-prompts:
       ```bash
       grep -E "401|503|ADMIN_TOKEN|X-Admin-Token" /root/Brain/packages/core/src/server.ts
       ```
       Esperado: Todos os 4 padrões presentes.

    6. Build TypeScript:
       ```bash
       cd /root/Brain && pnpm --filter @brain-pkg/core typecheck
       ```
       Esperado: Sem erros TypeScript.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se todos os checks passaram, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-3-04-01 | /reload-prompts sem autenticação permite reload arbitrário de prompts por qualquer cliente | HIGH | `X-Admin-Token` header validado contra `ADMIN_TOKEN` env var antes de chamar `refreshPrompts()`. Retorna 401 (sem distinguir token ausente vs incorreto para evitar enumeração). Retorna 503 se `ADMIN_TOKEN` não está configurado. |
| T-3-04-02 | ADMIN_TOKEN não configurado em produção — endpoint /reload-prompts fica aberto | HIGH | Se `ADMIN_TOKEN` é undefined/empty, endpoint retorna 503 (fail closed). Nunca permite acesso sem token configurado. |
| T-3-04-03 | Dependência circular entre packages/core e packages/transport | MEDIUM | handler.ts usa interface local `IBrainRunnerLike` (duck typing) — sem import de @brain-pkg/core. TypeScript valida compatibilidade estrutural em tempo de compilação. |
| T-3-04-04 | Barrel export de packages/core expondo internals não intencionais | LOW | index.ts exporta explicitamente apenas os símbolos públicos. Não usa `export * from` para evitar vazamento de tipos internos. |
</threat_model>

<verification>
1. `bun test packages/core` — todos os testes passam (verde)
2. `bun test packages/transport` — testes existentes continuam passando
3. `grep -r "MemorySaver" packages/core/src --include="*.ts" --exclude="*.test.ts"` retorna vazio
4. `ls packages/database/src/migrations/0001_*.sql` retorna arquivo
5. `grep "X-Admin-Token\|ADMIN_TOKEN" packages/core/src/server.ts` retorna matches
6. `pnpm --filter @brain-pkg/core typecheck` retorna 0
</verification>

<success_criteria>
- packages/core/src/index.ts exporta toda a API pública (IBrain, BrainBuildContext, BrainRegistry, BrainRunner, BrainRunnerOptions, ToolsRegistry, loadPrompts)
- POST /reload-prompts protegido por X-Admin-Token, retorna 401/503 sem token, 200 com token válido
- WebhookHandler faz wiring com BrainRunner sem dependência circular
- Suite completa de testes verde
- TypeScript typecheck sem erros
- Usuário aprovou via checkpoint
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-brain-sdk/03-04-SUMMARY.md`
</output>
