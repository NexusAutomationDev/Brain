---
phase: 03-brain-sdk
plan: "02"
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - packages/database/src/migrations/0001_prompts_table.sql
  - packages/core/src/prompts/loader.ts
  - packages/core/src/prompts/__tests__/loader.test.ts
autonomous: true
requirements:
  - SDK-04

must_haves:
  truths:
    - "Migration SQL para tabela prompts foi gerada via drizzle-kit generate"
    - "loadPrompts() carrega prompts do banco via (brainType, keys) e retorna Record<string, string>"
    - "loadPrompts() retorna apenas as keys solicitadas (sem vazar outros prompts do brainType)"
    - "Testes de loadPrompts() passam com mock do banco de dados"
    - "Nenhum string de prompt hardcoded existe em packages/core (grep confirma)"
  artifacts:
    - path: "packages/database/src/migrations/0001_prompts_table.sql"
      provides: "Migration SQL criando tabela prompts com UNIQUE index"
      contains: "CREATE TABLE prompts"
    - path: "packages/core/src/prompts/loader.ts"
      provides: "loadPrompts() function"
      exports: ["loadPrompts"]
    - path: "packages/core/src/prompts/__tests__/loader.test.ts"
      provides: "Testes de loadPrompts com mock do db"
  key_links:
    - from: "packages/core/src/prompts/loader.ts"
      to: "packages/database/src/schema/tables.ts"
      via: "import { prompts } from @brain-pkg/database"
      pattern: "import.*prompts.*@brain-pkg/database"
    - from: "packages/core/src/prompts/loader.ts"
      to: "drizzle-orm/postgres-js"
      via: "drizzle() com Sql instance (postgres.js)"
      pattern: "drizzle.*sql"
---

<objective>
Wave 1 (paralelo com Plano 01): Implementar prompts loader e gerar migration da tabela prompts.

Purpose: Entregar SDK-04 — todos os prompts carregados do banco via `promptKeys`, sem strings hardcoded em código. A migration é gerada aqui e aplicada automaticamente no startup do container via `migrate.ts` existente.

Output: `loadPrompts()` function com testes, arquivo de migration SQL gerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-brain-sdk/03-CONTEXT.md
@.planning/phases/03-brain-sdk/03-RESEARCH.md
@.planning/phases/03-brain-sdk/03-00-SUMMARY.md

<interfaces>
<!-- Padrões Drizzle existentes para loadPrompts -->

De packages/database/src/schema/tables.ts (tabela prompts adicionada no Plano 00):
```typescript
export const prompts = pgTable('prompts', {
  id: uuid('id').primaryKey().defaultRandom(),
  brainType: text('brain_type').notNull(),
  key: text('key').notNull(),
  content: text('content').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
}, (table) => ({
  brainTypeKeyIdx: uniqueIndex('prompts_brain_type_key_idx').on(table.brainType, table.key),
}));
```

De packages/database/src/index.ts (exports disponíveis):
```typescript
export * from './schema/tables.js';  // inclui 'prompts' table
export { TenantPoolManager } from './pool-manager.js';
export type { Sql } from 'postgres';
export { drizzle } from 'drizzle-orm/postgres-js';
export { eq, and, or, sql } from 'drizzle-orm';
```

De packages/memory/src/long-term.ts (padrão de query Drizzle com postgres.js — VERIFICADO):
```typescript
import { drizzle } from "drizzle-orm/postgres-js";
import { eq, and } from "drizzle-orm";
import type { Sql } from "postgres";

// Padrão: receber Sql instance, criar drizzle db local, executar query
export async function readProfile(sql: Sql, userId: string, key: string): Promise<unknown | null> {
  const db = drizzle(sql);
  const rows = await db.select().from(memories).where(and(eq(memories.userId, userId), eq(memories.key, key)));
  return rows[0]?.value ?? null;
}
```

De packages/database/drizzle.config.ts (config de migrations):
```typescript
// migrations são geradas em packages/database/src/migrations/
// drizzle-kit generate cria arquivos 0000_*.sql, 0001_*.sql, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Gerar migration SQL da tabela prompts</name>
  <files>
    packages/database/src/migrations/0001_prompts_table.sql
  </files>
  <read_first>
    - /root/Brain/packages/database/src/schema/tables.ts (confirmar que tabela prompts está definida — adicionada no Plano 00)
    - /root/Brain/packages/database/drizzle.config.ts (confirmar outDir das migrations)
    - /root/Brain/packages/database/src/migrations/ (listar arquivos existentes para saber qual será o próximo número)
  </read_first>
  <action>
**Pré-requisito:** A tabela `prompts` deve estar definida em `packages/database/src/schema/tables.ts` (feito no Plano 00). Verificar antes de rodar.

**Rodar geração da migration:**
```bash
cd /root/Brain/packages/database && bunx drizzle-kit generate
```

O comando vai gerar um arquivo `0001_*.sql` em `packages/database/src/migrations/`.

**Verificar o arquivo gerado:**
- Deve conter `CREATE TABLE IF NOT EXISTS "prompts"`
- Deve conter `"brain_type" text NOT NULL`
- Deve conter `"key" text NOT NULL`
- Deve conter `"content" text NOT NULL`
- Deve conter `CREATE UNIQUE INDEX "prompts_brain_type_key_idx"`

**NUNCA editar o arquivo SQL gerado** — edições devem ser feitas em `tables.ts` e o comando re-executado.

**Nota:** A migration será aplicada automaticamente no próximo startup do container via `migrate.ts`. Não é necessário rodar `db:migrate` agora — isso é responsabilidade do container/startup.
  </action>
  <verify>
    <automated>
      ls /root/Brain/packages/database/src/migrations/0001_*.sql 2>/dev/null &&
      grep -l 'prompts' /root/Brain/packages/database/src/migrations/0001_*.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - Existe um arquivo `packages/database/src/migrations/0001_*.sql`
    - O arquivo contém `"prompts"` (nome da tabela)
    - O arquivo contém `"brain_type" text NOT NULL`
    - O arquivo contém `"key" text NOT NULL`
    - O arquivo contém `"content" text NOT NULL`
    - O arquivo contém `prompts_brain_type_key_idx` (unique index)
  </acceptance_criteria>
  <done>Arquivo de migration SQL gerado via drizzle-kit generate, contendo definição completa da tabela prompts com unique index</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar loadPrompts() e testes (SDK-04)</name>
  <files>
    packages/core/src/prompts/loader.ts
    packages/core/src/prompts/__tests__/loader.test.ts
  </files>
  <read_first>
    - /root/Brain/packages/core/src/prompts/__tests__/loader.test.ts (stubs Wave 0 a completar)
    - /root/Brain/packages/memory/src/long-term.ts (padrão de query Drizzle com postgres.js — replicar)
    - /root/Brain/packages/database/src/schema/tables.ts (definição da tabela prompts)
    - /root/Brain/packages/database/src/index.ts (exports disponíveis: prompts, drizzle, eq, and, inArray)
  </read_first>
  <behavior>
    - Test 1: Mock retorna 2 rows para brainType "echo" com keys "system","greeting" → `{ system: "...", greeting: "..." }`
    - Test 2: Mock retorna apenas keys solicitadas — se DB tem "system","greeting","other" mas só "system" foi pedido → retorna `{ system: "..." }`
    - Test 3: Mock retorna rows vazias → retorna `{}`
  </behavior>
  <action>
**RED → GREEN → REFACTOR**

**Criar `packages/core/src/prompts/loader.ts`:**

```typescript
// SDK-04: loadPrompts — loads prompts from the prompts table by brainType and keys.
// D-06: Called by BrainRunner.init() to load all promptKeys before starting.
// D-08: Uses (brain_type, key) scoped queries — no cross-brainType contamination.
// Pattern: mirrors packages/memory/src/long-term.ts — Sql instance injected, drizzle created locally.
// Never use bun:sql as driver — postgres.js (Sql) only (see CLAUDE.md Critical Risks).

import { drizzle } from "drizzle-orm/postgres-js";
import { eq, and, inArray } from "drizzle-orm";
import { prompts } from "@brain-pkg/database";
import type { Sql } from "postgres";

/**
 * SDK-04: Load prompts from the prompts table for a given brainType.
 *
 * Returns a Record mapping key → content for the requested keys.
 * Only returns rows matching both brainType AND the requested keys.
 *
 * @param sql - postgres.js Sql instance (from TenantPoolManager or direct inject)
 * @param brainType - Brain category to scope the query (e.g., "echo", "sdr")
 * @param keys - Prompt keys to load (e.g., ["system", "greeting"])
 * @returns Record<string, string> — { [key]: content }
 */
export async function loadPrompts(
  sql: Sql,
  brainType: string,
  keys: string[]
): Promise<Record<string, string>> {
  if (keys.length === 0) return {};

  const db = drizzle(sql);
  const rows = await db
    .select({ key: prompts.key, content: prompts.content })
    .from(prompts)
    .where(and(eq(prompts.brainType, brainType), inArray(prompts.key, keys)));

  return Object.fromEntries(rows.map((r) => [r.key, r.content]));
}
```

**Atualizar `packages/core/src/prompts/__tests__/loader.test.ts`** — substituir stubs:

```typescript
// SDK-04: loadPrompts — load prompts from prompts table by brainType + keys
import { describe, test, expect, mock } from "bun:test";

// Mock the @brain-pkg/database module before importing loadPrompts
// to avoid real DB connection in unit tests
const mockSelect = mock(() => ({
  from: mock(() => ({
    where: mock(async () => []),
  })),
}));

const mockDrizzle = mock(() => ({
  select: mockSelect,
}));

mock.module("drizzle-orm/postgres-js", () => ({
  drizzle: mockDrizzle,
}));

mock.module("@brain-pkg/database", () => ({
  prompts: { brainType: "brain_type", key: "key", content: "content" },
  eq: mock((...args: unknown[]) => args),
  and: mock((...args: unknown[]) => args),
  inArray: mock((...args: unknown[]) => args),
}));

import { loadPrompts } from "../loader.js";

describe("loadPrompts", () => {
  test("returns Record<string, string> mapping key to content for given brainType", async () => {
    const mockRows = [
      { key: "system", content: "You are a helpful assistant" },
      { key: "greeting", content: "Hello! How can I help?" },
    ];
    mockSelect.mockImplementation(() => ({
      from: mock(() => ({
        where: mock(async () => mockRows),
      })),
    }));

    const result = await loadPrompts({} as never, "echo", ["system", "greeting"]);
    expect(result).toEqual({
      system: "You are a helpful assistant",
      greeting: "Hello! How can I help?",
    });
  });

  test("returns only the keys requested (ignores other keys for same brainType)", async () => {
    // DB returns only what was filtered — test that we don't accidentally return extras
    const mockRows = [{ key: "system", content: "System prompt" }];
    mockSelect.mockImplementation(() => ({
      from: mock(() => ({
        where: mock(async () => mockRows),
      })),
    }));

    const result = await loadPrompts({} as never, "echo", ["system"]);
    expect(Object.keys(result)).toEqual(["system"]);
    expect(result.system).toBe("System prompt");
  });

  test("returns empty object when no matching rows found", async () => {
    mockSelect.mockImplementation(() => ({
      from: mock(() => ({
        where: mock(async () => []),
      })),
    }));

    const result = await loadPrompts({} as never, "echo", ["missing-key"]);
    expect(result).toEqual({});
  });
});
```

**Nota sobre mock.module em bun test:** O padrão `mock.module()` deve ser chamado antes do import do módulo sendo testado. Se o teste falhar com erro de módulo não encontrado, verificar se `@brain-pkg/database` está resolvendo corretamente via path alias `tsconfig.base.json`.
  </action>
  <verify>
    <automated>
      cd /root/Brain && bun test packages/core/src/prompts --pass-with-no-tests
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/prompts/loader.ts` existe e contém `export async function loadPrompts(`
    - `packages/core/src/prompts/loader.ts` importa `prompts` de `@brain-pkg/database` (não de caminho relativo ao database)
    - `packages/core/src/prompts/loader.ts` usa `drizzle` de `drizzle-orm/postgres-js` (não `bun:sql`)
    - `packages/core/src/prompts/loader.ts` usa `inArray` para filtrar por lista de keys
    - `packages/core/src/prompts/loader.ts` NÃO contém nenhuma string de prompt hardcoded
    - `bun test packages/core/src/prompts` sai com código 0 e todos os 3 testes passam
    - `grep -r "system prompt\|você é\|you are a" packages/core/src --include="*.ts" --exclude="*.test.ts"` não retorna nenhum resultado (sem prompts hardcoded)
  </acceptance_criteria>
  <done>loadPrompts() implementado com 3 testes verdes, sem prompts hardcoded em packages/core</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-3-02-01 | Prompt injection via conteúdo da tabela prompts modificado por acesso não autorizado ao DB | MEDIUM | UNIQUE constraint limita superfície de ataque a usuários com acesso ao DB. Endpoint `/reload-prompts` com autenticação (Plano 04) controla reload em runtime. |
| T-3-02-02 | loadPrompts() vazando conteúdo de prompts de outro brainType via query sem filtro de brainType | HIGH | Query usa `and(eq(prompts.brainType, brainType), inArray(prompts.key, keys))` — filtro duplo garante isolamento por brainType. |
| T-3-02-03 | Migration SQL editada manualmente introduzindo SQL injection ou estrutura incorreta | MEDIUM | Migration gerada por `drizzle-kit generate` — nunca editar o arquivo SQL gerado. Editar apenas `tables.ts` e regenerar. |
| T-3-02-04 | `drizzle-kit generate` rodando sem database disponível pode gerar migration incorreta | LOW | `drizzle-kit generate` analisa apenas o schema TypeScript — não precisa de DB ativo para gerar o SQL. Operação é local e determinística. |
</threat_model>

<verification>
1. `ls packages/database/src/migrations/0001_*.sql` retorna um arquivo
2. `grep 'prompts' packages/database/src/migrations/0001_*.sql` retorna match
3. `bun test packages/core/src/prompts` — 3 testes verdes
4. `grep 'export async function loadPrompts' packages/core/src/prompts/loader.ts` retorna match
5. `grep -r 'MemorySaver' packages/core/src --include="*.ts" --exclude="*.test.ts"` retorna vazio
6. `grep 'inArray' packages/core/src/prompts/loader.ts` retorna match (garante filtro correto por keys)
</verification>

<success_criteria>
- Migration SQL da tabela prompts gerada em packages/database/src/migrations/0001_*.sql
- loadPrompts() implementado com query filtrada por (brainType, keys)
- 3 testes verdes — Record<string,string> correto, isolamento por brainType, empty result
- Zero strings de prompt hardcoded em packages/core (grep confirma)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-brain-sdk/03-02-SUMMARY.md`
</output>
