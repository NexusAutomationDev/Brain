---
phase: 03-brain-sdk
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/brain/__tests__/brain-registry.test.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
  - packages/core/src/tools/__tests__/tools-registry.test.ts
  - packages/core/src/prompts/__tests__/loader.test.ts
  - packages/database/src/schema/tables.ts
  - tsconfig.base.json
autonomous: true
requirements:
  - SDK-01
  - SDK-02
  - SDK-03
  - SDK-04

must_haves:
  truths:
    - "packages/core existe como workspace package com package.json e tsconfig.json"
    - "tsconfig.base.json contém o alias @brain-pkg/core"
    - "Stubs de teste existem para cada requisito SDK-01 a SDK-04 (testes falham com skip)"
    - "Tabela prompts está definida em tables.ts com colunas id, brain_type, key, content, created_at, updated_at e unique index"
  artifacts:
    - path: "packages/core/package.json"
      provides: "@brain-pkg/core workspace package definition"
    - path: "packages/core/tsconfig.json"
      provides: "TypeScript config extending tsconfig.base.json com project references"
    - path: "packages/core/src/brain/__tests__/brain-registry.test.ts"
      provides: "Stub de teste para SDK-01 BrainRegistry"
    - path: "packages/core/src/runner/__tests__/brain-runner.test.ts"
      provides: "Stub de teste para SDK-02 BrainRunner"
    - path: "packages/core/src/tools/__tests__/tools-registry.test.ts"
      provides: "Stub de teste para SDK-03 ToolsRegistry"
    - path: "packages/core/src/prompts/__tests__/loader.test.ts"
      provides: "Stub de teste para SDK-04 prompts loader"
    - path: "packages/database/src/schema/tables.ts"
      provides: "Schema da tabela prompts adicionado"
  key_links:
    - from: "tsconfig.base.json"
      to: "packages/core/src"
      via: "path alias @brain-pkg/core"
      pattern: '"@brain-pkg/core".*packages/core/src'
    - from: "packages/core/tsconfig.json"
      to: "packages/ai, packages/memory, packages/database, packages/transport, packages/observability, packages/shared"
      via: "project references"
      pattern: '"path".*\.\./ai'
---

<objective>
Wave 0: Scaffold do `packages/core` com configuração de workspace, alias TypeScript, stubs de teste Nyquist e adição da tabela `prompts` no schema do banco.

Purpose: Garantir que o ambiente está pronto para implementação — stubs de teste em vermelho antes da implementação (Nyquist compliance), e a definição da tabela `prompts` que será migrada no Plano 02.

Output: packages/core configurado como workspace, stubs de teste, tabela prompts em tables.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brain-sdk/03-CONTEXT.md
@.planning/phases/03-brain-sdk/03-RESEARCH.md

<interfaces>
<!-- Padrões existentes que este plano deve seguir -->

De packages/ai/package.json:
```json
{
  "name": "@brain-pkg/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test src",
    "typecheck": "tsc --noEmit"
  }
}
```

De packages/ai/tsconfig.json (padrão a seguir):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
}
```

De tsconfig.base.json (seção paths — adicionar entry):
```json
{
  "compilerOptions": {
    "paths": {
      "@brain-pkg/shared": ["packages/shared/src"],
      "@brain-pkg/database": ["packages/database/src"],
      "@brain-pkg/observability": ["packages/observability/src"],
      "@brain-pkg/ai": ["packages/ai/src"],
      "@brain-pkg/memory": ["packages/memory/src"],
      "@brain-pkg/transport": ["packages/transport/src"]
    }
  }
}
```

De packages/database/src/schema/tables.ts (schema existente — adicionar tabela prompts):
```typescript
import { pgTable, text, uuid, timestamp, uniqueIndex } from 'drizzle-orm/pg-core';

// Tabela prompts a adicionar ao final do arquivo:
export const prompts = pgTable('prompts', {
  id: uuid('id').primaryKey().defaultRandom(),
  brainType: text('brain_type').notNull(),
  key: text('key').notNull(),
  content: text('content').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
}, (table) => ({
  brainTypeKeyIdx: uniqueIndex('prompts_brain_type_key_idx').on(table.brainType, table.key),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scaffold packages/core e adicionar tabela prompts ao schema</name>
  <files>
    packages/core/package.json
    packages/core/tsconfig.json
    packages/database/src/schema/tables.ts
    tsconfig.base.json
  </files>
  <read_first>
    - /root/Brain/packages/ai/package.json (padrão de package.json de workspace package)
    - /root/Brain/packages/ai/tsconfig.json (padrão de tsconfig.json)
    - /root/Brain/tsconfig.base.json (paths existentes — adicionar @brain-pkg/core)
    - /root/Brain/packages/database/src/schema/tables.ts (schema existente — adicionar tabela prompts ao final)
    - /root/Brain/packages/database/drizzle.config.ts (confirmar configuração de migrations)
  </read_first>
  <action>
**1. Criar `packages/core/package.json`:**

```json
{
  "name": "@brain-pkg/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@brain-pkg/ai": "workspace:*",
    "@brain-pkg/memory": "workspace:*",
    "@brain-pkg/database": "workspace:*",
    "@brain-pkg/transport": "workspace:*",
    "@brain-pkg/observability": "workspace:*",
    "@brain-pkg/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
```

**2. Criar `packages/core/tsconfig.json`:**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"],
  "references": [
    { "path": "../shared" },
    { "path": "../ai" },
    { "path": "../memory" },
    { "path": "../database" },
    { "path": "../transport" },
    { "path": "../observability" }
  ]
}
```

**3. Adicionar alias `@brain-pkg/core` no `tsconfig.base.json` — na seção `paths`, adicionar entry:**

```json
"@brain-pkg/core": ["packages/core/src"]
```

(Mantendo todos os outros paths existentes intactos.)

**4. Adicionar tabela `prompts` no final de `packages/database/src/schema/tables.ts`:**

Adicionar `uniqueIndex` ao import existente de `drizzle-orm/pg-core`, depois ao final do arquivo:

```typescript
// SDK-04: Prompts table — stores all brain prompts keyed by (brain_type, key).
// D-11: Schema with id, brain_type, key, content, created_at, updated_at.
// D-08: UNIQUE constraint on (brain_type, key) — sub-agents are separate brain_types.
// D-09: No version column in v1 — direct UPDATE, history via git.
// D-10: No locale column in v1 — i18n is v2.
export const prompts = pgTable('prompts', {
  id: uuid('id').primaryKey().defaultRandom(),
  brainType: text('brain_type').notNull(),
  key: text('key').notNull(),
  content: text('content').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
}, (table) => ({
  brainTypeKeyIdx: uniqueIndex('prompts_brain_type_key_idx').on(table.brainType, table.key),
}));
```

Verificar se `uniqueIndex` já está no import. Se não estiver, adicionar ao import existente de `drizzle-orm/pg-core`.

**5. Criar estrutura de diretórios:**
```
packages/core/src/brain/__tests__/
packages/core/src/runner/__tests__/
packages/core/src/tools/__tests__/
packages/core/src/prompts/__tests__/
```
  </action>
  <verify>
    <automated>
      grep -r '"@brain-pkg/core"' /root/Brain/tsconfig.base.json &&
      grep 'prompts_brain_type_key_idx' /root/Brain/packages/database/src/schema/tables.ts &&
      grep 'brain_type' /root/Brain/packages/database/src/schema/tables.ts &&
      test -f /root/Brain/packages/core/package.json &&
      test -f /root/Brain/packages/core/tsconfig.json
    </automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.base.json` contém `"@brain-pkg/core": ["packages/core/src"]` na seção `paths`
    - `packages/core/package.json` contém `"name": "@brain-pkg/core"` e as 6 workspace dependencies (`@brain-pkg/ai`, `@brain-pkg/memory`, `@brain-pkg/database`, `@brain-pkg/transport`, `@brain-pkg/observability`, `@brain-pkg/shared`)
    - `packages/core/tsconfig.json` contém `"extends": "../../tsconfig.base.json"` e 6 project references (`../shared`, `../ai`, `../memory`, `../database`, `../transport`, `../observability`)
    - `packages/database/src/schema/tables.ts` contém `export const prompts = pgTable('prompts'`
    - `packages/database/src/schema/tables.ts` contém `uniqueIndex('prompts_brain_type_key_idx'`
    - `packages/database/src/schema/tables.ts` contém `brainType: text('brain_type').notNull()`
    - `packages/database/src/schema/tables.ts` contém `key: text('key').notNull()`
    - `packages/database/src/schema/tables.ts` contém `content: text('content').notNull()`
  </acceptance_criteria>
  <done>packages/core existe como workspace package com tsconfig, package.json, e tabela prompts adicionada ao schema com unique constraint em (brain_type, key)</done>
</task>

<task type="auto">
  <name>Task 2: Criar stubs de teste (Nyquist Wave 0)</name>
  <files>
    packages/core/src/brain/__tests__/brain-registry.test.ts
    packages/core/src/runner/__tests__/brain-runner.test.ts
    packages/core/src/tools/__tests__/tools-registry.test.ts
    packages/core/src/prompts/__tests__/loader.test.ts
  </files>
  <read_first>
    - /root/Brain/packages/ai/src/graph/state.test.ts (padrão de teste com bun test)
    - /root/Brain/.planning/phases/03-brain-sdk/03-VALIDATION.md (mapa de testes Wave 0)
  </read_first>
  <action>
Criar 4 arquivos de stub de teste. Cada stub deve:
- Importar o módulo que será criado (import ainda vai falhar até os planos 01/02/03 criarem os arquivos — isso é intencional para Wave 0)
- Usar `describe` + `test.todo` (ou `it.todo`) para documentar o comportamento esperado
- Usar `bun test` compatível (Jest API)

**`packages/core/src/brain/__tests__/brain-registry.test.ts`:**

```typescript
// SDK-01: BrainRegistry — register and resolve IBrain instances by ID
// Wave 0 stub: tests are todo until implementation exists in brain/registry.ts

import { describe, test, expect } from "bun:test";

describe("BrainRegistry", () => {
  test.todo("register() stores an IBrain and resolve() returns it by id");
  test.todo("resolve() throws ConfigurationError when brain id is not registered");
  test.todo("register() throws ConfigurationError when same brain id is registered twice");
});
```

**`packages/core/src/runner/__tests__/brain-runner.test.ts`:**

```typescript
// SDK-02: BrainRunner — lifecycle init() + run() returning { reply: string }
// Wave 0 stub: tests are todo until implementation exists in runner/runner.ts

import { describe, test } from "bun:test";

describe("BrainRunner", () => {
  test.todo("init() loads prompts from DB and compiles the graph");
  test.todo("init() calls process.exit(1) when a promptKey is missing from DB");
  test.todo("run(event) returns { reply: string } with the last AIMessage content");
  test.todo("run(event) does NOT expose LangGraph internal state in the return value");
  test.todo("refreshPrompts() reloads prompts from DB and recompiles the graph");
});
```

**`packages/core/src/tools/__tests__/tools-registry.test.ts`:**

```typescript
// SDK-03: ToolsRegistry — enable/disable tools per brainType
// Wave 0 stub: tests are todo until implementation exists in tools/registry.ts

import { describe, test } from "bun:test";

describe("ToolsRegistry", () => {
  test.todo("getTools() returns only tools enabled for a brainType");
  test.todo("getTools() returns empty array when brainType has no enabled tools");
  test.todo("getTools() throws ConfigurationError when brainType is not registered");
  test.todo("enableTool() adds a tool to the allowed set for a brainType");
  test.todo("disableTool() removes a tool from the allowed set for a brainType");
});
```

**`packages/core/src/prompts/__tests__/loader.test.ts`:**

```typescript
// SDK-04: loadPrompts — load prompts from prompts table by brainType + keys
// Wave 0 stub: tests are todo until implementation exists in prompts/loader.ts

import { describe, test } from "bun:test";

describe("loadPrompts", () => {
  test.todo("returns Record<string, string> mapping key to content for given brainType");
  test.todo("returns only the keys requested (ignores other keys for same brainType)");
  test.todo("returns empty object when no matching rows found");
});
```
  </action>
  <verify>
    <automated>
      cd /root/Brain && bun test packages/core/src --pass-with-no-tests
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/brain/__tests__/brain-registry.test.ts` existe e contém `describe("BrainRegistry"`
    - `packages/core/src/runner/__tests__/brain-runner.test.ts` existe e contém `describe("BrainRunner"`
    - `packages/core/src/tools/__tests__/tools-registry.test.ts` existe e contém `describe("ToolsRegistry"`
    - `packages/core/src/prompts/__tests__/loader.test.ts` existe e contém `describe("loadPrompts"`
    - `bun test packages/core/src --pass-with-no-tests` sai com código 0 (stubs todo não causam falha)
  </acceptance_criteria>
  <done>Quatro arquivos de stub de teste existem, bun test packages/core passa sem erros, todos os comportamentos esperados estão documentados como test.todo</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-3-00-01 | Tabela `prompts` sem UNIQUE constraint permite inserção de prompts duplicados causando comportamento não-determinístico | MEDIUM | `uniqueIndex('prompts_brain_type_key_idx').on(table.brainType, table.key)` implementado no schema (D-08) |
| T-3-00-02 | `packages/core/tsconfig.json` sem project references pode permitir imports de módulos não declarados | LOW | Declarar `references` para todos os 6 workspace packages no tsconfig.json |
</threat_model>

<verification>
1. `grep '"@brain-pkg/core"' tsconfig.base.json` retorna match
2. `grep 'prompts_brain_type_key_idx' packages/database/src/schema/tables.ts` retorna match
3. `test -f packages/core/package.json && test -f packages/core/tsconfig.json` retorna 0
4. `bun test packages/core/src --pass-with-no-tests` retorna 0
5. `grep 'describe("BrainRegistry"' packages/core/src/brain/__tests__/brain-registry.test.ts` retorna match
</verification>

<success_criteria>
- packages/core existe como workspace package @brain-pkg/core com todas as 6 workspace deps
- tsconfig.base.json tem alias @brain-pkg/core → packages/core/src
- packages/database/src/schema/tables.ts define tabela prompts com colunas id, brain_type, key, content, created_at, updated_at e UNIQUE(brain_type, key)
- 4 arquivos de stub de teste existem e bun test passa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-brain-sdk/03-00-SUMMARY.md`
</output>
