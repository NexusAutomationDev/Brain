---
phase: 03-brain-sdk
plan: "01"
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - packages/core/src/brain/interface.ts
  - packages/core/src/brain/registry.ts
  - packages/core/src/tools/registry.ts
  - packages/core/src/brain/__tests__/brain-registry.test.ts
  - packages/core/src/tools/__tests__/tools-registry.test.ts
autonomous: true
requirements:
  - SDK-01
  - SDK-03

must_haves:
  truths:
    - "IBrain interface e BrainBuildContext são types exportados de packages/core"
    - "BrainRegistry registra um IBrain por id e o resolve por id"
    - "BrainRegistry lança ConfigurationError quando id não existe ou já está registrado"
    - "ToolsRegistry filtra tools por brainType usando whitelist Map<brainType, Set<toolName>>"
    - "ToolsRegistry lança ConfigurationError quando brainType não está registrado (não retorna [] silenciosamente)"
    - "Todos os testes de BrainRegistry e ToolsRegistry passam (verde)"
  artifacts:
    - path: "packages/core/src/brain/interface.ts"
      provides: "IBrain interface e BrainBuildContext type"
      exports: ["IBrain", "BrainBuildContext"]
    - path: "packages/core/src/brain/registry.ts"
      provides: "BrainRegistry class com register() e resolve()"
      exports: ["BrainRegistry"]
    - path: "packages/core/src/tools/registry.ts"
      provides: "ToolsRegistry class com enableTool(), disableTool(), getTools()"
      exports: ["ToolsRegistry"]
  key_links:
    - from: "packages/core/src/brain/interface.ts"
      to: "@langchain/core"
      via: "BaseChatModel, StructuredTool imports"
      pattern: "import.*BaseChatModel.*@langchain/core"
    - from: "packages/core/src/brain/interface.ts"
      to: "@brain-pkg/ai"
      via: "BrainStateAnnotation import"
      pattern: "import.*BrainStateAnnotation.*@brain-pkg/ai"
    - from: "packages/core/src/brain/registry.ts"
      to: "@brain-pkg/shared"
      via: "ConfigurationError import"
      pattern: "import.*ConfigurationError.*@brain-pkg/shared"
---

<objective>
Wave 1: Definir os contratos de IBrain e BrainBuildContext, implementar BrainRegistry e ToolsRegistry com testes completos.

Purpose: Estabelecer os contratos que todo Brain implementará (SDK-01) e o sistema de controle de acesso a tools por brainType (SDK-03). Esses contratos são consumidos pelo BrainRunner no Plano 03.

Output: `interface.ts`, `brain/registry.ts`, `tools/registry.ts`, testes verdes para SDK-01 e SDK-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-brain-sdk/03-CONTEXT.md
@.planning/phases/03-brain-sdk/03-RESEARCH.md
@.planning/phases/03-brain-sdk/03-00-SUMMARY.md

<interfaces>
<!-- Contratos existentes que interface.ts deve seguir -->

De packages/ai/src/graph/state.ts (BrainStateAnnotation que IBrain usa):
```typescript
export { BrainStateAnnotation } from "./graph/state.js";
export type { BrainState } from "./graph/state.js";
```

De packages/shared/src/errors/index.ts:
```typescript
export class ConfigurationError extends BrainError {
  constructor(message: string, context?: Record<string, unknown>) {
    super(message, 'CONFIGURATION_ERROR', context);
    this.name = 'ConfigurationError';
  }
}
```

De packages/transport/src/interface.ts (padrão de interface abstrata):
```typescript
export interface ITransport {
  start(port?: number): Promise<void>;
  stop(): Promise<void>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar IBrain interface e BrainRegistry (SDK-01)</name>
  <files>
    packages/core/src/brain/interface.ts
    packages/core/src/brain/registry.ts
    packages/core/src/brain/__tests__/brain-registry.test.ts
  </files>
  <read_first>
    - /root/Brain/packages/core/src/brain/__tests__/brain-registry.test.ts (stubs Wave 0 a completar)
    - /root/Brain/packages/ai/src/graph/state.ts (BrainStateAnnotation — tipo de retorno de buildGraph)
    - /root/Brain/packages/shared/src/errors/index.ts (ConfigurationError — base de erros)
    - /root/Brain/packages/transport/src/interface.ts (padrão de interface abstrata a seguir)
  </read_first>
  <behavior>
    - Test 1: `register(brain)` + `resolve(brain.id)` retorna o mesmo objeto IBrain
    - Test 2: `resolve('nonexistent')` lança `ConfigurationError` com mensagem contendo o id
    - Test 3: `register(brain)` quando mesmo id já registrado lança `ConfigurationError`
  </behavior>
  <action>
**RED → GREEN → REFACTOR**

**Criar `packages/core/src/brain/interface.ts`:**

```typescript
// SDK-01: IBrain contract — the minimal interface any Brain must implement.
// D-01: buildGraph() receives dependencies injected by BrainRunner (llm, prompts, tools).
// D-02: buildGraph() returns StateGraph NOT compiled — BrainRunner calls .compile({ checkpointer }).
// D-03: tools[] are StructuredTool instances (pre-built). ToolsRegistry filters them before inject.
// D-04: All Brains use BrainStateAnnotation without field extensions in v1.

import type { BaseChatModel } from "@langchain/core/language_models/chat_models";
import type { StructuredTool } from "@langchain/core/tools";
import type { StateGraph } from "@langchain/langgraph";
import type { BrainStateAnnotation } from "@brain-pkg/ai";

/**
 * Dependencies injected by BrainRunner into buildGraph().
 * Brain uses these to wire nodes and edges — no external deps needed.
 */
export interface BrainBuildContext {
  /** LLM instance from createLLM() — provider configured via env */
  llm: BaseChatModel;
  /** Prompts loaded from prompts table: { [key]: content } */
  prompts: Record<string, string>;
  /** Brain's tools[] filtered by ToolsRegistry for this brainType */
  tools: StructuredTool[];
}

/**
 * SDK-01: Minimal contract for all Brain implementations.
 * Brains define their identity, prompt keys, tools, and graph structure.
 * Orchestration (checkpointing, memory, transport) is BrainRunner's responsibility.
 */
export interface IBrain {
  /** Unique Brain instance identifier (e.g., "sdr-brain-prod") */
  id: string;
  /** Brain category used for ToolsRegistry lookup and prompts scoping (e.g., "sdr", "support") */
  brainType: string;
  /** Keys to load from prompts table for this brainType at startup */
  promptKeys: string[];
  /** Full tool list — Runner filters via ToolsRegistry before injection */
  tools: StructuredTool[];
  /**
   * D-02: Returns StateGraph NOT compiled.
   * BrainRunner calls .compile({ checkpointer }) — never call .compile() here.
   */
  buildGraph(ctx: BrainBuildContext): StateGraph<typeof BrainStateAnnotation>;
}
```

**Criar `packages/core/src/brain/registry.ts`:**

```typescript
// SDK-01: BrainRegistry — stores IBrain instances mapped by id.
// Registered once at startup; resolved per request by BrainRunner.

import { ConfigurationError } from "@brain-pkg/shared";
import type { IBrain } from "./interface.js";

/**
 * SDK-01: Registry for IBrain instances.
 * Maps brainId → IBrain instance. Throws ConfigurationError on conflicts.
 */
export class BrainRegistry {
  private registry = new Map<string, IBrain>();

  /**
   * Register an IBrain instance. Throws if id already registered.
   */
  register(brain: IBrain): void {
    if (this.registry.has(brain.id)) {
      throw new ConfigurationError(
        `Brain already registered: ${brain.id}`,
        { brainId: brain.id }
      );
    }
    this.registry.set(brain.id, brain);
  }

  /**
   * Resolve an IBrain by id. Throws ConfigurationError if not found.
   */
  resolve(brainId: string): IBrain {
    const brain = this.registry.get(brainId);
    if (!brain) {
      throw new ConfigurationError(
        `Brain not found: ${brainId}`,
        { brainId }
      );
    }
    return brain;
  }
}
```

**Atualizar `packages/core/src/brain/__tests__/brain-registry.test.ts`** — substituir os test.todo pelos testes reais:

```typescript
// SDK-01: BrainRegistry — register and resolve IBrain instances by ID
import { describe, test, expect } from "bun:test";
import { BrainRegistry } from "../registry.js";
import { ConfigurationError } from "@brain-pkg/shared";
import type { IBrain } from "../interface.js";

// Minimal IBrain stub for testing (no real LangGraph dependency needed)
function makeBrain(id: string, brainType = "test"): IBrain {
  return {
    id,
    brainType,
    promptKeys: [],
    tools: [],
    buildGraph: () => { throw new Error("not implemented in stub"); },
  } as unknown as IBrain;
}

describe("BrainRegistry", () => {
  test("register() stores an IBrain and resolve() returns it by id", () => {
    const registry = new BrainRegistry();
    const brain = makeBrain("brain-1");
    registry.register(brain);
    expect(registry.resolve("brain-1")).toBe(brain);
  });

  test("resolve() throws ConfigurationError when brain id is not registered", () => {
    const registry = new BrainRegistry();
    expect(() => registry.resolve("nonexistent")).toThrow(ConfigurationError);
    expect(() => registry.resolve("nonexistent")).toThrow("Brain not found: nonexistent");
  });

  test("register() throws ConfigurationError when same brain id is registered twice", () => {
    const registry = new BrainRegistry();
    const brain = makeBrain("brain-dup");
    registry.register(brain);
    expect(() => registry.register(brain)).toThrow(ConfigurationError);
    expect(() => registry.register(brain)).toThrow("Brain already registered: brain-dup");
  });
});
```
  </action>
  <verify>
    <automated>
      cd /root/Brain && bun test packages/core/src/brain --pass-with-no-tests
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/brain/interface.ts` existe e contém `export interface IBrain`
    - `packages/core/src/brain/interface.ts` contém `export interface BrainBuildContext`
    - `packages/core/src/brain/interface.ts` contém `buildGraph(ctx: BrainBuildContext): StateGraph`
    - `packages/core/src/brain/registry.ts` contém `export class BrainRegistry`
    - `packages/core/src/brain/registry.ts` contém `register(brain: IBrain): void`
    - `packages/core/src/brain/registry.ts` contém `resolve(brainId: string): IBrain`
    - `packages/core/src/brain/registry.ts` importa `ConfigurationError` de `@brain-pkg/shared`
    - `bun test packages/core/src/brain` sai com código 0 e todos os 3 testes passam
  </acceptance_criteria>
  <done>IBrain interface, BrainBuildContext type e BrainRegistry implementados com 3 testes verdes cobrindo SDK-01</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar ToolsRegistry (SDK-03)</name>
  <files>
    packages/core/src/tools/registry.ts
    packages/core/src/tools/__tests__/tools-registry.test.ts
  </files>
  <read_first>
    - /root/Brain/packages/core/src/tools/__tests__/tools-registry.test.ts (stubs Wave 0 a completar)
    - /root/Brain/packages/shared/src/errors/index.ts (ConfigurationError)
    - /root/Brain/packages/core/src/brain/interface.ts (já criado neste plano — IBrain.tools type)
  </read_first>
  <behavior>
    - Test 1: `getTools("echo", [toolA, toolB])` após `enableTool("echo", "toolA")` retorna apenas `[toolA]`
    - Test 2: `getTools("other", [...])` após `enableTool("echo", "toolA")` — "other" não registrado → lança ConfigurationError (D-12 + A3)
    - Test 3: `getTools("empty-type", [toolA])` após `enableTool("empty-type", "someTool")` sem habilitar nenhuma tool real → retorna `[]`
    - Test 4: `enableTool` + `disableTool` → tool removida da whitelist
  </behavior>
  <action>
**RED → GREEN → REFACTOR**

**Criar `packages/core/src/tools/registry.ts`:**

```typescript
// SDK-03: ToolsRegistry — controls which tools are permitted per brainType.
// D-12: Keyed by brainType (string) → whitelist Set<toolName>.
// A3: brainType not registered throws ConfigurationError (not silent empty []).
//     This catches misconfiguration at startup rather than silently running without tools.

import { ConfigurationError } from "@brain-pkg/shared";
import type { StructuredTool } from "@langchain/core/tools";

/**
 * SDK-03: Registry of allowed tools per brainType.
 *
 * Usage:
 *   const registry = new ToolsRegistry();
 *   registry.enableTool("echo", "searchTool");
 *   const filtered = registry.getTools("echo", brain.tools);
 */
export class ToolsRegistry {
  // brainType → Set of allowed tool names (whitelist)
  private registry = new Map<string, Set<string>>();

  /**
   * Register a brainType with an allowed tool.
   * Creates the brainType entry if it does not exist.
   */
  enableTool(brainType: string, toolName: string): void {
    if (!this.registry.has(brainType)) {
      this.registry.set(brainType, new Set());
    }
    this.registry.get(brainType)!.add(toolName);
  }

  /**
   * Remove a tool from the allowed set for a brainType.
   * No-op if brainType or toolName does not exist.
   */
  disableTool(brainType: string, toolName: string): void {
    this.registry.get(brainType)?.delete(toolName);
  }

  /**
   * Return only the tools from brainTools that are whitelisted for brainType.
   *
   * Throws ConfigurationError if brainType has never been registered via enableTool().
   * This prevents silent "no tools" scenarios from misconfiguration.
   *
   * @param brainType - Brain category to look up
   * @param brainTools - Full list of tools from IBrain.tools[]
   */
  getTools(brainType: string, brainTools: StructuredTool[]): StructuredTool[] {
    const allowed = this.registry.get(brainType);
    if (!allowed) {
      throw new ConfigurationError(
        `brainType not registered in ToolsRegistry: ${brainType}`,
        { brainType }
      );
    }
    return brainTools.filter((t) => allowed.has(t.name));
  }
}
```

**Atualizar `packages/core/src/tools/__tests__/tools-registry.test.ts`** — substituir os test.todo:

```typescript
// SDK-03: ToolsRegistry — enable/disable tools per brainType
import { describe, test, expect } from "bun:test";
import { ToolsRegistry } from "../registry.js";
import { ConfigurationError } from "@brain-pkg/shared";
import type { StructuredTool } from "@langchain/core/tools";

// Minimal StructuredTool stub for testing
function makeTool(name: string): StructuredTool {
  return { name } as unknown as StructuredTool;
}

describe("ToolsRegistry", () => {
  test("getTools() returns only tools enabled for a brainType", () => {
    const registry = new ToolsRegistry();
    const toolA = makeTool("toolA");
    const toolB = makeTool("toolB");
    registry.enableTool("echo", "toolA");
    const result = registry.getTools("echo", [toolA, toolB]);
    expect(result).toEqual([toolA]);
    expect(result).not.toContain(toolB);
  });

  test("getTools() returns empty array when brainType is registered but no tools enabled", () => {
    const registry = new ToolsRegistry();
    const toolA = makeTool("toolA");
    // Register brainType with one tool, then remove it
    registry.enableTool("empty-type", "toolA");
    registry.disableTool("empty-type", "toolA");
    const result = registry.getTools("empty-type", [toolA]);
    expect(result).toEqual([]);
  });

  test("getTools() throws ConfigurationError when brainType is not registered", () => {
    const registry = new ToolsRegistry();
    const toolA = makeTool("toolA");
    expect(() => registry.getTools("other", [toolA])).toThrow(ConfigurationError);
    expect(() => registry.getTools("other", [toolA])).toThrow("brainType not registered");
  });

  test("enableTool() adds a tool to the allowed set for a brainType", () => {
    const registry = new ToolsRegistry();
    const toolA = makeTool("toolA");
    registry.enableTool("echo", "toolA");
    expect(registry.getTools("echo", [toolA])).toContain(toolA);
  });

  test("disableTool() removes a tool from the allowed set for a brainType", () => {
    const registry = new ToolsRegistry();
    const toolA = makeTool("toolA");
    registry.enableTool("echo", "toolA");
    registry.disableTool("echo", "toolA");
    const result = registry.getTools("echo", [toolA]);
    expect(result).toEqual([]);
  });
});
```
  </action>
  <verify>
    <automated>
      cd /root/Brain && bun test packages/core/src/tools --pass-with-no-tests
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/tools/registry.ts` contém `export class ToolsRegistry`
    - `packages/core/src/tools/registry.ts` contém `enableTool(brainType: string, toolName: string): void`
    - `packages/core/src/tools/registry.ts` contém `disableTool(brainType: string, toolName: string): void`
    - `packages/core/src/tools/registry.ts` contém `getTools(brainType: string, brainTools: StructuredTool[]): StructuredTool[]`
    - `packages/core/src/tools/registry.ts` importa `ConfigurationError` de `@brain-pkg/shared`
    - `packages/core/src/tools/registry.ts` lança `ConfigurationError` quando `brainType` não está no Map (grep: `throw new ConfigurationError.*brainType not registered`)
    - `bun test packages/core/src/tools` sai com código 0 e todos os 5 testes passam
  </acceptance_criteria>
  <done>ToolsRegistry implementado com 5 testes verdes cobrindo SDK-03: enable/disable por brainType, ConfigurationError para brainType não registrado</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-3-01-01 | BrainType spoofing — atacante injeta brainType arbitrário no BrainEvent para obter tools de outro Brain | HIGH | `brainType` vem EXCLUSIVAMENTE da instância `IBrain` registrada — nunca do `BrainEvent` recebido. `BrainRegistry.resolve(brainId)` retorna o `IBrain` com o brainType correto. |
| T-3-01-02 | ToolsRegistry retornando silenciosamente `[]` para brainType não registrado, mascarando misconfiguration | MEDIUM | `getTools()` lança `ConfigurationError` quando brainType não está no Map — força detecção no startup (alinha com padrão fail-fast) |
| T-3-01-03 | IBrain.buildGraph() compilando o grafo internamente com MemorySaver (violação de AI-01) | HIGH | Interface define `buildGraph()` retornando `StateGraph` não compilado. Executor deve grep por `import.*MemorySaver` em qualquer arquivo fora de `*.test.ts` após implementação. |
</threat_model>

<verification>
1. `bun test packages/core/src/brain` — 3 testes verdes (BrainRegistry)
2. `bun test packages/core/src/tools` — 5 testes verdes (ToolsRegistry)
3. `grep 'export interface IBrain' packages/core/src/brain/interface.ts` retorna match
4. `grep 'export interface BrainBuildContext' packages/core/src/brain/interface.ts` retorna match
5. `grep 'throw new ConfigurationError.*brainType not registered' packages/core/src/tools/registry.ts` retorna match
6. `grep -r 'MemorySaver' packages/core/src --include="*.ts" --exclude="*.test.ts"` não deve retornar nenhum resultado
</verification>

<success_criteria>
- IBrain interface e BrainBuildContext type definidos com contrato completo (id, brainType, promptKeys, tools, buildGraph)
- BrainRegistry com register() e resolve() — 3 testes verdes, ConfigurationError em casos de erro
- ToolsRegistry com enableTool(), disableTool(), getTools() — 5 testes verdes, ConfigurationError para brainType não registrado
- Nenhum import de MemorySaver fora de arquivos *.test.ts
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-brain-sdk/03-01-SUMMARY.md`
</output>
