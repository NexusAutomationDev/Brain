---
phase: 01
plan: 05
type: gap_closure
wave: 1
depends_on: []
files_modified:
  - packages/observability/src/server.ts
  - packages/observability/src/server.test.ts
  - packages/observability/src/index.ts
  - packages/observability/package.json
autonomous: true
requirements:
  - OBS-02

must_haves:
  truths:
    - "`GET /health` retorna JSON `{ status, checks: { db }, version, timestamp }` com HTTP 200 quando DB conectado"
    - "`GET /health` retorna HTTP 503 quando checks.db é 'failed'"
    - "A função `startServer()` é exportada de `@brain-pkg/observability` e pode ser chamada por outros packages"
    - "`pnpm build` continua passando após adicionar Hono ao package observability"
  artifacts:
    - path: "packages/observability/src/server.ts"
      provides: "Hono app com rota GET /health usando performHealthCheck"
      exports: ["createHealthApp", "startServer"]
    - path: "packages/observability/src/server.test.ts"
      provides: "Testes da rota GET /health (response shape, HTTP status codes)"
  key_links:
    - from: "packages/observability/src/server.ts"
      to: "packages/observability/src/health.ts"
      via: "import { performHealthCheck }"
      pattern: "performHealthCheck"
    - from: "packages/observability/src/index.ts"
      to: "packages/observability/src/server.ts"
      via: "re-export de createHealthApp, startServer"
      pattern: "startServer|createHealthApp"
---

# Plano 05: Endpoint HTTP GET /health

<objective>
Fechar o Gap 3 da verificação de Phase 1: criar um servidor HTTP Hono mínimo no package observability que expõe `GET /health`, usando o utilitário `performHealthCheck` já existente, retornando o JSON estruturado conforme D-13/D-14 do CONTEXT.md.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → GET /health | Qualquer cliente pode chamar o endpoint — sem autenticação em Phase 1 |
| performHealthCheck → PostgreSQL | O health check executa `SELECT 1` no banco — requer conexão TCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | GET /health response | mitigate | O endpoint expõe apenas `status`, `checks.db`, `version` (commit hash ou "unknown"), e `timestamp` — NUNCA incluir DATABASE_URL, credenciais, stack traces, ou nomes de tabelas na resposta. A função `performHealthCheck` já foi implementada com essa restrição em health.ts. |
| T-05-02 | Denial of Service | GET /health endpoint | accept | O endpoint é de health check (baixo valor para ataque); o `SELECT 1` é fast-path (<1ms); sem auth significa que qualquer cliente pode chamar, mas o custo por request é mínimo. Em Phase 1 (desenvolvimento), sem rate limiting é aceitável. |
| T-05-03 | Spoofing | Sql instance injetada em performHealthCheck | accept | Em Phase 1, o `Sql` é construído pelo TenantPoolManager com credenciais do env — sem input externo no path de criação do pool usado pelo health check. |
| T-05-04 | Tampering | Hono app routing | accept | Somente GET /health está exposto; não há rotas de escrita; Hono não expõe filesystem nem permite path traversal em rota estática. |
</threat_model>

<tasks>

<task id="5.1" type="create">
  <title>Criar packages/observability/src/server.ts — Hono app com GET /health</title>
  <read_first>
    - packages/observability/src/health.ts (exporta performHealthCheck e HealthCheckResult — usar diretamente)
    - packages/observability/src/index.ts (barrel export — precisará ser atualizado na task 5.3)
    - packages/observability/package.json (verificar se hono está nas dependências — se não estiver, adicionar)
    - packages/database/src/pool-manager.ts (TenantPoolManager — entender como o Sql é obtido para passar ao performHealthCheck)
  </read_first>
  <action>
**Passo 1: Adicionar Hono como dependência de runtime.**

Verificar se `hono` já está em `packages/observability/package.json` dependencies. Se não estiver:
```bash
cd /root/Brain/packages/observability && pnpm add hono
```

**Passo 2: Criar `packages/observability/src/server.ts` com o conteúdo abaixo.**

O server deve:
- Exportar `createHealthApp(sql: Sql): Hono` — cria o app Hono com a rota GET /health injetando a instância `Sql`
- Exportar `startServer(sql: Sql, port?: number): void` — inicializa o servidor Bun com o app
- Aplicar os HTTP status codes de D-14: 200 para ok, 503 para checks.db === 'failed', 500 para status === 'error'
- NUNCA incluir DATABASE_URL ou credenciais na resposta (T-05-01)

```typescript
import { Hono } from 'hono';
import type { Sql } from 'postgres';
import { performHealthCheck } from './health.js';

/**
 * Creates a Hono application with the GET /health route.
 *
 * OBS-02: Exposes health check endpoint per D-13 and D-14.
 * The sql instance is injected so the caller controls which DB connection is checked.
 *
 * @param sql - postgres.js Sql instance to use for DB connectivity check
 */
export function createHealthApp(sql: Sql): Hono {
  const app = new Hono();

  app.get('/health', async (c) => {
    const result = await performHealthCheck(sql);

    // D-14: HTTP status codes
    // 200 = ok, 503 = dependency (DB) failed, 500 = internal error
    const httpStatus =
      result.status === 'ok'
        ? 200
        : result.checks.db === 'failed'
          ? 503
          : 500;

    return c.json(result, httpStatus);
  });

  return app;
}

/**
 * Starts the HTTP server using Bun.serve.
 *
 * @param sql - postgres.js Sql instance for health check
 * @param port - Port to listen on (default: PORT env or 3000)
 */
export function startServer(sql: Sql, port?: number): void {
  const app = createHealthApp(sql);
  const listenPort = port ?? parseInt(process.env.PORT || '3000', 10);

  Bun.serve({
    port: listenPort,
    fetch: app.fetch,
  });

  console.log(`Health server listening on port ${listenPort}`);
}
```

**Nota sobre o Sql injetado:** O consumidor de `startServer` ou `createHealthApp` é responsável por fornecer a instância `Sql`. Em testes, pode-se passar um mock. Em runtime, o consumidor usaria `TenantPoolManager.getPool(databaseName)` ou uma instância direta de `postgres(DATABASE_URL)`.
  </action>
  <acceptance_criteria>
    - `ls packages/observability/src/server.ts` retorna exit code 0 (arquivo existe)
    - `grep -c 'export function createHealthApp' packages/observability/src/server.ts` retorna `1`
    - `grep -c 'export function startServer' packages/observability/src/server.ts` retorna `1`
    - `grep -c "import { performHealthCheck }" packages/observability/src/server.ts` retorna `1`
    - `grep -c 'app.get.*\/health' packages/observability/src/server.ts` retorna `1`
    - `grep -c '503' packages/observability/src/server.ts` retorna `1` (HTTP 503 para DB failed)
    - `grep -c 'DATABASE_URL\|password\|secret\|credential' packages/observability/src/server.ts` retorna `0` (T-05-01: sem credenciais na resposta)
    - `grep 'hono' packages/observability/package.json` mostra hono em dependencies (não devDependencies)
  </acceptance_criteria>
</task>

<task id="5.2" type="create">
  <title>Criar packages/observability/src/server.test.ts — testes da rota GET /health</title>
  <read_first>
    - packages/observability/src/server.ts (arquivo criado na task 5.1 — testar createHealthApp)
    - packages/observability/src/health.ts (HealthCheckResult type para assertions de shape)
    - packages/observability/src/health.test.ts (padrão de estrutura de test existente no package)
  </read_first>
  <action>
Criar `packages/observability/src/server.test.ts` com testes reais (não todo) que verificam o comportamento do endpoint GET /health.

Os testes devem usar `createHealthApp()` injetando um mock da `Sql` interface para controlar o retorno do `performHealthCheck`:

```typescript
import { describe, it, expect, mock } from 'bun:test';
import { createHealthApp } from './server.js';
import type { Sql } from 'postgres';

// Mock da instância Sql do postgres.js
// Quando SELECT 1 sucede, checkDatabase retorna true
function makeMockSql(shouldSucceed: boolean): Sql {
  const fn = mock(async () => {
    if (!shouldSucceed) throw new Error('Connection refused');
    return [{ '?column?': 1 }];
  }) as unknown;
  return fn as Sql;
}

describe('GET /health (OBS-02)', () => {
  describe('when database is connected', () => {
    it('returns HTTP 200', async () => {
      const app = createHealthApp(makeMockSql(true));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      expect(res.status).toBe(200);
    });

    it('returns JSON with status "ok"', async () => {
      const app = createHealthApp(makeMockSql(true));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      const body = await res.json();
      expect(body.status).toBe('ok');
    });

    it('returns checks.db: "connected"', async () => {
      const app = createHealthApp(makeMockSql(true));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      const body = await res.json();
      expect(body.checks.db).toBe('connected');
    });

    it('returns timestamp as ISO string', async () => {
      const app = createHealthApp(makeMockSql(true));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      const body = await res.json();
      expect(typeof body.timestamp).toBe('string');
      expect(() => new Date(body.timestamp)).not.toThrow();
    });
  });

  describe('when database is unreachable', () => {
    it('returns HTTP 503', async () => {
      const app = createHealthApp(makeMockSql(false));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      expect(res.status).toBe(503);
    });

    it('returns checks.db: "failed"', async () => {
      const app = createHealthApp(makeMockSql(false));
      const req = new Request('http://localhost/health');
      const res = await app.fetch(req);
      const body = await res.json();
      expect(body.checks.db).toBe('failed');
    });
  });
});
```

**Nota:** O mock de `Sql` precisa ser compatível com a assinatura que `checkDatabase` usa (`sql\`SELECT 1\``). A função `checkDatabase` usa template literal tags — o mock deve ser callable como uma função tagged template. Ajustar o mock se necessário para que `sql\`SELECT 1\`` funcione sem rejeitar de forma inesperada.

Se o mock de template literal for muito complexo, uma alternativa é usar `mock.module` para interceptar a função `checkDatabase` diretamente:

```typescript
// Alternativa usando mock.module se o mock de Sql template literal for problemático
import { mock } from 'bun:test';

// Mock checkDatabase diretamente
const mockCheckDatabase = mock(async () => true); // ou false para DB failed tests
mock.module('./health.js', () => ({
  checkDatabase: mockCheckDatabase,
  performHealthCheck: async (sql: unknown) => ({
    status: 'ok' as const,
    checks: { db: 'connected' as const },
    version: 'test',
    timestamp: new Date().toISOString(),
  }),
}));
```

Usar a abordagem que resultar em testes mais limpos e funcionais com `bun test`.
  </action>
  <acceptance_criteria>
    - `ls packages/observability/src/server.test.ts` retorna exit code 0 (arquivo existe)
    - `grep -c "describe('GET /health" packages/observability/src/server.test.ts` retorna `1`
    - `grep -c "expect(res.status).toBe(200)" packages/observability/src/server.test.ts` retorna pelo menos `1`
    - `grep -c "expect(res.status).toBe(503)" packages/observability/src/server.test.ts` retorna pelo menos `1`
    - `grep -c "checks.db" packages/observability/src/server.test.ts` retorna pelo menos `2`
    - `cd /root/Brain && pnpm test --filter=@brain-pkg/observability` termina com exit code 0 (testes passam ou são todos todo)
    - Nenhum teste tem `it.todo()` com apenas 1 argumento (evitar regressão do Gap 1)
  </acceptance_criteria>
</task>

<task id="5.3" type="modify">
  <title>Atualizar barrel export e commitar — expor server.ts de @brain-pkg/observability</title>
  <read_first>
    - packages/observability/src/index.ts (barrel export atual — adicionar re-exports de server.ts)
    - packages/observability/src/server.ts (arquivo criado em task 5.1 — verificar exports)
    - packages/observability/tsconfig.json (verificar que server.test.ts será excluído do build via fix da task 4.1)
  </read_first>
  <action>
**Passo 1: Atualizar `packages/observability/src/index.ts`.**

Estado atual:
```typescript
// Logger exports
export { createLogger } from './logger.js';
export type { LogContext } from './logger.js';

// Health check exports
export { checkDatabase, performHealthCheck } from './health.js';
export type { HealthCheckResult } from './health.js';
```

Estado final (adicionar seção de server exports):
```typescript
// Logger exports
export { createLogger } from './logger.js';
export type { LogContext } from './logger.js';

// Health check exports
export { checkDatabase, performHealthCheck } from './health.js';
export type { HealthCheckResult } from './health.js';

// HTTP server exports (OBS-02: GET /health endpoint)
export { createHealthApp, startServer } from './server.js';
```

**Passo 2: Verificar que o build do package observability ainda passa.**

```bash
cd /root/Brain/packages/observability && bun run build
```

Deve terminar sem erros.

**Passo 3: Executar `pnpm build` completo do monorepo.**

```bash
cd /root/Brain && pnpm build
```

Deve terminar com exit code 0.

**Passo 4: Executar `pnpm test` para confirmar testes novos passam.**

```bash
cd /root/Brain && pnpm test
```

**Passo 5: Commitar todos os arquivos do Gap 3.**

Arquivos a commitar:
- `packages/observability/src/server.ts`
- `packages/observability/src/server.test.ts`
- `packages/observability/src/index.ts`
- `packages/observability/package.json` (se hono foi adicionado)
- `pnpm-lock.yaml` (atualizado pela adição do hono)

Mensagem do commit:

```
✨ feat(01-05): add GET /health HTTP endpoint to observability package

- Create packages/observability/src/server.ts with Hono app
  exposing GET /health via createHealthApp(sql) and startServer(sql, port)
- Implement D-13 response shape: { status, checks: { db }, version, timestamp }
- Implement D-14 HTTP status codes: 200 ok, 503 db failed, 500 error
- Add server.test.ts with tests for 200/503 response codes and response shape
- Export createHealthApp and startServer from @brain-pkg/observability barrel

Closes gap: SC5 (missing GET /health HTTP endpoint)
```
  </action>
  <acceptance_criteria>
    - `grep -c 'createHealthApp\|startServer' packages/observability/src/index.ts` retorna `1` (linha com ambos os exports)
    - `cd /root/Brain && pnpm build` termina com exit code 0
    - `cd /root/Brain && pnpm test` termina com exit code 0
    - `git status packages/observability/src/server.ts` mostra arquivo commitado
    - `git status packages/observability/src/index.ts` mostra arquivo commitado
    - `git log --oneline -1` mostra commit com `feat(01-05):`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Após completar todas as tasks:

1. `pnpm build` — deve terminar com exit 0 em todos os packages
2. `pnpm test` — deve terminar com exit 0 (testes novos em server.test.ts passam)
3. `grep 'createHealthApp\|startServer' packages/observability/src/index.ts` — deve mostrar os exports
4. `cat packages/observability/src/server.ts` — deve conter `app.get('/health', ...)` e os HTTP status codes 200/503
5. `git log --oneline -5` — deve mostrar os 2 commits de gap closure (01-04 e 01-05)
</verification>

<success_criteria>
- `GET /health` retorna `{ status, checks: { db }, version, timestamp }` com HTTP 200 quando DB conectado
- `GET /health` retorna HTTP 503 quando `checks.db === 'failed'`
- `createHealthApp` e `startServer` são exportados de `@brain-pkg/observability`
- Testes em `server.test.ts` cobrem os casos 200 e 503 com assertions nos campos do response body
- `pnpm build` e `pnpm test` passam com exit 0 após os changes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-foundation/01-05-SUMMARY.md` documentando:
- Como server.ts foi estruturado (createHealthApp vs startServer)
- Como o mock de Sql foi implementado nos testes
- Commit hash do commit de fechamento do Gap 3
- Qualquer decisão de implementação tomada (ex: mock strategy escolhida)
</output>
```
