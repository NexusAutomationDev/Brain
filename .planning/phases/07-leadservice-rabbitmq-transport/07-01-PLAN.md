---
phase: 07-leadservice-rabbitmq-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/leads/lead-service.ts
  - packages/core/src/leads/__tests__/lead-service.test.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
  - packages/core/src/index.ts
  - packages/transport/src/webhook/handler.ts
  - packages/transport/src/webhook/handler.test.ts
autonomous: true
requirements:
  - LEAD-02
  - LEAD-03
  - TRP-01

must_haves:
  truths:
    - "Primeira mensagem de um número desconhecido cria o lead automaticamente — upsert por numero"
    - "unique_id (IDLead) nunca é sobrescrito em chamadas subsequentes do mesmo numero"
    - "BrainRunner.run() retorna null silenciosamente quando lead.iaAtivada=false"
    - "Nenhuma chamada LLM ocorre quando iaAtivada=false"
    - "WebhookTransport handler trata resultado null de runner.run() sem TypeError"
    - "POST /api/v1/webhook sem IDLead retorna 400 com body { error: 'Invalid BrainEvent' }"
  artifacts:
    - path: "packages/core/src/leads/lead-service.ts"
      provides: "Classe LeadService com upsertLead e getByNumero"
      exports: ["LeadService", "Lead"]
    - path: "packages/core/src/leads/__tests__/lead-service.test.ts"
      provides: "Testes unitários LEAD-02 com mock de sql/drizzle"
      contains: "upsertLead"
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner.run() com gate ia_ativada"
      contains: "Promise<BrainRunResult | null>"
    - path: "packages/core/src/runner/__tests__/brain-runner.test.ts"
      provides: "Testes de gate ia_ativada — LEAD-03"
      contains: "ia_ativada"
  key_links:
    - from: "packages/core/src/runner/runner.ts BrainRunner.run()"
      to: "packages/core/src/leads/lead-service.ts LeadService.upsertLead()"
      via: "this.leadService.upsertLead(event.Numero, event.IDLead, event.Name)"
      pattern: "leadService\\.upsertLead"
    - from: "packages/transport/src/webhook/handler.ts createWebhookApp()"
      to: "runner.run() → null"
      via: "if (result === null) return c.json({ status: 'ignored' }, 200)"
      pattern: "status.*ignored"
---

<objective>
Implementa o LeadService (LEAD-02) e o gate ia_ativada no BrainRunner (LEAD-03).

Purpose: Garantir que todo lead seja cadastrado automaticamente na primeira mensagem e que leads com ia_ativada=false sejam descartados silenciosamente antes de qualquer chamada LLM. Qualquer transport que chamar runner.run() obtém ambos os comportamentos automaticamente (D-03).

Output: LeadService em packages/core/src/leads/, BrainRunner.run() com retorno nullable, WebhookTransport tratando null, testes unitários para LEAD-02 e LEAD-03, teste explícito TRP-01 para IDLead ausente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-leadservice-rabbitmq-transport/07-CONTEXT.md
@.planning/phases/07-leadservice-rabbitmq-transport/07-RESEARCH.md

# Referências de fase anterior
@.planning/phases/06-leads-schema-migration/06-CONTEXT.md
</context>

<interfaces>
<!-- Interfaces e contratos que o executor precisa. Extraídos do codebase. -->

De packages/database/src/schema/tables.ts:
```typescript
export const leads = pgTable('leads', {
  id: uuid('id').primaryKey().defaultRandom(),
  uniqueId: text('unique_id').notNull(),
  nome: text('nome'),
  numero: text('numero').notNull(),
  iaAtivada: boolean('ia_ativada').notNull().default(true),
  fullpp: boolean('fullpp'),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
}, (table) => ({
  numeroIdx: uniqueIndex('leads_numero_unique_idx').on(table.numero),
}));
```

De packages/core/src/runner/runner.ts (assinatura ATUAL — será alterada):
```typescript
export interface BrainRunResult {
  reply: string;
}

// Assinatura ATUAL (mudar para Promise<BrainRunResult | null>):
async run(event: BrainEvent): Promise<BrainRunResult>

// BrainRunner já tem this.sql: Sql no construtor — LeadService instanciado em _compileGraph()
// ou no construtor usando this.sql diretamente
```

De packages/transport/src/webhook/handler.ts (linha 52-57 — trecho a ATUALIZAR):
```typescript
// Trecho atual que vai quebrar com result === null:
if (runner) {
  try {
    const result = await runner.run(event);
    return c.json({ status: "ok", reply: result.reply }); // ← TypeError se result é null
  }
}

// Trecho atualizado:
if (runner) {
  try {
    const result = await runner.run(event);
    if (result === null) {
      return c.json({ status: "ignored" }, 200); // lead com ia_ativada=false
    }
    return c.json({ status: "ok", reply: result.reply });
  }
}
```

De packages/transport/src/webhook/handler.ts (IBrainRunnerLike — será atualizada):
```typescript
// Interface ATUAL:
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{ reply: string }>;
}

// Interface ATUALIZADA (compatível com BrainRunner.run() nullable):
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{ reply: string } | null>;
}
```

De packages/memory/src/long-term.ts (padrão onConflictDoUpdate — referência):
```typescript
// Padrão confirmado no codebase para upsert Drizzle:
await db.insert(table).values({...}).onConflictDoUpdate({ target: col, set: {...} }).returning();
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task W0: Criar stubs de teste para LEAD-02 e TRP-01</name>
  <files>
    packages/core/src/leads/__tests__/lead-service.test.ts
    packages/transport/src/webhook/handler.test.ts
  </files>
  <read_first>
    - packages/core/src/runner/__tests__/brain-runner.test.ts (padrão de mock.module e estrutura de describe)
    - packages/transport/src/webhook/handler.test.ts (arquivo existente — verificar testes atuais antes de adicionar)
    - packages/database/src/schema/tables.ts (campos do Lead para escrever expectations corretas)
  </read_first>
  <behavior>
    LEAD-02 (lead-service.test.ts):
    - Test 1: upsertLead com numero novo cria lead e retorna objeto com campos { id, uniqueId, numero, iaAtivada }
    - Test 2: upsertLead com mesmo numero não cria duplicata — segundo upsert retorna lead existente com mesmo uniqueId
    - Test 3: upsertLead com mesmo numero não sobrescreve uniqueId — campo uniqueId do retorno é igual ao inserido originalmente
    - Test 4: upsertLead atualiza nome se fornecido na segunda chamada

    TRP-01 (handler.test.ts — adicionar ao describe existente):
    - Test 5: POST /api/v1/webhook sem IDLead (payload com Name, Message, Numero mas sem IDLead) retorna 400 com body.error === "Invalid BrainEvent"
    - Verificar se já existe teste cobrindo "IDLead ausente" — se sim, apenas adicionar comentário "# TRP-01 covered"
  </behavior>
  <action>
    1. Criar diretório packages/core/src/leads/__tests__/ se não existir.

    2. Criar packages/core/src/leads/__tests__/lead-service.test.ts com stubs RED que falham:

    ```typescript
    // LEAD-02: LeadService — upsert por numero, uniqueId nunca sobrescrito
    import { describe, it, expect, mock, beforeEach } from "bun:test";

    // Mock drizzle-orm/postgres-js ANTES do import do LeadService
    const mockReturning = mock(async () => [
      {
        id: "uuid-1",
        uniqueId: "lead-abc",
        numero: "5511999990001",
        nome: "João",
        iaAtivada: true,
        fullpp: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      },
    ]);
    const mockOnConflictDoUpdate = mock(() => ({ returning: mockReturning }));
    const mockValues = mock(() => ({ onConflictDoUpdate: mockOnConflictDoUpdate }));
    const mockInsert = mock(() => ({ values: mockValues }));
    const mockSelect = mock(() => ({ from: mock(() => ({ where: mock(() => ({ limit: mock(async () => []) })) })) }));
    const mockDb = { insert: mockInsert, select: mockSelect };

    mock.module("drizzle-orm/postgres-js", () => ({
      drizzle: mock(() => mockDb),
    }));

    // Mock drizzle-orm para eq()
    mock.module("drizzle-orm", () => ({
      eq: mock((col: unknown, val: unknown) => ({ col, val })),
    }));

    import { LeadService } from "../lead-service.js";

    describe("LeadService (LEAD-02)", () => {
      let service: LeadService;

      beforeEach(() => {
        mockReturning.mockClear();
        mockOnConflictDoUpdate.mockClear();
        mockValues.mockClear();
        mockInsert.mockClear();
        service = new LeadService({} as never);
      });

      it("upsertLead retorna lead com campos esperados para numero novo", async () => {
        const lead = await service.upsertLead("5511999990001", "lead-abc", "João");
        expect(lead).toHaveProperty("id");
        expect(lead.uniqueId).toBe("lead-abc");
        expect(lead.numero).toBe("5511999990001");
        expect(lead.iaAtivada).toBe(true);
      });

      it("upsertLead chama onConflictDoUpdate com target leads.numero", async () => {
        await service.upsertLead("5511999990001", "lead-abc", "João");
        expect(mockOnConflictDoUpdate).toHaveBeenCalledTimes(1);
        const callArg = mockOnConflictDoUpdate.mock.calls[0][0] as Record<string, unknown>;
        // set NÃO deve conter uniqueId (LEAD-02: nunca sobrescrito)
        expect(callArg.set).not.toHaveProperty("uniqueId");
      });

      it("upsertLead com nome undefined não inclui nome no set", async () => {
        await service.upsertLead("5511999990001", "lead-abc");
        const setArg = (mockOnConflictDoUpdate.mock.calls[0][0] as Record<string, unknown>).set as Record<string, unknown>;
        expect(setArg).toHaveProperty("updatedAt");
      });
    });
    ```

    3. Em packages/transport/src/webhook/handler.test.ts, verificar o teste existente na linha 87-97. Esse teste já cobre "campo faltando → 400" mas com múltiplos campos ausentes. Adicionar ao describe existente um teste específico para IDLead ausente (TRP-01):

    ```typescript
    it("POST /api/v1/webhook sem IDLead retorna 400 com error Invalid BrainEvent (TRP-01)", async () => {
      const req = new Request("http://localhost/api/v1/webhook", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ Name: "João", Message: "Olá", Numero: "5511999990001" }), // IDLead ausente
      });
      const res = await app.fetch(req);
      expect(res.status).toBe(400);
      const body = await res.json() as Record<string, unknown>;
      expect(body.error).toBe("Invalid BrainEvent");
    });
    ```
  </action>
  <verify>
    <automated>cd /root/Brain/packages/core && bun test src/leads 2>&1 | tail -5</automated>
    Esperado: testes falham com "Cannot find module '../lead-service.js'" — confirmando RED state.
    Para handler.test.ts: cd /root/Brain/packages/transport && bun test src/webhook/handler.test.ts 2>&1 | tail -5 — deve passar (TRP-01 teste adicionado a arquivo existente que já importa o handler correto).
  </verify>
  <acceptance_criteria>
    - Arquivo packages/core/src/leads/__tests__/lead-service.test.ts existe
    - Arquivo contém string "upsertLead"
    - Arquivo contém string "uniqueId"
    - Arquivo contém string "onConflictDoUpdate"
    - packages/transport/src/webhook/handler.test.ts contém string "IDLead" e "TRP-01"
    - bun test packages/transport/src/webhook/handler.test.ts passa (testes existentes + novo TRP-01)
  </acceptance_criteria>
  <done>Stubs de teste existem, TRP-01 coberto no handler.test.ts, testes de lead-service em RED state aguardando implementação.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implementar LeadService (LEAD-02)</name>
  <files>
    packages/core/src/leads/lead-service.ts
    packages/core/src/index.ts
  </files>
  <read_first>
    - packages/core/src/leads/__tests__/lead-service.test.ts (stubs criados em W0 — implementar contra esses testes)
    - packages/database/src/schema/tables.ts (schema completo do leads, campos exatos)
    - packages/database/src/index.ts (confirmar que leads é exportado do barrel)
    - packages/memory/src/long-term.ts (padrão de drizzle(sql) + onConflictDoUpdate para replicar)
    - packages/core/src/index.ts (barrel existente — adicionar export de LeadService)
  </read_first>
  <behavior>
    - LeadService instanciado com sql (postgres.js Sql) no construtor: `new LeadService(sql)`
    - upsertLead(numero, uniqueId, nome?): Promise<Lead> — usa Drizzle insert().onConflictDoUpdate()
    - onConflictDoUpdate target: leads.numero (UNIQUE index); set contém nome e updatedAt mas NÃO uniqueId (LEAD-02)
    - getByNumero(numero): Promise<Lead | null> — select + where eq + limit 1
    - Lead type = typeof leads.$inferSelect (Drizzle inferido)
    - Erro esperado se returning() retornar array vazio: throw new Error("upsertLead returned no rows")
  </behavior>
  <action>
    1. Criar diretório packages/core/src/leads/ se não existir.

    2. Criar packages/core/src/leads/lead-service.ts:

    ```typescript
    // LEAD-02: LeadService — cadastro automático de leads por numero + gate ia_ativada (D-01, D-02, D-03)
    // D-01: localização packages/core — junto ao BrainRunner (único consumidor em v1.1)
    // D-02: classe com sql injetado no construtor; métodos upsertLead + getByNumero
    import { drizzle } from "drizzle-orm/postgres-js";
    import { eq } from "drizzle-orm";
    import { leads } from "@brain-pkg/database";
    import type { Sql } from "postgres";

    /** Tipo Lead derivado do schema Drizzle — campos da tabela leads */
    export type Lead = typeof leads.$inferSelect;

    /**
     * LEAD-02: Gerencia leads — cadastro automático e consulta por numero.
     *
     * D-02: Interface: class LeadService { constructor(sql: Sql) }
     * D-03: Chamado por BrainRunner.run() — não pelos transport handlers.
     *
     * Segurança: iaAtivada vem do banco (upsert), nunca do payload externo.
     */
    export class LeadService {
      private readonly db: ReturnType<typeof drizzle>;

      constructor(sql: Sql) {
        this.db = drizzle(sql);
      }

      /**
       * LEAD-02: Upsert atômico por numero.
       * - INSERT se numero não existe — cria lead com uniqueId e nome fornecidos.
       * - UPDATE se numero já existe — atualiza nome e updatedAt; uniqueId NUNCA é sobrescrito.
       *
       * @param numero - Número do WhatsApp/CRM (chave única de identificação)
       * @param uniqueId - IDLead do payload (gerado pela integração, nunca sobrescrito no update)
       * @param nome - Nome opcional do lead (vem de event.Name)
       */
      async upsertLead(numero: string, uniqueId: string, nome?: string): Promise<Lead> {
        const rows = await this.db
          .insert(leads)
          .values({
            numero,
            uniqueId,
            nome: nome ?? null,
          })
          .onConflictDoUpdate({
            target: leads.numero,
            set: {
              // LEAD-02: uniqueId ausente do set — nunca sobrescrito após primeiro insert
              nome: nome ?? null,
              updatedAt: new Date(),
            },
          })
          .returning();

        if (!rows[0]) {
          throw new Error(`upsertLead returned no rows for numero=${numero}`);
        }

        return rows[0];
      }

      /**
       * Busca lead por numero. Retorna null se não existir.
       *
       * @param numero - Número do WhatsApp/CRM
       */
      async getByNumero(numero: string): Promise<Lead | null> {
        const rows = await this.db
          .select()
          .from(leads)
          .where(eq(leads.numero, numero))
          .limit(1);

        return rows[0] ?? null;
      }
    }
    ```

    3. Atualizar packages/core/src/index.ts — adicionar exports do LeadService:

    Após a linha `export { loadPrompts } from "./prompts/loader.js";`, adicionar:
    ```typescript
    // LEAD-02: LeadService
    export { LeadService } from "./leads/lead-service.js";
    export type { Lead } from "./leads/lead-service.js";
    ```
  </action>
  <verify>
    <automated>cd /root/Brain/packages/core && bun test src/leads 2>&1</automated>
    Esperado: todos os testes de lead-service passam (GREEN).
  </verify>
  <acceptance_criteria>
    - Arquivo packages/core/src/leads/lead-service.ts existe
    - Contém "export class LeadService"
    - Contém "export type Lead = typeof leads.$inferSelect"
    - Contém "onConflictDoUpdate"
    - O campo "uniqueId" NÃO aparece dentro do bloco "set:" do onConflictDoUpdate (grep: set deve conter "nome" e "updatedAt" mas não "uniqueId")
    - Contém "getByNumero"
    - packages/core/src/index.ts contém "export { LeadService }"
    - cd /root/Brain/packages/core && bun test src/leads sai com código 0
  </acceptance_criteria>
  <done>LeadService implementado, todos os testes LEAD-02 passam, LeadService exportado do barrel de core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar LeadService no BrainRunner + gate ia_ativada (LEAD-03)</name>
  <files>
    packages/core/src/runner/runner.ts
    packages/core/src/runner/__tests__/brain-runner.test.ts
    packages/transport/src/webhook/handler.ts
  </files>
  <read_first>
    - packages/core/src/runner/runner.ts (arquivo COMPLETO — entender construtor, _compileGraph, run() atual)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (testes existentes — NÃO quebrar; adicionar novos ao describe existente)
    - packages/transport/src/webhook/handler.ts (arquivo COMPLETO — IBrainRunnerLike + createWebhookApp + trecho result.reply que vai quebrar)
    - packages/core/src/leads/lead-service.ts (implementado em Task 1 — importar corretamente)
  </read_first>
  <behavior>
    BrainRunner.run() modificado:
    - Retorna Promise<BrainRunResult | null>
    - Antes de qualquer processamento LLM: chama this.leadService.upsertLead(event.Numero, event.IDLead, event.Name)
    - Se lead.iaAtivada === false: this.logger.debug({ numero: event.Numero }, "ia_ativada=false — ignoring"); return null
    - Se lead.iaAtivada === true: continua para LLM (restante do método inalterado)
    - LeadService instanciado no construtor de BrainRunner: this.leadService = new LeadService(this.sql)

    IBrainRunnerLike atualizado:
    - run(event: BrainEvent): Promise<{ reply: string } | null>

    createWebhookApp() atualizado:
    - Após const result = await runner.run(event):
    - if (result === null) return c.json({ status: "ignored" }, 200)
    - else return c.json({ status: "ok", reply: result.reply })

    Novos testes em brain-runner.test.ts:
    - Test: run() retorna null quando mock de leadService retorna lead com iaAtivada=false
    - Test: run() NÃO chama compiledGraph.invoke() quando iaAtivada=false
    - Test: run() retorna { reply: string } quando iaAtivada=true (comportamento atual mantido)
  </behavior>
  <action>
    1. Modificar packages/core/src/runner/runner.ts:

    a) Adicionar import do LeadService:
    ```typescript
    import { LeadService } from "../leads/lead-service.js";
    import type { Lead } from "../leads/lead-service.js";
    ```

    b) Adicionar campo privado na classe BrainRunner:
    ```typescript
    private leadService!: LeadService; // inicializado no construtor
    ```

    c) No construtor, após as atribuições existentes, adicionar:
    ```typescript
    this.leadService = new LeadService(options.sql);
    ```

    d) Alterar assinatura de run():
    ```typescript
    async run(event: BrainEvent): Promise<BrainRunResult | null> {
    ```

    e) Logo após o guard `if (!this.compiledGraph || !this.memoryManager)`, adicionar ANTES da linha `const threadId = event.Numero;`:
    ```typescript
    // D-06: Fluxo — upsert lead → gate ia_ativada → LLM (LEAD-02, LEAD-03)
    const lead: Lead = await this.leadService.upsertLead(
      event.Numero,
      event.IDLead,
      event.Name
    );

    // D-04/D-05: Gate ia_ativada — retorna null silenciosamente (LEAD-03)
    // Segurança: iaAtivada vem do banco (upsert), nunca do payload externo
    if (!lead.iaAtivada) {
      this.logger.debug({ numero: event.Numero }, "ia_ativada=false — ignoring message");
      return null;
    }
    ```

    f) Atualizar o comentário em threadId:
    ```typescript
    // Phase 8: substituir por lead.uniqueId após Phase 7 entregar o lead object
    // Por ora: event.Numero como antes
    const threadId = event.Numero;
    ```

    2. Modificar packages/transport/src/webhook/handler.ts:

    a) Atualizar IBrainRunnerLike:
    ```typescript
    export interface IBrainRunnerLike {
      run(event: BrainEvent): Promise<{ reply: string } | null>;
    }
    ```

    b) Atualizar trecho dentro de createWebhookApp() que usa result:
    ```typescript
    if (runner) {
      try {
        const result = await runner.run(event);
        // LEAD-03: runner.run() retorna null quando ia_ativada=false
        if (result === null) {
          return c.json({ status: "ignored" }, 200);
        }
        return c.json({ status: "ok", reply: result.reply });
      } catch (err) {
        console.error({ err }, "BrainRunner.run() failed");
        return c.json({ error: "Internal error" }, 500);
      }
    }
    ```

    3. Adicionar testes ao packages/core/src/runner/__tests__/brain-runner.test.ts:

    Dentro do describe("BrainRunner") existente, após os testes já presentes, adicionar:

    ```typescript
    // --- Testes LEAD-03: gate ia_ativada ---

    describe("gate ia_ativada (LEAD-03)", () => {
      // Mock LeadService para controlar iaAtivada
      const mockUpsertLead = mock(async () => ({
        id: "uuid-1",
        uniqueId: "lead-abc",
        numero: "5511999990001",
        nome: "Test User",
        iaAtivada: false, // gate deve bloquear
        fullpp: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      }));

      beforeEach(() => {
        mockUpsertLead.mockClear();
      });

      test("run() retorna null quando lead.iaAtivada=false", async () => {
        mock.module("../../leads/lead-service.js", () => ({
          LeadService: mock(function () {
            return { upsertLead: mockUpsertLead, getByNumero: mock(async () => null) };
          }),
        }));

        const brain = makeBrain(["system"]);
        const runner = new BrainRunner({ brain, sql: {} as never, toolsRegistry: registry });
        await runner.init();

        const result = await runner.run(makeEvent());
        expect(result).toBeNull();
      });

      test("run() NÃO chama compiledGraph.invoke() quando iaAtivada=false", async () => {
        mock.module("../../leads/lead-service.js", () => ({
          LeadService: mock(function () {
            return { upsertLead: mockUpsertLead, getByNumero: mock(async () => null) };
          }),
        }));

        const brain = makeBrain(["system"]);
        const runner = new BrainRunner({ brain, sql: {} as never, toolsRegistry: registry });
        await runner.init();

        await runner.run(makeEvent());

        // compiledGraph.invoke deve NÃO ter sido chamado
        const compiledInvoke = (brain.buildGraph as ReturnType<typeof mock>).mock.results[0]?.value?.compile?.mock?.results[0]?.value?.invoke as ReturnType<typeof mock> | undefined;
        if (compiledInvoke) {
          expect(compiledInvoke).not.toHaveBeenCalled();
        }
      });

      test("run() retorna { reply } quando iaAtivada=true", async () => {
        const mockUpsertActive = mock(async () => ({
          id: "uuid-1",
          uniqueId: "lead-abc",
          numero: "5511999990001",
          nome: "Test User",
          iaAtivada: true, // ativo — deve processar LLM
          fullpp: null,
          createdAt: new Date(),
          updatedAt: new Date(),
        }));

        mock.module("../../leads/lead-service.js", () => ({
          LeadService: mock(function () {
            return { upsertLead: mockUpsertActive, getByNumero: mock(async () => null) };
          }),
        }));

        const brain = makeBrain(["system"]);
        const runner = new BrainRunner({ brain, sql: {} as never, toolsRegistry: registry });
        await runner.init();

        const result = await runner.run(makeEvent());
        expect(result).not.toBeNull();
        expect(result?.reply).toBe("test reply");
      });
    });
    ```

    NOTA IMPORTANTE: O mock.module("../../leads/lead-service.js") deve ser registrado ANTES do import do BrainRunner se possível. Como o arquivo já importa BrainRunner no topo, usar mock.module dentro de cada teste força o mock por teste. Verificar o padrão do arquivo brain-runner.test.ts existente para alinhar. Se mock.module dentro de describe não funcionar, criar módulo mock no topo do arquivo junto aos outros mocks.

    Alternativa mais robusta — adicionar ao bloco de mocks do TOPO do arquivo (antes dos imports):
    ```typescript
    const mockUpsertLead = mock(async () => ({
      id: "uuid-1", uniqueId: "lead-abc", numero: "5511999990001",
      nome: "Test User", iaAtivada: true, fullpp: null,
      createdAt: new Date(), updatedAt: new Date(),
    }));

    mock.module("../../leads/lead-service.js", () => ({
      LeadService: mock(function () {
        return { upsertLead: mockUpsertLead, getByNumero: mock(async () => null) };
      }),
    }));
    ```
    E nos testes gate, usar `mockUpsertLead.mockImplementationOnce(async () => ({ ...lead, iaAtivada: false }))`.
  </action>
  <verify>
    <automated>cd /root/Brain/packages/core && bun test src/runner/__tests__/brain-runner.test.ts 2>&1</automated>
    Esperado: TODOS os testes passam (antigos + novos gate ia_ativada). Saída com "pass" para todos.
    <automated>cd /root/Brain/packages/transport && bun test src/webhook/handler.test.ts 2>&1</automated>
    Esperado: todos os testes do handler passam (incluindo o novo TRP-01 e o teste com runner que agora trata null).
  </verify>
  <acceptance_criteria>
    - packages/core/src/runner/runner.ts contém "Promise<BrainRunResult | null>"
    - packages/core/src/runner/runner.ts contém "leadService.upsertLead"
    - packages/core/src/runner/runner.ts contém "ia_ativada=false"
    - packages/core/src/runner/runner.ts contém "return null" (dentro de run(), após o gate)
    - packages/core/src/runner/runner.ts importa "from '../leads/lead-service.js'"
    - packages/transport/src/webhook/handler.ts contém "status.*ignored" (grep -E)
    - packages/transport/src/webhook/handler.ts IBrainRunnerLike.run retorna "Promise<{ reply: string } | null>"
    - packages/core/src/runner/__tests__/brain-runner.test.ts contém "ia_ativada"
    - cd /root/Brain/packages/core && bun test src/runner/__tests__/brain-runner.test.ts sai com código 0
    - cd /root/Brain/packages/transport && bun test src/webhook/handler.test.ts sai com código 0
  </acceptance_criteria>
  <done>BrainRunner.run() retorna null quando iaAtivada=false, todos os testes LEAD-03 passam, WebhookTransport trata null com { status: "ignored" }.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload → BrainEventSchema | Input externo (WhatsApp/CRM) validado pelo Zod antes de qualquer processamento |
| payload.IDLead → banco | IDLead do payload é usado como uniqueId no INSERT; nunca sobrescrito no UPDATE |
| banco → iaAtivada | iaAtivada vem do banco (SELECT pós-upsert), nunca do payload do caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | BrainEventSchema | mitigate | Zod safeParse valida {Name, Message, Numero, IDLead} com string.min(1) — rejeita 400 antes de upsert. Já implementado em Phase 5, testado formalmente em Task W0 (TRP-01). |
| T-07-02 | Elevation of Privilege | LeadService.upsertLead | mitigate | iaAtivada não está no payload e não é aceito como input — vem exclusivamente do banco pós-upsert. Caller não pode manipular o gate. |
| T-07-03 | Information Disclosure | createWebhookApp null result | mitigate | Quando result === null (ia_ativada=false), retornar { status: "ignored" } sem expor razão interna (ASVS V5 — não confirmar nem negar razão do descarte). |
| T-07-04 | DoS | upsertLead sem limite de rate | accept | Rate limiting é responsabilidade da camada de infra (nginx/API gateway) — fora do escopo do SDK. Aceito para v1.1. |
| T-07-05 | Tampering | unique_id sobrescrito | mitigate | onConflictDoUpdate.set NÃO inclui uniqueId — testado em Task W0 (assertion: set não tem uniqueId). Garante que IDLead original é preservado mesmo com payload diferente em mensagem subsequente. |
</threat_model>

<verification>
Após execução de todos os tasks:

```bash
# Verificar que testes passam em packages/core
cd /root/Brain/packages/core && bun test

# Verificar que testes passam em packages/transport
cd /root/Brain/packages/transport && bun test

# Verificar exports do barrel
grep "LeadService" /root/Brain/packages/core/src/index.ts

# Verificar assinatura nullable no runner
grep "BrainRunResult | null" /root/Brain/packages/core/src/runner/runner.ts

# Verificar gate na handler
grep "ignored" /root/Brain/packages/transport/src/webhook/handler.ts

# Suite completa
cd /root/Brain && bun run test
```
</verification>

<success_criteria>
- LeadService.upsertLead() cria lead na primeira mensagem, não duplica em chamadas subsequentes com mesmo numero
- uniqueId nunca sobrescrito no update (confirmado por teste de assertion no set do onConflictDoUpdate)
- BrainRunner.run() retorna null para leads com iaAtivada=false sem chamar LLM
- WebhookTransport retorna { status: "ignored" } com 200 quando runner.run() retorna null
- POST /api/v1/webhook sem IDLead retorna 400 com body.error === "Invalid BrainEvent" (TRP-01)
- cd /root/Brain/packages/core && bun test sai com código 0
- cd /root/Brain/packages/transport && bun test sai com código 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07-leadservice-rabbitmq-transport/07-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Padrão de mock.module usado no brain-runner.test.ts (para Plan 02 replicar se necessário)
- Qualquer desvio de implementação em relação ao plano
</output>
