---
phase: 16-dynamic-responsemode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - packages/core/src/output/schema.ts
  - packages/core/src/tools/respond.ts
  - packages/core/src/index.ts
  - packages/core/src/tools/__tests__/respond.test.ts
  - packages/core/src/__tests__/unit/output/schema.test.ts
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03

must_haves:
  truths:
    - "createRespondTool() retorna uma tool LangChain com nome 'respond' e schema Zod com fullResponse, responseMode, mediaType e mediaUrl"
    - "ResponseMode type em packages/shared inclui 'undefined' como valor válido"
    - "ResponseModeSchema Zod em packages/core inclui 'undefined' no enum"
    - "BrainOutputSchema.parse() aceita responseMode: 'undefined' sem lançar ZodError"
    - "createRespondTool está exportada pelo barrel packages/core/src/index.ts"
  artifacts:
    - path: "packages/core/src/tools/respond.ts"
      provides: "Factory createRespondTool() com schema Zod v4 e validação condicional mediaType/mediaUrl"
      exports: ["createRespondTool"]
    - path: "packages/core/src/tools/__tests__/respond.test.ts"
      provides: "Testes unitários de createRespondTool — RESP-01 e RESP-02"
    - path: "packages/core/src/__tests__/unit/output/schema.test.ts"
      provides: "Casos de teste adicionais para responseMode 'undefined'"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/tools/respond.ts"
      via: "named export createRespondTool"
      pattern: "export.*createRespondTool"
    - from: "packages/core/src/output/schema.ts"
      to: "packages/shared/src/types/index.ts"
      via: "re-export de ResponseMode type"
      pattern: "export type.*ResponseMode.*from.*shared"
---

<objective>
Criar a factory `createRespondTool()` em packages/core e atualizar os contratos de tipo para suportar `responseMode: "undefined"` como valor explícito.

Purpose: Este plano entrega a peça central do padrão schema-as-tool — a respond tool que o LLM invoca para sinalizar o formato de resposta dinamicamente. Também atualiza `ResponseMode` e `ResponseModeSchema` para incluir "undefined" (D-04, D-06, D-07), necessário para o fallback D-10 na Fase 16.

Output: packages/core/src/tools/respond.ts (novo), atualizações em shared types + core schema + barrel export, testes unitários verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-dynamic-responsemode/16-CONTEXT.md
@.planning/phases/16-dynamic-responsemode/16-RESEARCH.md

@packages/shared/src/types/index.ts
@packages/core/src/output/schema.ts
@packages/core/src/tools/pause-session.ts
@packages/core/src/tools/finish-conversation.ts
@packages/core/src/index.ts
@packages/core/src/tools/__tests__/finish-conversation.test.ts
@packages/core/src/__tests__/unit/output/schema.test.ts
</context>

<interfaces>
<!-- Padrão de factory já estabelecido — createRespondTool() segue este padrão exatamente -->

De packages/core/src/tools/pause-session.ts:
```typescript
import { tool } from "@langchain/core/tools";
import { z } from "zod";

export function createPauseSessionTool(sql: Sql) {
  return tool(
    async (_args, config?) => { /* impl */ },
    { name: "pause_session", description: "...", schema: z.object({}) }
  );
}
```

De packages/core/src/output/schema.ts:
```typescript
export const ResponseModeSchema = z.enum(["text", "image", "audio", "video", "document"]);
export const BrainOutputSchema = z.object({ ... }).superRefine(...);
export type { ResponseMode, BrainOutput } from "@brain-pkg/shared";
```

De packages/shared/src/types/index.ts:
```typescript
export type ResponseMode = "text" | "image" | "audio" | "video" | "document";
```

De packages/core/src/index.ts:
```typescript
export { createPauseSessionTool } from "./tools/pause-session.js";
export { createFinishConversationTool } from "./tools/finish-conversation.js";
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar ResponseMode type + ResponseModeSchema para incluir "undefined"</name>
  <files>packages/shared/src/types/index.ts, packages/core/src/output/schema.ts, packages/core/src/__tests__/unit/output/schema.test.ts</files>

  <read_first>
    - packages/shared/src/types/index.ts — ler antes de editar (ver estado atual do ResponseMode type)
    - packages/core/src/output/schema.ts — ler antes de editar (ver estado atual de ResponseModeSchema e MODES_REQUIRING_MEDIA)
    - packages/core/src/__tests__/unit/output/schema.test.ts — ler antes de editar (ver casos existentes para não quebrar)
    - packages/core/src/tools/pause-session.ts — referência de estilo/comentários Zod
  </read_first>

  <behavior>
    - Test 1: BrainOutputSchema.parse({ fullResponse: "oi", responseMode: "undefined" }) não lança ZodError
    - Test 2: BrainOutputSchema.parse({ fullResponse: "oi", responseMode: "undefined" }) retorna responseMode === "undefined"
    - Test 3: ResponseModeSchema.parse("undefined") retorna "undefined" sem erro
    - Test 4: ResponseModeSchema.parse("invalid_value") lança ZodError (enum continua restrito)
    - Test 5: Todos os testes existentes em schema.test.ts continuam passando (text, audio, image, video, document)
  </behavior>

  <action>
1. Em `packages/shared/src/types/index.ts`, atualizar `ResponseMode` (D-06):
   - Trocar: `export type ResponseMode = "text" | "image" | "audio" | "video" | "document";`
   - Por: `export type ResponseMode = "undefined" | "text" | "image" | "audio" | "video" | "document";`
   - Atualizar o JSDoc: adicionar `- "undefined": sem preferência de formato específica — valor padrão do fallback D-10`

2. Em `packages/core/src/output/schema.ts`, atualizar `ResponseModeSchema` (D-07):
   - Trocar: `export const ResponseModeSchema = z.enum(["text", "image", "audio", "video", "document"]);`
   - Por: `export const ResponseModeSchema = z.enum(["undefined", "text", "image", "audio", "video", "document"]);`
   - NOTA: `MODES_REQUIRING_MEDIA` permanece `["image", "video", "document"] as const` — "undefined" não exige mídia (D-08). Não alterar.

3. Em `packages/core/src/__tests__/unit/output/schema.test.ts`, adicionar novos casos de teste no describe existente "BrainOutputSchema — PARSER-01":
   ```typescript
   test("undefined mode: fullResponse + responseMode são suficientes — fallback D-10", () => {
     expect(() =>
       BrainOutputSchema.parse({ fullResponse: "oi", responseMode: "undefined" })
     ).not.toThrow();
     const result = BrainOutputSchema.parse({ fullResponse: "oi", responseMode: "undefined" });
     expect(result.responseMode).toBe("undefined");
   });

   test("ResponseModeSchema aceita 'undefined' como valor explícito (D-04)", () => {
     const { ResponseModeSchema } = await import("../../../output/schema.js");
     expect(ResponseModeSchema.parse("undefined")).toBe("undefined");
   });
   ```
   ATENÇÃO: Usar import dinâmico no segundo teste para evitar cache do módulo. Ou importar `ResponseModeSchema` no topo do arquivo junto aos outros imports existentes.
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/output/schema.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `packages/shared/src/types/index.ts` contém: `"undefined" | "text" | "image" | "audio" | "video" | "document"`
    - `packages/core/src/output/schema.ts` contém: `z.enum(["undefined", "text", "image", "audio", "video", "document"])`
    - `bun test packages/core/src/__tests__/unit/output/schema.test.ts` sai com código 0 (todos os testes passam)
    - `grep -n "undefined" packages/core/src/__tests__/unit/output/schema.test.ts` retorna ao menos 2 linhas (novos testes adicionados)
    - `grep -n "MODES_REQUIRING_MEDIA" packages/core/src/output/schema.ts` ainda contém `["image", "video", "document"]` (não modificado)
  </acceptance_criteria>

  <done>ResponseMode e ResponseModeSchema incluem "undefined"; todos os testes de schema passam incluindo os novos casos de fallback D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar createRespondTool() + testes unitários + barrel export</name>
  <files>packages/core/src/tools/respond.ts, packages/core/src/tools/__tests__/respond.test.ts, packages/core/src/index.ts</files>

  <read_first>
    - packages/core/src/tools/pause-session.ts — padrão exato a replicar (factory function, tool(), z.object(), nome/descrição)
    - packages/core/src/tools/finish-conversation.ts — segundo exemplo do padrão factory
    - packages/core/src/tools/__tests__/finish-conversation.test.ts — padrão de teste das tool factories
    - packages/core/src/index.ts — ler antes de editar (adicionar export no final, sem remover nada)
    - packages/core/package.json — confirmar que zod versão é v4 (^4.4.3) antes de usar APIs
  </read_first>

  <behavior>
    - Test 1: createRespondTool() retorna objeto com .name === "respond"
    - Test 2: createRespondTool() retorna objeto com .description contendo "Sempre invoque" (instrução crítica contra PITFALL-6)
    - Test 3: Schema da tool tem campo fullResponse obrigatório (z.string())
    - Test 4: Schema da tool tem campo responseMode obrigatório com enum ["undefined", "text", "audio"]
    - Test 5: Schema da tool tem campo mediaType opcional com enum ["image", "file", "video", "audio"]
    - Test 6: Schema da tool tem campo mediaUrl opcional (z.string().url())
    - Test 7: Validação superRefine — mediaType sem mediaUrl lança ZodError
    - Test 8: Validação superRefine — mediaUrl sem mediaType lança ZodError
    - Test 9: Invocação da tool com args válidos retorna string (não lança, não retorna undefined)
    - Test 10: args.fullResponse chega inalterado na execução da tool (RESP-02)
  </behavior>

  <action>
1. Criar `packages/core/src/tools/respond.ts` com o seguinte conteúdo:

```typescript
// RESP-01: createRespondTool — schema-as-tool para responseMode dinâmico.
// D-03: Schema Zod v4 com fullResponse, responseMode, mediaType, mediaUrl.
// D-09: Factory stateless — sem closure sobre sql (respond tool não toca banco).
// PITFALL-6 mitigation: description instrui o LLM a sempre invocar esta tool.

import { tool } from "@langchain/core/tools";
import { z } from "zod";
import { createLogger } from "@brain-pkg/observability";

const logger = createLogger();

// D-03: Schema da respond tool — Zod v4 (packages/core usa ^4.4.3)
// PITFALL-5: usar z.string().url() (ZodString), NÃO z.url() (ZodURL — tipo diferente em Zod v4)
const respondToolSchema = z
  .object({
    fullResponse: z
      .string()
      .describe(
        "Mensagem completa da resposta em formato contínuo, sem divisões. OBRIGATÓRIO — sempre preencha com o texto completo da sua resposta."
      ),
    responseMode: z
      .enum(["undefined", "text", "audio"])
      .describe(
        "Modo de entrega da resposta. Use 'undefined' quando não há preferência de formato específica. Use 'audio' quando o usuário pedir explicitamente para ouvir a resposta. Use 'text' para respostas textuais explícitas."
      ),
    mediaType: z
      .enum(["image", "file", "video", "audio"])
      .optional()
      .describe(
        "Tipo da mídia enviada em mediaUrl. Obrigatório quando mediaUrl estiver presente."
      ),
    mediaUrl: z
      .string()
      .url()
      .optional()
      .describe(
        "URL direta de download de um arquivo de mídia (https://). Obrigatório quando mediaType estiver presente."
      ),
  })
  .superRefine((data, ctx) => {
    // D-03: validação condicional — mediaType e mediaUrl são co-dependentes
    if (data.mediaType && !data.mediaUrl) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        message: "mediaUrl é obrigatório quando mediaType está presente",
        path: ["mediaUrl"],
      });
    }
    if (data.mediaUrl && !data.mediaType) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        message: "mediaType é obrigatório quando mediaUrl está presente",
        path: ["mediaType"],
      });
    }
  });

/**
 * RESP-01: Cria a respond tool para responseMode dinâmico via schema-as-tool.
 *
 * O LLM invoca esta tool como último passo de cada turno, sinalizando o
 * formato de resposta desejado (fullResponse + responseMode).
 *
 * Factory stateless — não requer closure sobre sql ou outros recursos.
 * Compatível com OpenAI e Anthropic via bindTools() (RESP-03).
 *
 * @returns DynamicStructuredTool com nome "respond"
 */
export function createRespondTool() {
  return tool(
    async (args) => {
      // D-09: apenas loga — o nó respond em brain.ts lê state.messages para extrair os args
      logger.info({ responseMode: args.responseMode }, "respond tool called");
      return "ok";
    },
    {
      name: "respond",
      description:
        "SEMPRE invoque esta tool ao final da sua resposta para confirmar o formato de entrega. " +
        "Preencha fullResponse com o texto completo da sua mensagem. " +
        "Escolha responseMode baseado no contexto: 'audio' se o usuário pediu para ouvir, " +
        "'text' para resposta textual explícita, 'undefined' quando não há preferência específica.",
      schema: respondToolSchema,
    }
  );
}
```

2. Criar `packages/core/src/tools/__tests__/respond.test.ts`:

```typescript
// RESP-01, RESP-02: testes unitários de createRespondTool()
import { describe, test, expect } from "bun:test";
import { createRespondTool } from "../../respond.js";
import { ZodError } from "zod";

describe("createRespondTool — RESP-01: schema-as-tool para responseMode dinâmico", () => {
  test("retorna tool com nome 'respond'", () => {
    const tool = createRespondTool();
    expect(tool.name).toBe("respond");
  });

  test("description contém instrução 'SEMPRE invoque' para mitigar PITFALL-6", () => {
    const tool = createRespondTool();
    expect(tool.description).toContain("SEMPRE invoque");
  });

  test("schema tem campo fullResponse obrigatório (string)", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    // Verificar que fullResponse é obrigatório no JSON Schema
    const parsed = schema.safeParse({ fullResponse: "hello", responseMode: "undefined" });
    expect(parsed.success).toBe(true);
  });

  test("schema tem campo responseMode com enum ['undefined', 'text', 'audio']", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    // responseMode "undefined" aceito
    expect(schema.safeParse({ fullResponse: "oi", responseMode: "undefined" }).success).toBe(true);
    // responseMode "text" aceito
    expect(schema.safeParse({ fullResponse: "oi", responseMode: "text" }).success).toBe(true);
    // responseMode "audio" aceito
    expect(schema.safeParse({ fullResponse: "oi", responseMode: "audio" }).success).toBe(true);
    // responseMode "image" NÃO aceito (não está no enum da tool — D-03)
    expect(schema.safeParse({ fullResponse: "oi", responseMode: "image" }).success).toBe(false);
  });

  test("schema aceita mediaType opcional com enum ['image', 'file', 'video', 'audio']", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    const parsed = schema.safeParse({
      fullResponse: "veja",
      responseMode: "undefined",
      mediaType: "file",
      mediaUrl: "https://example.com/doc.pdf",
    });
    expect(parsed.success).toBe(true);
  });

  test("schema aceita mediaUrl opcional como URL válida (z.string().url())", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    const parsed = schema.safeParse({
      fullResponse: "ouça",
      responseMode: "audio",
      mediaType: "audio",
      mediaUrl: "https://example.com/audio.mp3",
    });
    expect(parsed.success).toBe(true);
  });

  test("validação condicional: mediaType sem mediaUrl lança ZodError", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    const result = schema.safeParse({
      fullResponse: "veja",
      responseMode: "undefined",
      mediaType: "image",
      // mediaUrl ausente
    });
    expect(result.success).toBe(false);
    expect(result.error).toBeInstanceOf(ZodError);
    const issues = result.error.issues;
    expect(issues.some((i: any) => i.path.includes("mediaUrl"))).toBe(true);
  });

  test("validação condicional: mediaUrl sem mediaType lança ZodError", () => {
    const tool = createRespondTool();
    const schema = (tool.schema as any);
    const result = schema.safeParse({
      fullResponse: "veja",
      responseMode: "undefined",
      // mediaType ausente
      mediaUrl: "https://example.com/img.png",
    });
    expect(result.success).toBe(false);
    const issues = result.error.issues;
    expect(issues.some((i: any) => i.path.includes("mediaType"))).toBe(true);
  });
});

describe("createRespondTool — RESP-02: fullResponse não é alterado", () => {
  test("invocação da tool retorna 'ok' sem alterar fullResponse", async () => {
    const tool = createRespondTool();
    // tool.invoke chama a função com os args parseados
    const result = await tool.invoke({
      fullResponse: "Mensagem original do LLM sem alteração",
      responseMode: "text",
    });
    // A tool retorna "ok" — fullResponse não é modificado pela execução da tool
    expect(result).toBe("ok");
  });

  test("fullResponse com caracteres especiais chega intacto nos args", async () => {
    const tool = createRespondTool();
    let capturedFullResponse = "";
    // Substituir temporariamente a invocação para capturar args
    // Verificar via schema.parse que fullResponse passa sem modificação
    const schema = (tool.schema as any);
    const input = "Resposta com: acentos, emojis 🎉, e\nnewlines";
    const parsed = schema.safeParse({ fullResponse: input, responseMode: "undefined" });
    expect(parsed.success).toBe(true);
    expect(parsed.data.fullResponse).toBe(input);
  });
});
```

3. Em `packages/core/src/index.ts`, adicionar ao final da seção "SDK-07":
   - Após `export { createFinishConversationTool } from "./tools/finish-conversation.js";`
   - Adicionar: `export { createRespondTool } from "./tools/respond.js";`
   - Atualizar comentário da seção: `// SDK-07: Standard Tools — factories para tools de controle de sessão + respond tool (RESP-01)`
  </action>

  <verify>
    <automated>bun test packages/core/src/tools/__tests__/respond.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `packages/core/src/tools/respond.ts` existe com `export function createRespondTool()`
    - `grep -n "createRespondTool" packages/core/src/index.ts` retorna 1 linha com export
    - `bun test packages/core/src/tools/__tests__/respond.test.ts` sai com código 0
    - `grep -n "name.*respond" packages/core/src/tools/respond.ts` retorna linha com `name: "respond"`
    - `grep -n "SEMPRE invoque" packages/core/src/tools/respond.ts` retorna 1 linha (instrução crítica PITFALL-6)
    - `grep -n "z.string().url()" packages/core/src/tools/respond.ts` retorna 1 linha (PITFALL-5: não usar z.url())
    - `grep -n "superRefine" packages/core/src/tools/respond.ts` retorna 1 linha (validação condicional D-03)
    - `bun test packages/core/` sai com código 0 (suite completa do packages/core)
  </acceptance_criteria>

  <done>createRespondTool() está implementado com schema Zod v4 correto, exportado pelo barrel do packages/core, e todos os testes unitários (respond.test.ts + schema.test.ts) passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → respond tool schema | O LLM gera os valores de fullResponse, responseMode, mediaType, mediaUrl — input não confiável que passa pelo schema Zod |
| LLM → mediaUrl | O LLM pode gerar URLs maliciosas ou inválidas no campo mediaUrl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | respondToolSchema.mediaUrl | mitigate | `z.string().url()` valida formato URI; downstream não deve fazer fetch cego de mediaUrl sem validação adicional de domínio — responsabilidade do consumer |
| T-16-02 | Information Disclosure | respondToolSchema.fullResponse | accept | fullResponse é o conteúdo gerado pelo LLM para o usuário — exposição intencional, sem PII novo introduzido por este mecanismo |
| T-16-03 | Spoofing | LLM → enum responseMode | mitigate | Zod enum restrito a ["undefined", "text", "audio"] — valores fora do enum são rejeitados com ZodError antes de chegar ao nó respond; superRefine adiciona validação cruzada mediaType/mediaUrl |
| T-16-04 | Denial of Service | createRespondTool() invocação | accept | Tool executa em microsegundos (apenas logger.info + return "ok") — sem risco de bloqueio ou consumo excessivo de recursos |
</threat_model>

<verification>
Suite completa do packages/core deve passar:
```
bun test packages/core/
```

Verificações individuais:
- `bun test packages/core/src/tools/__tests__/respond.test.ts` — 10 testes verdes (RESP-01, RESP-02)
- `bun test packages/core/src/__tests__/unit/output/schema.test.ts` — todos os testes existentes + novos casos "undefined" verdes (D-04, D-07)
- `grep -c "undefined" packages/shared/src/types/index.ts` retorna >= 1 (D-06)
- `grep -c "createRespondTool" packages/core/src/index.ts` retorna 1 (D-09)
</verification>

<success_criteria>
- packages/shared: ResponseMode type inclui "undefined" como primeira opção no union
- packages/core/src/output/schema.ts: ResponseModeSchema enum inclui "undefined"
- packages/core/src/tools/respond.ts: createRespondTool() implementada com schema Zod v4 correto (superRefine, enums D-03)
- packages/core/src/index.ts: exporta createRespondTool
- bun test packages/core/ passa com código 0 (todos os testes do packages/core)
- Nenhuma modificação em MODES_REQUIRING_MEDIA — "undefined" não exige mídia (D-08)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-dynamic-responsemode/16-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Decisões D-04, D-06, D-07, D-08, D-09 implementadas
- Estado dos testes (contagem de testes novos)
- Pitfalls mitigados (PITFALL-5, PITFALL-6 description)
</output>
