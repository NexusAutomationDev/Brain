---
phase: 16-dynamic-responsemode
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - apps/brain-sdr/src/brain.ts
  - apps/brain-echo/src/brain.ts
  - apps/brain-sdr/src/__tests__/unit/brain.test.ts
  - apps/brain-echo/src/__tests__/unit/brain.test.ts
  - docs/guides/response-format-prompt.md
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - RESP-03

must_haves:
  truths:
    - "brain-sdr não tem responseMode hardcoded — o nó llm não seta responseMode: 'text' as const no caminho normal"
    - "brain-sdr tem nó 'respond' que seta brainOutput lendo args do tool_call 'respond' no estado"
    - "brain-sdr usa routeAfterLlm em vez de toolsCondition — roteia para 'respond', 'tools' ou '__end__'"
    - "brain-echo recebe as mesmas mudanças que brain-sdr — mesmo código de grafo, sem branching por provider"
    - "Teste brain-sdr: brainOutput.responseMode no fallback D-10 é 'undefined', não 'text'"
    - "Teste brain-sdr: bindTools recebe 4 tools quando mcpTools=[] (qualify_lead + pause_session + finish_conversation + respond)"
    - "docs/guides/response-format-prompt.md documenta a abordagem schema-as-tool com createRespondTool()"
  artifacts:
    - path: "apps/brain-sdr/src/brain.ts"
      provides: "Grafo com routeAfterLlm + nó respond + createRespondTool em bindTools"
      contains: "routeAfterLlm"
    - path: "apps/brain-echo/src/brain.ts"
      provides: "Grafo idêntico ao brain-sdr (sem tools nativas, mas com respond tool)"
      contains: "routeAfterLlm"
    - path: "apps/brain-sdr/src/__tests__/unit/brain.test.ts"
      provides: "Testes atualizados: responseMode fallback = 'undefined', bindTools count = 4, testes de routeAfterLlm"
    - path: "docs/guides/response-format-prompt.md"
      provides: "Documentação da abordagem schema-as-tool (D-12)"
  key_links:
    - from: "apps/brain-sdr/src/brain.ts"
      to: "@brain-pkg/core"
      via: "import { createRespondTool }"
      pattern: "createRespondTool"
    - from: "nó llm (brain-sdr)"
      to: "brainOutput fallback"
      via: "D-10: texto plano sem tool_calls → responseMode: 'undefined'"
      pattern: "responseMode.*undefined.*as const"
    - from: "nó respond (brain-sdr)"
      to: "brainOutput"
      via: "extrai args do tool_call respond, seta brainOutput + emite ToolMessage"
      pattern: "ToolMessage"
---

<objective>
Integrar `createRespondTool()` nos grafos de brain-sdr e brain-echo, substituir `toolsCondition` pelo router customizado `routeAfterLlm`, adicionar o nó `respond`, e atualizar os testes que assertam `responseMode: "text"`.

Purpose: Este plano fecha o ciclo do padrão schema-as-tool — os dois Brains passam a usar o mecanismo dinâmico de responseMode em vez do valor hardcoded. A documentação é recriada (D-12) para refletir a abordagem atual.

Output: brain-sdr e brain-echo com grafo atualizado; testes unitários verdes; docs/guides/response-format-prompt.md recriado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-dynamic-responsemode/16-CONTEXT.md
@.planning/phases/16-dynamic-responsemode/16-RESEARCH.md
@.planning/phases/16-dynamic-responsemode/16-01-SUMMARY.md

@apps/brain-sdr/src/brain.ts
@apps/brain-echo/src/brain.ts
@apps/brain-sdr/src/__tests__/unit/brain.test.ts
@apps/brain-echo/src/__tests__/unit/brain.test.ts
@packages/core/src/tools/respond.ts
</context>

<interfaces>
<!-- Interfaces criadas no Plano 01 — executor usa diretamente -->

De packages/core/src/tools/respond.ts (criado no Plano 01):
```typescript
export function createRespondTool(): DynamicStructuredTool
// name: "respond"
// schema: z.object({ fullResponse, responseMode, mediaType?, mediaUrl? })
// invocação retorna "ok"
```

De @langchain/langgraph (já instalado — v1.4.1):
```typescript
import { END } from "@langchain/langgraph";
import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
// toolsCondition SERÁ REMOVIDO das importações — substituído por routeAfterLlm
```

De @langchain/core/messages (já instalado):
```typescript
import { AIMessage, ToolMessage } from "@langchain/core/messages";
// ToolMessage constructor: new ToolMessage({ content, tool_call_id, name })
```

Estado atual de apps/brain-sdr/src/brain.ts (linhas críticas):
```typescript
// linha 11: import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
// linha 65-70: const llmWithTools = ctx.llm.bindTools([boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool, ...ctx.mcpTools]);
// linha 88-93: retorna { messages: [response], brainOutput: { fullResponse, responseMode: "text" as const }, tokenUsage }
// linha 103: .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
```

Estado atual de apps/brain-echo/src/brain.ts (linhas críticas):
```typescript
// linha 6: import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
// linha 30: const llmWithTools = ctx.llm.bindTools(allTools);  ← allTools = [...ctx.mcpTools]
// linha 55-59: retorna { messages: [...state.messages, response], brainOutput: { fullResponse, responseMode: "text" as const } }
// linha 68: .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
```

Padrão de ToolMessage para paridade AIMessage/ToolMessage (D-02, PITFALL-4):
```typescript
new ToolMessage({
  content: "ok",
  tool_call_id: respondCall.id ?? "",
  name: "respond",
})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar brain-sdr — routeAfterLlm + nó respond + bindTools + testes</name>
  <files>apps/brain-sdr/src/brain.ts, apps/brain-sdr/src/__tests__/unit/brain.test.ts</files>

  <read_first>
    - apps/brain-sdr/src/brain.ts — ler COMPLETAMENTE antes de editar (entender todas as importações e estrutura do grafo)
    - apps/brain-sdr/src/__tests__/unit/brain.test.ts — ler COMPLETAMENTE antes de editar (identificar todos os testes que precisam atualização: linha 142 responseMode "text", linha 98 count 3 tools, linha 166 count 4 tools)
    - packages/core/src/tools/respond.ts — ler para confirmar assinatura exata de createRespondTool() (criada no Plano 01)
    - .planning/phases/16-dynamic-responsemode/16-RESEARCH.md — seção "Code Examples" para referência do routeAfterLlm e respondNode
  </read_first>

  <behavior>
    - Test A (atualizar linha 142): brainOutput.responseMode no fallback D-10 (LLM sem tool_calls) é "undefined", não "text"
    - Test B (atualizar linha 98): buildGraph() com mcpTools=[] chama bindTools com 4 tools (adicionar "respond" ao expect)
    - Test C (novo): routeAfterLlm retorna "respond" quando last tool_call.name === "respond"
    - Test D (novo): routeAfterLlm retorna "tools" quando last tool_call.name !== "respond" (ex: "qualify_lead")
    - Test E (novo): routeAfterLlm retorna END quando não há tool_calls (fallback D-10)
    - Test F (atualizar linha 166): buildGraph() com mcpTools=[fakeMcpTool] chama bindTools com 5 tools (era 4, agora 5 com respond)
  </behavior>

  <action>
### Mudanças em `apps/brain-sdr/src/brain.ts`

1. **Importações**: atualizar linha 11 de:
   ```typescript
   import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
   ```
   Para:
   ```typescript
   import { ToolNode } from "@langchain/langgraph/prebuilt";
   import { END } from "@langchain/langgraph";
   import { AIMessage, ToolMessage } from "@langchain/core/messages";
   ```

2. **Import de createRespondTool**: atualizar linha 14 de:
   ```typescript
   import { createPauseSessionTool, createFinishConversationTool } from "@brain-pkg/core";
   ```
   Para:
   ```typescript
   import { createPauseSessionTool, createFinishConversationTool, createRespondTool } from "@brain-pkg/core";
   ```

3. **Dentro de buildGraph()**, antes da criação de `llmWithTools`, adicionar a função `routeAfterLlm`:
   ```typescript
   // D-01 (Fase 16): router customizado — substitui toolsCondition
   // Inspeciona o NOME do tool_call em vez de apenas a presença
   function routeAfterLlm(state: typeof BrainStateAnnotation.State): "respond" | "tools" | typeof END {
     const messages = state.messages;
     const lastMessage = messages[messages.length - 1];
     if (!lastMessage || !("tool_calls" in lastMessage)) return END;
     const toolCalls = (lastMessage as AIMessage).tool_calls ?? [];
     if (toolCalls.length === 0) return END;
     // D-01: respond tool → nó respond; qualquer outra tool → ReAct loop
     if (toolCalls[0].name === "respond") return "respond";
     return "tools";
   }
   ```
   NOTA: `routeAfterLlm` é definida DENTRO de buildGraph() para ter acesso ao tipo `BrainState` sem precisar de importação adicional. Alternativamente, pode ser definida fora de buildGraph() com tipo genérico — neste caso usar `(state: any)` ou importar o tipo correto de @brain-pkg/ai.

4. **Adicionar respondTool** à criação de `llmWithTools`:
   ```typescript
   const respondTool = createRespondTool();
   const llmWithTools = ctx.llm.bindTools([
     boundQualifyTool,
     boundPauseSessionTool,
     boundFinishConversationTool,
     respondTool,          // D-09 (Fase 16): respond tool para responseMode dinâmico
     ...ctx.mcpTools,
   ]);
   ```

5. **Atualizar nó "llm"** — remover `responseMode: "text" as const` e adicionar fallback D-10:
   Substituir o bloco de retorno do nó llm:
   ```typescript
   // ANTES:
   return {
     messages: [response],
     brainOutput: { fullResponse, responseMode: "text" as const },
     tokenUsage: extractTokenUsage(response),
   };
   
   // DEPOIS:
   const toolCalls = (response as AIMessage).tool_calls ?? [];
   const hasRespondCall = toolCalls.some((tc: any) => tc.name === "respond");
   
   if (!hasRespondCall) {
     // D-10 (Fase 16): fallback — LLM emitiu texto plano sem invocar respond tool (PITFALL-6)
     // Comportamento degradado — não erro; logar como warn
     if (!fullResponse) {
       logger.warn("LLM emitiu resposta vazia sem tool call — PITFALL-6");
     } else {
       logger.warn({ content: fullResponse }, "LLM emitiu texto plano sem respond tool — PITFALL-6");
     }
     return {
       messages: [response],
       brainOutput: { fullResponse, responseMode: "undefined" as const },
       tokenUsage: extractTokenUsage(response),
     };
   }
   
   // Caminho normal: respond tool será chamada pelo nó respond
   // brainOutput será setado pelo nó respond — não setar aqui
   return {
     messages: [response],
     tokenUsage: extractTokenUsage(response),
   };
   ```
   NOTA IMPORTANTE: Manter `const fullResponse = typeof response.content === "string" ? response.content : "";` antes do bloco acima (já existe no código atual).

6. **Adicionar nó "respond"** — inserir após `.addNode("tools", new ToolNode(...))`:
   ```typescript
   .addNode("respond", async (state) => {
     // D-02 (Fase 16): nó respond como nó regular (não ToolNode) — pode setar brainOutput + messages
     // PITFALL-4: emitir ToolMessage para manter paridade AIMessage/ToolMessage no PostgresSaver
     const messages = state.messages;
     let respondCall: any = null;
     for (let i = messages.length - 1; i >= 0; i--) {
       const msg = messages[i];
       if (msg.getType?.() === "ai" || (msg as any)._getType?.() === "ai") {
         const tc = (msg as AIMessage).tool_calls ?? [];
         respondCall = tc.find((c: any) => c.name === "respond");
         if (respondCall) break;
       }
     }
     if (!respondCall) {
       logger.error("respondNode chamado sem tool_call 'respond' no estado — estado inconsistente");
       return {};
     }
     const args = respondCall.args;
     // D-05 (Fase 16): mapear mediaType "file" → "document" antes de BrainOutputSchema.parse()
     const mediaType = args.mediaType === "file" ? "document" : args.mediaType;
     const toolMessage = new ToolMessage({
       content: "ok",
       tool_call_id: respondCall.id ?? "",
       name: "respond",
     });
     return {
       messages: [toolMessage],  // D-02: paridade AIMessage/ToolMessage
       brainOutput: {
         fullResponse: args.fullResponse,
         responseMode: args.responseMode,
         ...(mediaType ? { mediaType } : {}),
         ...(args.mediaUrl ? { mediaUrl: args.mediaUrl } : {}),
       },
     };
   })
   ```

7. **Atualizar addConditionalEdges** — substituir:
   ```typescript
   // ANTES:
   .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
   
   // DEPOIS:
   .addConditionalEdges("llm", routeAfterLlm, ["tools", "respond", "__end__"])
   ```

8. **Adicionar edge** de "respond" para "__end__" — após `.addEdge("tools", "llm")`:
   ```typescript
   .addEdge("respond", "__end__")  // D-01 (Fase 16): respond sempre termina o turno
   ```

### Mudanças em `apps/brain-sdr/src/__tests__/unit/brain.test.ts`

9. **Atualizar linha 142** (describe "BrainSDR — nó llm seta brainOutput"):
   ```typescript
   // ANTES:
   expect(result.brainOutput.responseMode).toBe("text");
   
   // DEPOIS: D-10 fallback — LLM sem tool_calls usa responseMode "undefined"
   expect(result.brainOutput.responseMode).toBe("undefined");
   ```

10. **Atualizar describe "BrainSDR — Standard Tools binding"** (teste que verifica count=3):
    - No teste `"buildGraph(ctx) com ctx.sql mock chama bindTools com 3 tools"`:
    - Atualizar nome do teste para: `"buildGraph(ctx) com ctx.sql mock chama bindTools com 4 tools (incluindo respond)"`
    - Atualizar: `expect(callArgs).toHaveLength(3);` → `expect(callArgs).toHaveLength(4);`
    - Adicionar: `expect(toolNames).toContain("respond");`

11. **Atualizar describe "BrainSDR — MCP tools integration"** (teste que verifica count=4 com 1 MCP tool):
    - No teste `"buildGraph(ctx) com ctx.mcpTools=[mockTool] chama bindTools com 4 tools"`:
    - Atualizar nome: `"buildGraph(ctx) com ctx.mcpTools=[mockTool] chama bindTools com 5 tools (3 nativas + respond + 1 MCP)"`
    - Atualizar: `expect(callArgs).toHaveLength(4);` → `expect(callArgs).toHaveLength(5);`

12. **Adicionar novos testes** para routeAfterLlm em novo describe:
    ```typescript
    describe("BrainSDR — routeAfterLlm router customizado (D-01, RESP-01)", () => {
      test("retorna 'respond' quando último tool_call.name é 'respond'", async () => {
        const mod = await import("../../brain.js");
        const bindToolsMock = mock(() => ({
          invoke: mock(async () =>
            new AIMessage({
              content: "",
              tool_calls: [{ name: "respond", args: { fullResponse: "oi", responseMode: "text" }, id: "tc-1", type: "tool_call" }],
            })
          ),
        }));
        const ctx = {
          llm: { bindTools: bindToolsMock },
          prompts: { system: "s", qualification: "q" },
          tools: [],
          sql: {} as any,
          mcpTools: [],
        };
        const graph = mod.sdrBrain.buildGraph(ctx as any);
        const compiled = graph.compile();
        // Invocar o grafo — com respond tool_call, deve ir para nó respond e setar brainOutput
        const result = await compiled.invoke(
          { messages: [{ role: "user", content: "olá" }] },
          { configurable: { thread_id: "test-router-respond" } }
        );
        // O nó respond deve ter setado brainOutput com o responseMode da tool call
        expect(result.brainOutput).toBeDefined();
        expect(result.brainOutput.responseMode).toBe("text");
        expect(result.brainOutput.fullResponse).toBe("oi");
      });

      test("retorna 'undefined' responseMode no fallback D-10 quando LLM não chama nenhuma tool", async () => {
        const mod = await import("../../brain.js");
        const bindToolsMock = mock(() => ({
          invoke: mock(async () =>
            new AIMessage({ content: "texto direto sem tool", tool_calls: [] })
          ),
        }));
        const ctx = {
          llm: { bindTools: bindToolsMock },
          prompts: { system: "s", qualification: "q" },
          tools: [],
          sql: {} as any,
          mcpTools: [],
        };
        const graph = mod.sdrBrain.buildGraph(ctx as any);
        const compiled = graph.compile();
        const result = await compiled.invoke(
          { messages: [{ role: "user", content: "olá" }] },
          { configurable: { thread_id: "test-router-end" } }
        );
        expect(result.brainOutput.responseMode).toBe("undefined");
      });
    });
    ```
  </action>

  <verify>
    <automated>bun test apps/brain-sdr/src/__tests__/unit/brain.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "toolsCondition" apps/brain-sdr/src/brain.ts` retorna 0 linhas (toolsCondition removido)
    - `grep -n "routeAfterLlm" apps/brain-sdr/src/brain.ts` retorna ao menos 2 linhas (definição + uso em addConditionalEdges)
    - `grep -n "createRespondTool" apps/brain-sdr/src/brain.ts` retorna ao menos 2 linhas (import + uso)
    - `grep -n "responseMode.*text.*as const" apps/brain-sdr/src/brain.ts` retorna 0 linhas (hardcode removido)
    - `grep -n "responseMode.*undefined.*as const" apps/brain-sdr/src/brain.ts` retorna 1 linha (fallback D-10)
    - `grep -n "ToolMessage" apps/brain-sdr/src/brain.ts` retorna ao menos 1 linha (nó respond emite ToolMessage — PITFALL-4)
    - `grep -n "addEdge.*respond.*__end__" apps/brain-sdr/src/brain.ts` retorna 1 linha
    - `grep -n "PITFALL-6" apps/brain-sdr/src/brain.ts` retorna ao menos 1 linha (warn de fallback)
    - `bun test apps/brain-sdr/src/__tests__/unit/brain.test.ts` sai com código 0
    - `grep -n "toBe(\"undefined\")" apps/brain-sdr/src/__tests__/unit/brain.test.ts` retorna ao menos 1 linha (asserção atualizada D-10)
    - `grep -n "toHaveLength(4)" apps/brain-sdr/src/__tests__/unit/brain.test.ts` retorna ao menos 1 linha (bindTools com 4 tools nativas)
  </acceptance_criteria>

  <done>brain-sdr usa routeAfterLlm com 3 destinos, tem nó respond que seta brainOutput + emite ToolMessage, createRespondTool está no bindTools, e todos os testes unitários passam.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar brain-echo + recriar docs/guides/response-format-prompt.md</name>
  <files>apps/brain-echo/src/brain.ts, apps/brain-echo/src/__tests__/unit/brain.test.ts, docs/guides/response-format-prompt.md</files>

  <read_first>
    - apps/brain-echo/src/brain.ts — ler COMPLETAMENTE antes de editar (brain-echo tem estrutura diferente: sem tools nativas, allTools = [...ctx.mcpTools])
    - apps/brain-echo/src/__tests__/unit/brain.test.ts — ler COMPLETAMENTE antes de editar (identificar testes que assertam responseMode ou bindTools count)
    - apps/brain-sdr/src/brain.ts — APÓS edição da Task 1 — usar como referência de padrão para brain-echo
    - docs/guides/response-format-prompt.md — ler antes de recriar (D-12: conteúdo atual está desatualizado)
  </read_first>

  <action>
### Mudanças em `apps/brain-echo/src/brain.ts`

Brain-echo tem diferenças em relação ao brain-sdr:
- Sem tools nativas (qualify_lead, pause_session, finish_conversation)
- `allTools = [...ctx.mcpTools]` — apenas MCP tools
- `messages: [...state.messages, response]` no nó llm (spread diferente do brain-sdr)
- Sem import de createPauseSessionTool / createFinishConversationTool

Aplicar as seguintes mudanças em `apps/brain-echo/src/brain.ts`:

1. **Importações**: atualizar linha 6 de:
   ```typescript
   import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
   ```
   Para:
   ```typescript
   import { ToolNode } from "@langchain/langgraph/prebuilt";
   import { END } from "@langchain/langgraph";
   import { AIMessage, ToolMessage } from "@langchain/core/messages";
   ```

2. **Import de createRespondTool**: adicionar nova linha de import após os imports existentes de @brain-pkg/core:
   ```typescript
   import type { IBrain, BrainBuildContext } from "@brain-pkg/core";
   import { createRespondTool } from "@brain-pkg/core";
   ```

3. **Dentro de buildGraph()**, adicionar `createRespondTool()` e incluir no `allTools`:
   ```typescript
   const respondTool = createRespondTool();
   // D-09 (Fase 16): allTools agora inclui respond tool + MCP tools
   const allTools = [respondTool, ...ctx.mcpTools];
   ```
   ATENÇÃO: manter a lógica do bindTools com allTools (brain-echo usa allTools diretamente, não lista individual como brain-sdr).

4. **Adicionar `routeAfterLlm` dentro de buildGraph()** — versão com guarda para ToolNode vazio:

   Brain-echo não tem tools nativas. Quando `ctx.mcpTools=[]`, o ToolNode do nó "tools" ficaria
   vazio. O router precisa garantir que nunca roteia para "tools" nessa condição — caso o LLM
   chame uma tool desconhecida (que não seja "respond"), o branch correto é `END` com fallback D-10.

   ```typescript
   // D-01 (Fase 16): router customizado — substitui toolsCondition
   // GUARDA brain-echo: captura hasMcpTools via closure para evitar ToolNode vazio
   const hasMcpTools = ctx.mcpTools.length > 0;
   function routeAfterLlm(state: any): "respond" | "tools" | typeof END {
     const messages = state.messages;
     const lastMessage = messages[messages.length - 1];
     if (!lastMessage || !("tool_calls" in lastMessage)) return END;
     const toolCalls = (lastMessage as AIMessage).tool_calls ?? [];
     if (toolCalls.length === 0) return END;
     if (toolCalls[0].name === "respond") return "respond";
     // Sem MCP tools registradas, qualquer outra tool call é inatingível via ToolNode.
     // Cai no fallback D-10 em vez de tentar executar um ToolNode vazio.
     if (!hasMcpTools) return END;
     return "tools";
   }
   ```

5. **Atualizar nó "llm"** — aplicar D-10 (mesmo padrão do brain-sdr):
   - Remover: `brainOutput: { fullResponse, responseMode: "text" as const }`
   - Adicionar lógica de fallback D-10 com `responseMode: "undefined" as const`
   - O retorno no caminho normal NÃO seta brainOutput (delegado ao nó respond)
   - MANTER o spread `messages: [...state.messages, response]` que é específico do brain-echo

6. **Adicionar nó "respond"** — mesma implementação do brain-sdr (copiar exatamente, sem alterações).

7. **Atualizar ToolNode "tools"** — excluir respondTool do ToolNode de tools:
   ```typescript
   // ANTES:
   .addNode("tools", new ToolNode(allTools, { handleToolErrors: true }))
   
   // DEPOIS: respond tool tem seu próprio nó — não vai para o ToolNode de tools
   // toolsForToolNode contém apenas MCP tools; pode ser [] quando ctx.mcpTools=[]
   // Nesse caso o nó "tools" existe mas nunca é atingido (routeAfterLlm retorna END quando !hasMcpTools)
   const toolsForToolNode = [...ctx.mcpTools]; // exclui respondTool
   .addNode("tools", new ToolNode(toolsForToolNode, { handleToolErrors: true }))
   ```
   ATENÇÃO: `allTools` ainda inclui respondTool para `bindTools()` — apenas o ToolNode do nó "tools" exclui a respond tool.
   A guarda `!hasMcpTools` no `routeAfterLlm` (item 4) garante que o nó "tools" nunca é atingido
   com array vazio — mesmo que o LLM tente chamar uma tool desconhecida.

8. **Atualizar addConditionalEdges**:
   ```typescript
   // ANTES:
   .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
   
   // DEPOIS:
   .addConditionalEdges("llm", routeAfterLlm, ["tools", "respond", "__end__"])
   ```

9. **Adicionar edge "respond" → "__end__"**:
   ```typescript
   .addEdge("respond", "__end__")
   ```

### Mudanças em `apps/brain-echo/src/__tests__/unit/brain.test.ts`

10. Se houver algum teste que asserte `responseMode: "text"` ou `brainOutput.responseMode === "text"`, atualizar para `"undefined"` (fallback D-10).

11. Se houver teste verificando count de tools em bindTools, atualizar para incluir a respond tool.

12. Adicionar teste de regressão: `buildGraph() retorna grafo com addConditionalEdges para 3 destinos (respond, tools, __end__)` — verificar via presença de nó "respond" no grafo.

13. **Adicionar teste de guarda do ToolNode vazio** — confirma que o branch "tools" nunca é
    atingido quando `mcpTools=[]`:
    ```typescript
    describe("BrainEcho — routeAfterLlm guarda ToolNode vazio (mcpTools=[])", () => {
      test("quando mcpTools=[] e LLM chama tool desconhecida, roteia para END (não para 'tools')", async () => {
        const mod = await import("../../brain.js");
        // LLM simula tool call com nome desconhecido (não é "respond", não é MCP tool)
        const bindToolsMock = mock(() => ({
          invoke: mock(async () =>
            new AIMessage({
              content: "",
              tool_calls: [{ name: "tool_inexistente", args: {}, id: "tc-unknown", type: "tool_call" }],
            })
          ),
        }));
        const ctx = {
          llm: { bindTools: bindToolsMock },
          prompts: { system: "s" },
          tools: [],
          sql: {} as any,
          mcpTools: [], // sem MCP tools — ToolNode de "tools" estaria vazio
        };
        const graph = mod.echoBrain.buildGraph(ctx as any);
        const compiled = graph.compile();
        // O grafo deve terminar sem lançar erro (fallback D-10 via END)
        // brainOutput.responseMode será "undefined" pois o nó llm seta fallback quando sem respond call
        const result = await compiled.invoke(
          { messages: [{ role: "user", content: "olá" }] },
          { configurable: { thread_id: "test-echo-guard-empty-toolnode" } }
        );
        // Não deve lançar erro de ToolNode vazio; brainOutput deve existir com responseMode undefined
        expect(result.brainOutput).toBeDefined();
        expect(result.brainOutput.responseMode).toBe("undefined");
      });
    });
    ```

### Recriar `docs/guides/response-format-prompt.md` (D-12)

Criar o arquivo com o seguinte conteúdo:

```markdown
# Response Format — schema-as-tool com createRespondTool()

**Abordagem:** schema-as-tool via `createRespondTool()` (Phase 16 — Brain Core v1.3)

## Conceito

O LLM escolhe o formato de resposta (`responseMode`) dinamicamente ao invocar a `respond` tool
como **último passo de cada turno**. O schema da tool define os valores aceitos e é convertido
automaticamente para JSON Schema pelo LangChain Core via `bindTools()`.

Esta abordagem substitui o hardcode `responseMode: "text" as const` que existia no nó `llm`.

## Por que schema-as-tool (e não as alternativas)?

| Abordagem | Problema |
|-----------|---------|
| `withStructuredOutput()` | Incompatível com `bindTools()` — langchainjs #7757 |
| `responseFormat` do `createReactAgent` | Faz segunda chamada LLM reescrevendo `fullResponse` — LangGraph #4756 |
| Instrução de prompt para JSON | Alucinações de JSON malformado; não garante `fullResponse` correto |
| **schema-as-tool (abordagem atual)** | `bindTools()` é API padrão do `BaseChatModel` — funciona com OpenAI e Anthropic sem branching de código |

## Schema da respond tool

```typescript
// packages/core/src/tools/respond.ts
{
  fullResponse: z.string(),           // OBRIGATÓRIO: texto completo da resposta
  responseMode: z.enum([              // OBRIGATÓRIO: modo de entrega
    "undefined",                      // sem preferência específica (valor padrão)
    "text",                           // resposta textual explícita
    "audio",                          // converter para áudio via TTS downstream
  ]),
  mediaType: z.enum(["image", "file", "video", "audio"]).optional(),
  mediaUrl: z.string().url().optional(),
}
```

`mediaType` e `mediaUrl` são co-dependentes: se um está presente, o outro é obrigatório.

**Nota:** `mediaType: "file"` é mapeado para `"document"` no nó `respond` antes de passar para `BrainOutputSchema.parse()` — o contrato de saída usa "document", mas o schema da tool usa "file" (mais intuitivo para o LLM).

## Fluxo do grafo

```
__start__ → llm → routeAfterLlm:
  ├── "respond"  → nó respond (seta brainOutput + emite ToolMessage)  → __end__
  ├── "tools"    → ToolNode com tools nativas/MCP                     → llm (ReAct loop)
  └── "__end__"  → fallback D-10: brainOutput { responseMode: "undefined" }
```

**Atenção brain-echo:** quando `ctx.mcpTools=[]`, o router retorna `END` para qualquer tool call
que não seja "respond" — evita invocar um `ToolNode` vazio. O nó "tools" existe no grafo mas é
inalcançável nessa configuração.

## Fallback D-10 (PITFALL-6)

Se o LLM emitir texto plano sem invocar a `respond` tool:
- O nó `llm` detecta `tool_calls.length === 0`
- Seta `brainOutput` com `{ fullResponse: <conteúdo>, responseMode: "undefined" }`
- Loga `warn` com tag "PITFALL-6"
- O grafo termina normalmente (comportamento degradado, não erro)

A description da tool instrui o LLM: **"SEMPRE invoque esta tool ao final da sua resposta"**.

## Como usar em um novo Brain

```typescript
import { createRespondTool } from "@brain-pkg/core";

// Em buildGraph():
const respondTool = createRespondTool();
const hasMcpTools = ctx.mcpTools.length > 0; // necessário para guarda do router (Brain sem tools nativas)
const llmWithTools = ctx.llm.bindTools([
  ...minhasTools,
  respondTool,         // adicionar por último (ou antes de mcpTools)
  ...ctx.mcpTools,
]);

// Router com guarda para ToolNode vazio (obrigatório quando Brain não tem tools nativas):
function routeAfterLlm(state: any) {
  const toolCalls = (state.messages.at(-1) as AIMessage)?.tool_calls ?? [];
  if (toolCalls.length === 0) return END;
  if (toolCalls[0].name === "respond") return "respond";
  if (!hasMcpTools) return END; // guarda: ToolNode ficaria vazio
  return "tools";
}

// Adicionar nó respond e router no StateGraph:
.addNode("respond", respondNode)          // nó regular que seta brainOutput
.addConditionalEdges("llm", routeAfterLlm, ["tools", "respond", "__end__"])
.addEdge("respond", "__end__")
```

Copiar a implementação completa de `respondNode` de `apps/brain-sdr/src/brain.ts`.

## Valores de responseMode

| Valor | Quando usar | Consumer downstream |
|-------|-------------|---------------------|
| `"undefined"` | Sem preferência específica (fallback) | Trata como texto |
| `"text"` | Resposta textual explícita | Renderiza como texto |
| `"audio"` | Usuário pediu para ouvir | Converte via TTS |

## Fora de escopo (pós v1.3)

- `responseMode: "video"` e `"document"` — RESP-F01
- Publicar `responseMode` + `mediaUrl` no RabbitMQ — RESP-F02
- `splitResponse` (dividir em múltiplos balões) — feature futura
```
  </action>

  <verify>
    <automated>bun test apps/brain-echo/src/__tests__/unit/brain.test.ts && bun test apps/brain-sdr/ apps/brain-echo/ packages/core/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "toolsCondition" apps/brain-echo/src/brain.ts` retorna 0 linhas (toolsCondition removido)
    - `grep -n "routeAfterLlm" apps/brain-echo/src/brain.ts` retorna ao menos 2 linhas
    - `grep -n "createRespondTool" apps/brain-echo/src/brain.ts` retorna ao menos 2 linhas (import + uso)
    - `grep -n "responseMode.*text.*as const" apps/brain-echo/src/brain.ts` retorna 0 linhas
    - `grep -n "responseMode.*undefined.*as const" apps/brain-echo/src/brain.ts` retorna 1 linha (fallback D-10)
    - `grep -n "ToolMessage" apps/brain-echo/src/brain.ts` retorna ao menos 1 linha (PITFALL-4)
    - `grep -n "respond.*__end__" apps/brain-echo/src/brain.ts` retorna 1 linha (.addEdge)
    - `grep -n "hasMcpTools" apps/brain-echo/src/brain.ts` retorna ao menos 2 linhas (declaração + uso na guarda do router)
    - `bun test apps/brain-echo/src/__tests__/unit/brain.test.ts` sai com código 0
    - `docs/guides/response-format-prompt.md` existe e contém "schema-as-tool"
    - `grep -n "PITFALL-6" docs/guides/response-format-prompt.md` retorna ao menos 1 linha
    - `grep -n "createRespondTool" docs/guides/response-format-prompt.md` retorna ao menos 2 linhas
    - `grep -n "hasMcpTools" docs/guides/response-format-prompt.md` retorna ao menos 1 linha (guarda documentada)
    - `grep -n "tool_inexistente\|guarda.*ToolNode\|ToolNode vazio" apps/brain-echo/src/__tests__/unit/brain.test.ts` retorna ao menos 1 linha (teste de guarda presente)
    - `bun test apps/ packages/core/` sai com código 0 (suite completa dos apps e packages/core)
  </acceptance_criteria>

  <done>brain-echo tem o mesmo padrão de routeAfterLlm + nó respond que o brain-sdr, com guarda explícita para ToolNode vazio quando mcpTools=[]; docs recriados documentam a abordagem schema-as-tool incluindo a guarda; suite completa de apps e packages/core passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → args do tool_call respond | O LLM gera fullResponse, responseMode, mediaType, mediaUrl — o nó respond extrai args sem reprocessamento LLM |
| tool_call.id (respondCall.id) | LangGraph/LangChain gera o id; nó respond usa como tool_call_id no ToolMessage |
| state.messages → respondNode | O nó respond percorre mensagens em busca do tool_call respond — não confia em índice fixo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering | respondNode — args.fullResponse | accept | fullResponse é passado direto para brainOutput sem reprocessamento (RESP-02 — intencional); validação de schema Zod já ocorreu antes da invocação da tool |
| T-16-06 | Denial of Service | routeAfterLlm — loop infinito de ReAct | accept | Mesmo comportamento existente de toolsCondition; LangGraph tem recursionLimit padrão (25) que previne loops infinitos |
| T-16-07 | Spoofing | mediaType "file" → "document" mapeamento | mitigate | Mapeamento explícito e restrito no nó respond (D-05): apenas "file" → "document"; outros valores passam sem alteração. BrainOutputSchema.parse() valida o resultado final |
| T-16-08 | Repudiation | PITFALL-6 warn log | mitigate | logger.warn com conteúdo do texto plano e tag "PITFALL-6" — auditável via pino logs estruturados |
| T-16-09 | Information Disclosure | logger.warn em D-10 — loga content | accept | content é a resposta do LLM para o usuário — já seria exposto normalmente; sem PII novo introduzido |
| T-16-10 | Denial of Service | brain-echo ToolNode vazio (mcpTools=[]) | mitigate | routeAfterLlm com guarda `!hasMcpTools` retorna END antes de atingir ToolNode; teste de regressão confirma o branch nunca é atingido |
</threat_model>

<verification>
Suite completa de apps e packages/core:
```
bun test apps/ packages/core/
```

Verificações adicionais após conclusão:
- `grep -rn "responseMode.*text.*as const" apps/` retorna 0 linhas (hardcode completamente removido)
- `grep -rn "toolsCondition" apps/brain-sdr/src/brain.ts apps/brain-echo/src/brain.ts` retorna 0 linhas
- `grep -rn "routeAfterLlm" apps/` retorna ao menos 4 linhas (2 por Brain)
- `grep -n "schema-as-tool" docs/guides/response-format-prompt.md` retorna ao menos 1 linha
- `grep -n "hasMcpTools" apps/brain-echo/src/brain.ts` retorna ao menos 2 linhas (guarda ativa)
</verification>

<success_criteria>
- brain-sdr.buildGraph() vincula 4 tools nativas (qualify_lead + pause_session + finish_conversation + respond) + mcpTools
- brain-echo.buildGraph() vincula respond tool + mcpTools no bindTools
- Ambos os brains têm nó "respond" que extrai args do tool_call, mapeia file→document (D-05), seta brainOutput e emite ToolMessage (PITFALL-4)
- routeAfterLlm substitui toolsCondition em ambos os brains — 3 destinos: "respond", "tools", "__end__"
- brain-echo: routeAfterLlm tem guarda `!hasMcpTools` — nunca roteia para "tools" quando mcpTools=[]
- Fallback D-10: nó llm em ambos os brains seta responseMode "undefined" quando LLM não invoca respond tool
- bun test apps/ packages/core/ passa com código 0
- docs/guides/response-format-prompt.md recriado com conteúdo atual (D-12) incluindo documentação da guarda hasMcpTools
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-dynamic-responsemode/16-02-SUMMARY.md` com:
- Arquivos modificados e mudanças principais
- Decisões D-01, D-02, D-05, D-10, D-11, D-12 implementadas
- Estado dos testes (contagem total, quais foram atualizados, quais foram adicionados)
- Pitfalls mitigados (PITFALL-4, PITFALL-6 — logs)
- Confirmação de que responseMode hardcode foi removido de ambos os brains
- Confirmação da guarda hasMcpTools no brain-echo (T-16-10)
</output>
