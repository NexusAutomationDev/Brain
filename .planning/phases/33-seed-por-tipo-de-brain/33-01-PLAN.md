---
phase: 33-seed-por-tipo-de-brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/seed.ts
  - packages/database/src/seeds/sdr/0001_fup_defaults.sql
  - packages/database/src/index.ts
  - packages/core/src/runner/runner.ts
  - apps/brain-sdr/Dockerfile
  - apps/brain-sdr/.env.example
  - packages/database/src/__tests__/unit/seed.test.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
  - packages/core/src/runner/__tests__/runner-fup.test.ts
  - packages/core/src/runner/__tests__/runner-wr.test.ts
autonomous: true
requirements: [SEED-02, SEED-03, SEED-04, SEED-05]

must_haves:
  truths:
    - "Starting brain-sdr against a fresh database results in exactly one fup_config row for brain_type='sdr', inserted automatically with no manual SQL (SEED-02)"
    - "Starting brain-sdr against a fresh database results in exactly one prompts row with key='fup' for brain_type='sdr', inserted automatically with no manual SQL (SEED-03)"
    - "packages/database/src/migrate.ts, its _schema_lock row-lock mechanism, and migrations 0002_echo_brain_seed.sql/0005_brain_sdr_prompts.sql/0010_brain_support_prompts.sql remain byte-for-byte unmodified by this plan (SEED-05, Pitfall 1)"
    - "If fup_config or prompts(key='fup') is missing for brain_type='sdr' after runBrainSeed() executes, BrainRunner.init() fails loudly via process.exit(1) instead of letting FupScheduler silently skip the lead later (Pitfall 2, D-08/D-09)"
    - "Calling runBrainSeed() twice in a row against the same database for brain_type='sdr' does not duplicate rows and does not throw (SEED-04)"
  artifacts:
    - packages/database/src/seed.ts
    - packages/database/src/seeds/sdr/0001_fup_defaults.sql
    - packages/database/src/__tests__/unit/seed.test.ts
  key_links:
    - "BrainRunner.init() calls runBrainSeed(this.sql, this.brain.brainType, seedsFolder) after runMigrations() completes and before loadPrompts()/the promptKeys fail-fast loop"
    - "runBrainSeed() reuses the existing _schema_lock row (id=1) via FOR UPDATE NOWAIT — same physical lock table as runMigrations(), no second lock mechanism introduced"
    - "apps/brain-sdr/Dockerfile COPYs packages/database/src/seeds/sdr into /app/seeds; SEEDS_FOLDER=/app/seeds tells runner.ts where to read the seed file(s) at runtime"
  prohibitions:
    - "runBrainSeed() (packages/database) must never call process.exit() itself — it must throw on failure and let BrainRunner.init()'s .catch() decide to exit, the same throw-not-exit library contract documented on runMigrations()"
    - "The seeded prompts(key='fup') content must not read as an unfinished placeholder — no square-bracket placeholder tokens ('[' or ']') and no instruction telling the client to edit the message before going live (D-06)"
    - "apps/brain-sdr/Dockerfile must COPY only packages/database/src/seeds/sdr — never the full packages/database/src/seeds/ tree (Pitfall 3, physical separation)"
---

## Artifacts this phase produces

New files/symbols created by this plan (exclude these from drift/source-grounding checks —
they do not exist yet in the codebase):

- `packages/database/src/seed.ts` — new file, exports `runBrainSeed(sql, brainType, seedsFolder)`
- `packages/database/src/seeds/sdr/0001_fup_defaults.sql` — new file
- New export line in `packages/database/src/index.ts`: `export { runBrainSeed } from './seed.js';`
- New `BrainRunnerOptions.seedsFolder?: string` field in `packages/core/src/runner/runner.ts`
- New private field `this.seedsFolder` on `BrainRunner` (packages/core/src/runner/runner.ts)
- New `SEEDS_FOLDER` ENV var (read via `process.env.SEEDS_FOLDER`, mirroring `MIGRATIONS_FOLDER`)
- `packages/database/src/__tests__/unit/seed.test.ts` — new test file

Modified (not new) files: `apps/brain-sdr/Dockerfile`, `apps/brain-sdr/.env.example`,
`packages/core/src/runner/__tests__/brain-runner.test.ts`,
`packages/core/src/runner/__tests__/runner-fup.test.ts`,
`packages/core/src/runner/__tests__/runner-wr.test.ts`.

<flagged_assumptions>
## Spec-less probe note (read before planning further work in this phase)

This phase has no `*-SPEC.md`. The deterministic edge-probe engine classified all five
requirements (SEED-01..05) as `category: "unclassified"` / `status: "unresolved"` against
their Portuguese requirement text (see orchestrator's probe JSON — 5/5 unresolved, 0
resolved, 0 backstop). Per the specless-probe-fallback protocol this is being surfaced
explicitly, not silently dropped or upgraded.

In addition to that flag, `must_haves.truths` above was derived normally (not from the
probe) directly from ROADMAP.md's Phase 33 Success Criteria (1:1 mapped to SEED-01..05)
and CONTEXT.md's D-01..D-09 decisions, which are concrete and verifiable. The probe's
inconclusive verdict does not change or weaken those truths — it is recorded here only so
the unresolved classification is never silently lost.
</flagged_assumptions>

<objective>
Fix the root cause of "FUP is silently dead on every fresh Brain database": build the new,
non-drizzle, per-brain-type seed mechanism (`runBrainSeed()`), wire it into
`BrainRunner.init()` between `runMigrations()` and `loadPrompts()`, and prove it end-to-end
for one brain type (`sdr`) — a fresh `sdr` database gets a `fup_config` row and a
`prompts(key='fup')` row automatically, idempotently, with a loud fail-fast if either is
missing, and without touching `migrate.ts` or any of the three existing seed migrations
(0002/0005/0010).

This is the tracer for the whole phase: every layer the phase will ever touch (seed SQL →
`runBrainSeed()` → `BrainRunner.init()` wiring → Dockerfile/ENV) gets proven once, for `sdr`,
before Plan 33-02 mechanically repeats the same shape for `support`/`echo`.

Purpose: SEED-02/SEED-03 are the actual product fix (FUP works out-of-the-box); SEED-04/05
are the safety rails (idempotent, non-destructive) that make it safe to ship to existing
customer databases.
Output: `packages/database/src/seed.ts` (`runBrainSeed()`), `packages/database/src/seeds/sdr/0001_fup_defaults.sql`,
wired `BrainRunner.init()`, updated `apps/brain-sdr/Dockerfile`/`.env.example`, and a full
unit test suite proving the mechanism (including that SEED-05's untouched files stay untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-seed-por-tipo-de-brain/33-CONTEXT.md
@.planning/phases/33-seed-por-tipo-de-brain/33-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

@packages/database/src/migrate.ts
@packages/database/src/migrate.test.ts
@packages/database/src/migrations/0005_brain_sdr_prompts.sql
@packages/database/src/schema/tables.ts
@packages/database/src/index.ts
@packages/core/src/runner/runner.ts
@packages/core/src/leads/__tests__/lead-service.test.ts
@apps/brain-sdr/Dockerfile
</context>

<tasks>

<task type="tracer">
  <name>Task 1: runBrainSeed() core mechanism + brain-sdr end-to-end wiring</name>
  <files>packages/database/src/seed.ts, packages/database/src/seeds/sdr/0001_fup_defaults.sql, packages/database/src/index.ts, packages/core/src/runner/runner.ts, apps/brain-sdr/Dockerfile, apps/brain-sdr/.env.example</files>
  <read_first>
    packages/database/src/migrate.ts (the exact throw-not-exit contract, the _schema_lock
    row-lock retry loop, the isLockNotAvailable/sleep helpers, the CREATE TABLE IF NOT
    EXISTS bootstrap — this new file must NOT import from or modify migrate.ts, only mirror
    its shape),
    packages/database/src/migrations/0005_brain_sdr_prompts.sql (ON CONFLICT DO NOTHING
    idiom to reuse),
    packages/database/src/schema/tables.ts lines ~65-74 (prompts columns) and ~134-150
    (fupConfig columns — note brainType is the PK, a single-column conflict target),
    packages/core/src/runner/runner.ts lines ~130-203 (exact init() sequence: migrationsFolder
    fail-fast → runMigrations() → embeddingProvider/dimension checks → loadPrompts() →
    promptKeys fail-fast loop — the new seed call must land after "Migrations completed" and
    before the promptKeys loop),
    apps/brain-sdr/Dockerfile lines ~93-102 (COPY .../migrations + ENV MIGRATIONS_FOLDER
    block to mirror for SEEDS_FOLDER)
  </read_first>
  <action>
    Create packages/database/src/seed.ts exporting async function
    runBrainSeed(sql: Sql, brainType: string, seedsFolder: string): Promise&lt;void&gt;.
    Import { readdir, readFile } from "fs/promises" and { join } from "path" — do NOT
    import anything from migrate.ts. Duplicate (do not import) two small local helpers
    mirroring migrate.ts exactly: isLockNotAvailable(err: unknown) checking
    (err as {code:string}).code === "55P03", and sleep(ms: number).
    Before the retry loop, unconditionally run the same two bootstrap statements as
    migrate.ts: CREATE TABLE IF NOT EXISTS _schema_lock (id INTEGER PRIMARY KEY, locked_at
    TIMESTAMPTZ) and INSERT INTO _schema_lock (id, locked_at) VALUES (1, NOW()) ON CONFLICT
    (id) DO NOTHING — this makes runBrainSeed() safe to call standalone (e.g. from tests)
    without depending on runMigrations() having run first in the same process.
    Before entering the retry loop, read the seed file contents once: readdir(seedsFolder),
    filter entries ending in ".sql", sort() them, then readFile each as utf-8 — store an
    array of file contents in memory so file I/O is not repeated across lock-retry attempts.
    Then run a MAX_RETRIES=3 retry loop identical in shape to migrate.ts's: each attempt
    calls sql.begin(async (tx) => {...}); inside the tx, first acquire the lock with
    tx`SELECT id FROM _schema_lock WHERE id = 1 FOR UPDATE NOWAIT`, then for each file
    content in order call await tx.unsafe(content) to execute it (simple-protocol,
    multi-statement per file, no interpolated params), then run the D-08/D-09 fail-fast
    validation: tx`SELECT 1 FROM fup_config WHERE brain_type = ${brainType}` and
    tx`SELECT 1 FROM prompts WHERE brain_type = ${brainType} AND key = 'fup'`; if either
    query's row array has length 0, throw new Error naming brainType and exactly which row
    is missing (fup_config vs prompts key='fup') plus a pointer to
    packages/database/src/seeds/${brainType}/ for diagnosis. On catch: if
    isLockNotAvailable(err) and attempt &lt; MAX_RETRIES-1, sleep(200) and retry; if
    isLockNotAvailable(err) on the last attempt, throw a descriptive "could not acquire
    seed lock" Error; any OTHER error (including the fail-fast validation Error thrown
    above) must propagate immediately with NO retry, exactly like migrate.ts's "Outros
    erros — propagar imediatamente" branch. runBrainSeed() must never terminate the process
    itself anywhere in this file — it only throws; ending the process on failure stays
    entirely the caller's decision (BrainRunner.init()), the identical library contract
    already documented on runMigrations().

    Create packages/database/src/seeds/sdr/0001_fup_defaults.sql with two idempotent
    statements using the exact ON CONFLICT DO NOTHING idiom from 0005_brain_sdr_prompts.sql:
    (1) INSERT INTO fup_config (brain_type, enabled, intervals_seconds, min_hour, max_hour,
    allowed_days, timezone) VALUES ('sdr', true, ARRAY[3600, 86400, 259200], 8, 18,
    ARRAY['mon','tue','wed','thu','fri'], 'America/Sao_Paulo') ON CONFLICT (brain_type) DO
    NOTHING — values per D-01/D-02/D-03/D-04; (2) INSERT INTO prompts (brain_type, key,
    content) VALUES ('sdr', 'fup', &lt;content&gt;) ON CONFLICT (brain_type, key) DO NOTHING,
    where &lt;content&gt; is a complete, production-ready, generic Portuguese follow-up
    system-prompt (D-05/D-06) instructing the LLM to write a short (1-3 sentence), cordial,
    professional WhatsApp-appropriate re-engagement message from conversation history,
    without being pushy, without repeating prior messages verbatim, asking politely about
    continued interest when history is insufficient, and never revealing it is an AI unless
    asked directly. The content string must contain zero square-bracket characters and zero
    instructions telling the client/operator to edit the text before shipping (D-06
    prohibition — this must be a real, usable prompt, not a placeholder).

    Add "export { runBrainSeed } from './seed.js';" to packages/database/src/index.ts,
    alongside the existing "export { runMigrations } from './migrate.js';" line.

    In packages/core/src/runner/runner.ts: add "runBrainSeed" to the existing
    "import { runMigrations } from ..." line from "@brain-pkg/database" (line ~12). Add an
    optional `seedsFolder?: string` field to the BrainRunnerOptions interface (mirroring the
    existing `migrationsFolder?: string` field) and a `private readonly seedsFolder:
    string | undefined;` class field set from `options.seedsFolder` in the constructor
    (mirroring `this.migrationsFolder = options.migrationsFolder;`). In init(), immediately
    after the existing `this.logger.info({ brainId: this.brain.id }, 'Migrations
    completed');` line (~145) and before the embeddingProvider resolution block, insert:
    resolve `const seedsFolder = this.seedsFolder ?? process.env.SEEDS_FOLDER;`, fail-fast
    with the identical shape as the MIGRATIONS_FOLDER check just above it (same
    logger.error(...) call followed by the same process-termination call with the same exit
    code, when seedsFolder is falsy, log message "SEEDS_FOLDER not set — cannot run brain
    seed"), then `await runBrainSeed(this.sql, this.brain.brainType, seedsFolder).catch((err:
    unknown) => { this.logger.error({ brainId: this.brain.id, err }, 'Brain seed failed —
    aborting init'); &lt;same process-termination call, exit code 1&gt; });` followed by a
    `this.logger.info({ brainId: this.brain.id }, 'Brain seed completed');` log line.

    In apps/brain-sdr/Dockerfile: immediately after the existing MIGRATIONS_FOLDER COPY/ENV
    block (~lines 93-102), add a new block: `COPY --from=builder
    /app/packages/database/src/seeds/sdr ./seeds` followed by `ENV SEEDS_FOLDER=/app/seeds`
    — copy ONLY the sdr subfolder, never the seeds/ root (Pitfall 3). Attempt to add a
    matching SEEDS_FOLDER documentation line to apps/brain-sdr/.env.example mirroring
    however MIGRATIONS_FOLDER is documented there for local dev; if the sandbox denies
    reading/editing that file, skip this one sub-step and note the omission in the plan's
    SUMMARY — it does not block SEED-02/03/04/05 (the Dockerfile ENV default already
    documents production behavior).
  </action>
  <verify>
    <automated>cd packages/database && bun test src/__tests__/unit/seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/seed.ts exports an async function named runBrainSeed with
      signature (sql: Sql, brainType: string, seedsFolder: string): Promise&lt;void&gt;
    - packages/database/src/seed.ts contains no occurrence of the literal string
      "process.exit" (the throw-not-exit contract)
    - packages/database/src/seeds/sdr/0001_fup_defaults.sql contains "ON CONFLICT
      (brain_type) DO NOTHING" and "ON CONFLICT (brain_type, key) DO NOTHING", and contains
      zero "[" / "]" characters
    - packages/core/src/runner/runner.ts contains "runBrainSeed(this.sql,
      this.brain.brainType" and that call site textually appears before the existing
      "await loadPrompts(this.sql, this.brain.brainType, this.brain.promptKeys)" line
    - apps/brain-sdr/Dockerfile contains "seeds/sdr" and does NOT contain "seeds/support" or
      "seeds/echo"
    - packages/database/src/migrate.ts is byte-identical to its pre-task state (git diff
      packages/database/src/migrate.ts returns no output)
    - packages/database/src/migrations/0002_echo_brain_seed.sql,
      0005_brain_sdr_prompts.sql, and 0010_brain_support_prompts.sql are byte-identical to
      their pre-task state (git diff on all three returns no output)
  </acceptance_criteria>
  <done>
    runBrainSeed() exists, is wired into BrainRunner.init() in the correct order, brain-sdr's
    Docker image only ever contains its own seed SQL, and none of the pre-existing
    migration/lock files were touched.
  </done>
  <reversibility rating="reversible">Purely additive: new files plus one new call site in
    init(). Removing the call and deleting the new files fully reverts behavior with no data
    migration required — existing prompts/fup_config rows created by this seed are just
    inert extra rows if reverted, same class as the already-accepted 0002/0005/0010 legacy
    rows.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Unit tests for runBrainSeed() + fix existing runner test mocks</name>
  <files>packages/database/src/__tests__/unit/seed.test.ts, packages/core/src/runner/__tests__/brain-runner.test.ts, packages/core/src/runner/__tests__/runner-fup.test.ts, packages/core/src/runner/__tests__/runner-wr.test.ts</files>
  <read_first>
    packages/database/src/migrate.test.ts (the exact sql/tx tagged-template mock pattern —
    sqlCalls/beginCalls tracking arrays, the begin() Proxy shape — reuse this pattern
    verbatim, do not invent a new mocking style),
    packages/database/src/__tests__/integration/migration-v14.test.ts (the real-fs,
    read-actual-file convention for asserting migration file contents — mirror this for the
    new SEED-05 check instead of mocking fs),
    packages/core/src/runner/__tests__/brain-runner.test.ts lines ~139-161 and ~302-327
    (the exact mock.module("@brain-pkg/database", ...) block to extend, and the exact
    process.exit-mocking pattern for a "missing ENV" test — mirror it for SEEDS_FOLDER),
    packages/core/src/runner/__tests__/runner-fup.test.ts lines ~75-77 and ~173-175 (same
    mock.module block + the two process.env lines that must gain a SEEDS_FOLDER sibling),
    packages/core/src/runner/__tests__/runner-wr.test.ts lines ~92-106 and ~114-116 (same),
    STATE.md "Known Pitfalls" section (bun test's mock.module() is process-global — do NOT
    mock "fs" or "fs/promises" at module level in the new seed.test.ts; use real temporary
    directories with real files instead, exactly like migration-v14.test.ts reads real
    files)
  </read_first>
  <action>
    In all three existing runner test files (brain-runner.test.ts, runner-fup.test.ts,
    runner-wr.test.ts): add `runBrainSeed: mock(async () => {})` as a new property inside
    each file's existing `mock.module("@brain-pkg/database", () => ({ ... }))` block
    (alongside the existing `runMigrations: mock(async () => {})` line). Add a
    `process.env.SEEDS_FOLDER = "/tmp/test-seeds"` line (choose a distinct per-file tmp path
    to avoid cross-file confusion, e.g. "/tmp/test-seeds", "/tmp/fake-seeds",
    "/tmp/test-seeds-wr") immediately next to each file's existing
    `process.env.MIGRATIONS_FOLDER = ...` line — without this, runner.ts's new SEEDS_FOLDER
    fail-fast check calls process.exit(1) during these tests' init() and would abort the
    whole bun test process. In brain-runner.test.ts, add one new test mirroring the existing
    "init() calls process.exit(1) when MIGRATIONS_FOLDER ENV is not set..." test (~lines
    302-327): delete process.env.SEEDS_FOLDER, mock process.exit to throw, construct a
    BrainRunner with no seedsFolder option, call init(), and assert process.exit was called
    with 1 — then restore process.env.SEEDS_FOLDER in a finally block.

    Create packages/database/src/__tests__/unit/seed.test.ts. Do NOT mock "fs" or
    "fs/promises" (process-global mock.module pollution risk per STATE.md). Instead, in a
    beforeAll/afterAll pair, create a real temporary directory (fs/promises.mkdtemp under
    os.tmpdir()), write one real fixture file "0001_fixture.sql" into it containing a
    trivial INSERT statement, and remove the directory afterwards (fs/promises.rm with
    recursive+force). Mock only "sql"/"tx" exactly like migrate.test.ts's mockSql (tagged
    template function tracking calls in an array, a .begin() implementation proxying calls
    into a beginCalls array, plus a .unsafe mock tracking executed file contents in its own
    array) — dispatch the tagged-template mock's return value based on inspecting
    strings.raw[0]: return a non-empty array for the fup_config/prompts validation SELECTs
    by default (happy path), and override to return [] in specific tests to exercise the
    D-08 fail-fast throw. Cover: (1) bootstraps _schema_lock idempotently before the retry
    loop, (2) acquires the lock via FOR UPDATE NOWAIT inside sql.begin(), (3) executes every
    file from the temp fixture directory via .unsafe(), sorted by filename, (4) throws when
    the fup_config validation SELECT returns empty, naming the brainType in the message, (5)
    throws when the prompts key='fup' validation SELECT returns empty, (6) a thrown
    validation error is NOT retried (propagates on the first attempt, no second sql.begin()
    call), (7) a 55P03 lock error IS retried up to 3 attempts with the same semantics as
    migrate.test.ts, (8) runBrainSeed() never calls process.exit — assert by spying on
    process.exit and expecting zero calls across every test in the file. Add one more
    describe block "SEED-05: existing seed migrations untouched" that reads the real files
    packages/database/src/migrations/0002_echo_brain_seed.sql,
    0005_brain_sdr_prompts.sql, and 0010_brain_support_prompts.sql from disk (via
    import.meta.dir relative path, same convention as migration-v14.test.ts) and asserts
    each still contains its known original marker substring — "'echo'" and "ON CONFLICT
    (brain_type, key) DO NOTHING" in 0002, "'sdr'" and "'qualification'" in 0005, "'support'"
    in 0010 — proving this phase did not rewrite them.
  </action>
  <verify>
    <automated>cd packages/database && bun test src/__tests__/unit/seed.test.ts && cd ../core && bun test src/runner/__tests__/brain-runner.test.ts src/runner/__tests__/runner-fup.test.ts src/runner/__tests__/runner-wr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/__tests__/unit/seed.test.ts has zero occurrences of
      `mock.module("fs"` or `mock.module("fs/promises"`
    - `bun test src/__tests__/unit/seed.test.ts` (packages/database) exits 0
    - `bun test src/runner/__tests__/brain-runner.test.ts src/runner/__tests__/runner-fup.test.ts src/runner/__tests__/runner-wr.test.ts`
      (packages/core) exits 0 with no test newly failing due to an unhandled process.exit(1)
    - each of the three runner test files' `mock.module("@brain-pkg/database", ...)` block
      contains a `runBrainSeed` key
    - each of the three runner test files sets `process.env.SEEDS_FOLDER` at module scope
  </acceptance_criteria>
  <done>
    runBrainSeed() has a dedicated, DB-free unit test suite covering the happy path, both
    D-08 fail-fast branches, retry semantics, the throw-not-exit contract, and SEED-05's
    untouched-migrations guarantee; none of the pre-existing runner tests regress from the
    new SEEDS_FOLDER fail-fast check.
  </done>
  <reversibility rating="reversible">Test-only changes; reverting deletes/edits test files
    with zero production impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build → runtime filesystem | Seed SQL content is baked into the image at build time (COPY), never fetched or interpolated from a runtime/request source |
| BrainRunner.init() → process lifecycle | A seed failure escalates to process.exit(1), same trust tier as the existing MIGRATIONS_FOLDER/promptKeys fail-fast checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Denial of Service | BrainRunner.init() SEEDS_FOLDER/runBrainSeed() fail-fast checks | medium | mitigate | Seed SQL is baked into the Docker image at build time, not fetched at runtime — a missing-seed failure can only originate from a genuine build/config defect, and the thrown Error names the exact brainType + missing entity (fup_config vs prompts.key='fup') for immediate diagnosis, mirroring the existing promptKeys fail-fast precedent in the same file |
| T-33-02 | Information Disclosure | Error log emitted by runBrainSeed()/BrainRunner.init() on validation failure | low | accept | Logged via the existing pino logger to container stdout only; the message contains brainType + which row is missing, never prompt content, DATABASE_URL, or lead PII |
| T-33-03 | Tampering | runBrainSeed() executes seed file contents via tx.unsafe() (simple query protocol) | medium | mitigate | Executed content is exclusively the literal, developer-authored, code-reviewed .sql file baked into the image at build time — zero runtime/user-supplied interpolation into the executed string, eliminating SQL-injection surface |
| T-33-05 | Tampering | apps/brain-sdr/Dockerfile COPY instruction for the new seeds/ tree | high | mitigate | COPY targets only packages/database/src/seeds/sdr (never the seeds/ root), preventing SDR's image from ever containing support/echo seed SQL; full cross-type verification (all three Dockerfiles + all three seed folders inspected together) completes in Plan 33-02 once support/echo exist |
</threat_model>

<verification>
Run `bun test src/__tests__/unit/seed.test.ts` in packages/database and the three named
runner test files in packages/core — all must pass with exit code 0. Confirm via `git diff`
that migrate.ts and migrations 0002/0005/0010 show zero changes.
</verification>

<success_criteria>
A fresh brain-sdr database, after BrainRunner.init() completes, has exactly one fup_config
row for brain_type='sdr' and exactly one prompts row (brain_type='sdr', key='fup') — proven
by unit test, not by inspection. Re-running the seed does not duplicate or throw. No
existing migration/lock file changed.
</success_criteria>

<output>
Create `.planning/phases/33-seed-por-tipo-de-brain/33-01-SUMMARY.md` when done
</output>
