---
phase: 33-seed-por-tipo-de-brain
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - packages/database/src/seeds/support/0001_fup_defaults.sql
  - packages/database/src/seeds/echo/0001_fup_defaults.sql
  - apps/brain-support/Dockerfile
  - apps/brain-support/.env.example
  - apps/brain-echo/Dockerfile
  - apps/brain-echo/.env.example
  - packages/database/src/__tests__/unit/seed-cross-brain-isolation.test.ts
  - packages/database/src/__tests__/integration/seed-idempotency.test.ts
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04, SEED-05]

must_haves:
  truths:
    - "Starting brain-support and brain-echo against fresh databases each results in exactly one fup_config row and one prompts(key='fup') row scoped to their own brain_type, with no manual insert (SEED-02, SEED-03)"
    - "Inspecting any one brain type's seeds/<type> folder or its built Docker image never reveals another brain_type's seed SQL — physical separation, not just query-time filtering (SEED-01, Pitfall 3)"
    - "Running runBrainSeed() twice in a row against the same database for the same brain_type does not create duplicate fup_config/prompts rows and does not throw, for all three brain types (SEED-04)"
    - "Migrations 0002_echo_brain_seed.sql, 0005_brain_sdr_prompts.sql, and 0010_brain_support_prompts.sql remain byte-for-byte unchanged from before this phase (SEED-05 — re-confirmed after adding the two remaining brain types)"
  artifacts:
    - packages/database/src/seeds/support/0001_fup_defaults.sql
    - packages/database/src/seeds/echo/0001_fup_defaults.sql
    - packages/database/src/__tests__/unit/seed-cross-brain-isolation.test.ts
    - packages/database/src/__tests__/integration/seed-idempotency.test.ts
  key_links:
    - "apps/brain-support/Dockerfile COPYs only seeds/support; apps/brain-echo/Dockerfile COPYs only seeds/echo — each Brain's SEEDS_FOLDER points at a physically disjoint subtree, same pattern as brain-sdr from Plan 33-01"
    - "runBrainSeed(sql, 'support', ...) and runBrainSeed(sql, 'echo', ...) share the same fup_config/prompts tables as 'sdr' but insert disjoint (brain_type[, key]) rows — verified idempotent under repeated invocation"
  prohibitions:
    - "apps/brain-support/Dockerfile and apps/brain-echo/Dockerfile must each COPY only their own packages/database/src/seeds/<own-type> subfolder — never the full packages/database/src/seeds/ tree (Pitfall 3)"
    - "The seeded prompts(key='fup') content for support and echo must not read as an unfinished placeholder — no square-bracket placeholder tokens and no instruction to edit before going live (D-06), same bar as sdr's content from Plan 33-01"
---

## Artifacts this phase produces

New files/symbols created by this plan (exclude these from drift/source-grounding checks —
they do not exist yet in the codebase):

- `packages/database/src/seeds/support/0001_fup_defaults.sql` — new file
- `packages/database/src/seeds/echo/0001_fup_defaults.sql` — new file
- `packages/database/src/__tests__/unit/seed-cross-brain-isolation.test.ts` — new test file
- `packages/database/src/__tests__/integration/seed-idempotency.test.ts` — new test file

Modified (not new) files: `apps/brain-support/Dockerfile`, `apps/brain-support/.env.example`,
`apps/brain-echo/Dockerfile`, `apps/brain-echo/.env.example`.

Depends on artifacts from Plan 33-01 already existing: `packages/database/src/seed.ts`
(`runBrainSeed()`), `packages/database/src/seeds/sdr/0001_fup_defaults.sql`.

<objective>
Mechanically repeat Plan 33-01's proven shape for the two remaining brain types
(`support`, `echo`) — per D-07, brain-echo gets the exact same seed treatment as the
production brain types, no exceptions by type — and add the cross-brain verification that
only becomes possible once all three brain types exist side by side: physical seed
isolation (Pitfall 3) and idempotency across brain types (SEED-04).

Purpose: closes SEED-01 (the actual "no cross-contamination" claim can only be verified
once there is more than one brain type to contaminate) and extends SEED-02/03's fix to every
Brain this milestone covers.
Output: `packages/database/src/seeds/support/0001_fup_defaults.sql`,
`packages/database/src/seeds/echo/0001_fup_defaults.sql`, updated Dockerfiles/`.env.example`
for both apps, and two new test files proving physical isolation + idempotency.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-seed-por-tipo-de-brain/33-CONTEXT.md
@.planning/phases/33-seed-por-tipo-de-brain/33-PATTERNS.md
@.planning/research/PITFALLS.md

@packages/database/src/seed.ts
@packages/database/src/seeds/sdr/0001_fup_defaults.sql
@apps/brain-sdr/Dockerfile
@apps/brain-support/Dockerfile
@apps/brain-echo/Dockerfile
@packages/core/src/runner/__tests__/brain-runner.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Support + Echo seed files and Dockerfile wiring</name>
  <files>packages/database/src/seeds/support/0001_fup_defaults.sql, packages/database/src/seeds/echo/0001_fup_defaults.sql, apps/brain-support/Dockerfile, apps/brain-support/.env.example, apps/brain-echo/Dockerfile, apps/brain-echo/.env.example</files>
  <read_first>
    packages/database/src/seeds/sdr/0001_fup_defaults.sql (the exact content/shape to
    replicate, only swapping the brain_type literal),
    packages/database/src/seed.ts (confirms runBrainSeed()'s contract — no code change
    needed here, just seed data + Docker wiring),
    apps/brain-sdr/Dockerfile (the COPY seeds/sdr + ENV SEEDS_FOLDER block added in Plan
    33-01 — mirror its exact placement),
    apps/brain-support/Dockerfile lines ~94-104 (existing MIGRATIONS_FOLDER COPY/ENV block
    — note the D-04 comment "packages/database/src/migrations é compartilhado entre todos
    os Brains", which is correct for migrations and must stay that way — the new
    seeds/support COPY is the brain-type-scoped counterpart),
    apps/brain-echo/Dockerfile lines ~88-96 (existing MIGRATIONS_DIR COPY/ENV block — note
    this file uses the legacy ENV name MIGRATIONS_DIR rather than MIGRATIONS_FOLDER; that
    naming inconsistency is pre-existing and out of scope for this phase — do not rename it,
    only add the new SEEDS_FOLDER block using the correct name SEEDS_FOLDER, consistent with
    sdr/support)
  </read_first>
  <action>
    Create packages/database/src/seeds/support/0001_fup_defaults.sql and
    packages/database/src/seeds/echo/0001_fup_defaults.sql, each a byte-identical copy of
    packages/database/src/seeds/sdr/0001_fup_defaults.sql except the brain_type literal
    ('sdr' → 'support' in one file, 'sdr' → 'echo' in the other) in both the fup_config
    INSERT and the prompts INSERT — per D-01/D-05, all three brain types share the exact
    same fup_config values (enabled=true, intervals_seconds=ARRAY[3600, 86400, 259200],
    min_hour=8, max_hour=18, allowed_days=ARRAY['mon','tue','wed','thu','fri'],
    timezone='America/Sao_Paulo') and the exact same generic prompts(key='fup') content —
    no per-type tone variation this phase (D-05).

    In apps/brain-support/Dockerfile: immediately after the existing MIGRATIONS_FOLDER
    COPY/ENV block, add `COPY --from=builder /app/packages/database/src/seeds/support
    ./seeds` followed by `ENV SEEDS_FOLDER=/app/seeds`. In apps/brain-echo/Dockerfile: same
    block shape, using `COPY --from=builder /app/packages/database/src/seeds/echo ./seeds`
    and `ENV SEEDS_FOLDER=/app/seeds`. Attempt to add a matching SEEDS_FOLDER documentation
    line to each app's .env.example mirroring however MIGRATIONS_FOLDER/MIGRATIONS_DIR is
    already documented there; if the sandbox denies reading/editing those files, skip this
    sub-step and note the omission in the SUMMARY (non-blocking, same fallback as Plan
    33-01).
  </action>
  <verify>
    <automated>grep -c "seeds/support" apps/brain-support/Dockerfile && grep -c "seeds/echo" apps/brain-echo/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/seeds/support/0001_fup_defaults.sql contains "'support'" in both
      the fup_config and prompts INSERT statements and contains zero "[" / "]" characters
    - packages/database/src/seeds/echo/0001_fup_defaults.sql contains "'echo'" in both the
      fup_config and prompts INSERT statements and contains zero "[" / "]" characters
    - apps/brain-support/Dockerfile contains "seeds/support" and does NOT contain
      "seeds/sdr" or "seeds/echo"
    - apps/brain-echo/Dockerfile contains "seeds/echo" and does NOT contain "seeds/sdr" or
      "seeds/support"
    - packages/database/src/migrations/0002_echo_brain_seed.sql,
      0005_brain_sdr_prompts.sql, 0010_brain_support_prompts.sql remain byte-identical
      (git diff on all three returns no output)
  </acceptance_criteria>
  <done>
    All three brain types (sdr, support, echo) have their own seed file with identical
    fup_config/fup-prompt content, each Brain's Dockerfile copies only its own subfolder.
  </done>
  <reversibility rating="reversible">Purely additive seed files and Dockerfile lines,
    identical shape to Plan 33-01's sdr wiring — trivially revertible.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Cross-brain physical isolation test + idempotency integration test</name>
  <files>packages/database/src/__tests__/unit/seed-cross-brain-isolation.test.ts, packages/database/src/__tests__/integration/seed-idempotency.test.ts</files>
  <read_first>
    packages/database/src/seeds/sdr/0001_fup_defaults.sql,
    packages/database/src/seeds/support/0001_fup_defaults.sql,
    packages/database/src/seeds/echo/0001_fup_defaults.sql (the three real files to read and
    cross-check — no mocking, real fs reads, same convention as migration-v14.test.ts),
    apps/brain-sdr/Dockerfile, apps/brain-support/Dockerfile, apps/brain-echo/Dockerfile
    (the three real Dockerfiles to grep for cross-type leakage),
    packages/core/src/runner/__tests__/brain-runner.integration.test.ts (the
    describeOrSkip/TEST_DB_URL gating convention, the beforeAll/afterAll real-postgres
    connection setup, and the cleanup-by-brainType-or-numero pattern — mirror this exactly
    for the new seed-idempotency integration test rather than inventing a new DB-test
    convention),
    packages/database/src/seed.ts (runBrainSeed()'s signature to call directly)
  </read_first>
  <action>
    Create packages/database/src/__tests__/unit/seed-cross-brain-isolation.test.ts (no DB,
    no mocks — pure real-file reads via readFileSync/existsSync from "fs", same style as
    migration-v14.test.ts). For each pair of the three brain types, read both types' seed
    file content and assert brain type A's file text does not contain brain type B's
    brain_type literal in single-quoted form (e.g. assert sdr's file text does not contain
    "'support'" or "'echo'"); repeat for all 6 ordered pairs. Also read all three
    Dockerfiles and assert each one's text contains its own "seeds/&lt;type&gt;" path exactly
    once and contains neither of the other two types' "seeds/&lt;type&gt;" paths — this is the
    Pitfall 3 physical-separation proof.

    Create packages/database/src/__tests__/integration/seed-idempotency.test.ts, gated with
    the same `const TEST_DB_URL = process.env.POSTGRES_URL ?? process.env.TEST_DATABASE_URL;`
    / `const describeOrSkip = TEST_DB_URL ? describe : describe.skip;` pattern as
    brain-runner.integration.test.ts (so this test is a no-op, not a failure, in any
    environment without a real test database configured — note this explicitly as a
    precondition, it is not a blocker for completing this task). In beforeAll, connect via
    postgres(TEST_DB_URL) assuming the schema (fup_config, prompts tables) already exists in
    that test database, same assumption as the existing integration test. For each of the
    three brain types in turn, using a synthetic brainType string that will not collide with
    real data (e.g. "seed-idem-sdr", "seed-idem-support", "seed-idem-echo") and that type's
    real seeds/&lt;type&gt; folder path, call runBrainSeed(sql, syntheticBrainType, folderPath)
    TWICE in a row and assert: (a) neither call throws, (b) a COUNT query on fup_config
    WHERE brain_type = syntheticBrainType returns exactly 1, (c) a COUNT query on prompts
    WHERE brain_type = syntheticBrainType AND key = 'fup' returns exactly 1. In afterAll,
    delete the fup_config and prompts rows created for all three synthetic brainTypes and
    close the sql connection, mirroring the existing test's cleanup-by-filter convention.
  </action>
  <verify>
    <automated>cd packages/database && bun test src/__tests__/unit/seed-cross-brain-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/__tests__/unit/seed-cross-brain-isolation.test.ts` (packages/database)
      exits 0
    - the isolation test file contains at least 6 distinct cross-type content assertions
      (one per ordered pair among sdr/support/echo) plus 3 Dockerfile assertions
    - seed-idempotency.test.ts opens with `const TEST_DB_URL = process.env.POSTGRES_URL ??
      process.env.TEST_DATABASE_URL;` and a `describeOrSkip` derived from it, matching
      brain-runner.integration.test.ts's exact gating idiom
    - when run with TEST_DATABASE_URL unset, `bun test
      src/__tests__/integration/seed-idempotency.test.ts` exits 0 (all cases reported as
      skipped, not failed)
  </acceptance_criteria>
  <done>
    An automated, DB-free test proves no brain type's seed SQL or Dockerfile ever references
    another type's brain_type; a gated, DB-backed test proves runBrainSeed() is idempotent
    for all three brain types when a real test database is available.
  </done>
  <precondition>TEST_DATABASE_URL or POSTGRES_URL env var — if unset, the new integration
    test auto-skips via the same describeOrSkip convention already used by
    brain-runner.integration.test.ts; this is expected in most local/CI runs and is not a
    failure.</precondition>
  <reversibility rating="reversible">Test-only changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker build → runtime filesystem | Same boundary as Plan 33-01, now exercised for support/echo — seed SQL baked in at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05 | Tampering | apps/brain-support/Dockerfile and apps/brain-echo/Dockerfile COPY instructions | high | mitigate | Each Dockerfile COPYs only its own packages/database/src/seeds/&lt;own-type&gt; subfolder; the new automated test (Task 2) inspects all three Dockerfiles + all three seeds/ subfolders together and asserts no cross-type brain_type literal ever appears in a different type's seed file or Dockerfile path (Pitfall 3) |
| T-33-01 (carried forward from 33-01) | Denial of Service | Same SEEDS_FOLDER/runBrainSeed() fail-fast mechanism, now exercised for support/echo | medium | mitigate | Same mitigation as Plan 33-01 — verified for all three brain types via the new idempotency integration test |
</threat_model>

<verification>
Run the new DB-free isolation test — exit 0. When a test database is available, run the new
gated idempotency test and confirm no duplicate rows after two runs per brain type.
</verification>

<success_criteria>
All three brain types (sdr, support, echo) seed their own fup_config + fup prompt
automatically and idempotently; no brain type's Docker image or seed SQL ever references
another type's brain_type.
</success_criteria>

<output>
Create `.planning/phases/33-seed-por-tipo-de-brain/33-02-SUMMARY.md` when done
</output>
