---
phase: 25-fup-activation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/core/src/leads/__tests__/lead-service.test.ts
autonomous: true
requirements:
  - FUP-01
  - FUP-02

must_haves:
  truths:
    - "Test suite verifies INSERT with fup_config enabled=true sets fup_enabled=true"
    - "Test suite verifies INSERT with fup_config enabled=false sets fup_enabled=false"
    - "Test suite verifies INSERT without brainType defaults to fup_enabled=false"
    - "Test suite verifies UPDATE never changes fup_enabled (preserves existing value)"
    - "Test suite verifies INSERT with nonexistent fup_config defaults to fup_enabled=false"
  artifacts:
    - path: "packages/core/src/leads/__tests__/lead-service.test.ts"
      provides: "Test cases for FUP activation logic"
      min_lines: 200
  key_links:
    - from: "lead-service.test.ts"
      to: "LeadService.upsertLead()"
      via: "mock assertions on fup_enabled behavior"
      pattern: "expect.*fupEnabled"
---

<objective>
Create test stubs for FUP activation logic — verify that LeadService.upsertLead() correctly activates fup_enabled based on fup_config when brainType is provided.

Purpose: Nyquist validation — tests written before implementation ensure behavioral contract is clear
Output: 5 new test cases in lead-service.test.ts covering INSERT/UPDATE scenarios with fup_config
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-fup-activation/25-CONTEXT.md
@.planning/phases/25-fup-activation/25-RESEARCH.md
@packages/core/src/leads/__tests__/lead-service.test.ts
@packages/database/src/schema/tables.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add FUP activation test cases to lead-service.test.ts</name>
  <read_first>
    - packages/core/src/leads/__tests__/lead-service.test.ts — current test structure and mock patterns
    - packages/database/src/schema/tables.ts — fupConfig schema (brainType PK, enabled boolean)
    - .planning/phases/25-fup-activation/25-CONTEXT.md — D-02 (INSERT activates), D-03 (UPDATE preserves), D-04 (silent fallback)
  </read_first>
  <files>packages/core/src/leads/__tests__/lead-service.test.ts</files>
  <behavior>
    - Test 1 (INSERT + config enabled): upsertLead("5511999990001", "lead-new", "João", "sdr") when fup_config exists with enabled=true → returns lead with fupEnabled=true
    - Test 2 (INSERT + config disabled): upsertLead("5511999990001", "lead-new", "João", "sdr") when fup_config exists with enabled=false → returns lead with fupEnabled=false
    - Test 3 (INSERT without brainType): upsertLead("5511999990001", "lead-new", "João") → returns lead with fupEnabled=false (default)
    - Test 4 (UPDATE preserves fup_enabled): upsertLead on existing lead with fupEnabled=false → lead.fupEnabled remains false (not overwritten)
    - Test 5 (INSERT + nonexistent config): upsertLead("5511999990001", "lead-new", "João", "unknown-brain") when fup_config does not exist → returns lead with fupEnabled=false, no error thrown
  </behavior>
  <action>
Add a new describe block "LeadService — FUP activation (Phase 25)" to packages/core/src/leads/__tests__/lead-service.test.ts.

Within this describe block, create 5 test cases (it() blocks) covering the behaviors above. Use the existing mock pattern from the file:
- Mock fupConfig table in the @brain-pkg/database module mock (add to line ~36-52 where leads mock exists)
- Create new mocks for SELECT query to fup_config: mockLimit4, mockWhere4, mockFrom4, mockSelect4
- In each test's beforeEach, configure the SELECT mock to return appropriate fup_config rows:
  - Test 1: return [{ brainType: "sdr", enabled: true }]
  - Test 2: return [{ brainType: "sdr", enabled: false }]
  - Test 3: no brainType passed, SELECT not called
  - Test 4: existing lead scenario — mock SELECT from leads table to return existing row with fupEnabled=false
  - Test 5: return [] (empty array — config not found)

Assert:
- Test 1: expect(lead.fupEnabled).toBe(true)
- Test 2: expect(lead.fupEnabled).toBe(false)
- Test 3: expect(lead.fupEnabled).toBe(false)
- Test 4: expect(lead.fupEnabled).toBe(false) AND expect(mockSet.mock.calls[0][0]).not.toHaveProperty("fupEnabled") — confirms UPDATE doesn't touch fupEnabled
- Test 5: expect(lead.fupEnabled).toBe(false)

**Critical**: These tests will FAIL until Task 2 (Wave 1) implements the actual logic. This is expected (RED phase of TDD).

Per D-04 from CONTEXT.md: behavior is silent when fup_config doesn't exist — no warning, just defaults to false.
  </action>
  <verify>
    <automated>bun test packages/core/src/leads/__tests__/lead-service.test.ts -t "FUP activation"</automated>
  </verify>
  <acceptance_criteria>
    - lead-service.test.ts contains new describe block "LeadService — FUP activation (Phase 25)"
    - New describe block contains exactly 5 it() test cases
    - Test 1 asserts fupEnabled=true when config enabled=true
    - Test 2 asserts fupEnabled=false when config enabled=false
    - Test 3 asserts fupEnabled=false when brainType not passed
    - Test 4 asserts fupEnabled unchanged on UPDATE (preserves false)
    - Test 5 asserts fupEnabled=false when config not found, no exception thrown
    - All 5 tests currently FAIL (RED phase) — implementation in Wave 1 will make them pass
    - Running `bun test packages/core/src/leads/__tests__/lead-service.test.ts` exits with non-zero (tests fail) — expected at this stage
  </acceptance_criteria>
  <done>
5 test cases added to lead-service.test.ts covering FUP activation scenarios; tests fail (RED) awaiting implementation
  </done>
</task>

</tasks>

<verification>
Run test suite to confirm tests are structured correctly (will fail — expected):

```bash
bun test packages/core/src/leads/__tests__/lead-service.test.ts -t "FUP activation"
```

Expected: 5 tests FAIL (RED phase). Test structure is valid, assertions are in place.
</verification>

<success_criteria>
- [ ] 5 new test cases exist in lead-service.test.ts
- [ ] Tests cover INSERT with enabled=true/false, INSERT without brainType, UPDATE preserving fupEnabled, INSERT with nonexistent config
- [ ] Tests use mock pattern consistent with existing lead-service.test.ts structure
- [ ] All 5 tests FAIL at this stage (RED phase) — implementation comes in Wave 1
</success_criteria>

<output>
After completion, create `.planning/phases/25-fup-activation/25-01-SUMMARY.md`
</output>
