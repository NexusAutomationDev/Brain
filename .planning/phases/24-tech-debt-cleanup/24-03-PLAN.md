---
phase: 24-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01-PLAN.md
  - 24-02-PLAN.md
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "RAG-02 e RAG-03 marcados como [x] em REQUIREMENTS.md (implementados e verificados na Phase 23)"
    - "Traceability RAG-02 e RAG-03 mostram 'Complete' (não 'Pending')"
    - "Traceability EVT-03 aponta para Phase 22 (não Phase 20) — implementado em fup-scheduler.ts"
    - "bun tsc --noEmit -p packages/core/tsconfig.json retorna 0 erros (SC-5 confirmado)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Tracker atualizado refletindo estado real do código implementado"
      contains: "[x] **RAG-02**"
  key_links:
    - from: "REQUIREMENTS.md RAG-02 checkbox"
      to: "[x]"
      via: "mudança de [ ] para [x]"
      pattern: "\\[x\\] \\*\\*RAG-02\\*\\*"
    - from: "REQUIREMENTS.md traceability EVT-03"
      to: "Phase 22"
      via: "correção de Phase 20 → Phase 22"
      pattern: "EVT-03.*Phase 22"
---

<objective>
Verificar SC-5 (TypeScript limpo) e atualizar REQUIREMENTS.md com estado real do código — 5 mudanças pontuais no tracker (D-06).

Purpose: Fechar o gap de rastreabilidade identificado no audit: 12 requirements marcados como `[ ]` no REQUIREMENTS.md mas já implementados e verificados. Esta fase corrige apenas RAG-02, RAG-03 e a traceability de EVT-03 (as demais já foram corrigidas em fases anteriores ou estão fora do escopo de WR items desta fase).
Output: REQUIREMENTS.md com checkboxes e traceability corretos para RAG-02, RAG-03 e EVT-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-tech-debt-cleanup/24-CONTEXT.md
@.planning/phases/24-tech-debt-cleanup/24-01-SUMMARY.md
@.planning/phases/24-tech-debt-cleanup/24-02-SUMMARY.md

<!-- Arquivo a modificar -->
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar SC-5 — TypeScript zero-error em packages/core</name>
  <read_first>
    - packages/core/tsconfig.json (verificar path do tsconfig)
  </read_first>
  <files>packages/core/tsconfig.json</files>
  <action>
Esta task é de VERIFICAÇÃO, não de modificação.

Executar o comando TypeScript e capturar o resultado:

```bash
cd /root/Brain && bun tsc --noEmit -p packages/core/tsconfig.json 2>&1
```

Conforme D-05 do CONTEXT.md, `bun tsc --noEmit` em `packages/core` já retorna **0 erros** hoje (verificado em 2026-06-24). Os 4 erros do audit (lastMessageAt, fupNextAt Drizzle types, TokenUsage, responseMode) foram resolvidos incidentalmente em fases anteriores.

Se o output for vazio (sem erros), SC-5 está met — documentar na SUMMARY que `packages/core` compilou limpo.

Se houver erros inesperados (não previstos na auditoria), listar os erros na SUMMARY mas NÃO bloquear a fase — reportar ao orquestrador via SUMMARY.md.

NÃO modificar nenhum arquivo TypeScript nesta task.
  </action>
  <verify>
    <automated>cd /root/Brain && bun tsc --noEmit -p packages/core/tsconfig.json 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `bun tsc --noEmit -p packages/core/tsconfig.json` retorna exit code 0 (sem erros)
    - Output do comando é vazio ou contém apenas warnings (não errors)
    - Resultado documentado na SUMMARY com saída exata do comando
  </acceptance_criteria>
  <done>SC-5 confirmado: packages/core compila sem erros TypeScript</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar REQUIREMENTS.md — RAG-02, RAG-03 e traceability EVT-03</name>
  <read_first>
    - .planning/REQUIREMENTS.md (estado atual completo — ver checkboxes na seção RAG e tabela de traceability)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Realizar **5 mudanças pontuais e cirúrgicas** em REQUIREMENTS.md, conforme D-06 do CONTEXT.md.

**Mudança 1 — RAG-02 checkbox:**
```
ANTES: - [ ] **RAG-02**: LLM pode buscar contexto relevante chamando a tool `search_knowledge(query, collections[])` que retorna trechos ordenados por similaridade
DEPOIS: - [x] **RAG-02**: LLM pode buscar contexto relevante chamando a tool `search_knowledge(query, collections[])` que retorna trechos ordenados por similaridade
```

**Mudança 2 — RAG-03 checkbox:**
```
ANTES: - [ ] **RAG-03**: `search_knowledge` aceita array de coleções e busca em múltiplas coleções simultaneamente numa única chamada
DEPOIS: - [x] **RAG-03**: `search_knowledge` aceita array de coleções e busca em múltiplas coleções simultaneamente numa única chamada
```

**Mudança 3 — Traceability RAG-02 Phase:**
Na tabela de traceability (seção "## Traceability"), linha de RAG-02:
```
ANTES: | RAG-02 | Phase 23 | Pending |
DEPOIS: | RAG-02 | Phase 23 | Complete |
```

**Mudança 4 — Traceability RAG-03 Phase:**
```
ANTES: | RAG-03 | Phase 23 | Pending |
DEPOIS: | RAG-03 | Phase 23 | Complete |
```

**Mudança 5 — Traceability EVT-03 Phase:**
```
ANTES: | EVT-03 | Phase 20 | Complete |
DEPOIS: | EVT-03 | Phase 22 | Complete |
```
(EVT-03 foi implementado em fup-scheduler.ts da Phase 22, não na Phase 20 como estava mapeado)

Não alterar nenhum outro item no arquivo. Não alterar descrições, seções de futuro, seções de out-of-scope, ou o campo "Last updated".

Atualizar a linha `*Last updated*` ao final do arquivo:
```
ANTES: *Last updated: 2026-06-23 — traceability filled after roadmap v1.4 creation*
DEPOIS: *Last updated: 2026-06-24 — Phase 24 cleanup: RAG-02/03 [x], EVT-03 traceability Phase 20→22*
```
  </action>
  <verify>
    <automated>cd /root/Brain && grep -n "RAG-02\|RAG-03\|EVT-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contém `[x] **RAG-02**` (checkbox marcado)
    - REQUIREMENTS.md contém `[x] **RAG-03**` (checkbox marcado)
    - Na tabela de traceability: linha `RAG-02` tem coluna Status = `Complete`
    - Na tabela de traceability: linha `RAG-03` tem coluna Status = `Complete`
    - Na tabela de traceability: linha `EVT-03` tem coluna Phase = `Phase 22` (não `Phase 20`)
    - `grep "RAG-02" .planning/REQUIREMENTS.md` retorna linha com `[x]` E linha de traceability com `Complete`
    - `grep "EVT-03" .planning/REQUIREMENTS.md` retorna linha de traceability com `Phase 22`
  </acceptance_criteria>
  <done>REQUIREMENTS.md atualizado: RAG-02/03 marcados como [x] + Complete; EVT-03 apontando para Phase 22</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md | Documento de planejamento interno — sem dados externos |
| tsc --noEmit | Compilação local — sem I/O externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Tampering | REQUIREMENTS.md edição manual | accept | Arquivo de planejamento interno; mudanças são rastreadas por git; sem impacto de segurança em produção |
| T-24-07 | Information Disclosure | tsc output em logs | accept | Output de compilação TypeScript não contém dados sensíveis; é informação de desenvolvimento |
</threat_model>

<verification>
```bash
# 1. Verificar RAG-02 checkbox atualizado
grep "RAG-02" .planning/REQUIREMENTS.md

# 2. Verificar RAG-03 checkbox atualizado
grep "RAG-03" .planning/REQUIREMENTS.md

# 3. Verificar EVT-03 traceability corrigida
grep "EVT-03" .planning/REQUIREMENTS.md

# 4. SC-5 confirmado
bun tsc --noEmit -p packages/core/tsconfig.json && echo "TypeScript: 0 errors"
```
</verification>

<success_criteria>
- `grep "RAG-02" .planning/REQUIREMENTS.md` mostra `[x]` na definição e `Complete` na traceability
- `grep "RAG-03" .planning/REQUIREMENTS.md` mostra `[x]` na definição e `Complete` na traceability
- `grep "EVT-03" .planning/REQUIREMENTS.md` mostra `Phase 22` na traceability (não Phase 20)
- `bun tsc --noEmit -p packages/core/tsconfig.json` = exit 0
- Phase 24 success criteria 1-6 do ROADMAP.md são todos satisfeitos
</success_criteria>

<output>
After completion, create `.planning/phases/24-tech-debt-cleanup/24-03-SUMMARY.md`
</output>
