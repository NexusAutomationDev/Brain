---
phase: 21-rag
plan: 02
type: tdd
wave: 2
depends_on:
  - 21-01
files_modified:
  - packages/core/src/rag/chunker.ts
  - packages/core/src/rag/search.ts
  - packages/core/src/rag/ingest.ts
  - packages/core/src/rag/index.ts
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04

must_haves:
  truths:
    - "splitText() retorna array de strings com chunks de 1000 chars e overlap de 200 (D-01/D-02)"
    - "searchKnowledge() busca em múltiplas coleções com inArray e filtra por embeddingModel (D-03a, RAG-03)"
    - "POST /api/v1/ingest retorna 401 sem Bearer token válido e 503 sem INGEST_TOKEN configurado (D-13)"
    - "POST /api/v1/ingest realiza DELETE por collection+embeddingModel ANTES do INSERT batch (D-03)"
    - "INSERT batch registra embeddingModel, chunkIndex e totalChunks não-nulos (RAG-04)"
  artifacts:
    - path: "packages/core/src/rag/chunker.ts"
      provides: "splitText(text): Promise<string[]>"
      exports: ["splitText"]
    - path: "packages/core/src/rag/search.ts"
      provides: "searchKnowledge() com cosine similarity multi-coleção + resolveEmbeddingModel()"
      exports: ["searchKnowledge", "resolveEmbeddingModel", "ChunkResult"]
    - path: "packages/core/src/rag/ingest.ts"
      provides: "createIngestApp(sql): Hono — POST /api/v1/ingest"
      exports: ["createIngestApp"]
    - path: "packages/core/src/rag/index.ts"
      provides: "barrel export do módulo rag/"
      exports: ["splitText", "searchKnowledge", "createIngestApp", "resolveEmbeddingModel"]
  key_links:
    - from: "packages/core/src/rag/ingest.ts"
      to: "packages/core/src/rag/chunker.ts"
      via: "import splitText"
      pattern: "splitText"
    - from: "packages/core/src/rag/ingest.ts"
      to: "packages/ai/src/embeddings/factory.ts"
      via: "createEmbeddings() (D-17 já aplicado no Plano 01)"
      pattern: "createEmbeddings"
    - from: "packages/core/src/rag/ingest.ts"
      to: "knowledgeChunks (drizzle schema)"
      via: "db.delete + db.insert"
      pattern: "knowledgeChunks"
    - from: "packages/core/src/rag/search.ts"
      to: "knowledgeChunks (drizzle schema)"
      via: "cosineDistance + inArray"
      pattern: "inArray"
---

<objective>
Implementar os 3 arquivos do núcleo RAG: `chunker.ts` (splitting), `search.ts` (cosine query) e `ingest.ts` (Hono endpoint) — tornando GREEN os testes RED criados no Plano 01.

Purpose: Toda a lógica de RAG fica em `packages/core/src/rag/` conforme D-04, reutilizando padrões já estabelecidos: `RecursiveCharacterTextSplitter` (ou implementação própria seguindo D-02), `cosineDistance` de `drizzle-orm` (padrão de `packages/memory/src/semantic.ts`), auth Bearer em Hono (padrão de `packages/core/src/server.ts`).

Output: 3 arquivos de implementação + barrel `rag/index.ts` com todos os testes do Plano 01 virando GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-rag/21-CONTEXT.md
@.planning/phases/21-rag/21-RESEARCH.md
@.planning/phases/21-rag/21-01-SUMMARY.md

<interfaces>
<!-- Schema knowledgeChunks (packages/database/src/schema/tables.ts) -->
```typescript
export const knowledgeChunks = pgTable('knowledge_chunks', {
  id: uuid('id').primaryKey().defaultRandom(),
  collection: text('collection').notNull(),
  content: text('content').notNull(),
  embedding: vector('embedding', { dimensions: EMBEDDING_DIM }).notNull(),
  embeddingModel: text('embedding_model').notNull(),
  chunkIndex: integer('chunk_index').notNull(),
  totalChunks: integer('total_chunks').notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
});
```

<!-- Padrão de cosine similarity (packages/memory/src/semantic.ts) -->
```typescript
import { cosineDistance, desc, gt, and, sql, inArray, eq } from "drizzle-orm";
// similarity = sql<number>`1 - (${cosineDistance(table.embedding, queryVector)})`
// .where(and(inArray(col, arr), eq(col, val), gt(similarity, threshold)))
// .orderBy(desc(similarity))
// .limit(topK)
```

<!-- Padrão de auth Hono (packages/core/src/server.ts) -->
```typescript
// ADMIN_TOKEN → X-Admin-Token header
// Para ingest: INGEST_TOKEN → Authorization: Bearer <token>
if (!ingestToken) return c.json({ error: "..." }, 503);
const authHeader = c.req.header("Authorization");
const token = authHeader?.replace("Bearer ", "");
if (!token || token !== ingestToken) return c.json({ error: "Unauthorized" }, 401);
```

<!-- createEmbeddings() após Plano 01 (D-17 aplicado) -->
```typescript
// EMBEDDING_MODEL agora é opcional — sem throw
// LLM_PROVIDER=openai/openrouter → text-embedding-3-small
// LLM_PROVIDER=gemini → text-embedding-004
import { createEmbeddings } from "@brain-pkg/ai";
const embedder = await createEmbeddings();
const vectors = await embedder.embedDocuments(chunks);  // batch → number[][]
const queryVector = await embedder.embedQuery(queryText); // single → number[]
```

<!-- resolveEmbeddingModel() — helper de D-14 -->
```typescript
export function resolveEmbeddingModel(): string {
  if (process.env.EMBEDDING_MODEL) return process.env.EMBEDDING_MODEL;
  const provider = process.env.LLM_PROVIDER || "openai";
  const defaults: Record<string, string> = {
    gemini: "text-embedding-004",
    openai: "text-embedding-3-small",
    openrouter: "text-embedding-3-small",
  };
  return defaults[provider] ?? "text-embedding-3-small";
}
```

<!-- @langchain/textsplitters API (D-01/D-02) -->
```typescript
import { RecursiveCharacterTextSplitter } from "@langchain/textsplitters";
// splitText é async (Pitfall 7 do RESEARCH.md)
export async function splitText(text: string): Promise<string[]> {
  const splitter = new RecursiveCharacterTextSplitter({
    chunkSize: 1000,     // D-01
    chunkOverlap: 200,   // D-01
    separators: ["\n\n", "\n", " ", ""],  // D-02
  });
  return splitter.splitText(text);  // await obrigatório
}
```

<!-- ChunkResult type -->
```typescript
export interface ChunkResult {
  id: string;
  content: string;
  collection: string;
  chunkIndex: number;
  totalChunks: number;
  similarity: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar chunker.ts e search.ts — RED→GREEN</name>
  <files>
    packages/core/src/rag/chunker.ts
    packages/core/src/rag/search.ts
  </files>
  <read_first>
    - packages/core/src/rag/__tests__/chunker.test.ts (testes RED a tornar GREEN — criados no Plano 01)
    - packages/core/src/rag/__tests__/search.test.ts (testes RED a tornar GREEN — criados no Plano 01)
    - packages/memory/src/semantic.ts (padrão exato de cosine similarity query com drizzle-orm)
    - .planning/phases/21-rag/21-RESEARCH.md §Pattern 3, Pattern 4 (código de referência)
    - .planning/phases/21-rag/21-RESEARCH.md §Common Pitfalls (Pitfall 2, 3, 7)
  </read_first>
  <behavior>
    chunker.ts — deve tornar GREEN todos os testes de chunker.test.ts:
    - splitText("texto curto") retorna array com 1 elemento (texto < 1000 chars)
    - splitText de texto longo (>1000 chars) retorna múltiplos chunks
    - cada chunk tem tamanho <= 1000 chars (D-01)
    - há overlap de ~200 chars entre chunks consecutivos (D-01/D-02)

    search.ts — deve tornar GREEN todos os testes de search.test.ts:
    - searchKnowledge filtra por collections usando inArray
    - searchKnowledge filtra por embeddingModel (D-03a)
    - searchKnowledge filtra chunks com similarity > 0.5 (D-08)
    - searchKnowledge retorna no máximo 5 resultados (D-07)
    - resolveEmbeddingModel() retorna model correto por provider
  </behavior>
  <action>
    ARQUIVO 1: packages/core/src/rag/chunker.ts

    Tentar usar `@langchain/textsplitters`. Se o pacote não estiver instalado, instalar primeiro:
    ```bash
    cd packages/core && pnpm add @langchain/textsplitters@1.0.1
    ```
    Se `js-tiktoken` (dep de @langchain/textsplitters) causar erro de compatibilidade com Bun, usar implementação própria conforme fallback abaixo.

    Implementação PRIMÁRIA (com @langchain/textsplitters):
    ```typescript
    // RAG-01/D-01/D-02: Chunking recursivo de texto para ingestão na base de conhecimento
    import { RecursiveCharacterTextSplitter } from "@langchain/textsplitters";

    const CHUNK_SIZE = 1000;    // D-01: hardcoded, sem ENV (YAGNI)
    const CHUNK_OVERLAP = 200;  // D-01: hardcoded, sem ENV (YAGNI)

    /**
     * D-02: Split recursivo por parágrafo → linha → espaço → chars.
     * splitText() é async (retorna Promise<string[]>) mesmo sendo operação em memória.
     */
    export async function splitText(text: string): Promise<string[]> {
      const splitter = new RecursiveCharacterTextSplitter({
        chunkSize: CHUNK_SIZE,
        chunkOverlap: CHUNK_OVERLAP,
        separators: ["\n\n", "\n", " ", ""],  // D-02: parágrafo → linha → espaço → chars
      });
      return splitter.splitText(text);
    }
    ```

    Implementação FALLBACK (se @langchain/textsplitters incompatível com Bun):
    ```typescript
    // D-01/D-02: Implementação própria de recursive character text splitter
    // Separators: ["\n\n", "\n", " ", ""] — D-02
    const CHUNK_SIZE = 1000;
    const CHUNK_OVERLAP = 200;

    export async function splitText(text: string): Promise<string[]> {
      if (text.length <= CHUNK_SIZE) return [text];
      const separators = ["\n\n", "\n", " ", ""];
      return recursiveSplit(text, separators);
    }

    function recursiveSplit(text: string, separators: string[]): string[] {
      const [sep, ...rest] = separators;
      if (!sep && text.length <= CHUNK_SIZE) return [text];
      const parts = sep ? text.split(sep) : text.split("");
      const chunks: string[] = [];
      let current = "";

      for (const part of parts) {
        const candidate = current ? current + (sep || "") + part : part;
        if (candidate.length <= CHUNK_SIZE) {
          current = candidate;
        } else {
          if (current) {
            chunks.push(current);
            // overlap: manter os últimos CHUNK_OVERLAP chars como início do próximo
            const overlapStart = Math.max(0, current.length - CHUNK_OVERLAP);
            current = current.slice(overlapStart) + (sep || "") + part;
          } else {
            // part sozinha > CHUNK_SIZE: recursar com separadores menores
            if (rest.length > 0) {
              chunks.push(...recursiveSplit(part, rest));
            } else {
              chunks.push(part.slice(0, CHUNK_SIZE));
            }
            current = "";
          }
        }
      }
      if (current) chunks.push(current);
      return chunks.filter((c) => c.trim().length > 0);
    }
    ```

    ARQUIVO 2: packages/core/src/rag/search.ts

    ```typescript
    // RAG-02/RAG-03/D-03a/D-07/D-08: Cosine similarity search em knowledge_chunks
    // Adaptado de packages/memory/src/semantic.ts — troca userId/embeddings por collection/knowledgeChunks
    import { cosineDistance, desc, gt, and, sql, inArray, eq } from "drizzle-orm";
    import { knowledgeChunks } from "@brain-pkg/database";
    import type { PostgresJsDatabase } from "drizzle-orm/postgres-js";

    /**
     * D-15: Metadados de resultado de chunk para formatação (D-10)
     */
    export interface ChunkResult {
      id: string;
      content: string;
      collection: string;
      chunkIndex: number;
      totalChunks: number;
      similarity: number;
    }

    /**
     * D-14: Resolve o modelo de embedding pelo LLM_PROVIDER quando EMBEDDING_MODEL ausente.
     * Usada tanto em ingest.ts quanto em createSearchKnowledgeTool para garantir consistência.
     */
    export function resolveEmbeddingModel(): string {
      if (process.env.EMBEDDING_MODEL) return process.env.EMBEDDING_MODEL;
      const provider = process.env.LLM_PROVIDER || "openai";
      const defaults: Record<string, string> = {
        gemini: "text-embedding-004",
        openai: "text-embedding-3-small",
        openrouter: "text-embedding-3-small",
      };
      return defaults[provider] ?? "text-embedding-3-small";
    }

    /**
     * RAG-02/RAG-03: Busca cosine similarity em múltiplas coleções.
     *
     * D-03a: Filtra por embeddingModel = modelo atual — chunks de modelos antigos ignorados.
     * D-07: Retorna top 5 global (sem limite por coleção).
     * D-08: Threshold de similaridade cosine: 0.5.
     *
     * ANTI-PATTERN: search.ts recebe queryVector já calculado — não chama embedder aqui.
     * A conversão query→vector é responsabilidade de createSearchKnowledgeTool.
     *
     * @param db - Drizzle database instance
     * @param queryVector - Vetor da query já calculado via embedder.embedQuery()
     * @param collections - Array de coleções (mínimo 1 — validado no schema Zod da tool)
     * @param embeddingModel - Modelo atual resolvido via resolveEmbeddingModel()
     * @param topK - Máximo de resultados (default: 5 — D-07)
     * @param threshold - Mínimo de similaridade (default: 0.5 — D-08)
     */
    export async function searchKnowledge(
      db: PostgresJsDatabase,
      queryVector: number[],
      collections: string[],
      embeddingModel: string,
      topK = 5,
      threshold = 0.5
    ): Promise<ChunkResult[]> {
      // Pitfall 3: guard collections vazias antes do inArray
      if (collections.length === 0) return [];

      const similarity = sql<number>`1 - (${cosineDistance(knowledgeChunks.embedding, queryVector)})`;

      return db
        .select({
          id: knowledgeChunks.id,
          content: knowledgeChunks.content,
          collection: knowledgeChunks.collection,
          chunkIndex: knowledgeChunks.chunkIndex,
          totalChunks: knowledgeChunks.totalChunks,
          similarity,
        })
        .from(knowledgeChunks)
        .where(
          and(
            inArray(knowledgeChunks.collection, collections),  // RAG-03: multi-coleção
            eq(knowledgeChunks.embeddingModel, embeddingModel), // D-03a: filtrar modelo atual
            gt(similarity, threshold)                           // D-08: threshold 0.5
          )
        )
        .orderBy(desc(similarity))  // RAG-03: ordenar por score global
        .limit(topK);               // D-07: top 5 global
    }
    ```
  </action>
  <verify>
    <automated>bun test packages/core/src/rag/__tests__/chunker.test.ts packages/core/src/rag/__tests__/search.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - `bun test packages/core/src/rag/__tests__/chunker.test.ts` passa com 0 falhas (GREEN)
    - `bun test packages/core/src/rag/__tests__/search.test.ts` passa com 0 falhas (GREEN)
    - `grep -n "resolveEmbeddingModel" packages/core/src/rag/search.ts` retorna linha de export
    - `grep -n "inArray" packages/core/src/rag/search.ts` retorna linha no WHERE clause
    - `grep -n "topK = 5" packages/core/src/rag/search.ts` retorna linha (D-07)
    - `grep -n "threshold = 0.5" packages/core/src/rag/search.ts` retorna linha (D-08)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar ingest.ts + barrel rag/index.ts — RED→GREEN</name>
  <files>
    packages/core/src/rag/ingest.ts
    packages/core/src/rag/index.ts
  </files>
  <read_first>
    - packages/core/src/rag/__tests__/ingest.test.ts (testes RED a tornar GREEN — criados no Plano 01)
    - packages/core/src/server.ts (padrão EXATO de auth Hono + estrutura do handler — adaptar para Bearer)
    - packages/core/src/rag/chunker.ts (import de splitText — criado na Task 1)
    - packages/core/src/rag/search.ts (import de resolveEmbeddingModel — criado na Task 1)
    - .planning/phases/21-rag/21-RESEARCH.md §Pattern 1, Pattern 5 (fluxo de ingest completo)
    - .planning/phases/21-rag/21-RESEARCH.md §Common Pitfalls (Pitfall 4, 5, 6, 7)
  </read_first>
  <behavior>
    ingest.ts — deve tornar GREEN todos os testes de ingest.test.ts:
    - POST /api/v1/ingest retorna 401 sem header Authorization
    - POST /api/v1/ingest retorna 401 com Bearer token errado
    - POST /api/v1/ingest retorna 503 quando INGEST_TOKEN não configurado (fail-closed)
    - POST /api/v1/ingest retorna 400 quando body não tem "text"
    - POST /api/v1/ingest retorna 400 quando body não tem "collection"
    - POST /api/v1/ingest retorna 200 com { status: "ok", chunks: N } em happy path
    - DELETE é chamado com filtro de collection AND embeddingModel (D-03)
    - INSERT batch registra embeddingModel, chunkIndex e totalChunks não-nulos (RAG-04)
  </behavior>
  <action>
    ARQUIVO 1: packages/core/src/rag/ingest.ts

    ```typescript
    // RAG-01: POST /api/v1/ingest — ingestão de texto na base de conhecimento
    // D-05: createIngestApp(sql) exportada de @brain-pkg/core e montada explicitamente no server.ts de cada Brain
    // D-13: Authorization: Bearer <INGEST_TOKEN> — fail-closed (503) se INGEST_TOKEN não configurado
    // D-03: Re-ingestão = DELETE WHERE collection AND embedding_model = current + INSERT batch
    // RAG-04: INSERT registra embeddingModel, chunkIndex, totalChunks como metadados não-nulos
    // Security T-21-02-02: limite de payload 1MB para prevenir DoS de memória/CPU no chunking
    import { Hono } from "hono";
    import { drizzle } from "drizzle-orm/postgres-js";
    import { and, eq } from "drizzle-orm";
    import { knowledgeChunks } from "@brain-pkg/database";
    import { createEmbeddings } from "@brain-pkg/ai";
    import { createLogger } from "@brain-pkg/observability";
    import type { Sql } from "postgres";
    import { splitText } from "./chunker.js";
    import { resolveEmbeddingModel } from "./search.js";

    const logger = createLogger();
    const MAX_TEXT_BYTES = 1_000_000; // D: 1MB max — DoS mitigation (T-21-02-02)

    /**
     * D-05: Cria sub-app Hono para ingestão de texto na base de conhecimento RAG.
     *
     * Segurança: Bearer token via INGEST_TOKEN ENV. Fail-closed (503) se não configurado.
     * Sem dependência em BrainRunner — recebe sql direto (como createHealthApp).
     *
     * @param sql - postgres.js Sql instance do tenant
     */
    export function createIngestApp(sql: Sql): Hono {
      const app = new Hono();

      app.post("/api/v1/ingest", async (c) => {
        const ingestToken = process.env.INGEST_TOKEN;

        // D-13: Fail-closed — INGEST_TOKEN não configurado → 503
        if (!ingestToken) {
          logger.warn({}, "/api/v1/ingest called but INGEST_TOKEN env var is not set");
          return c.json({ error: "Service unavailable — ingest endpoint not configured" }, 503);
        }

        // D-13: Bearer token — "Authorization: Bearer <token>"
        const authHeader = c.req.header("Authorization");
        const token = authHeader?.replace("Bearer ", "");
        if (!token || token !== ingestToken) {
          logger.warn({}, "/api/v1/ingest unauthorized attempt");
          return c.json({ error: "Unauthorized" }, 401);
        }

        // Body parsing — catch JSON inválido
        const body = await c.req.json().catch(() => null);
        if (!body || typeof body.text !== "string" || !body.text.trim()) {
          return c.json({ error: "Bad Request — field 'text' is required and must be a non-empty string" }, 400);
        }
        if (typeof body.collection !== "string" || !body.collection.trim()) {
          return c.json({ error: "Bad Request — field 'collection' is required and must be a non-empty string" }, 400);
        }

        // T-21-02-02: Limite de tamanho de payload
        const textBytes = Buffer.byteLength(body.text, "utf8");
        if (textBytes > MAX_TEXT_BYTES) {
          return c.json({ error: "Payload Too Large — text exceeds 1MB limit" }, 413);
        }

        const { text, collection } = body as { text: string; collection: string };

        // D-14/D-15: Resolver modelo de embedding pelo provider
        const embeddingModel = resolveEmbeddingModel();

        // D-02: Chunk recursivo do texto
        const chunks = await splitText(text);

        // Gerar embeddings em batch (Pitfall 1: usar embedDocuments para batch)
        const embedder = await createEmbeddings();
        const vectors = await embedder.embedDocuments(chunks);

        const db = drizzle(sql);

        // D-03: DELETE chunks do modelo atual nessa coleção (preserva chunks de outros modelos)
        await db
          .delete(knowledgeChunks)
          .where(
            and(
              eq(knowledgeChunks.collection, collection),
              eq(knowledgeChunks.embeddingModel, embeddingModel)
            )
          );

        // RAG-04: INSERT batch com metadados obrigatórios não-nulos
        const rows = chunks.map((content, i) => ({
          collection,
          content,
          embedding: vectors[i],
          embeddingModel,          // D-15: modelo gravado em cada chunk
          chunkIndex: i,           // RAG-04: índice do chunk (0-based)
          totalChunks: chunks.length, // RAG-04: total de chunks do documento
          updatedAt: new Date(),
        }));
        await db.insert(knowledgeChunks).values(rows);

        logger.info({ collection, chunks: rows.length, embeddingModel }, "RAG ingest complete");
        return c.json({ status: "ok", chunks: rows.length });
      });

      return app;
    }
    ```

    ARQUIVO 2: packages/core/src/rag/index.ts (barrel export do módulo rag/)

    ```typescript
    // RAG barrel export — exports públicos do módulo packages/core/src/rag/
    export { splitText } from "./chunker.js";
    export { searchKnowledge, resolveEmbeddingModel } from "./search.js";
    export type { ChunkResult } from "./search.js";
    export { createIngestApp } from "./ingest.js";
    ```
  </action>
  <verify>
    <automated>bun test packages/core/src/rag/__tests__/ingest.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    - `bun test packages/core/src/rag/__tests__/ingest.test.ts` passa com 0 falhas (GREEN)
    - `grep -n "status.*ok.*chunks" packages/core/src/rag/ingest.ts` retorna linha do return 200
    - `grep -n "503" packages/core/src/rag/ingest.ts` retorna linha do fail-closed (D-13)
    - `grep -n "DELETE" packages/core/src/rag/ingest.ts` OU `grep -n "\.delete(" packages/core/src/rag/ingest.ts` retorna linha do delete antes do insert
    - `grep -n "embeddingModel" packages/core/src/rag/ingest.ts` retorna pelo menos 2 linhas (DELETE filter + INSERT field)
    - `grep -n "chunkIndex" packages/core/src/rag/ingest.ts` retorna linha no rows.map (RAG-04)
    - `grep -n "totalChunks" packages/core/src/rag/ingest.ts` retorna linha no rows.map (RAG-04)
    - Suite completa do rag/: `bun test packages/core/src/rag/__tests__/` — todos GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /api/v1/ingest | Operador externo envia texto e nome de coleção não sanitizados |
| INGEST_TOKEN ENV → handler | Segredo de autenticação lido do ambiente — nunca logado |
| body.text → chunker → embedder | Texto do operador processado em memória antes de ir ao banco |
| collections[] → drizzle inArray | Array de strings fornecido pelo LLM — filtrado via Zod na tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Elevation of Privilege | POST /api/v1/ingest sem autenticação | mitigate | Bearer token fail-closed: 503 se INGEST_TOKEN ausente, 401 se token inválido (D-13) |
| T-21-02-02 | Denial of Service | body.text gigante → chunking exaurem memória/CPU | mitigate | Limite de 1MB em MAX_TEXT_BYTES; retorna 413 se excedido |
| T-21-02-03 | Tampering | `collection` com SQL injection | accept | Drizzle ORM parametriza `eq(col, val)` — sem SQL manual; inArray também parametrizado |
| T-21-02-04 | Information Disclosure | INGEST_TOKEN logado em mensagens de erro | mitigate | Log apenas presença/ausência (warn "not set"); nunca logar o valor do token |
| T-21-02-05 | Tampering | D-03 delete sem filtro de embeddingModel apaga chunks de outros modelos | mitigate | DELETE usa `and(eq(collection, ?), eq(embeddingModel, ?))` — ambos os filtros obrigatórios |
| T-21-02-06 | Denial of Service | collections: [] chega na searchKnowledge → inArray vazio | mitigate | Guard `if (collections.length === 0) return []` em search.ts antes do query |
</threat_model>

<verification>
Verificação completa do plano 02:

```bash
# Todos os testes RAG core GREEN
bun test packages/core/src/rag/__tests__/

# Verificar exports do barrel
grep -n "export" packages/core/src/rag/index.ts

# Verificar padrões obrigatórios
grep -n "D-03\|\.delete(" packages/core/src/rag/ingest.ts
grep -n "inArray" packages/core/src/rag/search.ts
grep -n "resolveEmbeddingModel" packages/core/src/rag/search.ts

# Sem regressão na suite core
bun test packages/core/src/__tests__/
```
</verification>

<success_criteria>
- `bun test packages/core/src/rag/__tests__/` — todos os testes GREEN (ingest, chunker, search)
- `packages/core/src/rag/` contém: `chunker.ts`, `search.ts`, `ingest.ts`, `index.ts`
- `ingest.ts` tem auth fail-closed (503 sem INGEST_TOKEN), 401 sem bearer válido, DELETE antes de INSERT
- `search.ts` exporta `searchKnowledge`, `resolveEmbeddingModel` e interface `ChunkResult`
- `bun test packages/core/src/__tests__/` ainda passa sem regressões
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-rag/21-02-SUMMARY.md`
</output>
