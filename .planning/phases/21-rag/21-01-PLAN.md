---
phase: 21-rag
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/ai/src/embeddings/factory.ts
  - packages/ai/src/embeddings/__tests__/factory.test.ts
  - packages/core/src/rag/__tests__/ingest.test.ts
  - packages/core/src/rag/__tests__/chunker.test.ts
  - packages/core/src/rag/__tests__/search.test.ts
  - packages/core/src/tools/__tests__/search-knowledge.test.ts
autonomous: true
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04

must_haves:
  truths:
    - "createEmbeddings() NÃO lança ConfigurationError quando EMBEDDING_MODEL está ausente"
    - "createEmbeddings() com LLM_PROVIDER=openai retorna embedder usando text-embedding-3-small"
    - "createEmbeddings() com LLM_PROVIDER=gemini retorna embedder usando text-embedding-004"
    - "Todos os 5 arquivos de test stub existem e executam (RED ou GREEN)"
  artifacts:
    - path: "packages/ai/src/embeddings/factory.ts"
      provides: "createEmbeddings() com defaults por provider (D-14/D-17)"
      contains: "DEFAULT_MODELS"
    - path: "packages/ai/src/embeddings/__tests__/factory.test.ts"
      provides: "Testes de default model por provider"
      contains: "default por provider"
    - path: "packages/core/src/rag/__tests__/ingest.test.ts"
      provides: "Stubs RAG-01, RAG-04"
    - path: "packages/core/src/rag/__tests__/chunker.test.ts"
      provides: "Stubs D-02"
    - path: "packages/core/src/rag/__tests__/search.test.ts"
      provides: "Stubs RAG-02, RAG-03"
    - path: "packages/core/src/tools/__tests__/search-knowledge.test.ts"
      provides: "Stubs RAG-02, RAG-03, D-11"
  key_links:
    - from: "packages/ai/src/embeddings/factory.ts"
      to: "process.env.LLM_PROVIDER"
      via: "DEFAULT_MODELS lookup"
      pattern: "DEFAULT_MODELS"
    - from: "packages/ai/src/embeddings/__tests__/factory.test.ts"
      to: "packages/ai/src/embeddings/factory.ts"
      via: "import after mock.module"
      pattern: "default por provider"
---

<objective>
Preparar a base de testes (Wave 0 / Nyquist) e modificar `createEmbeddings()` para suportar defaults de modelo por provider — pré-requisito para todos os arquivos RAG.

Purpose: D-17 remove o `throw` quando `EMBEDDING_MODEL` é ausente e adiciona defaults por provider (`text-embedding-3-small` para openai/openrouter, `text-embedding-004` para gemini). Sem essa mudança, ingest e search falham para operadores que não setaram `EMBEDDING_MODEL`. Os 5 arquivos de test stub garantem que nenhuma tarefa posterior escreva código sem cobertura de teste.

Output: `factory.ts` modificado + 5 arquivos de test stub com casos RED prontos para virar GREEN nos planos 02 e 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-rag/21-CONTEXT.md
@.planning/phases/21-rag/21-RESEARCH.md
@.planning/phases/21-rag/21-VALIDATION.md

<interfaces>
<!-- Código atual a modificar — packages/ai/src/embeddings/factory.ts -->
```typescript
// ESTADO ATUAL (linhas 16-44):
export async function createEmbeddings(): Promise<Embeddings> {
  const model = process.env.EMBEDDING_MODEL;
  const dimensions = process.env.EMBEDDING_DIMENSIONS
    ? parseInt(process.env.EMBEDDING_DIMENSIONS, 10)
    : undefined;
  const apiKey = process.env.API_KEY;

  if (!model) {
    throw new ConfigurationError("EMBEDDING_MODEL env var is required", { model: "missing" });
  }
  // ... resto do switch
}
```

<!-- Código alvo após D-17 -->
```typescript
// ESTADO ALVO:
const DEFAULT_MODELS: Record<string, string> = {
  openai: "text-embedding-3-small",
  openrouter: "text-embedding-3-small",
  gemini: "text-embedding-004",
};

export async function createEmbeddings(): Promise<Embeddings> {
  const provider = process.env.LLM_PROVIDER || "openai";
  const model = process.env.EMBEDDING_MODEL ?? DEFAULT_MODELS[provider] ?? "text-embedding-3-small";
  const dimensions = process.env.EMBEDDING_DIMENSIONS
    ? parseInt(process.env.EMBEDDING_DIMENSIONS, 10)
    : undefined;
  const apiKey = process.env.API_KEY;
  // SEM throw — model sempre resolvido
  // ... switch(provider) inalterado
}
```

<!-- Teste existente a PRESERVAR (factory.test.ts linha 37-43): -->
```typescript
it("throws ConfigurationError when EMBEDDING_MODEL is not set", ...);
it("throws with message 'EMBEDDING_MODEL env var is required'", ...);
```
// ATENÇÃO: esses 2 testes devem ser REMOVIDOS/SUBSTITUÍDOS — após D-17 o throw não existe mais.
// Substituir por testes de default por provider.

<!-- Padrão de mock para test stubs (de pause-session.test.ts e server.test.ts): -->
```typescript
import { describe, it, expect, mock, beforeEach } from "bun:test";
mock.module("drizzle-orm/postgres-js", () => ({ drizzle: mock(() => mockDb) }));
// Import AFTER mocks
import { createXxx } from "../xxx.js";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar arquivos de test stub (Wave 0 — RED)</name>
  <files>
    packages/core/src/rag/__tests__/ingest.test.ts
    packages/core/src/rag/__tests__/chunker.test.ts
    packages/core/src/rag/__tests__/search.test.ts
    packages/core/src/tools/__tests__/search-knowledge.test.ts
  </files>
  <read_first>
    - packages/core/src/__tests__/server.test.ts (padrão de mock.module + app.fetch para ingest)
    - packages/core/src/tools/__tests__/pause-session.test.ts (padrão de mock drizzle + tool.invoke)
    - packages/memory/src/semantic.ts (assinatura de searchSimilar — adaptar para searchKnowledge)
    - .planning/phases/21-rag/21-VALIDATION.md (lista exata dos stubs e requisitos cobertos)
    - .planning/phases/21-rag/21-CONTEXT.md (D-10, D-11: formato de retorno esperado)
  </read_first>
  <behavior>
    ingest.test.ts — casos RED (importa de módulo que ainda não existe):
    - RAG-01: POST /api/v1/ingest retorna 401 sem token Bearer
    - RAG-01: POST /api/v1/ingest retorna 401 com token Bearer errado
    - RAG-01: POST /api/v1/ingest retorna 503 quando INGEST_TOKEN não configurado
    - RAG-01: POST /api/v1/ingest retorna 400 quando body não tem "text"
    - RAG-01: POST /api/v1/ingest retorna 400 quando body não tem "collection"
    - RAG-01: POST /api/v1/ingest retorna 200 com { status: "ok", chunks: N } em happy path (mock db)
    - RAG-04: INSERT batch registra embeddingModel, chunkIndex e totalChunks não-nulos (mock db)
    - D-03: DELETE filtra por collection AND embeddingModel (não apaga chunks de outros modelos)

    chunker.test.ts — casos RED:
    - D-01/D-02: splitText("texto curto") retorna array com 1 elemento
    - D-01/D-02: splitText de texto longo (>1000 chars) retorna múltiplos chunks
    - D-02: cada chunk tem tamanho <= 1000 chars
    - D-02: há overlap entre chunks consecutivos (overlap de 200 chars)

    search.test.ts — casos RED:
    - RAG-02/RAG-03: searchKnowledge filtra por collections usando inArray (mock db)
    - D-03a: searchKnowledge filtra por embeddingModel (mock db)
    - D-08: searchKnowledge filtra chunks com similarity > 0.5 (threshold)
    - D-07: searchKnowledge retorna no máximo 5 resultados

    search-knowledge.test.ts — casos RED:
    - RAG-02: tool retorna string formatada "[Coleção: X] chunk N/M\n..." quando há resultados (mock db)
    - D-11: tool retorna "Nenhum resultado encontrado para a consulta nas coleções informadas." quando array vazio
    - RAG-03: tool aceita collections array com múltiplos elementos
    - Zod: tool rejeita collections: [] (min(1))
    - tool.name === "search_knowledge"
  </behavior>
  <action>
    Criar os 4 arquivos de test stub seguindo EXATAMENTE o padrão de mock.module de pause-session.test.ts e server.test.ts.

    ESTRUTURA OBRIGATÓRIA de cada stub:
    1. mock.module() ANTES de qualquer import do módulo a testar
    2. Import do módulo a testar DEPOIS dos mocks
    3. describe() com it() cases que representam os comportamentos listados acima
    4. Cada it() deve ter EXPECT statements que definem o comportamento esperado
    5. Os testes DEVEM FALHAR (RED) porque os módulos importados ainda não existem

    Para ingest.test.ts:
    - Mock: `drizzle-orm/postgres-js` (drizzle), `@brain-pkg/database` (knowledgeChunks), `drizzle-orm` (and, eq, inArray, cosineDistance, desc, gt, sql)
    - Mock db com cadeia: `{ delete: () => ({ where: mockWhere }), insert: () => ({ values: mockValues }) }`
    - Mock embedder: módulo inline `{ embedDocuments: mock(async (chunks) => chunks.map(() => [0.1, 0.2])) }`
    - Mock `@brain-pkg/ai`: `{ createEmbeddings: mock(async () => mockEmbedder) }`
    - Import: `import { createIngestApp } from "../../rag/ingest.js"`
    - Usar `app.fetch(new Request("http://localhost/api/v1/ingest", { method: "POST", headers: {...}, body: JSON.stringify({...}) }))` para testes de endpoint
    - Setar/dessetar `process.env.INGEST_TOKEN` via beforeEach/afterEach

    Para chunker.test.ts:
    - Sem mocks externos necessários (função pura)
    - Import: `import { splitText } from "../../rag/chunker.js"`
    - Criar helper: `const longText = "palavra ".repeat(200)` (~1400 chars) para testar múltiplos chunks

    Para search.test.ts:
    - Mock: drizzle com `{ select: () => ({ from: () => ({ where: () => ({ orderBy: () => ({ limit: mockLimit }) }) }) }) }`
    - Import: `import { searchKnowledge } from "../../rag/search.js"`
    - Verificar que o mock db foi chamado com os argumentos corretos (coleta do call args do mock)

    Para search-knowledge.test.ts:
    - Mock: `drizzle-orm/postgres-js`, `@brain-pkg/database`, `drizzle-orm`
    - Mock searchKnowledge inline como módulo: `mock.module("../../rag/search.js", () => ({ searchKnowledge: mockSearchKnowledge }))`
    - Import: `import { createSearchKnowledgeTool } from "../../tools/search-knowledge.js"`
    - Testar `tool.invoke({ query: "...", collections: ["faq"] })` e verificar string retornada

    DIRETÓRIO: criar `packages/core/src/rag/__tests__/` se não existir (os arquivos vão nesse diretório, não em `packages/core/src/__tests__/`).
  </action>
  <verify>
    <automated>bun test packages/core/src/rag/__tests__/ packages/core/src/tools/__tests__/search-knowledge.test.ts 2>&1 | grep -E "(FAIL|error: Cannot find module|ingest|chunker|search)" | head -30</automated>
  </verify>
  <done>
    - 4 arquivos existem nos caminhos especificados
    - `bun test packages/core/src/rag/__tests__/` reporta erros "Cannot find module" ou falhas nos imports — RED confirmado
    - Cada arquivo tem pelo menos 4 it() cases
    - Nenhum arquivo está vazio
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atualizar createEmbeddings() com defaults por provider (D-17) — RED→GREEN</name>
  <files>
    packages/ai/src/embeddings/factory.ts
    packages/ai/src/embeddings/__tests__/factory.test.ts
  </files>
  <read_first>
    - packages/ai/src/embeddings/factory.ts (código atual com throw na linha 24)
    - packages/ai/src/embeddings/factory.test.ts (testes existentes — 2 testes com throw devem ser substituídos)
    - .planning/phases/21-rag/21-CONTEXT.md §D-14, D-15, D-16, D-17 (spec exata da mudança)
    - .planning/phases/21-rag/21-RESEARCH.md §Pattern 6 (código alvo documentado)
  </read_first>
  <behavior>
    factory.test.ts — novos casos a adicionar (substituindo os 2 testes de throw):
    - D-17/D-14: createEmbeddings() sem EMBEDDING_MODEL e LLM_PROVIDER=openai → NÃO lança ConfigurationError
    - D-14: createEmbeddings() com LLM_PROVIDER=openai resolve modelo "text-embedding-3-small" (verificar via config do mock)
    - D-14: createEmbeddings() com LLM_PROVIDER=openrouter resolve modelo "text-embedding-3-small"
    - D-14: createEmbeddings() com LLM_PROVIDER=gemini resolve modelo "text-embedding-004"
    - D-14: createEmbeddings() sem LLM_PROVIDER usa default "openai" → resolve "text-embedding-3-small"
    - Preservar testes existentes: EMBEDDING_DIMENSIONS, API_KEY handling, FakeEmbeddings
  </behavior>
  <action>
    PASSO 1 — Modificar packages/ai/src/embeddings/factory.ts:

    Substituir o bloco `if (!model) { throw ... }` e a leitura de `model` pelo seguinte:

    ```typescript
    // D-17/D-14: Defaults de modelo por provider — EMBEDDING_MODEL é opcional
    const DEFAULT_MODELS: Record<string, string> = {
      openai: "text-embedding-3-small",
      openrouter: "text-embedding-3-small",
      gemini: "text-embedding-004",
    };

    export async function createEmbeddings(): Promise<Embeddings> {
      const provider = process.env.LLM_PROVIDER || "openai";
      // D-14: EMBEDDING_MODEL opcional — resolve default por provider se ausente
      const model = process.env.EMBEDDING_MODEL ?? DEFAULT_MODELS[provider] ?? "text-embedding-3-small";
      const dimensions = process.env.EMBEDDING_DIMENSIONS
        ? parseInt(process.env.EMBEDDING_DIMENSIONS, 10)
        : undefined;
      // T-2-03: API_KEY never logged
      const apiKey = process.env.API_KEY;

      // Remover completamente o bloco: if (!model) { throw new ConfigurationError(...) }

      switch (provider) {
        case "gemini": {
          const { GoogleGenerativeAIEmbeddings } = await import("@langchain/google-genai");
          return new GoogleGenerativeAIEmbeddings({ model, apiKey });
        }
        default: {
          const { OpenAIEmbeddings } = await import("@langchain/openai");
          return new OpenAIEmbeddings({ model, openAIApiKey: apiKey, dimensions });
        }
      }
    }
    ```

    Manter o import de `Embeddings` de `@langchain/core/embeddings`.
    REMOVER o import de `ConfigurationError` de `@brain-pkg/shared` se não for mais usado em nenhum outro lugar do arquivo.
    Manter o comentário de jsdoc atualizado: remover menção a `EMBEDDING_MODEL` como required, adicionar que é opcional com defaults por provider.

    PASSO 2 — Atualizar packages/ai/src/embeddings/__tests__/factory.test.ts:

    REMOVER os 2 testes que verificam o throw de ConfigurationError:
    - `it("throws ConfigurationError when EMBEDDING_MODEL is not set", ...)`
    - `it("throws with message 'EMBEDDING_MODEL env var is required'", ...)`

    ADICIONAR novos testes de default por provider (dentro do mesmo describe block):

    ```typescript
    describe("D-14/D-17: defaults de modelo por provider", () => {
      beforeEach(() => {
        delete process.env.EMBEDDING_MODEL;
        delete process.env.LLM_PROVIDER;
        process.env.API_KEY = "test-key";
      });

      it("não lança quando EMBEDDING_MODEL está ausente (D-17)", async () => {
        process.env.LLM_PROVIDER = "openai";
        await expect(createEmbeddings()).resolves.toBeDefined();
      });

      it("resolve text-embedding-3-small para LLM_PROVIDER=openai (D-14)", async () => {
        process.env.LLM_PROVIDER = "openai";
        const embedder = await createEmbeddings() as any;
        expect(embedder.config?.model ?? embedder.model).toBe("text-embedding-3-small");
      });

      it("resolve text-embedding-3-small para LLM_PROVIDER=openrouter (D-14)", async () => {
        process.env.LLM_PROVIDER = "openrouter";
        const embedder = await createEmbeddings() as any;
        expect(embedder.config?.model ?? embedder.model).toBe("text-embedding-3-small");
      });

      it("resolve text-embedding-004 para LLM_PROVIDER=gemini (D-14)", async () => {
        process.env.LLM_PROVIDER = "gemini";
        const embedder = await createEmbeddings() as any;
        expect(embedder.config?.model ?? embedder.model).toBe("text-embedding-004");
      });

      it("usa openai como provider padrão quando LLM_PROVIDER ausente (D-14)", async () => {
        // LLM_PROVIDER não setado
        await expect(createEmbeddings()).resolves.toBeDefined();
      });

      it("EMBEDDING_MODEL explícito sobrescreve o default (D-14)", async () => {
        process.env.LLM_PROVIDER = "openai";
        process.env.EMBEDDING_MODEL = "text-embedding-ada-002";
        const embedder = await createEmbeddings() as any;
        expect(embedder.config?.model ?? embedder.model).toBe("text-embedding-ada-002");
      });
    });
    ```

    ATENÇÃO: Os mocks de `@langchain/openai` e `@langchain/google-genai` no início do arquivo expõem `config` no constructor — verificar que o mock armazena `model` como `this.config.model` para que os testes de default funcionem.
    O mock existente de `OpenAIEmbeddings` recebe `{ model, openAIApiKey, dimensions }` no constructor e armazena em `this.config`. Usar `embedder.config.model` para verificar.
    Para GoogleGenerativeAIEmbeddings, o mock recebe `{ model, apiKey }` — verificar `embedder.config.model`.
  </action>
  <verify>
    <automated>bun test packages/ai/src/embeddings/__tests__/factory.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `bun test packages/ai/src/embeddings/__tests__/factory.test.ts` passa com 0 falhas
    - Grep: `grep -n "DEFAULT_MODELS" packages/ai/src/embeddings/factory.ts` retorna linha com `DEFAULT_MODELS`
    - Grep: `grep -n "throw new ConfigurationError" packages/ai/src/embeddings/factory.ts` retorna vazio (throw removido)
    - Grep: `grep -n "text-embedding-3-small" packages/ai/src/embeddings/factory.ts` retorna linha em DEFAULT_MODELS
    - Grep: `grep -n "text-embedding-004" packages/ai/src/embeddings/factory.ts` retorna linha em DEFAULT_MODELS
    - Suite core não regride: `bun test packages/core/src/__tests__/server.test.ts` passa
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV vars → factory.ts | `EMBEDDING_MODEL` e `LLM_PROVIDER` controlam qual modelo é usado — lidos em runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | factory.ts — API_KEY never logged | mitigate | Manter comentário `// T-2-03: API_KEY never logged`; não alterar o comportamento de logging existente ao modificar factory.ts |
| T-21-01-02 | Elevation of Privilege | DEFAULT_MODELS hardcoded | accept | Provider defaults são não-secretos e documentados; operador pode sobrescrever via EMBEDDING_MODEL se necessário |
</threat_model>

<verification>
Verificação completa do plano 01:

```bash
# Stubs existem
ls packages/core/src/rag/__tests__/ingest.test.ts
ls packages/core/src/rag/__tests__/chunker.test.ts
ls packages/core/src/rag/__tests__/search.test.ts
ls packages/core/src/tools/__tests__/search-knowledge.test.ts

# D-17: throw removido, defaults presentes
grep -n "DEFAULT_MODELS" packages/ai/src/embeddings/factory.ts
grep -c "throw new ConfigurationError" packages/ai/src/embeddings/factory.ts
# deve retornar 0

# Testes factory passando
bun test packages/ai/src/embeddings/__tests__/factory.test.ts

# Regressão core: 57 testes ainda passam
bun test packages/core/src/__tests__/
```
</verification>

<success_criteria>
- `packages/ai/src/embeddings/factory.ts` contém `DEFAULT_MODELS` e não contém `throw new ConfigurationError`
- `bun test packages/ai/src/embeddings/__tests__/factory.test.ts` passa com zero falhas
- 4 arquivos de test stub existem com casos RED representando RAG-01, RAG-02, RAG-03, RAG-04, D-02, D-11
- `bun test packages/core/src/__tests__/` ainda passa (sem regressão)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-rag/21-01-SUMMARY.md`
</output>
