---
phase: 15-mcp-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified: []
autonomous: false
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
  - MCP-05

must_haves:
  truths:
    - "Brain SDR inicia com MCP_URL configurado e lista as MCP tools carregadas no log"
    - "Brain SDR com MCP server inacessível sobe normalmente com log de warn"
    - "SIGTERM encerra processo Bun sem hang em menos de 3 segundos"
    - "Todos os testes unitários da phase 15 estão verdes"
  artifacts:
    - path: "packages/core/src/__tests__/unit/mcp-init.test.ts"
      provides: "Testes MCP lifecycle verdes"
    - path: "packages/core/src/__tests__/unit/mcp-tool-error.test.ts"
      provides: "Teste handleToolErrors verde"
  key_links:
    - from: "Suite completa"
      to: "0 falhas"
      via: "bun test"
      pattern: "0 failed"
---

<objective>
Verificar que a integração MCP está completa e funcional: suite de testes verdes, checkpoint de verificação manual do SIGTERM.

Purpose: Confirma que a implementação das phases 15-01 e 15-02 entregou os 5 critérios de sucesso do roadmap (MCP-01 a MCP-05) e que o SIGTERM encerra o processo corretamente sem hang.
Output: Sinal de aprovação para avançar para Phase 16.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-mcp-integration/15-CONTEXT.md
@.planning/phases/15-mcp-integration/15-01-SUMMARY.md
@.planning/phases/15-mcp-integration/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar suite completa e verificar cobertura dos requisitos</name>
  <read_first>
    - .planning/phases/15-mcp-integration/15-01-SUMMARY.md (confirmar que 15-01 foi concluído)
    - .planning/phases/15-mcp-integration/15-02-SUMMARY.md (confirmar que 15-02 foi concluído)
    - .planning/phases/15-mcp-integration/15-VALIDATION.md (ver checklist de validação)
  </read_first>
  <files></files>
  <action>
    Executar a suite de testes completa e verificar os critérios de sucesso da fase:

    ```bash
    # Suite completa do packages/core
    bun test packages/core/src/__tests__/

    # Suite completa do brain-sdr
    bun test apps/brain-sdr/src/__tests__/

    # Verificações por grep dos critérios estruturais
    echo "=== MCP-01: mcpTools no BrainBuildContext ==="
    grep -n "mcpTools: StructuredTool\[\]" packages/core/src/brain/interface.ts

    echo "=== MCP-02: brain-sdr espalha mcpTools ==="
    grep -n "ctx\.mcpTools" apps/brain-sdr/src/brain.ts

    echo "=== MCP-02: brain-echo tem ReAct com mcpTools ==="
    grep -n "ctx\.mcpTools" apps/brain-echo/src/brain.ts

    echo "=== MCP-03: onConnectionError ignore ==="
    grep -n "onConnectionError" packages/core/src/runner/runner.ts

    echo "=== MCP-04: handleToolErrors em brain-sdr ==="
    grep -n "handleToolErrors" apps/brain-sdr/src/brain.ts

    echo "=== MCP-04: handleToolErrors em brain-echo ==="
    grep -n "handleToolErrors" apps/brain-echo/src/brain.ts

    echo "=== MCP-05: close() em runner.ts ==="
    grep -n "async close" packages/core/src/runner/runner.ts

    echo "=== MCP-05: SIGTERM em runner.ts ==="
    grep -n "SIGTERM" packages/core/src/runner/runner.ts

    echo "=== D-14 CORREÇÃO: ausência de streamable_http ==="
    grep -rn "streamable_http" packages/ apps/ && echo "ERRO: streamable_http encontrado" || echo "OK: streamable_http ausente"

    echo "=== D-13: .env.example com MCP_URL ==="
    grep "MCP_URL" apps/brain-sdr/.env.example apps/brain-echo/.env.example
    ```

    Se qualquer grep falhar ou teste falhar, identificar e corrigir o problema antes de prosseguir para o checkpoint.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/__tests__/ && bun test apps/brain-sdr/src/__tests__/unit/</automated>
  </verify>
  <acceptance_criteria>
    - `bun test packages/core/src/__tests__/` sai com código 0
    - `bun test apps/brain-sdr/src/__tests__/unit/` sai com código 0
    - grep de "mcpTools: StructuredTool[]" em interface.ts retorna pelo menos 1 linha
    - grep de "ctx.mcpTools" em brain-sdr/brain.ts retorna pelo menos 2 linhas (bindTools + ToolNode)
    - grep de "ctx.mcpTools" em brain-echo/brain.ts retorna pelo menos 1 linha
    - grep de "streamable_http" retorna 0 ocorrências
    - grep de "SIGTERM" em runner.ts retorna pelo menos 1 linha
    - grep de "async close" em runner.ts retorna pelo menos 1 linha
  </acceptance_criteria>
  <done>Suite completa verde; todos os critérios estruturais verificados por grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    MCP Integration completo (Phase 15):
    - packages/core: BrainBuildContext com mcpTools, BrainRunner com close() e SIGTERM handler
    - apps/brain-sdr: bindTools e ToolNode com ctx.mcpTools e handleToolErrors: true
    - apps/brain-echo: refatorado para ReAct com ctx.mcpTools
    - .env.example: variáveis MCP documentadas em ambos os apps
    - Testes unitários: mcp-init.test.ts e mcp-tool-error.test.ts
  </what-built>
  <how-to-verify>
    **Verificação SIGTERM (MCP-05) — manual obrigatório:**

    1. Configurar variáveis no terminal:
       ```bash
       export MCP_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05
       export DATABASE_URL=postgresql://postgres:change-me@localhost:5432/brain
       export MIGRATIONS_FOLDER=packages/database/src/migrations
       export LLM_PROVIDER=openai
       export OPENAI_API_KEY=sk-...
       export TRANSPORT=webhook
       export PORT=3001
       export ADMIN_TOKEN=test-token
       ```

    2. Iniciar brain-sdr em modo dev:
       ```bash
       cd /root/Brain && bun run apps/brain-sdr/src/index.ts
       ```

    3. Verificar que os logs mostram MCP tools carregadas:
       - Deve aparecer: `"MCP tools loaded successfully"` com `mcpToolCount` no JSON de log
       - OU: se o servidor MCP não estiver acessível, deve aparecer: `"MCP server unreachable at startup — continuing with native tools only"` (warn)

    4. Em outro terminal, enviar SIGTERM:
       ```bash
       kill -SIGTERM <PID>
       ```
       (O PID aparece nos logs de inicialização ou via `ps aux | grep brain-sdr`)

    5. Verificar shutdown limpo:
       - Log deve mostrar: `"SIGTERM received — shutting down cleanly"`
       - Processo deve encerrar em MENOS de 3 segundos
       - Terminal original deve retornar ao prompt (sem hang)

    **Verificação alternativa (se ambiente sem DB disponível):**
    - Rodar `bun test packages/core/src/__tests__/unit/mcp-init.test.ts` — deve passar todos os testes
    - Inspecionar runner.ts e confirmar que SIGTERM handler está presente após `_compileGraph()`
    - Aprovação condicional: "approved — sem ambiente DB; estrutura do código verificada"

    **Critérios de aprovação:**
    - SIGTERM encerra em < 3 segundos (ou aprovação condicional com justificativa)
    - Suite de testes unitários verde (Task 1 deste plano concluída com sucesso)
    - Os 5 critérios de sucesso do ROADMAP.md Phase 15 foram verificados:
      1. MCP tools carregadas quando MCP_URL definido
      2. Brain sobe sem MCP tools quando servidor inacessível
      3. Erro em MCP tool não corrompe thread (handleToolErrors)
      4. SIGTERM encerra sem hang
      5. Brain sem MCP_URL funciona idêntico ao v1.2
  </how-to-verify>
  <resume-signal>Digite "approved" para confirmar SIGTERM limpo, ou "approved — conditional: [motivo]" para aprovação condicional. Em caso de problema, descreva o comportamento observado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova boundary neste plano de verificação.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Information Disclosure | Logs durante verificação SIGTERM | accept | Verificação manual usa ambiente de dev; MCP_AUTH_TOKEN não aparece nos logs (T-15-01 já mitiga) |
</threat_model>

<verification>
Critério final da phase 15:
```bash
bun test packages/core/src/__tests__/ && bun test apps/brain-sdr/src/__tests__/unit/
```
Deve sair com código 0 e "0 failed".
</verification>

<success_criteria>
- Todos os testes unitários da phase 15 verdes (0 falhas)
- SIGTERM verificado manualmente (ou aprovação condicional justificada)
- Os 5 critérios de sucesso do ROADMAP Phase 15 confirmados
- Phase 15 concluída — pronto para Phase 16 (Dynamic responseMode)
</success_criteria>

<output>
Após aprovação do checkpoint, criar `.planning/phases/15-mcp-integration/15-03-SUMMARY.md` com:
- Resultado da suite de testes (N passed, 0 failed)
- Resultado da verificação SIGTERM (manual ou condicional)
- Confirmação dos 5 critérios de sucesso do ROADMAP
- Status: Phase 15 COMPLETE
</output>
