---
phase: 15-mcp-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - apps/brain-sdr/src/brain.ts
  - apps/brain-echo/src/brain.ts
  - apps/brain-sdr/.env.example
  - apps/brain-echo/.env.example
  - packages/core/src/__tests__/integration/mcp-connection.test.ts
autonomous: true
requirements:
  - MCP-02
  - MCP-03
  - MCP-04

must_haves:
  truths:
    - "brain-sdr espalha ctx.mcpTools em bindTools() e ToolNode() junto com tools nativas"
    - "brain-echo ganha grafo ReAct com bindTools() e ToolNode() para suportar MCP tools"
    - "ToolNode em ambos os Brains usa { handleToolErrors: true } para evitar thread corrompido"
    - "Quando ctx.mcpTools = [], brain-sdr e brain-echo operam idêntico ao v1.2"
    - ".env.example de ambos os apps tem MCP_URL, MCP_TOOLS, MCP_AUTH_TOKEN comentados com exemplo"
    - "Teste de integração real conecta ao servidor MCP de teste e lista tools disponíveis"
  artifacts:
    - path: "apps/brain-sdr/src/brain.ts"
      provides: "brain-sdr com mcpTools integrados"
      contains: "ctx.mcpTools"
    - path: "apps/brain-echo/src/brain.ts"
      provides: "brain-echo refatorado para ReAct com mcpTools"
      contains: "ctx.mcpTools"
    - path: "apps/brain-sdr/.env.example"
      provides: "Documentação das variáveis MCP"
      contains: "MCP_URL"
    - path: "apps/brain-echo/.env.example"
      provides: "Documentação das variáveis MCP"
      contains: "MCP_URL"
    - path: "packages/core/src/__tests__/integration/mcp-connection.test.ts"
      provides: "Teste de integração contra servidor MCP real"
      contains: "webhook.biellil.com.br"
  key_links:
    - from: "brain-sdr/brain.ts bindTools()"
      to: "ctx.mcpTools"
      via: "[boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool, ...ctx.mcpTools]"
      pattern: "ctx\\.mcpTools"
    - from: "brain-sdr/brain.ts ToolNode()"
      to: "{ handleToolErrors: true }"
      via: "new ToolNode([...tools], { handleToolErrors: true })"
      pattern: "handleToolErrors.*true"
    - from: "brain-echo/brain.ts"
      to: "ToolNode + toolsCondition"
      via: "ReAct com allTools = [...ctx.mcpTools]"
      pattern: "toolsCondition"
---

<objective>
Adaptar brain-sdr e brain-echo para espalhar ctx.mcpTools no grafo LangGraph, e documentar as variáveis MCP nos .env.example.

Purpose: Concretiza MCP-02 — os Brains recebem a lista de tools do SDK e as registram no LLM e ToolNode. Sem esta task, as tools são carregadas pelo runner mas ignoradas pelos Brains.
Output: brain-sdr com mcpTools no ReAct, brain-echo refatorado para ReAct com suporte a mcpTools, .env.example atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mcp-integration/15-CONTEXT.md
@.planning/phases/15-mcp-integration/15-RESEARCH.md
@.planning/phases/15-mcp-integration/15-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que este plano CONSOME — extraídos de 15-01-PLAN.md e da RESEARCH.md -->

De packages/core/src/brain/interface.ts (após 15-01):
```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
  sql?: Sql;
  mcpTools: StructuredTool[];  // D-01: sempre array, nunca undefined
}
```

brain-sdr/brain.ts ESTADO ATUAL (antes desta task):
```typescript
// Spread atual (SEM mcpTools — Task 1 desta task adiciona):
const llmWithTools = ctx.llm.bindTools([
  boundQualifyTool,
  boundPauseSessionTool,
  boundFinishConversationTool,
]);
.addNode("tools", new ToolNode([boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool]))
// ↑ ToolNode sem { handleToolErrors: true } — Task 1 adiciona

// Imports existentes em brain-sdr (não duplicar):
import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
import { BrainStateAnnotation, extractTokenUsage } from "@brain-pkg/ai";
```

brain-echo/brain.ts ESTADO ATUAL (antes desta task):
```typescript
// Sem bindTools(), sem ToolNode, sem toolsCondition — usa ctx.llm.invoke() direto
// Task 2 desta task refatora para ReAct (PITFALL-5 da RESEARCH.md)
// Imports que PRECISAM ser adicionados:
import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
```

ToolNode options (de @langchain/langgraph@1.4.1 instalado):
```typescript
// ToolNodeOptions = { name?: string; tags?: string[]; handleToolErrors?: boolean }
new ToolNode(tools: (StructuredTool | RunnableToolLike)[], options?: ToolNodeOptions)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar brain-sdr — espalhar ctx.mcpTools com handleToolErrors</name>
  <read_first>
    - apps/brain-sdr/src/brain.ts (OBRIGATÓRIO — ler o arquivo completo antes de modificar)
    - apps/brain-sdr/src/__tests__/unit/brain.test.ts (OBRIGATÓRIO — ver testes existentes que devem continuar passando)
    - .planning/phases/15-mcp-integration/15-CONTEXT.md (D-03, D-11)
  </read_first>
  <files>
    apps/brain-sdr/src/brain.ts
  </files>
  <behavior>
    - Test existente "buildGraph(ctx) com ctx.sql mock chama bindTools com 3 tools" DEVE ser atualizado para verificar que bindTools é chamado com 3 tools nativas + mcpTools (o mock ctx passa mcpTools: [] então ainda são 3)
    - Test existente "buildGraph(ctx) retorna StateGraph" deve continuar passando com ctx incluindo mcpTools: []
    - Novo comportamento: quando ctx.mcpTools tem tools, elas aparecem em bindTools() após as nativas
    - ToolNode deve usar { handleToolErrors: true }
  </behavior>
  <action>
    Modificar apps/brain-sdr/src/brain.ts em dois pontos:

    **Ponto 1 — bindTools()**: Adicionar `...ctx.mcpTools` ao array (D-03):
    ```typescript
    // ANTES:
    const llmWithTools = ctx.llm.bindTools([
      boundQualifyTool,
      boundPauseSessionTool,
      boundFinishConversationTool,
    ]);

    // DEPOIS (D-03, MCP-02):
    const llmWithTools = ctx.llm.bindTools([
      boundQualifyTool,
      boundPauseSessionTool,
      boundFinishConversationTool,
      ...ctx.mcpTools,  // D-03: MCP tools injetadas pelo BrainRunner; [] quando MCP_URL ausente (D-02)
    ]);
    ```

    **Ponto 2 — ToolNode()**: Adicionar `...ctx.mcpTools` e `{ handleToolErrors: true }` (D-03, D-11):
    ```typescript
    // ANTES:
    .addNode("tools", new ToolNode([boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool]))

    // DEPOIS (D-03, D-11, MCP-04):
    .addNode("tools", new ToolNode(
      [boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool, ...ctx.mcpTools],
      { handleToolErrors: true }  // D-11: captura erro de MCP tool, injeta ToolMessage — evita thread corrompido (PITFALL-2)
    ))
    ```

    Atualizar o arquivo de testes `apps/brain-sdr/src/__tests__/unit/brain.test.ts`:
    - No test "buildGraph(ctx) retorna StateGraph (tem addNode e compile)": adicionar `mcpTools: []` ao ctx mock
    - No test "buildGraph(ctx) com ctx.sql mock chama bindTools com 3 tools": adicionar `mcpTools: []` ao ctx mock (bindTools ainda recebe 3 tools porque mcpTools está vazio)
    - No test do describe "BrainSDR — nó llm seta brainOutput": adicionar `mcpTools: []` ao ctx mock

    Adicionar novo teste ao brain.test.ts para verificar MCP-02:
    ```typescript
    describe("BrainSDR — MCP tools integration (MCP-02, D-03)", () => {
      test("buildGraph(ctx) com ctx.mcpTools=[mockTool] chama bindTools com 4 tools", async () => {
        const mod = await import("../../brain.js");
        const bindToolsMock = mock(() => ({
          invoke: mock(async () => ({ content: "resposta", tool_calls: [] })),
        }));
        const fakeMcpTool = {
          name: "mcp_fake_tool",
          description: "fake",
          invoke: async () => "result",
        };
        const ctx = {
          llm: { bindTools: bindToolsMock },
          prompts: { system: "s", qualification: "q" },
          tools: [],
          sql: {} as any,
          mcpTools: [fakeMcpTool],
        };
        (mod.sdrBrain as any).buildGraph(ctx as any);
        const callArgs = (bindToolsMock as any).mock.calls[0][0] as Array<{ name: string }>;
        expect(callArgs).toHaveLength(4);
        const toolNames = callArgs.map((t: any) => t.name);
        expect(toolNames).toContain("mcp_fake_tool");
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test apps/brain-sdr/src/__tests__/unit/brain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-sdr/src/brain.ts contém `...ctx.mcpTools` em bindTools() (grep: `grep "ctx\.mcpTools" apps/brain-sdr/src/brain.ts`)
    - apps/brain-sdr/src/brain.ts contém `handleToolErrors: true` em ToolNode
    - apps/brain-sdr/src/__tests__/unit/brain.test.ts contém `mcpTools: []` nos mocks existentes
    - `bun test apps/brain-sdr/src/__tests__/unit/brain.test.ts` sai com código 0
    - Todos os testes pré-existentes de brain-sdr continuam passando (sem regressão)
    - Novo teste "MCP tools integration" passa verificando 4 tools quando mcpTools tem 1 tool
  </acceptance_criteria>
  <done>brain-sdr espalha ctx.mcpTools corretamente; ToolNode com handleToolErrors: true; testes atualizados e verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar brain-echo para ReAct com suporte a ctx.mcpTools</name>
  <read_first>
    - apps/brain-echo/src/brain.ts (OBRIGATÓRIO — ler o arquivo completo; brain-echo atual não tem bindTools/ToolNode)
    - .planning/phases/15-mcp-integration/15-RESEARCH.md (ver seção "Exemplo: brain-echo com MCP tools" e "Pitfall 5")
    - .planning/phases/15-mcp-integration/15-CONTEXT.md (D-03, D-11)
  </read_first>
  <files>
    apps/brain-echo/src/brain.ts
  </files>
  <behavior>
    - brain-echo com ctx.mcpTools = [] deve produzir BrainOutput correto (comportamento idêntico ao v1.2)
    - brain-echo com ctx.mcpTools = [mockTool] deve usar bindTools + ToolNode no grafo ReAct
    - ctx.llm.bindTools([]) com array vazio é seguro — LLM sem tools, toolsCondition sempre vai para __end__
    - ToolNode com { handleToolErrors: true } e array vazio não deve lançar erro
    - grafo brain-echo NÃO deve usar `ctx.llm.invoke()` diretamente — usar llmWithTools.invoke() (mesmo com 0 tools)
  </behavior>
  <action>
    Refatorar apps/brain-echo/src/brain.ts completamente para padrão ReAct com suporte a ctx.mcpTools.

    O arquivo deve ficar assim:
    ```typescript
    // D-01: EchoBrain usa LLM real + system prompt carregado da tabela `prompts`
    // D-02: tools = [] — sem tools nativas no echo; MCP tools injetadas via ctx.mcpTools (D-03)
    // D-03: buildGraph() retorna StateGraph NÃO compilado — BrainRunner chama .compile({ checkpointer })
    // MCP-02, D-03: brain-echo tem ReAct completo para suportar ctx.mcpTools dinamicamente

    import { StateGraph } from "@langchain/langgraph";
    import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
    import { BrainStateAnnotation, extractTokenUsage } from "@brain-pkg/ai";
    import type { IBrain, BrainBuildContext } from "@brain-pkg/core";

    export const echoBrain: IBrain = {
      id: "brain-echo",
      brainType: "echo",
      promptKeys: ["system"],
      tools: [],
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      buildGraph(ctx: BrainBuildContext): any {
        // D-03: NUNCA chamar .compile() aqui — BrainRunner é o responsável

        // MCP-02, D-03: brain-echo não tem tools nativas — apenas MCP tools dinamicamente injetadas
        // D-02: ctx.mcpTools é sempre array (nunca undefined); [] quando MCP_URL não definido
        const allTools = [...ctx.mcpTools];

        // BaseChatModel.bindTools é opcional na tipagem — guard de runtime
        if (!ctx.llm.bindTools) {
          throw new Error("LLM provider não suporta tool calling — configure um provider compatível");
        }

        // D-11, MCP-04: bindTools com allTools (pode ser []) — LLM sem tools usa toolsCondition → __end__
        const llmWithTools = ctx.llm.bindTools(allTools);

        const contextWindowSize = (() => {
          const n = parseInt(process.env.CONTEXT_WINDOW_MESSAGES ?? "40", 10);
          return n > 0 && isFinite(n) ? n : 40;  // SECURITY: T-08-ENV
        })();

        return new StateGraph(BrainStateAnnotation)
          .addNode("llm", async (state) => {
            // D-01: system prompt carregado de ctx.prompts['system'] (nunca hardcoded)
            // HIST-03: slice no nó, não no invoke() do BrainRunner — evita duplicação
            const messagesForLLM = state.messages.slice(-contextWindowSize);

            const response = await llmWithTools.invoke([
              { role: "system", content: ctx.prompts["system"] },
              ...messagesForLLM,
            ]);

            // SDK-06: BrainOutput manual — sem .withStructuredOutput()
            const fullResponse =
              typeof response.content === "string" ? response.content : "";

            // D-03 nota: messages com spread (para não duplicar histórico no ReAct)
            // brain-echo usa [...state.messages, response] — preserva compatibilidade com v1.2
            return {
              messages: [...state.messages, response],
              brainOutput: {
                fullResponse,
                responseMode: "text" as const,
              },
              tokenUsage: extractTokenUsage(response),
            };
          })
          // D-11, MCP-04: handleToolErrors: true — captura erro de MCP tool, injeta ToolMessage
          .addNode("tools", new ToolNode(allTools, { handleToolErrors: true }))
          .addEdge("__start__", "llm")
          // toolsCondition: vai para "tools" se LLM emitiu tool_calls, senão "__end__"
          // Com allTools = [] e nenhuma tool disponível, LLM nunca emite tool_calls → sempre __end__
          .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
          .addEdge("tools", "llm");
      },
    };
    ```

    IMPORTANTE: O padrão `messages: [...state.messages, response]` é mantido no brain-echo (diferente do brain-sdr que usa `messages: [response]`). Não alterar este comportamento — brain-echo acumula explicitamente enquanto brain-sdr usa o reducer do LangGraph via spread do ReAct.
  </action>
  <verify>
    <automated>cd /root/Brain && bun build apps/brain-echo/src/brain.ts --target bun 2>&1 | head -10 && bun test apps/brain-echo/src/__tests__/unit/brain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-echo/src/brain.ts contém `import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt"`
    - apps/brain-echo/src/brain.ts contém `const allTools = [...ctx.mcpTools]`
    - apps/brain-echo/src/brain.ts contém `handleToolErrors: true`
    - apps/brain-echo/src/brain.ts contém `toolsCondition`
    - apps/brain-echo/src/brain.ts NÃO contém `ctx.llm.invoke(` (agora usa llmWithTools.invoke)
    - `bun build apps/brain-echo/src/brain.ts --target bun` não retorna erros de TypeScript
    - `bun test apps/brain-echo/src/__tests__/unit/brain.test.ts` passa verificando que buildGraph() com ctx.mcpTools=[] não lança
  </acceptance_criteria>
  <done>brain-echo refatorado para ReAct com suporte a ctx.mcpTools; compilação TypeScript sem erros; smoke test do grafo com mcpTools=[] verde.</done>
</task>

<task type="auto">
  <name>Task 3: Documentar variáveis MCP nos .env.example de brain-sdr e brain-echo</name>
  <read_first>
    - apps/brain-sdr/.env.example (OBRIGATÓRIO — ler antes de modificar; adicionar seção MCP ao final)
    - apps/brain-echo/.env.example (OBRIGATÓRIO — ler antes de modificar; adicionar seção MCP ao final)
    - .planning/phases/15-mcp-integration/15-CONTEXT.md (D-13, seção Specifics com exemplo das entradas)
  </read_first>
  <files>
    apps/brain-sdr/.env.example,
    apps/brain-echo/.env.example
  </files>
  <action>
    Adicionar seção MCP ao FINAL de cada .env.example, exatamente como abaixo.

    **apps/brain-sdr/.env.example** — adicionar após a seção `# --- RabbitMQ`:
    ```
    # --- MCP Integration (opcional) ---
    # Brain opera normalmente sem MCP — apenas definir MCP_URL para ativar
    # MCP_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05
    # MCP_TOOLS=tool1,tool2      # CSV de nomes de tools; vazio ou ausente = carregar todas
    # MCP_AUTH_TOKEN=<bearer-token>  # Ausente = sem autenticação (para servidores locais)
    ```

    **apps/brain-echo/.env.example** — adicionar ao FINAL do arquivo (após última linha existente):
    ```
    # --- MCP Integration (opcional) ---
    # Brain opera normalmente sem MCP — apenas definir MCP_URL para ativar
    # MCP_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05
    # MCP_TOOLS=tool1,tool2      # CSV de nomes de tools; vazio ou ausente = carregar todas
    # MCP_AUTH_TOKEN=<bearer-token>  # Ausente = sem autenticação (para servidores locais)
    ```

    As linhas estão todas comentadas (prefixo `#`) — Brain não ativa MCP por padrão (D-09).
    O valor de exemplo usa a URL real de testes: `https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05`
  </action>
  <verify>
    <automated>grep -n "MCP_URL" apps/brain-sdr/.env.example apps/brain-echo/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MCP_URL" apps/brain-sdr/.env.example` retorna pelo menos 1 linha
    - `grep "MCP_URL" apps/brain-echo/.env.example` retorna pelo menos 1 linha
    - `grep "MCP_TOOLS" apps/brain-sdr/.env.example` retorna pelo menos 1 linha
    - `grep "MCP_AUTH_TOKEN" apps/brain-sdr/.env.example` retorna pelo menos 1 linha
    - As linhas MCP estão todas comentadas (iniciadas com `#`)
    - O URL de exemplo `webhook.biellil.com.br/mcp` aparece em ambos os arquivos
  </acceptance_criteria>
  <done>Variáveis MCP documentadas e comentadas nos .env.example de ambos os apps.</done>
</task>

<task type="auto">
  <name>Task 4: Criar teste de integração real contra servidor MCP de teste</name>
  <read_first>
    - packages/core/src/runner/runner.ts (ver como MultiServerMCPClient é inicializado após 15-01)
    - packages/core/src/__tests__/unit/mcp-init.test.ts (ver padrão de testes já criados em 15-01)
    - packages/core/package.json (confirmar @langchain/mcp-adapters instalado)
  </read_first>
  <files>
    packages/core/src/__tests__/integration/mcp-connection.test.ts
  </files>
  <action>
    Criar o diretório e arquivo de teste de integração:
    ```bash
    mkdir -p packages/core/src/__tests__/integration
    ```

    Criar `packages/core/src/__tests__/integration/mcp-connection.test.ts`:
    ```typescript
    /**
     * Teste de integração MCP — conecta ao servidor real de testes.
     * Requer rede. Roda apenas quando MCP_TEST_URL está definido.
     *
     * Uso:
     *   MCP_TEST_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05 \
     *   bun test packages/core/src/__tests__/integration/mcp-connection.test.ts
     */
    import { describe, test, expect, beforeAll } from "bun:test";
    import { MultiServerMCPClient } from "@langchain/mcp-adapters";

    const MCP_TEST_URL =
      process.env.MCP_TEST_URL ??
      "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05";

    describe("MCP Integration — servidor real", () => {
      let client: MultiServerMCPClient;

      beforeAll(async () => {
        client = new MultiServerMCPClient({
          mcpServers: {
            test: {
              url: MCP_TEST_URL,
              // D-14: "http" no JS (@langchain/mcp-adapters), NUNCA "streamable_http" (Python)
              transport: "http",
            },
          },
          onConnectionError: "ignore",
        });
      });

      test("conecta ao servidor MCP e retorna lista de tools", async () => {
        let tools: Awaited<ReturnType<typeof client.getTools>>;
        try {
          tools = await client.getTools();
        } catch (err) {
          // Se servidor inacessível: passa com skip (não é falha de código)
          console.warn("Servidor MCP inacessível — pulando teste de integração:", err);
          return;
        }

        // Servidor acessível: deve retornar array (pode ser vazio se nenhuma tool configurada)
        expect(Array.isArray(tools)).toBe(true);
        console.log(
          `MCP tools disponíveis (${tools.length}):`,
          tools.map((t) => t.name)
        );
      });

      test("tools retornadas têm name e description", async () => {
        let tools: Awaited<ReturnType<typeof client.getTools>>;
        try {
          tools = await client.getTools();
        } catch {
          return; // servidor inacessível — skip
        }

        if (tools.length === 0) {
          console.warn("Servidor MCP não retornou tools — verifique a configuração do servidor");
          return;
        }

        for (const tool of tools) {
          expect(typeof tool.name).toBe("string");
          expect(tool.name.length).toBeGreaterThan(0);
          expect(typeof tool.description).toBe("string");
        }
      });

      test("MCP_TOOLS CSV filtra tools por nome exato (MCP-01)", async () => {
        let allTools: Awaited<ReturnType<typeof client.getTools>>;
        try {
          allTools = await client.getTools();
        } catch {
          return;
        }

        if (allTools.length === 0) return;

        // Pegar o nome da primeira tool para testar filtro
        const firstToolName = allTools[0].name;

        const filteredClient = new MultiServerMCPClient({
          mcpServers: {
            test: {
              url: MCP_TEST_URL,
              transport: "http",
            },
          },
          onConnectionError: "ignore",
        });

        const filteredTools = await filteredClient.getTools();
        // Simula filtro CSV: filtra manualmente pelo nome
        const filtered = filteredTools.filter((t) => t.name === firstToolName);
        expect(filtered).toHaveLength(1);
        expect(filtered[0].name).toBe(firstToolName);

        await filteredClient.close();
      });

      test("close() encerra conexão sem hang", async () => {
        const closePromise = client.close();
        // Deve resolver em menos de 5 segundos
        const timeout = new Promise((_, reject) =>
          setTimeout(() => reject(new Error("close() demorou mais de 5s")), 5000)
        );
        await expect(Promise.race([closePromise, timeout])).resolves.toBeUndefined();
      });
    });
    ```

    O teste usa `onConnectionError: "ignore"` e try/catch — se o servidor estiver inacessível, os testes passam com skip (não bloqueiam CI). O teste de `close()` verifica MCP-05 (shutdown sem hang) em ambiente real.
  </action>
  <verify>
    <automated>MCP_TEST_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05 bun test packages/core/src/__tests__/integration/mcp-connection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/__tests__/integration/mcp-connection.test.ts` existe
    - Arquivo contém `webhook.biellil.com.br` como URL padrão
    - Arquivo contém `transport: "http"` (não "streamable_http")
    - Arquivo contém `onConnectionError: "ignore"`
    - `MCP_TEST_URL=https://... bun test packages/core/src/__tests__/integration/mcp-connection.test.ts` sai com código 0 (mesmo se servidor inacessível — testes fazem skip gracioso)
    - Log do teste exibe lista de MCP tools disponíveis se servidor estiver acessível
  </acceptance_criteria>
  <done>Teste de integração criado; conecta ao servidor MCP real; verifica listagem de tools, filtro CSV e close() sem hang.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ctx.mcpTools → bindTools() | Tools vindas do MCP server externo entram no grafo LangGraph junto com tools nativas |
| ToolNode → MCP tool execution | Execução de MCP tool pode lançar exceção ou retornar dados malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | MCP tool output → LangGraph state | mitigate | `handleToolErrors: true` em ToolNode: erros viram ToolMessage controlado, não exception solta; output de tool normal é conteúdo de ToolMessage, não instrução interpretada diretamente |
| T-15-07 | Elevation of Privilege | MCP tools no mesmo ToolNode das tools nativas | accept | MCP tools têm o mesmo nível de acesso que tools nativas — design intencional; Brain SDR inclui tools com acesso ao DB; risco gerenciado por configuração do MCP_URL pelo operador |
| T-15-08 | Denial of Service | ctx.mcpTools com tool que trava em loop | accept | LangGraph tem limite de steps configurável; `handleToolErrors: true` captura errors mas não timeouts longos; operador é responsável por configurar MCP server confiável |
</threat_model>

<verification>
Todos os testes da suite devem estar verdes:
```bash
bun test apps/brain-sdr/src/__tests__/unit/ && bun test packages/core/src/__tests__/unit/
```

Teste de integração real (requer rede):
```bash
MCP_TEST_URL=https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05 \
  bun test packages/core/src/__tests__/integration/mcp-connection.test.ts
```

Verificações por grep:
```bash
grep -n "ctx\.mcpTools" apps/brain-sdr/src/brain.ts
grep -n "handleToolErrors" apps/brain-sdr/src/brain.ts
grep -n "toolsCondition" apps/brain-echo/src/brain.ts
grep -n "ctx\.mcpTools" apps/brain-echo/src/brain.ts
grep "MCP_URL" apps/brain-sdr/.env.example apps/brain-echo/.env.example
```
</verification>

<success_criteria>
- brain-sdr tem `...ctx.mcpTools` em bindTools() e ToolNode com `handleToolErrors: true`
- brain-echo refatorado para ReAct com `allTools = [...ctx.mcpTools]` e `toolsCondition`
- .env.example de ambos os apps tem seção MCP comentada com URL de exemplo real
- `packages/core/src/__tests__/integration/mcp-connection.test.ts` existe e sai com código 0
- `bun test apps/brain-sdr/src/__tests__/unit/brain.test.ts` → 0 falhas
- `bun test apps/brain-sdr/src/__tests__/` → 0 falhas
- `bun test packages/core/src/__tests__/` → 0 falhas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/15-mcp-integration/15-02-SUMMARY.md` com:
- Arquivos modificados (brain-sdr/brain.ts, brain-echo/brain.ts, ambos .env.example, mcp-connection.test.ts)
- Refatoração do brain-echo (de invoke() direto para ReAct)
- Resultado dos testes unitários (N passed)
- Resultado do teste de integração (tools listadas ou skip gracioso se servidor inacessível)
- Qualquer decisão tomada no campo de discretion (brain-echo com allTools = [] e toolsCondition)
</output>
