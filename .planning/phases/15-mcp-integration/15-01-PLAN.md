---
phase: 15-mcp-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/brain/interface.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/__tests__/unit/mcp-init.test.ts
  - packages/core/src/__tests__/unit/mcp-tool-error.test.ts
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-05

must_haves:
  truths:
    - "BrainBuildContext tem campo mcpTools: StructuredTool[] (nunca undefined)"
    - "BrainRunner._compileGraph() inicializa MultiServerMCPClient quando MCP_URL definido"
    - "Se MCP server inacessível, ctx.mcpTools = [] e warn é logado — Brain sobe normalmente"
    - "Filtro MCP_TOOLS CSV funciona por nome exato; ausente/vazio = todas as tools"
    - "Bearer token MCP_AUTH_TOKEN passado como header Authorization quando presente"
    - "BrainRunner.close() fecha MultiServerMCPClient e é no-op quando mcpClient é null"
    - "SIGTERM handler registrado em init() após _compileGraph() chama close() e process.exit(0)"
    - "Testes unitários cobrem MCP-01, MCP-02, MCP-03, MCP-05 com mocks"
  artifacts:
    - path: "packages/core/src/brain/interface.ts"
      provides: "BrainBuildContext com mcpTools: StructuredTool[]"
      contains: "mcpTools: StructuredTool[]"
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner com MCP lifecycle completo"
      exports: ["BrainRunner", "BrainRunnerOptions"]
    - path: "packages/core/src/__tests__/unit/mcp-init.test.ts"
      provides: "Testes unitários MCP-01, MCP-02, MCP-03, MCP-05"
    - path: "packages/core/src/__tests__/unit/mcp-tool-error.test.ts"
      provides: "Teste unitário MCP-04 (handleToolErrors via ToolNode)"
  key_links:
    - from: "runner.ts _compileGraph()"
      to: "MultiServerMCPClient"
      via: "new MultiServerMCPClient({ mcpServers, onConnectionError: 'ignore' })"
      pattern: "onConnectionError.*ignore"
    - from: "runner.ts close()"
      to: "mcpClient.close()"
      via: "if (this.mcpClient) await this.mcpClient.close()"
      pattern: "mcpClient.*close"
    - from: "runner.ts init()"
      to: "process.on('SIGTERM')"
      via: "após await this._compileGraph()"
      pattern: "process.on.*SIGTERM"
---

<objective>
Instalar @langchain/mcp-adapters, criar testes Wave 0, adicionar mcpTools ao BrainBuildContext e implementar o ciclo de vida MCP completo no BrainRunner (init, close, SIGTERM).

Purpose: Sem esta base, os Brains não têm como receber MCP tools. Este plano constrói o núcleo do SDK que todos os Brains dependem.
Output: interface.ts atualizado, runner.ts com MCP lifecycle, testes unitários cobrindo MCP-01/02/03/05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-mcp-integration/15-CONTEXT.md
@.planning/phases/15-mcp-integration/15-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces extraídas dos arquivos canônicos — executor usa diretamente sem explorar o codebase -->

De packages/core/src/brain/interface.ts (ESTADO ATUAL — antes desta task):
```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
  sql?: Sql;
  // mcpTools: StructuredTool[] — AINDA NÃO EXISTE — Task 1 adiciona este campo
}
```

De packages/core/src/runner/runner.ts (campos privados relevantes — padrão a seguir):
```typescript
private compiledGraph: any | null = null;
private memoryManager: MemoryManager | null = null;
// mcpClient: MultiServerMCPClient | null = null — AINDA NÃO EXISTE — Task 2 adiciona

// _compileGraph() monta ctx assim (ATUAL — sem mcpTools):
const ctx: BrainBuildContext = {
  llm,
  prompts: this.prompts,
  tools: filteredTools,
  sql: this.sql,
};
// Task 2 adiciona: mcpTools ao ctx
```

Imports já presentes em runner.ts (não duplicar):
```typescript
import { createLogger } from "@brain-pkg/observability";
// this.logger.info / this.logger.warn — já usados
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar @langchain/mcp-adapters e criar testes Wave 0</name>
  <read_first>
    - packages/core/package.json (ver dependências atuais antes de instalar)
    - packages/core/src/__tests__/unit/output/schema.test.ts (ver padrão de testes existentes — mock, describe, test, expect do bun:test)
    - .planning/phases/15-mcp-integration/15-VALIDATION.md (ver quais testes Wave 0 são necessários)
  </read_first>
  <files>
    packages/core/src/__tests__/unit/mcp-init.test.ts,
    packages/core/src/__tests__/unit/mcp-tool-error.test.ts
  </files>
  <behavior>
    - Test: quando MCP_URL não está definido, mcpTools deve ser [] (D-02, D-09)
    - Test: quando MCP_URL está definido e getTools() resolve, mcpTools contém as tools retornadas (MCP-01)
    - Test: quando MCP_URL está definido mas servidor inacessível (getTools() lança), mcpTools = [] e warn é logado (MCP-03, D-12)
    - Test: MCP_TOOLS CSV "tool1,tool2" filtra apenas essas tools por nome exato (D-08)
    - Test: MCP_TOOLS ausente/vazio retorna todas as tools sem filtro (D-07)
    - Test: close() é no-op quando mcpClient é null (MCP-05)
    - Test: close() chama mcpClient.close() quando mcpClient não é null (MCP-05)
    - Test: SIGTERM handler registrado em init() chama this.close() (MCP-05)
    - Test mcp-tool-error.test.ts: ToolNode com handleToolErrors: true injeta ToolMessage de erro quando tool lança exceção, sem corromper o thread (MCP-04)
  </behavior>
  <action>
    1. Instalar dependência no packages/core:
       ```bash
       pnpm add @langchain/mcp-adapters@1.1.3 --filter @brain-pkg/core
       ```

    2. Criar packages/core/src/__tests__/unit/mcp-init.test.ts com os seguintes testes usando mock() do bun:test para simular MultiServerMCPClient:

       ```typescript
       import { describe, test, expect, mock, beforeEach } from "bun:test";

       // Helper: simular o bloco MCP de _compileGraph()
       // Este helper replica a lógica exata que será implementada no runner.ts
       async function runMcpInit(options: {
         mcpUrl: string | undefined;
         mcpTools: string | undefined;
         mcpAuthToken: string | undefined;
         getToolsResult: Array<{ name: string }> | Error;
       }): Promise<{
         mcpTools: Array<{ name: string }>;
         warnCalled: boolean;
         infoCalled: boolean;
         clientCreated: boolean;
         clientConfig: any;
       }> {
         let warnCalled = false;
         let infoCalled = false;
         let clientCreated = false;
         let clientConfig: any = null;
         let capturedMcpTools: Array<{ name: string }> = [];

         const fakeLogger = {
           info: (_obj: any, _msg: string) => { infoCalled = true; },
           warn: (_obj: any, _msg: string) => { warnCalled = true; },
         };

         const fakeClient = {
           getTools: mock(async () => {
             if (options.getToolsResult instanceof Error) throw options.getToolsResult;
             return options.getToolsResult;
           }),
           close: mock(async () => {}),
         };

         // Simula a lógica de _compileGraph() bloco MCP (Pattern 3 da RESEARCH.md)
         const mcpUrl = options.mcpUrl?.trim();
         let mcpToolsResult: Array<{ name: string }> = [];

         if (mcpUrl) {
           try {
             clientCreated = true;
             clientConfig = {
               mcpServers: {
                 "external-server": {
                   url: mcpUrl,
                   ...(options.mcpAuthToken && {
                     headers: { Authorization: `Bearer ${options.mcpAuthToken}` },
                   }),
                 },
               },
               onConnectionError: "ignore",
             };

             let allTools = await fakeClient.getTools();
             const toolFilter = options.mcpTools?.trim();
             if (toolFilter) {
               const allowed = new Set(
                 toolFilter.split(",").map((t: string) => t.trim()).filter(Boolean)
               );
               allTools = allTools.filter((t: { name: string }) => allowed.has(t.name));
             }
             mcpToolsResult = allTools;
             fakeLogger.info(
               { mcpToolCount: mcpToolsResult.length },
               "MCP tools loaded successfully"
             );
           } catch (err) {
             fakeLogger.warn({ err }, "MCP server unreachable at startup — continuing with native tools only (MCP-03)");
             mcpToolsResult = [];
           }
         }

         return {
           mcpTools: mcpToolsResult,
           warnCalled,
           infoCalled,
           clientCreated,
           clientConfig,
         };
       }

       describe("MCP Init — BrainRunner bloco MCP (MCP-01, MCP-02, MCP-03)", () => {
         test("MCP_URL ausente → mcpTools = [], sem cliente criado (D-09)", async () => {
           const result = await runMcpInit({
             mcpUrl: undefined,
             mcpTools: undefined,
             mcpAuthToken: undefined,
             getToolsResult: [],
           });
           expect(result.mcpTools).toEqual([]);
           expect(result.clientCreated).toBe(false);
         });

         test("MCP_URL definido, getTools() retorna 2 tools → mcpTools tem 2 tools (MCP-01)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: undefined,
             mcpAuthToken: undefined,
             getToolsResult: [{ name: "tool_a" }, { name: "tool_b" }],
           });
           expect(result.mcpTools).toHaveLength(2);
           expect(result.mcpTools.map(t => t.name)).toContain("tool_a");
           expect(result.mcpTools.map(t => t.name)).toContain("tool_b");
           expect(result.infoCalled).toBe(true);
         });

         test("MCP server inacessível → mcpTools = [], warn logado, sem throw (MCP-03, D-12)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: undefined,
             mcpAuthToken: undefined,
             getToolsResult: new Error("ECONNREFUSED"),
           });
           expect(result.mcpTools).toEqual([]);
           expect(result.warnCalled).toBe(true);
         });

         test("MCP_TOOLS='tool_a,tool_b' filtra por nome exato (D-08)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: "tool_a,tool_b",
             mcpAuthToken: undefined,
             getToolsResult: [{ name: "tool_a" }, { name: "tool_b" }, { name: "tool_c" }],
           });
           expect(result.mcpTools).toHaveLength(2);
           expect(result.mcpTools.map(t => t.name)).not.toContain("tool_c");
         });

         test("MCP_TOOLS vazio ('') → retorna todas as tools (D-07)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: "",
             mcpAuthToken: undefined,
             getToolsResult: [{ name: "tool_a" }, { name: "tool_b" }, { name: "tool_c" }],
           });
           expect(result.mcpTools).toHaveLength(3);
         });

         test("MCP_AUTH_TOKEN definido → header Authorization Bearer adicionado ao config (D-10)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: undefined,
             mcpAuthToken: "secret-token-123",
             getToolsResult: [],
           });
           expect(result.clientConfig.mcpServers["external-server"].headers).toEqual({
             Authorization: "Bearer secret-token-123",
           });
         });

         test("MCP_AUTH_TOKEN ausente → sem campo headers no config (D-10)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: undefined,
             mcpAuthToken: undefined,
             getToolsResult: [],
           });
           expect(result.clientConfig.mcpServers["external-server"].headers).toBeUndefined();
         });

         test("onConnectionError é 'ignore' na config do cliente (PITFALL-1)", async () => {
           const result = await runMcpInit({
             mcpUrl: "https://webhook.biellil.com.br/mcp/01c8bedd-b8c8-4b40-8d95-e37c203cdd05",
             mcpTools: undefined,
             mcpAuthToken: undefined,
             getToolsResult: [],
           });
           expect(result.clientConfig.onConnectionError).toBe("ignore");
         });
       });

       describe("BrainRunner.close() — shutdown limpo (MCP-05)", () => {
         test("close() é no-op quando mcpClient é null", async () => {
           // Simula BrainRunner com mcpClient = null
           let mcpClient: { close: ReturnType<typeof mock> } | null = null;
           const closeFn = async () => {
             if (mcpClient) {
               await mcpClient.close();
               mcpClient = null;
             }
           };
           // Não deve lançar
           await expect(closeFn()).resolves.toBeUndefined();
         });

         test("close() chama mcpClient.close() e seta campo como null", async () => {
           const closeMock = mock(async () => {});
           let mcpClient: { close: ReturnType<typeof mock> } | null = { close: closeMock };
           const closeFn = async () => {
             if (mcpClient) {
               await mcpClient.close();
               mcpClient = null;
             }
           };
           await closeFn();
           expect(closeMock).toHaveBeenCalledTimes(1);
           expect(mcpClient).toBeNull();
         });
       });
       ```

    3. Criar packages/core/src/__tests__/unit/mcp-tool-error.test.ts:

       ```typescript
       import { describe, test, expect } from "bun:test";
       import { ToolNode } from "@langchain/langgraph/prebuilt";
       import { AIMessage, ToolMessage } from "@langchain/core/messages";

       describe("ToolNode handleToolErrors — PITFALL-2 (MCP-04)", () => {
         test("ToolNode com handleToolErrors:true injeta ToolMessage de erro sem lançar", async () => {
           const { tool } = await import("@langchain/core/tools");
           const { z } = await import("zod");

           const failingTool = tool(
             async (_input: { query: string }) => {
               throw new Error("MCP tool timeout");
             },
             {
               name: "failing_mcp_tool",
               description: "Simula MCP tool que falha",
               schema: z.object({ query: z.string() }),
             }
           );

           const toolNode = new ToolNode([failingTool], { handleToolErrors: true });

           const aiMessage = new AIMessage({
             content: "",
             tool_calls: [
               {
                 id: "call_123",
                 name: "failing_mcp_tool",
                 args: { query: "test" },
                 type: "tool_call",
               },
             ],
           });

           // Não deve lançar — handleToolErrors captura e injeta ToolMessage de erro
           const result = await toolNode.invoke({ messages: [aiMessage] });

           // Deve haver pelo menos 1 ToolMessage no resultado
           const toolMessages = result.messages.filter(
             (m: any) => m instanceof ToolMessage
           );
           expect(toolMessages).toHaveLength(1);
           // ToolMessage de erro deve ter o mesmo tool_call_id
           expect((toolMessages[0] as ToolMessage).tool_call_id).toBe("call_123");
           // Conteúdo deve mencionar o erro
           expect(typeof (toolMessages[0] as ToolMessage).content).toBe("string");
         });

         test("ToolNode com array vazio e handleToolErrors:true não lança em construção", () => {
           expect(() => new ToolNode([], { handleToolErrors: true })).not.toThrow();
         });
       });
       ```
  </action>
  <verify>
    <automated>cd /root/Brain && pnpm --filter @brain-pkg/core add @langchain/mcp-adapters@1.1.3 2>/dev/null; bun test packages/core/src/__tests__/unit/mcp-init.test.ts packages/core/src/__tests__/unit/mcp-tool-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/package.json contém `"@langchain/mcp-adapters": "1.1.3"` em dependencies
    - Arquivo packages/core/src/__tests__/unit/mcp-init.test.ts existe
    - Arquivo packages/core/src/__tests__/unit/mcp-tool-error.test.ts existe
    - `bun test packages/core/src/__tests__/unit/mcp-init.test.ts` sai com código 0 (todos os testes passam)
    - `bun test packages/core/src/__tests__/unit/mcp-tool-error.test.ts` sai com código 0
    - mcp-init.test.ts contém ao menos 7 describes/tests
    - mcp-tool-error.test.ts contém teste que verifica ToolMessage após tool error
  </acceptance_criteria>
  <done>Testes Wave 0 vermelhos criados e passando (RED → GREEN com lógica helper); @langchain/mcp-adapters instalado em packages/core.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar mcpTools ao BrainBuildContext e implementar MCP lifecycle no BrainRunner</name>
  <read_first>
    - packages/core/src/brain/interface.ts (OBRIGATÓRIO — ver estado atual antes de modificar)
    - packages/core/src/runner/runner.ts (OBRIGATÓRIO — ler inteiro; a task modifica _compileGraph() e init())
    - .planning/phases/15-mcp-integration/15-RESEARCH.md (ver Pattern 3, Pattern 4, Anti-Patterns)
    - .planning/phases/15-mcp-integration/15-CONTEXT.md (ver D-01 a D-12 — todas as decisões desta task)
  </read_first>
  <files>
    packages/core/src/brain/interface.ts,
    packages/core/src/runner/runner.ts
  </files>
  <action>
    ### interface.ts

    Adicionar campo `mcpTools: StructuredTool[]` ao `BrainBuildContext` (D-01, D-02):

    ```typescript
    export interface BrainBuildContext {
      /** LLM instance from createLLM() — provider configured via env */
      llm: BaseChatModel;
      /** Prompts loaded from prompts table: { [key]: content } */
      prompts: Record<string, string>;
      /** Brain's tools[] filtered by ToolsRegistry for this brainType */
      tools: StructuredTool[];
      /** D-01: sql opcional — injetado pelo BrainRunner para tools de DB via buildGraph(). Brains sem DB tools ignoram. */
      sql?: Sql;
      /**
       * MCP-01, D-01: MCP tools carregadas do servidor externo no startup.
       * D-02: Sempre um array (nunca undefined) — [] quando MCP_URL não definido.
       * Brains espalham via: bindTools([...nativeTools, ...ctx.mcpTools])
       */
      mcpTools: StructuredTool[];
    }
    ```

    Adicionar import de `StructuredTool` se não estiver já importado (verificar imports existentes primeiro).

    ### runner.ts

    1. Adicionar import de `MultiServerMCPClient` no topo do arquivo (após os imports existentes):
       ```typescript
       import { MultiServerMCPClient } from "@langchain/mcp-adapters";
       ```

    2. Adicionar campo privado na classe `BrainRunner` (após `private memoryManager: MemoryManager | null = null;`):
       ```typescript
       private mcpClient: MultiServerMCPClient | null = null;
       ```

    3. Adicionar método público `close()` (após o método `run()`, antes de `_compileGraph()`):
       ```typescript
       /**
        * MCP-05, D-04: Fecha o MultiServerMCPClient de forma limpa.
        * No-op quando mcpClient é null (sem MCP configurado).
        * Chamado pelo handler SIGTERM registrado em init().
        */
       async close(): Promise<void> {
         if (this.mcpClient) {
           await this.mcpClient.close();
           this.mcpClient = null;
         }
       }
       ```

    4. Em `init()`, após `await this._compileGraph();` e ANTES de `this.logger.info({ brainId: this.brain.id }, "BrainRunner initialized");`, adicionar o handler SIGTERM (D-05):
       ```typescript
       // D-05, MCP-05: Auto-registrar SIGTERM handler — SDK cuida do shutdown transparentemente.
       // Registrado APÓS _compileGraph() para garantir que mcpClient está pronto quando SIGTERM chegar.
       // Apps (index.ts) NÃO precisam adicionar SIGTERM handlers.
       process.on('SIGTERM', async () => {
         this.logger.info({ brainId: this.brain.id }, 'SIGTERM received — shutting down cleanly');
         await this.close();
         process.exit(0);
       });
       ```

    5. Em `_compileGraph()`, inserir o bloco MCP entre `const llm = await createLLM(this.llmOptions);` e a construção de `const ctx: BrainBuildContext = {...}`:

       ```typescript
       // --- BLOCO MCP (MCP-01, D-01) ---
       // D-14 CORREÇÃO: transport é "http" no JS (@langchain/mcp-adapters JS) — NÃO "streamable_http"
       // "streamable_http" é o valor do Python (langchain-mcp-adapters) e causa erro no schema Zod do JS.
       let mcpTools: StructuredTool[] = [];
       const mcpUrl = process.env.MCP_URL?.trim();

       if (mcpUrl) {
         try {
           this.mcpClient = new MultiServerMCPClient({
             mcpServers: {
               "external-server": {
                 // transport omitido — presença de `url` identifica HTTP. Alternativa: transport: "http"
                 url: mcpUrl,
                 // D-10: Bearer token via MCP_AUTH_TOKEN — ausente = sem auth
                 ...(process.env.MCP_AUTH_TOKEN && {
                   headers: { Authorization: `Bearer ${process.env.MCP_AUTH_TOKEN}` },
                 }),
               },
             },
             // PITFALL-1: default é "throw" — usar "ignore" para não travar startup se servidor inacessível
             onConnectionError: "ignore",
           });

           let allTools = await this.mcpClient.getTools();

           // D-08: MCP_TOOLS CSV filtra por nome exato; D-07: ausente/vazio = todas
           const toolFilter = process.env.MCP_TOOLS?.trim();
           if (toolFilter) {
             const allowed = new Set(
               toolFilter.split(",").map((t) => t.trim()).filter(Boolean)
             );
             allTools = allTools.filter((t) => allowed.has(t.name));
           }

           mcpTools = allTools;
           this.logger.info(
             { brainId: this.brain.id, mcpToolCount: mcpTools.length },
             "MCP tools loaded successfully"
           );
         } catch (err) {
           // D-12: defensive catch — servidor inacessível não impede startup
           // SECURITY: nunca logar process.env.MCP_AUTH_TOKEN no err
           this.logger.warn(
             { brainId: this.brain.id, err },
             "MCP server unreachable at startup — continuing with native tools only (MCP-03)"
           );
           this.mcpClient = null;
           mcpTools = [];
         }
       }
       // --- FIM BLOCO MCP ---
       ```

    6. Atualizar o `ctx: BrainBuildContext` para incluir `mcpTools` (D-01, D-02):
       ```typescript
       const ctx: BrainBuildContext = {
         llm,
         prompts: this.prompts,
         tools: filteredTools,
         sql: this.sql,
         mcpTools,  // D-01: sempre array; [] quando MCP_URL ausente (D-09)
       };
       ```

    IMPORTANTE: A posição do bloco MCP em _compileGraph() é APÓS `const llm = await createLLM(...)` e ANTES de `const ctx: BrainBuildContext`. Verificar a ordem do código existente antes de inserir.

    IMPORTANTE: O import de `StructuredTool` de `@langchain/core/tools` já pode estar presente em interface.ts — verificar antes de duplicar.
  </action>
  <verify>
    <automated>cd /root/Brain && bun build packages/core/src/brain/interface.ts --target bun 2>&1 | head -5; bun build packages/core/src/runner/runner.ts --target bun 2>&1 | head -10; bun test packages/core/src/__tests__/unit/mcp-init.test.ts packages/core/src/__tests__/unit/mcp-tool-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/brain/interface.ts contém `mcpTools: StructuredTool[]`
    - packages/core/src/runner/runner.ts contém `private mcpClient: MultiServerMCPClient | null = null`
    - packages/core/src/runner/runner.ts contém `async close(): Promise<void>`
    - packages/core/src/runner/runner.ts contém `process.on('SIGTERM'`
    - packages/core/src/runner/runner.ts contém `onConnectionError: "ignore"`
    - packages/core/src/runner/runner.ts contém `process.env.MCP_URL?.trim()`
    - packages/core/src/runner/runner.ts contém `process.env.MCP_TOOLS?.trim()`
    - packages/core/src/runner/runner.ts NÃO contém `"streamable_http"` (D-14 CORREÇÃO)
    - packages/core/src/runner/runner.ts contém `mcpTools,` dentro da construção de `ctx: BrainBuildContext`
    - `bun test packages/core/src/__tests__/unit/mcp-init.test.ts` sai com código 0
    - `bun test packages/core/src/__tests__/unit/mcp-tool-error.test.ts` sai com código 0
    - `bun test packages/core/src/__tests__/` sai com código 0 (suite completa do package)
  </acceptance_criteria>
  <done>BrainBuildContext tem mcpTools, BrainRunner tem MCP lifecycle completo, testes unitários verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → runner.ts | MCP_URL, MCP_TOOLS, MCP_AUTH_TOKEN vêm de variáveis de ambiente — podem ser malformadas |
| runner.ts → MCP server externo | Conexão HTTP para servidor externo — pode estar inacessível ou retornar dados inesperados |
| MCP tool output → LangGraph state | Output de MCP tool entra no histórico de mensagens — pode conter conteúdo não sanitizado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | runner.ts warn log | mitigate | `this.logger.warn({ brainId, err })` — NUNCA logar `process.env.MCP_AUTH_TOKEN`; apenas logar o objeto `err` |
| T-15-02 | Tampering | MCP tool output → ToolMessage | accept | `handleToolErrors: true` garante que output de tool com erro vira ToolMessage — não instrução interpretada pelo LLM; output normal é tratado como conteúdo, não código |
| T-15-03 | Denial of Service | MCP_TOOLS CSV com nomes inválidos | accept | `Array.filter()` retorna `[]` silenciosamente; Brain opera com tools nativas; sem impacto em runtime |
| T-15-04 | Spoofing | MCP_AUTH_TOKEN em bearer | accept | Simples bearer token — adequado para v1.3; HTTPS é responsabilidade do servidor MCP externo; token nunca logado (T-15-01) |
| T-15-05 | Denial of Service | MCP server inacessível no startup | mitigate | `onConnectionError: "ignore"` + try/catch em `getTools()` garante que Brain sobe mesmo com MCP down |
</threat_model>

<verification>
Suite completa do packages/core deve estar verde:
```bash
bun test packages/core/src/__tests__/
```

Verificações por grep:
```bash
grep -n "mcpTools: StructuredTool\[\]" packages/core/src/brain/interface.ts
grep -n "private mcpClient" packages/core/src/runner/runner.ts
grep -n "async close" packages/core/src/runner/runner.ts
grep -n "SIGTERM" packages/core/src/runner/runner.ts
grep -n "onConnectionError" packages/core/src/runner/runner.ts
grep -rn "streamable_http" packages/core/src/ && echo "ERRO: streamable_http encontrado" || echo "OK: streamable_http ausente"
```
</verification>

<success_criteria>
- `bun test packages/core/src/__tests__/unit/mcp-init.test.ts` → 0 falhas
- `bun test packages/core/src/__tests__/unit/mcp-tool-error.test.ts` → 0 falhas
- `bun test packages/core/src/__tests__/` → 0 falhas (suite completa)
- interface.ts tem `mcpTools: StructuredTool[]`
- runner.ts tem `close()`, SIGTERM handler, bloco MCP em `_compileGraph()`, `mcpTools` no ctx
- Nenhuma ocorrência de `"streamable_http"` no código JS
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/15-mcp-integration/15-01-SUMMARY.md` com:
- Arquivos modificados (interface.ts, runner.ts)
- Testes criados (mcp-init.test.ts, mcp-tool-error.test.ts)
- Versão do @langchain/mcp-adapters instalada
- Decisões implementadas (D-01 a D-12, D-14 CORREÇÃO)
- Resultado dos testes (N passed)
- Qualquer pitfall encontrado durante execução
</output>
