---
phase: 13-suporte-a-pgbouncer-para-connection-pooling
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/brain-sdr/src/qualifier.ts
  - apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts
autonomous: true
requirements:
  - PGB-04

must_haves:
  truths:
    - "runQualificationAgent() fecha o pg.Pool interno do PostgresSaver em `finally` após getTuple()"
    - "saver.end() é chamado via API pública tipada — sem cast (end() está em index.d.ts v1.0.3)"
    - "saver.end() é chamado APÓS getTuple() retornar, mas ANTES de compiledQualificationGraph.invoke()"
    - "o teste estático de qualifier.unit.test.ts detecta a presença de saver.end() em finally"
    - "nenhum comportamento existente de runQualificationAgent() é alterado — apenas o finally é adicionado"
  artifacts:
    - path: "apps/brain-sdr/src/qualifier.ts"
      provides: "CR-01 fix — PostgresSaver fechado em finally"
      contains: "saver.end()"
    - path: "apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts"
      provides: "testes para CR-01 + análise estática"
      contains: "saver.end"
  key_links:
    - from: "apps/brain-sdr/src/qualifier.ts"
      to: "PostgresSaver.end()"
      via: "finally block após saver.getTuple()"
      pattern: "finally"
---

<objective>
Corrigir o CR-01 identificado na revisão da Phase 12: `PostgresSaver.fromConnString()` em
`runQualificationAgent()` cria um `pg.Pool` interno que nunca é fechado — connection leak
que cresce com cada chamada ao sub-agente de qualificação.

Purpose: Eliminar o connection leak que se acumula a cada invocação de `qualify_lead` tool. Em produção com muitas conversas, isso esgota as conexões disponíveis no banco.
Output: qualifier.ts com `saver.end()` em finally; testes estáticos e comportamentais atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-CONTEXT.md
@.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces extraídas do codebase — o executor não precisa explorar. -->

From apps/brain-sdr/src/qualifier.ts (bloco a modificar, linha 193-201):
```typescript
try {
  // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
  // Pitfall 4: NÃO chamar saver.setup() aqui
  const saver = PostgresSaver.fromConnString(dbUrl);

  const tuple = await saver.getTuple({
    configurable: { thread_id: sessionId },
  });
  // ... usa tuple ...
  // ... invoke compiledQualificationGraph (stateless, não usa saver) ...
  return finalResult;
} catch (err) {
  logger.error({ err, sessionId }, "Qualification agent error — returning fallback");
  return fallback;
}
```

From @langchain/langgraph-checkpoint-postgres dist/index.d.ts linha 138:
```typescript
// end() é método público tipado — sem necessidade de cast
end(): Promise<void>;
```

From apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts (testes existentes a manter):
// Testa: fallback quando DATABASE_URL ausente
// Testa: não chama saver.setup()
// Testa: usa PostgresSaver.fromConnString()
// Testa: usa _getType() em vez de instanceof
// Testa: tuple undefined com fallback ?? []
// Testa: try/catch garante fallback em qualquer erro
// TODOS devem continuar passando — apenas adicionar testes de CR-01
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: [RED] Adicionar testes de CR-01 ao qualifier.unit.test.ts</name>
  <files>
    apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts
  </files>
  <read_first>
    - apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts (estado atual — testes a manter + ponto de adição)
    - apps/brain-sdr/src/qualifier.ts (estado atual — entender a estrutura do try/catch a ser modificada)
    - .planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-RESEARCH.md (Padrão 3: Pitfall 5 — saver.end() não deve ser chamado antes do getTuple() retornar)
  </read_first>
  <behavior>
    Adicionar dois novos describes ao arquivo existente (NÃO remover os existentes):

    describe 'CR-01: PostgresSaver.end() chamado em finally' (análise estática):
    - Test: 'qualifier.ts contém saver.end() em bloco finally' —
      ler qualifier.ts com readFileSync, verificar que o source contém
      a sequência `finally` e `.end()` (não necessariamente na mesma linha, mas na mesma região)
    - Test: 'saver.end() aparece após saver.getTuple() no source' —
      verificar com indexOf que 'saver.end()' aparece após 'saver.getTuple(' no source
    - Test: 'saver.end() não aparece antes de saver.getTuple() no source (Pitfall 5)' —
      verificar que a primeira ocorrência de 'saver.end()' NÃO precede 'saver.getTuple('
    - Test: 'saver.end() é chamado via API tipada (sem cast as any)' —
      verificar que o source contém 'saver.end()' sem cast:
      source NÃO contém '(saver as any)' na linha que chama end()

    NOTA: Testes estáticos (readFileSync) são apropriados aqui porque:
    1. O objeto PostgresSaver real requer conexão de banco para mock
    2. O padrão já é usado nos testes existentes do mesmo arquivo
    3. O Pitfall 5 do RESEARCH.md é exatamente sobre ordem de chamadas — análise estática verifica isso

    Os testes DEVEM falhar nesta etapa (RED) pois qualifier.ts ainda não tem saver.end().
  </behavior>
  <action>
    Adicionar ao final de apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts
    (após o último describe existente), sem remover nada:

    ```typescript
    describe("CR-01: PostgresSaver connection leak — saver.end() em finally", () => {
      const src = readFileSync(resolve(import.meta.dir, "../../qualifier.ts"), "utf-8");
      // Excluir linhas de comentário para evitar falsos positivos
      const codeLines = src.split("\n").filter(l => !l.trim().startsWith("//")).join("\n");

      test("qualifier.ts contém saver.end() para fechar o pg.Pool interno (CR-01)", () => {
        expect(codeLines).toMatch(/saver\.end\(\)/);
      });

      test("saver.end() está em bloco finally (não no fluxo normal)", () => {
        // Verificar que 'finally' aparece no contexto do saver
        expect(codeLines).toMatch(/finally\s*\{[^}]*saver\.end\(\)/s);
      });

      test("saver.end() é chamado APÓS saver.getTuple() (Pitfall 5: não fechar antes de usar)", () => {
        const getTupleIdx = codeLines.indexOf("saver.getTuple(");
        const endIdx = codeLines.indexOf("saver.end()");
        expect(getTupleIdx).toBeGreaterThan(-1);
        expect(endIdx).toBeGreaterThan(-1);
        expect(endIdx).toBeGreaterThan(getTupleIdx);
      });

      test("saver.end() usa API pública tipada — sem cast (saver as any) na chamada end()", () => {
        // Verificar que a linha com saver.end() não tem cast de tipo
        const lines = src.split("\n");
        const endLine = lines.find(l => l.includes("saver.end()") && !l.trim().startsWith("//"));
        expect(endLine).toBeDefined();
        expect(endLine).not.toMatch(/as any/);
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Os 4 novos testes do describe 'CR-01' falham (RED phase — qualifier.ts não tem saver.end() ainda).
    Os testes existentes (5 describes anteriores) continuam passando.
    Nenhum syntax error no arquivo de teste.
  </done>
  <acceptance_criteria>
    - grep -c 'saver.end' apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts retorna >= 3
    - grep -c 'CR-01' apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts retorna >= 1
    - grep -c 'finally' apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts retorna >= 1
    - bun test retorna falhas especificamente nos testes do describe 'CR-01' (RED confirmado)
    - Os 5 testes anteriores continuam passando (sem regressão)
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: [GREEN] Corrigir CR-01 — saver.end() em finally em qualifier.ts</name>
  <files>
    apps/brain-sdr/src/qualifier.ts
  </files>
  <read_first>
    - apps/brain-sdr/src/qualifier.ts (estado atual completo — entender o bloco try/catch existente para inserir o finally corretamente)
    - apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts (testes recém-escritos — guia do comportamento esperado)
    - .planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-RESEARCH.md (Padrão 3: Pitfall 5 sobre a ordem correta do finally)
  </read_first>
  <behavior>
    A função runQualificationAgent() tem atualmente:
    ```
    const saver = PostgresSaver.fromConnString(dbUrl);
    const tuple = await saver.getTuple(...)
    // usa tuple
    // invoca compiledQualificationGraph (stateless, não usa saver)
    ```

    Após o fix, deve ter:
    ```
    const saver = PostgresSaver.fromConnString(dbUrl);
    let tuple;
    try {
      tuple = await saver.getTuple(...)
    } finally {
      await saver.end()  // fecha pg.Pool após getTuple retornar
    }
    // usa tuple (saver já fechado — mas tuple não depende de saver)
    // invoca compiledQualificationGraph (stateless, não usa saver)
    ```

    IMPORTANTE (Pitfall 5 do RESEARCH.md): saver é usado APENAS para getTuple().
    O compiledQualificationGraph.invoke() é stateless — não usa saver.
    Portanto saver.end() deve ser chamado após getTuple() mas antes de invoke().
  </behavior>
  <action>
    Em apps/brain-sdr/src/qualifier.ts, dentro da função runQualificationAgent(),
    localizar o bloco interno do try/catch principal (começando em "try {" na linha ~193):

    ANTES (trecho atual):
    ```typescript
    try {
      // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
      // Pitfall 4: NÃO chamar saver.setup() aqui
      const saver = PostgresSaver.fromConnString(dbUrl);

      const tuple = await saver.getTuple({
        configurable: { thread_id: sessionId },
      });

      // Extrair mensagens — tuple pode ser undefined se sessão não tem checkpoint
      const allMessages: BaseMessage[] =
        ((tuple?.checkpoint?.channel_values?.messages) as BaseMessage[]) ?? [];
      // ...resto...
    } catch (err) {
      logger.error({ err, sessionId }, "Qualification agent error — returning fallback");
      return fallback;
    }
    ```

    DEPOIS (fix CR-01 — D-09):
    ```typescript
    try {
      // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
      // Pitfall 4: NÃO chamar saver.setup() aqui
      const saver = PostgresSaver.fromConnString(dbUrl);

      // CR-01 fix: fechar pg.Pool após getTuple() — saver não é usado após esta chamada
      // compiledQualificationGraph é stateless e não depende do saver (Pitfall 5 do RESEARCH.md)
      let tuple: Awaited<ReturnType<typeof saver.getTuple>>;
      try {
        tuple = await saver.getTuple({
          configurable: { thread_id: sessionId },
        });
      } finally {
        await saver.end(); // D-09: API pública tipada — fecha o pg.Pool interno
      }

      // Extrair mensagens — tuple pode ser undefined se sessão não tem checkpoint
      const allMessages: BaseMessage[] =
        ((tuple?.checkpoint?.channel_values?.messages) as BaseMessage[]) ?? [];
      // ...resto do bloco try principal inalterado...
    } catch (err) {
      logger.error({ err, sessionId }, "Qualification agent error — returning fallback");
      return fallback;
    }
    ```

    ATENÇÃO: O `try/finally` interno é DIFERENTE do `try/catch` externo.
    O `catch (err)` do bloco externo continua capturando qualquer erro que o
    `try/finally` interno possa lançar (incluindo erros de saver.end()).
    Não remover nem modificar o try/catch externo.

    O resto de runQualificationAgent() após o bloco de allMessages (saveQualificationToMemories,
    compiledQualificationGraph.invoke(), etc.) permanece intacto.

    VERIFICAR tipo: ReturnType<typeof saver.getTuple> pode precisar de ajuste dependendo da versão
    do TypeScript. Se o tipo `Awaited<ReturnType<typeof saver.getTuple>>` causar erro, usar:
    ```typescript
    let tuple: CheckpointTuple | undefined;
    ```
    onde CheckpointTuple é importado de '@langchain/langgraph-checkpoint'.
    Verificar o import existente antes de adicionar — o tipo já pode estar em uso.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Todos os testes em qualifier.unit.test.ts passam (GREEN phase), incluindo os 4 novos testes de CR-01.
    qualifier.ts contém saver.end() em bloco finally após saver.getTuple().
    Full suite do brain-sdr passa sem regressões.
  </done>
  <acceptance_criteria>
    - bun test apps/brain-sdr/src/__tests__/unit/ retorna 0 falhas (todos passam)
    - grep -c 'saver.end()' apps/brain-sdr/src/qualifier.ts retorna >= 1
    - grep -c 'finally' apps/brain-sdr/src/qualifier.ts retorna >= 1
    - grep -n 'saver.end()' apps/brain-sdr/src/qualifier.ts — a linha retornada vem APÓS a linha com 'saver.getTuple('
    - grep -c '(saver as any)' apps/brain-sdr/src/qualifier.ts retorna 0 (sem cast)
    - bun test apps/brain-sdr/src/ retorna 0 falhas (sem regressão em brain.test.ts)
    - cd /root/Brain && bun build apps/brain-sdr/src/index.ts --target bun retorna 0 erros TypeScript
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qualifier.ts → PostgreSQL via pg.Pool | PostgresSaver cria um pg.Pool que precisa ser fechado — pool aberto indefinidamente esgota conexões disponíveis |
| DATABASE_URL → qualifier.ts | Connection string usada para criar PostgresSaver — não deve ser logada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Denial of Service | qualifier.ts PostgresSaver leak | mitigate | saver.end() em finally garante que o pg.Pool é fechado após cada invocação de qualify_lead, independente de sucesso ou erro em getTuple() |
| T-13-02-02 | Information Disclosure | qualifier.ts DATABASE_URL | accept | DATABASE_URL já não é logada no código atual — padrão mantido; o fix de CR-01 não adiciona nenhum log de credenciais |
| T-13-02-03 | Tampering | qualifier.ts saver.end() antes de getTuple() | mitigate | Pitfall 5 verificado via teste estático — saver.end() aparece após saver.getTuple() no source; teste falha se a ordem for invertida |
</threat_model>

<verification>
Verificação geral do Plan 02:

```bash
# Todos os testes do qualifier passam incluindo CR-01
cd /root/Brain && bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts

# saver.end() presente e em finally
grep -n 'saver\.end\(\)' apps/brain-sdr/src/qualifier.ts
grep -n 'finally' apps/brain-sdr/src/qualifier.ts

# Full suite brain-sdr sem regressão
bun test apps/brain-sdr/src/

# Full suite cross-package
bun test packages/database/src/ packages/ai/src/ apps/brain-sdr/src/
```
</verification>

<success_criteria>
- bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts — 0 falhas (todos os testes passam, incluindo 4 novos CR-01)
- qualifier.ts: saver.end() presente em bloco finally após getTuple(); sem cast (saver as any)
- Full suite bun test packages/database/src/ packages/ai/src/ apps/brain-sdr/src/ — 0 falhas
- Nenhum arquivo fora do escopo desta fase foi modificado
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/13-suporte-a-pgbouncer-para-connection-pooling/13-02-SUMMARY.md`
</output>
