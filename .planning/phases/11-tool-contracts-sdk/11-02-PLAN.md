---
phase: 11-tool-contracts-sdk
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - packages/core/src/tools/pause-session.ts
  - packages/core/src/tools/finish-conversation.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - TOOLS-STD-01
  - TOOLS-STD-02
  - TOOLS-ENV-01
  - TOOLS-ENV-02

must_haves:
  truths:
    - "createPauseSessionTool(sql) existe em packages/core/tools e retorna StructuredTool com name='pause_session'"
    - "createFinishConversationTool(sql) existe em packages/core/tools e retorna StructuredTool com name='finish_conversation'"
    - "pause_session invocada com thread_id faz UPDATE em leads.fullpp=false"
    - "finish_conversation invocada com thread_id faz UPDATE atômico em leads.ia_ativada=false E leads.fullpp=false"
    - "BrainRunner._compileGraph() passa sql: this.sql ao BrainBuildContext"
    - "packages/core/src/index.ts exporta createPauseSessionTool e createFinishConversationTool"
    - "Todos os testes de pause-session.test.ts e finish-conversation.test.ts passam (GREEN)"
  artifacts:
    - path: "packages/core/src/tools/pause-session.ts"
      provides: "factory createPauseSessionTool(sql: Sql): StructuredTool"
      exports: ["createPauseSessionTool"]
    - path: "packages/core/src/tools/finish-conversation.ts"
      provides: "factory createFinishConversationTool(sql: Sql): StructuredTool"
      exports: ["createFinishConversationTool"]
    - path: "packages/core/src/runner/runner.ts"
      provides: "ctx com sql: this.sql em _compileGraph()"
      contains: "sql: this.sql"
    - path: "packages/core/src/index.ts"
      provides: "barrel com exports das factories"
      contains: "createPauseSessionTool"
  key_links:
    - from: "packages/core/src/tools/pause-session.ts"
      to: "leads.uniqueId via thread_id"
      via: "config?.configurable?.thread_id passado pelo BrainRunner no invoke()"
      pattern: "thread_id"
    - from: "packages/core/src/runner/runner.ts"
      to: "packages/core/src/brain/interface.ts BrainBuildContext.sql"
      via: "sql: this.sql no ctx em _compileGraph()"
      pattern: "sql: this.sql"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/tools/pause-session.ts e finish-conversation.ts"
      via: "export nomeado explícito"
      pattern: "createPauseSessionTool|createFinishConversationTool"
---

<objective>
Criar as factories createPauseSessionTool e createFinishConversationTool, passar sql ao BrainBuildContext no BrainRunner, e exportar tudo pelo barrel.

Purpose: Completar o SDK de tool contracts — após este plan, qualquer Brain pode chamar createPauseSessionTool(ctx.sql!) dentro de buildGraph() para obter uma tool pronta para pausar sessões, e createFinishConversationTool(ctx.sql!) para encerrar conversas. TOOLS-STD-01 e TOOLS-STD-02 ficam totalmente implementados.

Output: 2 arquivos de factory, BrainRunner atualizado, barrel atualizado, todos os testes verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-tool-contracts-sdk/11-CONTEXT.md
@.planning/phases/11-tool-contracts-sdk/11-RESEARCH.md
@.planning/phases/11-tool-contracts-sdk/11-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que o executor precisa ANTES de implementar. -->

De packages/core/src/brain/interface.ts (após Plan 01):
```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
  /** D-01: sql opcional — injetado pelo BrainRunner para tools de DB */
  sql?: Sql;
}
```

De packages/core/src/runner/runner.ts — montagem do ctx em _compileGraph() (linhas ~283-287):
```typescript
const ctx: BrainBuildContext = {
  llm,
  prompts: this.prompts,
  tools: filteredTools,
  // sql: this.sql,  ← AUSENTE — deve ser adicionado (D-03)
};
```

Padrão de tool LangChain (apps/brain-sdr/src/brain.ts):
```typescript
import { tool } from "@langchain/core/tools";
import type { RunnableConfig } from "@langchain/core/runnables";
import { z } from "zod";

const myTool = tool(
  async (_args: Record<string, never>, config?: RunnableConfig) => {
    const threadId = config?.configurable?.thread_id as string | undefined;
    if (!threadId) return "Erro: thread_id não disponível na configuração";
    // ... lógica com threadId
    return "Status: operação concluída";
  },
  { name: "tool_name", description: "...", schema: z.object({}) }
);
```

Barrel atual (packages/core/src/index.ts — final):
```typescript
// SDK-06: BrainOutput contract
export { BrainOutputSchema, ResponseModeSchema } from "./output/schema.js";
export type { BrainOutput, ResponseMode } from "./output/schema.js";
// createPauseSessionTool e createFinishConversationTool AUSENTES — devem ser adicionados
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Factories createPauseSessionTool e createFinishConversationTool</name>
  <files>
    packages/core/src/tools/pause-session.ts
    packages/core/src/tools/finish-conversation.ts
  </files>
  <read_first>
    - packages/core/src/tools/__tests__/pause-session.test.ts (testes RED do Plan 01 — implementar para tornar GREEN)
    - packages/core/src/tools/__tests__/finish-conversation.test.ts (testes RED do Plan 01 — implementar para tornar GREEN)
    - packages/core/src/leads/lead-service.ts (padrão Drizzle existente — replicar imports)
    - apps/brain-sdr/src/brain.ts (padrão boundQualifyTool — referência de como usar tool() com closure)
    - .planning/phases/11-tool-contracts-sdk/11-RESEARCH.md §"Pattern 1: Factory Function para Standard Tools" e §"Anti-Patterns a Evitar"
  </read_first>
  <behavior>
    FASE GREEN: Implementar para que os testes do Plan 01 (pause-session.test.ts e finish-conversation.test.ts) passem.

    === pause-session.ts ===
    Testes que devem passar:
    - "invoca db.update com fullpp=false quando thread_id está presente" → update chain chamado, retorno contém "pausada"
    - "retorna string de erro quando thread_id ausente" → sem update, retorno contém "thread_id não disponível"
    - "retorna string de erro quando config é undefined" → sem update, retorno contém "thread_id não disponível"
    - "name da tool é 'pause_session'" → tool.name === "pause_session"

    === finish-conversation.ts ===
    Testes que devem passar:
    - "invoca db.update com iaAtivada=false E fullpp=false em único update" → mockUpdate chamado 1x, retorno contém "encerrada"
    - "set atômico: mockSet chamado com { iaAtivada: false, fullpp: false }" → verificar que set foi chamado com ambos os campos no mesmo call
    - "retorna string de erro quando thread_id ausente" → retorno contém "thread_id não disponível"
    - "name da tool é 'finish_conversation'" → tool.name === "finish_conversation"
  </behavior>
  <action>
    === 1. Criar packages/core/src/tools/pause-session.ts ===

    ```typescript
    // TOOLS-STD-01: pause_session — pausa sessão de um lead via thread_id do RunnableConfig.
    // D-04: thread_id lido de config.configurable.thread_id (padrão LangChain) — nunca do LLM (D-06).
    // D-05: thread_id = lead.uniqueId (IDLead canonical), definido pelo BrainRunner.
    // D-11: Factory function — closure sobre sql para compatibilidade multi-tenant.

    import { tool } from "@langchain/core/tools";
    import type { RunnableConfig } from "@langchain/core/runnables";
    import { drizzle } from "drizzle-orm/postgres-js";
    import { eq } from "drizzle-orm";
    import { leads } from "@brain-pkg/database";
    import type { Sql } from "postgres";
    import { z } from "zod";

    /**
     * TOOLS-STD-01: Cria a tool pause_session bound ao sql do tenant.
     *
     * Quando invocada, altera leads.fullpp = false para o lead identificado
     * pelo thread_id do RunnableConfig (= lead.uniqueId canonical).
     *
     * @param sql - postgres.js Sql instance do tenant (de BrainBuildContext.sql)
     */
    export function createPauseSessionTool(sql: Sql) {
      const db = drizzle(sql);
      return tool(
        async (_args: Record<string, never>, config?: RunnableConfig) => {
          // D-04: thread_id do RunnableConfig — seguro contra alucinação do LLM (D-06)
          const threadId = config?.configurable?.thread_id as string | undefined;
          if (!threadId) {
            return "Erro: thread_id não disponível na configuração";
          }
          await db
            .update(leads)
            .set({ fullpp: false, updatedAt: new Date() })
            .where(eq(leads.uniqueId, threadId));
          return "Sessão pausada com sucesso — atendimento humano ativado";
        },
        {
          name: "pause_session",
          description:
            "Pausa a sessão atual e transfere para atendimento humano. Use quando o usuário pede para falar com um humano ou quando a conversa requer intervenção manual.",
          schema: z.object({}),
        }
      );
    }
    ```

    === 2. Criar packages/core/src/tools/finish-conversation.ts ===

    ```typescript
    // TOOLS-STD-02: finish_conversation — encerra conversa de um lead via thread_id do RunnableConfig.
    // D-04: thread_id lido de config.configurable.thread_id (padrão LangChain) — nunca do LLM (D-06).
    // D-05: thread_id = lead.uniqueId (IDLead canonical), definido pelo BrainRunner.
    // D-11: Factory function — closure sobre sql para compatibilidade multi-tenant.
    // Pitfall 1: update ATÔMICO — ia_ativada e fullpp no mesmo .set() para evitar inconsistência.

    import { tool } from "@langchain/core/tools";
    import type { RunnableConfig } from "@langchain/core/runnables";
    import { drizzle } from "drizzle-orm/postgres-js";
    import { eq } from "drizzle-orm";
    import { leads } from "@brain-pkg/database";
    import type { Sql } from "postgres";
    import { z } from "zod";

    /**
     * TOOLS-STD-02: Cria a tool finish_conversation bound ao sql do tenant.
     *
     * Quando invocada, altera leads.ia_ativada = false E leads.fullpp = false
     * em um único UPDATE atômico para o lead identificado pelo thread_id do RunnableConfig.
     *
     * @param sql - postgres.js Sql instance do tenant (de BrainBuildContext.sql)
     */
    export function createFinishConversationTool(sql: Sql) {
      const db = drizzle(sql);
      return tool(
        async (_args: Record<string, never>, config?: RunnableConfig) => {
          // D-04: thread_id do RunnableConfig — seguro contra alucinação do LLM (D-06)
          const threadId = config?.configurable?.thread_id as string | undefined;
          if (!threadId) {
            return "Erro: thread_id não disponível na configuração";
          }
          // TOOLS-STD-02: update atômico — ia_ativada=false E fullpp=false no mesmo UPDATE
          // Pitfall 1: dois updates separados criariam risco de inconsistência
          await db
            .update(leads)
            .set({ iaAtivada: false, fullpp: false, updatedAt: new Date() })
            .where(eq(leads.uniqueId, threadId));
          return "Conversa encerrada — IA desativada para este lead";
        },
        {
          name: "finish_conversation",
          description:
            "Encerra definitivamente a conversa automatizada. Use quando o usuário solicita explicitamente encerrar o atendimento ou quando a conversa está concluída.",
          schema: z.object({}),
        }
      );
    }
    ```

    AVISO: NÃO exportar instâncias prontas — apenas as factories (Pitfall 4 do RESEARCH.md).
    NÃO chamar LeadService.setFullpp/setIaAtivada dentro das tools — update direto via Drizzle para atomicidade (Pitfall 1).
  </action>
  <verify>
    <automated>bun test packages/core/src/tools/__tests__/pause-session.test.ts packages/core/src/tools/__tests__/finish-conversation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls packages/core/src/tools/pause-session.ts` existe
    - `ls packages/core/src/tools/finish-conversation.ts` existe
    - `grep -n "export function createPauseSessionTool" packages/core/src/tools/pause-session.ts` retorna 1 linha
    - `grep -n "export function createFinishConversationTool" packages/core/src/tools/finish-conversation.ts` retorna 1 linha
    - `grep -n "name: \"pause_session\"" packages/core/src/tools/pause-session.ts` retorna 1 linha
    - `grep -n "name: \"finish_conversation\"" packages/core/src/tools/finish-conversation.ts` retorna 1 linha
    - `grep -n "iaAtivada: false, fullpp: false" packages/core/src/tools/finish-conversation.ts` retorna 1 linha (update atômico)
    - `grep -n "pauseSessionTool\|finishConversationTool" packages/core/src/tools/pause-session.ts packages/core/src/tools/finish-conversation.ts 2>/dev/null | grep -v "createPause\|createFinish"` retorna vazio (sem instâncias exportadas)
    - `bun test packages/core/src/tools/__tests__/pause-session.test.ts` — 4 testes passam (GREEN)
    - `bun test packages/core/src/tools/__tests__/finish-conversation.test.ts` — 4 testes passam (GREEN)
  </acceptance_criteria>
  <done>pause-session.ts e finish-conversation.ts existem como factories. Todos os 8 testes (4 de cada) passam. Update atômico confirmado em finish-conversation.</done>
</task>

<task type="auto">
  <name>Task 2: BrainRunner sql injection + barrel exports</name>
  <files>
    packages/core/src/runner/runner.ts
    packages/core/src/index.ts
  </files>
  <read_first>
    - packages/core/src/runner/runner.ts (localizar _compileGraph() e o bloco de montagem do ctx — linhas ~280-290)
    - packages/core/src/index.ts (final do arquivo — adicionar exports após SDK-06)
    - packages/core/src/tools/pause-session.ts (confirmar nome de export: createPauseSessionTool)
    - packages/core/src/tools/finish-conversation.ts (confirmar nome de export: createFinishConversationTool)
    - .planning/phases/11-tool-contracts-sdk/11-RESEARCH.md §"Pattern 4: Modificação do BrainBuildContext" e §"Barrel export (index.ts)"
  </read_first>
  <action>
    === 1. packages/core/src/runner/runner.ts ===
    Localizar o método `_compileGraph()` (privado, no final da classe).
    Localizar o bloco de montagem do `const ctx: BrainBuildContext`:
    ```typescript
    // ESTADO ATUAL (aproximadamente linha 283):
    const ctx: BrainBuildContext = {
      llm,
      prompts: this.prompts,
      tools: filteredTools,
    };
    ```

    Adicionar `sql: this.sql` ao objeto ctx:
    ```typescript
    // DEPOIS (D-03):
    const ctx: BrainBuildContext = {
      llm,
      prompts: this.prompts,
      tools: filteredTools,
      sql: this.sql,  // D-03: injetado para tools de DB — buildGraph() acessa via ctx.sql
    };
    ```

    NÃO modificar nenhuma outra parte do runner.ts. A linha `this.compiledGraph = this.brain.buildGraph(ctx).compile({ checkpointer });` permanece inalterada.

    === 2. packages/core/src/index.ts ===
    Adicionar ao FINAL do arquivo (após o bloco SDK-06):

    ```typescript
    // SDK-07: Standard Tools — factories para tools de controle de sessão
    // D-11: Exportar apenas factories (não instâncias) — Brain chama createXTool(ctx.sql!) em buildGraph()
    export { createPauseSessionTool } from "./tools/pause-session.js";
    export { createFinishConversationTool } from "./tools/finish-conversation.js";
    ```

    NÃO usar `export *` — seguir padrão de exports nomeados explícitos do barrel (T-3-04-04 no arquivo).
  </action>
  <verify>
    <automated>bun test packages/core/ && cd packages/core && bun run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sql: this.sql" packages/core/src/runner/runner.ts` retorna 1 linha dentro de _compileGraph()
    - `grep -n "createPauseSessionTool\|createFinishConversationTool" packages/core/src/index.ts` retorna 2 linhas
    - `grep -n "export \*" packages/core/src/index.ts` retorna vazio (sem export star)
    - `bun test packages/core/` — todos os testes passam (sem falhas)
    - `cd packages/core && bun run build 2>&1 | grep -i "error"` retorna vazio (sem erros TypeScript)
    - `cd packages/core && bun run build 2>&1 | grep "createPauseSessionTool\|createFinishConversationTool"` — não retorna erros para esses exports
  </acceptance_criteria>
  <done>BrainRunner._compileGraph() passa sql: this.sql ao BrainBuildContext. barrel exporta createPauseSessionTool e createFinishConversationTool. Build limpo e toda a suite packages/core verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph ToolNode → tool handler | ToolNode invoca o handler com args do LLM e config do sistema |
| tool handler → PostgreSQL | Drizzle UPDATE em leads usando thread_id do config (não do LLM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Spoofing | `finish_conversation` thread_id | mitigate | Guard `if (!threadId) return "Erro"` — update sem WHERE é impossível; threadId vem do BrainRunner, não do LLM (D-05/D-06) |
| T-11-05 | Tampering | `finish_conversation` atomicidade | mitigate | UPDATE único com `.set({ iaAtivada: false, fullpp: false })` — impossível ter ia_ativada=false sem fullpp=false (Pitfall 1 mitigado) |
| T-11-06 | Repudiation | Tool invocação sem log | accept | LangSmith traces cobrem a execução do grafo; log explícito da tool seria ruído para v1.2 |
| T-11-07 | Information Disclosure | Factory sql closure leak | accept | sql é injetado via BrainBuildContext (não global); cada instância do Brain tem seu próprio sql isolado |
</threat_model>

<verification>
Verificação completa do Plan 02:

```bash
# Testes das factories (GREEN)
bun test packages/core/src/tools/__tests__/pause-session.test.ts
bun test packages/core/src/tools/__tests__/finish-conversation.test.ts

# Suite completa do packages/core
bun test packages/core/

# Build TypeScript limpo
cd packages/core && bun run build

# Verificar exports no barrel
grep "createPauseSessionTool\|createFinishConversationTool" packages/core/src/index.ts

# Verificar sql injection no runner
grep "sql: this.sql" packages/core/src/runner/runner.ts
```
</verification>

<success_criteria>
1. `bun test packages/core/src/tools/__tests__/pause-session.test.ts` — 4 testes passam
2. `bun test packages/core/src/tools/__tests__/finish-conversation.test.ts` — 4 testes passam
3. `bun test packages/core/` — suite completa verde (todos os testes de tools, registry, lead-service, runner)
4. `packages/core/src/tools/pause-session.ts` exporta `createPauseSessionTool` (factory, não instância)
5. `packages/core/src/tools/finish-conversation.ts` exporta `createFinishConversationTool` com update atômico
6. `packages/core/src/runner/runner.ts` contém `sql: this.sql` no ctx de `_compileGraph()`
7. `packages/core/src/index.ts` exporta ambas as factories
8. `cd packages/core && bun run build` passa sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/11-tool-contracts-sdk/11-02-SUMMARY.md` com:
- Arquivos criados/modificados
- Resultados dos testes (contagem de testes passando)
- Decisões implementadas (D-02, D-03, D-04, D-05, D-06, D-10, D-11)
- Qualquer desvio do plano
</output>
