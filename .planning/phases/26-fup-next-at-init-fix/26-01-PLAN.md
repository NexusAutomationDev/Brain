---
phase: 26-fup-next-at-init-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts
  - packages/core/src/leads/lead-service.ts
  - packages/core/src/fup/fup-scheduler.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FUP-02

must_haves:
  truths:
    - "upsertLead() em INSERT com fupEnabled=true persiste fupNextAt como Date concreto (não null)"
    - "upsertLead() em INSERT com fupEnabled=false mantém fupNextAt como null"
    - "upsertLead() em UPDATE nunca altera fupNextAt (campo ausente do set{})"
    - "FupScheduler._tick() processa lead recém-criado no próximo poll após fup_next_at <= NOW()"
    - "EVT-04 em REQUIREMENTS.md documenta exceção de formato de event_id para FUP events"
    - "Code comment em fup-scheduler.ts linha 222 documenta divergência intencional de EVT-04"
  artifacts:
    - path: "packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts"
      provides: "Testes do novo comportamento fupNextAt no INSERT"
      contains: "fupNextAt como Date (não null)"
    - path: "packages/core/src/leads/lead-service.ts"
      provides: "upsertLead() com cálculo e persistência de fupNextAt"
      contains: "getNextValidSlot"
    - path: "packages/core/src/fup/fup-scheduler.ts"
      provides: "Code comment EVT-04 divergência"
      contains: "diverge intencionalmente de EVT-04"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EVT-04 com nota de exceção FUP events"
      contains: "uniqueId:fup:step"
  key_links:
    - from: "packages/core/src/leads/lead-service.ts"
      to: "packages/core/src/fup/fup-scheduler.ts"
      via: "import { getNextValidSlot }"
      pattern: "getNextValidSlot"
    - from: "packages/core/src/leads/lead-service.ts"
      to: "leads.fupNextAt"
      via: "INSERT values({ fupNextAt })"
      pattern: "fupNextAt"
    - from: "FupScheduler._tick()"
      to: "leads.fupNextAt"
      via: "WHERE fup_next_at <= NOW()"
      pattern: "fup_next_at <= NOW()"
---

<objective>
Fechar o gap bloqueador FUP-02: leads criados com `fup_enabled = true` (Phase 25) têm `fup_next_at = NULL`,
e a cláusula `fup_next_at <= NOW()` do FupScheduler nunca avalia NULL como verdadeiro no PostgreSQL —
portanto esses leads jamais são processados pelo scheduler.

Purpose: Tornar o FUP automático operacional em produção para todos os leads criados com FUP ativado, sem intervenção manual.

Output:
- `upsertLead()` calcula e persiste `fupNextAt = getNextValidSlot(NOW() + intervals_seconds[0], ...)` no INSERT quando `fupEnabled = true`
- Testes unitários cobrindo os 4 cenários de fupNextAt no INSERT
- Code comment em `fup-scheduler.ts` linha ~222 documenta divergência EVT-04 (D-07)
- `REQUIREMENTS.md` EVT-04 atualizado com nota de exceção FUP events (D-06)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-fup-next-at-init-fix/26-CONTEXT.md
@.planning/phases/26-fup-next-at-init-fix/26-RESEARCH.md
@.planning/phases/26-fup-next-at-init-fix/26-VALIDATION.md

<interfaces>
<!-- Contratos relevantes que o executor deve usar diretamente. -->

De packages/core/src/leads/lead-service.ts (estado atual):
```typescript
// upsertLead() — estado após Phase 25
async upsertLead(numero: string, uniqueId: string, nome?: string, brainType?: string): Promise<Lead>

// Step 2 atual (a ser expandido):
const configRows = await this.db
  .select({ enabled: fupConfig.enabled })  // ← apenas enabled; Phase 26 expande
  .from(fupConfig)
  .where(eq(fupConfig.brainType, brainType))
  .limit(1);

if (configRows[0]?.enabled === true) {
  fupEnabled = true;
  // fupNextAt NÃO calculado aqui (Phase 25 D-05) ← GAP A FECHAR
}

// Step 3 INSERT (a ser estendido):
.values({
  numero,
  uniqueId,
  nome: nome ?? null,
  fupEnabled,
  // fupNextAt ausente ← ADICIONAR
})
.onConflictDoUpdate({
  target: leads.numero,
  set: {
    nome: nome ?? null,
    updatedAt: new Date(),
    // fupEnabled e fupNextAt ausentes do set{} ← MANTER ASSIM
  },
})
```

De packages/core/src/fup/fup-scheduler.ts (estado atual):
```typescript
// getNextValidSlot — linha 355, já exportada
export function getNextValidSlot(
  from: Date,
  minHour: number,
  maxHour: number,
  allowedDays: string[],
  timezone: string
): Date

// Code comment atual — linha 222 (a ser expandido conforme D-07):
event_id: `${lead.uniqueId}:fup:${lead.fupStep}`,  // D-17: idempotente por step
```

De packages/database/src/schema/tables.ts (campos disponíveis em fupConfig):
```typescript
// fupConfig table — campos relevantes para Phase 26
brainType: text("brain_type").primaryKey()
enabled: boolean("enabled").notNull().default(false)
intervalsSeconds: integer("intervals_seconds").array().notNull()
minHour: integer("min_hour").notNull()
maxHour: integer("max_hour").notNull()
allowedDays: text("allowed_days").array().notNull()
timezone: text("timezone").notNull()

// leads table — campo a ser populado
fupNextAt: timestamp("fup_next_at", { withTimezone: true })
```

Padrão de mock de bun:test existente em lead-service-fup.test.ts:
```typescript
// Pattern atual: mocka apenas db.update().set().where()
// Phase 26 precisa mockar db.select() chain (dois selects) + db.insert() chain
// Ver RESEARCH.md seção "Pitfall 4" para o padrão de mock completo com selectCallCount
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Adicionar stubs de teste para fupNextAt no INSERT em lead-service-fup.test.ts</name>
  <files>packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts</files>

  <read_first>
    - packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts (arquivo atual — ver estrutura de mocks de resetFup)
    - packages/core/src/leads/lead-service.ts (upsertLead() atual — entender o que mockar)
    - .planning/phases/26-fup-next-at-init-fix/26-RESEARCH.md (seção "Pitfall 4" — padrão de mock recomendado para upsertLead)
  </read_first>

  <behavior>
    - Test 1: `upsertLead() INSERT com fupEnabled=true persiste fupNextAt como Date (não null)` — lead não existe, fup_config retorna `enabled=true, intervalsSeconds=[3600], minHour=8, maxHour=18, allowedDays=['mon','tue','wed','thu','fri'], timezone='America/Sao_Paulo'`; espera `rows[0].fupNextAt instanceof Date` e `fupNextAt !== null`
    - Test 2: `upsertLead() INSERT sem fup_config mantém fupNextAt=null` — lead não existe, fup_config retorna `[]`; espera `fupNextAt` no INSERT como `null`
    - Test 3: `upsertLead() INSERT com intervals_seconds=[] mantém fupNextAt=null (guard Pitfall 2)` — lead não existe, fup_config retorna `enabled=true` mas `intervalsSeconds=[]`; espera que o guard previna cálculo e `fupNextAt` seja `null`
    - Test 4: `upsertLead() UPDATE não altera fupNextAt (campo ausente do set{})` — lead existe (isInsert=false), fup_config NÃO é consultada; INSERT nunca chamado; apenas `onConflictDoUpdate.set` executado sem `fupNextAt`
  </behavior>

  <action>
IMPORTANTE: Estes testes devem falhar (RED) antes da implementação da Task 2. O objetivo é descrever o comportamento esperado antecipadamente.

Adicionar ao final de `packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts` um novo describe block `describe("LeadService.upsertLead() — fupNextAt no INSERT", ...)`.

O mock para `upsertLead()` precisa simular dois `select()` chains encadeados. Usar contador de chamadas para distinguir o primeiro SELECT (busca lead existente) do segundo SELECT (busca fup_config). Usar o padrão `makeUpsertDbMock` descrito em RESEARCH.md Pitfall 4, adaptando para simular `db.insert().values().onConflictDoUpdate().returning()`.

Estrutura do helper `makeUpsertDbMock`:
```typescript
type FupConfigRow = {
  enabled: boolean;
  intervalsSeconds: number[];
  minHour: number;
  maxHour: number;
  allowedDays: string[];
  timezone: string;
};

type LeadRow = { numero: string; uniqueId: string; fupEnabled: boolean; fupNextAt: Date | null; [key: string]: unknown };

function makeUpsertDbMock(opts: {
  existing: LeadRow | null;  // null = INSERT path
  fupConfigRow: FupConfigRow | null;  // null = sem config
  insertResult: LeadRow;  // o que .returning() retorna
}) {
  // Chain: db.insert().values().onConflictDoUpdate().returning()
  const returningMock = mock(() => Promise.resolve([opts.insertResult]));
  const onConflictMock = mock(() => ({ returning: returningMock }));
  const valuesMock = mock(() => ({ onConflictDoUpdate: onConflictMock }));
  const insertMock = mock(() => ({ values: valuesMock }));

  // Chain: db.select().from().where().limit() — dois selects
  let selectCallCount = 0;
  const limitMock = mock(() => {
    selectCallCount++;
    if (selectCallCount === 1) {
      return Promise.resolve(opts.existing ? [opts.existing] : []);
    }
    // Segunda chamada: fup_config
    return Promise.resolve(opts.fupConfigRow ? [opts.fupConfigRow] : []);
  });
  const whereMock = mock(() => ({ limit: limitMock }));
  const fromMock = mock(() => ({ where: whereMock }));
  const selectMock = mock(() => ({ from: fromMock }));

  return { selectMock, insertMock, valuesMock, onConflictMock, returningMock, valuesMock };
}
```

Injetar mock no LeadService com:
```typescript
(service as unknown as { db: unknown }).db = {
  select: selectMock,
  insert: insertMock,
};
```

Para os testes, capturar os argumentos passados para `valuesMock` e verificar:
- Test 1: `valuesMock.mock.calls[0][0].fupNextAt instanceof Date` → true E `fupNextAt !== null`
- Test 2: `valuesMock.mock.calls[0][0].fupNextAt` → null
- Test 3: `valuesMock.mock.calls[0][0].fupNextAt` → null
- Test 4: `insertMock` não chamado (UPDATE path, mas o mock usa onConflictDoUpdate — verificar via returningMock que retorna lead existente; OU, alternativamente, testar que `valuesMock.mock.calls[0][0]` não contém `fupNextAt` diferente de null no UPDATE — ajustar conforme o padrão real)

NOTA: Como o Drizzle usa `INSERT ... ON CONFLICT DO UPDATE`, a `db.insert()` é sempre chamada (não há bifurcação no JS); o que muda é o que vai em `values()` (fupEnabled=false, fupNextAt=null no UPDATE path). O teste de UPDATE deve verificar que `valuesMock.mock.calls[0][0].fupEnabled === false` e `fupNextAt === null`.
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts 2>&1 | tail -20</automated>
  </verify>

  <done>
    Arquivo lead-service-fup.test.ts contém novo describe block "LeadService.upsertLead() — fupNextAt no INSERT" com 4 testes.
    Os 4 novos testes FALHAM (RED) pois upsertLead() ainda não calcula fupNextAt.
    Os 3 testes existentes de resetFup() continuam passando (sem regressão).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1): Implementar fupNextAt no INSERT em upsertLead()</name>
  <files>packages/core/src/leads/lead-service.ts</files>

  <read_first>
    - packages/core/src/leads/lead-service.ts (estado atual completo — ler antes de editar)
    - packages/core/src/fup/fup-scheduler.ts (linha 355-388 — assinatura de getNextValidSlot)
    - packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts (testes RED da Task 1 — guiam a implementação)
    - .planning/phases/26-fup-next-at-init-fix/26-RESEARCH.md (seção "Code Examples" — implementação referência)
  </read_first>

  <behavior>
    - Quando isInsert=true E brainType fornecido E fup_config.enabled=true E intervalsSeconds.length > 0:
      `fupNextAt = getNextValidSlot(new Date(Date.now() + intervalsSeconds[0]! * 1000), minHour, maxHour, allowedDays, timezone)`
    - Quando isInsert=true E fup_config ausente ou enabled=false ou intervalsSeconds=[]: `fupNextAt = null`
    - Quando isInsert=false (UPDATE): `fupEnabled=false, fupNextAt=null` em values() — campos ausentes do set{} de onConflictDoUpdate
  </behavior>

  <action>
Modificar `packages/core/src/leads/lead-service.ts` com as seguintes mudanças:

1. **Adicionar import** de `getNextValidSlot` (import direto, sem novo arquivo — D-05 Opção A):
```typescript
import { getNextValidSlot } from "../fup/fup-scheduler.js";
```
Adicionar logo abaixo dos imports existentes de drizzle-orm.

2. **Expandir Step 2** (query de fup_config) para incluir todos os campos de business hours (D-04):
```typescript
// Step 2: Query fup_config only on INSERT with brainType (D-02, D-04, Phase 26 D-01)
let fupEnabled = false; // default per leads table schema
let fupNextAt: Date | null = null; // Phase 26 D-01: calculado no INSERT quando fupEnabled=true

if (isInsert && brainType) {
  const configRows = await this.db
    .select({
      enabled: fupConfig.enabled,
      intervalsSeconds: fupConfig.intervalsSeconds,  // Phase 26 D-04: expandido
      minHour: fupConfig.minHour,                   // Phase 26 D-04
      maxHour: fupConfig.maxHour,                   // Phase 26 D-04
      allowedDays: fupConfig.allowedDays,           // Phase 26 D-04
      timezone: fupConfig.timezone,                 // Phase 26 D-04
    })
    .from(fupConfig)
    .where(eq(fupConfig.brainType, brainType))
    .limit(1);

  const config = configRows[0];
  // D-02: activate FUP only if config exists AND enabled = true AND intervals não-vazio (Pitfall 2)
  // D-04: silent when config missing — no warning, just default to false
  if (config?.enabled === true && config.intervalsSeconds.length > 0) {
    fupEnabled = true;
    // Phase 26 D-01: fupNextAt = NOW() + intervals_seconds[0], ajustado para business hours
    const rawNextAt = new Date(Date.now() + config.intervalsSeconds[0]! * 1000);
    fupNextAt = getNextValidSlot(
      rawNextAt,
      config.minHour,
      config.maxHour,
      config.allowedDays,
      config.timezone,
    );
  }
}
```

3. **Expandir Step 3** para incluir `fupNextAt` em values() (ausente do set{} de onConflictDoUpdate):
```typescript
// Step 3: Upsert with fupEnabled and fupNextAt (only affects INSERT)
const rows = await this.db
  .insert(leads)
  .values({
    numero,
    uniqueId,
    nome: nome ?? null,
    fupEnabled, // only used on INSERT; default (false) when UPDATE path
    fupNextAt,  // Phase 26 D-01: Date no INSERT com FUP ativo; null caso contrário
  })
  .onConflictDoUpdate({
    target: leads.numero,
    set: {
      // LEAD-02: uniqueId ausente do set — nunca sobrescrito após primeiro insert
      // D-03: fupEnabled ausente do set — preserva valor atual no UPDATE
      // Phase 26 D-01: fupNextAt ausente do set — INSERT-only; UPDATE nunca altera
      nome: nome ?? null,
      updatedAt: new Date(),
    },
  })
  .returning();
```

4. **Atualizar o JSDoc** do método `upsertLead()` para mencionar Phase 26:
```
 * - Phase 25: Se brainType fornecido E lead novo, consulta fup_config e ativa fup_enabled automaticamente.
 * - Phase 26 (D-01): Se fup_enabled ativado no INSERT, calcula e persiste fupNextAt = NOW() + intervals_seconds[0]
 *   ajustado para próximo slot de business hours — FupScheduler processa sem intervenção manual.
```

VERIFICAR após implementação: A assinatura da função `getNextValidSlot` em `fup-scheduler.ts` é:
`export function getNextValidSlot(from: Date, minHour: number, maxHour: number, allowedDays: string[], timezone: string): Date`
Garantir que os argumentos passados correspondem exatamente à assinatura.

VERIFICAR dependência circular: `fup-scheduler.ts` NÃO importa nada de `lead-service.ts` — sem ciclo. Confirmar via grep:
`grep -n "lead-service" packages/core/src/fup/fup-scheduler.ts` deve retornar vazio.
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/fup/lead-service-fup.test.ts 2>&1 | tail -20</automated>
  </verify>

  <done>
    Os 4 testes novos de upsertLead() passam (GREEN).
    Os 3 testes de resetFup() continuam passando.
    `bun tsc --noEmit` no packages/core retorna 0 erros.
    grep "getNextValidSlot" packages/core/src/leads/lead-service.ts retorna resultado.
    grep "fupNextAt" packages/core/src/leads/lead-service.ts retorna resultado nos valores do INSERT.
  </done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1): Atualizar code comment EVT-04 em fup-scheduler.ts e REQUIREMENTS.md</name>
  <files>
    packages/core/src/fup/fup-scheduler.ts
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    - packages/core/src/fup/fup-scheduler.ts (linha 218-235 — localizar o code comment de event_id atual em linha ~222)
    - .planning/REQUIREMENTS.md (linha 22-23 — texto atual de EVT-04 a ser atualizado)
    - .planning/phases/26-fup-next-at-init-fix/26-RESEARCH.md (seção "EVT-04 — Atualização de REQUIREMENTS.md" e "EVT-04 — Code comment em fup-scheduler.ts" — texto exato a usar)
  </read_first>

  <action>
MODIFICAÇÃO 1 — fup-scheduler.ts linha ~222:

Localizar a linha:
```typescript
event_id: `${lead.uniqueId}:fup:${lead.fupStep}`,  // D-17: idempotente por step
```

Substituir o comment por versão expandida que documenta a divergência com EVT-04 (D-07):
```typescript
event_id: `${lead.uniqueId}:fup:${lead.fupStep}`,
// D-17: formato diverge intencionalmente de EVT-04 (thread_id:tool_call_id).
// FUP events não têm tool_call_id — identificados por uniqueId:fup:step.
// Idempotente: mesmo step re-enviado produz o mesmo event_id.
```

MODIFICAÇÃO 2 — .planning/REQUIREMENTS.md linha ~22:

Localizar o texto atual de EVT-04:
```
- [x] **EVT-04**: Cada evento carrega `event_id` derivado de `thread_id:tool_call_id` para permitir deduplicação idempotente pelo consumidor
```

Substituir por (D-06 — adicionar nota de exceção FUP events):
```
- [x] **EVT-04**: Cada evento carrega `event_id` derivado de `thread_id:tool_call_id` para permitir deduplicação idempotente pelo consumidor. **Exceção FUP:** eventos de FUP usam `event_id = ${lead.uniqueId}:fup:${fup_step}` — FUP events não têm `tool_call_id` (D-17 da Phase 22, decisão intencional).
```

NENHUMA outra linha do REQUIREMENTS.md deve ser alterada. Usar Read para verificar linha exata antes de editar.
  </action>

  <verify>
    <automated>grep -n "diverge intencionalmente de EVT-04" packages/core/src/fup/fup-scheduler.ts && grep -n "uniqueId:fup:step" .planning/REQUIREMENTS.md</automated>
  </verify>

  <done>
    fup-scheduler.ts linha ~222-225 contém o comment expandido com "diverge intencionalmente de EVT-04".
    REQUIREMENTS.md EVT-04 contém a nota de exceção com "uniqueId:fup:step".
    Suite completa de testes FUP continua verde: `bun test packages/core/src/__tests__/unit/fup/` passa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB config → LeadService | `intervals_seconds`, `min_hour`, `max_hour`, `allowed_days`, `timezone` vêm do banco (tabela `fup_config`) — input operador-controlado, não externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | `fup_config.intervals_seconds` | accept | Array vem do banco, controlado pelo operador. Guard `intervalsSeconds.length > 0` previne cálculo com array vazio (Pitfall 2 do RESEARCH.md). `intervals_seconds[0]! * 1000` não aceita input externo. |
| T-26-02 | Denial of Service | `getNextValidSlot` loop (até 14*24 iterações) | accept | Loop limitado a 336 iterações máximas por design (fallback de 14 dias). Input vem do banco operador-controlado. Sem input externo. |
| T-26-03 | Information Disclosure | Log de `fupNextAt` | accept | `fupNextAt` é timestamp — sem PII. Logs existentes no LeadService não expõem payload do lead. Padrão T-22-03 (logar apenas uniqueId em warns) não afetado por esta mudança. |
| T-26-04 | Elevation of Privilege | Import circular `lead-service → fup-scheduler` | mitigate | Verificar via grep que `fup-scheduler.ts` não importa `lead-service.ts` antes de commitar. Se ciclo detectado, extrair `getNextValidSlot` para `scheduling-utils.ts` (Opção B de D-05). |
</threat_model>

<verification>
Verificações da fase completa (rodar após Task 3):

1. `bun test packages/core/src/__tests__/unit/fup/` — todos os testes passam (suite fup completa)
2. `bun test packages/core/` — suite completa do pacote verde
3. `grep -n "getNextValidSlot" packages/core/src/leads/lead-service.ts` — retorna resultado (import presente)
4. `grep -n "fupNextAt" packages/core/src/leads/lead-service.ts | grep values` — retorna resultado (fupNextAt no INSERT)
5. `grep -rn "import.*lead-service" packages/core/src/fup/fup-scheduler.ts` — retorna vazio (sem ciclo)
6. `grep -n "diverge intencionalmente de EVT-04" packages/core/src/fup/fup-scheduler.ts` — retorna resultado
7. `grep -n "uniqueId:fup:step" .planning/REQUIREMENTS.md` — retorna resultado
</verification>

<success_criteria>
Phase 26 completa quando:

1. `LeadService.upsertLead()` calcula e persiste `fupNextAt = getNextValidSlot(NOW() + intervals_seconds[0], minHour, maxHour, allowedDays, timezone)` no INSERT quando `fupEnabled = true`
2. Guard defensivo: `intervals_seconds = []` → `fupNextAt = null` (sem crash, sem Invalid Date)
3. UPDATE path: `fupNextAt` ausente do `set{}` — nunca sobrescrito em leads existentes
4. Testes unitários: 4 novos cenários em `lead-service-fup.test.ts` passam + 3 existentes sem regressão
5. `fup-scheduler.ts` linha ~222 documenta divergência com EVT-04 explicitamente
6. `REQUIREMENTS.md` EVT-04 contém nota de exceção FUP events com formato `uniqueId:fup:step`
7. `bun test packages/core/` verde sem regressões
8. Sem dependência circular: `fup-scheduler.ts` não importa `lead-service.ts`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/26-fup-next-at-init-fix/26-01-SUMMARY.md` com:
- O que foi implementado (fupNextAt no INSERT, guard Pitfall 2, import de getNextValidSlot)
- Decisão D-05: import direto escolhido (Opção A) — sem extração para scheduling-utils.ts
- Resultado dos testes (quantos passaram, nenhuma regressão)
- Known limitation documentada: segundo ciclo de FUP (após resetFup) ainda tem fup_next_at=NULL — DEFERRED conforme CONTEXT.md
- FUP-02 status: COMPLETE — leads criados com FUP ativado agora são processados pelo scheduler
</output>
