---
phase: 19-database-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - packages/core/src/leads/lead-service.ts
  - packages/core/src/leads/__tests__/lead-service.test.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/runner/__tests__/runner-fup.test.ts
autonomous: true
requirements:
  - FUP-06
must_haves:
  truths:
    - "LeadService tem método touchLastMessage(uniqueId: string): Promise<void> que atualiza last_message_at via db.update().set({ lastMessageAt: new Date() }).where(eq(leads.uniqueId, uniqueId))"
    - "BrainRunner.run() chama touchLastMessage(lead.uniqueId) ANTES do gate if (!lead.iaAtivada)"
    - "lead-service.test.ts cobre touchLastMessage() com mock db.update"
    - "runner-fup.test.ts verifica que touchLastMessage é chamado ANTES do gate ia_ativada"
    - "Migration 0007_v1_4_foundation aplicada sem erro contra banco real (verificação [BLOCKING])"
  artifacts:
    - path: "packages/core/src/leads/lead-service.ts"
      provides: "Método touchLastMessage() para FUP-06"
      contains: "async touchLastMessage"
    - path: "packages/core/src/runner/runner.ts"
      provides: "Chamada a touchLastMessage() antes do gate ia_ativada"
      contains: "touchLastMessage"
    - path: "packages/core/src/runner/__tests__/runner-fup.test.ts"
      provides: "Teste de integração BrainRunner + touchLastMessage"
  key_links:
    - from: "runner.ts"
      to: "lead-service.ts"
      via: "this.leadService.touchLastMessage(lead.uniqueId) chamado ANTES de if (!lead.iaAtivada)"
      pattern: "touchLastMessage.*lead.uniqueId"
    - from: "lead-service.ts"
      to: "leads (schema Drizzle)"
      via: "db.update(leads).set({ lastMessageAt }).where(eq(leads.uniqueId, uniqueId))"
      pattern: "lastMessageAt.*new Date"
---

<objective>
Implementar `LeadService.touchLastMessage()` e integrá-lo ao `BrainRunner.run()` antes do gate `ia_ativada`, satisfazendo FUP-06. Incluir testes unitários e a verificação [BLOCKING] de aplicação da migration contra banco real.

Purpose: Garantir que `last_message_at` é atualizado a cada mensagem recebida — mesmo quando `ia_ativada = false` — para que o scheduler de FUP da Phase 22 saiba quando o lead foi ativo pela última vez.

Output: LeadService com novo método, BrainRunner integrado, testes verdes, migration verificada em banco real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-database-foundation/19-CONTEXT.md
@.planning/phases/19-database-foundation/19-RESEARCH.md

@packages/core/src/leads/lead-service.ts
@packages/core/src/leads/__tests__/lead-service.test.ts
@packages/core/src/runner/runner.ts
@packages/core/src/runner/__tests__/brain-runner.test.ts
@packages/database/src/schema/tables.ts
</context>

<interfaces>
<!-- Interfaces existentes que o executor precisa conhecer -->

Padrão de método de update em LeadService (setFullpp como referência):
```typescript
async setFullpp(uniqueId: string, value: boolean): Promise<void> {
  await this.db
    .update(leads)
    .set({ fullpp: value, updatedAt: new Date() })
    .where(eq(leads.uniqueId, uniqueId));
}
```

Ponto de inserção em runner.ts (linhas 182-194 atuais):
```typescript
// D-06: Fluxo — upsert lead → gate ia_ativada → LLM (LEAD-02, LEAD-03)
const lead: Lead = await this.leadService.upsertLead(
  event.Numero,
  event.IDLead,
  event.Name
);

// D-04/D-05: Gate ia_ativada — retorna null silenciosamente (LEAD-03)
if (!lead.iaAtivada) {
  this.logger.debug({ numero: event.Numero }, "ia_ativada=false — ignoring message");
  return null;
}
```

Mock do @brain-pkg/database em lead-service.test.ts (DEVE ser atualizado com novas colunas):
```typescript
mock.module("@brain-pkg/database", () => ({
  leads: {
    numero: "leads.numero",
    uniqueId: "leads.unique_id",
    nome: "leads.nome",
    iaAtivada: "leads.ia_ativada",
    fullpp: "leads.fullpp",
    createdAt: "leads.created_at",
    updatedAt: "leads.updated_at",
    id: "leads.id",
    // ADICIONAR:
    lastMessageAt: "leads.last_message_at",
    fupEnabled: "leads.fup_enabled",
    fupStep: "leads.fup_step",
    fupNextAt: "leads.fup_next_at",
  },
}));
```

Novas colunas em tables.ts (criadas pelo Plan 01):
```typescript
fupEnabled: boolean('fup_enabled').notNull().default(false),
fupStep: integer('fup_step').notNull().default(0),
fupNextAt: timestamp('fup_next_at', { withTimezone: true }),
lastMessageAt: timestamp('last_message_at', { withTimezone: true }),
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar touchLastMessage() no LeadService + atualizar testes</name>
  <read_first>
    - packages/core/src/leads/lead-service.ts (arquivo COMPLETO — entender padrão de setFullpp e setIaAtivada)
    - packages/core/src/leads/__tests__/lead-service.test.ts (arquivo COMPLETO — entender mock existente do @brain-pkg/database e padrão de testes)
  </read_first>
  <files>
    packages/core/src/leads/lead-service.ts
    packages/core/src/leads/__tests__/lead-service.test.ts
  </files>
  <behavior>
    - Test 1: touchLastMessage() chama db.update(leads).set({ lastMessageAt: Date }) onde eq(leads.uniqueId, uniqueId)
    - Test 2: touchLastMessage() NÃO inclui updatedAt no set (distinção de D-11 — last_message_at é coluna especializada, não timestamp de atualização genérico)
    - Test 3: touchLastMessage() com uniqueId "lead-abc" chama where com leads.uniqueId
    - Testes existentes de setFullpp() e setIaAtivada() continuam passando após atualização do mock
  </behavior>
  <action>
**1. Adicionar `touchLastMessage()` ao `packages/core/src/leads/lead-service.ts`:**

Adicionar após o método `setIaAtivada()` (manter toda a estrutura existente intacta):

```typescript
/**
 * FUP-06: Atualiza last_message_at do lead para NOW() por uniqueId.
 * Chamado por BrainRunner.run() ANTES do gate ia_ativada — FUP-06 exige
 * atualização a cada mensagem recebida, inclusive quando ia_ativada=false.
 *
 * D-11: last_message_at é DISTINTO de updatedAt — rastreia especificamente
 * quando o humano enviou mensagem (não mudanças programáticas como setIaAtivada).
 * Por isso NÃO incluímos updatedAt no set desta operação.
 *
 * @param uniqueId - lead.uniqueId (IDLead canonical = thread_id do BrainRunner)
 */
async touchLastMessage(uniqueId: string): Promise<void> {
  await this.db
    .update(leads)
    .set({ lastMessageAt: new Date() })
    .where(eq(leads.uniqueId, uniqueId));
}
```

**2. Atualizar mock do `@brain-pkg/database` em `lead-service.test.ts`:**

No bloco `mock.module("@brain-pkg/database", ...)`, adicionar as novas colunas ao objeto `leads`:
```typescript
lastMessageAt: "leads.last_message_at",
fupEnabled: "leads.fup_enabled",
fupStep: "leads.fup_step",
fupNextAt: "leads.fup_next_at",
```

**3. Adicionar novo describe block em `lead-service.test.ts` para `touchLastMessage()`:**

Adicionar ao final do arquivo, seguindo o mesmo padrão do describe "métodos de atualização de lead":

```typescript
describe("LeadService — touchLastMessage (FUP-06)", () => {
  const mockWhere3 = mock(async () => []);
  const mockSet3 = mock(() => ({ where: mockWhere3 }));
  const mockUpdate3 = mock(() => ({ set: mockSet3 }));

  let service3: LeadService;

  beforeEach(() => {
    mockWhere3.mockClear();
    mockSet3.mockClear();
    mockUpdate3.mockClear();
    (mockDb as Record<string, unknown>).update = mockUpdate3;
    service3 = new LeadService({} as never);
  });

  it("touchLastMessage() chama db.update com { lastMessageAt: Date } onde eq(leads.uniqueId, uniqueId)", async () => {
    await service3.touchLastMessage("lead-abc");
    expect(mockUpdate3).toHaveBeenCalledTimes(1);
    expect(mockSet3).toHaveBeenCalledTimes(1);
    const setArg = mockSet3.mock.calls[0]?.[0] as Record<string, unknown>;
    expect(setArg).toHaveProperty("lastMessageAt");
    expect(setArg.lastMessageAt).toBeInstanceOf(Date);
  });

  it("touchLastMessage() NÃO inclui updatedAt no set (D-11: last_message_at é coluna especializada)", async () => {
    await service3.touchLastMessage("lead-abc");
    const setArg = mockSet3.mock.calls[0]?.[0] as Record<string, unknown>;
    expect(setArg).not.toHaveProperty("updatedAt");
  });

  it("touchLastMessage() chama where com leads.uniqueId (eq chamado com uniqueId correto)", async () => {
    await service3.touchLastMessage("lead-abc");
    expect(mockWhere3).toHaveBeenCalledTimes(1);
  });
});
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/leads/__tests__/lead-service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lead-service.ts contém `async touchLastMessage(uniqueId: string): Promise&lt;void&gt;`
    - Método usa `db.update(leads).set({ lastMessageAt: new Date() }).where(eq(leads.uniqueId, uniqueId))`
    - Todos os testes em lead-service.test.ts passam GREEN (incluindo os 3 novos de touchLastMessage)
    - Testes existentes de setFullpp e setIaAtivada continuam passando
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar touchLastMessage() ao BrainRunner.run() e criar teste runner-fup.test.ts</name>
  <read_first>
    - packages/core/src/runner/runner.ts (linhas 165-200 — área do fluxo upsertLead + gate ia_ativada)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (linhas 1-80 — padrão de mocks para BrainRunner)
    - packages/core/src/leads/lead-service.ts (verificar assinatura de touchLastMessage após Task 1)
  </read_first>
  <files>
    packages/core/src/runner/runner.ts
    packages/core/src/runner/__tests__/runner-fup.test.ts
  </files>
  <behavior>
    - Test 1: BrainRunner.run() com lead de iaAtivada=true chama touchLastMessage() com lead.uniqueId
    - Test 2: BrainRunner.run() com lead de iaAtivada=false AINDA chama touchLastMessage() (antes do gate — FUP-06)
    - Test 3: touchLastMessage() é chamado ANTES do retorno null do gate ia_ativada=false
  </behavior>
  <action>
**1. Modificar `packages/core/src/runner/runner.ts`:**

Localizar o bloco (linhas ~182-194):
```typescript
const lead: Lead = await this.leadService.upsertLead(
  event.Numero,
  event.IDLead,
  event.Name
);

// D-04/D-05: Gate ia_ativada — retorna null silenciosamente (LEAD-03)
// Segurança: iaAtivada vem do banco (upsert), nunca do payload externo
if (!lead.iaAtivada) {
  this.logger.debug({ numero: event.Numero }, "ia_ativada=false — ignoring message");
  return null;
}
```

Substituir por (inserindo chamada a touchLastMessage ANTES do gate):
```typescript
const lead: Lead = await this.leadService.upsertLead(
  event.Numero,
  event.IDLead,
  event.Name
);

// D-13, FUP-06: Atualizar last_message_at INCONDICIONALMENTE — antes do gate ia_ativada.
// FUP-06 exige que last_message_at seja atualizado a cada mensagem recebida,
// inclusive quando ia_ativada=false (scheduler de FUP precisa saber o último contato).
await this.leadService.touchLastMessage(lead.uniqueId);

// D-04/D-05: Gate ia_ativada — retorna null silenciosamente (LEAD-03)
// Segurança: iaAtivada vem do banco (upsert), nunca do payload externo
if (!lead.iaAtivada) {
  this.logger.debug({ numero: event.Numero }, "ia_ativada=false — ignoring message");
  return null;
}
```

**2. Criar `packages/core/src/runner/__tests__/runner-fup.test.ts`:**

Este teste verifica especificamente que touchLastMessage() é chamado ANTES do gate ia_ativada.
Seguir o padrão de mocks do brain-runner.test.ts existente.

```typescript
// FUP-06: BrainRunner.run() chama touchLastMessage() ANTES do gate ia_ativada
import { describe, it, expect, mock, beforeEach } from "bun:test";
type BrainEvent = { Name: string; Message: string; Numero: string; IDLead: string };

// --- Mocks (before any imports that use them) ---

const mockLoadPrompts = mock(async () => ({ system: "test prompt" }));
const mockUpsertPrompts = mock(async () => {});
mock.module("../../prompts/loader.js", () => ({
  loadPrompts: mockLoadPrompts,
  upsertPrompts: mockUpsertPrompts,
}));

class MockMemorySaver { storage: Record<string, unknown> = {}; }
class MockAIMessage { constructor(public content: string) {} }
class MockHumanMessage { constructor(public content: string) {} }

mock.module("@langchain/langgraph", () => ({
  MemorySaver: MockMemorySaver,
  StateGraph: class {},
  START: "__start__",
  END: "__end__",
}));

mock.module("@langchain/core/messages", () => ({
  AIMessage: MockAIMessage,
  HumanMessage: MockHumanMessage,
  BaseMessage: MockAIMessage,
}));

mock.module("@langchain/mcp-adapters", () => ({
  MultiServerMCPClient: class {
    async getTools() { return []; }
    async close() {}
  },
}));

const mockMemorySaver = new MockMemorySaver();
mock.module("@brain-pkg/ai", () => ({
  createCheckpointer: mock(async () => mockMemorySaver),
  createLLM: mock(async () => ({ invoke: mock(async () => new MockAIMessage("reply")) })),
  BrainStateAnnotation: {},
}));

mock.module("@brain-pkg/memory", () => ({
  MemoryManager: mock(function () {
    return {
      getContext: mock(async () => ({ profile: null, checkpoint: undefined, similarEmbeddings: [] })),
      saveContext: mock(async () => {}),
    };
  }),
}));

mock.module("@brain-pkg/observability", () => ({
  createLogger: mock(() => ({
    info: mock(() => {}),
    debug: mock(() => {}),
    warn: mock(() => {}),
    error: mock(() => {}),
  })),
  createTracingCallbacks: mock(() => []),
}));

mock.module("@brain-pkg/database", () => ({
  runMigrations: mock(async () => {}),
}));

mock.module("@brain-pkg/shared", () => ({
  ConfigurationError: class extends Error {},
  BrainOutputValidationError: class extends Error {},
}));

// Mock mockTouchLastMessage para rastrear chamadas
const mockTouchLastMessage = mock(async (_uniqueId: string) => {});
const mockUpsertLead = mock(async () => ({
  id: "uuid-1",
  uniqueId: "lead-fup-test",
  numero: "5511999990001",
  nome: "Test",
  iaAtivada: true,
  fullpp: null,
  idDeal: null,
  idContato: null,
  fupEnabled: false,
  fupStep: 0,
  fupNextAt: null,
  lastMessageAt: null,
  createdAt: new Date(),
  updatedAt: new Date(),
}));

mock.module("../../leads/lead-service.js", () => ({
  LeadService: mock(function () {
    return {
      upsertLead: mockUpsertLead,
      touchLastMessage: mockTouchLastMessage,
    };
  }),
}));

mock.module("drizzle-orm/postgres-js", () => ({
  drizzle: mock(() => ({})),
}));

// Mock do compiledGraph — retorna brainOutput válido
const mockBrainOutput = {
  reply: "ok",
  fullResponse: "ok",
  responseMode: "text",
  action: null,
};

const mockGetState = mock(async () => ({ values: { messages: [] } }));
const mockInvoke = mock(async () => ({
  brainOutput: mockBrainOutput,
  tokenUsage: { inputTokens: 10, outputTokens: 5, totalTokens: 15 },
}));
const mockCompiledGraph = {
  getState: mockGetState,
  invoke: mockInvoke,
};

const mockBrainOutputSchema = {
  parse: mock((v: unknown) => v),
};
mock.module("../output/schema.js", () => ({
  BrainOutputSchema: mockBrainOutputSchema,
}));

const mockBuildGraph = mock(() => ({
  compile: mock(() => mockCompiledGraph),
}));

import { BrainRunner } from "../runner.js";
import { ToolsRegistry } from "../../tools/registry.js";

const mockBrain = {
  id: "brain-fup-test",
  brainType: "sdr",
  promptKeys: ["system"],
  tools: [],
  defaultPrompts: undefined,
  buildGraph: mockBuildGraph,
};

describe("BrainRunner.run() — FUP-06: touchLastMessage antes do gate ia_ativada", () => {
  let runner: BrainRunner;
  const toolsRegistry = new ToolsRegistry();

  beforeEach(async () => {
    mockTouchLastMessage.mockClear();
    mockUpsertLead.mockClear();
    mockGetState.mockClear();
    mockInvoke.mockClear();

    process.env.DATABASE_URL = "postgres://test:test@localhost:5432/test";
    process.env.MIGRATIONS_FOLDER = "/tmp/fake-migrations";

    runner = new BrainRunner({
      brain: mockBrain as never,
      sql: {} as never,
      toolsRegistry,
      migrationsFolder: "/tmp/fake-migrations",
    });
    await runner.init();
  });

  it("touchLastMessage() é chamado quando lead.iaAtivada=true (caminho normal)", async () => {
    mockUpsertLead.mockResolvedValueOnce({
      id: "uuid-1", uniqueId: "lead-fup-test", numero: "5511999990001",
      nome: "Test", iaAtivada: true, fullpp: null, idDeal: null, idContato: null,
      fupEnabled: false, fupStep: 0, fupNextAt: null, lastMessageAt: null,
      createdAt: new Date(), updatedAt: new Date(),
    } as never);

    const event: BrainEvent = { Name: "Test", Message: "Olá", Numero: "5511999990001", IDLead: "lead-fup-test" };
    await runner.run(event as never);

    expect(mockTouchLastMessage).toHaveBeenCalledTimes(1);
    expect(mockTouchLastMessage.mock.calls[0]?.[0]).toBe("lead-fup-test");
  });

  it("touchLastMessage() é chamado MESMO quando lead.iaAtivada=false (FUP-06)", async () => {
    mockUpsertLead.mockResolvedValueOnce({
      id: "uuid-2", uniqueId: "lead-ia-off", numero: "5511999990002",
      nome: "Test2", iaAtivada: false, fullpp: null, idDeal: null, idContato: null,
      fupEnabled: false, fupStep: 0, fupNextAt: null, lastMessageAt: null,
      createdAt: new Date(), updatedAt: new Date(),
    } as never);

    const event: BrainEvent = { Name: "Test2", Message: "Oi", Numero: "5511999990002", IDLead: "lead-ia-off" };
    const result = await runner.run(event as never);

    // Gate retorna null (ia_ativada=false) MAS touchLastMessage foi chamado antes
    expect(result).toBeNull();
    expect(mockTouchLastMessage).toHaveBeenCalledTimes(1);
    expect(mockTouchLastMessage.mock.calls[0]?.[0]).toBe("lead-ia-off");
  });

  it("touchLastMessage() é chamado ANTES do gate: invoke() NÃO é chamado quando ia_ativada=false", async () => {
    mockUpsertLead.mockResolvedValueOnce({
      id: "uuid-3", uniqueId: "lead-ia-off-2", numero: "5511999990003",
      nome: "Test3", iaAtivada: false, fullpp: null, idDeal: null, idContato: null,
      fupEnabled: false, fupStep: 0, fupNextAt: null, lastMessageAt: null,
      createdAt: new Date(), updatedAt: new Date(),
    } as never);

    const event: BrainEvent = { Name: "Test3", Message: "Oi", Numero: "5511999990003", IDLead: "lead-ia-off-2" };
    await runner.run(event as never);

    // touchLastMessage chamado, invoke não (gate bloqueou)
    expect(mockTouchLastMessage).toHaveBeenCalledTimes(1);
    expect(mockInvoke).not.toHaveBeenCalled();
  });
});
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/runner/__tests__/runner-fup.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - runner.ts contém `await this.leadService.touchLastMessage(lead.uniqueId)` ANTES do `if (!lead.iaAtivada)`
    - runner-fup.test.ts existe com 3 testes GREEN
    - Teste 2 confirma touchLastMessage chamado mesmo quando iaAtivada=false (FUP-06)
    - Teste 3 confirma que invoke() não é chamado quando iaAtivada=false (gate ainda funciona)
    - `bun test packages/core` passa sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Verificar migration 0007 aplicada com sucesso contra banco real</name>
  <read_first>
    - packages/database/src/migrate.ts (API de runMigrations — entender como aplica migrations)
    - packages/database/src/migrations/0007_v1_4_foundation.sql (confirmar que existe após Plan 01)
    - packages/database/src/migrations/meta/_journal.json (confirmar entry idx=7)
  </read_first>
  <files>
    packages/database/src/__tests__/integration/migration-v14.test.ts
  </files>
  <action>
Esta task verifica que a migration se aplica sem erros em banco real. Execução em duas etapas:

**Etapa 1 — Verificação via script Bun (requer DATABASE_URL):**

Se DATABASE_URL está disponível no ambiente, criar e executar um script temporário em `/tmp/verify-migration-19.ts`:

```typescript
import postgres from 'postgres';
import { runMigrations } from './packages/database/src/migrate.ts';
import { join } from 'path';

const sql = postgres(process.env.DATABASE_URL!);
const migrationsFolder = join(import.meta.dir, 'packages/database/src/migrations');

try {
  await runMigrations(sql, migrationsFolder);
  console.log('✓ Migration 0007_v1_4_foundation applied successfully');

  // Verificar que as tabelas existem
  const [knowledgeChunks] = await sql`
    SELECT table_name FROM information_schema.tables
    WHERE table_schema = 'public' AND table_name = 'knowledge_chunks'
  `;
  console.log('✓ knowledge_chunks:', knowledgeChunks ? 'EXISTS' : 'MISSING');

  const [fupConfig] = await sql`
    SELECT table_name FROM information_schema.tables
    WHERE table_schema = 'public' AND table_name = 'fup_config'
  `;
  console.log('✓ fup_config:', fupConfig ? 'EXISTS' : 'MISSING');

  // Verificar colunas FUP em leads
  const leadsCols = await sql`
    SELECT column_name FROM information_schema.columns
    WHERE table_schema = 'public' AND table_name = 'leads'
    AND column_name IN ('fup_enabled', 'fup_step', 'fup_next_at', 'last_message_at')
    ORDER BY column_name
  `;
  console.log('✓ leads FUP columns:', leadsCols.map(r => r.column_name));

  if (leadsCols.length !== 4) {
    console.error('✗ MISSING COLUMNS:', 4 - leadsCols.length, 'columns not found');
    process.exit(1);
  }

  console.log('✓ ALL CHECKS PASSED — Phase 19 migration complete');
} catch (err) {
  console.error('✗ Migration failed:', err);
  process.exit(1);
} finally {
  await sql.end();
}
```

Executar: `bun run /tmp/verify-migration-19.ts`

**Etapa 2 — Verificação de idempotência (re-aplicar migration):**

Executar novamente para confirmar que o journal previne re-aplicação:
`bun run /tmp/verify-migration-19.ts`

Deve completar sem erro ("migration already applied" ou similar — não re-executar DDL).

**Se DATABASE_URL não está disponível:**

Reportar explicitamente: "DATABASE_URL não configurado — verificação manual necessária conforme VALIDATION.md:
- `psql $DATABASE_URL -c '\\d knowledge_chunks'` deve retornar schema da tabela
- `psql $DATABASE_URL -c '\\d fup_config'` deve retornar schema da tabela
- `psql $DATABASE_URL -c '\\d leads'` deve mostrar colunas fup_enabled, fup_step, fup_next_at, last_message_at"

**Após verificação bem-sucedida:**

Executar suite completa de testes:
```bash
bun test packages/database packages/core
```

Todos os testes devem passar GREEN.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database packages/core 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Script de verificação executou sem erros (ou verificação manual documentada)
    - Tabelas knowledge_chunks e fup_config existem no banco
    - Colunas fup_enabled, fup_step, fup_next_at, last_message_at existem em leads
    - Migration é idempotente (re-aplicação não falha)
    - `bun test packages/database packages/core` → todos os testes passam GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrainEvent (externo) → BrainRunner.run() | Payload de mensagem do lead entra via webhook/RabbitMQ; iaAtivada NUNCA vem do payload |
| LeadService → PostgreSQL | Queries de update de last_message_at; uniqueId vem do banco, não do payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Spoofing | touchLastMessage(uniqueId) | accept | uniqueId é derivado do upsertLead() que usa `leads.numero` como chave de upsert — nunca aceita uniqueId diretamente do payload externo. Sequência: payload.Numero → upsert → lead.uniqueId (do banco) → touchLastMessage. |
| T-19-06 | Tampering | BrainRunner.run() ordering | mitigate | Testes em runner-fup.test.ts verificam ordem de chamadas: touchLastMessage ANTES do gate. Code review obrigatório em PRs que modificam runner.ts. |
| T-19-07 | Denial of Service | touchLastMessage() em cada mensagem | accept | Uma query UPDATE por mensagem — custo O(1) com índice em leads.unique_id. Aceitável para volume SDR (não há rate limit por lead na arquitetura atual). |
| T-19-08 | Information Disclosure | runner-fup.test.ts mock de DATABASE_URL | mitigate | Testes usam valor fake "postgres://test:test@localhost:5432/test" — nunca logar process.env.DATABASE_URL em erros de teste (pattern existente no brain-runner.test.ts). |
</threat_model>

<verification>
Suite verde indica Phase 19 Plan 02 completo:

```bash
# Testes unitários
bun test packages/core/src/leads/__tests__/lead-service.test.ts
# Expected: todos os testes passam (incluindo 3 novos de touchLastMessage)

bun test packages/core/src/runner/__tests__/runner-fup.test.ts
# Expected: 3 testes passam (FUP-06: chamada antes do gate)

# Suite completa
bun test packages/database packages/core
# Expected: todos os testes passam

# Verificação de conteúdo
grep "touchLastMessage" packages/core/src/runner/runner.ts
# Expected: linha com await this.leadService.touchLastMessage(lead.uniqueId)

grep -A5 "touchLastMessage" packages/core/src/runner/runner.ts | grep -v "touchLastMessage"
# Expected: linha com if (!lead.iaAtivada) logo abaixo — confirma ordem correta
```
</verification>

<success_criteria>
1. `bun test packages/core/src/leads/__tests__/lead-service.test.ts` → todos passam (incluindo 3 novos)
2. `bun test packages/core/src/runner/__tests__/runner-fup.test.ts` → 3 testes passam
3. `bun test packages/database packages/core` → suite completa verde
4. `lead-service.ts` contém `async touchLastMessage(uniqueId: string): Promise<void>`
5. `runner.ts` contém `await this.leadService.touchLastMessage(lead.uniqueId)` ANTES de `if (!lead.iaAtivada)`
6. Migration aplicada no banco com knowledge_chunks, fup_config e 4 colunas em leads (ou verificação manual documentada)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/19-database-foundation/19-02-SUMMARY.md` com:
- Artefatos modificados (lead-service.ts, runner.ts, testes)
- Resultado da verificação de migration ([BLOCKING] status)
- Decisões D-12, D-13 implementadas
- Status final dos testes (bun test packages/database packages/core)
</output>
