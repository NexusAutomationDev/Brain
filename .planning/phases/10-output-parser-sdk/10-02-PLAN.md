---
phase: 10-output-parser-sdk
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - packages/ai/src/graph/state.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
autonomous: true
requirements:
  - PARSER-01
  - PARSER-02

must_haves:
  truths:
    - "BrainStateAnnotation tem campo brainOutput: BrainOutput | null com reducer last-write-wins"
    - "BrainRunner.run() retorna Promise<BrainOutput | null> — nunca mais { reply: string }"
    - "BrainRunner.run() lança BrainOutputValidationError quando brainOutput é null após invoke"
    - "BrainRunner.run() lança BrainOutputValidationError quando brainOutput falha no schema Zod"
    - "BrainRunResult é removido de runner.ts e do barrel de packages/core"
    - "Testes unitários do BrainRunner passam com novo contrato"
  artifacts:
    - path: "packages/ai/src/graph/state.ts"
      provides: "BrainStateAnnotation com campo brainOutput"
      contains: "brainOutput: Annotation<BrainOutput | null>"
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner.run() retornando Promise<BrainOutput | null>"
      contains: "Promise<BrainOutput | null>"
    - path: "packages/core/src/runner/__tests__/brain-runner.test.ts"
      provides: "Testes atualizados com mock de brainOutput e assertions corretas"
      contains: "result.fullResponse"
  key_links:
    - from: "packages/ai/src/graph/state.ts"
      to: "@brain-pkg/shared"
      via: "import type { BrainOutput } from '@brain-pkg/shared'"
      pattern: "import.*BrainOutput.*@brain-pkg/shared"
    - from: "packages/core/src/runner/runner.ts"
      to: "BrainOutputSchema"
      via: "BrainOutputSchema.parse(result.brainOutput)"
      pattern: "BrainOutputSchema.parse"
    - from: "packages/core/src/runner/runner.ts"
      to: "BrainOutputValidationError"
      via: "throw new BrainOutputValidationError"
      pattern: "BrainOutputValidationError"
---

<objective>
Aplica o contrato de saída às camadas AI e Core: adiciona `brainOutput` ao `BrainStateAnnotation` (packages/ai), substitui o retorno de `BrainRunner.run()` de `{ reply: string }` para `BrainOutput | null` com validação via `BrainOutputSchema.parse()`, remove `BrainRunResult`, e atualiza todos os testes unitários do runner.

Purpose: Fecha o ciclo do contrato estruturado — após esse plano, qualquer Brain que não setar `state.brainOutput` no grafo causará erro explícito em runtime.
Output: `state.ts` atualizado, `runner.ts` com novo retorno, testes do runner verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-output-parser-sdk/10-CONTEXT.md
@.planning/phases/10-output-parser-sdk/10-RESEARCH.md
@.planning/phases/10-output-parser-sdk/10-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes que o executor DEVE ler antes de modificar. -->

De packages/ai/src/graph/state.ts (arquivo a MODIFICAR — adicionar campo brainOutput):
```typescript
import { Annotation, messagesStateReducer } from "@langchain/langgraph";
import type { BaseMessage } from "@langchain/core/messages";

export const BrainStateAnnotation = Annotation.Root({
  schema_version: Annotation<number>({ default: () => 1, reducer: (_, next) => next }),
  messages: Annotation<BaseMessage[]>({ reducer: messagesStateReducer, default: () => [] }),
  userId: Annotation<string>({ default: () => "", reducer: (_, next) => next }),
  sessionId: Annotation<string>({ default: () => "", reducer: (_, next) => next }),
  // NOVO CAMPO a adicionar aqui (D-09, D-10):
  // brainOutput: Annotation<BrainOutput | null>({ default: () => null, reducer: (_, next) => next }),
});

export type BrainState = typeof BrainStateAnnotation.State;
```

De packages/core/src/runner/runner.ts — trecho a SUBSTITUIR (Step 3 e return):
```typescript
// ATUAL (linhas 223-239):
const messages: BaseMessage[] = result.messages ?? [];
const lastAI = [...messages].reverse().find((m) => m._getType() === "ai");
const reply = typeof lastAI?.content === "string" ? lastAI.content : "";

await this.memoryManager.saveContext({
  userId: event.IDLead,
  profileKey: "context",
  profileValue: { lastUserMessage: event.Message, lastReply: reply, conversationId: threadId },
});

return { reply };
```

De packages/core/src/runner/runner.ts — imports a ADICIONAR:
```typescript
// Adicionar aos imports existentes do runner.ts:
import { BrainOutputSchema } from "../output/schema.js";
import { BrainOutputValidationError } from "@brain-pkg/shared";
import type { BrainOutput } from "@brain-pkg/shared";
```

TIPO BrainRunResult atual (a ser REMOVIDO de runner.ts):
```typescript
/** Return type of BrainRunner.run() — internal LangGraph state never leaks beyond this */
export interface BrainRunResult {
  reply: string;
}
```

Assinatura atual de run() (a ser ALTERADA):
```typescript
async run(event: BrainEvent): Promise<BrainRunResult | null>
```

De packages/core/src/runner/__tests__/brain-runner.test.ts — mock a ATUALIZAR:
```typescript
// ATUAL — buildGraph mock retorna invoke sem brainOutput:
buildGraph: mock(() => ({
  compile: mock(() => ({
    invoke: mock(async () => ({
      messages: [new HumanMessage("hello"), new AIMessage("test reply")],
    })),
    getState: mock(async () => ({ values: { messages: [] } })),
  })),
})) as unknown as IBrain["buildGraph"],

// NOVO — invoke deve incluir brainOutput no resultado:
buildGraph: mock(() => ({
  compile: mock(() => ({
    invoke: mock(async () => ({
      messages: [new HumanMessage("hello"), new AIMessage("test reply")],
      brainOutput: {
        fullResponse: "test reply",
        responseMode: "text",
      },
    })),
    getState: mock(async () => ({ values: { messages: [] } })),
  })),
})) as unknown as IBrain["buildGraph"],
```

GRAFO DE DEPENDÊNCIAS (crítico — D-11 requer que packages/ai importe de @brain-pkg/shared, NÃO de @brain-pkg/core):
- packages/ai depende de: @brain-pkg/shared (PERMITIDO)
- packages/ai NÃO depende de: @brain-pkg/core (PROIBIDO — criaria ciclo)
- packages/core depende de: @brain-pkg/ai e @brain-pkg/shared (AMBOS PERMITIDOS)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar brainOutput ao BrainStateAnnotation (D-09, D-10, D-11)</name>
  <read_first>
    - packages/ai/src/graph/state.ts (arquivo a modificar)
    - packages/ai/package.json (verificar se @brain-pkg/shared já é dependência — deve estar)
  </read_first>

  <action>
**PASSO 1 — Verificar que @brain-pkg/shared está em packages/ai/package.json:**
```bash
grep "@brain-pkg/shared" packages/ai/package.json
```
Se não aparecer: `cd packages/ai && bun add @brain-pkg/shared@workspace:*`

**PASSO 2 — Modificar packages/ai/src/graph/state.ts:**

Adicionar import no topo do arquivo:
```typescript
import type { BrainOutput } from "@brain-pkg/shared";
```

Adicionar campo `brainOutput` após `sessionId` em `BrainStateAnnotation`:
```typescript
  // SDK-06: D-09/D-10 — brainOutput: contrato de saída estruturado
  // last-write-wins: o nó do grafo define o valor; reducer descarta o anterior
  // Default: null — nó DEVE setar; BrainRunner valida e lança erro se null após invoke
  brainOutput: Annotation<BrainOutput | null>({
    default: () => null,
    reducer: (_, next) => next,
  }),
```

O arquivo final deve ter:
- Import `import type { BrainOutput } from "@brain-pkg/shared";` (import type para evitar dependência de runtime)
- Campo `brainOutput` no `Annotation.Root({ ... })`
- `BrainState` type alias permanece inalterado (inferido automaticamente)

**PASSO 3 — Verificar build:**
```bash
bun build packages/ai/src/index.ts --outdir /tmp/ai-check 2>&1 | head -20
```
  </action>

  <verify>
    <automated>grep -n "brainOutput" packages/ai/src/graph/state.ts && grep -n "BrainOutput" packages/ai/src/graph/state.ts</automated>
  </verify>

  <acceptance_criteria>
    - packages/ai/src/graph/state.ts contém `import type { BrainOutput } from "@brain-pkg/shared"`
    - packages/ai/src/graph/state.ts contém `brainOutput: Annotation<BrainOutput | null>`
    - packages/ai/src/graph/state.ts contém `default: () => null` para o campo brainOutput
    - packages/ai/src/graph/state.ts contém `reducer: (_, next) => next` para o campo brainOutput
    - O arquivo NÃO contém importação de `@brain-pkg/core` (ciclo proibido)
    - `bun test packages/ai/src 2>/dev/null || echo "no tests"` não produz erros de compilação
  </acceptance_criteria>

  <done>BrainStateAnnotation exporta campo brainOutput com reducer last-write-wins e default null; import de BrainOutput vem de @brain-pkg/shared sem criar ciclo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atualizar BrainRunner.run() — novo retorno, validação e remoção de BrainRunResult</name>
  <read_first>
    - packages/core/src/runner/runner.ts (arquivo a modificar — ler inteiro)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (arquivo a modificar — ler inteiro)
    - packages/core/src/index.ts (verificar que BrainRunResult já foi removido do barrel no Plano 01)
    - packages/core/src/output/schema.ts (schema e tipos criados no Plano 01)
  </read_first>

  <behavior>
    - runner.run() retorna `BrainOutput` quando grafo seta brainOutput válido
    - runner.run() retorna `null` quando `ia_ativada=false` (comportamento inalterado)
    - runner.run() lança `BrainOutputValidationError` quando `result.brainOutput` é null após invoke
    - runner.run() lança `BrainOutputValidationError` quando `result.brainOutput` falha no schema Zod
    - `result.fullResponse` e `result.responseMode` estão presentes no retorno bem-sucedido
    - `saveContext` usa `brainOutput.fullResponse` em vez de `reply` (variável que não existe mais)
    - Propriedade `reply` NÃO existe no retorno de runner.run()
  </behavior>

  <files>
    packages/core/src/runner/runner.ts,
    packages/core/src/runner/__tests__/brain-runner.test.ts
  </files>

  <action>
**PASSO 1 — Atualizar testes ANTES de modificar o runner (TDD RED → GREEN):**

No arquivo `packages/core/src/runner/__tests__/brain-runner.test.ts`:

1. Atualizar a função `makeBrain()` para incluir `brainOutput` no mock do invoke:
```typescript
function makeBrain(promptKeys = ["system"]): IBrain {
  return {
    id: "test-brain",
    brainType: "test",
    promptKeys,
    tools: [],
    buildGraph: mock(() => ({
      compile: mock(() => ({
        invoke: mock(async () => ({
          messages: [
            new HumanMessage("hello"),
            new AIMessage("test reply"),
          ],
          // SDK-06: D-08 — nó do grafo seta brainOutput manualmente
          brainOutput: {
            fullResponse: "test reply",
            responseMode: "text",
          },
        })),
        getState: mock(async () => ({ values: { messages: [] } })),  // HIST-03
      })),
    })) as unknown as IBrain["buildGraph"],
  };
}
```

2. Atualizar os testes que assertam `result.reply` para assertar `result.fullResponse` e `result.responseMode`:

TESTE: "run(event) returns { reply: string } with the last AIMessage content"
→ Renomear para: "run(event) retorna BrainOutput com fullResponse e responseMode"
→ Substituir assertions:
```typescript
// ANTES:
expect(result).toHaveProperty("reply");
expect(typeof result.reply).toBe("string");
expect(result.reply).toBe("test reply");

// DEPOIS:
expect(result).not.toBeNull();
expect(result!.fullResponse).toBe("test reply");
expect(result!.responseMode).toBe("text");
```

TESTE: "run() returns only { reply } — no state leakage"
→ Renomear para: "run() retorna BrainOutput — sem vazamento de estado interno"
→ Substituir assertions:
```typescript
// ANTES:
expect(Object.keys(result)).toEqual(["reply"]);

// DEPOIS:
expect(result).toHaveProperty("fullResponse");
expect(result).toHaveProperty("responseMode");
expect(result).not.toHaveProperty("messages");  // T-05-03: estado interno não vaza
expect(result).not.toHaveProperty("sessionId");
```

3. Atualizar todos os outros testes que usam `result?.reply` para `result?.fullResponse`:
- "run() retorna { reply } quando iaAtivada=true" → assert `result?.fullResponse`
- Qualquer outro assert com `.reply` → substituir por `.fullResponse`

4. Adicionar 2 novos testes para D-14 (lançar erro quando brainOutput é inválido):
```typescript
test("run() lança BrainOutputValidationError quando brainOutput é null após invoke (D-14)", async () => {
  // Brain que não seta brainOutput — resultado do invoke não tem brainOutput
  const brainSemOutput: IBrain = {
    ...makeBrain(),
    buildGraph: mock(() => ({
      compile: mock(() => ({
        invoke: mock(async () => ({
          messages: [new HumanMessage("hello"), new AIMessage("reply")],
          brainOutput: null,  // nó não setou brainOutput
        })),
        getState: mock(async () => ({ values: { messages: [] } })),
      })),
    })) as unknown as IBrain["buildGraph"],
  };
  const runner = new BrainRunner({ brain: brainSemOutput, sql: {} as never, toolsRegistry: registry });
  await runner.init();

  await expect(runner.run(makeEvent())).rejects.toThrow();
});

test("run() lança BrainOutputValidationError quando brainOutput tem schema inválido (D-14)", async () => {
  const brainOutputInvalido: IBrain = {
    ...makeBrain(),
    buildGraph: mock(() => ({
      compile: mock(() => ({
        invoke: mock(async () => ({
          messages: [new HumanMessage("hello"), new AIMessage("reply")],
          brainOutput: { fullResponse: "", responseMode: "text" },  // fullResponse vazia falha no Zod
        })),
        getState: mock(async () => ({ values: { messages: [] } })),
      })),
    })) as unknown as IBrain["buildGraph"],
  };
  const runner = new BrainRunner({ brain: brainOutputInvalido, sql: {} as never, toolsRegistry: registry });
  await runner.init();

  await expect(runner.run(makeEvent())).rejects.toThrow();
});
```

Rodar testes (RED esperado — runner ainda retorna `{ reply }`):
```bash
bun test packages/core/src/runner/__tests__/brain-runner.test.ts
```

**PASSO 2 — Modificar packages/core/src/runner/runner.ts (GREEN):**

Adicionar imports no topo do arquivo (após os imports existentes):
```typescript
import { BrainOutputSchema } from "../output/schema.js";
import { BrainOutputValidationError } from "@brain-pkg/shared";
import type { BrainOutput } from "@brain-pkg/shared";
```

Remover a interface `BrainRunResult` (D-13):
```typescript
// REMOVER COMPLETAMENTE estas linhas:
/** Return type of BrainRunner.run() — internal LangGraph state never leaks beyond this */
export interface BrainRunResult {
  reply: string;
}
```

Alterar a assinatura do método `run()`:
```typescript
// ANTES:
async run(event: BrainEvent): Promise<BrainRunResult | null> {
// DEPOIS:
async run(event: BrainEvent): Promise<BrainOutput | null> {
```

Atualizar o comentário JSDoc do método run():
```typescript
/**
 * D-12: Run a single conversation turn.
 * Returns BrainOutput — structured output contract (fullResponse + responseMode).
 * Returns null when ia_ativada=false (LEAD-03).
 * Throws BrainOutputValidationError if brainOutput is null or fails BrainOutputSchema.
 */
```

Substituir Step 3 e return (linhas 223-239 do arquivo original):
```typescript
    // Step 3: Validate structured output (SDK-06, D-12, D-14)
    // O nó do grafo DEVE setar state.brainOutput — BrainRunner valida e lança erro se ausente.
    // Pitfall 3: BrainOutputSchema.parse() lança ZodError — re-lançar como BrainOutputValidationError.
    const rawOutput = result.brainOutput;
    if (rawOutput === null || rawOutput === undefined) {
      throw new BrainOutputValidationError(
        "Brain graph returned null brainOutput — node must set state.brainOutput before '__end__'",
        { brainId: this.brain.id, threadId }
      );
    }

    let brainOutput: BrainOutput;
    try {
      brainOutput = BrainOutputSchema.parse(rawOutput);
    } catch (err) {
      // Re-lançar como BrainOutputValidationError para catch específico em handler.ts
      const zodMessage = err instanceof Error ? err.message : String(err);
      throw new BrainOutputValidationError(
        `BrainOutput schema validation failed: ${zodMessage}`,
        { brainId: this.brain.id, threadId, rawOutput }
      );
    }

    // Step 4: Persist long-term memory after the turn (MEM-04)
    // Pitfall 5: usar brainOutput.fullResponse em vez de 'reply' (variável removida)
    await this.memoryManager.saveContext({
      userId: event.IDLead,
      profileKey: "context",
      profileValue: {
        lastUserMessage: event.Message,
        lastReply: brainOutput.fullResponse,
        conversationId: threadId,
      },
    });

    return brainOutput;
```

Remover também as linhas que não são mais necessárias:
- `const messages: BaseMessage[] = result.messages ?? [];` (Step 3 antigo)
- `const lastAI = [...messages].reverse().find((m) => m._getType() === "ai");` (Step 3 antigo)
- `const reply = typeof lastAI?.content === "string" ? lastAI.content : "";` (Step 3 antigo)
- Import `BaseMessage` se não for mais usado em outro lugar no arquivo

ATENÇÃO: Verificar se `BaseMessage` ainda é necessário. No arquivo atual, é usado em:
- `const historicalMessages: BaseMessage[] = snapshot?.values?.messages ?? [];` (linha 188)
Se ainda for usado, manter o import.

**PASSO 3 — Rodar testes (GREEN):**
```bash
bun test packages/core/src/runner/__tests__/brain-runner.test.ts
```

**PASSO 4 — Rodar suite completa do core para detectar regressões:**
```bash
bun test packages/core/src
```
  </action>

  <verify>
    <automated>bun test packages/core/src/runner/__tests__/brain-runner.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - packages/core/src/runner/runner.ts NÃO contém `interface BrainRunResult`
    - packages/core/src/runner/runner.ts contém `async run(event: BrainEvent): Promise<BrainOutput | null>`
    - packages/core/src/runner/runner.ts contém `BrainOutputSchema.parse(rawOutput)`
    - packages/core/src/runner/runner.ts contém `throw new BrainOutputValidationError(`
    - packages/core/src/runner/runner.ts contém `lastReply: brainOutput.fullResponse`
    - packages/core/src/runner/runner.ts NÃO contém `const reply = typeof lastAI`
    - packages/core/src/runner/runner.ts NÃO contém `return { reply }`
    - packages/core/src/runner/__tests__/brain-runner.test.ts contém `result!.fullResponse`
    - packages/core/src/runner/__tests__/brain-runner.test.ts NÃO contém `result.reply` (exceto em comentários)
    - `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` passa todos os testes
    - `bun test packages/core/src` passa sem regressões
  </acceptance_criteria>

  <done>BrainRunner.run() retorna Promise&lt;BrainOutput | null&gt;; BrainRunResult removido; validação com BrainOutputSchema.parse() + BrainOutputValidationError; todos os testes do runner verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph state → BrainRunner | result.brainOutput vem do grafo — origem não confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | BrainRunner.run() | mitigate | `BrainOutputSchema.parse()` valida estrutura antes de retornar — schema mismatch lança erro |
| T-10-05 | Denial of Service | BrainRunner.run() | mitigate | null check antes de parse() lança BrainOutputValidationError explícito — nunca retorno silencioso |
| T-10-06 | Information Disclosure | BrainRunner.run() | mitigate | BrainOutputValidationError em context inclui rawOutput apenas internamente — handler.ts não repassa ao cliente |
</threat_model>

<verification>
Verificação final do Plano 02:
1. `bun test packages/core/src` — todos os testes verdes, sem regressões
2. `grep "Promise<BrainOutput | null>" packages/core/src/runner/runner.ts` — confirma nova assinatura
3. `grep "BrainRunResult" packages/core/src/runner/runner.ts` — confirmar resultado vazio (removido)
4. `grep "brainOutput" packages/ai/src/graph/state.ts` — confirma campo no state
5. `grep "BrainOutputValidationError" packages/core/src/runner/runner.ts` — confirma throw
</verification>

<success_criteria>
- BrainStateAnnotation exporta campo `brainOutput: BrainOutput | null` com last-write-wins reducer
- BrainRunner.run() retorna `Promise<BrainOutput | null>` (nunca `{ reply: string }`)
- BrainRunResult interface removida de runner.ts
- Lança BrainOutputValidationError quando brainOutput é null ou inválido
- saveContext usa brainOutput.fullResponse
- Testes do runner atualizados e verdes (sem `result.reply`)
- Suite completa do core verde (sem regressões)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-output-parser-sdk/10-02-SUMMARY.md`
</output>
