---
phase: 10-output-parser-sdk
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
files_modified:
  - apps/brain-echo/src/brain.ts
  - apps/brain-echo/src/__tests__/unit/brain.test.ts
  - packages/transport/src/webhook/handler.ts
  - packages/transport/src/webhook/handler.test.ts
autonomous: true
requirements:
  - PARSER-02

must_haves:
  truths:
    - "brain-echo compila com o novo contrato BrainOutput"
    - "Nó 'llm' do brain-echo seta state.brainOutput com responseMode: 'text'"
    - "Testes unitários do brain-echo passam com novo contrato"
    - "IBrainRunnerLike em transport usa duck typing para BrainOutput sem importar @brain-pkg/core"
    - "handler.ts extrai result.fullResponse (não result.reply) para a resposta HTTP"
    - "Resposta HTTP do webhook mantém campo reply: string (sem breaking change na API pública)"
    - "Testes do handler.ts passam com o novo mock do runner"
  artifacts:
    - path: "apps/brain-echo/src/brain.ts"
      provides: "Nó 'llm' setando brainOutput no retorno do estado"
      contains: "brainOutput:"
    - path: "packages/transport/src/webhook/handler.ts"
      provides: "IBrainRunnerLike atualizada com duck typing; handler usa result.fullResponse"
      contains: "fullResponse: string"
    - path: "packages/transport/src/webhook/handler.test.ts"
      provides: "Testes atualizados com mock retornando BrainOutput"
      contains: "fullResponse"
  key_links:
    - from: "apps/brain-echo/src/brain.ts"
      to: "BrainStateAnnotation.brainOutput"
      via: "retorno do nó 'llm' inclui { messages, brainOutput }"
      pattern: "brainOutput:.*responseMode.*text"
    - from: "packages/transport/src/webhook/handler.ts"
      to: "result.fullResponse"
      via: "handler extrai fullResponse e retorna como reply na HTTP response"
      pattern: "reply: result.fullResponse"
---

<objective>
Migra brain-echo para setar `state.brainOutput` no nó "llm" e atualiza o transport para usar duck typing compatível com `BrainOutput`. Após esse plano, a Fase 10 está completa: todos os 4 critérios de success do ROADMAP §Phase 10 são satisfeitos.

Purpose: Fecha o ciclo de migração — brain-echo é o único Brain migrado na Fase 10 (brain-sdr migra na Fase 12); transport usa duck typing para não criar ciclo de dependência.
Output: brain-echo compila e passa nos testes; handler.ts usa result.fullResponse; nenhuma regressão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-output-parser-sdk/10-CONTEXT.md
@.planning/phases/10-output-parser-sdk/10-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes que o executor DEVE ler antes de modificar. -->

De apps/brain-echo/src/brain.ts — nó "llm" ATUAL (retorno sem brainOutput):
```typescript
.addNode("llm", async (state) => {
  const contextWindowSize = (() => {
    const n = parseInt(process.env.CONTEXT_WINDOW_MESSAGES ?? "40", 10);
    return n > 0 && isFinite(n) ? n : 40;
  })();
  const messagesForLLM = state.messages.slice(-contextWindowSize);

  const response = await ctx.llm.invoke([
    { role: "system", content: ctx.prompts["system"] },
    ...messagesForLLM,
  ]);
  return { messages: [...state.messages, response] };  // ← SEM brainOutput
})
```

O nó MODIFICADO deve retornar:
```typescript
return {
  messages: [...state.messages, response],
  brainOutput: {
    fullResponse: typeof response.content === "string" ? response.content : "",
    responseMode: "text" as const,
  },
};
```

ATENÇÃO sobre fullResponse vazio: BrainOutputSchema exige `fullResponse.length >= 1`. Se o LLM retornar content não-string (ex: tool_calls array), o Brain falha com BrainOutputValidationError no runner. Para brain-echo (text-only), isso é aceitável — registrar o risco mas não contornar com fallback silencioso.

De packages/transport/src/webhook/handler.ts — IBrainRunnerLike ATUAL:
```typescript
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{ reply: string } | null>;
}
```

IBrainRunnerLike NOVO (duck typing — sem importar @brain-pkg/core para evitar ciclo):
```typescript
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{
    fullResponse: string;
    responseMode: string;
    mediaType?: string;
    mediaUrl?: string;
  } | null>;
}
```

De packages/transport/src/webhook/handler.ts — linha que usa result (linha 77):
```typescript
// ATUAL:
return c.json({ status: "ok", reply: result.reply });
// NOVO (mantém campo 'reply' na resposta HTTP — sem breaking change na API pública):
return c.json({ status: "ok", reply: result.fullResponse });
```

De packages/transport/src/webhook/handler.test.ts — mock ATUAL que usa `{ reply: ... }`:
```typescript
const mockRunner = {
  run: async (_event: unknown) => ({ reply: "Olá! Posso te ajudar." }),
};
```

Mock NOVO compatível com IBrainRunnerLike atualizada:
```typescript
const mockRunner = {
  run: async (_event: unknown) => ({
    fullResponse: "Olá! Posso te ajudar.",
    responseMode: "text" as const,
  }),
};
```

De apps/brain-echo/src/__tests__/unit/brain.test.ts — testes existentes relevantes:
```typescript
test("buildGraph(ctx) retorna StateGraph (tem método addNode)", async () => {
  // Este teste não invoca o nó — continua válido sem alteração
});

test("nó do grafo invoca LLM com slice das mensagens quando CONTEXT_WINDOW_MESSAGES=2", async () => {
  // Este teste acessa nodeHandler e chama await nodeHandler(fakeState)
  // Após a mudança, o retorno incluirá brainOutput — não quebra o teste (não asserta o retorno)
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar brain-echo — nó "llm" seta state.brainOutput (D-07, D-08)</name>
  <read_first>
    - apps/brain-echo/src/brain.ts (arquivo a modificar)
    - apps/brain-echo/src/__tests__/unit/brain.test.ts (verificar quais testes podem ser afetados)
    - packages/ai/src/graph/state.ts (confirmar que brainOutput está no state — do Plano 02)
  </read_first>

  <action>
**PASSO 1 — Modificar apps/brain-echo/src/brain.ts:**

No nó "llm", substituir o `return` para incluir `brainOutput`:

```typescript
// D-07: LLM retorna string, nó monta BrainOutput manualmente (sem .withStructuredOutput())
// D-08: Fluxo: llm.invoke() → extrai content como string → monta { fullResponse, responseMode } → seta state.brainOutput
const fullResponse =
  typeof response.content === "string" ? response.content : "";

return {
  messages: [...state.messages, response],
  brainOutput: {
    fullResponse,
    responseMode: "text" as const,  // brain-echo é text-only em v1.2
  },
};
```

O nó completo após a modificação deve ficar:
```typescript
.addNode("llm", async (state) => {
  // D-01: system prompt carregado de ctx.prompts['system'] (nunca hardcoded)
  // D-03: ctx.llm já configurado pelo BrainRunner — não criar LLM aqui

  // HIST-03: Limitar mensagens enviadas ao LLM — histórico completo fica no PostgresSaver
  const contextWindowSize = (() => {
    const n = parseInt(process.env.CONTEXT_WINDOW_MESSAGES ?? "40", 10);
    return n > 0 && isFinite(n) ? n : 40;  // SECURITY: T-08-ENV
  })();
  const messagesForLLM = state.messages.slice(-contextWindowSize);

  const response = await ctx.llm.invoke([
    { role: "system", content: ctx.prompts["system"] },
    ...messagesForLLM,
  ]);

  // SDK-06: D-07, D-08 — nó monta BrainOutput manualmente; sem .withStructuredOutput()
  const fullResponse =
    typeof response.content === "string" ? response.content : "";

  return {
    messages: [...state.messages, response],
    brainOutput: {
      fullResponse,
      responseMode: "text" as const,  // brain-echo é text-only em v1.2
    },
  };
})
```

**PASSO 2 — Verificar que os testes unitários existentes continuam passando:**
```bash
bun test apps/brain-echo/src/__tests__/unit/brain.test.ts
```

Os testes existentes não assertam o retorno do nó — devem continuar passando.

Se algum teste falhar porque o nodeHandler retorna `{ messages, brainOutput }` em vez de `{ messages }`, verificar a assertion e atualizar apenas se necessário (não remover testes — apenas adaptar).

**PASSO 3 — Rodar suite completa do brain-echo (unit):**
```bash
bun test apps/brain-echo/src/__tests__/unit
```
  </action>

  <verify>
    <automated>bun test apps/brain-echo/src/__tests__/unit/brain.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - apps/brain-echo/src/brain.ts contém `brainOutput:` no retorno do nó "llm"
    - apps/brain-echo/src/brain.ts contém `responseMode: "text" as const`
    - apps/brain-echo/src/brain.ts contém `fullResponse` como variável local antes do return
    - apps/brain-echo/src/brain.ts NÃO usa `.withStructuredOutput()` (proibido por D-07)
    - `bun test apps/brain-echo/src/__tests__/unit/brain.test.ts` passa sem falha
    - O arquivo mantém todos os comentários de SECURITY e histórico existentes (não apagar contexto)
  </acceptance_criteria>

  <done>Nó "llm" do brain-echo retorna `{ messages, brainOutput: { fullResponse, responseMode: "text" } }`; testes unitários do brain passam.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar IBrainRunnerLike e handler.ts no transport (duck typing, sem ciclo)</name>
  <read_first>
    - packages/transport/src/webhook/handler.ts (arquivo a modificar — ler inteiro)
    - packages/transport/src/webhook/handler.test.ts (arquivo a modificar — ler inteiro)
    - packages/transport/package.json (verificar que NÃO tem @brain-pkg/core como dependência — não adicionar)
  </read_first>

  <action>
**PASSO 1 — Modificar packages/transport/src/webhook/handler.ts:**

Substituir a interface `IBrainRunnerLike`:
```typescript
/**
 * T-3-04-03: Local interface to avoid circular dependency.
 * packages/core imports from @brain-pkg/transport (for BrainEvent).
 * If handler.ts imported from @brain-pkg/core it would create a cycle:
 *   core → transport → core.
 * SDK-06: Duck typed to BrainOutput shape — structurally compatible with BrainRunner.run() return.
 * Mantém campo 'reply' na resposta HTTP para não quebrar clientes existentes do webhook.
 */
export interface IBrainRunnerLike {
  run(event: BrainEvent): Promise<{
    fullResponse: string;
    responseMode: string;
    mediaType?: string;
    mediaUrl?: string;
  } | null>;
}
```

Na linha que usa `result.reply` (dentro do `if (runner)` block):
```typescript
// ANTES:
return c.json({ status: "ok", reply: result.reply });

// DEPOIS (mantém 'reply' na resposta HTTP — API pública do webhook não muda):
return c.json({ status: "ok", reply: result.fullResponse });
```

NÃO modificar nada mais no handler.ts. O resto (auth, validation, error handling) permanece idêntico.

**PASSO 2 — Modificar packages/transport/src/webhook/handler.test.ts:**

Localizar o teste que usa `{ reply: "Olá! Posso te ajudar." }` e atualizar o mock:
```typescript
// ANTES:
const mockRunner = {
  run: async (_event: unknown) => ({ reply: "Olá! Posso te ajudar." }),
};

// DEPOIS (duck typed — compatível com nova IBrainRunnerLike):
const mockRunner = {
  run: async (_event: unknown) => ({
    fullResponse: "Olá! Posso te ajudar.",
    responseMode: "text",
  }),
};
```

O teste que asserta `body.reply` na HTTP response permanece válido — a resposta HTTP ainda tem `reply: result.fullResponse`:
```typescript
expect(body.reply).toBe("Olá! Posso te ajudar.");  // Continua funcionando — reply vem de fullResponse
```

**PASSO 3 — Rodar testes do transport:**
```bash
bun test packages/transport/src/webhook/handler.test.ts
```

**PASSO 4 — Rodar suite completa do transport:**
```bash
bun test packages/transport/src
```

**PASSO 5 — Rodar suite completa cruzada (core + ai + transport + brain-echo unit):**
```bash
bun test packages/core/src packages/ai/src packages/transport/src apps/brain-echo/src/__tests__/unit
```
  </action>

  <verify>
    <automated>bun test packages/transport/src/webhook/handler.test.ts && bun test apps/brain-echo/src/__tests__/unit</automated>
  </verify>

  <acceptance_criteria>
    - packages/transport/src/webhook/handler.ts contém `fullResponse: string` na interface IBrainRunnerLike
    - packages/transport/src/webhook/handler.ts contém `reply: result.fullResponse` na linha de resposta HTTP
    - packages/transport/src/webhook/handler.ts NÃO contém `reply: result.reply`
    - packages/transport/src/webhook/handler.ts NÃO importa de `@brain-pkg/core` (ciclo proibido)
    - packages/transport/src/webhook/handler.test.ts contém `fullResponse: "Olá! Posso te ajudar."` no mock
    - `bun test packages/transport/src/webhook/handler.test.ts` passa todos os testes
    - `bun test packages/core/src packages/ai/src packages/transport/src apps/brain-echo/src/__tests__/unit` passa sem falha
    - Resposta HTTP do webhook ainda tem campo `reply` (verificar via teste existente que asserta `body.reply`)
  </acceptance_criteria>

  <done>IBrainRunnerLike usa duck typing com fullResponse/responseMode; handler extrai result.fullResponse; resposta HTTP mantém campo reply; todos os testes verdes em core, ai, transport e brain-echo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain-echo nó → BrainStateAnnotation | Retorno do nó cruza para o state — BrainRunner valida via BrainOutputSchema |
| BrainRunner → WebhookTransport handler | result.fullResponse retornado como reply na HTTP response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | brain-echo nó "llm" | mitigate | BrainRunner.run() valida brainOutput via BrainOutputSchema.parse() — fullResponse vazia ou inválida lança BrainOutputValidationError antes de retornar ao cliente |
| T-10-08 | Information Disclosure | handler.ts HTTP response | accept | Campo `reply` na resposta HTTP contém apenas fullResponse (texto da resposta do LLM) — sem vazamento de state interno, thread_id, ou checkpoint data (T-05-03 mantido) |
| T-10-09 | Tampering | IBrainRunnerLike duck typing | accept | Duck typing estrutural — TypeScript garante em compile-time que qualquer instância passada a createWebhookApp() satisfaz o contrato fullResponse/responseMode |
</threat_model>

<verification>
Verificação final da Fase 10 completa:

1. `bun test packages/core/src/__tests__/unit/output` — BrainOutputSchema verde (PARSER-01)
2. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` — runner verde (PARSER-02)
3. `bun test apps/brain-echo/src/__tests__/unit` — brain-echo verde (PARSER-02 / Success Criteria 4)
4. `bun test packages/transport/src` — transport verde (sem regressões)
5. `grep "BrainOutputSchema" packages/core/src/index.ts` — exportado no barrel (Success Criteria 1)
6. `grep "Promise<BrainOutput | null>" packages/core/src/runner/runner.ts` — novo retorno (Success Criteria 2)
7. `grep "brainOutput" packages/ai/src/graph/state.ts` — campo no state (Success Criteria 2)
8. `grep "BrainRunResult" packages/core/src/runner/runner.ts` — vazio (removido — Success Criteria 3)
9. `grep "brainOutput" apps/brain-echo/src/brain.ts` — nó seta brainOutput (Success Criteria 4)

**Phase Gate:** Todos os 9 checks devem passar antes de `/gsd-verify-work`.
</verification>

<success_criteria>
Critérios de sucesso da Fase 10 (do ROADMAP §Phase 10):
1. `packages/core` exporta `BrainOutputSchema` (Zod) e o tipo `BrainOutput` com fullResponse, responseMode, mediaType, mediaUrl conforme D-01 a D-06
2. `BrainRunner.run()` rejeita (lança BrainOutputValidationError) qualquer saída do LangGraph que não valide contra BrainOutputSchema
3. A interface `IBrain.run()` NÃO existe — IBrain define apenas `buildGraph()`. O contrato é sobre o campo `brainOutput` no state, não sobre IBrain.run(). [Nota: ROADMAP diz "IBrain.run() retorna BrainOutput" mas IBrain não tem run() — o contrato real é BrainRunner.run() retornando BrainOutput]
4. brain-echo compila e seus testes passam com o novo contrato de saída
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-output-parser-sdk/10-03-SUMMARY.md`
</output>
