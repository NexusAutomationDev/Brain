---
phase: 10-output-parser-sdk
plan: 05
type: execute
wave: 5
depends_on:
  - 10-04-PLAN.md
files_modified:
  - .planning/phases/10-output-parser-sdk/10-VERIFICATION.md
autonomous: true
requirements:
  - PARSER-01
  - PARSER-02
gap_closure: true

must_haves:
  truths:
    - "VERIFICATION.md commitado com status gaps_closed e score 8/8"
    - "bun test packages/core/src/runner/__tests__/brain-runner.test.ts reporta 17 pass, 0 fail"
    - "Todos os 8 must-haves do VERIFICATION.md estão VERIFIED"
  artifacts:
    - path: ".planning/phases/10-output-parser-sdk/10-VERIFICATION.md"
      provides: "Relatório de verificação final com gaps fechados"
      contains: "status: gaps_closed"
  key_links:
    - from: "10-04-SUMMARY.md"
      to: "10-VERIFICATION.md"
      via: "re-verificação após gap closure"
      pattern: "gaps_closed"
---

<objective>
Re-verificar a Fase 10 após o gap closure do plano 10-04 para confirmar que os 2 gaps originais estão fechados, atualizar o VERIFICATION.md com status `gaps_closed` e commitar o arquivo (atualmente untracked). Nenhuma mudança de código é necessária — os testes já passam (17/17 unit tests do BrainRunner).

Purpose: Fechar o ciclo de verificação da Fase 10. O VERIFICATION.md foi criado pela verificação inicial mostrando `gaps_found`. O plano 10-04 fechou os gaps (removeu arquivos .js stale, corrigiu testes). Este plano documenta o resultado final e commita o artefato.
Output: VERIFICATION.md commitado com `status: gaps_closed`, `score: 8/8`, evidências atualizadas para cada truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-output-parser-sdk/10-VERIFICATION.md
@.planning/phases/10-output-parser-sdk/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar gap closure com suite de testes</name>
  <files>
    packages/core/src/runner/__tests__/brain-runner.test.ts
    packages/core/src/__tests__/unit/output/schema.test.ts
    packages/transport/src/__tests__/unit/
    apps/brain-echo/src/__tests__/unit/
  </files>
  <read_first>
    - packages/core/src/runner/__tests__/brain-runner.test.ts (confirmar que BrainOutputValidationError é importado e testado)
    - packages/shared/src/errors/index.ts (confirmar que BrainOutputValidationError está presente)
    - .planning/phases/10-output-parser-sdk/10-VERIFICATION.md (ler Truths 7 e 8 que falharam)
  </read_first>

  <action>
Executar a suite completa de testes da Fase 10 para confirmar que todos os gaps estão fechados.

**PASSO 1 — Testes unitários do BrainRunner (gap principal — Truths 7 e 8):**

```bash
bun test packages/core/src/runner/__tests__/brain-runner.test.ts
```

Resultado esperado: `17 pass, 0 fail`

Se falhar com qualquer erro, PARAR e reportar o erro. Não prosseguir para o passo 2.

**PASSO 2 — Testes do schema de output (Truths 1-3):**

```bash
bun test packages/core/src/__tests__/unit/output
```

Resultado esperado: `9 pass, 0 fail`

**PASSO 3 — Testes de transport e brain-echo (Truths 4-6):**

```bash
bun test packages/transport/src packages/shared/src apps/brain-echo/src/__tests__/unit
```

Resultado esperado: sem regressões nos testes que passavam antes.

**PASSO 4 — Confirmar que arquivos .js stale foram removidos (verificação do fix):**

```bash
ls packages/shared/src/errors/ packages/shared/src/types/ packages/shared/src/utils/
```

Nenhum arquivo `.js` ou `.js.map` deve estar listado. Apenas `.ts` (e opcionalmente `.d.ts`).

Registrar os resultados de cada passo para incluir no VERIFICATION.md atualizado.
  </action>

  <verify>
    <automated>bun test packages/core/src/runner/__tests__/brain-runner.test.ts 2>&1 | grep -E "pass|fail"</automated>
  </verify>

  <acceptance_criteria>
    - `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` reporta `17 pass, 0 fail`
    - `bun test packages/core/src/__tests__/unit/output` reporta `9 pass, 0 fail`
    - Nenhum arquivo `.js` ou `.js.map` existe em `packages/shared/src/errors/`, `packages/shared/src/types/`, `packages/shared/src/utils/`
    - Nenhuma regressão nos testes do transport e brain-echo
  </acceptance_criteria>

  <done>Suite de testes executada com sucesso — 17+9+outros testes passam, confirmando gap closure.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar VERIFICATION.md com status gaps_closed</name>
  <files>
    .planning/phases/10-output-parser-sdk/10-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/10-output-parser-sdk/10-VERIFICATION.md (ler inteiro — é necessário atualizar vários campos)
    - .planning/phases/10-output-parser-sdk/10-04-SUMMARY.md (ler para confirmar o que foi feito)
  </read_first>

  <action>
Atualizar o VERIFICATION.md para refletir o estado final após gap closure.

**Campos do frontmatter a atualizar:**

1. `status`: mudar de `gaps_found` para `gaps_closed`
2. `score`: mudar de `6/8 must-haves verified` para `8/8 must-haves verified`
3. `gaps`: substituir o bloco de gaps pela lista de gaps resolvidos:

```yaml
gaps_resolved:
  - truth: "BrainRunner.run() lança BrainOutputValidationError quando brainOutput é null após invoke"
    status: resolved
    fixed_by: "10-04-PLAN.md"
    resolution: "Remoção dos arquivos .js stale (errors/index.js, types/index.js, utils/index.js, index.js) de packages/shared/src/ e correção do arquivo de teste brain-runner.test.ts para usar mock.module() e DATABASE_URL fake"
    verified: "<data-hora atual>"

  - truth: "Testes unitários do BrainRunner passam com novo contrato"
    status: resolved
    fixed_by: "10-04-PLAN.md"
    resolution: "Consequência da remoção dos .js stale — bun agora resolve ./errors/index.js para ./errors/index.ts, expondo BrainOutputValidationError. 17 testes passam."
    verified: "<data-hora atual>"
```

**No corpo do relatório — seção Observable Truths, Truths 7 e 8:**

Mudar status de `FAILED` para `VERIFIED` e atualizar evidência:

| 7 | BrainRunner.run() lança BrainOutputValidationError quando brainOutput é null após invoke | VERIFIED | Plano 10-04 removeu arquivos .js stale de shared/src/ — bun resolve corretamente para .ts. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` → 17 pass, 0 fail |
| 8 | Testes unitários do BrainRunner passam com novo contrato | VERIFIED | `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` → 17 pass, 0 fail |

**Score:** atualizar para `8/8 truths verified`

**Seção Behavioral Spot-Checks — linha BrainRunner.run() tests:**

Mudar de:
```
| BrainRunner.run() tests | `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` | SyntaxError: BrainOutputValidationError not found | FAIL |
```

Para:
```
| BrainRunner.run() tests | `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` | 17 pass, 0 fail | PASS |
```

**Requirements Coverage — PARSER-02:**

Mudar de `PARTIAL` para `SATISFIED`:
```
| PARSER-02 | 10-02-PLAN.md, 10-03-PLAN.md, 10-04-PLAN.md | Todos os Brains retornam exclusivamente o formato estruturado | SATISFIED | Contrato definido, runner.ts implementado, testes do runner 17/17 verdes após gap closure (10-04) |
```

**Anti-Patterns Found — seção:**

Mover para uma seção "Anti-Patterns Resolved":
```
| packages/shared/src/errors/index.js | Arquivo .js compilado desatualizado em diretório src/ | RESOLVED | Removido por 10-04 — bun agora resolve para .ts |
| packages/shared/src/types/index.js | Arquivo .js desatualizado em src/ | RESOLVED | Removido por 10-04 |
```

**Adicionar no final:**

```markdown
## Gap Closure Summary

**Status final:** 8/8 truths verificadas — Fase 10 completa

Os 2 gaps identificados na verificação inicial foram fechados pelo plano 10-04:
1. **Arquivos .js stale removidos** — `packages/shared/src/errors/index.js` e outros removidos. Bun resolve `./errors/index.js` para `./errors/index.ts` corretamente.
2. **17 testes do BrainRunner passam** — Consequência direta da correção anterior + mocks para evitar conflito de zod v4 em bun 1.3.2.

---

_Re-verificado: <data-hora atual>_
_Plano de gap closure: 10-04-PLAN.md_
```
  </action>

  <verify>
    <automated>grep "status: gaps_closed" .planning/phases/10-output-parser-sdk/10-VERIFICATION.md && echo "OK: status atualizado"</automated>
  </verify>

  <acceptance_criteria>
    - `status: gaps_closed` presente no frontmatter do VERIFICATION.md
    - `score: 8/8 must-haves verified` presente no frontmatter
    - Truths 7 e 8 mostram `VERIFIED` na tabela Observable Truths
    - PARSER-02 mostra `SATISFIED` na tabela de Requirements Coverage
    - `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` mostra `17 pass, 0 fail` na linha de evidência
    - Seção "Gap Closure Summary" presente ao final do arquivo
  </acceptance_criteria>

  <done>VERIFICATION.md atualizado com status gaps_closed, score 8/8, todas as truths VERIFIED.</done>
</task>

<task type="auto">
  <name>Task 3: Commitar VERIFICATION.md</name>
  <files>
    .planning/phases/10-output-parser-sdk/10-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/10-output-parser-sdk/10-VERIFICATION.md (confirmar que status: gaps_closed está presente antes de commitar)
  </read_first>

  <action>
Commitar o VERIFICATION.md (atualmente untracked — `?? .planning/phases/10-output-parser-sdk/10-VERIFICATION.md`).

```bash
git add .planning/phases/10-output-parser-sdk/10-VERIFICATION.md
git commit -m "📝 docs(10): fechar gaps — VERIFICATION.md status gaps_closed 8/8"
```

Verificar o commit:

```bash
git show --stat HEAD
```

Confirmar que apenas `10-VERIFICATION.md` foi commitado.
  </action>

  <verify>
    <automated>git show --stat HEAD | grep "10-VERIFICATION.md" && echo "OK: VERIFICATION.md commitado"</automated>
  </verify>

  <acceptance_criteria>
    - `git status` não mostra `?? .planning/phases/10-output-parser-sdk/10-VERIFICATION.md` (arquivo não é mais untracked)
    - `git show --stat HEAD` mostra apenas `10-VERIFICATION.md` como arquivo modificado
    - Commit message segue o formato Conventional Commits com emoji
  </acceptance_criteria>

  <done>VERIFICATION.md commitado — Fase 10 encerrada com 8/8 truths verificadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git commit | Apenas VERIFICATION.md deve ser incluído — não commitar arquivos de código não relacionados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-20 | Tampering | VERIFICATION.md update | accept | Task 2 verifica grep antes de concluir — status incorreto é detectado |
| T-10-21 | Denial of Service | bun test suite | mitigate | Task 1 passo 1 para se falhar — não prossegue para documentação de resultado falso |
</threat_model>

<verification>
Verificação final do plano 10-05:

1. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` → `17 pass, 0 fail`
2. `grep "status: gaps_closed" .planning/phases/10-output-parser-sdk/10-VERIFICATION.md` → match
3. `grep "score: 8/8" .planning/phases/10-output-parser-sdk/10-VERIFICATION.md` → match
4. `git show --stat HEAD | grep "10-VERIFICATION.md"` → arquivo commitado
5. `git status` → nenhum untracked ou modified relevante

**Fase 10 completa quando:** 8/8 truths VERIFIED, VERIFICATION.md commitado, PARSER-01 e PARSER-02 SATISFIED.
</verification>

<success_criteria>
1. `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` → 17 pass, 0 fail
2. VERIFICATION.md tem `status: gaps_closed` e `score: 8/8 must-haves verified`
3. VERIFICATION.md commitado (não é mais untracked)
4. Nenhuma regressão nos outros 51 testes da fase
5. PARSER-02 marcado como SATISFIED no VERIFICATION.md
</success_criteria>
