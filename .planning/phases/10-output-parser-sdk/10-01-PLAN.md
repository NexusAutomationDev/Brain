---
phase: 10-output-parser-sdk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/index.ts
  - packages/shared/src/errors/index.ts
  - packages/core/src/output/schema.ts
  - packages/core/src/index.ts
  - packages/core/src/runner/runner.ts
  - packages/core/src/__tests__/unit/output/schema.test.ts
autonomous: true
requirements:
  - PARSER-01

must_haves:
  truths:
    - "BrainOutputSchema valida corretamente todos os 5 valores de responseMode"
    - "mediaType e mediaUrl são exigidos apenas para image, video e document"
    - "fullResponse vazia falha na validação com ZodError"
    - "BrainOutput type está disponível em @brain-pkg/shared sem ciclo de dependência"
    - "BrainOutputValidationError permite catch específico separado de ConfigurationError"
    - "packages/core exporta BrainOutputSchema, BrainOutput e ResponseMode no barrel"
  artifacts:
    - path: "packages/shared/src/types/index.ts"
      provides: "Interface TypeScript BrainOutput (sem Zod — evita ciclo packages/ai)"
      contains: "export interface BrainOutput"
    - path: "packages/shared/src/errors/index.ts"
      provides: "BrainOutputValidationError extends BrainError"
      contains: "BrainOutputValidationError"
    - path: "packages/core/src/output/schema.ts"
      provides: "BrainOutputSchema (Zod), ResponseModeSchema, ResponseMode type"
      exports:
        - "BrainOutputSchema"
        - "ResponseModeSchema"
        - "ResponseMode"
    - path: "packages/core/src/__tests__/unit/output/schema.test.ts"
      provides: "Testes unitários do BrainOutputSchema cobrindo PARSER-01"
      min_cases: 8
  key_links:
    - from: "packages/core/src/output/schema.ts"
      to: "packages/core/src/index.ts"
      via: "named export"
      pattern: "export.*BrainOutputSchema.*from.*output/schema"
    - from: "packages/shared/src/types/index.ts"
      to: "packages/core/src/output/schema.ts"
      via: "z.infer preserves structural compatibility"
      pattern: "BrainOutput.*fullResponse.*responseMode"
---

<objective>
Define o contrato de saída estruturado do SDK: cria o tipo TypeScript `BrainOutput` em `packages/shared` (sem Zod, evitando ciclo de dependência), o schema Zod `BrainOutputSchema` com validação condicional `mediaType`/`mediaUrl` em `packages/core`, a classe `BrainOutputValidationError`, e os testes unitários que cobrem PARSER-01.

Purpose: Estabelece o "source of truth" do contrato de saída — todos os planos seguintes dependem desse schema e desse tipo.
Output: `packages/shared/src/types/index.ts`, `packages/shared/src/errors/index.ts`, `packages/core/src/output/schema.ts`, `packages/core/src/index.ts` (atualizado), `packages/core/src/__tests__/unit/output/schema.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-output-parser-sdk/10-CONTEXT.md
@.planning/phases/10-output-parser-sdk/10-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor DEVE respeitar. Lidos diretamente do codebase. -->

De packages/shared/src/errors/index.ts (arquivo a MODIFICAR — adicionar nova classe APÓS ConfigurationError):
```typescript
export class BrainError extends Error {
  constructor(message: string, public readonly code: string, public readonly context?: Record<string, unknown>) {
    super(message);
    this.name = 'BrainError';
  }
}

export class ConfigurationError extends BrainError {
  constructor(message: string, context?: Record<string, unknown>) {
    super(message, 'CONFIGURATION_ERROR', context);
    this.name = 'ConfigurationError';
  }
}
```

De packages/shared/src/types/index.ts (arquivo a SUBSTITUIR — atualmente é placeholder):
```typescript
// Placeholder for shared types (will be populated in subsequent phases)
export {};
```

De packages/shared/src/index.ts (barrel — NÃO modificar, já re-exporta tudo de types e errors):
```typescript
export * from './types/index.js';
export * from './utils/index.js';
export * from './errors/index.js';
```

De packages/core/src/index.ts (arquivo a MODIFICAR — adicionar exports de output/ e remover BrainRunResult):
```typescript
export type { IBrain, BrainBuildContext } from "./brain/interface.js";
export { BrainRegistry } from "./brain/registry.js";
export { BrainRunner } from "./runner/runner.js";
export type { BrainRunnerOptions, BrainRunResult } from "./runner/runner.js";  // BrainRunResult SERÁ REMOVIDO neste plano via packages/core/src/runner/runner.ts
export { ToolsRegistry } from "./tools/registry.js";
export { loadPrompts, upsertPrompts } from "./prompts/loader.js";
export { createCoreApp } from "./server.js";
export { LeadService } from "./leads/lead-service.js";
export type { Lead } from "./leads/lead-service.js";
```

GRAFO DE DEPENDÊNCIAS (crítico — NÃO criar ciclos):
- packages/shared → depende de: nada (folha)
- packages/ai → depende de: @brain-pkg/shared (NÃO depende de @brain-pkg/core)
- packages/core → depende de: @brain-pkg/ai, @brain-pkg/shared (e outros)
- CONCLUSÃO: BrainOutput como interface TypeScript em packages/shared é a única opção sem ciclo
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar BrainOutput type em shared e BrainOutputSchema em core</name>
  <read_first>
    - packages/shared/src/types/index.ts (arquivo atual — substituir placeholder)
    - packages/shared/src/errors/index.ts (adicionar BrainOutputValidationError após ConfigurationError)
    - packages/shared/src/index.ts (verificar que re-exporta types e errors — NÃO modificar)
    - packages/core/src/output/schema.ts (NÃO existe — criar do zero)
    - packages/core/src/index.ts (adicionar exports de output/ e atualizar BrainRunResult)
    - packages/core/package.json (verificar se zod já é dependência direta)
  </read_first>

  <behavior>
    - BrainOutput interface com fullResponse: string, responseMode: ResponseMode, mediaType?: string, mediaUrl?: string
    - ResponseMode como type alias "text" | "image" | "audio" | "video" | "document"
    - BrainOutputSchema.parse({ fullResponse: "hello", responseMode: "text" }) → sucesso
    - BrainOutputSchema.parse({ fullResponse: "speak", responseMode: "audio" }) → sucesso (sem mediaType/mediaUrl)
    - BrainOutputSchema.parse({ fullResponse: "caption", responseMode: "image", mediaType: "image/jpeg", mediaUrl: "https://ex.com/img.jpg" }) → sucesso
    - BrainOutputSchema.parse({ fullResponse: "caption", responseMode: "image" }) → lança ZodError com issues em mediaType e mediaUrl
    - BrainOutputSchema.parse({ fullResponse: "caption", responseMode: "video" }) → lança ZodError (video requer mediaType + mediaUrl)
    - BrainOutputSchema.parse({ fullResponse: "doc", responseMode: "document", mediaType: "application/pdf" }) → lança ZodError (falta mediaUrl)
    - BrainOutputSchema.parse({ fullResponse: "", responseMode: "text" }) → lança ZodError (fullResponse não pode ser vazia)
    - BrainOutputSchema.parse({ fullResponse: "ok", responseMode: "invalid" }) → lança ZodError (enum inválido)
  </behavior>

  <files>
    packages/shared/src/types/index.ts,
    packages/shared/src/errors/index.ts,
    packages/core/src/output/schema.ts,
    packages/core/src/index.ts,
    packages/core/src/__tests__/unit/output/schema.test.ts
  </files>

  <action>
**PASSO 1 — Verificar se zod é dependência direta de packages/core:**
```bash
grep '"zod"' packages/core/package.json
```
Se NÃO aparecer: `cd packages/core && bun add zod`

**PASSO 2 — Escrever testes ANTES da implementação (TDD RED):**

Criar diretório e arquivo: `packages/core/src/__tests__/unit/output/schema.test.ts`

```typescript
// PARSER-01: BrainOutputSchema — validação de contrato de saída
import { describe, test, expect } from "bun:test";
import { BrainOutputSchema } from "../../../../output/schema.js";
import { ZodError } from "zod";

describe("BrainOutputSchema — PARSER-01", () => {
  test("text mode: fullResponse + responseMode são suficientes", () => {
    expect(() =>
      BrainOutputSchema.parse({ fullResponse: "hello", responseMode: "text" })
    ).not.toThrow();
    const result = BrainOutputSchema.parse({ fullResponse: "hello", responseMode: "text" });
    expect(result.fullResponse).toBe("hello");
    expect(result.responseMode).toBe("text");
  });

  test("audio mode: não exige mediaType nem mediaUrl (D-03: TTS signal)", () => {
    expect(() =>
      BrainOutputSchema.parse({ fullResponse: "fale isso", responseMode: "audio" })
    ).not.toThrow();
  });

  test("image mode com mediaType + mediaUrl: válido", () => {
    expect(() =>
      BrainOutputSchema.parse({
        fullResponse: "veja a imagem",
        responseMode: "image",
        mediaType: "image/jpeg",
        mediaUrl: "https://example.com/img.jpg",
      })
    ).not.toThrow();
  });

  test("image mode sem mediaType e mediaUrl: inválido — 2 issues (D-04)", () => {
    let error: ZodError | null = null;
    try {
      BrainOutputSchema.parse({ fullResponse: "veja a imagem", responseMode: "image" });
    } catch (e) {
      if (e instanceof ZodError) error = e;
    }
    expect(error).not.toBeNull();
    const paths = error!.issues.map((i) => i.path[0]);
    expect(paths).toContain("mediaType");
    expect(paths).toContain("mediaUrl");
  });

  test("video mode sem mediaUrl: inválido — issue em mediaUrl", () => {
    let error: ZodError | null = null;
    try {
      BrainOutputSchema.parse({ fullResponse: "assista", responseMode: "video", mediaType: "video/mp4" });
    } catch (e) {
      if (e instanceof ZodError) error = e;
    }
    expect(error).not.toBeNull();
    const paths = error!.issues.map((i) => i.path[0]);
    expect(paths).toContain("mediaUrl");
    expect(paths).not.toContain("mediaType");
  });

  test("document mode sem mediaType: inválido", () => {
    let error: ZodError | null = null;
    try {
      BrainOutputSchema.parse({ fullResponse: "veja o doc", responseMode: "document", mediaUrl: "https://example.com/doc.pdf" });
    } catch (e) {
      if (e instanceof ZodError) error = e;
    }
    expect(error).not.toBeNull();
    const paths = error!.issues.map((i) => i.path[0]);
    expect(paths).toContain("mediaType");
  });

  test("fullResponse vazia: inválido (D-03: TTS com string vazia causaria silêncio)", () => {
    expect(() =>
      BrainOutputSchema.parse({ fullResponse: "", responseMode: "text" })
    ).toThrow(ZodError);
  });

  test("responseMode inválido: inválido (enum guard D-02)", () => {
    expect(() =>
      BrainOutputSchema.parse({ fullResponse: "ok", responseMode: "invalid_mode" })
    ).toThrow(ZodError);
  });

  test("safeParse retorna success=false sem lançar (API alternativa)", () => {
    const result = BrainOutputSchema.safeParse({ fullResponse: "", responseMode: "text" });
    expect(result.success).toBe(false);
  });
});
```

Rodar para confirmar que falham (RED): `bun test packages/core/src/__tests__/unit/output`

**PASSO 3 — Implementar packages/shared/src/types/index.ts (GREEN):**

Substituir o placeholder com:
```typescript
// SDK-06: BrainOutput — contrato de saída estruturado de todos os Brains
// Definido aqui (packages/shared) para evitar ciclo de dependência:
//   packages/ai NÃO pode importar de packages/core (core já depende de ai).
//   packages/shared é folha — sem dependências internas.

/**
 * Tipo de mídia da resposta do Brain.
 * - "text": resposta textual pura
 * - "audio": texto para conversão TTS pelo sistema downstream (D-03)
 * - "image" | "video" | "document": requer mediaType + mediaUrl (D-04)
 */
export type ResponseMode = "text" | "image" | "audio" | "video" | "document";

/**
 * Contrato de saída estruturado de todos os Brains.
 * Interface TypeScript pura — sem Zod (evita dependência de runtime em shared).
 * O schema Zod com validação em runtime está em packages/core/src/output/schema.ts.
 */
export interface BrainOutput {
  /** Texto obrigatório em todos os modos — legenda, acompanhamento ou texto para TTS */
  fullResponse: string;
  /** Tipo de mídia da resposta (D-01, D-02) */
  responseMode: ResponseMode;
  /** MIME type livre (ex: "image/jpeg", "application/pdf") — obrigatório para image/video/document */
  mediaType?: string;
  /** URL externa do arquivo — obrigatório para image/video/document (D-06: sem upload base64) */
  mediaUrl?: string;
}
```

**PASSO 4 — Implementar packages/shared/src/errors/index.ts (adicionar BrainOutputValidationError):**

Adicionar APÓS a classe `ConfigurationError` existente:
```typescript
export class BrainOutputValidationError extends BrainError {
  constructor(message: string, context?: Record<string, unknown>) {
    super(message, 'BRAIN_OUTPUT_VALIDATION_ERROR', context);
    this.name = 'BrainOutputValidationError';
  }
}
```

**PASSO 5 — Implementar packages/core/src/output/schema.ts (criar novo arquivo):**

```typescript
// SDK-06: BrainOutputSchema — schema Zod para validação em runtime do contrato de saída.
// D-01 a D-06: definições de campo conforme contexto da Fase 10.
// Pitfall 3: BrainOutputSchema.parse() lança ZodError — envolver em try/catch no runner
//            para relançar como BrainOutputValidationError.

import { z } from "zod";

export const ResponseModeSchema = z.enum([
  "text",
  "image",
  "audio",
  "video",
  "document",
]);

// D-04: image, video e document exigem mediaType + mediaUrl
const MODES_REQUIRING_MEDIA = ["image", "video", "document"] as const;

export const BrainOutputSchema = z
  .object({
    /** Texto obrigatório em todos os modos — legenda, acompanhamento ou texto para TTS (D-03) */
    fullResponse: z.string().min(1, "fullResponse is required and cannot be empty"),
    /** Tipo de mídia da resposta — enum restrito (D-02) */
    responseMode: ResponseModeSchema,
    /** MIME type livre (ex: "image/jpeg") — obrigatório quando responseMode requer mídia (D-05) */
    mediaType: z.string().optional(),
    /** URL externa — obrigatório quando responseMode requer mídia; sem upload base64 (D-06) */
    mediaUrl: z.string().optional(),
  })
  .superRefine((data, ctx) => {
    // D-04: validação condicional — image, video, document exigem mediaType + mediaUrl
    const needsMedia = (MODES_REQUIRING_MEDIA as readonly string[]).includes(
      data.responseMode
    );
    if (needsMedia) {
      if (!data.mediaType) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          message: `mediaType is required when responseMode is "${data.responseMode}"`,
          path: ["mediaType"],
        });
      }
      if (!data.mediaUrl) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          message: `mediaUrl is required when responseMode is "${data.responseMode}"`,
          path: ["mediaUrl"],
        });
      }
    }
  });

// Nota: ResponseMode e BrainOutput types são re-exportados de @brain-pkg/shared
// para evitar duplicação. packages/core importa de shared (direção permitida).
export type { ResponseMode, BrainOutput } from "@brain-pkg/shared";
```

**PASSO 6 — Atualizar packages/core/src/index.ts:**

Adicionar bloco SDK-06 e remover `BrainRunResult` da linha de export do runner (D-13 — BrainRunResult será removido do runner.ts no Plano 02):

```typescript
// SDK-06: BrainOutput contract — schema Zod em core, type em shared
export { BrainOutputSchema, ResponseModeSchema } from "./output/schema.js";
export type { BrainOutput, ResponseMode } from "./output/schema.js";
```

Na linha de export do runner, remover `BrainRunResult`:
```typescript
// ANTES:
export type { BrainRunnerOptions, BrainRunResult } from "./runner/runner.js";
// DEPOIS:
export type { BrainRunnerOptions } from "./runner/runner.js";
```

ATENÇÃO: `BrainRunResult` ainda existe em runner.ts neste plano (será removido no Plano 02). Remover apenas do barrel — TypeScript não reclama de export não listado.

**PASSO 7 — Rodar testes para confirmar GREEN:**
```bash
bun test packages/core/src/__tests__/unit/output
```
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/output</automated>
  </verify>

  <acceptance_criteria>
    - packages/core/src/__tests__/unit/output/schema.test.ts existe e contém 8 casos de teste
    - `bun test packages/core/src/__tests__/unit/output` passa todos os testes sem falha
    - packages/shared/src/types/index.ts contém `export interface BrainOutput` e `export type ResponseMode`
    - packages/shared/src/errors/index.ts contém `BrainOutputValidationError` com `code: 'BRAIN_OUTPUT_VALIDATION_ERROR'`
    - packages/core/src/output/schema.ts contém `export const BrainOutputSchema` e `export const ResponseModeSchema`
    - packages/core/src/output/schema.ts usa `.superRefine(` para validação condicional
    - packages/core/src/index.ts contém `export { BrainOutputSchema, ResponseModeSchema } from "./output/schema.js"`
    - packages/core/src/index.ts NÃO contém `BrainRunResult` na linha de export do runner
    - `bun test packages/core/src/__tests__` passa (sem regressões nos testes existentes)
  </acceptance_criteria>

  <done>BrainOutputSchema valida todas as combinações de responseMode; BrainOutput type em shared; BrainOutputValidationError em shared; tudo exportado no barrel de core; 8 testes unitários verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph → BrainRunner | Saída do grafo (brainOutput) cruza para o runner — origem não confiável até validação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | BrainOutputSchema | mitigate | `z.enum()` rejeita responseMode fora do conjunto permitido em runtime |
| T-10-02 | Tampering | BrainOutputSchema | mitigate | `z.string().min(1)` impede fullResponse vazia que causaria TTS silencioso ou resposta em branco |
| T-10-03 | Denial of Service | BrainOutputSchema | mitigate | `.superRefine()` garante que image/video/document sem mediaUrl/mediaType falham com ZodError antes de retornar |
</threat_model>

<verification>
Verificação final do Plano 01:
1. `bun test packages/core/src/__tests__/unit/output` — 8 testes verdes
2. `bun test packages/core/src/__tests__` — sem regressões nos testes existentes do core
3. `grep "BrainOutputSchema" packages/core/src/index.ts` — confirma export no barrel
4. `grep "BrainOutputValidationError" packages/shared/src/errors/index.ts` — confirma nova classe
5. `grep "export interface BrainOutput" packages/shared/src/types/index.ts` — confirma type em shared
</verification>

<success_criteria>
- BrainOutputSchema (Zod) em packages/core valida todos os 5 valores de responseMode
- Validação condicional: image/video/document exigem mediaType + mediaUrl
- fullResponse vazia rejeita com ZodError
- BrainOutput type (TypeScript puro) em packages/shared — sem ciclo de dependência
- BrainOutputValidationError em packages/shared — permite catch específico
- packages/core barrel exporta BrainOutputSchema, ResponseModeSchema, BrainOutput, ResponseMode
- 8 testes unitários verdes cobrindo PARSER-01
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-output-parser-sdk/10-01-SUMMARY.md`
</output>
