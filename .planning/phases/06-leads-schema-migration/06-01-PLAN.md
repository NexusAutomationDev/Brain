---
phase: 06-leads-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/tables.ts
  - packages/database/src/migrate.ts
autonomous: true
requirements:
  - LEAD-01
  - LEAD-04

must_haves:
  truths:
    - "leadsTable definida em tables.ts com todos os campos de D-01 a D-07"
    - "UNIQUE constraint em numero presente na definição Drizzle"
    - "tabela users intacta — nenhuma remoção ou modificação"
    - "runMigrations() adquire pg_advisory_lock antes de chamar migrate()"
    - "segunda instância que chama runMigrations() aguarda até a primeira liberar o lock"
  artifacts:
    - path: "packages/database/src/schema/tables.ts"
      provides: "leadsTable Drizzle definition exportada"
      contains: "export const leads"
    - path: "packages/database/src/migrate.ts"
      provides: "runMigrations() com advisory lock"
      contains: "pg_advisory_lock"
  key_links:
    - from: "packages/database/src/schema/tables.ts"
      to: "packages/database/src/index.ts"
      via: "export * from './schema/tables.js'"
      pattern: "export const leads"
    - from: "packages/database/src/migrate.ts"
      to: "PostgreSQL advisory lock"
      via: "sql`SELECT pg_advisory_lock(...)`"
      pattern: "pg_advisory_lock"
---

<objective>
Define o schema da tabela `leads` no Drizzle ORM e adiciona advisory lock ao runMigrations() para coordenar múltiplas instâncias.

Purpose: Fundação de dados para Phases 7-9 — a tabela leads armazena identity, estado de IA e metadados de cada lead. O advisory lock previne race condition de migrations em deploys multi-instância.

Output: leadsTable exportada de packages/database (disponível para outros pacotes via barrel), runMigrations() com lock blocking garantindo execução serial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-leads-schema-migration/06-CONTEXT.md

<interfaces>
<!-- Padrões existentes em packages/database/src/schema/tables.ts -->
<!-- O executor DEVE seguir esses padrões para leadsTable — não inventar. -->

```typescript
// UUID PK pattern (D-01)
uuid('id').primaryKey().defaultRandom()

// uniqueIndex pattern (para D-04)
uniqueIndex('memories_user_key_idx').on(table.userId, table.key)
// — para leads usar: uniqueIndex('leads_numero_unique_idx').on(table.numero)

// Timestamp pattern (D-07)
timestamp('created_at').defaultNow().notNull()
timestamp('updated_at').defaultNow().notNull()

// Boolean pattern (D-05)
boolean('ia_ativada').notNull().default(true)

// Nullable boolean pattern (D-06)
// Drizzle: omitir .notNull() deixa o campo nullable
```

<!-- runMigrations() atual em packages/database/src/migrate.ts -->
```typescript
export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void> {
  const db = drizzle(sql);
  await sql`CREATE EXTENSION IF NOT EXISTS vector`;
  await migrate(db, { migrationsFolder });
}
```
<!-- Após a modificação, deve adquirir lock ANTES de qualquer migration -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar leadsTable ao schema Drizzle</name>
  <files>packages/database/src/schema/tables.ts</files>
  <read_first>
    - packages/database/src/schema/tables.ts — estado atual, padrões UUID/timestamp/uniqueIndex a replicar
    - packages/database/src/index.ts — confirmar que export * from './schema/tables.js' já existe (leads será exportado automaticamente)
  </read_first>
  <action>
Adicionar import de `boolean` do `drizzle-orm/pg-core` na linha de imports existente (já há uuid, text, timestamp, uniqueIndex — só adicionar `boolean`).

Adicionar a seguinte definição após a tabela `prompts`:

```typescript
// LEAD-01: Leads table — identity and state for each lead (D-01 to D-08)
// D-08: aditiva — tabela users NÃO é removida em v1.1
export const leads = pgTable('leads', {
  // D-01: UUID PK
  id: uuid('id').primaryKey().defaultRandom(),
  // D-02: IDLead do payload — chave de vínculo com thread_id do PostgresSaver
  uniqueId: text('unique_id').notNull(),
  // D-03: Nome nullable — primeira mensagem pode não incluir nome
  nome: text('nome'),
  // D-04: Numero NOT NULL + UNIQUE — chave de upsert para identificação do lead
  numero: text('numero').notNull(),
  // D-05: ia_ativada DEFAULT true — desativação é ação manual explícita
  iaAtivada: boolean('ia_ativada').notNull().default(true),
  // D-06: fullpp nullable — flag "follow up IA", sem regra de negócio em v1.1
  fullpp: boolean('fullpp'),
  // D-07: timestamps padrão
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
}, (table) => ({
  // D-04: UNIQUE constraint em numero — chave de upsert para Phase 7
  numeroIdx: uniqueIndex('leads_numero_unique_idx').on(table.numero),
}));
```

NÃO modificar nem remover nenhuma tabela existente (users, memories, agentState, embeddings, prompts).
  </action>
  <verify>grep -n "export const leads" packages/database/src/schema/tables.ts</verify>
  <done>
    - tables.ts contém `export const leads = pgTable('leads', {`
    - Campo `ia_ativada` com `.notNull().default(true)` presente
    - Campo `fullpp` sem `.notNull()` (nullable) presente
    - uniqueIndex `leads_numero_unique_idx` presente
    - tabela `users` inalterada (grep "export const users" ainda retorna a definição original)
  </done>
  <acceptance_criteria>
    - grep "export const leads" packages/database/src/schema/tables.ts retorna resultado
    - grep "leads_numero_unique_idx" packages/database/src/schema/tables.ts retorna resultado
    - grep "ia_ativada.*notNull.*default(true)\|notNull.*ia_ativada" packages/database/src/schema/tables.ts retorna resultado
    - grep "export const users" packages/database/src/schema/tables.ts ainda retorna resultado (tabela não removida)
    - grep "fullpp" packages/database/src/schema/tables.ts retorna resultado SEM .notNull() na mesma linha
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar pg_advisory_lock ao runMigrations()</name>
  <files>packages/database/src/migrate.ts</files>
  <read_first>
    - packages/database/src/migrate.ts — implementação atual completa antes de modificar
  </read_first>
  <action>
Modificar `runMigrations()` para adquirir `pg_advisory_lock` ANTES de executar as migrations. A função deve:

1. Definir constante `MIGRATION_LOCK_KEY = 7316882` (número fixo arbitrário — D-14).
2. Adquirir lock blocking com `await sql\`SELECT pg_advisory_lock(${MIGRATION_LOCK_KEY})\`` ANTES de qualquer outra operação.
3. Envolver toda a lógica de migration em try/finally para liberar o lock ao final (mesmo em caso de erro) com `await sql\`SELECT pg_advisory_unlock(${MIGRATION_LOCK_KEY})\``.
4. Adicionar log estruturado via `console.log` (pino não disponível aqui) nas transições: aguardando lock → lock adquirido → migrations completas → lock liberado.

Resultado final de `runMigrations()`:

```typescript
const MIGRATION_LOCK_KEY = 7316882; // D-14: chave fixa arbitrária para advisory lock

export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void> {
  // D-13: pg_advisory_lock blocking — segunda instância aguarda até a primeira terminar
  // D-15: lock é por database no PostgreSQL — isolamento multi-tenant automático
  console.log('[migrate] Aguardando advisory lock para migrations...');
  await sql`SELECT pg_advisory_lock(${MIGRATION_LOCK_KEY})`;
  console.log('[migrate] Advisory lock adquirido — iniciando migrations');
  try {
    const db = drizzle(sql);
    await sql`CREATE EXTENSION IF NOT EXISTS vector`;
    await migrate(db, { migrationsFolder });
    console.log('[migrate] Migrations concluídas com sucesso');
  } finally {
    await sql`SELECT pg_advisory_unlock(${MIGRATION_LOCK_KEY})`;
    console.log('[migrate] Advisory lock liberado');
  }
}
```

NÃO alterar o bloco `if (import.meta.main)` — apenas a função `runMigrations()`.
  </action>
  <verify>grep -n "pg_advisory_lock\|MIGRATION_LOCK_KEY" packages/database/src/migrate.ts</verify>
  <done>
    - migrate.ts contém `MIGRATION_LOCK_KEY = 7316882`
    - `SELECT pg_advisory_lock` antes do try block
    - `SELECT pg_advisory_unlock` dentro do finally block
    - Estrutura try/finally presente
  </done>
  <acceptance_criteria>
    - grep "MIGRATION_LOCK_KEY" packages/database/src/migrate.ts retorna linha com valor numérico
    - grep "pg_advisory_lock" packages/database/src/migrate.ts retorna pelo menos 2 resultados (lock + unlock)
    - grep "finally" packages/database/src/migrate.ts retorna resultado (garantia de unlock em erro)
    - grep "pg_advisory_unlock" packages/database/src/migrate.ts retorna resultado dentro do finally
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → migrate.ts | MIGRATIONS_FOLDER vem de ENV — path traversal se não for validado no caller |
| múltiplas instâncias → PostgreSQL advisory lock | Coordenação via lock DB-side — sem protocolo de rede externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | tables.ts leadsTable | accept | Schema é código versionado em git; alterações requerem deploy deliberado |
| T-06-02 | Denial of Service | runMigrations advisory lock | accept | Lock blocking é intencional para coordenação; timeout futuro se produção demandar (D-deferred) |
| T-06-03 | Elevation of Privilege | pg_advisory_lock key collision | accept | Advisory locks são por database — isolamento multi-tenant é automático pelo PostgreSQL; colisão de chave entre Brains distintos no mesmo DB não é cenário v1.1 |
| T-06-04 | Information Disclosure | console.log em migrate.ts | accept | Mensagens de log não expõem dados sensíveis — apenas estado do lock e status de migration |
</threat_model>

<verification>
Após execução do plano 01:

```bash
# Verificar leadsTable
grep "export const leads" packages/database/src/schema/tables.ts

# Verificar UNIQUE constraint
grep "leads_numero_unique_idx" packages/database/src/schema/tables.ts

# Verificar advisory lock
grep "pg_advisory_lock\|MIGRATION_LOCK_KEY" packages/database/src/migrate.ts

# Verificar que users não foi removida
grep "export const users" packages/database/src/schema/tables.ts

# Typecheck
cd packages/database && bun run typecheck 2>/dev/null || bunx tsc --noEmit
```
</verification>

<success_criteria>
- leadsTable definida com todos os 8 campos (D-01 a D-07) e uniqueIndex em `numero`
- tabela `users` inalterada em tables.ts
- runMigrations() envolve migrate() em pg_advisory_lock/pg_advisory_unlock com try/finally
- Typecheck passa em packages/database sem erros novos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-leads-schema-migration/06-01-SUMMARY.md`
</output>
