---
phase: 06-leads-schema-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - packages/database/src/migrations/0004_leads_table.sql
  - packages/database/src/migrations/meta/_journal.json
  - packages/core/src/runner/runner.ts
  - apps/brain-echo/src/index.ts
autonomous: true
requirements:
  - LEAD-01
  - LEAD-04

must_haves:
  truths:
    - "arquivo SQL de migration para leads existe com CREATE TABLE correto"
    - "_journal.json tem entry idx:4 apontando para a nova migration"
    - "BrainRunner.init() chama runMigrations() usando MIGRATIONS_FOLDER ENV"
    - "apps/brain-echo/src/index.ts não chama runMigrations() diretamente"
    - "Brain não aceita mensagens se migration não completou"
  artifacts:
    - path: "packages/database/src/migrations/0004_leads_table.sql"
      provides: "SQL de criação da tabela leads com UNIQUE constraint"
      contains: "CREATE TABLE"
    - path: "packages/database/src/migrations/meta/_journal.json"
      provides: "Journal atualizado com entry da nova migration"
      contains: "0004_leads_table"
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner.init() com runMigrations() integrado"
      contains: "runMigrations"
  key_links:
    - from: "packages/core/src/runner/runner.ts"
      to: "packages/database/src/migrate.ts"
      via: "import { runMigrations } from '@brain-pkg/database'"
      pattern: "runMigrations"
    - from: "BrainRunner.init()"
      to: "runMigrations()"
      via: "await runMigrations(this.sql, migrationsFolder)"
      pattern: "await runMigrations"
---

<objective>
Gera o arquivo SQL de migration para a tabela `leads` via drizzle-kit e integra runMigrations() ao ciclo de vida do BrainRunner.

Purpose: Sem o arquivo SQL gerado, o Drizzle migrator não cria a tabela no banco. Sem a integração no BrainRunner, Brains futuros precisariam chamar runMigrations() manualmente — violando D-10 (SDK cuida automaticamente).

Output: Migration SQL commitável, journal atualizado, BrainRunner auto-migra na init().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-leads-schema-migration/06-CONTEXT.md
@.planning/phases/06-leads-schema-migration/06-01-SUMMARY.md

<interfaces>
<!-- BrainRunnerOptions atual — packages/core/src/runner/runner.ts -->
```typescript
export interface BrainRunnerOptions {
  brain: IBrain;
  sql: Sql;
  toolsRegistry: ToolsRegistry;
  llmOptions?: LLMOptions;
}
```
<!-- Adicionar campo migrationsFolder opcional: -->
```typescript
export interface BrainRunnerOptions {
  brain: IBrain;
  sql: Sql;
  toolsRegistry: ToolsRegistry;
  llmOptions?: LLMOptions;
  /** Pasta de migrations para auto-migrate no init(). Se omitido, usa MIGRATIONS_FOLDER ENV. */
  migrationsFolder?: string;
}
```

<!-- runMigrations signature (packages/database/src/migrate.ts, após plan 01) -->
```typescript
export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void>
```

<!-- apps/brain-echo/src/index.ts — padrão atual de startup (a ser simplificado) -->
<!-- Atualmente chama runMigrations() diretamente antes de runner.init(). -->
<!-- Após esta fase: remover chamada direta; BrainRunner.init() cuida disso. -->
<!-- MIGRATIONS_FOLDER ENV deve ser definida no app ou derivada do path atual. -->

<!-- _journal.json atual — último entry: idx 3, tag "0003_memories_unique_user_key" -->
<!-- Nova entry deve ter idx: 4, seguir o mesmo formato -->
```json
{
  "idx": 4,
  "version": "7",
  "when": <timestamp_unix_ms>,
  "tag": "0004_leads_table",
  "breakpoints": true
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Gerar migration SQL com drizzle-kit</name>
  <files>
    packages/database/src/migrations/0004_leads_table.sql
    packages/database/src/migrations/meta/_journal.json
  </files>
  <read_first>
    - packages/database/src/migrations/meta/_journal.json — estado atual do journal (último idx = 3)
    - packages/database/src/migrations/0000_lyrical_scrambler.sql — formato SQL esperado com statement-breakpoint
    - packages/database/drizzle.config.ts — confirmar schema path e out path
    - packages/database/src/schema/tables.ts — confirmar que leadsTable foi adicionada (plano 01 deve estar completo)
  </read_first>
  <action>
Esta task é BLOQUEANTE — sem o arquivo SQL gerado, a fase não pode ser verificada.

Executar o comando de geração de migration no diretório correto:

```bash
cd /root/Brain/packages/database && bunx drizzle-kit generate --config drizzle.config.ts
```

O comando irá:
1. Detectar a diferença entre o schema atual (tables.ts, agora com leadsTable) e o último snapshot
2. Gerar um novo arquivo SQL em `packages/database/src/migrations/` com a criação da tabela `leads`
3. Atualizar automaticamente `packages/database/src/migrations/meta/_journal.json` com a nova entry

Se o drizzle-kit pedir confirmação interativa (prompt), responder `y` ou usar flag `--accept-warnings` se disponível.

**Após geração, verificar o arquivo SQL gerado:**
- Deve conter `CREATE TABLE "leads"` com os campos corretos
- Deve conter `CONSTRAINT "leads_numero_unique_idx" UNIQUE` ou `CREATE UNIQUE INDEX "leads_numero_unique_idx"`
- Deve usar o formato com `--> statement-breakpoint` separando DDL statements

Se drizzle-kit renomear o arquivo com hash diferente de `0004_leads_table`, aceitar o nome gerado — o journal será atualizado automaticamente com o nome correto.

NÃO editar o arquivo SQL gerado manualmente — o drizzle-kit é a fonte de verdade.
  </action>
  <verify>ls packages/database/src/migrations/0004_*.sql 2>/dev/null || ls packages/database/src/migrations/ | grep "^000[4-9]"</verify>
  <done>
    - Novo arquivo SQL exists em packages/database/src/migrations/ com idx >= 4
    - Arquivo SQL contém CREATE TABLE "leads"
    - _journal.json tem entry com idx: 4 (ou superior)
    - bun typecheck passa em packages/database
  </done>
  <acceptance_criteria>
    - ls packages/database/src/migrations/ | grep "000[4-9]" retorna pelo menos 1 arquivo .sql
    - grep "CREATE TABLE" packages/database/src/migrations/000[4-9]*.sql retorna resultado com "leads"
    - grep "leads_numero_unique" packages/database/src/migrations/000[4-9]*.sql retorna resultado
    - grep "idx.*4\|\"idx\": 4" packages/database/src/migrations/meta/_journal.json retorna resultado
    - grep "ia_ativada" packages/database/src/migrations/000[4-9]*.sql retorna resultado
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar runMigrations() ao BrainRunner.init() e atualizar brain-echo</name>
  <files>
    packages/core/src/runner/runner.ts
    apps/brain-echo/src/index.ts
  </files>
  <read_first>
    - packages/core/src/runner/runner.ts — implementação completa atual, especialmente BrainRunnerOptions e init()
    - apps/brain-echo/src/index.ts — startup atual que chama runMigrations() diretamente (a ser removido)
    - packages/database/src/index.ts — confirmar que runMigrations está exportado do barrel
  </read_first>
  <action>
**Parte A — packages/core/src/runner/runner.ts:**

1. Adicionar import de `runMigrations` no topo:
   ```typescript
   import { runMigrations } from '@brain-pkg/database';
   ```

2. Adicionar campo `migrationsFolder?: string` à interface `BrainRunnerOptions`:
   ```typescript
   /** Pasta de migrations para auto-migrate no init(). Se omitido, usa MIGRATIONS_FOLDER ENV. */
   migrationsFolder?: string;
   ```

3. Adicionar propriedade privada na classe:
   ```typescript
   private readonly migrationsFolder: string | undefined;
   ```

4. Atribuir no constructor:
   ```typescript
   this.migrationsFolder = options.migrationsFolder;
   ```

5. No início do método `init()`, ANTES de `loadPrompts()`, adicionar:
   ```typescript
   // D-10: runMigrations() chamada dentro de BrainRunner.init() — SDK cuida automaticamente
   // D-11: MIGRATIONS_FOLDER via ENV ou opção do construtor
   // D-12: migration completa antes do Brain aceitar mensagens
   const migrationsFolder = this.migrationsFolder ?? process.env.MIGRATIONS_FOLDER;
   if (!migrationsFolder) {
     this.logger.error(
       { brainId: this.brain.id },
       'MIGRATIONS_FOLDER not set — cannot run migrations'
     );
     process.exit(1);
   }
   await runMigrations(this.sql, migrationsFolder).catch((err) => {
     this.logger.error({ brainId: this.brain.id, err }, 'Migrations failed — aborting init');
     process.exit(1);
   });
   this.logger.info({ brainId: this.brain.id }, 'Migrations completed');
   ```

**Parte B — apps/brain-echo/src/index.ts:**

Remover o bloco de migration manual (linhas que definem `migrationsDir` e chamam `runMigrations()`):
```typescript
// REMOVER estas linhas:
const migrationsDir = process.env.MIGRATIONS_DIR
  ?? join(dirname(fileURLToPath(import.meta.url)), "../../migrations");

await runMigrations(sql, migrationsDir).catch((err) => {
  logger.error({ err }, "Migrations failed — aborting startup");
  process.exit(1);
});
logger.info({}, "Migrations completed");
```

Remover também o import de `runMigrations` de `@brain-pkg/database` no index.ts (se ficar sem uso).

Remover imports de `fileURLToPath`, `join`, `dirname` SE não forem mais usados após remover o bloco de migrationsDir.

Garantir que `MIGRATIONS_FOLDER` ENV está definida ou é passada ao BrainRunner. No brain-echo, o valor correto para desenvolvimento é o path das migrations do pacote database. Adicionar ao BrainRunner a opção `migrationsFolder` resolvida a partir de `process.env.MIGRATIONS_FOLDER` (que o app deve definir no .env). NÃO duplicar a lógica — basta garantir que o env está documentado no comentário.

Adicionar comentário explicativo sobre a ENV necessária:
```typescript
// MIGRATIONS_FOLDER ENV deve apontar para packages/database/src/migrations
// Em Docker: MIGRATIONS_FOLDER=/app/migrations (via ENV no Dockerfile)
// Em dev: definir em .env ou usar path relativo
```
  </action>
  <verify>grep -n "runMigrations\|MIGRATIONS_FOLDER" packages/core/src/runner/runner.ts && grep -n "runMigrations" apps/brain-echo/src/index.ts</verify>
  <done>
    - runner.ts contém `import { runMigrations } from '@brain-pkg/database'`
    - runner.ts init() contém `await runMigrations(this.sql, migrationsFolder)`
    - apps/brain-echo/src/index.ts NÃO contém chamada direta a `runMigrations()`
    - bun typecheck passa em packages/core e apps/brain-echo
  </done>
  <acceptance_criteria>
    - grep "runMigrations" packages/core/src/runner/runner.ts retorna pelo menos 2 resultados (import + call)
    - grep "MIGRATIONS_FOLDER" packages/core/src/runner/runner.ts retorna resultado
    - grep "await runMigrations" packages/core/src/runner/runner.ts retorna resultado dentro de init()
    - grep "runMigrations" apps/brain-echo/src/index.ts retorna 0 resultados (chamada removida do app)
    - cd packages/core && bun run typecheck 2>&1 | grep -c "error" deve retornar 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → BrainRunner | MIGRATIONS_FOLDER vem de ENV — path não sanitizado pode apontar para diretório arbitrário |
| drizzle-kit → filesystem | Geração de SQL escreve em src/migrations/ — somente durante desenvolvimento, não em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | migration SQL gerado | accept | Arquivo SQL é revisado via git diff antes de commit; gerado por drizzle-kit a partir do schema versionado |
| T-06-06 | Denial of Service | BrainRunner.init() blocking em migration | accept | Comportamento intencional — D-12 exige que migration complete antes de aceitar mensagens |
| T-06-07 | Elevation of Privilege | MIGRATIONS_FOLDER path traversal | mitigate | process.exit(1) se env não definida; valor deve ser path absoluto ou relativo ao projeto; não há eval ou execução dinâmica do conteúdo do path |
| T-06-08 | Repudiation | migration aplicada sem log | mitigate | console.log em migrate.ts (plan 01) registra estado do lock e conclusão; pino logger em runner.ts registra início e fim |
</threat_model>

<verification>
Após execução do plano 02, verificar integridade completa da fase:

```bash
# 1. Migration SQL existe com conteúdo correto
ls /root/Brain/packages/database/src/migrations/000[4-9]*.sql
grep "CREATE TABLE" /root/Brain/packages/database/src/migrations/000[4-9]*.sql

# 2. Journal atualizado
grep "0004\|000[4-9]" /root/Brain/packages/database/src/migrations/meta/_journal.json

# 3. BrainRunner.init() com runMigrations
grep "runMigrations\|MIGRATIONS_FOLDER" /root/Brain/packages/core/src/runner/runner.ts

# 4. brain-echo sem chamada direta
grep "runMigrations" /root/Brain/apps/brain-echo/src/index.ts && echo "FAIL: ainda tem chamada direta" || echo "OK: sem chamada direta"

# 5. Typecheck nos pacotes afetados
cd /root/Brain/packages/database && bunx tsc --noEmit
cd /root/Brain/packages/core && bunx tsc --noEmit
cd /root/Brain/apps/brain-echo && bunx tsc --noEmit
```
</verification>

<success_criteria>
- Arquivo SQL 0004_*.sql existe com CREATE TABLE "leads", campo ia_ativada, e unique constraint em numero
- _journal.json contém entry idx:4 com tag correspondente ao arquivo gerado
- BrainRunner.init() chama runMigrations() antes de loadPrompts() — migration sempre precede aceitação de mensagens
- apps/brain-echo não chama runMigrations() diretamente — SDK cuida
- Typecheck passa em packages/database, packages/core e apps/brain-echo sem erros novos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-leads-schema-migration/06-02-SUMMARY.md`
</output>
