---
phase: 27-tech-debt-fixes
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/__tests__/integration/fup-e2e.test.ts
autonomous: true
requirements:
  - TECH-02
must_haves:
  truths:
    - "Existe teste de integração E2E do FupScheduler em packages/core que roda contra PostgreSQL real"
    - "Teste cobre multi-step: lead elegível → scheduler processa → fup_step avança → fup_next_at atualizado"
    - "Teste verifica que após último step: ia_ativada=false e fup_enabled=false (FUP-05)"
    - "LLM é mockado — sem custo de API; webhook HTTP é mockado — sem dependência de rede"
    - "Teste skipa graciosamente quando DATABASE_URL não está configurado no ambiente"
  artifacts:
    - path: "packages/core/src/__tests__/integration/fup-e2e.test.ts"
      provides: "Teste E2E do FupScheduler com PostgreSQL real"
      contains: "FupScheduler"
      min_lines: 100
  key_links:
    - from: "packages/core/src/__tests__/integration/fup-e2e.test.ts"
      to: "packages/core/src/fup/fup-scheduler.ts"
      via: "import FupScheduler + _tick() direto"
      pattern: "FupScheduler.*_tick"
    - from: "packages/core/src/__tests__/integration/fup-e2e.test.ts"
      to: "PostgreSQL real via DATABASE_URL"
      via: "postgres(DATABASE_URL) + runMigrations()"
      pattern: "runMigrations"
---

<objective>
Corrigir FUP-02: não existe teste de integração E2E do FupScheduler rodando contra banco PostgreSQL real. Os testes unitários existentes usam mocks de sql.begin() e não verificam a interação real com o banco.

Purpose: Garantir que o FupScheduler funciona end-to-end — SELECT FOR UPDATE SKIP LOCKED, UPDATE de fup_step/fup_next_at, e finalização com ia_ativada=false/fup_enabled=false — com dados reais no PostgreSQL.

Output: Arquivo packages/core/src/__tests__/integration/fup-e2e.test.ts com ciclo completo multi-step testado contra banco real, LLM mockado para evitar custo de API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-tech-debt-fixes/27-CONTEXT.md

<interfaces>
<!-- Contratos que o executor precisa para escrever o teste -->

De packages/core/src/fup/fup-scheduler.ts:
```typescript
export interface FupSchedulerOptions {
  sql: Sql;
  brainType: string;
  checkpointer: ICheckpointerLike;
  eventPublisher: IEventPublisher | null;
  fupWebhookUrl: string;
}

export class FupScheduler implements IFupScheduler {
  constructor(private readonly opts: FupSchedulerOptions) {}
  async start(): Promise<void>
  async stop(): Promise<void>
  async _tick(): Promise<void>  // exportada para testes
  async _processFupForLead(lead: FupLeadRow): Promise<void>  // exportada para testes
}
```

Schema de tabelas relevantes (leads + fup_config):
- leads: id, unique_id, nome, numero, brain_type, fup_step, fup_next_at, fup_failure_count, ia_ativada, fup_enabled, last_message_at, created_at, updated_at
- fup_config: brain_type, enabled, intervals_seconds (int[]), min_hour, max_hour, allowed_days (text[]), timezone

Padrão de integration test (apps/brain-sdr/src/__tests__/integration/rag-e2e.test.ts):
```typescript
const DATABASE_URL = process.env.DATABASE_URL;
const RUN = !!DATABASE_URL;

let sql: ReturnType<typeof postgres> | null = null;

describe("...", () => {
  beforeAll(async () => {
    if (!RUN) return;
    sql = postgres(DATABASE_URL!, { prepare: false });
    await runMigrations(sql, MIGRATIONS_FOLDER);
  });

  afterAll(async () => {
    // DELETE registros criados pelo teste
    await sql!`DELETE FROM tabela WHERE condition`;
    await sql!.end();
  });

  test.skipIf(!RUN)("...", async () => { ... });
});
```

De packages/core/src/fup/fup-scheduler.ts — estrutura FupLeadRow (interna ao scheduler, usada em _processFupForLead):
```typescript
interface FupLeadRow {
  id: string; uniqueId: string; nome: string | null; numero: string;
  fupStep: number; fupNextAt: Date | null; fupFailureCount: number;
  iaAtivada: boolean; fupEnabled: boolean;
  intervalsSeconds: number[]; minHour: number; maxHour: number;
  allowedDays: string[]; timezone: string;
}
```
</interfaces>
</context>

<feature>
  <name>FupScheduler E2E Integration Test</name>
  <files>packages/core/src/__tests__/integration/fup-e2e.test.ts</files>
  <behavior>
    Setup (beforeAll):
    - Conectar ao PostgreSQL real via DATABASE_URL com prepare: false (PgBouncer compat)
    - Rodar runMigrations() para garantir schema atualizado
    - Inserir fup_config com intervals_seconds=[1, 2] (2 steps), brain_type='sdr-fup-e2e', enabled=true, min_hour=0, max_hour=23, timezone='UTC', allowed_days=['mon','tue','wed','thu','fri','sat','sun']
    - Inserir lead com: unique_id='fup-e2e-lead-1', fup_enabled=true, ia_ativada=true, fup_step=0, fup_next_at=NOW()-1 minute, brain_type='sdr-fup-e2e', nome='E2E Test Lead', numero='5511000000001'

    Mock setup:
    - checkpointer mock: getTuple() retorna { checkpoint: { channel_values: { messages: [] } } }
    - fetchMock (globalThis.fetch override): retorna Response(null, { status: 200 })
    - eventPublisher mock: publish() e close() são mocks no-op

    Test 1 — Step 0 → Step 1:
    - Chamar scheduler._tick() diretamente
    - Verificar no banco: fup_step = 1, fup_next_at > NOW(), ia_ativada = true, fup_enabled = true
    - fetchMock foi chamado uma vez com body contendo IDLead = 'fup-e2e-lead-1'

    Test 2 — Step 1 → último step (ia_ativada=false):
    - Atualizar lead no banco: fup_next_at = NOW()-1 minute (torná-lo elegível novamente)
    - Chamar scheduler._tick() novamente
    - Verificar no banco: ia_ativada = false, fup_enabled = false, fup_step = 2
    - fetchMock foi chamado segunda vez

    Teardown (afterAll):
    - DELETE FROM leads WHERE unique_id = 'fup-e2e-lead-1'
    - DELETE FROM fup_config WHERE brain_type = 'sdr-fup-e2e'
    - sql.end()
  </behavior>
  <implementation>
    1. Criar packages/core/src/__tests__/integration/fup-e2e.test.ts
    2. Importar: postgres, runMigrations, FupScheduler, mock/describe/test/beforeAll/afterAll/expect de bun:test
    3. Descobrir MIGRATIONS_FOLDER: usar process.env.MIGRATIONS_FOLDER ?? path.join(import.meta.dir, '../../../../apps/brain-sdr/migrations')
    4. Criar scheduler com fupWebhookUrl='http://localhost:19999/fup-test' (não vai chamar rede — fetch mockado)
    5. Substituir globalThis.fetch no beforeAll para capturar chamadas do scheduler._sendFupWebhook()
    6. Restaurar globalThis.fetch no afterAll
    7. Inserir prompt 'fup' para brain_type='sdr-fup-e2e' na tabela prompts (scheduler busca do banco): content='Envie uma mensagem de follow-up'
    8. Adicionar DELETE FROM prompts WHERE brain_type='sdr-fup-e2e' no teardown

    Padrão de PgBouncer: usar postgres(DATABASE_URL!, { prepare: false })

    Mock do LLM: FupScheduler chama createLLM() internamente em _generateFupMessage().
    Estratégia: substituir _generateFupMessage no scheduler via monkey-patch antes de _tick():
    ```typescript
    (scheduler as any)._generateFupMessage = async () => "Mensagem de follow-up de teste E2E";
    ```
    Isso evita importar createLLM e configura mockagem sem necessidade de API key.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teste → PostgreSQL real | Teste escreve e lê dados reais; teardown obrigatório para não poluir banco |
| Teste → fetch mock | fetch substituído globalmente no scope do describe; restaurado no afterAll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | Dados de teste no banco real | mitigate | unique_id/brain_type únicos com sufixo 'e2e' evitam conflito com dados reais; DELETE no afterAll limpa tudo |
| T-27-02-02 | Denial of Service | Teste sem DATABASE_URL trava | mitigate | test.skipIf(!RUN_FUP) em todos os testes; beforeAll retorna imediatamente se !RUN_FUP |
| T-27-02-03 | Information Disclosure | DATABASE_URL logado em erro | accept | Erro de conexão pode vazar URL no stack trace; aceitável em ambiente de teste (não produção) |
</threat_model>

<verification>
Verificações após execução:

1. Arquivo existe: ls packages/core/src/__tests__/integration/fup-e2e.test.ts
2. Arquivo contém importação de FupScheduler e runMigrations
3. Arquivo contém _tick() e verificação de ia_ativada=false no banco
4. <automated>cd /root/Brain && bun test packages/core/src/__tests__/integration/fup-e2e.test.ts 2>&1 | tail -20</automated>
   (sem DATABASE_URL: todos os testes skipados, 0 failed)
</verification>

<success_criteria>
- TECH-02 implementado: teste E2E de FUP em packages/core/__tests__/integration/ existindo com cobertura multi-step
- bun test packages/core/src/__tests__/integration/fup-e2e.test.ts sem DATABASE_URL: 0 failed (todos skipados)
- Teste cobre FUP-02 (scheduler processa lead elegível) e FUP-05 (último step desativa ia_ativada + fup_enabled)
- LLM mockado via _generateFupMessage monkey-patch — sem API key necessária
- Teardown limpa todos os registros inseridos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/27-tech-debt-fixes/27-02-SUMMARY.md` com o arquivo criado, estratégia de mock adotada e comandos para rodar o teste com banco real.
</output>
