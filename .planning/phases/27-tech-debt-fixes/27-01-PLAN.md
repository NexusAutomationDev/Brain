---
phase: 27-tech-debt-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/brain/interface.ts
  - packages/core/src/tools/registry.ts
  - packages/core/src/runner/runner.ts
  - apps/brain-sdr/src/brain.ts
autonomous: true
requirements:
  - TECH-01
must_haves:
  truths:
    - "Developer seta BRAIN_TOOLS=qualify_lead e tools criadas como closures em buildGraph() são excluídas do ToolNode se o nome não constar no whitelist"
    - "ctx.enabledTools está disponível em BrainBuildContext para que qualquer Brain filtre suas closures nativamente"
    - "Quando BRAIN_TOOLS não está setado, ctx.enabledTools é null e todas as tools são permitidas (sem regressão)"
    - "brain-sdr aplica o filtro corretamente em boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool, boundSearchKnowledgeTool e respondTool"
  artifacts:
    - path: "packages/core/src/brain/interface.ts"
      provides: "BrainBuildContext com campo enabledTools: Set<string> | null"
      contains: "enabledTools"
    - path: "packages/core/src/tools/registry.ts"
      provides: "getter getEnvWhitelist() expondo envWhitelist"
      contains: "getEnvWhitelist"
    - path: "packages/core/src/runner/runner.ts"
      provides: "ctx.enabledTools populado antes de chamar buildGraph()"
      contains: "enabledTools"
    - path: "apps/brain-sdr/src/brain.ts"
      provides: "bindTools() filtrado por ctx.enabledTools"
      contains: "enabledTools"
  key_links:
    - from: "packages/core/src/tools/registry.ts"
      to: "packages/core/src/runner/runner.ts"
      via: "this.toolsRegistry.getEnvWhitelist()"
      pattern: "getEnvWhitelist"
    - from: "packages/core/src/runner/runner.ts"
      to: "packages/core/src/brain/interface.ts"
      via: "ctx.enabledTools = this.toolsRegistry.getEnvWhitelist()"
      pattern: "enabledTools.*getEnvWhitelist"
    - from: "apps/brain-sdr/src/brain.ts"
      to: "packages/core/src/brain/interface.ts"
      via: "ctx.enabledTools?.has(toolName)"
      pattern: "enabledTools"
---

<objective>
Corrigir TD-03: o ENV BRAIN_TOOLS hoje filtra apenas IBrain.tools[] (stubs de schema), mas não as tools criadas como closures diretamente em buildGraph() (boundQualifyTool, boundSearchKnowledgeTool, etc.). Isso torna BRAIN_TOOLS inerte para a maioria das tools reais do brain-sdr.

Purpose: Permitir que operadores controlem exatamente quais tools o LLM pode chamar via BRAIN_TOOLS ENV, cobrindo tanto as tools registradas no ToolsRegistry quanto as closures nativas criadas em buildGraph().

Output: BrainBuildContext com campo enabledTools, ToolsRegistry expondo getter, BrainRunner injetando o whitelist no ctx, e brain-sdr aplicando o filtro antes do bindTools().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tech-debt-fixes/27-CONTEXT.md

<interfaces>
<!-- Contratos atuais que o executor deve conhecer antes de implementar -->

De packages/core/src/brain/interface.ts (atual — será modificado):
```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];        // filtrado pelo ToolsRegistry para IBrain.tools[]
  sql?: Sql;
  mcpTools: StructuredTool[];     // tools MCP injetadas pelo runner
}
```

De packages/core/src/tools/registry.ts (atual — será modificado):
```typescript
export class ToolsRegistry {
  private readonly envWhitelist: Set<string> | null;  // privado hoje
  constructor() { /* parseia BRAIN_TOOLS no ctor */ }
  enableTool(brainType: string, toolName: string): void { /* respeita envWhitelist */ }
  getTools(brainType: string, brainTools: StructuredTool[]): StructuredTool[] { /* filtra IBrain.tools[] */ }
}
```

De packages/core/src/runner/runner.ts (_compileGraph — ~linha 509):
```typescript
const ctx: BrainBuildContext = {
  llm,
  prompts: this.prompts,
  tools: filteredTools,
  sql: this.sql,
  mcpTools,
};
this.compiledGraph = this.brain.buildGraph(ctx).compile({ checkpointer });
```

De apps/brain-sdr/src/brain.ts (atual — bindTools sem filtro):
```typescript
const llmWithTools = ctx.llm.bindTools([
  boundQualifyTool,
  boundPauseSessionTool,
  boundFinishConversationTool,
  boundSearchKnowledgeTool,
  respondTool,
  ...ctx.mcpTools,
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expor enabledTools no BrainBuildContext e ToolsRegistry, injetar no runner</name>
  <files>
    packages/core/src/brain/interface.ts
    packages/core/src/tools/registry.ts
    packages/core/src/runner/runner.ts
  </files>
  <read_first>
    - packages/core/src/brain/interface.ts — ver interface BrainBuildContext atual antes de adicionar campo
    - packages/core/src/tools/registry.ts — ver campo envWhitelist privado antes de expor getter
    - packages/core/src/runner/runner.ts — ver _compileGraph() (~linha 509) onde ctx é montado
  </read_first>
  <behavior>
    - Test 1: new ToolsRegistry() sem BRAIN_TOOLS → getEnvWhitelist() retorna null
    - Test 2: new ToolsRegistry() com BRAIN_TOOLS="qualify_lead,search_knowledge" → getEnvWhitelist() retorna Set{"qualify_lead","search_knowledge"}
    - Test 3: new ToolsRegistry() com BRAIN_TOOLS="" → getEnvWhitelist() retorna null (sem filtro)
    - Test 4: ctx montado pelo runner contém enabledTools igual ao retorno de getEnvWhitelist()
  </behavior>
  <action>
1. Em packages/core/src/brain/interface.ts, adicionar campo enabledTools à BrainBuildContext:

```typescript
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
  sql?: Sql;
  mcpTools: StructuredTool[];
  /**
   * D-01/TECH-01: Whitelist de tools habilitadas via BRAIN_TOOLS ENV.
   * null = sem filtro (BRAIN_TOOLS não setado ou vazio).
   * Set<string> com nomes exatos = filtro ativo para closures nativas em buildGraph().
   * Brain usa: [...tools].filter(t => !ctx.enabledTools || ctx.enabledTools.has(t.name))
   */
  enabledTools: Set<string> | null;
}
```

2. Em packages/core/src/tools/registry.ts, adicionar getter público após o construtor:

```typescript
/**
 * TECH-01: Expõe o envWhitelist para injeção em BrainBuildContext.
 * null = BRAIN_TOOLS não setado ou vazio (sem filtro).
 * Set<string> = whitelist ativa com nomes exatos de tools.
 */
getEnvWhitelist(): Set<string> | null {
  return this.envWhitelist;
}
```

3. Em packages/core/src/runner/runner.ts, na função _compileGraph(), adicionar enabledTools ao ctx imediatamente antes de chamar buildGraph():

Localizar o bloco (linha ~509):
```typescript
const ctx: BrainBuildContext = {
  llm,
  prompts: this.prompts,
  tools: filteredTools,
  sql: this.sql,
  mcpTools,
};
```

Substituir por:
```typescript
const ctx: BrainBuildContext = {
  llm,
  prompts: this.prompts,
  tools: filteredTools,
  sql: this.sql,
  mcpTools,
  // D-02/TECH-01: enabledTools exposto para Brain filtrar closures nativas em buildGraph()
  enabledTools: this.toolsRegistry.getEnvWhitelist(),
};
```

4. Criar arquivo de teste packages/core/src/__tests__/unit/registry/registry-env-whitelist.test.ts com os casos acima usando bun:test. Estrutura seguindo convenção __tests__/unit/.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/__tests__/unit/registry/ --no-summary 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BrainBuildContext.enabledTools: Set&lt;string&gt; | null presente em interface.ts
    - ToolsRegistry.getEnvWhitelist() público retorna this.envWhitelist
    - ctx em _compileGraph() tem campo enabledTools: this.toolsRegistry.getEnvWhitelist()
    - Testes unitários do getter passam
    - TypeScript sem erros: bun run --cwd packages/core tsc --noEmit retorna 0
  </done>
  <acceptance_criteria>
    - grep "enabledTools" packages/core/src/brain/interface.ts retorna linha com "Set&lt;string&gt; | null"
    - grep "getEnvWhitelist" packages/core/src/tools/registry.ts retorna definição do método público
    - grep "enabledTools.*getEnvWhitelist\|getEnvWhitelist.*enabledTools" packages/core/src/runner/runner.ts retorna match
    - bun test packages/core/src/__tests__/unit/registry/ termina com 0 failed
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar filtro enabledTools no brain-sdr buildGraph()</name>
  <files>
    apps/brain-sdr/src/brain.ts
  </files>
  <read_first>
    - apps/brain-sdr/src/brain.ts — ver bloco completo do bindTools() (~linha 99-110) antes de modificar
    - packages/core/src/brain/interface.ts — confirmar que enabledTools foi adicionado na Task 1
  </read_first>
  <behavior>
    - Test 1: ctx.enabledTools = null → bindTools recebe todas as 5 tools nativas + mcpTools (sem filtro)
    - Test 2: ctx.enabledTools = Set{"qualify_lead"} → bindTools recebe apenas boundQualifyTool (+ mcpTools se presentes)
    - Test 3: ctx.enabledTools = Set{"respond"} → bindTools recebe apenas respondTool
    - Test 4: ctx.enabledTools = Set{} vazio impossível por design do ToolsRegistry (BRAIN_TOOLS="" → null), mas se ocorrer: bindTools recebe apenas mcpTools
  </behavior>
  <action>
Em apps/brain-sdr/src/brain.ts, localizar o bloco que monta nativeTools e chama bindTools (após criar as 5 tools: boundQualifyTool, boundPauseSessionTool, boundFinishConversationTool, boundSearchKnowledgeTool, respondTool).

Substituir a chamada direta de bindTools pelo padrão com filtro:

```typescript
// D-03/TECH-01: Filtrar tools nativas pelo enabledTools whitelist.
// ctx.enabledTools = null → sem filtro (BRAIN_TOOLS não setado).
// ctx.enabledTools = Set<string> → apenas tools com nome no Set são vinculadas ao LLM.
// Aplica-se a closures nativas (boundQualifyTool, etc.) E a ctx.mcpTools injetadas.
const nativeTools = [
  boundQualifyTool,
  boundPauseSessionTool,
  boundFinishConversationTool,
  boundSearchKnowledgeTool,
  respondTool,
];
const allTools = [...nativeTools, ...ctx.mcpTools];
const filteredAllTools = ctx.enabledTools
  ? allTools.filter((t) => ctx.enabledTools!.has(t.name))
  : allTools;

const llmWithTools = ctx.llm.bindTools(filteredAllTools);
```

O ToolNode que usa as tools para execução também deve usar filteredAllTools:

```typescript
// O ToolNode usa as mesmas tools filtradas — LLM só pode chamar o que está bound
const toolNode = new ToolNode(filteredAllTools);
```

Verificar se a constante toolNode já existe no brain.ts e substituir sua lista de tools por filteredAllTools.
  </action>
  <verify>
    <automated>cd /root/Brain && bun build apps/brain-sdr/src/brain.ts --target=bun 2>&1 | tail -20</automated>
  </verify>
  <done>
    - brain.ts usa filteredAllTools no bindTools() e no ToolNode()
    - Lógica: ctx.enabledTools null → sem filtro; Set → filtra por nome
    - ToolNode e LLM sincronizados — LLM só chama tools que ToolNode pode executar
    - bun build sem erros de tipo
  </done>
  <acceptance_criteria>
    - grep -n "enabledTools" apps/brain-sdr/src/brain.ts retorna pelo menos 2 linhas (declaração filteredAllTools e uso)
    - grep "filteredAllTools" apps/brain-sdr/src/brain.ts retorna uso em bindTools e em ToolNode
    - grep "ctx\.mcpTools" apps/brain-sdr/src/brain.ts retorna linha dentro do spread para allTools (não no bindTools diretamente)
    - bun build apps/brain-sdr/src/brain.ts --target=bun termina sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → ToolsRegistry | BRAIN_TOOLS vem do ambiente de processo; valor inválido pode ampliar ou suprimir tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | BRAIN_TOOLS ENV parsing | mitigate | Já implementado: filter(Boolean) remove strings vazias; split(",").trim() normaliza espaços; envWhitelist null quando BRAIN_TOOLS="" (WR-02 no registry) |
| T-27-01-02 | Elevation of Privilege | bindTools com enabledTools | mitigate | filteredAllTools filtra tanto nativas quanto mcpTools — LLM não pode chamar tool não listada no BRAIN_TOOLS; ToolNode sincronizado com mesma lista |
| T-27-01-03 | Denial of Service | BRAIN_TOOLS filtra todas as tools | accept | Operador pode filtrar todas as tools intencionalmente; BrainRunner não lança erro (D-07: silencioso); Brain pode não responder a tool calls — risco de negócio, não segurança |
</threat_model>

<verification>
Após execução completa do plano:

1. Com BRAIN_TOOLS não setado: Brain SDR funciona normalmente sem filtro
2. Com BRAIN_TOOLS="qualify_lead": ctx.enabledTools = Set{"qualify_lead"} e bindTools recebe apenas boundQualifyTool + mcpTools com nome "qualify_lead"
3. TypeScript compila sem erros em packages/core e apps/brain-sdr
4. Testes unitários do getter passam
</verification>

<success_criteria>
- TECH-01 implementado: enabledTools flui de ToolsRegistry → BrainRunner → BrainBuildContext → Brain.buildGraph()
- brain-sdr aplica filtro correto em closures nativas E mcpTools
- ToolNode sincronizado com o mesmo filteredAllTools do LLM
- Zero regressão: BRAIN_TOOLS ausente = comportamento idêntico ao anterior
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/27-tech-debt-fixes/27-01-SUMMARY.md` com artefatos criados, decisões implementadas (D-01 a D-05) e padrão de filtro para referência futura.
</output>
