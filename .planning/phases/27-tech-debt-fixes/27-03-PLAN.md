---
phase: 27-tech-debt-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/transport/src/interface.ts
  - packages/transport/src/webhook/handler.ts
  - packages/transport/src/rabbitmq/consumer.ts
  - packages/observability/src/health.ts
  - packages/observability/src/server.ts
  - apps/brain-sdr/src/server.ts
  - apps/brain-sdr/src/index.ts
autonomous: true
requirements:
  - TECH-03
must_haves:
  truths:
    - "GET /health retorna campo 'transport' com type e connected refletindo estado real da conexão"
    - "WebhookTransport.getStatus() sempre retorna { type: 'webhook', connected: true }"
    - "RabbitMQTransport.getStatus() retorna connected: true quando conexão estabelecida, false em reconexão ou não iniciada"
    - "HTTP status 503 quando transport desconectado (mesmo com DB ok) — transport é falha crítica"
    - "createHealthApp(sql) sem transport mantém comportamento anterior (backward compatible)"
    - "brain-sdr passa transport como segundo argumento para createHealthApp()"
  artifacts:
    - path: "packages/transport/src/interface.ts"
      provides: "ITransport com getStatus() + TransportStatus type"
      contains: "getStatus"
    - path: "packages/transport/src/webhook/handler.ts"
      provides: "WebhookTransport.getStatus() implementado"
      contains: "getStatus"
    - path: "packages/transport/src/rabbitmq/consumer.ts"
      provides: "RabbitMQTransport.getStatus() com flag de estado real"
      contains: "getStatus"
    - path: "packages/observability/src/health.ts"
      provides: "HealthCheckResult com transport opcional + performHealthCheck com transport param"
      contains: "TransportStatus"
    - path: "packages/observability/src/server.ts"
      provides: "createHealthApp(sql, transport?) aceita ITransport opcional"
      contains: "transport"
    - path: "apps/brain-sdr/src/server.ts"
      provides: "createServer() aceita transport opcional e passa para createHealthApp"
      contains: "createHealthApp(sql, transport"
    - path: "apps/brain-sdr/src/index.ts"
      provides: "transport criado antes de createServer() em ambos os modos; passado como terceiro arg"
      contains: "createServer(sql, runner, transport"
  key_links:
    - from: "packages/transport/src/rabbitmq/consumer.ts"
      to: "packages/transport/src/interface.ts"
      via: "implements ITransport.getStatus()"
      pattern: "getStatus.*TransportStatus"
    - from: "packages/observability/src/server.ts"
      to: "packages/transport/src/interface.ts"
      via: "transport?.getStatus()"
      pattern: "transport.*getStatus"
    - from: "apps/brain-sdr/src/server.ts"
      to: "packages/observability/src/server.ts"
      via: "createHealthApp(sql, transport)"
      pattern: "createHealthApp.*transport"
    - from: "apps/brain-sdr/src/index.ts"
      to: "apps/brain-sdr/src/server.ts"
      via: "createServer(sql, runner, transport)"
      pattern: "createServer.*transport"
---

<objective>
Corrigir OBS-02: GET /health não expõe o status do transport (conectado/desconectado). Developer não consegue saber via health check se o RabbitMQ está acessível ou se o Brain está efetivamente processando mensagens.

Purpose: Tornar o /health endpoint operacionalmente útil — refletindo estado real do transport além do estado do banco, e retornando 503 quando o transport está desconectado (Brain não processa mensagens = falha crítica).

Output: ITransport com getStatus(), implementações em WebhookTransport e RabbitMQTransport, HealthCheckResult expandido, createHealthApp() com parâmetro opcional de transport, brain-sdr server.ts e index.ts wired corretamente com transport criado antes de createServer() em ambos os modos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-tech-debt-fixes/27-CONTEXT.md

<interfaces>
<!-- Contratos atuais e novos que o executor deve conhecer -->

De packages/transport/src/interface.ts (atual — será expandido):
```typescript
export interface ITransport {
  start(port?: number): Promise<void>;
  stop(): Promise<void>;
  // ADICIONAR: getStatus(): TransportStatus
}
```

De packages/observability/src/health.ts (atual — será expandido):
```typescript
export interface HealthCheckResult {
  status: 'ok' | 'degraded' | 'error';
  checks: { db: 'connected' | 'failed'; };
  version?: string;
  timestamp: string;
}
export async function performHealthCheck(sql: Sql): Promise<HealthCheckResult>
```

De packages/observability/src/server.ts (atual — será modificado):
```typescript
export function createHealthApp(sql: Sql): Hono
```

De apps/brain-sdr/src/server.ts (atual — sera modificado):
```typescript
export function createServer(sql: Sql, runner: BrainRunner): Hono {
  app.route("/", createHealthApp(sql));  // sem transport
  ...
}
```

De apps/brain-sdr/src/index.ts (atual — precisa ser modificado):
```typescript
// Problema atual: transport só é criado APÓS createServer() e apenas quando TRANSPORT=rabbitmq.
// WebhookTransport não é criado em modo webhook — não há objeto para passar ao createServer().
// Novo fluxo obrigatório:
// 1. createTransport(runner) em AMBOS os modos (retorna WebhookTransport ou RabbitMQTransport)
// 2. createServer(sql, runner, transport) — transport disponível para /health
// 3. transport.start() — inicia escuta (webhook usa Bun.serve, rabbitmq inicia consumer)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar TransportStatus + getStatus() na interface e implementações</name>
  <files>
    packages/transport/src/interface.ts
    packages/transport/src/webhook/handler.ts
    packages/transport/src/rabbitmq/consumer.ts
    packages/transport/src/__tests__/unit/transport-status.test.ts
  </files>
  <read_first>
    - packages/transport/src/interface.ts — ver interface ITransport atual antes de expandir
    - packages/transport/src/webhook/handler.ts — ver classe WebhookTransport (campo this.server) para determinar connected
    - packages/transport/src/rabbitmq/consumer.ts — ver classe RabbitMQTransport (campo this.rabbit) para estado de conexão
  </read_first>
  <behavior>
    - Test 1: WebhookTransport.getStatus() retorna { type: 'webhook', connected: true } antes de start()
    - Test 2: WebhookTransport.getStatus() retorna { type: 'webhook', connected: true } após stop() (HTTP server não tem estado de conexão separado)
    - Test 3: RabbitMQTransport.getStatus() retorna { type: 'rabbitmq', connected: false } antes de start()
    - Test 4: RabbitMQTransport.getStatus() retorna { type: 'rabbitmq', connected: true } após setar this.connected=true via monkey-patch (simula evento 'connection')
    - Test 5: RabbitMQTransport.getStatus() retorna { type: 'rabbitmq', connected: false } após stop() (campo connected setado como false no stop())
  </behavior>
  <action>
1. Em packages/transport/src/interface.ts, adicionar TransportStatus e getStatus():

```typescript
/**
 * D-11/TECH-03: Status snapshot do transport.
 * Retornado por ITransport.getStatus() para expor no GET /health.
 */
export interface TransportStatus {
  type: 'webhook' | 'rabbitmq';
  connected: boolean;
}

/**
 * TRANS-01: Abstract transport interface.
 * All transport implementations must satisfy this contract.
 */
export interface ITransport {
  start(port?: number): Promise<void>;
  stop(): Promise<void>;
  /**
   * D-10/TECH-03: Retorna status atual do transport.
   * WebhookTransport: sempre { type: 'webhook', connected: true }.
   * RabbitMQTransport: connected=true quando conexão RabbitMQ está estabelecida.
   */
  getStatus(): TransportStatus;
}
```

2. Em packages/transport/src/webhook/handler.ts, na classe WebhookTransport, adicionar método getStatus():

```typescript
/**
 * D-12/TECH-03: HTTP server não tem estado de conexão separado — sempre connected.
 * Servidor Bun para de aceitar requests apenas quando stop() é chamado (gerenciado externamente).
 */
getStatus(): TransportStatus {
  return { type: 'webhook', connected: true };
}
```

Adicionar import de TransportStatus: `import type { ITransport, TransportStatus } from "../interface.js";`
(Substituir import existente de ITransport para incluir TransportStatus)

3. Em packages/transport/src/rabbitmq/consumer.ts, na classe RabbitMQTransport:

Adicionar campo privado ANTES do construtor:
```typescript
private connected = false;
```

No método start(), dentro do listener do evento de conexão bem-sucedida (onde logger.info de "RabbitMQ connected" é chamado), adicionar:
```typescript
this.connected = true;
```

No método stop(), ANTES de fechar as conexões, adicionar:
```typescript
this.connected = false;
```

Adicionar método getStatus():
```typescript
/**
 * D-13/TECH-03: Retorna estado real da conexão RabbitMQ.
 * connected=true após evento 'connection' + antes de stop().
 * connected=false antes de start() ou após stop().
 */
getStatus(): TransportStatus {
  return { type: 'rabbitmq', connected: this.connected };
}
```

Substituir import existente de ITransport para incluir TransportStatus.

4. Criar packages/transport/src/__tests__/unit/transport-status.test.ts com os testes acima usando bun:test. Não usar network real — testar WebhookTransport instanciando diretamente (sem chamar start()), e RabbitMQTransport via monkey-patch de (transport as any).connected = true/false.
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/transport/src/__tests__/unit/transport-status.test.ts --no-summary 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TransportStatus type exportado de packages/transport/src/interface.ts
    - ITransport.getStatus(): TransportStatus na interface
    - WebhookTransport.getStatus() sempre retorna { type: 'webhook', connected: true }
    - RabbitMQTransport tem campo connected = false inicialmente; getStatus() reflete estado real
    - Testes unitários de status passam (sem network real)
  </done>
  <acceptance_criteria>
    - grep "TransportStatus" packages/transport/src/interface.ts retorna definição da interface com 'webhook' | 'rabbitmq'
    - grep "getStatus" packages/transport/src/interface.ts retorna assinatura do método
    - grep "getStatus" packages/transport/src/webhook/handler.ts retorna implementação retornando { type: 'webhook', connected: true }
    - grep "getStatus\|connected" packages/transport/src/rabbitmq/consumer.ts retorna campo + implementação
    - bun test packages/transport/src/__tests__/unit/transport-status.test.ts termina sem erros
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expandir HealthCheckResult, createHealthApp() e rearranjar startup do brain-sdr</name>
  <files>
    packages/observability/src/health.ts
    packages/observability/src/server.ts
    apps/brain-sdr/src/server.ts
    apps/brain-sdr/src/index.ts
  </files>
  <read_first>
    - packages/observability/src/health.ts — ver HealthCheckResult e performHealthCheck() atual antes de expandir
    - packages/observability/src/server.ts — ver createHealthApp(sql) atual antes de adicionar parâmetro
    - apps/brain-sdr/src/server.ts — ver como createServer() monta createHealthApp() e sua assinatura atual
    - apps/brain-sdr/src/index.ts — entender startup sequence atual: createServer() é chamado ANTES de createTransport(); o novo fluxo inverte essa ordem
  </read_first>
  <behavior>
    - Test 1: performHealthCheck(sql, undefined) → resultado sem campo transport (backward compat)
    - Test 2: performHealthCheck(sql, webhookTransport) → resultado.transport = { type: 'webhook', connected: true }; status 'ok'
    - Test 3: performHealthCheck(sql, rabbitMQTransport com connected=false) + db ok → status 'degraded', checks.transport = 'disconnected', HTTP 503
    - Test 4: performHealthCheck(sql, rabbitMQTransport com connected=true) + db ok → status 'ok', checks.transport = 'connected', HTTP 200
    - Test 5: performHealthCheck(sql, transport) + db failed → status 'error', HTTP 503 (db falha é mais grave)
  </behavior>
  <action>
1. Em packages/observability/src/health.ts:

Adicionar import de ITransport e TransportStatus (import type de @brain-pkg/transport):
```typescript
import type { ITransport, TransportStatus } from "@brain-pkg/transport";
```

Expandir HealthCheckResult:
```typescript
export interface HealthCheckResult {
  status: 'ok' | 'degraded' | 'error';
  checks: {
    db: 'connected' | 'failed';
    /**
     * D-15/TECH-03: Status do transport. Omitido quando transport não injetado (backward compat).
     * 'connected' = transport operacional; 'disconnected' = transport em falha/reconexão.
     */
    transport?: 'connected' | 'disconnected';
  };
  /**
   * D-16/TECH-03: TransportStatus completo (type + connected).
   * Omitido quando transport não injetado (backward compat).
   */
  transport?: TransportStatus;
  version?: string;
  timestamp: string;
}
```

Expandir performHealthCheck() para aceitar transport opcional:
```typescript
/**
 * OBS-02/TECH-03: Realiza health check completo incluindo status do transport.
 *
 * HTTP status code mapping (D-14/D-16):
 * - 200: status === 'ok' (db + transport ok)
 * - 503: db === 'failed' OU transport === 'disconnected' (qualquer dependência crítica com falha)
 * - 500: status === 'error' (erro interno inesperado)
 *
 * @param sql - postgres.js Sql instance para verificar DB
 * @param transport - ITransport opcional; omitido = campo transport ausente do resultado
 */
export async function performHealthCheck(sql: Sql, transport?: ITransport): Promise<HealthCheckResult> {
  const dbOk = await checkDatabase(sql);
  const transportStatus = transport?.getStatus();
  const transportOk = transportStatus ? transportStatus.connected : true; // sem transport = não verifica

  // D-16: transport desconectado = 'degraded' (Brain não processa mensagens)
  // db falha = 'error' (mais grave — sem dados)
  const status = !dbOk ? 'error' : !transportOk ? 'degraded' : 'ok';

  return {
    status,
    checks: {
      db: dbOk ? 'connected' : 'failed',
      ...(transportStatus && {
        transport: transportStatus.connected ? 'connected' : 'disconnected',
      }),
    },
    ...(transportStatus && { transport: transportStatus }),
    version: process.env.GIT_COMMIT || 'unknown',
    timestamp: new Date().toISOString(),
  };
}
```

2. Em packages/observability/src/server.ts:

Adicionar import de ITransport:
```typescript
import type { ITransport } from "@brain-pkg/transport";
```

Expandir createHealthApp() para aceitar transport opcional:
```typescript
/**
 * D-14/TECH-03: Cria o Hono app com GET /health expandido com status do transport.
 *
 * @param sql - postgres.js Sql instance para health check de DB
 * @param transport - ITransport opcional para expor status de conexão.
 *   Omitido = resposta sem campo transport (backward compatible — brain-echo não precisa alterar).
 */
export function createHealthApp(sql: Sql, transport?: ITransport): Hono {
  const app = new Hono();

  app.get('/health', async (c) => {
    const result = await performHealthCheck(sql, transport);

    // D-14/D-16: HTTP status
    // 200 = ok, 503 = db failed OU transport disconnected, 500 = error interno
    const httpStatus =
      result.status === 'ok'
        ? 200
        : result.status === 'degraded' || result.checks.db === 'failed'
          ? 503
          : 500;

    return c.json(result, httpStatus);
  });

  return app;
}
```

Manter startServer() sem alteração (não usa transport diretamente — apenas createHealthApp).

3. Em apps/brain-sdr/src/server.ts:

Modificar createServer() para aceitar transport opcional como TERCEIRO argumento e passá-lo ao createHealthApp():
```typescript
import type { ITransport } from "@brain-pkg/transport";

export function createServer(sql: Sql, runner: BrainRunner, transport?: ITransport): Hono {
  const app = new Hono();

  app.route("/", createHealthApp(sql, transport));  // D-14/TECH-03: transport para /health
  app.route("/", createWebhookApp(runner));
  app.route("/", createCoreApp(runner));
  app.route("/", createIngestApp(sql));

  return app;
}
```

4. Em apps/brain-sdr/src/index.ts — rearranjar startup sequence:

O problema atual: `createTransport(runner)` é chamado APÓS `createServer()` e apenas quando TRANSPORT=rabbitmq. Isso impede passar o transport para createServer(). Novo fluxo:

- Criar o transport ANTES de createServer() em AMBOS os modos (webhook e rabbitmq).
- createTransport() já retorna o tipo correto com base na ENV TRANSPORT.
- Passar o transport retornado como terceiro argumento de createServer().
- Chamar transport.start() APÓS createServer() (o start() em modo webhook faz Bun.serve; em rabbitmq inicia consumer).

```typescript
// Substituir o bloco atual (de "const app = createServer..." até o final do if rabbitmq) por:

// D-TECH-03: criar transport ANTES de createServer() para que /health possa expor status.
// createTransport() retorna WebhookTransport ou RabbitMQTransport baseado em TRANSPORT ENV.
const transport = createTransport(runner);

// Montar app com transport disponível para /health
const app = createServer(sql, runner, transport);
const port = parseInt(process.env.PORT || "3000", 10);

// Iniciar servidor HTTP (Bun.serve para o Hono app — sempre necessário para /health e /webhook)
Bun.serve({ port, fetch: app.fetch });
logger.info({ port }, "brain-sdr server listening");

// Iniciar transport (webhook não precisa de start() adicional — usa Bun.serve acima;
// rabbitmq precisa de start() para conectar ao broker e iniciar consumer)
await transport.start();
logger.info({ transport: process.env.TRANSPORT ?? "webhook" }, "transport started");
```

ATENÇÃO: verificar a assinatura atual de createTransport() para confirmar que aceita BrainRunner como argumento único. Se webhook mode precisar de porta, verificar se transport.start(port) precisa ser chamado ou se Bun.serve é suficiente. Ler o código de WebhookTransport.start() antes de modificar para não quebrar o comportamento existente do webhook.
  </action>
  <verify>
    <automated>cd /root/Brain && bun build apps/brain-sdr/src/index.ts --target=bun 2>&1 | tail -20</automated>
  </verify>
  <done>
    - performHealthCheck(sql, transport?) aceita transport opcional
    - HealthCheckResult tem campos transport? e checks.transport?
    - createHealthApp(sql, transport?) repassa transport para performHealthCheck
    - HTTP 503 quando status='degraded' (transport desconectado)
    - apps/brain-sdr/src/server.ts aceita transport como terceiro argumento e passa para createHealthApp
    - apps/brain-sdr/src/index.ts cria transport ANTES de createServer() em ambos os modos; passa como terceiro arg
    - TypeScript sem erros em todos os arquivos modificados
  </done>
  <acceptance_criteria>
    - grep "transport\?: ITransport" packages/observability/src/server.ts retorna assinatura expandida de createHealthApp
    - grep "transport\?" packages/observability/src/health.ts retorna campos em HealthCheckResult
    - grep "performHealthCheck.*transport\|transport.*performHealthCheck" packages/observability/src/server.ts retorna passagem do arg
    - grep "createHealthApp.*transport\|transport.*createHealthApp" apps/brain-sdr/src/server.ts retorna linha com transport passado
    - grep "503\|degraded" packages/observability/src/server.ts retorna lógica de HTTP status para transport desconectado
    - grep "createServer.*transport\|transport.*createServer" apps/brain-sdr/src/index.ts retorna chamada com transport
    - grep "createTransport" apps/brain-sdr/src/index.ts retorna chamada ANTES da linha createServer (verificar ordem no arquivo)
    - bun build apps/brain-sdr/src/index.ts --target=bun termina sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /health → consumer | Endpoint público (protegido apenas por WEBHOOK_TOKEN em POST; GET /health é geralmente sem auth) |
| TransportStatus → response JSON | Status de conexão exposto externamente via HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Information Disclosure | /health expondo TransportStatus | mitigate | TransportStatus contém apenas type (webhook/rabbitmq) e connected (boolean) — nunca expõe RABBITMQ_URL, credenciais ou stack traces (alinhado com T-05-01 existente) |
| T-27-03-02 | Denial of Service | RabbitMQ disconnected → 503 loop | accept | 503 é o comportamento correto — sinaliza para load balancer/k8s que o pod não está saudável; auto-reconexão do rabbitmq-client resolve sem intervenção manual |
| T-27-03-03 | Spoofing | Falso connected=true em RabbitMQTransport | mitigate | connected flag setada apenas no evento 'connection' do rabbitmq-client (não configurável via ENV); setada como false no stop() — estado derivado de evento real, não de configuração |
| T-27-03-04 | Tampering | Adição de campo transport sem backward compat | mitigate | transport? é opcional em HealthCheckResult e createHealthApp() — brain-echo e outros consumers sem transport continuam funcionando sem alteração (D-14 garante backward compat) |
</threat_model>

<verification>
Após execução completa do plano:

1. curl http://localhost:3000/health retorna JSON com campo "transport" quando transport injetado
2. Campo transport.type = "webhook" e transport.connected = true para WebhookTransport
3. checks.transport = "connected" no response JSON
4. HTTP 200 quando db ok e transport connected
5. HTTP 503 quando transport disconnected (simulado com RabbitMQTransport antes de start())
6. brain-echo (sem transport) continua retornando health sem campo transport (backward compat)
7. bun build apps/brain-sdr/src/index.ts --target=bun termina sem erros (startup sequence correta)
</verification>

<success_criteria>
- TECH-03 implementado: GET /health expõe transport status em campo dedicado
- ITransport.getStatus() implementado em WebhookTransport e RabbitMQTransport
- HTTP 503 quando transport desconectado (Brain não processa mensagens = falha crítica per D-16)
- backward compatible: createHealthApp(sql) sem transport funciona como antes
- brain-sdr wired: transport criado antes de createServer(), passado como terceiro argumento
- index.ts: fluxo correto — createTransport() → createServer(sql, runner, transport) → transport.start()
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/27-tech-debt-fixes/27-03-SUMMARY.md` com artefatos modificados, contratos novos (TransportStatus, ITransport.getStatus()), novo startup sequence do index.ts e exemplo de resposta JSON do /health expandido.
</output>
