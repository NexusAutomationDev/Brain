---
phase: 08-brainrunner-conversation-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/runner/__tests__/brain-runner.integration.test.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "Integration test verifica explicitamente que thread_id = lead.uniqueId (IDLead canônico), não event.Numero"
    - "Integration test verifica que a segunda chamada com mesmo IDLead mas Numero diferente acumula histórico no mesmo thread"
    - "Comentário 'Phase 8: substituir por lead.unique_id' foi removido do teste"
  artifacts:
    - path: "packages/core/src/runner/__tests__/brain-runner.integration.test.ts"
      provides: "Testes de integração com asserts de HIST-01 e HIST-02"
      contains: "lead-hist-001"
  key_links:
    - from: "brain-runner.integration.test.ts"
      to: "BrainRunner.run()"
      via: "runner.run(event) com IDLead='lead-hist-001'"
      pattern: "lead-hist-001"
    - from: "brain-runner.integration.test.ts"
      to: "PostgresSaver checkpoint"
      via: "segundo run() com mesmo IDLead acumula messages"
      pattern: "state.messages.length\\|msgCount"
---

<objective>
Atualizar o integration test de BrainRunner para verificar HIST-01 e HIST-02 com asserts explícitos e verificáveis.

Purpose: O código `threadId = lead.uniqueId` já está implementado em `runner.ts:171` (WR-02 da Phase 7). Esta fase confirma que o behavior está correto com asserts que provam: (a) thread_id deriva do IDLead canônico e não do Numero de telefone, e (b) o histórico de conversa persiste entre chamadas ao mesmo IDLead.

Output: `brain-runner.integration.test.ts` atualizado com dois novos testes (HIST-01 e HIST-02) e o comentário legado removido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-brainrunner-conversation-history/08-CONTEXT.md
@.planning/phases/08-brainrunner-conversation-history/08-RESEARCH.md

<interfaces>
<!-- Tipos e contratos que o executor precisa. Extraídos do codebase. -->

De packages/core/src/runner/runner.ts (linha 171):
```typescript
// WR-02: usar lead.uniqueId (IDLead canonical) como thread_id — não event.Numero.
const threadId = lead.uniqueId;
```

De @brain-pkg/transport (BrainEvent):
```typescript
interface BrainEvent {
  Name: string;
  Message: string;
  Numero: string;   // telefone — NÃO é o thread_id
  IDLead: string;   // identificador canônico do lead = thread_id
}
```

De packages/ai/src/graph/state.ts:
```typescript
export const BrainStateAnnotation = Annotation.Root({
  messages: Annotation<BaseMessage[]>({
    reducer: messagesStateReducer,
    default: () => [],
  }),
  userId: Annotation<string>({ ... }),
  sessionId: Annotation<string>({ ... }),
});
```

Padrão de graph de teste (existente no arquivo):
```typescript
buildGraph: (context) => {
  const graph = new StateGraph(BrainStateAnnotation);
  graph.addNode("respond", async (state: any) => {
    return {
      messages: [
        { role: "human", content: "hello" },
        { role: "ai", content: "Integration test reply" },
      ],
    };
  });
  graph.setEntryPoint("respond");
  return graph;
},
```

Pitfall crítico (RESEARCH.md Pitfall 5): `compiledGraph` é private em BrainRunner.
Solução: o graph de teste codifica `state.messages.length` na resposta — permite assert via `result.reply`.

Pitfall crítico (RESEARCH.md Pitfall 4): comentário legado `// mesmo Numero = mesmo thread` na linha 101 está ERRADO. Remover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar integration test — asserts HIST-01 e HIST-02</name>
  <files>packages/core/src/runner/__tests__/brain-runner.integration.test.ts</files>
  <read_first>
    - packages/core/src/runner/__tests__/brain-runner.integration.test.ts (arquivo a modificar — ler estado atual completo)
    - packages/core/src/runner/runner.ts (verificar assinatura de run() e threadId em linha 171)
    - packages/ai/src/graph/state.ts (verificar BrainStateAnnotation e messagesStateReducer)
    - .planning/phases/08-brainrunner-conversation-history/08-RESEARCH.md (seção "Exemplo 3: Assert de HIST-01", "Common Pitfalls #4, #5")
  </read_first>
  <action>
Substituir o integration test existente por uma versão atualizada que:

1. **Mantém** o teste existente "BrainRunner end-to-end with PostgreSQL real" (renomear para "HIST-00: BrainRunner basic end-to-end")

2. **Remove** o comentário legado `// mesmo Numero = mesmo thread (Phase 8: substituir por lead.unique_id)` da linha 101 e a variável `event2` atual com mesmo Numero

3. **Adiciona** um novo `testBrain` "history-aware" no escopo do `describeOrSkip` que codifica `state.messages.length` na resposta:
   ```typescript
   const historyAwareBrain: IBrain = {
     id: "test-hist",
     brainType: "integration-test",
     promptKeys: ["system"],
     tools: [],
     buildGraph: (_context) => {
       const graph = new StateGraph(BrainStateAnnotation);
       graph.addNode("respond", async (state: any) => {
         const msgCount = (state.messages ?? []).length;
         return {
           messages: [
             { role: "ai", content: `reply:msgCount=${msgCount}` },
           ],
         };
       });
       graph.setEntryPoint("respond");
       return graph;
     },
   };
   ```

4. **Adiciona** teste "HIST-01: thread_id = lead.uniqueId (IDLead), não event.Numero":
   ```typescript
   test("HIST-01: thread_id = IDLead canônico, não event.Numero", async () => {
     const registry = new ToolsRegistry();
     registry.enableTool("integration-test", "dummy");
     const runner = new BrainRunner({
       brain: historyAwareBrain,
       sql,
       toolsRegistry: registry,
     });
     await runner.init();

     // Mesmo IDLead, Numeros DIFERENTES — se thread_id fosse Numero, o
     // segundo evento não teria histórico do primeiro
     const event1: BrainEvent = {
       Name: "Lead A",
       Message: "primeira mensagem",
       Numero: "5511111111111",     // Numero 1
       IDLead: "lead-hist-001",     // IDLead canônico
     };
     const event2: BrainEvent = {
       Name: "Lead A",
       Message: "segunda mensagem",
       Numero: "5519999999999",     // Numero DIFERENTE
       IDLead: "lead-hist-001",     // mesmo IDLead canônico
     };

     const result1 = await runner.run(event1);
     expect(result1).not.toBeNull();
     // Primeiro turno: nenhuma mensagem no checkpoint ainda — msgCount=0 ou 1 (apenas a mensagem atual)
     expect(result1!.reply).toContain("reply:msgCount=");

     const result2 = await runner.run(event2);
     expect(result2).not.toBeNull();
     // Segundo turno com mesmo IDLead (Numero diferente): deve ver mensagens do turno anterior
     // Se thread_id fosse Numero, segundo turno começaria do zero (msgCount=0 ou 1)
     // Se thread_id for IDLead, segundo turno herda histórico (msgCount > 1)
     const msgCount2 = parseInt(result2!.reply.split("msgCount=")[1] ?? "0", 10);
     expect(msgCount2).toBeGreaterThan(1);
   }, 30000);
   ```

5. **Adiciona** teste "HIST-02: histórico persiste entre chamadas com mesmo IDLead":
   ```typescript
   test("HIST-02: histórico de conversa persiste entre chamadas com mesmo IDLead", async () => {
     const registry = new ToolsRegistry();
     registry.enableTool("integration-test", "dummy");
     const runner = new BrainRunner({
       brain: historyAwareBrain,
       sql,
       toolsRegistry: registry,
     });
     await runner.init();

     const baseEvent: BrainEvent = {
       Name: "Lead Hist02",
       Message: "mensagem inicial",
       Numero: "5511222222222",
       IDLead: "lead-hist-002",
     };

     // Primeira chamada — histórico vazio
     const result1 = await runner.run(baseEvent);
     expect(result1).not.toBeNull();
     const msgCount1 = parseInt(result1!.reply.split("msgCount=")[1] ?? "0", 10);

     // Segunda chamada com mesmo IDLead — mesmo runner, deve recuperar checkpoint do PostgresSaver
     const result2 = await runner.run({ ...baseEvent, Message: "mensagem de retorno" });
     expect(result2).not.toBeNull();
     const msgCount2 = parseInt(result2!.reply.split("msgCount=")[1] ?? "0", 10);

     // HIST-02: segundo turno deve ter mais mensagens que o primeiro
     expect(msgCount2).toBeGreaterThan(msgCount1);
   }, 30000);
   ```

6. **Adiciona** cleanup do beforeAll/afterAll para incluir delete em `leads` onde numero IN ('5511111111111', '5519999999999', '5511222222222') para não deixar lixo entre execuções de teste.

Nota: o `beforeAll` existente semeia `prompts` com `brainType: "integration-test"` — manter esse seed, os novos testes usam o mesmo `brainType`.

Usar os imports existentes — não adicionar novos imports, todos já estão no arquivo (`BrainEvent`, `IBrain`, `StateGraph`, `BrainStateAnnotation`, `BrainRunner`, `ToolsRegistry`).
  </action>
  <verify>
    <automated>grep -n "HIST-01\|HIST-02\|lead-hist-001\|lead-hist-002\|msgCount\|Phase 8: substituir" packages/core/src/runner/__tests__/brain-runner.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "HIST-01" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna linha de teste
    - grep "HIST-02" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna linha de teste
    - grep "Phase 8: substituir" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna VAZIO (comentário removido)
    - grep "lead-hist-001" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna ao menos 2 linhas
    - grep "5519999999999" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna linha (Numero diferente no HIST-01)
    - grep "msgCount2.*toBeGreaterThan" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna linha
    - grep "historyAwareBrain" packages/core/src/runner/__tests__/brain-runner.integration.test.ts retorna ao menos 3 linhas
  </acceptance_criteria>
  <done>Integration test contém dois novos testes (HIST-01 e HIST-02) com asserts verificáveis via msgCount codificado na reply. Comentário legado removido. O arquivo compila sem erros de TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evento externo → BrainRunner.run() | Campos Name, Message, Numero, IDLead vêm do transport (webhook ou RabbitMQ) — não são confiáveis diretamente |
| IDLead payload → thread_id | IDLead do payload NUNCA é usado diretamente como thread_id — passa pelo DB upsert que retorna lead.uniqueId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | thread_id derivado de IDLead externo | mitigate | Já mitigado: `threadId = lead.uniqueId` vem do banco após upsert (runner.ts:171). HIST-01 test verifica que Numero diferente com mesmo IDLead usa mesmo thread — confirmando que spoofing via Numero é impossível. |
| T-08-02 | Tampering | Dados de teste poluindo ambiente real | accept | Testes de integração usam `describeOrSkip` com `TEST_DB_URL` separado. Cleanup em `afterAll` remove dados de teste. Risco baixo — sem dado sensível nos fixtures. |
</threat_model>

<verification>
```bash
# Verificar que asserts existem e comentário legado foi removido
grep -c "HIST-01\|HIST-02" packages/core/src/runner/__tests__/brain-runner.integration.test.ts
grep -c "Phase 8: substituir" packages/core/src/runner/__tests__/brain-runner.integration.test.ts  # deve ser 0
grep "msgCount2.*toBeGreaterThan" packages/core/src/runner/__tests__/brain-runner.integration.test.ts

# Verificar que arquivo compila (TypeScript check)
cd /root/Brain && bun build packages/core/src/runner/__tests__/brain-runner.integration.test.ts --outdir /tmp/check-08-01 2>&1 | head -20
```
</verification>

<success_criteria>
- `brain-runner.integration.test.ts` contém teste HIST-01 com dois eventos de mesmo IDLead e Numeros diferentes
- `brain-runner.integration.test.ts` contém teste HIST-02 com assert de msgCount crescente entre chamadas
- Comentário `// Phase 8: substituir por lead.unique_id` removido
- O arquivo não tem erros de compilação TypeScript
- Quando `TEST_DB_URL` disponível: `TEST_DB_URL=<url> bun test packages/core/src/runner/__tests__/brain-runner.integration.test.ts` passa todos os testes (incluindo novos)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-brainrunner-conversation-history/08-01-SUMMARY.md` com:
- O que foi implementado (asserts HIST-01 e HIST-02)
- Padrão usado (msgCount codificado na reply do historyAwareBrain)
- Qualquer desvio das decisões (se algum)
- Resultado dos testes (se DB disponível)
</output>
