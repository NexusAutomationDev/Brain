---
phase: 04-validation-brain
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/database/src/migrate.ts
  - packages/database/src/index.ts
  - apps/brain-echo/package.json
  - apps/brain-echo/tsconfig.json
  - apps/brain-echo/src/__tests__/unit/brain.test.ts
  - apps/brain-echo/src/__tests__/integration/webhook.test.ts
  - apps/brain-echo/src/__tests__/integration/restart.test.ts
  - apps/brain-echo/src/__tests__/integration/tenant-pool.test.ts
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "O workspace apps/brain-echo existe com package.json e tsconfig.json válidos"
    - "runMigrations é exportável como função — não executa ao ser importada"
    - "Stubs de teste existem para todos os 4 cenários (unit brain, integration webhook/restart/tenant-pool)"
    - "bun test apps/brain-echo/src/__tests__/unit roda sem erro de parse (stubs passam)"
  artifacts:
    - path: "packages/database/src/migrate.ts"
      provides: "runMigrations(sql, migrationsFolder) exportável + CLI entry via import.meta.main"
      exports: ["runMigrations"]
    - path: "packages/database/src/index.ts"
      provides: "re-export de runMigrations"
      contains: "export { runMigrations }"
    - path: "apps/brain-echo/package.json"
      provides: "workspace @brain-app/echo com deps e scripts corretos"
      contains: "@brain-pkg/core"
    - path: "apps/brain-echo/src/__tests__/unit/brain.test.ts"
      provides: "stub do contrato IBrain"
    - path: "apps/brain-echo/src/__tests__/integration/webhook.test.ts"
      provides: "stub SC-2"
    - path: "apps/brain-echo/src/__tests__/integration/restart.test.ts"
      provides: "stub SC-3"
    - path: "apps/brain-echo/src/__tests__/integration/tenant-pool.test.ts"
      provides: "stub SC-4"
  key_links:
    - from: "apps/brain-echo/src/index.ts"
      to: "packages/database/src/migrate.ts"
      via: "import { runMigrations } from '@brain-pkg/database'"
      pattern: "runMigrations"
    - from: "packages/database/src/index.ts"
      to: "packages/database/src/migrate.ts"
      via: "named export"
      pattern: "export.*runMigrations"
---

<objective>
Preparar o terreno para a implementação do Echo Brain: criar o workspace apps/brain-echo,
refatorar migrate.ts para ser importável, e criar stubs de teste Nyquist-compliant para
os 4 cenários de validação da fase.

Purpose: Nyquist compliance requer que os arquivos de teste existam ANTES da implementação
(Wave 0 = test-first). A refatoração do runMigrations é pré-requisito do entrypoint (Wave 1).
Output: Workspace configurado + função runMigrations exportável + 4 stubs de teste prontos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-brain/04-CONTEXT.md
@.planning/phases/04-validation-brain/04-RESEARCH.md
@.planning/phases/04-validation-brain/04-VALIDATION.md

<interfaces>
<!-- Interfaces críticas que o executor precisa — extraídas do codebase -->

De packages/database/src/migrate.ts (ATUAL — script-only, NÃO importável):
```typescript
// PROBLEMA: runMigrations() é chamada no top-level sem import.meta.main
// Importar este módulo executa as migrations imediatamente (side-effect)
async function runMigrations() { /* ... */ }
runMigrations(); // ← linha 35: este é o gap que precisa ser corrigido
```

De packages/database/src/index.ts (ATUAL — sem runMigrations):
```typescript
export * from './schema/tables.js';
export { TenantPoolManager } from './pool-manager.js';
export type { Sql } from 'postgres';
export { drizzle } from 'drizzle-orm/postgres-js';
export { eq, and, or, sql } from 'drizzle-orm';
// FALTA: export { runMigrations } from './migrate.js';
```

De packages/core/src/brain/interface.ts (IBrain que o EchoBrain vai implementar):
```typescript
export interface IBrain {
  id: string;
  brainType: string;
  promptKeys: string[];
  tools: StructuredTool[];
  buildGraph(ctx: BrainBuildContext): StateGraph<typeof BrainStateAnnotation>;
}
```

De packages/database/src/pool-manager.ts (usado no stub SC-4):
```typescript
export class TenantPoolManager {
  constructor(config: { host: string; port: number; username: string; password: string; max: number; idle_timeout: number }, maxTenants: number)
  getPool(databaseName: string): Sql
  closeAll(): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refatorar migrate.ts + criar workspace apps/brain-echo</name>
  <read_first>
    - packages/database/src/migrate.ts (ler antes de modificar — entender script atual)
    - packages/database/src/index.ts (ler antes de modificar — entender exports atuais)
    - packages/core/tsconfig.json (padrão de tsconfig para apps/)
    - packages/core/package.json (padrão de package.json para workspace)
    - pnpm-workspace.yaml (confirmar que apps/* já está incluído)
  </read_first>
  <files>
    packages/database/src/migrate.ts,
    packages/database/src/index.ts,
    apps/brain-echo/package.json,
    apps/brain-echo/tsconfig.json
  </files>
  <action>
**1. Refatorar packages/database/src/migrate.ts**

Substituir o conteúdo completo do arquivo. A função runMigrations deve:
- Aceitar `sql: Sql` e `migrationsFolder: string` como parâmetros
- Criar extensão vector e rodar migrations com o sql injetado
- NÃO chamar `process.exit()` — lançar erro para o caller tratar
- O bloco de script CLI usa `import.meta.main` para detectar execução direta

Conteúdo final:
```typescript
import { drizzle } from 'drizzle-orm/postgres-js';
import { migrate } from 'drizzle-orm/postgres-js/migrator';
import type { Sql } from 'postgres';

/**
 * Exportável: chamado pelo entrypoint de apps/brain-echo no startup.
 * Recebe Sql injetado — sem criar nova conexão.
 * Lança erro em caso de falha (o caller decide se faz process.exit).
 */
export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void> {
  const db = drizzle(sql);
  await sql`CREATE EXTENSION IF NOT EXISTS vector`;
  await migrate(db, { migrationsFolder });
}

// Script CLI: mantém comportamento original para `bun src/migrate.ts`
if (import.meta.main) {
  const postgres = (await import('postgres')).default;
  const connectionString = process.env.DATABASE_URL;
  if (!connectionString) {
    console.error('DATABASE_URL not set');
    process.exit(1);
  }
  const sql = postgres(connectionString, { max: 1 });
  try {
    console.log('Starting migrations...');
    await runMigrations(sql, './src/migrations');
    console.log('Migrations completed successfully');
    process.exit(0);
  } catch (error) {
    console.error('Migration failed:', error);
    process.exit(1);
  } finally {
    await sql.end();
  }
}
```

**2. Atualizar packages/database/src/index.ts**

Adicionar a seguinte linha ao final dos exports existentes:
```typescript
export { runMigrations } from './migrate.js';
```

**3. Criar apps/brain-echo/package.json**

Criar o diretório `apps/brain-echo/` e o arquivo package.json com o conteúdo exato:
```json
{
  "name": "@brain-app/echo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "bun test src/__tests__/unit",
    "test:integration": "bun test src/__tests__/integration",
    "typecheck": "tsc --noEmit",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@brain-pkg/core": "workspace:*",
    "@brain-pkg/database": "workspace:*",
    "@brain-pkg/observability": "workspace:*",
    "@brain-pkg/transport": "workspace:*",
    "hono": "^4.12.0",
    "postgres": "^3.4.9",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
```

**4. Criar apps/brain-echo/tsconfig.json**

Criar o arquivo com o conteúdo exato (seguindo padrão de packages/core/tsconfig.json, ajustando paths para ../../):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"],
  "references": [
    { "path": "../../packages/shared" },
    { "path": "../../packages/database" },
    { "path": "../../packages/observability" },
    { "path": "../../packages/ai" },
    { "path": "../../packages/memory" },
    { "path": "../../packages/transport" },
    { "path": "../../packages/core" }
  ]
}
```

**5. Rodar pnpm install para atualizar lockfile**

Após criar os arquivos, rodar `pnpm install` na raiz para que apps/brain-echo seja registrado no workspace e o lockfile seja atualizado. Isso é necessário para que `pnpm install --frozen-lockfile` no Dockerfile funcione.
  </action>
  <verify>
    grep -n "export async function runMigrations" /root/Brain/packages/database/src/migrate.ts
    grep -n "import.meta.main" /root/Brain/packages/database/src/migrate.ts
    grep -n "runMigrations" /root/Brain/packages/database/src/index.ts
    grep -n "@brain-app/echo" /root/Brain/apps/brain-echo/package.json
    grep -n "workspace:\*" /root/Brain/apps/brain-echo/package.json
    test -f /root/Brain/apps/brain-echo/tsconfig.json
  </verify>
  <acceptance_criteria>
    - packages/database/src/migrate.ts contém `export async function runMigrations(sql: Sql, migrationsFolder: string): Promise<void>`
    - packages/database/src/migrate.ts contém `if (import.meta.main)` — não chama runMigrations no top-level
    - packages/database/src/index.ts contém `export { runMigrations } from './migrate.js'`
    - apps/brain-echo/package.json contém `"name": "@brain-app/echo"`
    - apps/brain-echo/package.json contém `"@brain-pkg/core": "workspace:*"`
    - apps/brain-echo/tsconfig.json contém `"extends": "../../tsconfig.base.json"`
    - apps/brain-echo/tsconfig.json contém referência a `"../../packages/core"`
    - pnpm install completa sem ERR_PNPM_OUTDATED_LOCKFILE
  </acceptance_criteria>
  <done>migrate.ts é importável sem side-effects; runMigrations está exportado em index.ts; workspace apps/brain-echo existe com package.json e tsconfig.json válidos; lockfile atualizado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar stubs de teste Nyquist-compliant (Wave 0)</name>
  <read_first>
    - .planning/phases/04-validation-brain/04-VALIDATION.md (quais stubs são necessários e o que cada um cobre)
    - .planning/phases/04-validation-brain/04-RESEARCH.md (padrões de teste SC-3 e SC-4 já escritos)
    - packages/core/src/__tests__/ (ver estrutura de diretórios existente como referência)
  </read_first>
  <files>
    apps/brain-echo/src/__tests__/unit/brain.test.ts,
    apps/brain-echo/src/__tests__/integration/webhook.test.ts,
    apps/brain-echo/src/__tests__/integration/restart.test.ts,
    apps/brain-echo/src/__tests__/integration/tenant-pool.test.ts
  </files>
  <action>
Criar os 4 arquivos de stub de teste. Cada stub deve:
- Importar o módulo correto (mesmo que ainda não exista — todo test.todo passa)
- Usar `test.todo()` para marcar os comportamentos esperados
- Ter pelo menos um `test()` que passa imediatamente (para validar que o arquivo é parseável)

**1. apps/brain-echo/src/__tests__/unit/brain.test.ts**

```typescript
// Wave 0 stub — IBrain contract tests for EchoBrain
// Stubs criados antes da implementação (Nyquist compliance)
// Implementação completa ocorre no plano 04-02 (Wave 1)

import { describe, test, expect } from "bun:test";

describe("EchoBrain — IBrain contract", () => {
  test("placeholder: arquivo existe e é parseável", () => {
    expect(true).toBe(true);
  });

  test.todo("echoBrain.id é 'brain-echo'");
  test.todo("echoBrain.brainType é 'echo'");
  test.todo("echoBrain.promptKeys contém 'system'");
  test.todo("echoBrain.tools é array vazio");
  test.todo("echoBrain.buildGraph(ctx) retorna StateGraph não compilado");
  test.todo("buildGraph retorna objeto com método addNode (StateGraph interface)");
});
```

**2. apps/brain-echo/src/__tests__/integration/webhook.test.ts**

```typescript
// Wave 0 stub — SC-2: HTTP end-to-end webhook integration test
// Stubs criados antes da implementação (Nyquist compliance)
// Implementação completa ocorre no plano 04-04 (Wave 2)

import { describe, test, expect } from "bun:test";

const BASE_URL = process.env.ECHO_URL || "http://localhost:3000";

describe("SC-2: POST /api/v1/webhook end-to-end", () => {
  test("placeholder: arquivo existe e é parseável", () => {
    expect(BASE_URL).toBeTruthy();
  });

  test.todo("POST /api/v1/webhook sem X-Request-Id retorna 400");
  test.todo("POST /api/v1/webhook com body inválido retorna 400");
  test.todo("POST /api/v1/webhook duplicado (mesmo X-Request-Id) retorna 409");
  test.todo("POST /api/v1/webhook válido retorna 200 com { status: 'ok', reply: string }");
  test.todo("reply não é vazio (LLM respondeu)");
  test.todo("BrainRunner processa o evento end-to-end (transport → LangGraph → resposta)");
});
```

**3. apps/brain-echo/src/__tests__/integration/restart.test.ts**

```typescript
// Wave 0 stub — SC-3: PostgresSaver persistence across container restart
// Stubs criados antes da implementação (Nyquist compliance)
// Implementação completa ocorre no plano 04-04 (Wave 2)

import { describe, test, expect } from "bun:test";

const CONTAINER_NAME = process.env.ECHO_CONTAINER_NAME || "brain-echo-test";
const BASE_URL = process.env.ECHO_URL || "http://localhost:3000";

describe("SC-3: PostgresSaver persistence across container restart", () => {
  test("placeholder: arquivo existe e é parseável", () => {
    expect(CONTAINER_NAME).toBeTruthy();
  });

  test.todo("turno 1: enviar mensagem com contexto único retorna resposta");
  test.todo("docker restart retorna exit code 0");
  test.todo("turno 2 (após restart): resposta referencia contexto do turno 1");
});
```

**4. apps/brain-echo/src/__tests__/integration/tenant-pool.test.ts**

```typescript
// Wave 0 stub — SC-4: 10 tenants simultâneos abaixo do LRU cap
// Stubs criados antes da implementação (Nyquist compliance)
// Implementação completa ocorre no plano 04-04 (Wave 2)

import { describe, test, expect } from "bun:test";

describe("SC-4: 10 tenants simultâneos abaixo do LRU cap", () => {
  test("placeholder: arquivo existe e é parseável", () => {
    expect(true).toBe(true);
  });

  test.todo("TenantPoolManager instancia 10 pools com DATABASE_NAME diferentes");
  test.todo("queries concorrentes em 10 tenants completam sem erro");
  test.todo("pg_stat_activity mostra conexões abaixo de MAX_TENANTS * POOL_SIZE");
  test.todo("closeAll() fecha todos os pools sem erro");
});
```

Após criar os 4 arquivos, rodar os testes unitários para confirmar que os stubs passam:
`bun test apps/brain-echo/src/__tests__/unit`
  </action>
  <verify>
    bun test /root/Brain/apps/brain-echo/src/__tests__/unit/brain.test.ts
  </verify>
  <acceptance_criteria>
    - apps/brain-echo/src/__tests__/unit/brain.test.ts existe e contém `test("placeholder: arquivo existe e é parseável"`
    - apps/brain-echo/src/__tests__/integration/webhook.test.ts existe e contém `test.todo("POST /api/v1/webhook`
    - apps/brain-echo/src/__tests__/integration/restart.test.ts existe e contém `test.todo("docker restart`
    - apps/brain-echo/src/__tests__/integration/tenant-pool.test.ts existe e contém `test.todo("TenantPoolManager`
    - `bun test apps/brain-echo/src/__tests__/unit` sai com código 0 (stubs placeholder passam)
    - Nenhum arquivo de teste fora de __tests__/ (seguir convenção CLAUDE.md)
  </acceptance_criteria>
  <done>4 arquivos de stub criados; bun test unit sai 0; Nyquist wave_0_complete pode ser marcado true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate.ts import → caller | runMigrations agora é função pura; o caller controla quando e se executa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-00-01 | Tampering | migrate.ts refactor | mitigate | Garantir que `import.meta.main` guard está presente antes de `runMigrations()` call; testar que importar o módulo NÃO executa migrations (acceptance_criteria verifica isso via grep) |
| T-4-00-02 | Information Disclosure | migrate.ts CLI block | accept | O bloco CLI loga erros de migration para stdout — aceitável pois é runtime de inicialização, não resposta HTTP; DATABASE_URL não é logada |
</threat_model>

<verification>
- `grep -n "export async function runMigrations" packages/database/src/migrate.ts` retorna a função exportada
- `grep -n "if (import.meta.main)" packages/database/src/migrate.ts` retorna o guard
- `grep "runMigrations" packages/database/src/index.ts` retorna o export
- `bun test apps/brain-echo/src/__tests__/unit` sai 0
- `ls apps/brain-echo/src/__tests__/integration/` lista 3 arquivos (webhook, restart, tenant-pool)
</verification>

<success_criteria>
- migrate.ts exporta runMigrations(sql, migrationsFolder) sem side-effects ao importar
- packages/database/src/index.ts inclui `export { runMigrations } from './migrate.js'`
- apps/brain-echo workspace criado com package.json (@brain-app/echo) e tsconfig.json válidos
- 4 stubs de teste existem e o suite unit passa
- pnpm-lock.yaml atualizado para incluir apps/brain-echo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-validation-brain/04-00-SUMMARY.md` com:
- Arquivos criados/modificados
- Resultado do bun test unit (saída)
- Confirmação de que runMigrations é exportável
- Decisões tomadas (se alguma)
</output>
