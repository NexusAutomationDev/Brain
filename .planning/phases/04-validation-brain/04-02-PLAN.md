---
phase: 04-validation-brain
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - packages/database/src/migrations/0002_echo_brain_seed.sql
  - apps/brain-echo/Dockerfile
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Migration SQL 0002_echo_brain_seed.sql existe com INSERT do system prompt do Echo Brain"
    - "INSERT usa ON CONFLICT (brain_type, key) DO NOTHING para idempotência"
    - "Dockerfile multi-stage usa oven/bun:1 para ambos os estágios (builder e runner)"
    - "Builder stage instala deps com pnpm install --frozen-lockfile (não bun install)"
    - "Runner stage copia migrations SQL explicitamente (NÃO estão em dist/)"
    - "Runner stage preserva estrutura packages/*/dist/ para symlinks pnpm funcionarem"
    - "docker build -f apps/brain-echo/Dockerfile . produz imagem sem erros"
  artifacts:
    - path: "packages/database/src/migrations/0002_echo_brain_seed.sql"
      provides: "Seed do system prompt do Echo Brain na tabela prompts"
      contains: "INSERT INTO prompts"
    - path: "apps/brain-echo/Dockerfile"
      provides: "Dockerfile multi-stage com 2 estágios: builder + runner"
      contains: "FROM oven/bun:1 AS builder"
  key_links:
    - from: "apps/brain-echo/Dockerfile"
      to: "packages/database/src/migrations/"
      via: "COPY --from=builder /app/packages/database/src/migrations ./migrations"
      pattern: "COPY.*migrations"
    - from: "apps/brain-echo/src/index.ts"
      to: "migrations/0002_echo_brain_seed.sql"
      via: "runMigrations aplica os SQLs na ordem ao iniciar"
      pattern: "runMigrations"
---

<objective>
Criar a migration SQL de seed do system prompt do Echo Brain e o Dockerfile multi-stage
que empacota a aplicação para distribuição.

Purpose: D-06 exige que o prompt seja seedado via migration SQL (idempotente, roda no startup).
D-07/D-08 exigem Dockerfile 2 estágios com oven/bun:1, pnpm --frozen-lockfile, e cópia
explícita dos migrations SQL (pitfall crítico: .sql não vão para dist/).
Output: Imagem Docker construível + prompt disponível no DB ao subir o container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-validation-brain/04-CONTEXT.md
@.planning/phases/04-validation-brain/04-RESEARCH.md
@.planning/phases/04-validation-brain/04-01-SUMMARY.md

<interfaces>
<!-- Interfaces extraídas do codebase — executor usa diretamente -->

De packages/database/src/migrations/0001_lazy_deathstrike.sql (padrão de migration existente):
```sql
CREATE TABLE "prompts" (
  "id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL,
  "brain_type" text NOT NULL,
  "key" text NOT NULL,
  "content" text NOT NULL,
  "created_at" timestamp DEFAULT now() NOT NULL,
  "updated_at" timestamp DEFAULT now() NOT NULL
);
--> statement-breakpoint
CREATE UNIQUE INDEX "prompts_brain_type_key_idx" ON "prompts" USING btree ("brain_type","key");
```

De packages/database/src/migrations/meta/ (estrutura do Drizzle para migrations customizadas):
```
0000_lyrical_scrambler.sql   ← DDL tables originais
0001_lazy_deathstrike.sql    ← DDL prompts table
0002_echo_brain_seed.sql     ← PRÓXIMO: seed do Echo Brain
```

De package.json (raiz) — versão do pnpm a usar no Dockerfile:
```json
"packageManager": "pnpm@11.5.3"
```

De pnpm-workspace.yaml — workspaces incluídos:
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

De apps/brain-echo/src/index.ts (após plano 04-01):
```typescript
const migrationsDir = process.env.MIGRATIONS_DIR
  ?? join(dirname(fileURLToPath(import.meta.url)), "../../migrations");
```
O Dockerfile DEVE definir `ENV MIGRATIONS_DIR=/app/migrations`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar migration SQL de seed do system prompt (D-06)</name>
  <read_first>
    - packages/database/src/migrations/0001_lazy_deathstrike.sql (ver schema da tabela prompts)
    - packages/database/src/migrations/0000_lyrical_scrambler.sql (ver convenção de numeração Drizzle)
    - .planning/phases/04-validation-brain/04-CONTEXT.md (D-06: seed via migration + texto do prompt é Claude's Discretion)
    - .planning/phases/04-validation-brain/04-RESEARCH.md (Pattern 5: seed de prompts via migration SQL + Pitfall 7)
  </read_first>
  <files>
    packages/database/src/migrations/0002_echo_brain_seed.sql
  </files>
  <action>
Criar o arquivo packages/database/src/migrations/0002_echo_brain_seed.sql com o conteúdo:

```sql
-- Seed: system prompt do Echo Brain
-- D-06: System prompt seedado via migration SQL para rodar automaticamente no startup
-- ON CONFLICT DO NOTHING garante idempotência (seguro rodar múltiplas vezes)
-- UNIQUE INDEX em (brain_type, key) foi criado em 0001_lazy_deathstrike.sql

INSERT INTO prompts (brain_type, key, content)
VALUES (
  'echo',
  'system',
  'Você é um assistente útil. Responda às perguntas do usuário de forma clara e concisa. Se não souber a resposta, diga isso honestamente.'
)
ON CONFLICT (brain_type, key) DO NOTHING;
```

Nota: O conteúdo do prompt é Claude's Discretion (conforme CONTEXT.md). O texto escolhido
é simples, em português (alinhado ao projeto), e adequado para validação end-to-end.
O `ON CONFLICT` usa a constraint `(brain_type, key)` que está coberta pelo
`UNIQUE INDEX "prompts_brain_type_key_idx"` criado em 0001_lazy_deathstrike.sql.
  </action>
  <verify>
    grep -n "INSERT INTO prompts" /root/Brain/packages/database/src/migrations/0002_echo_brain_seed.sql
    grep -n "ON CONFLICT" /root/Brain/packages/database/src/migrations/0002_echo_brain_seed.sql
    grep -n "'echo'" /root/Brain/packages/database/src/migrations/0002_echo_brain_seed.sql
    grep -n "'system'" /root/Brain/packages/database/src/migrations/0002_echo_brain_seed.sql
  </verify>
  <acceptance_criteria>
    - packages/database/src/migrations/0002_echo_brain_seed.sql existe
    - Arquivo contém `INSERT INTO prompts (brain_type, key, content)`
    - Arquivo contém `'echo'` como valor de brain_type
    - Arquivo contém `'system'` como valor de key
    - Arquivo contém `ON CONFLICT (brain_type, key) DO NOTHING`
    - Arquivo NÃO contém `ON CONFLICT DO UPDATE` (idempotente — não sobrescreve se já existir)
  </acceptance_criteria>
  <done>Migration SQL de seed existe e é idempotente; ao rodar runMigrations(), o prompt 'echo/system' estará disponível no DB.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar Dockerfile multi-stage (D-07, D-08)</name>
  <read_first>
    - apps/brain-echo/src/index.ts (ver como MIGRATIONS_DIR é usado — precisa de ENV no Dockerfile)
    - .planning/phases/04-validation-brain/04-CONTEXT.md (D-07, D-08 — especificações do Dockerfile)
    - .planning/phases/04-validation-brain/04-RESEARCH.md (Pattern 4: Dockerfile multi-stage + Pitfall 1 migrations + Pitfall 3 symlinks + Pitfall 5 lockfile)
    - package.json (raiz) — confirmar "packageManager": "pnpm@11.5.3"
    - pnpm-workspace.yaml — confirmar apps/* incluído
    - turbo.json — confirmar se existe para copiar no builder stage
  </read_first>
  <files>
    apps/brain-echo/Dockerfile
  </files>
  <action>
Criar apps/brain-echo/Dockerfile com 2 estágios (D-07, D-08).

**Pontos críticos a observar:**
- Pitfall 1: Migrations SQL NÃO estão em dist/ — COPY explícito necessário
- Pitfall 3: Symlinks pnpm — copiar packages/*/dist/ E packages/*/node_modules/ com path idêntico
- Pitfall 5: pnpm install --frozen-lockfile exige lockfile atualizado (feito no plano 04-00)
- D-05/RESEARCH Pattern 4: ENV MIGRATIONS_DIR=/app/migrations para o entrypoint

```dockerfile
# apps/brain-echo/Dockerfile
# Build context: raiz do monorepo (D-08)
# Uso: docker build -f apps/brain-echo/Dockerfile . -t brain-echo

# ─────────────────────────────────────────────────────────────
# Stage 1: builder
# Instala todas as deps e compila TypeScript
# ─────────────────────────────────────────────────────────────
FROM oven/bun:1 AS builder
WORKDIR /app

# Copiar manifests do workspace primeiro (cache layer para deps)
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
# Copiar tsconfig base (necessário para compilação dos packages)
COPY tsconfig.base.json ./
# Copiar turbo.json se existir (Turborepo pipeline para pnpm build)
COPY turbo.json ./

# Copiar todos os packages e o app target
COPY packages/ ./packages/
COPY apps/brain-echo/ ./apps/brain-echo/

# Instalar pnpm na versão exata do projeto (não usar bun install — regressão Jan 2026)
RUN npm install -g pnpm@11.5.3

# Instalar dependências — --frozen-lockfile garante reprodutibilidade
RUN pnpm install --frozen-lockfile

# Compilar todos os packages + app (Turborepo resolve dependências na ordem correta)
RUN pnpm build

# ─────────────────────────────────────────────────────────────
# Stage 2: runner
# Imagem final com apenas artefatos de produção
# ─────────────────────────────────────────────────────────────
FROM oven/bun:1 AS runner
WORKDIR /app

# node_modules do root — contém symlinks pnpm para workspace packages
COPY --from=builder /app/node_modules ./node_modules

# Workspace packages compilados (dist/) — os symlinks em node_modules apontam para esses paths
# Pitfall 3: estrutura de diretórios DEVE ser idêntica ao builder para symlinks funcionarem
COPY --from=builder /app/packages/ai/dist ./packages/ai/dist
COPY --from=builder /app/packages/core/dist ./packages/core/dist
COPY --from=builder /app/packages/database/dist ./packages/database/dist
COPY --from=builder /app/packages/memory/dist ./packages/memory/dist
COPY --from=builder /app/packages/observability/dist ./packages/observability/dist
COPY --from=builder /app/packages/shared/dist ./packages/shared/dist
COPY --from=builder /app/packages/transport/dist ./packages/transport/dist

# node_modules por package — pnpm coloca deps externas não-workspace aqui
COPY --from=builder /app/packages/ai/node_modules ./packages/ai/node_modules
COPY --from=builder /app/packages/core/node_modules ./packages/core/node_modules
COPY --from=builder /app/packages/database/node_modules ./packages/database/node_modules
COPY --from=builder /app/packages/memory/node_modules ./packages/memory/node_modules
COPY --from=builder /app/packages/observability/node_modules ./packages/observability/node_modules
COPY --from=builder /app/packages/shared/node_modules ./packages/shared/node_modules
COPY --from=builder /app/packages/transport/node_modules ./packages/transport/node_modules

# App brain-echo compilado + node_modules do app
COPY --from=builder /app/apps/brain-echo/dist ./apps/brain-echo/dist
COPY --from=builder /app/apps/brain-echo/node_modules ./apps/brain-echo/node_modules

# Migrations SQL — NÃO estão em dist/, DEVEM ser copiadas explicitamente (Pitfall 1)
# Copiadas para /app/migrations/ — path configurado via ENV MIGRATIONS_DIR
COPY --from=builder /app/packages/database/src/migrations ./migrations

# Variáveis de ambiente padrão
ENV PORT=3000
ENV NODE_ENV=production
# MIGRATIONS_DIR: informa ao entrypoint onde encontrar os arquivos SQL em runtime
ENV MIGRATIONS_DIR=/app/migrations

# Usuário não-root para segurança (bun image já tem usuário 'bun')
USER bun

CMD ["bun", "apps/brain-echo/dist/index.js"]
```

Após criar o Dockerfile, executar o build para validar SC-1:
```bash
cd /root/Brain
docker build -f apps/brain-echo/Dockerfile . -t brain-echo-test 2>&1 | tail -20
```

Se o build falhar com `ERR_PNPM_OUTDATED_LOCKFILE`, significa que o pnpm-lock.yaml não foi
atualizado no plano 04-00. Nesse caso, rodar `pnpm install` localmente primeiro.

Se o build falhar com `Cannot find module`, provavelmente algum node_modules de package
está faltando — adicionar o COPY correspondente.
  </action>
  <verify>
    docker build -f /root/Brain/apps/brain-echo/Dockerfile /root/Brain -t brain-echo-test 2>&1 | tail -5
  </verify>
  <acceptance_criteria>
    - apps/brain-echo/Dockerfile existe
    - Dockerfile contém `FROM oven/bun:1 AS builder`
    - Dockerfile contém `FROM oven/bun:1 AS runner`
    - Dockerfile contém `pnpm install --frozen-lockfile` (não `bun install`)
    - Dockerfile contém `COPY --from=builder /app/packages/database/src/migrations ./migrations`
    - Dockerfile contém `ENV MIGRATIONS_DIR=/app/migrations`
    - Dockerfile contém `CMD ["bun", "apps/brain-echo/dist/index.js"]`
    - `docker build -f apps/brain-echo/Dockerfile . -t brain-echo-test` completa com exit code 0
    - `docker images brain-echo-test` mostra a imagem criada
  </acceptance_criteria>
  <done>Dockerfile multi-stage cria imagem válida; migrations SQL copiadas explicitamente; SC-1 (docker build) verificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker builder → runner stage | Apenas artefatos compilados e migrations SQL são copiados; devDeps e source .ts ficam no builder |
| migration SQL → banco | Apenas DML INSERT com ON CONFLICT — sem DDL ou execução dinâmica |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02-01 | Information Disclosure | Dockerfile (imagem final) | mitigate | `USER bun` no runner stage — processo não roda como root; segredos (DATABASE_URL, API_KEY) NÃO são baked na imagem (passados via --env-file no docker run) |
| T-4-02-02 | Tampering | migration seed SQL | accept | Migration é idempotente (`ON CONFLICT DO NOTHING`); se a row já existir (modificada manualmente), a migration não sobrescreve — risco aceito para v1 |
| T-4-02-03 | Denial of Service | pnpm install no builder stage | accept | Builder stage usa --frozen-lockfile — sem resolução de deps em runtime; risco de supply chain attack aceito (pnpm lock garante hashes) |
</threat_model>

<verification>
- `test -f apps/brain-echo/Dockerfile` retorna 0
- `grep "FROM oven/bun:1 AS builder" apps/brain-echo/Dockerfile` encontra a linha
- `grep "pnpm install --frozen-lockfile" apps/brain-echo/Dockerfile` encontra a linha
- `grep "COPY.*migrations" apps/brain-echo/Dockerfile` encontra cópia explícita
- `grep "MIGRATIONS_DIR" apps/brain-echo/Dockerfile` encontra a ENV var
- `docker build -f apps/brain-echo/Dockerfile . -t brain-echo-test` sai 0
- `test -f packages/database/src/migrations/0002_echo_brain_seed.sql` retorna 0
</verification>

<success_criteria>
- 0002_echo_brain_seed.sql existe com INSERT ON CONFLICT DO NOTHING
- Dockerfile multi-stage cria imagem brain-echo-test sem erros (SC-1 verificado)
- Imagem runner contém migrations SQL em /app/migrations/
- ENV MIGRATIONS_DIR=/app/migrations definida na imagem runner
- USER bun no runner stage (não root)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-validation-brain/04-02-SUMMARY.md` com:
- Arquivos criados
- Saída do docker build (sucesso ou erros encontrados + como foram resolvidos)
- Confirmação de que migrations SQL estão na imagem (docker run --rm brain-echo-test ls /app/migrations/)
- Quaisquer ajustes no Dockerfile em relação ao RESEARCH.md
</output>
