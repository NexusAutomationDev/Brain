---
phase: 14-td-01-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/brain-sdr/src/qualifier.ts
  - apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts
autonomous: true
requirements:
  - TD-01
must_haves:
  truths:
    - "qualifier.ts abre conexão postgres com `prepare: false` — mesma configuração do TenantPoolManager"
    - "Static analysis test PGB-TD01 verifica via regex que qualifier.ts passa `prepare: false`"
    - "Todos os testes existentes de qualifier.unit.test.ts continuam passando após a mudança"
  artifacts:
    - path: "apps/brain-sdr/src/qualifier.ts"
      provides: "Conexão postgres.js com prepare: false em saveQualificationToMemories"
      contains: "postgres(dbUrl, { max: 1, prepare: false })"
    - path: "apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts"
      provides: "Test suite PGB-TD01 com static analysis regex"
      contains: "PGB-TD01"
  key_links:
    - from: "apps/brain-sdr/src/qualifier.ts"
      to: "postgres.js connection"
      via: "prepare: false option"
      pattern: "postgres\\(dbUrl,\\s*\\{[^}]*prepare:\\s*false"
    - from: "qualifier.unit.test.ts"
      to: "qualifier.ts source"
      via: "readFileSync + regex"
      pattern: "PGB-TD01"
---

<objective>
Corrigir TD-01: adicionar `prepare: false` à conexão postgres.js em `qualifier.ts` (função `saveQualificationToMemories`), tornando o sub-agente de qualificação compatível com PgBouncer transaction mode em produção. Adicionar teste de análise estática PGB-TD01 e comment de documentação sobre limitação do PostgresSaver.

Purpose: Eliminar o único blocker de produção remanescente antes das fases MCP e responseMode dinâmico. Sem `prepare: false`, queries com prepared statements falham silenciosamente ou com erro obscuro no PgBouncer transaction mode.

Output: qualifier.ts com `prepare: false`, qualifier.unit.test.ts com describe PGB-TD01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-td-01-fix/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar prepare: false e comment de documentacao em qualifier.ts (D-01, D-03)</name>
  <files>apps/brain-sdr/src/qualifier.ts</files>

  <read_first>
    - apps/brain-sdr/src/qualifier.ts — arquivo alvo; ver linha 28 (a unica linha alterada)
    - packages/database/src/migrate.ts — referencia de padrao: comentario `// D-01: PgBouncer-compatible` na mesma linha que `prepare: false`
    - packages/ai/src/graph/checkpointer.ts — referencia para o comment D-03 sobre limitacao do driver pg no PostgresSaver
  </read_first>

  <behavior>
    - Test 1 (analitico, em Task 2): regex `/postgres\(dbUrl,\s*\{[^}]*prepare:\s*false/` retorna true no source de qualifier.ts
    - Test 2 (analitico, em Task 2): source de qualifier.ts contem o comment "// PGB-TD01:" proximo a saveQualificationToMemories
    - Test 3 (analitico, em Task 2): source de qualifier.ts contem comment sobre PostgresSaver usar driver pg proxima de fromConnString
  </behavior>

  <action>
    Modificar SOMENTE a linha 28 de `apps/brain-sdr/src/qualifier.ts`.

    ANTES (linha 28):
    ```ts
    const sql = postgres(dbUrl, { max: 1 });
    ```

    DEPOIS (linha 28):
    ```ts
    const sql = postgres(dbUrl, { max: 1, prepare: false }); // PGB-TD01: prepare:false — PgBouncer transaction mode nao suporta prepared statements
    ```

    Adicionalmente, adicionar comment de documentacao D-03 imediatamente antes de `PostgresSaver.fromConnString(dbUrl)` (por volta da linha 196):

    ANTES:
    ```ts
    // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
    // Pitfall 4: NÃO chamar saver.setup() aqui
    const saver = PostgresSaver.fromConnString(dbUrl);
    ```

    DEPOIS:
    ```ts
    // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
    // Pitfall 4: NÃO chamar saver.setup() aqui
    // D-03: PostgresSaver usa driver pg (node-postgres v8.21) internamente — nao aceita prepare:false.
    // Limitacao documentada em packages/ai/src/graph/checkpointer.ts. Fora do escopo de TD-01.
    const saver = PostgresSaver.fromConnString(dbUrl);
    ```

    Nenhuma outra linha deve ser alterada. Nao alterar imports, logica, exports, ou qualquer outro aspecto do arquivo.
  </action>

  <verify>
    <automated>cd /root/Brain && grep -n "prepare: false" apps/brain-sdr/src/qualifier.ts</automated>
  </verify>

  <acceptance_criteria>
    - `apps/brain-sdr/src/qualifier.ts` linha com `postgres(dbUrl` contem `prepare: false`
    - Grep retorna: `postgres(dbUrl, { max: 1, prepare: false })`
    - Grep retorna: `PGB-TD01:` no mesmo contexto
    - Grep retorna: `D-03:` proxima a `PostgresSaver.fromConnString`
    - Nenhuma outra linha foi modificada (total de linhas do arquivo permanece igual ou +3 pelo comment D-03)
  </acceptance_criteria>

  <done>qualifier.ts com `prepare: false` em saveQualificationToMemories e comment D-03 proxima a fromConnString</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar describe PGB-TD01 em qualifier.unit.test.ts (D-02)</name>
  <files>apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts</files>

  <read_first>
    - apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts — arquivo alvo; ver estrutura existente e padroes de static analysis
    - packages/database/src/migrate.test.ts — referencia de padrao para PGB-05 (linhas 125-133): `readFileSync` + regex `/postgres\([^)]*prepare:\s*false/`
  </read_first>

  <behavior>
    - Test (PGB-TD01): regex `/postgres\(dbUrl,\s*\{[^}]*prepare:\s*false/` retorna true no source de qualifier.ts
    - Todos os describes existentes (SDR-05, CR-01, analise estatica) continuam passando sem alteracao
  </behavior>

  <action>
    Adicionar o seguinte `describe` ao FINAL do arquivo `apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts`, apos o ultimo describe existente (CR-01):

    ```ts
    describe("PGB-TD01: prepare: false em saveQualificationToMemories", () => {
      const src = readFileSync(resolve(import.meta.dir, "../../qualifier.ts"), "utf-8");
      // Excluir linhas de comentario para evitar falsos positivos
      const codeLines = src.split("\n").filter(l => !l.trim().startsWith("//")).join("\n");

      test("postgres() em saveQualificationToMemories passa prepare: false (PgBouncer transaction mode)", () => {
        // Mesmo padrao de PGB-05 em migrate.test.ts (linhas 126-132)
        const hasPrepare = /postgres\(dbUrl,\s*\{[^}]*prepare:\s*false/.test(codeLines);
        expect(hasPrepare).toBe(true);
      });
    });
    ```

    Nao alterar nenhum describe ou test existente. Apenas appendar o novo describe ao final do arquivo. Os imports `readFileSync` e `resolve` ja estao importados nos describes anteriores — nao re-importar (o arquivo ja usa `readFileSync` e `resolve` via destructuring no topo, verificar se estao presentes; se nao estiverem, adicionar ao import existente do "fs" e "path").

    Verificar o topo do arquivo: se `readFileSync` e `resolve` ja sao importados via `import { readFileSync } from "fs"` e `import { resolve } from "path"`, nao adicionar novamente. O arquivo atual ja os tem (linhas 2-3).
  </action>

  <verify>
    <automated>cd /root/Brain && bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - Output do `bun test` contem "PGB-TD01: prepare: false em saveQualificationToMemories"
    - Output do `bun test` contem "pass" para o test "postgres() em saveQualificationToMemories passa prepare: false"
    - Todos os describes anteriores (SDR-05, analise estatica, CR-01) continuam com status "pass"
    - Zero falhas no output do bun test para este arquivo
  </acceptance_criteria>

  <done>qualifier.unit.test.ts com describe PGB-TD01 passando; todos os testes pre-existentes intactos</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| qualifier.ts -> postgres.js | Conexao de curta duracao ao banco com credenciais de DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `saveQualificationToMemories` — DATABASE_URL em env | accept | DATABASE_URL ja e gerenciada pelo deployment; nao e logada; escopo deste plano nao altera o mecanismo de gestao de credenciais |
| T-14-02 | Tampering | static analysis test em qualifier.unit.test.ts — path relativo com readFileSync | accept | Path relativo a `import.meta.dir` e controlado pelo desenvolvedor; sem input externo; baixo risco em CI |
</threat_model>

<verification>
Checklist de conclusao da fase:

1. `grep "prepare: false" apps/brain-sdr/src/qualifier.ts` retorna a linha com `postgres(dbUrl, { max: 1, prepare: false })`
2. `grep "PGB-TD01" apps/brain-sdr/src/qualifier.ts` retorna o comment inline
3. `grep "D-03" apps/brain-sdr/src/qualifier.ts` retorna o comment de documentacao do PostgresSaver
4. `bun test apps/brain-sdr/src/__tests__/unit/qualifier.unit.test.ts` — todos os testes passam (zero falhas)
5. Output de `bun test` contem "PGB-TD01: prepare: false em saveQualificationToMemories"
</verification>

<success_criteria>
1. qualifier.ts linha 28: `postgres(dbUrl, { max: 1, prepare: false })` — mesma configuracao do TenantPoolManager e migrate.ts
2. Sub-agente de qualificacao compativel com PgBouncer transaction mode (prepared statements desabilitados)
3. Todos os testes de qualifier.unit.test.ts passam — zero regressoes
4. Novo describe PGB-TD01 presente e passando em qualifier.unit.test.ts
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/14-td-01-fix/14-01-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
