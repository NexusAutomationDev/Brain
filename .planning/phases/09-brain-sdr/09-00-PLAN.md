---
phase: 09-brain-sdr
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/brain-sdr/src/__tests__/unit/brain.test.ts
  - apps/brain-sdr/src/__tests__/integration/qualify.test.ts
  - apps/brain-sdr/package.json
autonomous: true
requirements: [SDR-01, SDR-02, SDR-03, SDR-04, SDR-05, INFRA-01]

must_haves:
  truths:
    - "bun test src/__tests__/unit passa (todos os stubs RED → cada describe() tem pelo menos 1 test que falha por ausência do módulo ou por to be implemented)"
    - "bun test src/__tests__/integration passa (stubs pending/skipped — sem erro de sintaxe)"
    - "package.json tem scripts 'test' e 'test:integration'"
  artifacts:
    - path: "apps/brain-sdr/src/__tests__/unit/brain.test.ts"
      provides: "Stubs de teste para SDR-01, SDR-04, SDR-05, INFRA-01"
    - path: "apps/brain-sdr/src/__tests__/integration/qualify.test.ts"
      provides: "Stubs de teste para SDR-05 E2E"
    - path: "apps/brain-sdr/package.json"
      provides: "Configuração do pacote com scripts de teste"
  key_links:
    - from: "apps/brain-sdr/package.json"
      to: "scripts.test"
      via: "bun test src/__tests__/unit"
      pattern: '"test":\s*"bun test src/__tests__/unit"'
---

<objective>
Wave 0: Criar scaffolding de testes e package.json do Brain SDR antes que qualquer código de produção seja escrito.

Purpose: Garantia Nyquist — os testes existem antes da implementação, forçando RED antes de GREEN. Todos os planos subsequentes (Wave 1, 2, 3) dependem desses arquivos para validação automática.

Output: package.json com scripts de teste, unit/brain.test.ts com stubs RED para todos os contratos, integration/qualify.test.ts com stubs skipped aguardando banco real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-brain-sdr/09-CONTEXT.md
@.planning/phases/09-brain-sdr/09-RESEARCH.md

<interfaces>
<!-- Contratos que os testes devem verificar -->
<!-- IBrain interface — packages/core/src/brain/interface.ts -->
```typescript
export interface IBrain {
  id: string;
  brainType: string;
  promptKeys: string[];
  tools: StructuredTool[];
  buildGraph(ctx: BrainBuildContext): StateGraph<typeof BrainStateAnnotation>;
}

export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
}
```

<!-- Referência: apps/brain-echo/src/__tests__/unit/brain.test.ts -->
<!-- Padrão de mock mínimo de BrainBuildContext: -->
```typescript
const ctx = {
  llm: { invoke: mock(async () => ({ content: "ok" })), bindTools: mock(() => ({ invoke: mock(async () => ({ content: "ok", tool_calls: [] })) })) },
  prompts: { system: "prompt sistema", qualification: "prompt qualificacao" },
  tools: [],
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar package.json do brain-sdr</name>
  <files>apps/brain-sdr/package.json</files>
  <read_first>
    - apps/brain-echo/package.json (template exato a ser adaptado — copiar dependências e scripts)
  </read_first>
  <action>
Criar `apps/brain-sdr/package.json` baseado no brain-echo. Diferenças em relação ao brain-echo:
- `"name": "@brain-app/sdr"` (não "@brain-app/echo")
- Adicionar `"@langchain/core": "^1.1.48"` nas dependencies (brain-sdr usa tool() e AIMessage diretamente)
- Adicionar `"zod": "^3.25.76"` nas dependencies (schema qualify_lead)
- Manter todos os outros campos idênticos ao brain-echo:
  - `"version": "0.0.0"`, `"private": true`, `"type": "module"`
  - scripts: `"build": "tsc"`, `"test": "bun test src/__tests__/unit"`, `"test:integration": "bun test src/__tests__/integration"`, `"typecheck": "tsc --noEmit"`, `"start": "bun dist/index.js"`
  - dependencies: `@brain-pkg/ai`, `@brain-pkg/core`, `@brain-pkg/database`, `@brain-pkg/observability`, `@brain-pkg/transport`, `@langchain/langgraph` `^1.4.1`, `hono` `^4.12.0`, `postgres` `^3.4.9`, `drizzle-orm` `0.45.2`
  - devDependencies: `@types/bun: latest`, `typescript: ^5.0.0`
  </action>
  <verify>
    <automated>cd /root/Brain/apps/brain-sdr && cat package.json | grep -E '"name"|"test"|"test:integration"'</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém `"name": "@brain-app/sdr"`
    - package.json contém `"test": "bun test src/__tests__/unit"`
    - package.json contém `"test:integration": "bun test src/__tests__/integration"`
    - package.json contém `"@langchain/core"` nas dependencies
    - package.json contém `"zod"` nas dependencies
  </acceptance_criteria>
  <done>package.json criado e validado com grep</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar stubs RED de testes unitários</name>
  <files>apps/brain-sdr/src/__tests__/unit/brain.test.ts</files>
  <read_first>
    - apps/brain-echo/src/__tests__/unit/brain.test.ts (padrão de teste IBrain para replicar)
    - .planning/phases/09-brain-sdr/09-RESEARCH.md (seção Validation Architecture → Phase Requirements → Test Map)
    - packages/core/src/brain/interface.ts (IBrain contract)
  </read_first>
  <behavior>
    - Test: sdrBrain.id === "brain-sdr" → import("../../brain.js") → expect id
    - Test: sdrBrain.brainType === "sdr" → import("../../brain.js") → expect brainType
    - Test: sdrBrain.promptKeys contém ["system", "qualification"] → toEqual(["system", "qualification"])
    - Test: sdrBrain.tools contém exatamente 1 tool de nome "qualify_lead" → tools.length === 1, tools[0].name === "qualify_lead"
    - Test: buildGraph(ctx) retorna objeto com addNode, compile (StateGraph) → expect(graph.addNode).toBeFunction()
    - Test: qualify_lead schema tem campos description e session_id → tool.schema tem description e session_id
    - Test: qualify_lead.name === "qualify_lead" → import("../../qualifier.js") → qualifyLeadTool.name
    - Test: context window no nó llm — variante simples: contextWindowSize parse "abc" → fallback 40
  </behavior>
  <action>
Criar `apps/brain-sdr/src/__tests__/unit/brain.test.ts` com stubs RED. O arquivo NÃO deve importar módulos que ainda não existem via import estático (import() dinâmico dentro dos tests para evitar falha de parse do arquivo). Seguir exatamente o padrão de brain-echo/brain.test.ts.

Estrutura exata do arquivo:

```typescript
import { describe, test, expect, mock } from "bun:test";

describe("BrainSDR — IBrain contract (SDR-01, SDR-04)", () => {
  test("sdrBrain.id é 'brain-sdr'", async () => {
    const mod = await import("../../brain.js");
    expect(mod.sdrBrain.id).toBe("brain-sdr");
  });

  test("sdrBrain.brainType é 'sdr'", async () => {
    const mod = await import("../../brain.js");
    expect(mod.sdrBrain.brainType).toBe("sdr");
  });

  test("sdrBrain.promptKeys é ['system', 'qualification']", async () => {
    const mod = await import("../../brain.js");
    expect(mod.sdrBrain.promptKeys).toEqual(["system", "qualification"]);
  });

  test("sdrBrain.tools tem exatamente 1 tool: qualify_lead", async () => {
    const mod = await import("../../brain.js");
    expect(mod.sdrBrain.tools).toHaveLength(1);
    expect(mod.sdrBrain.tools[0].name).toBe("qualify_lead");
  });

  test("buildGraph(ctx) retorna StateGraph (tem addNode e compile)", async () => {
    const mod = await import("../../brain.js");
    const ctx = {
      llm: {
        bindTools: mock(() => ({
          invoke: mock(async () => ({ content: "resposta", tool_calls: [] })),
        })),
      },
      prompts: { system: "prompt sistema", qualification: "prompt qualificacao" },
      tools: [],
    };
    const graph = mod.sdrBrain.buildGraph(ctx as any);
    expect(graph).toBeTruthy();
    expect(typeof graph.addNode).toBe("function");
    expect(typeof graph.compile).toBe("function");
  });
});

describe("qualify_lead tool — contrato SDR-05", () => {
  test("qualifyLeadTool.name é 'qualify_lead'", async () => {
    const mod = await import("../../qualifier.js");
    expect(mod.qualifyLeadTool.name).toBe("qualify_lead");
  });

  test("qualifyLeadTool.schema tem campo description", async () => {
    const mod = await import("../../qualifier.js");
    // DynamicStructuredTool tem schema.shape para ZodObject
    const shape = (mod.qualifyLeadTool.schema as any).shape;
    expect(shape).toHaveProperty("description");
  });

  test("qualifyLeadTool.schema tem campo session_id", async () => {
    const mod = await import("../../qualifier.js");
    const shape = (mod.qualifyLeadTool.schema as any).shape;
    expect(shape).toHaveProperty("session_id");
  });
});

describe("contextWindowSize — parse seguro (SDR-01, HIST-03)", () => {
  test("fallback 40 quando ENV é inválida", () => {
    const validate = (envVal: string | undefined): number => {
      const n = parseInt(envVal ?? "40", 10);
      return n > 0 && isFinite(n) ? n : 40;
    };
    expect(validate(undefined)).toBe(40);
    expect(validate("abc")).toBe(40);
    expect(validate("-5")).toBe(40);
    expect(validate("0")).toBe(40);
    expect(validate("20")).toBe(20);
  });
});
```

IMPORTANTE: Os imports dinâmicos (`../../brain.js`, `../../qualifier.js`) vão falhar com MODULE_NOT_FOUND até que o código de produção seja criado no Plan 02. Isso é o comportamento RED esperado.
  </action>
  <verify>
    <automated>cd /root/Brain/apps/brain-sdr && cat src/__tests__/unit/brain.test.ts | grep -c "test("</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em apps/brain-sdr/src/__tests__/unit/brain.test.ts
    - Arquivo contém pelo menos 8 calls a test()
    - Arquivo contém describe("BrainSDR — IBrain contract")
    - Arquivo contém describe("qualify_lead tool — contrato SDR-05")
    - Arquivo contém describe("contextWindowSize — parse seguro")
    - Arquivo NÃO usa import estático de "../../brain.js" (usa import() dinâmico dentro dos tests)
    - Arquivo é válido TypeScript (sem erros de sintaxe)
  </acceptance_criteria>
  <done>Stubs unitários criados — RED esperado até Plan 02</done>
</task>

<task type="auto">
  <name>Task 3: Criar stubs de testes de integração</name>
  <files>apps/brain-sdr/src/__tests__/integration/qualify.test.ts</files>
  <read_first>
    - .planning/phases/09-brain-sdr/09-VALIDATION.md (Per-Task Verification Map — task 9-02-03)
    - .planning/phases/09-brain-sdr/09-RESEARCH.md (Pattern 3: PostgresSaver.getTuple())
  </read_first>
  <action>
Criar `apps/brain-sdr/src/__tests__/integration/qualify.test.ts` com stubs skipped. O arquivo deve ser válido e executável com bun test, mas os testes devem usar `test.skip()` para não falhar sem banco real.

Estrutura exata:

```typescript
import { describe, test, expect } from "bun:test";

// SDR-05: Integration — requer PostgreSQL real com dados de checkpoint
// Para executar: DATABASE_URL e DATABASE_NAME devem estar definidos no ambiente
// Os testes são skipped por padrão — rodar explicitamente com DB real

describe("qualify_lead — integração E2E (SDR-05)", () => {
  test.skip("runQualificationAgent retorna {qualificado, motivo, proximo_passo} dado sessionId com checkpoint", async () => {
    // Pré-condição: banco deve ter checkpoint do PostgresSaver para o sessionId
    // Este teste é executado manualmente com DB real após Plan 02 implementar qualifier.ts
    const { runQualificationAgent } = await import("../../qualifier.js");
    const result = await runQualificationAgent(
      "Lead demonstrou interesse em saber mais sobre o produto",
      "test-session-id-with-real-checkpoint"
    );
    expect(result).toHaveProperty("qualificado");
    expect(typeof result.qualificado).toBe("boolean");
    expect(result).toHaveProperty("motivo");
    expect(typeof result.motivo).toBe("string");
    expect(result).toHaveProperty("proximo_passo");
    expect(typeof result.proximo_passo).toBe("string");
  });

  test.skip("runQualificationAgent retorna fallback quando sessionId não tem checkpoint", async () => {
    // Comportamento esperado: retorno gracioso em vez de throw
    const { runQualificationAgent } = await import("../../qualifier.js");
    const result = await runQualificationAgent(
      "Contexto qualquer",
      "session-que-nao-existe-no-banco"
    );
    // Sem checkpoint: retorna objeto válido com qualificado=false
    expect(result.qualificado).toBe(false);
    expect(typeof result.motivo).toBe("string");
    expect(result.motivo.length).toBeGreaterThan(0);
  });
});

describe("Brain SDR — integração completa (SDR-01, SDR-02, SDR-03)", () => {
  test.skip("BrainRunner com BrainSDR processa mensagem de lead ativo e retorna reply", async () => {
    // Requer: DATABASE_URL, MIGRATIONS_FOLDER, LLM_PROVIDER API key configurados
    // Verifica: ia_ativada gate funciona + histórico persiste (herdado do BrainRunner)
    expect(true).toBe(true); // placeholder — implementar após Plan 03
  });
});
```
  </action>
  <verify>
    <automated>cd /root/Brain/apps/brain-sdr && cat src/__tests__/integration/qualify.test.ts | grep "test.skip"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em apps/brain-sdr/src/__tests__/integration/qualify.test.ts
    - Arquivo contém pelo menos 3 calls a test.skip()
    - Arquivo contém describe("qualify_lead — integração E2E (SDR-05)")
    - Arquivo contém describe("Brain SDR — integração completa")
    - `bun test src/__tests__/integration` em apps/brain-sdr/ executa sem erro de sintaxe (skips não contam como falha)
  </acceptance_criteria>
  <done>Stubs de integração criados com test.skip — executáveis sem banco</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → production modules | Testes Wave 0 importam módulos que ainda não existem — RED esperado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Tampering | test stubs | accept | Arquivos de teste não são código de produção; sem risco de segurança direto |
| T-09-00-02 | Information Disclosure | integration/qualify.test.ts | accept | Sem credenciais reais hardcoded nos stubs; DATABASE_URL via ENV apenas |
</threat_model>

<verification>
1. `ls apps/brain-sdr/src/__tests__/unit/brain.test.ts apps/brain-sdr/src/__tests__/integration/qualify.test.ts apps/brain-sdr/package.json` — todos os 3 arquivos existem
2. `grep '"test"' apps/brain-sdr/package.json` → `"test": "bun test src/__tests__/unit"`
3. `grep '"test:integration"' apps/brain-sdr/package.json` → `"test:integration": "bun test src/__tests__/integration"`
4. `grep -c "test(" apps/brain-sdr/src/__tests__/unit/brain.test.ts` → 8 ou mais
5. `grep "test.skip" apps/brain-sdr/src/__tests__/integration/qualify.test.ts` → pelo menos 3 linhas
</verification>

<success_criteria>
- apps/brain-sdr/package.json existe com name "@brain-app/sdr" e scripts test e test:integration
- apps/brain-sdr/src/__tests__/unit/brain.test.ts existe com stubs para todos os contratos SDR
- apps/brain-sdr/src/__tests__/integration/qualify.test.ts existe com stubs skipped
- Nenhum arquivo de teste tem import estático de módulos inexistentes
- Wave 0 completa: VALIDATION.md pode marcar nyquist_compliant checklist
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-brain-sdr/09-00-SUMMARY.md`
</output>
