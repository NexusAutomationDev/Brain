---
phase: 09-brain-sdr
plan: "01"
type: execute
wave: 1
depends_on: ["09-00"]
files_modified:
  - packages/database/src/migrations/0005_brain_sdr_prompts.sql
autonomous: true
requirements: [SDR-04]

must_haves:
  truths:
    - "Migration 0005 existe em packages/database/src/migrations/"
    - "Migration insere prompts com brain_type = 'sdr' (não 'brain-sdr') para as chaves 'system' e 'qualification'"
    - "ON CONFLICT (brain_type, key) DO NOTHING garante idempotência — migration é segura para rodar múltiplas vezes"
  artifacts:
    - path: "packages/database/src/migrations/0005_brain_sdr_prompts.sql"
      provides: "Seed SQL dos prompts do Brain SDR"
      contains: "ON CONFLICT (brain_type, key) DO NOTHING"
  key_links:
    - from: "packages/database/src/migrations/0005_brain_sdr_prompts.sql"
      to: "prompts table"
      via: "runMigrations() no BrainRunner.init()"
      pattern: "INSERT INTO prompts.*sdr"
---

<objective>
Wave 1: Criar migration SQL com seed dos prompts do Brain SDR.

Purpose: SDR-04 exige zero prompts hardcoded. Os prompts system e qualification devem existir no banco antes do BrainRunner.init() tentar carregá-los — caso contrário, process.exit(1) é acionado (fail-fast). A migration é o mecanismo de bootstrap.

Output: `0005_brain_sdr_prompts.sql` com dois INSERTs idempotentes para brain_type='sdr'.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-brain-sdr/09-CONTEXT.md
@.planning/phases/09-brain-sdr/09-RESEARCH.md

<interfaces>
<!-- Tabela prompts — schema existente (0001_lazy_deathstrike.sql) -->
```sql
-- prompts(brain_type TEXT, key TEXT, content TEXT)
-- UNIQUE INDEX em (brain_type, key) — criado em 0001
-- Padrão de seed — de 0002_echo_brain_seed.sql:
INSERT INTO prompts (brain_type, key, content)
VALUES ('echo', 'system', '...')
ON CONFLICT (brain_type, key) DO NOTHING;
```

<!-- CRÍTICO: brain_type deve ser 'sdr' — igual a sdrBrain.brainType no IBrain.
     Se usar 'brain-sdr', BrainRunner.init() lança process.exit(1) por missing prompt key. -->

<!-- Pitfall 6 do RESEARCH.md:
     "Garantir que a migration usa brain_type = 'sdr' (não 'brain-sdr') —
     deve bater com brainType: 'sdr' do IBrain." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration 0005_brain_sdr_prompts.sql com seed SDR</name>
  <files>packages/database/src/migrations/0005_brain_sdr_prompts.sql</files>
  <read_first>
    - packages/database/src/migrations/0002_echo_brain_seed.sql (padrão exato de seed para replicar)
    - packages/database/src/migrations/0004_even_rick_jones.sql (confirmar que 0004 é a última migration — 0005 é a próxima)
  </read_first>
  <action>
Criar `packages/database/src/migrations/0005_brain_sdr_prompts.sql` com dois INSERTs idempotentes.

Conteúdo exato do arquivo:

```sql
-- Seed: prompts do Brain SDR
-- D-09: Prompts inseridos via migration SQL — cliente atualiza no banco sem deploy
-- ON CONFLICT DO NOTHING garante idempotência (seguro rodar múltiplas vezes)
-- CRÍTICO: brain_type = 'sdr' deve ser idêntico a sdrBrain.brainType em brain.ts
-- Se divergir, BrainRunner.init() lança process.exit(1) por missing prompt key

INSERT INTO prompts (brain_type, key, content)
VALUES (
  'sdr',
  'system',
  'Você é um assistente de atendimento comercial especializado em qualificação de leads B2B. Seu objetivo é conduzir conversas de atendimento inicial no WhatsApp de forma profissional e empática, descobrindo as necessidades do lead e apresentando o valor do produto/serviço de forma natural. Guie a conversa com perguntas abertas para entender o contexto do lead (empresa, cargo, dor principal, urgência). Quando perceber que o lead demonstrou interesse genuíno e forneceu informações suficientes sobre seu contexto, utilize a ferramenta qualify_lead para avaliar o nível de qualificação. Mantenha respostas curtas e diretas para o contexto de WhatsApp — máximo 3-4 frases por mensagem. Nunca revele que é uma IA, a menos que o lead pergunte diretamente.'
)
ON CONFLICT (brain_type, key) DO NOTHING;

INSERT INTO prompts (brain_type, key, content)
VALUES (
  'sdr',
  'qualification',
  'Você é um especialista em qualificação comercial B2B. Analise o histórico de conversa fornecido e determine se o lead está qualificado para avançar no processo de vendas. Um lead qualificado demonstra: (1) necessidade clara e articulada do produto/serviço, (2) autoridade de decisão ou influência na compra, (3) urgência ou prazo definido, (4) budget compatível (direto ou implícito). Avalie as mensagens do lead vs. as mensagens da IA para entender o engajamento real. Retorne EXCLUSIVAMENTE um JSON no formato: {"qualificado": true/false, "motivo": "explicação objetiva em 1-2 frases de por que qualificou ou não", "proximo_passo": "ação específica que o Brain principal deve tomar — ex: agendar demo, solicitar mais informações sobre budget, transferir para humano"}. Não inclua texto fora do JSON.'
)
ON CONFLICT (brain_type, key) DO NOTHING;
```

CRÍTICO: Não usar 'brain-sdr' — usar 'sdr' (corresponde a sdrBrain.brainType = "sdr" em brain.ts).
  </action>
  <verify>
    <automated>grep -c "ON CONFLICT (brain_type, key) DO NOTHING" /root/Brain/packages/database/src/migrations/0005_brain_sdr_prompts.sql</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em packages/database/src/migrations/0005_brain_sdr_prompts.sql
    - Arquivo contém exatamente 2 ocorrências de `ON CONFLICT (brain_type, key) DO NOTHING`
    - Arquivo contém `'sdr'` como brain_type (não 'brain-sdr', não 'SDR')
    - Arquivo contém INSERT com key = 'system'
    - Arquivo contém INSERT com key = 'qualification'
    - Arquivo NÃO modifica a tabela prompts de forma destrutiva (sem DROP, UPDATE, DELETE)
    - Arquivo NÃO usa brain_type = 'brain-sdr' (grep -c "brain-sdr" → 0)
  </acceptance_criteria>
  <done>Migration 0005 criada com seed idempotente para brain_type='sdr', chaves 'system' e 'qualification'</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → banco PostgreSQL | Conteúdo dos prompts é inserido no banco via runMigrations() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | prompts table seed | mitigate | ON CONFLICT DO NOTHING previne sobrescrever prompts customizados que cliente tenha atualizado; migration é append-only |
| T-09-01-02 | Information Disclosure | prompt content | accept | Prompts são instruções do sistema, não secrets; não contêm credenciais ou dados de usuário |
| T-09-01-03 | Denial of Service | migration idempotência | mitigate | ON CONFLICT DO NOTHING garante que múltiplas instâncias rodando init() simultâneo não causam erro de constraint violation |
</threat_model>

<verification>
1. `ls packages/database/src/migrations/0005_brain_sdr_prompts.sql` → arquivo existe
2. `grep "brain_type" packages/database/src/migrations/0005_brain_sdr_prompts.sql | grep "'sdr'"` → 2 linhas com 'sdr'
3. `grep -c "brain-sdr" packages/database/src/migrations/0005_brain_sdr_prompts.sql` → 0
4. `grep -c "ON CONFLICT (brain_type, key) DO NOTHING" packages/database/src/migrations/0005_brain_sdr_prompts.sql` → 2
5. `grep "'system'" packages/database/src/migrations/0005_brain_sdr_prompts.sql` → linha presente
6. `grep "'qualification'" packages/database/src/migrations/0005_brain_sdr_prompts.sql` → linha presente
</verification>

<success_criteria>
- Migration 0005_brain_sdr_prompts.sql existe e contém 2 INSERTs idempotentes
- brain_type = 'sdr' em ambos os INSERTs (alinhado com sdrBrain.brainType)
- Migration é append-only — segura para rodar em múltiplas instâncias simultaneamente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-brain-sdr/09-01-SUMMARY.md`
</output>
