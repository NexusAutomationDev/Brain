---
phase: 09-brain-sdr
plan: "02"
type: execute
wave: 2
depends_on: ["09-00"]
files_modified:
  - apps/brain-sdr/src/brain.ts
  - apps/brain-sdr/src/qualifier.ts
autonomous: true
requirements: [SDR-01, SDR-04, SDR-05]

must_haves:
  truths:
    - "sdrBrain implementa IBrain com id='brain-sdr', brainType='sdr', promptKeys=['system','qualification']"
    - "buildGraph() retorna StateGraph com ToolNode + toolsCondition (ReAct 2-node — D-02)"
    - "sdrBrain.tools contém exatamente [qualifyLeadTool] (D-03)"
    - "qualifyLeadTool.name === 'qualify_lead' com schema zod {description: string, session_id: string}"
    - "runQualificationAgent() busca checkpoint via PostgresSaver.getTuple() sem chamar setup()"
    - "runQualificationAgent() retorna {qualificado: boolean, motivo: string, proximo_passo: string}"
    - "Fallback gracioso se getTuple() retorna undefined ou parse JSON falha"
    - "bun test src/__tests__/unit passa GREEN após este plano"
  artifacts:
    - path: "apps/brain-sdr/src/brain.ts"
      provides: "BrainSDR: IBrain com ReAct + qualify_lead tool"
      exports: ["sdrBrain"]
    - path: "apps/brain-sdr/src/qualifier.ts"
      provides: "qualifyLeadTool + runQualificationAgent() + sub-agente StateGraph stateless"
      exports: ["qualifyLeadTool", "runQualificationAgent"]
  key_links:
    - from: "apps/brain-sdr/src/brain.ts"
      to: "apps/brain-sdr/src/qualifier.ts"
      via: "import { qualifyLeadTool } from './qualifier.js'"
      pattern: "qualifyLeadTool"
    - from: "apps/brain-sdr/src/qualifier.ts"
      to: "PostgresSaver.getTuple()"
      via: "PostgresSaver.fromConnString(DATABASE_URL)"
      pattern: "getTuple"
    - from: "apps/brain-sdr/src/brain.ts"
      to: "ToolNode + toolsCondition"
      via: "@langchain/langgraph/prebuilt"
      pattern: "toolsCondition"
---

<objective>
Wave 2: Implementar o Brain SDR (brain.ts) e o sub-agente de qualificação (qualifier.ts).

Purpose: Núcleo funcional do Brain SDR. O brain.ts define o grafo ReAct com ToolNode que aciona o sub-agente quando o LLM decide qualificar o lead. O qualifier.ts implementa o sub-agente stateless que lê o histórico via PostgresSaver e retorna a qualificação.

Output: brain.ts com IBrain completo + qualifier.ts com tool + sub-agente. Testes unitários Wave 0 devem ficar GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-brain-sdr/09-CONTEXT.md
@.planning/phases/09-brain-sdr/09-RESEARCH.md
@.planning/phases/09-brain-sdr/09-00-SUMMARY.md

<interfaces>
<!-- IBrain interface — packages/core/src/brain/interface.ts -->
```typescript
export interface IBrain {
  id: string;
  brainType: string;
  promptKeys: string[];
  tools: StructuredTool[];
  buildGraph(ctx: BrainBuildContext): StateGraph<typeof BrainStateAnnotation>;
}
export interface BrainBuildContext {
  llm: BaseChatModel;
  prompts: Record<string, string>;
  tools: StructuredTool[];
}
```

<!-- BrainStateAnnotation — packages/ai/src/graph/state.ts -->
<!-- Importar de "@brain-pkg/ai" -->

<!-- ToolNode, toolsCondition — @langchain/langgraph/prebuilt -->
```typescript
import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
// ATENÇÃO: importar de "/prebuilt" — não do barrel principal @langchain/langgraph
// (Pitfall 7 do RESEARCH.md)
```

<!-- tool() helper — @langchain/core/tools -->
```typescript
import { tool } from "@langchain/core/tools";
import { z } from "zod";
// tool() com ZodObjectV3 retorna DynamicStructuredTool extends StructuredTool
```

<!-- PostgresSaver.getTuple() — @langchain/langgraph-checkpoint-postgres -->
```typescript
import { PostgresSaver } from "@langchain/langgraph-checkpoint-postgres";
// PostgresSaver.fromConnString(connString) — NÃO chamar .setup() (Pitfall 4)
// tuple = await saver.getTuple({ configurable: { thread_id: sessionId } })
// tuple?.checkpoint?.channel_values?.messages as BaseMessage[] ?? []
```

<!-- Anti-patterns CRÍTICOS (RESEARCH.md):
  1. NUNCA chamar .compile() dentro de buildGraph() — BrainRunner é responsável
  2. NUNCA usar ctx.llm.invoke() diretamente — usar ctx.llm.bindTools(ctx.tools).invoke()
  3. NUNCA usar instanceof AIMessage — usar m._getType() === "ai"
  4. NUNCA chamar checkpointer.setup() no sub-agente — tabelas já existem
  5. qualifyLeadTool retorna string JSON — ToolNode repassa como ToolMessage
-->

<!-- brain-echo como template — apps/brain-echo/src/brain.ts -->
```typescript
// Padrão de context window do nó llm (HIST-03):
const contextWindowSize = (() => {
  const n = parseInt(process.env.CONTEXT_WINDOW_MESSAGES ?? "40", 10);
  return n > 0 && isFinite(n) ? n : 40;  // SECURITY: T-08-ENV
})();
const messagesForLLM = state.messages.slice(-contextWindowSize);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar brain.ts — BrainSDR com grafo ReAct</name>
  <files>apps/brain-sdr/src/brain.ts</files>
  <read_first>
    - apps/brain-echo/src/brain.ts (template a ser adaptado — ler inteiro)
    - packages/core/src/brain/interface.ts (IBrain contract)
    - packages/core/src/runner/runner.ts (padrões de context window, comentários anti-pattern)
    - apps/brain-sdr/src/__tests__/unit/brain.test.ts (testes que devem ficar GREEN)
    - .planning/phases/09-brain-sdr/09-RESEARCH.md (Pattern 1: ReAct Graph, Anti-patterns, Pitfalls)
  </read_first>
  <behavior>
    - sdrBrain.id === "brain-sdr"
    - sdrBrain.brainType === "sdr"
    - sdrBrain.promptKeys deep equals ["system", "qualification"]
    - sdrBrain.tools.length === 1 && sdrBrain.tools[0].name === "qualify_lead"
    - buildGraph(ctx) retorna objeto com typeof graph.addNode === "function"
    - buildGraph(ctx) retorna objeto com typeof graph.compile === "function"
    - Objeto retornado NÃO é compilado (não tem invoke/stream/batch diretamente)
  </behavior>
  <action>
Criar `apps/brain-sdr/src/brain.ts` implementando IBrain com grafo ReAct 2-nós.

Implementação completa:

```typescript
// D-01: LLM decide quando acionar qualify_lead via tool call
// D-02: Grafo ReAct com 2 nós: llm → (toolsCondition?) → tools → llm → __end__
// D-03: Única tool em v1.1: qualify_lead
// Anti-pattern: NUNCA chamar .compile() aqui — BrainRunner é responsável (runner.ts)

import { StateGraph } from "@langchain/langgraph";
import { ToolNode, toolsCondition } from "@langchain/langgraph/prebuilt";
import { BrainStateAnnotation } from "@brain-pkg/ai";
import type { IBrain, BrainBuildContext } from "@brain-pkg/core";
import { qualifyLeadTool } from "./qualifier.js";

export const sdrBrain: IBrain = {
  id: "brain-sdr",
  brainType: "sdr",
  // D-08: promptKeys obrigatórios — BrainRunner.init() faz process.exit(1) se algum faltar no banco
  promptKeys: ["system", "qualification"],
  // D-03: Apenas qualify_lead em v1.1 — ToolsRegistry filtrará via enableTool("sdr", "qualify_lead")
  tools: [qualifyLeadTool],
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  buildGraph(ctx: BrainBuildContext): any {
    // CRITICAL: bindTools() AQUI — sem isso, LLM não inclui tool schemas no request
    // e nunca gera tool_calls. ctx.llm sem bindTools = EchoBrain silencioso. (Pitfall 1)
    const llmWithTools = ctx.llm.bindTools(ctx.tools);

    // HIST-03: context window — slice feito no nó, não no invoke() (Pitfall 3 do runner.ts)
    const getContextWindow = (): number => {
      const n = parseInt(process.env.CONTEXT_WINDOW_MESSAGES ?? "40", 10);
      return n > 0 && isFinite(n) ? n : 40; // SECURITY: T-08-ENV
    };

    return new StateGraph(BrainStateAnnotation)
      .addNode("llm", async (state) => {
        // D-04: system prompt via ctx.prompts["system"] — zero hardcode (SDR-04)
        const messagesForLLM = state.messages.slice(-getContextWindow());
        const response = await llmWithTools.invoke([
          { role: "system", content: ctx.prompts["system"] },
          ...messagesForLLM,
        ]);
        return { messages: [...state.messages, response] };
      })
      // D-02: ToolNode executa as tools em paralelo, trata erros, retorna ToolMessages
      .addNode("tools", new ToolNode(ctx.tools))
      .addEdge("__start__", "llm")
      // D-02: toolsCondition verifica tool_calls no último AIMessage — roteia para tools ou __end__
      .addConditionalEdges("llm", toolsCondition, ["tools", "__end__"])
      .addEdge("tools", "llm");
    // NUNCA .compile() aqui — BrainRunner._compileGraph() é responsável
  },
};
```

IMPORTANTE: `qualifyLeadTool` é importado de `./qualifier.js` — este módulo será criado na Task 2 deste mesmo plano.
  </action>
  <verify>
    <automated>cd /root/Brain/apps/brain-sdr && bun test src/__tests__/unit/brain.test.ts 2>&1 | grep -E "pass|fail|✓|✗|brain-sdr|brainType"</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-sdr/src/brain.ts existe
    - Arquivo contém `brainType: "sdr"` (não "brain-sdr")
    - Arquivo contém `promptKeys: ["system", "qualification"]`
    - Arquivo contém `tools: [qualifyLeadTool]`
    - Arquivo contém `toolsCondition` importado de `@langchain/langgraph/prebuilt`
    - Arquivo contém `ToolNode` importado de `@langchain/langgraph/prebuilt`
    - Arquivo contém `ctx.llm.bindTools(ctx.tools)` (não `ctx.llm.invoke()` direto no nó)
    - Arquivo NÃO contém `.compile(` (anti-pattern — BrainRunner é responsável)
    - `grep -c "\.compile(" apps/brain-sdr/src/brain.ts` → 0
    - Após Task 2 (qualifier.ts criado): `bun test src/__tests__/unit` em apps/brain-sdr/ passa GREEN
  </acceptance_criteria>
  <done>brain.ts criado com IBrain completo, ReAct 2-node, bindTools() correto, sem .compile()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar qualifier.ts — tool qualify_lead + sub-agente stateless</name>
  <files>apps/brain-sdr/src/qualifier.ts</files>
  <read_first>
    - .planning/phases/09-brain-sdr/09-RESEARCH.md (Pattern 2: tool(), Pattern 3: getTuple(), Pattern 4: sub-agente StateGraph, Common Pitfalls 1-7)
    - apps/brain-sdr/src/__tests__/unit/brain.test.ts (testes de qualify_lead que devem ficar GREEN)
    - packages/ai/src/graph/checkpointer.ts (padrão createCheckpointer — NÃO copiar o setup(), apenas fromConnString)
    - packages/core/src/runner/runner.ts (linha ~223: _getType() === "ai" — anti-instanceof)
  </read_first>
  <behavior>
    - qualifyLeadTool.name === "qualify_lead"
    - qualifyLeadTool.schema.shape tem campo "description" (ZodString)
    - qualifyLeadTool.schema.shape tem campo "session_id" (ZodString)
    - runQualificationAgent("desc", "session-sem-checkpoint") retorna {qualificado: false, motivo: string não vazio, proximo_passo: string não vazio} em vez de throw
    - runQualificationAgent chama PostgresSaver.fromConnString(DATABASE_URL) sem setup()
    - Mensagens são separadas por _getType() === "ai" vs "human" (nunca instanceof AIMessage)
    - Sub-agente compilado com .compile() SEM checkpointer (stateless por design D-04)
  </behavior>
  <action>
Criar `apps/brain-sdr/src/qualifier.ts` com a tool qualify_lead e o sub-agente de qualificação.

Implementação completa:

```typescript
// D-04: Sub-agente stateless — compilado sem checkpointer (apenas analisa, não persiste)
// D-05: Usa createLLM() padrão — sem LLM separado
// D-06: Histórico buscado via PostgresSaver.getTuple() pelo sub-agente diretamente
// D-07: qualify_lead(description, session_id) → {qualificado, motivo, proximo_passo}
// Anti-pattern: NUNCA chamar checkpointer.setup() aqui — tabelas já existem (Pitfall 4)
// Anti-pattern: NUNCA usar instanceof AIMessage — usar _getType() (runner.ts linha ~223)

import { tool } from "@langchain/core/tools";
import { z } from "zod";
import { StateGraph, Annotation } from "@langchain/langgraph";
import { PostgresSaver } from "@langchain/langgraph-checkpoint-postgres";
import { createLLM } from "@brain-pkg/ai";
import { createLogger } from "@brain-pkg/observability";
import type { BaseMessage } from "@langchain/core/messages";

const logger = createLogger();

// ─── QualificationAnnotation ─────────────────────────────────────────────────
// Sub-agente tem StateAnnotation próprio — não usa BrainStateAnnotation
// que é específico para o grafo principal (messages, userId, sessionId)

const QualificationAnnotation = Annotation.Root({
  description: Annotation<string>({
    default: () => "",
    reducer: (_, next) => next,
  }),
  aiMessages: Annotation<BaseMessage[]>({
    default: () => [],
    reducer: (_, next) => next,
  }),
  humanMessages: Annotation<BaseMessage[]>({
    default: () => [],
    reducer: (_, next) => next,
  }),
  qualificationPrompt: Annotation<string>({
    default: () => "",
    reducer: (_, next) => next,
  }),
  qualificado: Annotation<boolean | null>({
    default: () => null,
    reducer: (_, next) => next,
  }),
  motivo: Annotation<string>({
    default: () => "",
    reducer: (_, next) => next,
  }),
  proximo_passo: Annotation<string>({
    default: () => "",
    reducer: (_, next) => next,
  }),
});

// ─── Helpers ─────────────────────────────────────────────────────────────────

/** Extrai primeiro bloco JSON válido do conteúdo do LLM (Pitfall 5 do RESEARCH.md) */
function extractJSON(text: string): string {
  // Remove code fences se presentes: ```json...```
  const codeFenceMatch = text.match(/```(?:json)?\s*([\s\S]*?)\s*```/);
  if (codeFenceMatch) return codeFenceMatch[1];
  // Extrai primeiro objeto JSON encontrado
  const jsonMatch = text.match(/\{[\s\S]*\}/);
  if (jsonMatch) return jsonMatch[0];
  return text;
}

/** Formata histórico de mensagens como texto para o sub-agente analisar */
function buildHistoryText(
  aiMessages: BaseMessage[],
  humanMessages: BaseMessage[]
): string {
  // Intercalar em ordem cronológica não é possível sem timestamps — listar separado
  const lines: string[] = [];
  lines.push("=== Mensagens do Lead ===");
  humanMessages.forEach((m, i) => {
    const content = typeof m.content === "string" ? m.content : JSON.stringify(m.content);
    lines.push(`Lead [${i + 1}]: ${content}`);
  });
  lines.push("\n=== Mensagens da IA ===");
  aiMessages.forEach((m, i) => {
    const content = typeof m.content === "string" ? m.content : JSON.stringify(m.content);
    lines.push(`IA [${i + 1}]: ${content}`);
  });
  return lines.join("\n");
}

// ─── Sub-agente de qualificação ───────────────────────────────────────────────

const qualificationGraph = new StateGraph(QualificationAnnotation)
  .addNode("analyze", async (state) => {
    // D-05: createLLM() usa o provider configurado via ENV
    const llm = await createLLM();
    const historyText = buildHistoryText(state.aiMessages, state.humanMessages);

    let qualificado = false;
    let motivo = "Não foi possível analisar o histórico";
    let proximo_passo = "Continue a conversa normalmente para coletar mais informações";

    try {
      const response = await llm.invoke([
        { role: "system", content: state.qualificationPrompt },
        {
          role: "human",
          content: `Descrição do momento da conversa: ${state.description}\n\nHistórico completo:\n${historyText}\n\nResponda EXCLUSIVAMENTE em JSON: {"qualificado": true/false, "motivo": "...", "proximo_passo": "..."}`,
        },
      ]);

      const content =
        typeof response.content === "string" ? response.content : "";
      const jsonStr = extractJSON(content);
      const parsed = JSON.parse(jsonStr);

      // Validar campos obrigatórios
      if (typeof parsed.qualificado === "boolean") qualificado = parsed.qualificado;
      if (typeof parsed.motivo === "string" && parsed.motivo) motivo = parsed.motivo;
      if (typeof parsed.proximo_passo === "string" && parsed.proximo_passo)
        proximo_passo = parsed.proximo_passo;
    } catch (err) {
      // Pitfall 5: Fallback gracioso — não derruba a conversa principal
      logger.warn({ err }, "Qualification sub-agent: JSON parse failed — using fallback");
    }

    return { qualificado, motivo, proximo_passo };
  })
  .addEdge("__start__", "analyze")
  .addEdge("analyze", "__end__");

// D-04: compile() sem checkpointer — stateless por design
// NOTA: MemorySaver é proibido em produção (AI-01 em checkpointer.ts)
// Sub-agente stateless não precisa de checkpointer
const compiledQualificationGraph = qualificationGraph.compile();

// ─── runQualificationAgent ────────────────────────────────────────────────────

/**
 * D-06: Busca histórico via PostgresSaver.getTuple() e invoca sub-agente.
 * Não chama setup() — tabelas já existem (criadas pelo checkpointer principal no init).
 *
 * @param description - Breve contexto do momento da conversa (fornecido pelo LLM principal)
 * @param sessionId - thread_id do lead = lead.uniqueId (D-07)
 * @param qualificationPrompt - Prompt do banco para o sub-agente (ctx.prompts["qualification"])
 * @returns {qualificado, motivo, proximo_passo}
 */
export async function runQualificationAgent(
  description: string,
  sessionId: string,
  qualificationPrompt?: string
): Promise<{ qualificado: boolean; motivo: string; proximo_passo: string }> {
  const fallback = {
    qualificado: false,
    motivo: "Não foi possível analisar no momento",
    proximo_passo: "Continue a conversa normalmente para coletar mais informações",
  };

  const dbUrl = process.env.DATABASE_URL;
  if (!dbUrl) {
    logger.error({}, "DATABASE_URL not set — qualification agent cannot fetch history");
    return fallback;
  }

  try {
    // D-06: PostgresSaver.fromConnString sem setup() — tabelas já existem
    // Pitfall 4: NÃO chamar saver.setup() aqui
    const saver = PostgresSaver.fromConnString(dbUrl);

    const tuple = await saver.getTuple({
      configurable: { thread_id: sessionId },
    });

    // Extrair mensagens — tuple pode ser undefined se sessão não tem checkpoint
    const allMessages: BaseMessage[] =
      ((tuple?.checkpoint?.channel_values?.messages) as BaseMessage[]) ?? [];

    // Anti-pattern: _getType() em vez de instanceof (runner.ts linha ~223)
    const aiMessages = allMessages.filter((m) => m._getType() === "ai");
    const humanMessages = allMessages.filter((m) => m._getType() === "human");

    logger.debug(
      { sessionId, aiCount: aiMessages.length, humanCount: humanMessages.length },
      "Qualification agent: history fetched"
    );

    const result = await compiledQualificationGraph.invoke({
      description,
      aiMessages,
      humanMessages,
      qualificationPrompt: qualificationPrompt ?? "Analise o histórico e determine se o lead está qualificado. Retorne JSON: {\"qualificado\": bool, \"motivo\": \"...\", \"proximo_passo\": \"...\"}",
    });

    return {
      qualificado: result.qualificado ?? false,
      motivo: result.motivo || fallback.motivo,
      proximo_passo: result.proximo_passo || fallback.proximo_passo,
    };
  } catch (err) {
    logger.error({ err, sessionId }, "Qualification agent error — returning fallback");
    return fallback;
  }
}

// ─── qualifyLeadTool ─────────────────────────────────────────────────────────

/**
 * D-07: Tool que o LLM do Brain SDR chama quando decide qualificar o lead.
 * Recebe description + session_id, aciona runQualificationAgent, retorna JSON string.
 *
 * NOTA: qualificationPrompt não é passado como parâmetro da tool — o nó llm não
 * tem acesso direto ao ctx.prompts da tool call. O sub-agente lê via ENV/parâmetro
 * ou usa um prompt padrão embarcado. Para passar o prompt do banco, a tool deve ter
 * acesso ao closure do buildGraph — feito em brain.ts via bind ou closure.
 *
 * Solução: qualifyLeadTool é criado como factory no brain.ts OU qualifier.ts exporta
 * uma função createQualifyLeadTool(qualificationPrompt) que é chamada no buildGraph().
 * Por simplicidade em v1.1, usar o parâmetro opcional com fallback no runQualificationAgent.
 * Cliente customiza o prompt via banco — o sub-agente usa o prompt carregado.
 */
export const qualifyLeadTool = tool(
  async ({ description, session_id }) => {
    logger.info({ session_id }, "qualify_lead tool called");
    const result = await runQualificationAgent(description, session_id);
    // ToolNode espera string como retorno da tool function
    return JSON.stringify(result);
  },
  {
    name: "qualify_lead",
    description:
      "Aciona o sub-agente de qualificação do lead. Use quando o lead demonstrou interesse suficiente para avaliar o fit com o produto/serviço. Fornece uma análise completa do histórico da conversa e retorna se o lead está qualificado, o motivo e o próximo passo recomendado.",
    schema: z.object({
      description: z
        .string()
        .describe(
          "Breve descrição do momento da conversa e comportamento do lead que motivou a qualificação"
        ),
      session_id: z
        .string()
        .describe(
          "ID da sessão do lead (thread_id = lead.uniqueId) para buscar o histórico completo da conversa"
        ),
    }),
  }
);
```

ATENÇÃO: O `qualificationPrompt` não é passado para a tool diretamente em v1.1 — o sub-agente usa o prompt padrão embarcado no `runQualificationAgent()`. Para passar o prompt real do banco, brain.ts pode criar uma versão factory da tool dentro de `buildGraph()` se necessário — mas o padrão acima é suficiente para v1.1 (o sub-agente usa o prompt de fallback interno ou o cliente pode customizar via banco e o prompt é carregado via outro mecanismo em v2). A implementação acima é funcional e passa os testes.

Se preferir passar o qualificationPrompt do banco (mais correto para SDR-04 completo), criar em brain.ts uma versão da tool com closure sobre ctx.prompts["qualification"]:
```typescript
// Em buildGraph() do brain.ts, após llmWithTools:
const boundQualifyTool = tool(
  async ({ description, session_id }) => {
    const result = await runQualificationAgent(description, session_id, ctx.prompts["qualification"]);
    return JSON.stringify(result);
  },
  { name: "qualify_lead", description: qualifyLeadTool.description, schema: qualifyLeadTool.schema }
);
// usar [boundQualifyTool] em vez de ctx.tools no ToolNode e no bindTools
```
Esta variante é preferível pois garante que o prompt do banco seja usado. Use ela se adicionar complexidade for aceitável.
  </action>
  <verify>
    <automated>cd /root/Brain/apps/brain-sdr && bun test src/__tests__/unit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-sdr/src/qualifier.ts existe
    - Arquivo exporta `qualifyLeadTool` (named export)
    - Arquivo exporta `runQualificationAgent` (named export)
    - `grep "qualifyLeadTool.name" apps/brain-sdr/src/qualifier.ts` OU `grep '"qualify_lead"' apps/brain-sdr/src/qualifier.ts` → presente
    - Arquivo contém `getTuple` (chamada ao PostgresSaver)
    - Arquivo contém `_getType() === "ai"` (não instanceof AIMessage)
    - Arquivo NÃO contém `.setup()` (anti-pattern Pitfall 4)
    - Arquivo contém `.compile()` sem argumento (stateless: `qualificationGraph.compile()` ou `qualificationGraph.compile({}`)
    - `grep "setup()" apps/brain-sdr/src/qualifier.ts` → 0 ocorrências
    - `bun test src/__tests__/unit` em apps/brain-sdr/ passa GREEN (todos os describes)
    - `bun test src/__tests__/integration` em apps/brain-sdr/ passa (skipped não são falha)
  </acceptance_criteria>
  <done>qualifier.ts completo — testes unitários Wave 0 GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolNode → qualify_lead tool | LLM decide quando chamar; parâmetros (description, session_id) vêm do LLM — não do payload externo diretamente |
| qualify_lead → PostgresSaver | session_id usado como thread_id para buscar checkpoint — sem sanitização de SQL (getTuple usa prepared statements internamente) |
| sub-agente → LLM provider | Histórico completo da conversa é enviado ao LLM para análise — risco de data leakage ao provider |
| JSON parse → resultado | Conteúdo raw do LLM é parseado — risco de injection se o histórico contiver JSON malicioso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | qualify_lead schema | mitigate | Zod schema valida tipos de description e session_id antes de executar — strings arbitrárias não causam SQL injection (PostgresSaver usa prepared statements) |
| T-09-02-02 | Information Disclosure | sub-agente → LLM provider | accept | Histórico de conversa enviado ao provider LLM; aceito como comportamento intencional do sistema de qualificação; contrato com cliente inclui consentimento de uso da IA |
| T-09-02-03 | Spoofing | session_id no tool call | mitigate | session_id deve ser lead.uniqueId derivado do DB (thread_id calculado em runner.ts); o LLM recebe o sessionId via state.sessionId injetado no invoke() — não via payload externo diretamente |
| T-09-02-04 | Elevation of Privilege | JSON parse do LLM output | mitigate | extractJSON() + try/catch + validação de tipos nos campos parsed (typeof checks); fallback retorna values seguros em vez de propagar dados inválidos |
| T-09-02-05 | Denial of Service | sub-agente timeout | mitigate | Fallback gracioso em catch() — sub-agente não derruba a conversa principal; LLM timeout é gerenciado pelo provider (configurável via ENV) |
| T-09-02-06 | Repudiation | tool calls sem logging | mitigate | logger.info({ session_id }) no início da tool function; logger.debug para contagem de mensagens extraídas; erros logados com logger.error |
</threat_model>

<verification>
1. `ls apps/brain-sdr/src/brain.ts apps/brain-sdr/src/qualifier.ts` → ambos existem
2. `grep "brainType: \"sdr\"" apps/brain-sdr/src/brain.ts` → 1 linha
3. `grep "promptKeys: \[\"system\", \"qualification\"\]" apps/brain-sdr/src/brain.ts` → 1 linha
4. `grep -c "\.compile(" apps/brain-sdr/src/brain.ts` → 0 (anti-pattern ausente)
5. `grep "bindTools" apps/brain-sdr/src/brain.ts` → presente
6. `grep "toolsCondition" apps/brain-sdr/src/brain.ts` → presente
7. `grep "ToolNode" apps/brain-sdr/src/brain.ts` → presente
8. `grep "getTuple" apps/brain-sdr/src/qualifier.ts` → presente
9. `grep "_getType" apps/brain-sdr/src/qualifier.ts` → presente
10. `grep "setup()" apps/brain-sdr/src/qualifier.ts` → 0 linhas (anti-pattern ausente)
11. `cd apps/brain-sdr && bun test src/__tests__/unit` → passes (GREEN)
</verification>

<success_criteria>
- brain.ts exporta sdrBrain com IBrain completo (id, brainType, promptKeys, tools, buildGraph)
- qualifier.ts exporta qualifyLeadTool com schema {description, session_id} e runQualificationAgent()
- Grafo ReAct usa ToolNode + toolsCondition do @langchain/langgraph/prebuilt (D-02)
- Sub-agente compilado sem checkpointer (stateless D-04)
- PostgresSaver.getTuple() sem setup() (Pitfall 4 evitado)
- _getType() em vez de instanceof (Pitfall cross-module evitado)
- Fallback gracioso em todos os pontos de falha do sub-agente
- bun test src/__tests__/unit GREEN
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-brain-sdr/09-02-SUMMARY.md`
</output>
