---
phase: 09-brain-sdr
plan: "03"
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - apps/brain-sdr/src/index.ts
  - apps/brain-sdr/src/server.ts
  - apps/brain-sdr/tsconfig.json
  - apps/brain-sdr/Dockerfile
autonomous: false
requirements: [SDR-01, SDR-02, SDR-03, INFRA-01]

must_haves:
  truths:
    - "Brain SDR arranca sem erros quando todas as ENVs obrigatórias estão presentes"
    - "TenantPoolManager.getPool(DATABASE_NAME) cria o pool e passa Sql ao BrainRunner (INFRA-01)"
    - "index.ts sai com processo exit(1) e mensagem de log se qualquer ENV obrigatória do TenantPoolManager estiver ausente"
    - "DATABASE_URL validado separadamente (necessário para PostgresSaver do BrainRunner)"
    - "ToolsRegistry registra brainType 'sdr' com tool 'qualify_lead' antes de criar BrainRunner"
    - "Hono server monta os 3 sub-apps: /health, POST /api/v1/webhook, POST /reload-prompts"
    - "Dockerfile copia brain-sdr dist + node_modules — padrão idêntico ao brain-echo"
    - "bun test src/__tests__/unit GREEN após este plano"
  artifacts:
    - path: "apps/brain-sdr/src/index.ts"
      provides: "Entrypoint com TenantPoolManager + BrainRunner + Hono server"
      exports: []
    - path: "apps/brain-sdr/src/server.ts"
      provides: "Hono app com 3 sub-apps montados"
      exports: ["createServer"]
    - path: "apps/brain-sdr/tsconfig.json"
      provides: "TypeScript config com referências aos packages do monorepo"
    - path: "apps/brain-sdr/Dockerfile"
      provides: "Docker multi-stage build para imagem de produção"
  key_links:
    - from: "apps/brain-sdr/src/index.ts"
      to: "TenantPoolManager.getPool(DATABASE_NAME)"
      via: "@brain-pkg/database"
      pattern: "getPool"
    - from: "apps/brain-sdr/src/index.ts"
      to: "new BrainRunner({ sql, brain: sdrBrain, toolsRegistry })"
      via: "@brain-pkg/core"
      pattern: "BrainRunner"
    - from: "apps/brain-sdr/src/server.ts"
      to: "createWebhookApp + createHealthApp + createCoreApp"
      via: "app.route('/')"
      pattern: "createServer"
---

<objective>
Wave 3: Criar o entrypoint, server Hono, tsconfig e Dockerfile do Brain SDR — tornando-o um app completo pronto para execução e containerização.

Purpose: INFRA-01 exige TenantPoolManager ativo no entrypoint. SDR-01/02/03 são verificados end-to-end via integração (comportamentos herdados do BrainRunner). O Dockerfile garante que o Brain SDR pode ser distribuído como imagem Docker independente.

Output: apps/brain-sdr/ completo como workspace Bun com entrypoint, server, tsconfig e Dockerfile. Imagem Docker buildável com `docker build -f apps/brain-sdr/Dockerfile . -t brain-sdr`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-brain-sdr/09-CONTEXT.md
@.planning/phases/09-brain-sdr/09-RESEARCH.md
@.planning/phases/09-brain-sdr/09-02-SUMMARY.md

<interfaces>
<!-- TenantPoolManager — packages/database/src/pool-manager.ts -->
```typescript
export class TenantPoolManager {
  constructor(config: {
    host: string;
    port: number;
    username: string;
    password: string;
    max: number;
    idle_timeout: number;
  }, maxTenants?: number)
  
  getPool(databaseName: string): Sql  // retorna postgres.js Sql instance
  closeAll(): Promise<void>
}
// Importar de "@brain-pkg/database"
```

<!-- BrainRunnerOptions — packages/core/src/runner/runner.ts -->
```typescript
export interface BrainRunnerOptions {
  brain: IBrain;
  sql: Sql;               // pool do TenantPoolManager.getPool(DATABASE_NAME)
  toolsRegistry: ToolsRegistry;
  llmOptions?: LLMOptions;
  migrationsFolder?: string;  // ou usar MIGRATIONS_FOLDER ENV
}
```

<!-- ToolsRegistry — packages/core/src/tools/registry.ts -->
```typescript
// enableTool cria brainType se não existir — registerBrainType() é opcional
toolsRegistry.enableTool("sdr", "qualify_lead");
// getTools("sdr", brain.tools) filtra pelo whitelist — lança ConfigurationError se brainType não registrado
```

<!-- brain-echo/src/index.ts — padrão a adaptar -->
<!-- Diferenças do brain-echo:
  1. Usar TenantPoolManager em vez de postgres() direto
  2. Validar DATABASE_HOST, DATABASE_PORT, DATABASE_USER, DATABASE_PASSWORD, DATABASE_NAME (D-12)
  3. toolsRegistry.enableTool("sdr", "qualify_lead") em vez de registerBrainType("echo")
  4. Importar sdrBrain em vez de echoBrain
-->

<!-- D-12: ENVs obrigatórias para TenantPoolManager:
  DATABASE_HOST, DATABASE_PORT, DATABASE_USER, DATABASE_PASSWORD, DATABASE_NAME
  DATABASE_URL ainda necessário para PostgresSaver interno do BrainRunner
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar index.ts, server.ts e tsconfig.json do Brain SDR</name>
  <files>apps/brain-sdr/src/index.ts, apps/brain-sdr/src/server.ts, apps/brain-sdr/tsconfig.json</files>
  <read_first>
    - apps/brain-echo/src/index.ts (padrão de entrypoint — ler inteiro, adaptar)
    - apps/brain-echo/src/server.ts (padrão de server Hono — copiar sem mudanças exceto import)
    - apps/brain-echo/tsconfig.json (copiar estrutura exata)
    - packages/database/src/pool-manager.ts (TenantPoolManager API — ler construtor e getPool)
    - packages/core/src/runner/runner.ts (BrainRunnerOptions — confirmar campos)
    - .planning/phases/09-brain-sdr/09-CONTEXT.md (D-10, D-11, D-12 — ENVs obrigatórias)
  </read_first>
  <action>
**1. Criar `apps/brain-sdr/src/server.ts`:**

Copiar exatamente de brain-echo/src/server.ts, sem alterações (os sub-apps são os mesmos). Apenas o import do BrainRunner type permanece — `createServer(sql, runner)` é genérico.

```typescript
// apps/brain-sdr/src/server.ts
// Idêntico ao brain-echo/src/server.ts — createServer é agnóstico ao Brain específico

import { Hono } from "hono";
import { createHealthApp } from "@brain-pkg/observability";
import { createWebhookApp } from "@brain-pkg/transport";
import { createCoreApp, type BrainRunner } from "@brain-pkg/core";
import type { Sql } from "postgres";

export function createServer(sql: Sql, runner: BrainRunner): Hono {
  const app = new Hono();
  app.route("/", createHealthApp(sql));
  app.route("/", createWebhookApp(runner));
  app.route("/", createCoreApp(runner));
  return app;
}
```

**2. Criar `apps/brain-sdr/src/index.ts`:**

Baseado em brain-echo/src/index.ts mas com TenantPoolManager (D-10) em vez de postgres() direto.

```typescript
// apps/brain-sdr/src/index.ts
// D-10: TenantPoolManager ativado no entrypoint — zero mudança no SDK (packages/core)
// D-11: 1 instância = 1 cliente — DATABASE_NAME é fixo por instância Docker via ENV
// D-12: ENVs obrigatórias: DATABASE_HOST, DATABASE_PORT, DATABASE_USER, DATABASE_PASSWORD, DATABASE_NAME
//        + DATABASE_URL para PostgresSaver interno do BrainRunner

import { TenantPoolManager } from "@brain-pkg/database";
import { BrainRunner, ToolsRegistry } from "@brain-pkg/core";
import { createLogger } from "@brain-pkg/observability";
import { createServer } from "./server.js";
import { sdrBrain } from "./brain.js";

const logger = createLogger();

async function main() {
  // D-12: Validar ENVs obrigatórias do TenantPoolManager — fail-fast (process.exit(1))
  const {
    DATABASE_HOST,
    DATABASE_PORT,
    DATABASE_USER,
    DATABASE_PASSWORD,
    DATABASE_NAME,
    DATABASE_URL,
  } = process.env;

  if (!DATABASE_HOST || !DATABASE_PORT || !DATABASE_USER || !DATABASE_PASSWORD || !DATABASE_NAME) {
    logger.error(
      {},
      "Missing required DATABASE_* env vars for TenantPoolManager (DATABASE_HOST, DATABASE_PORT, DATABASE_USER, DATABASE_PASSWORD, DATABASE_NAME)"
    );
    process.exit(1);
  }

  // DATABASE_URL ainda necessário para PostgresSaver interno do BrainRunner._compileGraph()
  if (!DATABASE_URL) {
    logger.error({}, "DATABASE_URL not set — required for PostgresSaver (BrainRunner internal)");
    process.exit(1);
  }

  // D-10: TenantPoolManager cria pool de conexões para o DATABASE_NAME desta instância
  // D-11: getPool(DATABASE_NAME) — 1 instância = 1 banco do cliente
  const tenantPoolManager = new TenantPoolManager({
    host: DATABASE_HOST,
    port: parseInt(DATABASE_PORT, 10),
    username: DATABASE_USER,
    password: DATABASE_PASSWORD,
    max: 10,
    idle_timeout: 300,
  });
  const sql = tenantPoolManager.getPool(DATABASE_NAME);

  // ToolsRegistry: habilitar qualify_lead para brainType "sdr"
  // enableTool() cria o brainType se não existir — registerBrainType() é opcional (registry.ts)
  // Pitfall 2 do RESEARCH.md: se "sdr" não estiver registrado, getTools() lança ConfigurationError
  const toolsRegistry = new ToolsRegistry();
  toolsRegistry.enableTool("sdr", "qualify_lead");

  const runner = new BrainRunner({ brain: sdrBrain, sql, toolsRegistry });
  await runner.init();
  logger.info({}, "BrainRunner initialized");

  const app = createServer(sql, runner);
  const port = parseInt(process.env.PORT || "3000", 10);

  Bun.serve({ port, fetch: app.fetch });
  logger.info({ port }, "brain-sdr server listening");
}

main();
```

**3. Criar `apps/brain-sdr/tsconfig.json`:**

Copiar exatamente de brain-echo/tsconfig.json:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"],
  "references": [
    { "path": "../../packages/shared" },
    { "path": "../../packages/database" },
    { "path": "../../packages/observability" },
    { "path": "../../packages/ai" },
    { "path": "../../packages/memory" },
    { "path": "../../packages/transport" },
    { "path": "../../packages/core" }
  ]
}
```
  </action>
  <verify>
    <automated>grep "TenantPoolManager" /root/Brain/apps/brain-sdr/src/index.ts && grep "enableTool" /root/Brain/apps/brain-sdr/src/index.ts && grep "DATABASE_NAME" /root/Brain/apps/brain-sdr/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-sdr/src/index.ts existe e contém `TenantPoolManager`
    - apps/brain-sdr/src/index.ts contém `toolsRegistry.enableTool("sdr", "qualify_lead")`
    - apps/brain-sdr/src/index.ts contém validação de todas as 5 ENVs: `DATABASE_HOST`, `DATABASE_PORT`, `DATABASE_USER`, `DATABASE_PASSWORD`, `DATABASE_NAME`
    - apps/brain-sdr/src/index.ts contém validação de `DATABASE_URL`
    - apps/brain-sdr/src/index.ts contém `tenantPoolManager.getPool(DATABASE_NAME)` (não hardcoded)
    - apps/brain-sdr/src/index.ts NÃO usa `postgres(connectionString)` direto (usa TenantPoolManager)
    - apps/brain-sdr/src/server.ts existe e contém `createHealthApp`, `createWebhookApp`, `createCoreApp`
    - apps/brain-sdr/tsconfig.json existe e contém `"extends": "../../tsconfig.base.json"`
    - apps/brain-sdr/tsconfig.json contém as 7 referências (shared, database, observability, ai, memory, transport, core)
  </acceptance_criteria>
  <done>index.ts, server.ts e tsconfig.json criados — app completo como workspace Bun</done>
</task>

<task type="auto">
  <name>Task 2: Criar Dockerfile do Brain SDR</name>
  <files>apps/brain-sdr/Dockerfile</files>
  <read_first>
    - apps/brain-echo/Dockerfile (template completo — ler inteiro, adaptar para brain-sdr)
    - apps/brain-sdr/package.json (confirmar nome "@brain-app/sdr" para --filter no pnpm build)
  </read_first>
  <action>
Criar `apps/brain-sdr/Dockerfile` baseado em apps/brain-echo/Dockerfile. Diferenças:
- Todas as referências `brain-echo` → `brain-sdr`
- `@brain-app/echo` → `@brain-app/sdr`
- `CMD` final: `bun apps/brain-sdr/dist/index.js`

Conteúdo completo:

```dockerfile
# apps/brain-sdr/Dockerfile
# Build context: raiz do monorepo
# Uso: docker build -f apps/brain-sdr/Dockerfile . -t brain-sdr

# ─────────────────────────────────────────────────────────────
# Stage 1: builder
# ─────────────────────────────────────────────────────────────
FROM node:22-slim AS builder
WORKDIR /app

COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
COPY tsconfig.base.json ./
COPY turbo.json ./

COPY packages/ ./packages/
COPY apps/brain-sdr/ ./apps/brain-sdr/

RUN npm install -g pnpm@11.5.3

RUN pnpm install --frozen-lockfile

# Compilar packages em ordem de dependência (mesma ordem do brain-echo)
RUN pnpm --filter @brain-pkg/shared build
RUN pnpm --filter @brain-pkg/database build
RUN pnpm --filter @brain-pkg/observability build
RUN pnpm --filter @brain-pkg/ai build
RUN pnpm --filter @brain-pkg/transport build
RUN pnpm --filter @brain-pkg/memory build
RUN pnpm --filter @brain-pkg/core build
RUN pnpm --filter @brain-app/sdr build

# ─────────────────────────────────────────────────────────────
# Stage 2: runner
# ─────────────────────────────────────────────────────────────
FROM oven/bun:1 AS runner
WORKDIR /app

COPY --from=builder /app/node_modules ./node_modules

COPY --from=builder /app/packages/ai/dist ./packages/ai/dist
COPY --from=builder /app/packages/ai/package.json ./packages/ai/package.json
COPY --from=builder /app/packages/core/dist ./packages/core/dist
COPY --from=builder /app/packages/core/package.json ./packages/core/package.json
COPY --from=builder /app/packages/database/dist ./packages/database/dist
COPY --from=builder /app/packages/database/package.json ./packages/database/package.json
COPY --from=builder /app/packages/memory/dist ./packages/memory/dist
COPY --from=builder /app/packages/memory/package.json ./packages/memory/package.json
COPY --from=builder /app/packages/observability/dist ./packages/observability/dist
COPY --from=builder /app/packages/observability/package.json ./packages/observability/package.json
COPY --from=builder /app/packages/shared/dist ./packages/shared/dist
COPY --from=builder /app/packages/shared/package.json ./packages/shared/package.json
COPY --from=builder /app/packages/transport/dist ./packages/transport/dist
COPY --from=builder /app/packages/transport/package.json ./packages/transport/package.json

COPY --from=builder /app/packages/ai/node_modules ./packages/ai/node_modules
COPY --from=builder /app/packages/core/node_modules ./packages/core/node_modules
COPY --from=builder /app/packages/database/node_modules ./packages/database/node_modules
COPY --from=builder /app/packages/memory/node_modules ./packages/memory/node_modules
COPY --from=builder /app/packages/observability/node_modules ./packages/observability/node_modules
COPY --from=builder /app/packages/shared/node_modules ./packages/shared/node_modules
COPY --from=builder /app/packages/transport/node_modules ./packages/transport/node_modules

COPY --from=builder /app/apps/brain-sdr/dist ./apps/brain-sdr/dist
COPY --from=builder /app/apps/brain-sdr/node_modules ./apps/brain-sdr/node_modules

# Migrations SQL — copiadas para /app/migrations (Pitfall do brain-echo Dockerfile)
COPY --from=builder /app/packages/database/src/migrations ./migrations

ENV PORT=3000
ENV NODE_ENV=production
ENV MIGRATIONS_FOLDER=/app/migrations

USER bun

CMD ["bun", "apps/brain-sdr/dist/index.js"]
```
  </action>
  <verify>
    <automated>grep -E "brain-sdr|@brain-app/sdr|CMD" /root/Brain/apps/brain-sdr/Dockerfile | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/brain-sdr/Dockerfile existe
    - Dockerfile contém `COPY apps/brain-sdr/ ./apps/brain-sdr/` no stage builder
    - Dockerfile contém `pnpm --filter @brain-app/sdr build`
    - Dockerfile contém `COPY --from=builder /app/apps/brain-sdr/dist ./apps/brain-sdr/dist`
    - Dockerfile contém `COPY --from=builder /app/packages/database/src/migrations ./migrations`
    - Dockerfile contém `ENV MIGRATIONS_FOLDER=/app/migrations` (nome correto conforme runner.ts)
    - Dockerfile contém `CMD ["bun", "apps/brain-sdr/dist/index.js"]`
    - Dockerfile contém `USER bun` (segurança — não-root)
    - Dockerfile NÃO contém referências a "brain-echo" (substituídas por "brain-sdr")
    - `grep -c "brain-echo" apps/brain-sdr/Dockerfile` → 0
  </acceptance_criteria>
  <done>Dockerfile multi-stage criado — brain-sdr buildável como imagem Docker independente</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Brain SDR completo:
- Wave 0: package.json + test stubs (unit RED → GREEN após Wave 2)
- Wave 1: migration 0005_brain_sdr_prompts.sql (seed SDR prompts)
- Wave 2: brain.ts (IBrain ReAct + ToolNode) + qualifier.ts (qualify_lead + sub-agente)
- Wave 3: index.ts (TenantPoolManager + BrainRunner) + server.ts (Hono) + tsconfig.json + Dockerfile
  </what-built>
  <how-to-verify>
**Verificação 1 — Estrutura de arquivos:**
```bash
ls apps/brain-sdr/src/brain.ts apps/brain-sdr/src/qualifier.ts apps/brain-sdr/src/index.ts apps/brain-sdr/src/server.ts apps/brain-sdr/tsconfig.json apps/brain-sdr/Dockerfile apps/brain-sdr/package.json
ls packages/database/src/migrations/0005_brain_sdr_prompts.sql
```

**Verificação 2 — Testes unitários GREEN:**
```bash
cd apps/brain-sdr && bun test src/__tests__/unit
```
Esperado: todos os describes passam (GREEN)

**Verificação 3 — Testes de integração executam (skipped):**
```bash
cd apps/brain-sdr && bun test src/__tests__/integration
```
Esperado: todos os tests skipped, zero failures

**Verificação 4 — Anti-patterns ausentes:**
```bash
grep -c "\.compile(" apps/brain-sdr/src/brain.ts    # → 0
grep -c "setup()" apps/brain-sdr/src/qualifier.ts    # → 0
grep -c "instanceof AIMessage" apps/brain-sdr/src/qualifier.ts  # → 0
grep -c "brain-echo" apps/brain-sdr/Dockerfile        # → 0
```

**Verificação 5 — Contratos críticos presentes:**
```bash
grep "toolsCondition" apps/brain-sdr/src/brain.ts     # → presente
grep "ToolNode" apps/brain-sdr/src/brain.ts            # → presente
grep "bindTools" apps/brain-sdr/src/brain.ts           # → presente
grep "getTuple" apps/brain-sdr/src/qualifier.ts        # → presente
grep "_getType" apps/brain-sdr/src/qualifier.ts        # → presente
grep "TenantPoolManager" apps/brain-sdr/src/index.ts  # → presente
grep 'enableTool.*sdr.*qualify_lead' apps/brain-sdr/src/index.ts  # → presente
```

**Verificação 6 — Migration SDR:**
```bash
grep -c "ON CONFLICT" packages/database/src/migrations/0005_brain_sdr_prompts.sql  # → 2
grep "brain_type" packages/database/src/migrations/0005_brain_sdr_prompts.sql | grep "'sdr'"  # → 2 linhas
```
  </how-to-verify>
  <resume-signal>Digite "aprovado" se todas as verificações passaram, ou descreva o problema encontrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → TenantPoolManager | DATABASE_HOST/PORT/USER/PASSWORD/NAME vêm do ambiente Docker — não do payload de request |
| TenantPoolManager → PostgreSQL | Conexão com banco do cliente via pool autenticado |
| Hono → BrainRunner | Payload de webhook/RabbitMQ atravessa a camada de transporte antes de chegar ao runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Spoofing | DATABASE_NAME via ENV | accept | DATABASE_NAME é imutável por instância Docker — fixo no momento de deploy (D-11). Não há roteamento dinâmico de tenant em v1.1. |
| T-09-03-02 | Tampering | TenantPoolManager credentials | mitigate | ENVs de credenciais (DATABASE_PASSWORD) injetadas via Docker secrets ou variáveis de ambiente do orquestrador; não hardcoded; validadas no startup com process.exit(1) se ausentes |
| T-09-03-03 | Denial of Service | pool max connections | mitigate | `max: 10, idle_timeout: 300` limita conexões por instância; LRU cache de 20 tenants no TenantPoolManager previne pool explosion |
| T-09-03-04 | Elevation of Privilege | USER bun no Dockerfile | mitigate | `USER bun` no stage runner executa o processo como não-root (imagem oven/bun:1 já inclui usuário bun) — ASVS L1 V10.2.1 |
| T-09-03-05 | Information Disclosure | log de startup | mitigate | logger.error não loga valores de DATABASE_PASSWORD; apenas indica qual ENV está faltando (nome, não valor) |
| T-09-03-06 | Repudiation | BrainRunner.init() silencioso | mitigate | runner.init() já loga brainId e brainType; index.ts adiciona log "BrainRunner initialized" e "brain-sdr server listening" com port |
</threat_model>

<verification>
1. `ls apps/brain-sdr/src/{brain,qualifier,index,server}.ts apps/brain-sdr/{tsconfig.json,Dockerfile,package.json}` → todos existem
2. `cd apps/brain-sdr && bun test src/__tests__/unit` → GREEN
3. `cd apps/brain-sdr && bun test src/__tests__/integration` → 0 failures (todos skipped)
4. `grep "TenantPoolManager" apps/brain-sdr/src/index.ts` → presente
5. `grep 'enableTool("sdr"' apps/brain-sdr/src/index.ts` → presente
6. `grep -c "\.compile(" apps/brain-sdr/src/brain.ts` → 0
7. `grep -c "brain-echo" apps/brain-sdr/Dockerfile` → 0
8. `grep "USER bun" apps/brain-sdr/Dockerfile` → presente
9. `grep "MIGRATIONS_FOLDER" apps/brain-sdr/Dockerfile` → presente
</verification>

<success_criteria>
- apps/brain-sdr/ é um workspace Bun completo com todos os 4 arquivos fonte + configuração + Dockerfile
- TenantPoolManager ativo no entrypoint com validação de todas as ENVs obrigatórias (D-10, D-11, D-12)
- ToolsRegistry registra "sdr" com "qualify_lead" antes de criar BrainRunner
- Dockerfile buildável com `docker build -f apps/brain-sdr/Dockerfile . -t brain-sdr` no contexto raiz do monorepo
- Testes unitários GREEN, integração skipped (sem falhas)
- Checkpoint humano: todas as verificações passaram
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/09-brain-sdr/09-03-SUMMARY.md`
</output>
