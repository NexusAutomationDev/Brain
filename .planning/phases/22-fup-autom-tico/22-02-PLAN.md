---
phase: 22-fup-autom-tico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/fup/fup-scheduler.ts
  - packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts
  - packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts
autonomous: true
requirements:
  - FUP-01
  - FUP-02
  - FUP-03
  - FUP-05
  - FUP-07
  - FUP-08
  - EVT-03
must_haves:
  truths:
    - "FupScheduler implementa IFupScheduler com start() e stop()"
    - "getNextValidSlot() calcula o próximo slot válido respeitando hora e dia em timezone IANA"
    - "SELECT FOR UPDATE OF l SKIP LOCKED é usado para evitar duplo envio em múltiplas instâncias"
    - "LLM one-shot usa checkpointer.getTuple() para recuperar histórico"
    - "Ao enviar último FUP: ia_ativada=false e fup_enabled=false no lead"
    - "fup_failure_count >= 3 desativa fup_enabled e loga logger.error"
    - "EVT-03 publicado fire-and-forget via eventPublisher.publish após envio bem-sucedido"
    - "Testes unitários cobrem todos os comportamentos acima com mocks"
  artifacts:
    - path: "packages/core/src/fup/fup-scheduler.ts"
      provides: "FupScheduler class + IFupScheduler interface"
      exports: ["FupScheduler", "IFupScheduler"]
    - path: "packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts"
      provides: "Testes unitários do FupScheduler"
      contains: "describe"
    - path: "packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts"
      provides: "Testes unitários de getNextValidSlot"
      contains: "getNextValidSlot"
  key_links:
    - from: "packages/core/src/fup/fup-scheduler.ts"
      to: "packages/core/src/events/event-publisher.ts"
      via: "IEventPublisher injetado no construtor (D-18)"
      pattern: "IEventPublisher"
    - from: "packages/core/src/fup/fup-scheduler.ts"
      to: "checkpointer.getTuple"
      via: "PostgresSaver injetado no construtor (D-12)"
      pattern: "getTuple"
---

<objective>
Implementar `FupScheduler` — a classe central do FUP Automático — com toda a lógica de negócio: polling, elegibilidade, geração LLM one-shot, envio via webhook, retry com fup_failure_count, cálculo de slot válido em timezone IANA, e publicação de EVT-03. Acompanhado de testes unitários completos com mocks.

Purpose: Esta é a peça central da feature. Pode rodar em paralelo com o Plan 01 (schema) porque os testes unitários usam mocks de banco — sem dependência do banco real neste plano.

Output:
- `packages/core/src/fup/fup-scheduler.ts` — implementação completa
- `packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts` — testes das regras de negócio
- `packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts` — testes de getNextValidSlot
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-fup-autom-tico/22-CONTEXT.md
@.planning/phases/22-fup-autom-tico/22-RESEARCH.md

@packages/core/src/events/event-publisher.ts
@packages/core/src/runner/runner.ts
@packages/core/src/leads/lead-service.ts
@packages/database/src/schema/tables.ts
</context>

<interfaces>
<!-- Interfaces e tipos que o FupScheduler usa — extraídos do código existente -->

```typescript
// De packages/core/src/events/event-publisher.ts:
export interface ToolEvent {
  event_id: string;    // para FUP: `${lead.uniqueId}:fup:${fup_step}`
  action: string;      // "fup" para EVT-03
  lead: { id: string; nome: string | null; numero: string; };
  result: string;      // para FUP: JSON.stringify({ step, message })
  timestamp: string;
}
export interface IEventPublisher {
  publish(events: ToolEvent[]): Promise<void>;
  close(): Promise<void>;
}

// De @langchain/langgraph-checkpoint-postgres (PostgresSaver):
// checkpointer.getTuple({ configurable: { thread_id: string } })
// Retorna: CheckpointTuple | undefined
// tuple?.checkpoint?.channel_values?.messages → BaseMessage[] | undefined

// De packages/database/src/schema/tables.ts — campos do lead relevantes ao FUP:
// fupEnabled: boolean
// fupStep: integer (DEFAULT 0)
// fupNextAt: timestamp | null
// lastMessageAt: timestamp | null
// fupFailureCount: integer (DEFAULT 0) — adicionado no Plan 01
// iaAtivada: boolean
// uniqueId: string
// nome: string | null
// numero: string

// De packages/core/src/runner/runner.ts — createLLM:
// import { createLLM } from "@brain-pkg/ai";
// const llm = await createLLM(); // retorna BaseChatModel

// De @langchain/core/messages:
// new SystemMessage(content: string)
// new HumanMessage(content: string)
// BaseMessage (tipo base)
```

<!-- Estrutura do evento EVT-03 (D-17 do CONTEXT.md): -->
```typescript
// Evento FUP para EVT-03 — diferente dos tool events (result é objeto, não string raw):
{
  event_id: `${lead.uniqueId}:fup:${fupStep}`,  // idempotente por step
  action: "fup",
  lead: { id: lead.uniqueId, nome: lead.nome ?? null, numero: lead.numero },
  result: JSON.stringify({ step: fupStep, message: generatedMessage }),  // serializado como string para compatibilidade com ToolEvent.result: string
  timestamp: new Date().toISOString(),
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar FupScheduler com lógica de negócio completa</name>
  <files>
    packages/core/src/fup/fup-scheduler.ts
  </files>

  <read_first>
    - packages/core/src/events/event-publisher.ts — IEventPublisher, ToolEvent (interfaces a injetar/usar)
    - packages/core/src/runner/runner.ts — padrão de campo `private X | null = null` + init condicional + close (replicar exatamente)
    - packages/database/src/schema/tables.ts — campos fupEnabled, fupStep, fupNextAt, fupFailureCount, iaAtivada, uniqueId, nome, numero
    - .planning/phases/22-fup-autom-tico/22-RESEARCH.md — Pattern 2 (SELECT FOR UPDATE SKIP LOCKED), Pattern 3 (getNextValidSlot), Pattern 4 (LLM one-shot), Pattern 5 (retry fup_failure_count), seção de pitfalls
  </read_first>

  <behavior>
    - Test 1 (FUP-02): quando sql mock retorna lead com fup_next_at <= NOW(), _tick() chama _processFupForLead; quando retorna vazio, não chama
    - Test 2 (FUP-07/getNextValidSlot): dado minHour=9, maxHour=18, allowedDays=['mon','tue','wed','thu','fri'], timezone='America/Sao_Paulo' — slot num sábado às 14h retorna próxima segunda >= 9h; slot numa terça às 10h retorna a mesma terça às 10h (já válido)
    - Test 3 (FUP-07/getNextValidSlot): hora 24 normalizada para 0 (pitfall do Intl)
    - Test 4 (FUP-05): quando fupStep é o último (fupStep+1 >= intervals_seconds.length), após envio bem-sucedido: ia_ativada=false e fup_enabled=false setados no DB
    - Test 5 (FUP-08): quando _generateFupMessage lança 3x, fup_failure_count incrementado; quando fup_failure_count atinge 3, fup_enabled=false e logger.error chamado
    - Test 6 (FUP-08): quando envio tem sucesso após falhas anteriores, fup_failure_count resetado para 0
    - Test 7 (EVT-03 / D-16): após envio bem-sucedido, eventPublisher.publish chamado com action="fup", event_id=`${lead.uniqueId}:fup:${fupStep}`, result contém step e message
    - Test 8 (D-18): quando eventPublisher é null, publicação é pulada silenciosamente sem erro
    - Test 9 (D-13): quando prompt key='fup' não existe no banco (mock retorna null), logger.warn chamado e lead pulado sem processar
    - Test 10 (D-02 / stop()): clearInterval chamado em stop(); segundo stop() é noop
  </behavior>

  <action>
Criar o diretório `packages/core/src/fup/` e o arquivo `packages/core/src/fup/fup-scheduler.ts`.

**Estrutura completa da classe:**

```typescript
// FUP-01 a FUP-08, EVT-03: FupScheduler — scheduler background para follow-ups automáticos
// D-04: Ciclo de vida gerenciado pelo BrainRunner (init/close)
// D-05: FUP_POLL_INTERVAL_MS ENV (default 30000ms)
// D-06: sql injetado pelo BrainRunner (mesmo pool)
// D-07: SELECT FOR UPDATE OF l SKIP LOCKED — concorrência segura
// T-22-03: Logar apenas lead.uniqueId em warns — nunca conteúdo de mensagem (PII)

import type { Sql } from "postgres";
import type { PostgresSaver } from "@langchain/langgraph-checkpoint-postgres";
import { SystemMessage, HumanMessage } from "@langchain/core/messages";
import type { BaseMessage } from "@langchain/core/messages";
import { createLLM } from "@brain-pkg/ai";
import { createLogger } from "@brain-pkg/observability";
import type { IEventPublisher, ToolEvent } from "../events/event-publisher.js";

// Constantes de módulo — configuráveis futuramente via ENV (D-05, D-15)
const FUP_POLL_INTERVAL_MS = parseInt(process.env.FUP_POLL_INTERVAL_MS ?? "30000", 10);
const BATCH_SIZE = 10;
const MAX_FUP_ATTEMPTS = 3;
const MAX_FUP_FAILURES = 3;

export interface IFupScheduler {
  start(): Promise<void>;
  stop(): Promise<void>;
}

export interface FupSchedulerOptions {
  sql: Sql;
  brainType: string;
  checkpointer: PostgresSaver;
  eventPublisher: IEventPublisher | null;  // D-18: injetável; null = sem publicação
  fupWebhookUrl: string;  // D-01: obrigatório — caller verifica presença antes de construir
}

export class FupScheduler implements IFupScheduler {
  private readonly logger = createLogger();
  private intervalId: ReturnType<typeof setInterval> | null = null;

  constructor(private readonly opts: FupSchedulerOptions) {}

  async start(): Promise<void> {
    this.logger.info({ brainType: this.opts.brainType }, "FupScheduler started");
    // D-05: setInterval com FUP_POLL_INTERVAL_MS; bind para garantir contexto correto
    this.intervalId = setInterval(() => {
      this._tick().catch((err: unknown) => {
        this.logger.error({ err }, "FupScheduler tick falhou inesperadamente");
      });
    }, FUP_POLL_INTERVAL_MS);
  }

  async stop(): Promise<void> {
    if (this.intervalId !== null) {
      clearInterval(this.intervalId);
      this.intervalId = null;
    }
    this.logger.info({}, "FupScheduler stopped");
  }

  /** Exportada para testes — não chamada diretamente pelo caller externo */
  async _tick(): Promise<void> {
    // D-07: SELECT FOR UPDATE OF l SKIP LOCKED dentro de transação curta
    // Pitfall 4 da research: operações I/O longas (LLM, HTTP) fora da transação
    // Abordagem: transação curta apenas para SELECT; processar fora; UPDATE final em nova transação

    const rows = await this.opts.sql`
      SELECT
        l.id,
        l.unique_id AS "uniqueId",
        l.nome,
        l.numero,
        l.fup_step AS "fupStep",
        l.fup_next_at AS "fupNextAt",
        l.fup_failure_count AS "fupFailureCount",
        l.ia_ativada AS "iaAtivada",
        l.fup_enabled AS "fupEnabled",
        fc.intervals_seconds AS "intervalsSeconds",
        fc.min_hour AS "minHour",
        fc.max_hour AS "maxHour",
        fc.allowed_days AS "allowedDays",
        fc.timezone
      FROM leads l
      JOIN fup_config fc ON fc.brain_type = ${this.opts.brainType}
      WHERE l.fup_enabled = true
        AND l.ia_ativada = true
        AND l.fup_next_at <= NOW()
        AND l.fup_step < array_length(fc.intervals_seconds, 1)
        AND fc.enabled = true
        AND l.fup_failure_count < ${MAX_FUP_FAILURES}
      LIMIT ${BATCH_SIZE}
      FOR UPDATE OF l SKIP LOCKED
    `.catch((err: unknown) => {
      this.logger.error({ err }, "FupScheduler: falha ao buscar leads elegíveis");
      return [];
    }) as FupLeadRow[];

    for (const row of rows) {
      await this._processFupForLead(row);
    }
  }

  private async _processFupForLead(lead: FupLeadRow): Promise<void> {
    // D-11: buscar prompt 'fup' do banco
    const promptRows = await this.opts.sql`
      SELECT content FROM prompts
      WHERE brain_type = ${this.opts.brainType}
        AND key = 'fup'
      LIMIT 1
    `.catch(() => [] as { content: string }[]) as { content: string }[];

    if (!promptRows[0]) {
      // D-13: sem fallback; loga warn e pula
      this.logger.warn(
        { brainType: this.opts.brainType, uniqueId: lead.uniqueId },
        "FupScheduler: prompt key='fup' não encontrado — pulando lead (D-13)"
      );
      return;
    }

    const fupPrompt = promptRows[0].content;
    let lastErr: unknown;

    for (let attempt = 1; attempt <= MAX_FUP_ATTEMPTS; attempt++) {
      try {
        // D-12: LLM one-shot via getTuple
        const message = await this._generateFupMessage(lead, fupPrompt);
        // D-01: POST para FUP_WEBHOOK_URL com payload { Name, Numero, Message, IDLead }
        await this._sendFupWebhook(lead, message);

        // Sucesso: calcular próximo slot e atualizar banco
        const nextFupStep = lead.fupStep + 1;
        const isLastFup = nextFupStep >= lead.intervalsSeconds.length;

        if (isLastFup) {
          // D-10: último FUP — desativar ia e fup
          await this.opts.sql`
            UPDATE leads SET
              fup_step = ${nextFupStep},
              fup_next_at = NULL,
              fup_failure_count = 0,
              ia_ativada = false,
              fup_enabled = false,
              updated_at = NOW()
            WHERE unique_id = ${lead.uniqueId}
          `;
          this.logger.info(
            { uniqueId: lead.uniqueId, step: nextFupStep },
            "FupScheduler: último FUP enviado — ia_ativada=false, fup_enabled=false (D-10/FUP-05)"
          );
        } else {
          // D-09: avançar step e calcular próximo slot válido (FUP-07)
          const intervalMs = (lead.intervalsSeconds[nextFupStep] ?? lead.intervalsSeconds[lead.intervalsSeconds.length - 1]!) * 1000;
          const rawNextAt = new Date(Date.now() + intervalMs);
          const nextAt = getNextValidSlot(
            rawNextAt,
            lead.minHour,
            lead.maxHour,
            lead.allowedDays,
            lead.timezone
          );

          await this.opts.sql`
            UPDATE leads SET
              fup_step = ${nextFupStep},
              fup_next_at = ${nextAt.toISOString()},
              fup_failure_count = 0,
              updated_at = NOW()
            WHERE unique_id = ${lead.uniqueId}
          `;
        }

        // D-16, EVT-03: publicar evento fire-and-forget
        if (this.opts.eventPublisher) {
          const fupEvent: ToolEvent = {
            event_id: `${lead.uniqueId}:fup:${lead.fupStep}`,  // D-17: idempotente por step
            action: "fup",
            lead: { id: lead.uniqueId, nome: lead.nome ?? null, numero: lead.numero },
            result: JSON.stringify({ step: lead.fupStep, message }),
            timestamp: new Date().toISOString(),
          };
          this.opts.eventPublisher.publish([fupEvent]).catch((err: unknown) => {
            // T-22-03: logar apenas eventId, nunca conteúdo da mensagem (PII)
            this.logger.warn({ err, eventId: fupEvent.event_id }, "FUP EVT-03 publish falhou — ignorando");
          });
        }

        return;  // sucesso — sair do loop de tentativas
      } catch (err) {
        lastErr = err;
        // T-22-03: logar apenas uniqueId, nunca conteúdo da mensagem gerada
        this.logger.warn(
          { err, uniqueId: lead.uniqueId, attempt },
          "FupScheduler: tentativa de FUP falhou"
        );
        // D-15: retry simples sem delay (backoff exponencial é FUP-F01)
      }
    }

    // D-14: falhou MAX_FUP_ATTEMPTS vezes — incrementar fup_failure_count
    const newCount = (lead.fupFailureCount ?? 0) + 1;
    if (newCount >= MAX_FUP_FAILURES) {
      await this.opts.sql`
        UPDATE leads SET
          fup_failure_count = ${newCount},
          fup_enabled = false,
          updated_at = NOW()
        WHERE unique_id = ${lead.uniqueId}
      `;
      // D-14: logger.error quando atinge limite
      this.logger.error(
        { uniqueId: lead.uniqueId, failures: newCount, err: lastErr },
        "FupScheduler: FUP falhou 3 vezes — fup_enabled=false (D-14/FUP-08)"
      );
    } else {
      await this.opts.sql`
        UPDATE leads SET
          fup_failure_count = ${newCount},
          updated_at = NOW()
        WHERE unique_id = ${lead.uniqueId}
      `;
    }
  }

  private async _generateFupMessage(lead: FupLeadRow, fupPrompt: string): Promise<string> {
    // D-12: recuperar histórico via getTuple (sem invocar o grafo completo)
    const tuple = await this.opts.checkpointer.getTuple({
      configurable: { thread_id: lead.uniqueId },
    });
    // Pitfall 3: getTuple pode retornar undefined para leads sem conversa
    const messages: BaseMessage[] =
      (tuple?.checkpoint?.channel_values?.messages as BaseMessage[] | undefined) ?? [];

    // Janela de contexto: últimas 10 mensagens
    const recentMessages = messages.slice(-10);

    const llm = await createLLM();
    const response = await llm.invoke([
      new SystemMessage(fupPrompt),
      ...recentMessages,
      new HumanMessage("Gere uma mensagem de follow-up personalizada para este lead."),
    ]);

    return typeof response.content === "string"
      ? response.content
      : JSON.stringify(response.content);
  }

  private async _sendFupWebhook(lead: FupLeadRow, message: string): Promise<void> {
    // D-01: payload igual ao formato do webhook de entrada (BrainEventSchema)
    // D-03: sem autenticação — endpoint privado do operador
    const payload = {
      Name: lead.nome ?? "",
      Numero: lead.numero,
      Message: message,
      IDLead: lead.uniqueId,
    };
    const response = await fetch(this.opts.fupWebhookUrl, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload),
      signal: AbortSignal.timeout(5000),  // mesmo timeout do EventPublisher (T-20-05)
    });
    if (!response.ok) {
      throw new Error(`FUP webhook retornou ${response.status} para lead ${lead.uniqueId}`);
    }
  }
}

/** Tipo interno — linha retornada pela query de elegibilidade */
interface FupLeadRow {
  id: string;
  uniqueId: string;
  nome: string | null;
  numero: string;
  fupStep: number;
  fupNextAt: Date | null;
  fupFailureCount: number;
  iaAtivada: boolean;
  fupEnabled: boolean;
  intervalsSeconds: number[];
  minHour: number;
  maxHour: number;
  allowedDays: string[];
  timezone: string;
}

/**
 * FUP-07: Calcula o próximo slot de envio válido dado um horário inicial.
 * Avança hora a hora até encontrar slot dentro da janela permitida.
 * Exportada para permitir testes unitários isolados.
 *
 * @param from - Data de início para busca
 * @param minHour - Hora mínima permitida (0-23, inclusivo)
 * @param maxHour - Hora máxima permitida (0-23, exclusivo — ex: 18 = até 17:59)
 * @param allowedDays - Dias permitidos em lowercase inglês: 'mon','tue','wed','thu','fri','sat','sun'
 * @param timezone - IANA timezone string (ex: 'America/Sao_Paulo')
 */
export function getNextValidSlot(
  from: Date,
  minHour: number,
  maxHour: number,
  allowedDays: string[],
  timezone: string
): Date {
  const candidate = new Date(from);

  // Avançar até 14 dias (336 horas) — fallback se config inválida
  for (let i = 0; i < 14 * 24; i++) {
    const parts = new Intl.DateTimeFormat("en-US", {
      timeZone: timezone,
      weekday: "short",
      hour: "numeric",
      hour12: false,
    }).formatToParts(candidate);

    const weekdayVal = parts.find((p) => p.type === "weekday")?.value?.toLowerCase();
    const hourRaw = parts.find((p) => p.type === "hour")?.value ?? "0";
    // Pitfall 2: Intl retorna '24' para meia-noite em alguns locales — normalizar
    const hour = parseInt(hourRaw, 10) % 24;

    if (weekdayVal && allowedDays.includes(weekdayVal) && hour >= minHour && hour < maxHour) {
      return candidate;
    }

    // Avançar 1 hora
    candidate.setTime(candidate.getTime() + 3_600_000);
  }

  // Fallback: config inválida (sem dias permitidos ou janela impossível) — retornar +24h
  return new Date(from.getTime() + 86_400_000);
}
```

**Notas de implementação:**
- Criar o diretório `packages/core/src/fup/` antes de criar o arquivo
- Não invocar o grafo LangGraph completo — usar `llm.invoke()` direto (anti-pattern documentado na research)
- `_tick()` e `_processFupForLead()` e `_generateFupMessage()` têm prefixo `_` mas são métodos de instância normais (sem `#private`) para permitir spy em testes
- `getNextValidSlot` exportada no nível de módulo para teste unitário isolado
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/fup/ --bail 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `packages/core/src/fup/fup-scheduler.ts` existe e contém `export class FupScheduler`
    - `packages/core/src/fup/fup-scheduler.ts` contém `export interface IFupScheduler`
    - `packages/core/src/fup/fup-scheduler.ts` contém `export function getNextValidSlot`
    - `grep "FOR UPDATE OF l SKIP LOCKED" packages/core/src/fup/fup-scheduler.ts` retorna resultado
    - `grep "clearInterval" packages/core/src/fup/fup-scheduler.ts` retorna resultado
    - `grep "AbortSignal.timeout" packages/core/src/fup/fup-scheduler.ts` retorna resultado
    - `bun test packages/core/src/__tests__/unit/fup/ --bail` passa (0 falhas)
    - `bun run tsc --noEmit` em packages/core passa sem erros de tipo
  </acceptance_criteria>

  <done>
    FupScheduler implementado com todos os comportamentos de negócio (FUP-01 a FUP-08, EVT-03). Testes unitários passando com mocks de sql, checkpointer, llm e fetch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar testes unitários do FupScheduler e getNextValidSlot</name>
  <files>
    packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts
    packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts
  </files>

  <read_first>
    - packages/core/src/fup/fup-scheduler.ts — interfaces FupSchedulerOptions, IFupScheduler, assinatura de getNextValidSlot (criado na Task 1)
    - packages/core/src/__tests__/unit/mcp-init.test.ts — padrão de mocks bun test (mock(), spyOn()) já usado no projeto
    - .planning/phases/22-fup-autom-tico/22-VALIDATION.md — lista de comportamentos a cobrir por arquivo
  </read_first>

  <behavior>
    (mesmos 10 comportamentos listados na Task 1 — os testes devem cobri-los)
  </behavior>

  <action>
Criar o diretório `packages/core/src/__tests__/unit/fup/` e os dois arquivos de teste.

**`fup-scheduler.test.ts`** — testes das regras de negócio do FupScheduler usando mocks:

Estrutura de mocks necessária:
- `sql` mock: função tagged template que retorna array de leads ou array de prompts conforme chamada
- `checkpointer` mock: `{ getTuple: mock(() => Promise.resolve(undefined)) }` — lead sem histórico
- `eventPublisher` mock: `{ publish: mock(() => Promise.resolve()), close: mock() }`
- `fetch` global mock via `mock.module("node:fetch")` ou `globalThis.fetch = mock(...)`

Cenários a implementar em `fup-scheduler.test.ts`:

```typescript
import { describe, test, expect, mock, beforeEach, afterEach } from "bun:test";
import { FupScheduler } from "../../../../fup/fup-scheduler.js";

// Helper para construir lead mock completo
function makeLead(overrides: Partial<FupLeadRow> = {}): FupLeadRow { ... }

describe("FupScheduler", () => {
  describe("_tick()", () => {
    test("quando sql retorna leads, chama _processFupForLead para cada um");
    test("quando sql retorna vazio, não chama _processFupForLead");
  });

  describe("_processFupForLead()", () => {
    test("D-13: quando prompt key='fup' não existe, loga warn e retorna sem processar");
    test("FUP-05/D-10: último FUP seta ia_ativada=false e fup_enabled=false no UPDATE");
    test("FUP-08/D-14: após 3 falhas, fup_enabled=false e logger.error chamado");
    test("FUP-08: fup_failure_count resetado para 0 após sucesso com falhas anteriores");
    test("EVT-03/D-16: após envio bem-sucedido, eventPublisher.publish chamado com action='fup'");
    test("EVT-03/D-17: event_id = `${lead.uniqueId}:fup:${fupStep}`");
    test("D-18: quando eventPublisher é null, publicação pulada silenciosamente");
  });

  describe("stop()", () => {
    test("clearInterval chamado; segundo stop() é noop sem erro");
  });
});
```

**`fup-business-hours.test.ts`** — testes puros de getNextValidSlot (sem dependência de banco):

```typescript
import { describe, test, expect } from "bun:test";
import { getNextValidSlot } from "../../../../fup/fup-scheduler.js";

describe("getNextValidSlot", () => {
  const weekdays = ["mon", "tue", "wed", "thu", "fri"];

  test("slot já válido (terça 10h em SP) retorna o mesmo momento");
  // new Date("2025-01-07T13:00:00Z") = terça-feira 10h em America/Sao_Paulo (-3)
  // minHour=9, maxHour=18, allowedDays=weekdays → retorna o próprio Date (sem avançar)

  test("slot num sábado avança para próxima segunda >=minHour");
  // new Date("2025-01-11T15:00:00Z") = sábado 12h em SP
  // allowedDays=weekdays → avança para segunda 9h (próximo slot válido)

  test("slot numa segunda antes do horário comercial avança para minHour");
  // new Date("2025-01-06T09:00:00Z") = segunda 06h em SP
  // minHour=9 → avança até às 9h da mesma segunda

  test("slot numa segunda após horário comercial avança para terça minHour");
  // new Date("2025-01-06T22:00:00Z") = segunda 19h em SP
  // maxHour=18 → avança para terça 9h

  test("hora '24' normalizada para 0 (pitfall Intl)");
  // Verificar que parseInt('24', 10) % 24 === 0 (comportamento correto do normalizador)
  // Testar indiretamente via slot exatamente à meia-noite em timezone que pode retornar '24'

  test("fallback: retorna +24h quando allowedDays vazio (config inválida)");
  // allowedDays=[] → loop nunca encontra slot → retorna from + 86400000ms
});
```

**Convenções de teste a seguir (do projeto):**
- Framework: `bun test` com API Jest-compatible
- Import das funções a testar com extensão `.js` (mesmo sendo `.ts`) — padrão ESM do projeto
- `mock()` da bun:test para criar spies
- Arquivos de teste em `__tests__/unit/fup/` com sufixo `.test.ts`
  </action>

  <verify>
    <automated>bun test packages/core/src/__tests__/unit/fup/ --bail 2>&1 | grep -E "(PASS|FAIL|tests? passed|error)"</automated>
  </verify>

  <acceptance_criteria>
    - `packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts` existe
    - `packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts` existe
    - `bun test packages/core/src/__tests__/unit/fup/` passa com 0 falhas
    - `grep -c "test(" packages/core/src/__tests__/unit/fup/fup-scheduler.test.ts` retorna >= 7
    - `grep -c "test(" packages/core/src/__tests__/unit/fup/fup-business-hours.test.ts` retorna >= 5
    - Nenhum arquivo `*.test.ts` fora de `__tests__/` (convenção do projeto)
  </acceptance_criteria>

  <done>
    Testes unitários implementados e passando para FupScheduler e getNextValidSlot. Cobertura: FUP-01, FUP-02, FUP-03, FUP-05, FUP-07, FUP-08, EVT-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FupScheduler → FUP_WEBHOOK_URL | Dado de saída (mensagem gerada por LLM) enviado para endpoint externo |
| LLM → FupScheduler | Conteúdo gerado pelo LLM incluindo dados do histórico do lead |
| checkpointer.getTuple → FupScheduler | Histórico de mensagens do lead (PII: nome, número, conversa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | fup-scheduler.ts logger | mitigate | Logar apenas `uniqueId` e `fupStep` em warn/error — NUNCA o conteúdo da mensagem gerada (PII); padrão T-20-02 do EventPublisher replicado |
| T-22-04 | Information Disclosure | fup-scheduler.ts (fupWebhookUrl em log) | mitigate | Logar apenas `hasFupUrl: true` no init — nunca a URL completa (pode conter tokens/credenciais no path); padrão T-20-03 do EventPublisher |
| T-22-05 | Tampering | FUP_WEBHOOK_URL destino | accept | Endpoint é privado/interno do operador (D-03); sem autenticação por design (decisão do usuário); risco residual: URL controlada por atacante recebe mensagens — mitigado pelo fato de que FUP_WEBHOOK_URL deve ser configurada pelo operador em ENV seguro |
| T-22-06 | Elevation of Privilege | LLM prompt injection via histórico do lead | mitigate | Conteúdo do lead não é interpolado diretamente no system prompt; histórico é passado como `BaseMessage[]` via `llm.invoke()` (não como string no system prompt); LangChain trata cada mensagem com tipo próprio; não há execução de código a partir do output do LLM |
| T-22-07 | Denial of Service | SELECT FOR UPDATE sem SKIP LOCKED | mitigate | Sempre usar `FOR UPDATE OF l SKIP LOCKED` (não apenas `FOR UPDATE`); instâncias que não obtêm lock pulam silenciosamente; transações curtas evitam travamento prolongado do pool |
| T-22-08 | Denial of Service | fup_failure_count sem cap | mitigate | Após `fup_failure_count >= MAX_FUP_FAILURES (3)`: `fup_enabled = false` — lead sai do ciclo; retry é limitado a 3 por tick (MAX_FUP_ATTEMPTS); sem possibilidade de loop infinito de envio |
</threat_model>

<verification>
```bash
# 1. Arquivo principal existe com exports corretos
grep -E "export (class FupScheduler|interface IFupScheduler|function getNextValidSlot)" \
  packages/core/src/fup/fup-scheduler.ts

# 2. Proteção de concorrência presente
grep "FOR UPDATE OF l SKIP LOCKED" packages/core/src/fup/fup-scheduler.ts

# 3. Testes unitários passam
bun test packages/core/src/__tests__/unit/fup/ --bail

# 4. TypeScript compila
cd packages/core && bun run tsc --noEmit 2>&1 | head -10
```
</verification>

<success_criteria>
- `fup-scheduler.ts` exporta FupScheduler, IFupScheduler, getNextValidSlot
- SELECT FOR UPDATE OF l SKIP LOCKED presente no _tick()
- Todos os testes unitários de fup/ passam
- Sem erros TypeScript em packages/core
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-fup-autom-tico/22-02-SUMMARY.md` com:
- Implementação de FupScheduler (arquivos, exports, behaviors)
- Testes criados e comportamentos cobertos
- Decisões D-01 a D-18 implementadas
- Pitfalls evitados (SKIP LOCKED, retry persistente, etc.)
</output>
