---
phase: 22-fup-autom-tico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/migrations/0008_fup_failure_count.sql
  - packages/database/src/schema/tables.ts
  - packages/database/src/migrations/meta/_journal.json
autonomous: true
requirements:
  - FUP-08
must_haves:
  truths:
    - "A coluna fup_failure_count existe na tabela leads com DEFAULT 0 NOT NULL"
    - "O schema Drizzle reflete a nova coluna com tipo integer"
    - "O journal de migrations registra a entrada idx=8"
  artifacts:
    - path: "packages/database/src/migrations/0008_fup_failure_count.sql"
      provides: "Migration DDL para fup_failure_count"
      contains: "ALTER TABLE"
    - path: "packages/database/src/schema/tables.ts"
      provides: "Schema Drizzle atualizado com fupFailureCount"
      contains: "fupFailureCount"
  key_links:
    - from: "packages/database/src/schema/tables.ts"
      to: "packages/database/src/migrations/0008_fup_failure_count.sql"
      via: "fupFailureCount integer → fup_failure_count integer"
      pattern: "fupFailureCount"
---

<objective>
Adicionar a coluna `fup_failure_count` à tabela `leads` — a base de resiliência para FUP-08 (retry com contador persistente no banco).

Purpose: O FupScheduler precisa de um contador de falhas por lead que sobreviva a restarts. Esta coluna é o único dado novo de schema desta fase — todo o resto foi criado na migration 0007 (Phase 19). Sem esta coluna o `FupScheduler` não pode implementar FUP-08.

Output:
- `0008_fup_failure_count.sql` — migration DDL
- `tables.ts` atualizado com `fupFailureCount` no schema Drizzle de `leads`
- `_journal.json` com entrada idx=8
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-fup-autom-tico/22-CONTEXT.md

@packages/database/src/schema/tables.ts
@packages/database/src/migrations/meta/_journal.json
@packages/database/src/migrations/0007_v1_4_foundation.sql
</context>

<interfaces>
<!-- Campos atuais da tabela leads (extraídos de tables.ts) relevantes para esta task -->
<!-- Adicionar fupFailureCount logo após fupNextAt -->

```typescript
// Estado atual da tabela leads (campos FUP já existentes):
fupEnabled: boolean('fup_enabled').notNull().default(false),
fupStep: integer('fup_step').notNull().default(0),
fupNextAt: timestamp('fup_next_at', { withTimezone: true }),
lastMessageAt: timestamp('last_message_at', { withTimezone: true }),

// NOVO campo a adicionar (D-14 do CONTEXT.md):
fupFailureCount: integer('fup_failure_count').notNull().default(0),
```

<!-- Formato de entrada no _journal.json (idx=8): -->
```json
{
  "idx": 8,
  "version": "7",
  "when": 1750100000000,
  "tag": "0008_fup_failure_count",
  "breakpoints": true
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar migration 0008 e atualizar schema Drizzle</name>
  <files>
    packages/database/src/migrations/0008_fup_failure_count.sql
    packages/database/src/schema/tables.ts
    packages/database/src/migrations/meta/_journal.json
  </files>

  <read_first>
    - packages/database/src/schema/tables.ts — ver posição exata onde inserir fupFailureCount (após fupNextAt e lastMessageAt)
    - packages/database/src/migrations/meta/_journal.json — ver estrutura e último idx (idx=7) para adicionar idx=8
    - packages/database/src/migrations/0007_v1_4_foundation.sql — confirmar que fup_failure_count não existe (ausência confirmada pela research)
  </read_first>

  <action>
**1. Criar `packages/database/src/migrations/0008_fup_failure_count.sql`:**

```sql
-- FUP-08: Adiciona contador de falhas de FUP por lead.
-- Persistente no banco para sobreviver a restarts do scheduler.
-- DEFAULT 0 NOT NULL — leads existentes herdam zero falhas sem necessidade de backfill.
ALTER TABLE "leads" ADD COLUMN "fup_failure_count" integer DEFAULT 0 NOT NULL;
```

**2. Atualizar `packages/database/src/schema/tables.ts`:**

Na definição da tabela `leads`, adicionar `fupFailureCount` **após** `lastMessageAt` e **antes** do fechamento do objeto de colunas `}, (table)`:

```typescript
  lastMessageAt: timestamp('last_message_at', { withTimezone: true }), // nullable por design (D-11)
  // FUP-08: Contador de falhas de envio de FUP — persistente no banco (D-14)
  // Incrementado a cada falha de LLM ou transport. Reset a cada FUP bem-sucedido.
  // Quando >= 3 (MAX_FUP_FAILURES): fup_enabled setado para false automaticamente.
  fupFailureCount: integer('fup_failure_count').notNull().default(0),
```

Adicionar também o import de `integer` se não estiver presente — verificar primeira linha do arquivo. Já está presente (`integer` importado na linha 1).

**3. Atualizar `packages/database/src/migrations/meta/_journal.json`:**

Adicionar nova entrada ao array `entries` após o idx=7:

```json
{
  "idx": 8,
  "version": "7",
  "when": 1750100000000,
  "tag": "0008_fup_failure_count",
  "breakpoints": true
}
```

**Atenção:** O campo `when` é timestamp em milliseconds. Usar `1750100000000` (valor fixo — não usar Date.now() para garantir reprodutibilidade).
  </action>

  <verify>
    <automated>grep -n "fup_failure_count" /root/Brain/packages/database/src/migrations/0008_fup_failure_count.sql && grep -n "fupFailureCount" /root/Brain/packages/database/src/schema/tables.ts && grep -n "0008_fup_failure_count" /root/Brain/packages/database/src/migrations/meta/_journal.json</automated>
  </verify>

  <acceptance_criteria>
    - `0008_fup_failure_count.sql` contém exatamente: `ALTER TABLE "leads" ADD COLUMN "fup_failure_count" integer DEFAULT 0 NOT NULL;`
    - `tables.ts` contém `fupFailureCount: integer('fup_failure_count').notNull().default(0),` dentro da definição da tabela `leads`
    - `_journal.json` contém entrada com `"idx": 8` e `"tag": "0008_fup_failure_count"`
    - `grep -c "fupFailureCount" packages/database/src/schema/tables.ts` retorna `1`
    - `bun run tsc --noEmit` em packages/database passa sem erros de tipo
  </acceptance_criteria>

  <done>
    Migration SQL criada, campo Drizzle adicionado ao schema de leads, journal atualizado com idx=8. O FupScheduler pode agora referenciar `lead.fupFailureCount` via Drizzle e usar `fup_failure_count` em queries raw SQL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → PostgreSQL | DDL executada com privilégios de aplicação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | 0008_fup_failure_count.sql | accept | Migration é DDL pura (ADD COLUMN com DEFAULT) — sem dados externos; sem risco de injeção SQL; executada apenas pelo migrate.ts com advisory lock (Phase 6 pattern) |
| T-22-02 | Information Disclosure | tables.ts schema | accept | Schema não expõe dados de runtime; tipagem TypeScript interna; sem risco de disclosure |
</threat_model>

<verification>
```bash
# 1. Arquivo de migration existe e contém o DDL correto
cat packages/database/src/migrations/0008_fup_failure_count.sql

# 2. Schema Drizzle contém o novo campo
grep "fupFailureCount" packages/database/src/schema/tables.ts

# 3. Journal contém entrada idx=8
grep "0008_fup_failure_count" packages/database/src/migrations/meta/_journal.json

# 4. TypeScript compila sem erros
cd packages/database && bun run tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- `0008_fup_failure_count.sql` existe com DDL correto
- `tables.ts` exporta `leads` com campo `fupFailureCount`
- `_journal.json` tem entrada `idx: 8` com tag `0008_fup_failure_count`
- Sem erros de TypeScript em packages/database
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/22-fup-autom-tico/22-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Campos adicionados ao schema
- Decisão D-14 implementada
</output>
