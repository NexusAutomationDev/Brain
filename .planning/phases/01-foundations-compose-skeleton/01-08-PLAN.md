---
phase: 01-foundations-compose-skeleton
plan: 08
type: execute
wave: 3
depends_on: [01-01, 01-03, 01-06, 01-07]
files_modified:
  - docker-compose.yml
  - docker-compose.lite.yml
  - compose/brain-topology-init/README.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-04, DEPLOY-05, DEPLOY-06]
tags: [docker-compose, langfuse, healthchecks, init-container, dual-postgres, networking]

must_haves:
  truths:
    - "`docker compose -f docker-compose.yml config` validates without error (no syntax / interpolation errors)"
    - "`docker compose -f docker-compose.lite.yml config` validates without error"
    - "Full stack declares 12 service entries: 10 long-running services (brain, brain-postgres, rabbitmq, qdrant, langfuse-web, langfuse-worker, langfuse-postgres, clickhouse, redis, minio) + 2 init containers (brain-migrate, brain-topology-init). DEPLOY-01 counts the 10 long-running services."
    - "Lite stack declares exactly 5 long-running services + 1 init container: brain, brain-migrate, brain-postgres, rabbitmq, qdrant, brain-topology-init (placeholder)"
    - "Every long-running service declares a `healthcheck:` block (D-11, DEPLOY-04)"
    - "Brain `depends_on` includes ONLY: brain-migrate (service_completed_successfully), brain-postgres (service_healthy), rabbitmq (service_healthy), qdrant (service_healthy); Langfuse is NOT in Brain's depends_on (PITFALL 8.1 / D-11)"
    - "MinIO image is pinned to `minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1` (last pre-archive community release per Pitfall 8 / A3)"
    - "Host ports exposed (D-12): only `8000` (brain), `3000` (langfuse-web — full only), `15672` (rabbitmq mgmt), `6333` (qdrant); Postgres `5432`, AMQP `5672`, ClickHouse, Redis, MinIO are internal-only on `brain-net`"
    - "Two Postgres instances (PITFALL 6.2): `brain-postgres` (Brain data) + `langfuse-postgres` (Langfuse data) — never shared"
    - "`brain-topology-init` placeholder service declared in BOTH compose files (DEPLOY-06; Phase 8 wires real topology)"
    - "`docker-compose.lite.yml` is a strict subset of full for shared services (parity verified by plan 01-09 `scripts/check-compose-parity.sh`)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full 10-service stack: Brain + brain-migrate + brain-postgres + RabbitMQ + Qdrant + Langfuse subsystem (5 services) + brain-topology-init placeholder"
      min_lines: 250
      contains: "langfuse-web"
    - path: "docker-compose.lite.yml"
      provides: "Inner-loop subset: Brain + brain-migrate + brain-postgres + RabbitMQ + Qdrant + brain-topology-init placeholder (no Langfuse subsystem; D-10 chose separate-file per RESEARCH.md Summary)"
      min_lines: 90
      contains: "brain-postgres"
    - path: "compose/brain-topology-init/README.md"
      provides: "Doc explaining the topology init container is a Phase-1 placeholder; Phase 8 fills it (DEPLOY-06 partial)"
  key_links:
    - from: "docker-compose.yml brain service"
      to: "docker/Dockerfile target prod"
      via: "build: { context: ., dockerfile: docker/Dockerfile, target: prod }"
      pattern: "target: prod"
    - from: "docker-compose.yml brain-migrate service"
      to: "src/brain/db/migrate.py"
      via: "command: ['python', '-m', 'brain.db.migrate']"
      pattern: "brain\\.db\\.migrate"
    - from: "docker-compose.yml brain service depends_on"
      to: "brain-migrate (service_completed_successfully) + brain-postgres + rabbitmq + qdrant (all service_healthy)"
      via: "depends_on with condition"
      pattern: "service_completed_successfully"
    - from: "docker-compose.yml langfuse-web env"
      to: ".env.example LANGFUSE_* secrets"
      via: "env interpolation ${LANGFUSE_NEXTAUTH_SECRET} etc."
      pattern: "LANGFUSE_NEXTAUTH_SECRET"
---

<objective>
Author both Docker Compose files. The full stack (`docker-compose.yml`) brings up the 10 long-running services plus 2 init containers (`brain-migrate` and the `brain-topology-init` placeholder for DEPLOY-06) with explicit healthchecks on every long-running service. The lite stack (`docker-compose.lite.yml`) is a strict-subset file (D-10 — RESEARCH.md recommended separate file over compose profiles because DEPLOY-02 names the file explicitly). Brain depends only on the core 4 deps (PITFALL 8.1 — Langfuse out); ports are host-restricted per D-12; two Postgres instances per PITFALL 6.2; MinIO is pinned to the last community release per Pitfall 8 / Assumption A3.

Purpose: DEPLOY-01 (full stack), DEPLOY-02 (lite stack), DEPLOY-04 (healthchecks everywhere), DEPLOY-05 (brain-migrate runs Alembic before Brain), DEPLOY-06 (brain-topology-init slot reserved).
Output: Both compose files validate via `docker compose config`; smoke (`scripts/smoke-up.sh lite` in plan 01-09) reaches `service_healthy` deterministically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md
@.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md
@.planning/research/PITFALLS.md
@.env.example
@docker/Dockerfile
@src/brain/db/migrate.py
</context>

<interfaces>
<!-- Compose contract that plan 01-09 smoke-up.sh consumes -->

Full stack `docker compose up -d` reaches healthy with:
  brain-postgres healthy → brain-migrate completes successfully → brain healthy
  rabbitmq healthy → brain healthy
  qdrant healthy → brain healthy
  (langfuse-* + langfuse-postgres + clickhouse + redis + minio start in parallel; brain does NOT wait on them)

Lite stack `docker compose -f docker-compose.lite.yml up -d` reaches healthy with the same core dependency graph; no Langfuse subsystem.

Host ports (D-12):
  - brain: 8000 (both stacks)
  - langfuse-web: 3000 (full only)
  - rabbitmq management UI: 15672 (both)
  - qdrant dashboard: 6333 (both)
  - postgres 5432, amqp 5672, clickhouse, redis, minio: NO host port (internal only)

Required env vars (consumed from `.env` via `env_file:`):
  Brain: BRAIN_AUTH__TOKEN, BRAIN_POSTGRES__DSN, BRAIN_RABBITMQ__URL, BRAIN_QDRANT__URL, BRAIN_LANGFUSE__*, BRAIN_SHUTDOWN__GRACE_SECONDS, BRAIN_SUPPORTED_SCHEMA_VERSIONS, BRAIN_LOG_*
  Langfuse subsystem (full only): LANGFUSE_NEXTAUTH_SECRET, LANGFUSE_SALT, LANGFUSE_ENCRYPTION_KEY, LANGFUSE_POSTGRES_PASSWORD, LANGFUSE_CLICKHOUSE_PASSWORD, LANGFUSE_MINIO_ROOT_USER, LANGFUSE_MINIO_ROOT_PASSWORD, LANGFUSE_REDIS_AUTH
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docker-compose.yml (full stack — 10 services + 2 init containers)</name>
  <read_first>
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md (§ "Code Examples" Example 8 lines 1135-1357 — full compose skeleton; § "Pitfall 8" lines 778-789 — MinIO pin; § "Pitfall 3" lines 728-737 — Langfuse out of depends_on)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md (D-09, D-10, D-11, D-12)
    - /root/Brain/.env.example (every interpolated var must exist there)
    - /root/Brain/docker/Dockerfile (target prod is what compose builds)
    - https://docs.docker.com/compose/how-tos/startup-order/ (service_healthy + service_completed_successfully semantics)
  </read_first>
  <action>
Create `/root/Brain/docker-compose.yml` — base it on RESEARCH.md Example 8 (lines 1136-1357), with these mandatory invariants:

1. Top-level structure: `networks: { brain-net: { driver: bridge } }`; `volumes:` enumerating named volumes for each stateful service (`brain-pg-data`, `rmq-data`, `qdrant-data`, `lf-pg-data`, `ch-data`, `redis-data`, `minio-data`); `services:` declaring all 12 service entries.

2. **Services declared (12 total)** — in this order for readability:
   - **Core path (always required by Brain):**
     - `brain-postgres`: `image: postgres:17-trixie`; env `POSTGRES_USER=brain POSTGRES_PASSWORD=brain POSTGRES_DB=brain`; volume `brain-pg-data:/var/lib/postgresql/data`; healthcheck `pg_isready -U brain -d brain` (interval 5s, timeout 5s, retries 10, start_period 10s); networks `[brain-net]`; NO host port (internal-only per D-12).
     - `rabbitmq`: `image: rabbitmq:4.1-management-alpine`; default user (`guest`/`guest`) but document override in `.env.example` for future hardening (current `.env.example` uses `brain:brain` in `BRAIN_RABBITMQ__URL` — set the env here: `RABBITMQ_DEFAULT_USER=brain`, `RABBITMQ_DEFAULT_PASS=brain` for parity with the URL); `ports: ["127.0.0.1:15672:15672"]` (management UI, host-bound); NO host port for AMQP 5672 (internal-only); volume `rmq-data:/var/lib/rabbitmq`; healthcheck `["CMD", "rabbitmq-diagnostics", "-q", "ping"]` (interval 10s, timeout 10s, retries 6, start_period 30s); networks `[brain-net]`.
     - `qdrant`: `image: qdrant/qdrant:v1.18.0`; `ports: ["127.0.0.1:6333:6333"]`; volume `qdrant-data:/qdrant/storage`; healthcheck `["CMD-SHELL", "wget -qO- localhost:6333/healthz || exit 1"]` (interval 5s, timeout 5s, retries 10, start_period 10s); networks `[brain-net]`.
     - `brain-migrate`: `build: { context: ., dockerfile: docker/Dockerfile, target: prod }`; `env_file: .env`; `command: ["python", "-m", "brain.db.migrate"]`; `depends_on: { brain-postgres: { condition: service_healthy } }`; `networks: [brain-net]`; `restart: "no"` (D-07: one-shot init container).
     - `brain-topology-init` (PLACEHOLDER for DEPLOY-06; Phase 8 fills): `image: alpine:3.20`; `command: ["true"]`; `restart: "no"`; networks `[brain-net]`; add a top-of-block comment `# Phase 1 placeholder for DEPLOY-06. Phase 8 replaces image+command with: aio-pika topology declarer for brain.in / brain.dlx / brain.dlq. Do not delete this entry — Brain's depends_on (Phase 8) will key off it.`. Pin alpine to `3.20` not `latest` (CLAUDE.md §10).
     - `brain`: `build: { context: ., dockerfile: docker/Dockerfile, target: prod }`; `env_file: .env`; `ports: ["8000:8000"]`; `depends_on: { brain-migrate: { condition: service_completed_successfully }, brain-postgres: { condition: service_healthy }, rabbitmq: { condition: service_healthy }, qdrant: { condition: service_healthy } }`; `networks: [brain-net]`. ABOVE this block, add a comment: `# Brain MUST NOT depends_on any langfuse-* service. Observability cannot block the request path (PITFALL 8.1 / D-11 / OBS-04).`

   - **Langfuse subsystem (5 services — D-09):**
     - `langfuse-postgres`: `image: postgres:17-trixie`; env `POSTGRES_USER=langfuse POSTGRES_PASSWORD=${LANGFUSE_POSTGRES_PASSWORD} POSTGRES_DB=langfuse`; volume `lf-pg-data:/var/lib/postgresql/data`; healthcheck `pg_isready -U langfuse`; networks `[brain-net]`; NO host port.
     - `clickhouse`: `image: clickhouse/clickhouse-server:24.8-alpine` (A5 — confirm 24.8 still on Docker Hub at execute time; if not, fall back to `24.3-alpine`); env `CLICKHOUSE_DB=default CLICKHOUSE_USER=clickhouse CLICKHOUSE_PASSWORD=${LANGFUSE_CLICKHOUSE_PASSWORD}`; volume `ch-data:/var/lib/clickhouse`; healthcheck `["CMD-SHELL", "wget -qO- localhost:8123/ping || exit 1"]` with `start_period: 60s` (ClickHouse first boot is slow); networks `[brain-net]`.
     - `redis`: `image: redis:7-alpine`; `command: ["redis-server", "--requirepass", "${LANGFUSE_REDIS_AUTH}"]`; volume `redis-data:/data`; healthcheck `["CMD-SHELL", "redis-cli -a ${LANGFUSE_REDIS_AUTH} ping"]`; networks `[brain-net]`.
     - `minio`: `image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1` (Pitfall 8 / A3 — last pre-archive community release; ABOVE this block add comment: `# MinIO upstream archived community images early 2026; this pin is the last stable release. See .planning/research/PITFALLS.md / RESEARCH.md Open Question #2 for migration plan (Garage / SeaweedFS / RustFS).`); `command: ["server", "--address", ":9000", "--console-address", ":9001", "/data"]`; env `MINIO_ROOT_USER=${LANGFUSE_MINIO_ROOT_USER} MINIO_ROOT_PASSWORD=${LANGFUSE_MINIO_ROOT_PASSWORD}`; volume `minio-data:/data`; healthcheck `["CMD-SHELL", "mc ready local || exit 1"]`; networks `[brain-net]`. NO host port for MinIO (internal-only).
     - `langfuse-web`: `image: langfuse/langfuse:3.175.0`; `ports: ["3000:3000"]`; env block per RESEARCH.md Example 8 lines 1280-1307 (all `NEXTAUTH_SECRET`, `SALT`, `ENCRYPTION_KEY`, `DATABASE_URL`, `CLICKHOUSE_URL`, `CLICKHOUSE_MIGRATION_URL`, `CLICKHOUSE_USER`, `CLICKHOUSE_PASSWORD`, `REDIS_HOST`, `REDIS_PORT`, `REDIS_AUTH`, all 8 `LANGFUSE_S3_*_*` keys, `NEXTAUTH_URL=http://localhost:3000`, `TELEMETRY_ENABLED=false`); `depends_on: { langfuse-postgres: { condition: service_healthy }, clickhouse: { condition: service_healthy }, redis: { condition: service_healthy }, minio: { condition: service_healthy } }`; healthcheck `["CMD-SHELL", "wget -qO- http://localhost:3000/api/public/health || exit 1"]` (A2 — upstream image ships no healthcheck; add ours) with `start_period: 60s`; networks `[brain-net]`.
     - `langfuse-worker`: `image: langfuse/langfuse-worker:3.175.0`; env block per RESEARCH.md Example 8 lines 1324-1349 (same shared block minus the `NEXTAUTH_*` web-only keys); same `depends_on` as langfuse-web (postgres + clickhouse + redis + minio); networks `[brain-net]`; no healthcheck (per A2 — worker has no liveness endpoint; depends_on chain ensures coherent boot).

3. **Top-level comments** at top of file:
```
# docker-compose.yml — Brain full stack (10 services + 2 init containers).
# Lite override: docker-compose.lite.yml (no Langfuse subsystem).
# See .planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md Example 8.
#
# Brain MUST NOT depends_on langfuse-* (PITFALL 8.1 / D-11 / OBS-04).
# Two Postgres instances per PITFALL 6.2 (never share Brain + Langfuse).
# MinIO pinned to last pre-archive release (Pitfall 8 / Open Question #2).
```

4. Verify by running `docker compose -f docker-compose.yml config` → must exit 0 and emit fully-interpolated YAML. (Requires `.env` to exist; copy `.env.example` to `.env` for the verify step; do NOT commit `.env`.)

5. Verify every long-running service has a `healthcheck:` block. Use `docker compose config --format json | jq` if `jq` is available; otherwise grep the rendered output for `healthcheck:` count matching the expected long-running service count (10).

6. Verify Brain's `depends_on` contains exactly: `brain-migrate`, `brain-postgres`, `rabbitmq`, `qdrant`. It does NOT contain any `langfuse-*`. Grep test: `grep -A 6 'service: brain$' docker-compose.yml | grep -i langfuse` must return empty (or use yq/jq).

NOTE on Compose schema `version:` field: omit it. The `version:` top-level key is ignored by Compose v2 and emits a warning in recent versions. Keep the file Compose-v2 native.
  </action>
  <verify>
    <automated>cd /root/Brain && test -f .env || cp .env.example .env && docker compose -f docker-compose.yml config >/tmp/compose-full.yml 2>&1 && grep -c 'healthcheck:' /tmp/compose-full.yml | (read n; test "$n" -ge 10) && grep -F 'image: postgres:17-trixie' docker-compose.yml | wc -l | grep -q "^2$" && grep -F 'minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1' docker-compose.yml && grep -F 'langfuse/langfuse:3.175.0' docker-compose.yml && grep -F 'langfuse/langfuse-worker:3.175.0' docker-compose.yml && grep -F 'qdrant/qdrant:v1.18.0' docker-compose.yml && grep -F 'rabbitmq:4.1-management-alpine' docker-compose.yml && grep -F 'brain-topology-init' docker-compose.yml && ! grep -F ':latest' docker-compose.yml && ! grep -E '^\s*version:\s*' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at repo root
    - `docker compose -f docker-compose.yml config` exits 0 (after `cp .env.example .env`)
    - 12 service entries declared: brain, brain-migrate, brain-topology-init, brain-postgres, rabbitmq, qdrant, langfuse-postgres, clickhouse, redis, minio, langfuse-web, langfuse-worker
    - 10 long-running services each declare a `healthcheck:` block (brain-migrate + brain-topology-init are init containers, no healthcheck required)
    - 2 instances of `image: postgres:17-trixie` (brain-postgres + langfuse-postgres — PITFALL 6.2)
    - MinIO pinned to `minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1` (or whichever pre-archive tag exists at execute time per A3; if A3 fallback used, document in SUMMARY)
    - Langfuse server + worker pinned to `langfuse/langfuse:3.175.0` / `langfuse/langfuse-worker:3.175.0`
    - Qdrant pinned to `qdrant/qdrant:v1.18.0`
    - RabbitMQ pinned to `rabbitmq:4.1-management-alpine`
    - ClickHouse pinned to `clickhouse/clickhouse-server:24.8-alpine` (or `24.3-alpine` fallback per A5)
    - Redis pinned to `redis:7-alpine`
    - alpine placeholder for brain-topology-init pinned to `alpine:3.20` (NOT `:latest`)
    - NO `:latest` tags anywhere
    - Brain `depends_on` includes brain-migrate (service_completed_successfully), brain-postgres (service_healthy), rabbitmq (service_healthy), qdrant (service_healthy); does NOT include any `langfuse-*` (PITFALL 8.1)
    - langfuse-web `depends_on` includes langfuse-postgres, clickhouse, redis, minio (all service_healthy)
    - Host port exposure (D-12): `8000:8000` (brain), `3000:3000` (langfuse-web), `127.0.0.1:15672:15672` (rabbitmq), `127.0.0.1:6333:6333` (qdrant); NO host ports for postgres-5432 / amqp-5672 / clickhouse / redis / minio
    - User-defined network `brain-net` declared; every service is on it
    - Top-level `version:` field is OMITTED
    - Comments explain (a) brain MUST NOT depends_on langfuse, (b) MinIO archive situation, (c) topology-init placeholder
  </acceptance_criteria>
  <done>
    Full stack compose validates and matches every D-09 / D-11 / D-12 invariant; healthchecks declared on every long-running service; MinIO pinned to last community release with documented migration plan; Brain protected from Langfuse outages (PITFALL 8.1). DEPLOY-01 + DEPLOY-04 + DEPLOY-05 + DEPLOY-06 (placeholder) — green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author docker-compose.lite.yml (strict subset; no Langfuse) + brain-topology-init README</name>
  <read_first>
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md (§ "Code Examples" Example 9 lines 1365-1436 — lite skeleton)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md (D-02 wording mandates the literal filename `docker-compose.lite.yml`)
    - /root/Brain/docker-compose.yml (from Task 1 — lite file must be a strict subset for the shared services)
  </read_first>
  <action>
**(a) Create `/root/Brain/docker-compose.lite.yml`** based on RESEARCH.md Example 9 (lines 1367-1436). MUST be a STRICT subset of `docker-compose.yml` for the shared services (`brain`, `brain-migrate`, `brain-postgres`, `rabbitmq`, `qdrant`, `brain-topology-init`) — image tags, env, healthchecks, ports must match exactly. Plan 01-09 will ship `scripts/check-compose-parity.sh` to enforce this.

Header comment:
```
# docker-compose.lite.yml — inner-loop dev subset (D-02, D-10).
# No Langfuse subsystem. Brain runs with BRAIN_LANGFUSE__ENABLED=false so no
# outbound Langfuse calls are attempted.
# Use `docker compose -f docker-compose.lite.yml up -d` for fast inner-loop dev.
# CI runs lite on every PR; full stack runs nightly.
```

Services (6 total):
- `brain-postgres` — identical to full's brain-postgres except volume name MAY use `brain-pg-data-lite` to keep lite/full data separate; if the executor prefers single volume name `brain-pg-data` to avoid the parity check flagging volume names, that is acceptable too — RECOMMENDATION: keep the SAME volume name `brain-pg-data` so `check-compose-parity.sh` doesn't have to allowlist volume-name differences. Confirm decision in SUMMARY.
- `rabbitmq` — identical to full's rabbitmq.
- `qdrant` — identical to full's qdrant.
- `brain-migrate` — identical to full's brain-migrate.
- `brain-topology-init` — same placeholder as full (alpine:3.20 + `command: ["true"]`).
- `brain` — same as full PLUS additional explicit env override `BRAIN_LANGFUSE__ENABLED=false` (belt-and-suspenders so lite never attempts a Langfuse connect even if `.env` says enabled=true).

Header `networks: { brain-net: { driver: bridge } }` and `volumes:` block listing only the volumes actually used (no `lf-pg-data` / `ch-data` / `redis-data` / `minio-data`).

Verify `docker compose -f docker-compose.lite.yml config` exits 0.

**(b) Create `/root/Brain/compose/brain-topology-init/README.md`**:

```markdown
# brain-topology-init (DEPLOY-06 placeholder)

Phase 1 declares this service as an alpine `true` no-op so DEPLOY-06's "init
container slot reserved" requirement is satisfied and Brain's compose
`depends_on` graph has a stable target.

Phase 8 replaces image + command with a small `aio-pika` script that declares:

  - exchanges: `brain.dlx` (DLX)
  - queues: `brain.in` (with `x-dead-letter-exchange=brain.dlx`), `brain.dlq`,
    `brain.out`
  - bindings as documented in `.planning/research/ARCHITECTURE.md` (queue layout
    section, to be added in Phase 8 research)

When Phase 8 lands, runtime consumers (in `src/brain/workers/`) declare the
same queues with `passive=True` so a missing topology fails fast rather than
silently re-declaring.

DO NOT delete this entry — Brain's `depends_on` (added in Phase 8) keys off
the service name `brain-topology-init`.
```

(Directory `compose/brain-topology-init/` only contains this README in Phase 1; Phase 8 may add a Dockerfile + entrypoint.)
  </action>
  <verify>
    <automated>cd /root/Brain && docker compose -f docker-compose.lite.yml config >/tmp/compose-lite.yml 2>&1 && ! grep -E 'langfuse-(web|worker|postgres)|clickhouse|^\s+minio:|^\s+redis:' /tmp/compose-lite.yml && grep -F 'brain-topology-init' docker-compose.lite.yml && grep -F 'BRAIN_LANGFUSE__ENABLED' docker-compose.lite.yml | grep -F 'false' && grep -F 'postgres:17-trixie' docker-compose.lite.yml && grep -F 'rabbitmq:4.1-management-alpine' docker-compose.lite.yml && grep -F 'qdrant/qdrant:v1.18.0' docker-compose.lite.yml && grep -F 'alpine:3.20' docker-compose.lite.yml && test -f compose/brain-topology-init/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.lite.yml` exists at repo root
    - `docker compose -f docker-compose.lite.yml config` exits 0
    - Lite contains exactly 6 service entries: `brain-postgres`, `rabbitmq`, `qdrant`, `brain-migrate`, `brain-topology-init`, `brain`
    - Lite contains NO `langfuse-*`, NO `clickhouse`, NO `redis`, NO `minio`
    - Lite's `brain` service has `BRAIN_LANGFUSE__ENABLED=false` env override (belt-and-suspenders per A7)
    - Lite's image tags for `brain-postgres`, `rabbitmq`, `qdrant`, `brain-topology-init` match full exactly
    - Lite's healthchecks for `brain-postgres`, `rabbitmq`, `qdrant` match full exactly (verified by check-compose-parity.sh in plan 01-09)
    - Lite's Brain `depends_on` is identical to full's Brain `depends_on`
    - `compose/brain-topology-init/README.md` exists; mentions DEPLOY-06 + Phase 8 fill plan
    - No `:latest` tags in lite file
  </acceptance_criteria>
  <done>
    Lite stack validates; strict subset of full for the 5 shared service definitions; explicit `BRAIN_LANGFUSE__ENABLED=false` belt-and-suspenders; brain-topology-init slot reserved in both files. DEPLOY-02 + DEPLOY-06 — green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host network → exposed compose ports | Only 8000 / 3000 / 15672 / 6333 published; other ports internal-only on `brain-net` (D-12) |
| compose env interpolation → service env | `.env` values are interpolated into service definitions; missing vars surface as empty strings (no fail-fast unless service-side validation triggers) |
| Brain service → Langfuse subsystem | `depends_on` boundary — Langfuse outage MUST NOT block Brain boot (PITFALL 8.1) |
| Brain-Postgres ↔ Langfuse-Postgres | Two separate instances per PITFALL 6.2; never share credentials or volumes |
| MinIO upstream supply chain | Image is from an archived upstream (Pitfall 8); no further CVE patches expected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Denial of Service | PITFALL 8.1 / D-11 — Langfuse subsystem outage stalls Brain boot or request path | critical | mitigate | Brain `depends_on` includes ONLY brain-migrate + brain-postgres + rabbitmq + qdrant; explicit grep acceptance forbids `langfuse` inside Brain's depends_on block; lite mode has explicit `BRAIN_LANGFUSE__ENABLED=false` |
| T-08-02 | Information Disclosure | PITFALL 6.2 — Brain and Langfuse share a Postgres → cross-product data bleed + entangled failure domains | critical | mitigate | Two separate `postgres:17-trixie` services with separate volumes (`brain-pg-data` vs `lf-pg-data`); acceptance test counts `image: postgres:17-trixie` occurrences == 2 |
| T-08-03 | Information Disclosure | Brain-Postgres or RabbitMQ exposed on host → reachable from network | high | mitigate | D-12: no host port for 5432 / 5672; only management UIs (15672) + Qdrant dashboard (6333) bound to `127.0.0.1`; brain itself on `0.0.0.0:8000` (required for external bot adapters) |
| T-08-04 | Tampering | `:latest` tag silently drift → non-reproducible deploys (CLAUDE.md §10) | high | mitigate | grep acceptance forbids `:latest` in compose files; alpine placeholder pinned to `3.20`; every service pinned to specific version |
| T-08-05 | Tampering | MinIO image archived → CVE accumulation without patches (Pitfall 8) | medium | accept | Pin to last known-good `RELEASE.2025-09-07T16-13-09Z-cpuv1`; document migration to Garage/SeaweedFS in `compose/brain-topology-init/README.md` and SUMMARY; track in Open Question #2; not a Phase-1 blocker |
| T-08-06 | Tampering | Langfuse image upstream changes env shape between minor versions | medium | accept | Hand-rolled config (D-09) → upgrades are explicit PRs; pinned to `3.175.0`; SUMMARY should re-verify env shape against `langfuse/langfuse:3.175.0` at execute time |
| T-08-07 | Denial of Service | brain-migrate fails silently — Brain starts with broken DB | high | mitigate | `depends_on: brain-migrate: condition: service_completed_successfully`; brain-migrate exits non-zero on Alembic/setup/assert failure per plan 01-06 |
| T-08-08 | Spoofing | RabbitMQ default `guest:guest` accessible across the bridge network | medium | mitigate | RABBITMQ_DEFAULT_USER=brain + RABBITMQ_DEFAULT_PASS=brain set in compose; matches `BRAIN_RABBITMQ__URL=amqp://brain:brain@rabbitmq:5672/` in `.env.example`; production-grade rotation deferred to Phase 9 |
| T-08-09 | Tampering | Compose interpolation references undefined env var (e.g., `${LANGFUSE_NEXTAUTH_SECRET}` absent) → empty string baked into Langfuse, NextAuth refuses to sign sessions | medium | mitigate | `.env.example` (plan 01-03) defines every interpolated key; `scripts/check-env-example.sh` (plan 01-09) enforces parity |
| T-08-10 | Denial of Service | ClickHouse first-boot exceeds default healthcheck retries → langfuse-web wait-loop times out before ClickHouse ready | medium | mitigate | `start_period: 60s` on clickhouse healthcheck; A5 documents fallback to `24.3-alpine` if `24.8-alpine` is unavailable |
| T-08-11 | Information Disclosure | brain-topology-init runs an unpinned alpine → CVE drift on a service that does nothing | low | mitigate | Pin to `alpine:3.20` explicitly; not `:latest` |
| T-08-12 | Tampering | Lite compose drifts from full (e.g., new env var added to full but not lite) | medium | mitigate | Plan 01-09 ships `scripts/check-compose-parity.sh` which diffs the shared service blocks |
</threat_model>

<verification>
- `docker compose -f docker-compose.yml config` exits 0 (with `.env` copied from `.env.example`)
- `docker compose -f docker-compose.lite.yml config` exits 0
- `grep -c 'healthcheck:' docker-compose.yml` ≥ 10 (one per long-running service; 12 total counts brain-migrate's absence ok)
- `grep -F 'image: postgres:17-trixie' docker-compose.yml | wc -l` returns `2`
- `grep -F ':latest' docker-compose.yml docker-compose.lite.yml` returns empty
- `grep -F 'brain-topology-init' docker-compose.yml docker-compose.lite.yml` returns ≥ 2
- Brain depends_on grep contains no `langfuse-*`
- `test -f compose/brain-topology-init/README.md` succeeds
- `docker compose -f docker-compose.lite.yml up -d --build` reaches `service_healthy` deterministically (full smoke runs in plan 01-09; this is implicit acceptance here)
</verification>

<success_criteria>
- DEPLOY-01 (full 10-service compose) — green
- DEPLOY-02 (lite subset compose, literally `docker-compose.lite.yml`) — green
- DEPLOY-04 (healthchecks everywhere + service_healthy depends_on) — green
- DEPLOY-05 (brain-migrate init runs Alembic + setup before Brain) — green (entrypoint from plan 01-06)
- DEPLOY-06 (brain-topology-init slot reserved) — green (placeholder + README)
- D-09 (5 Langfuse services pinned), D-10 (separate-file lite, RESEARCH.md recommended), D-11 (Brain depends only on core), D-12 (host port restrictions) — all green
</success_criteria>

<output>
Create `.planning/phases/01-foundations-compose-skeleton/01-08-SUMMARY.md` covering:
- Final service list + image pins for both compose files (table)
- Confirmation of A3 (MinIO tag pulled cleanly OR fallback tag used)
- Confirmation of A5 (ClickHouse 24.8-alpine or fallback)
- Confirmation that no `langfuse-*` appears in Brain's `depends_on`
- Volume naming decision (shared `brain-pg-data` vs `brain-pg-data-lite` — recommend shared)
- Note for Phase 8: replace alpine placeholder with real topology init container
- Note for Phase 9: MinIO migration tracking (Open Question #2)
- Note for plan 01-09: `scripts/check-compose-parity.sh` consumes both files
</output>
