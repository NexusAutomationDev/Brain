---
phase: 01-foundations-compose-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .gitignore
  - .gitleaks.toml
  - .gitleaksignore
  - .pre-commit-config.yaml
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-12]
tags: [python, packaging, uv, ruff, pre-commit, gitleaks, foundations]

must_haves:
  truths:
    - "`uv lock --check` succeeds on a fresh clone (lockfile reproducible)"
    - "`grep -E '^(langgraph|langgraph-checkpoint|langgraph-checkpoint-postgres)\\s*=\\s*\"==' pyproject.toml` returns exactly 3 lines"
    - "`uv run ruff --version` prints `0.15.14`; `uv run pytest --version` prints `9.0.3`"
    - "`git check-ignore .env` exits 0 (i.e. `.env` is gitignored)"
    - "Pre-commit installs cleanly: `uvx pre-commit install --install-hooks` succeeds"
    - "Seeded fake `sk-` key in a staged file is rejected by `gitleaks protect --staged --config .gitleaks.toml`"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, exact-pinned dependency table, ruff config (T201+G004 with alembic exception), pytest config (asyncio_mode=auto, testpaths=['tests'])"
      contains: "langgraph==1.2.1"
    - path: "uv.lock"
      provides: "Reproducible dependency lock"
      min_lines: 50
    - path: ".python-version"
      provides: "Pyenv / uv Python version pin"
      contains: "3.12"
    - path: ".gitignore"
      provides: "Ignore `.env`, `.venv`, `__pycache__`, `*.pyc`, `.pytest_cache`, `htmlcov`, `dist`, `build`, `*.egg-info`"
      contains: ".env"
    - path: ".gitleaks.toml"
      provides: "Gitleaks rules (extends default; brain-auth-token, openai-key, gemini-key); allowlist for `<REPLACE_ME>` in `.env.example` and `README.md`"
      contains: "useDefault = true"
    - path: ".gitleaksignore"
      provides: "Empty allowlist file (commit empty so future false-positives have a home)"
    - path: ".pre-commit-config.yaml"
      provides: "Hooks: gitleaks v8.21.2 (protect --staged), ruff (check + format), trailing-whitespace/end-of-file from pre-commit-hooks. Local hook stubs reserved for grep bans added by plan 01-04."
      contains: "gitleaks"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint]"
      to: "alembic/env.py (created in plan 01-06)"
      via: "[tool.ruff.lint.per-file-ignores]"
      pattern: "\"alembic/env\\.py\""
    - from: ".pre-commit-config.yaml"
      to: ".gitleaks.toml"
      via: "args: ['protect', '--staged', '--config', '.gitleaks.toml']"
      pattern: "\\.gitleaks\\.toml"
---

<objective>
Bootstrap the greenfield Python project: pinned dependency table (exact-pin on the LangGraph triple per FOUND-02), uv lockfile, ignore rules, pre-commit hook chain anchored on gitleaks, ruff config including the `T201`/`G004` bans and the alembic exception (per D-15), and pytest config skeleton. This plan establishes the spine every other Wave-2+ plan installs and lints against.

Purpose: Lock dependency reproducibility, secret-scanning hygiene, and lint surface before any code lands.
Output: Working `uv sync` / `uv lock --check` / `uv run ruff check .` / `uvx pre-commit run --all-files` on a fresh clone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md
@.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md
@.planning/phases/01-foundations-compose-skeleton/01-VALIDATION.md
</context>

<interfaces>
<!-- No code interfaces yet; this plan creates the build/lint substrate. -->
<!-- Critical pins copied verbatim from 01-RESEARCH.md §"Standard Stack" + CLAUDE.md §"Technology Stack". -->

Runtime deps (exact-pin via `==`):
  fastapi==0.136.1
  uvicorn[standard]==0.47.0
  pydantic==2.13.4
  pydantic-settings==2.14.1
  httpx==0.28.1
  langgraph==1.2.1                       # FOUND-02 — exact pin
  langgraph-checkpoint==4.1.0            # FOUND-02 — exact pin
  langgraph-checkpoint-postgres==3.1.0   # FOUND-02 — exact pin
  langchain-core==1.4.0
  psycopg[binary,pool]==3.3.4
  sqlalchemy==2.0.49
  alembic==1.18.4
  aio-pika==9.6.2
  qdrant-client==1.18.0
  structlog==25.5.0
  tenacity==9.1.4

Dev deps:
  pytest==9.0.3
  pytest-asyncio==1.3.0
  pytest-cov
  testcontainers[postgres,rabbitmq]
  ruff==0.15.14
  mypy
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pyproject.toml with exact-pinned deps, ruff config, pytest config</name>
  <read_first>
    - /root/Brain/CLAUDE.md (§ Technology Stack — all version pins, anti-recommendations)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md (§ "Standard Stack" lines 110-195 for pins; § "Code Examples" Example 6 lines 1057-1085 for ruff config)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md (D-01, D-15)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-VALIDATION.md (test config requirements lines 71-89)
  </read_first>
  <action>
Create `/root/Brain/pyproject.toml` with the following sections (copy values verbatim — do not substitute):

1. `[project]` block:
   - name = "brain"
   - version = "0.1.0"
   - description = "Centralized LangGraph-based AI orchestration service for conversational bots"
   - requires-python = "==3.12.*"
   - readme = "README.md"
   - dependencies = [exact-pinned list — see <interfaces> block above; every line uses `==`, no `^` no `~`]
   - Note: do NOT include `langfuse` in runtime deps yet (Phase 4 wires it; pinning here would force install before needed). Add `langfuse==4.6.1` as a runtime dep now to lock the version (per CLAUDE.md spine) — Phase 4 just imports it.
   - Actually: include `langfuse==4.6.1` per STACK.md §7 ("Phase 1 pins to lock matched server"). 

2. `[project.optional-dependencies]` or `[dependency-groups]`:
   - dev = ["pytest==9.0.3", "pytest-asyncio==1.3.0", "pytest-cov", "testcontainers[postgres,rabbitmq]", "ruff==0.15.14", "mypy"]

3. `[build-system]`:
   - requires = ["hatchling"]
   - build-backend = "hatchling.build"

4. `[tool.hatch.build.targets.wheel]`:
   - packages = ["src/brain"]   # D-01 src-layout

5. `[tool.ruff]`:
   - target-version = "py312"
   - line-length = 100
   - extend-exclude = ["alembic/versions"]

6. `[tool.ruff.lint]`:
   - select = ["E", "F", "W", "I", "B", "T201", "G004", "UP", "RUF"]   # T201=print ban, G004=logging f-string ban (D-15, FOUND-10)

7. `[tool.ruff.lint.per-file-ignores]`:
   - "alembic/env.py" = ["G004", "T201"]   # D-15 exception: Alembic's machinery uses stdlib logging
   - "alembic/versions/*.py" = ["G004", "T201", "E501"]
   - "tests/**/*.py" = ["T201"]   # tests may print for debugging

8. `[tool.ruff.format]`:
   - quote-style = "double"
   - indent-style = "space"

9. `[tool.pytest.ini_options]`:
   - asyncio_mode = "auto"
   - testpaths = ["tests"]
   - addopts = "--strict-markers -ra"
   - filterwarnings = ["error"]
   - markers = ["integration: requires docker / testcontainers"]

10. `[tool.coverage.run]`:
    - source = ["src/brain"]
    - branch = true

11. `[tool.coverage.report]`:
    - fail_under = 80
    - show_missing = true

12. `[tool.mypy]`:
    - python_version = "3.12"
    - strict = true
    - ignore_missing_imports = true   # third-party stubs may lag

After writing `pyproject.toml`, run `uv lock` to produce `uv.lock`. Then verify with `uv lock --check`.

Also create `/root/Brain/.python-version` containing exactly `3.12` (single line, no newline issues).

DO NOT install `langchain` meta-package. DO NOT install `asyncpg`. DO NOT install `gunicorn`. DO NOT install `langchain-openai` or `langchain-google-genai` yet (Phase 5).
  </action>
  <verify>
    <automated>cd /root/Brain && uv lock --check && uv run ruff --version | grep -F "0.15.14" && uv run pytest --version | grep -F "9.0.3" && grep -E '^\s*"langgraph(-checkpoint(-postgres)?)?==' pyproject.toml | wc -l | grep -q "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists at repo root
    - `uv.lock` exists at repo root with `langgraph==1.2.1`, `langgraph-checkpoint==4.1.0`, `langgraph-checkpoint-postgres==3.1.0` resolved exactly
    - `grep -E '^\s*"langgraph(-checkpoint(-postgres)?)?==' pyproject.toml | wc -l` returns `3`
    - `grep -F '"langchain"' pyproject.toml` returns empty (meta-package banned)
    - `grep -F '"asyncpg' pyproject.toml` returns empty (FOUND-06 / D-17)
    - `grep -F 'requires-python = "==3.12.*"' pyproject.toml` returns 1 line
    - `[tool.ruff.lint]` select list contains `"T201"` and `"G004"`
    - `[tool.ruff.lint.per-file-ignores]` includes `"alembic/env.py"` mapping to `["G004", "T201"]`
    - `[tool.pytest.ini_options]` contains `asyncio_mode = "auto"` and `testpaths = ["tests"]`
    - `.python-version` contains `3.12`
    - `uv run ruff check pyproject.toml` exits 0 (config self-validates)
  </acceptance_criteria>
  <done>
    `pyproject.toml` + `uv.lock` + `.python-version` committed; `uv lock --check` green; `uv run ruff --version` returns `0.15.14`; LangGraph triple exact-pinned (FOUND-02 satisfied).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .gitignore, .gitleaks.toml, .gitleaksignore, .pre-commit-config.yaml</name>
  <read_first>
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-RESEARCH.md (§ "Code Examples" Example 5, lines 995-1055)
    - /root/Brain/.planning/phases/01-foundations-compose-skeleton/01-CONTEXT.md (D-12, D-18)
    - /root/Brain/CLAUDE.md (§ Git Commit Guidelines — pre-commit must not break the emoji/conventional-commits flow)
  </read_first>
  <action>
Create four files.

**(a) `/root/Brain/.gitignore`** — Python + uv + IDE + secrets ignores. Required entries (one per line, in this order, headers as comments):
```
# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/

# Pytest / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Ruff / mypy
.ruff_cache/
.mypy_cache/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# uv
# (uv.lock is committed; .python-version is committed)

# Docker / Compose
*.local.yml
```

**(b) `/root/Brain/.gitleaks.toml`** — copy verbatim the block from 01-RESEARCH.md Example 5 lines 998-1030:
```toml
[extend]
useDefault = true

[[rules]]
id = "brain-auth-token"
description = "Brain Bearer auth token"
regex = '''BRAIN_AUTH(__TOKEN|_TOKEN)\s*=\s*[A-Za-z0-9_\-]{16,}'''
tags = ["secret", "brain"]

[[rules]]
id = "openai-key"
description = "OpenAI API key"
regex = '''sk-[A-Za-z0-9]{20,}'''
tags = ["secret", "openai"]

[[rules]]
id = "gemini-key"
description = "Google Gemini API key"
regex = '''AIza[0-9A-Za-z\-_]{35}'''
tags = ["secret", "gemini"]

[allowlist]
description = "Allowlist for example placeholders"
regexes = [
  '''<REPLACE_ME>''',
  '''<REPLACE_ME_64_CHAR_RANDOM>''',
  '''<REPLACE_ME_64_CHAR_HEX>''',
  '''replace-me-with-a-long-random-string''',
]
paths = [
  '''\.env\.example$''',
  '''README\.md$''',
  '''\.planning/''',
]
```

**(c) `/root/Brain/.gitleaksignore`** — create EMPTY file (no content). Future false-positives go here with `git blob hash:rule_id:fingerprint` entries. Empty file commits clean.

**(d) `/root/Brain/.pre-commit-config.yaml`** — pre-commit hooks for secret scanning, ruff lint+format, basic hygiene. Reserve local-hook slots for plans 01-04 (thread_id + asyncpg + sync-PostgresSaver grep bans) — do NOT add the actual grep bans here; plan 01-04 amends this file.

```yaml
# .pre-commit-config.yaml
# IMPORTANT: this file is amended by plans 01-04 (grep bans).
default_install_hook_types: [pre-commit]
fail_fast: false

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ["--maxkb=500"]

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
        args: ["protect", "--staged", "--config", ".gitleaks.toml"]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.14
    hooks:
      - id: ruff
        args: ["check", "--fix"]
      - id: ruff-format

  # Local hooks reserved for plan 01-04:
  #   - id: ban-asyncpg
  #   - id: ban-sync-postgressaver
  #   - id: ban-raw-thread-id
  #   - id: ban-stdlib-logging
  # See plan 01-04 PLAN.md.
```

After writing files, run `uvx pre-commit install --install-hooks` to verify the config is parseable.

Test the gitleaks rule by staging a temp file with `sk-thisisafaketestkey1234567890abc` and running `gitleaks protect --staged --config .gitleaks.toml` — must exit non-zero. THEN `git restore --staged` the temp file and delete it; do NOT commit the canary.
  </action>
  <verify>
    <automated>cd /root/Brain && git check-ignore .env >/dev/null && uvx pre-commit install --install-hooks >/dev/null 2>&1 && uvx pre-commit run --files .gitignore .gitleaks.toml .pre-commit-config.yaml 2>&1 | tail -5 && tmpfile=$(mktemp -p /root/Brain canary.XXXX.env) && echo 'OPENAI_API_KEY=sk-thisisafaketestkey1234567890abc' > "$tmpfile" && git add -f "$tmpfile" 2>/dev/null; gitleaks protect --staged --config .gitleaks.toml; rc=$?; git restore --staged "$tmpfile" 2>/dev/null; rm -f "$tmpfile"; test "$rc" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` exists; `git check-ignore .env` exits 0
    - `git check-ignore .env.example` exits non-zero (not ignored — the `!.env.example` line works)
    - `.gitleaks.toml` exists and parses (verified by `gitleaks detect --no-git --config .gitleaks.toml --source .` succeeds, returning 0 or non-zero findings cleanly without parse errors)
    - `.gitleaksignore` exists and is empty (`wc -c < .gitleaksignore` returns `0`)
    - `.pre-commit-config.yaml` exists; `uvx pre-commit install` succeeds
    - `uvx pre-commit run --files .pre-commit-config.yaml` passes the `check-yaml` hook
    - Canary test: staging a fake `sk-...` key triggers a gitleaks failure (non-zero exit)
    - `.pre-commit-config.yaml` references `https://github.com/gitleaks/gitleaks` at `rev: v8.21.2` and `https://github.com/astral-sh/ruff-pre-commit` at `rev: v0.15.14`
  </acceptance_criteria>
  <done>
    `.gitignore` ignores `.env`; gitleaks pre-commit and CI shapes both work; ruff hooks installed; pre-commit chain is parseable; canary leak is caught. FOUND-12 satisfied at the lint layer (CI shape added in plan 01-09).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer laptop → git origin | Untrusted commit content may include secrets |
| supply chain (PyPI / GitHub) → uv resolver | Unverified packages and floating versions risk dependency-confusion / silent regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` accidentally committed | high | mitigate | `.gitignore` includes `.env` + `.env.*` (Task 2); gitleaks pre-commit hook (Task 2) catches real-looking secrets even if `.gitignore` is sidestepped via `git add -f` |
| T-01-02 | Information Disclosure | provider API key in `.env.example` (PITFALL 4) | high | mitigate | `.gitleaks.toml` rule `openai-key` matches `sk-...`, rule `gemini-key` matches `AIza...`; allowlist permits only `<REPLACE_ME>` placeholder strings inside `.env.example` and `README.md` |
| T-01-03 | Tampering | unpinned dep silently bumps `langgraph-checkpoint-postgres` to 3.2.x (PITFALL 1.1) | high | mitigate | `pyproject.toml` exact-pins (`==`) the LangGraph triple per FOUND-02; verify acceptance criterion grep returns 3 |
| T-01-04 | Tampering | floating Python image at runtime | medium | mitigate | `.python-version` pins 3.12; `pyproject.toml requires-python = "==3.12.*"` blocks accidental 3.13 install (CLAUDE.md §10 — `python:3.13-slim` not yet) |
| T-01-05 | Tampering | floating `ruff-pre-commit` rev introduces unexpected lint failures across contributor machines | medium | mitigate | `.pre-commit-config.yaml` pins `rev: v0.15.14` (matches `ruff==0.15.14` in pyproject.toml) |
| T-01-06 | Tampering | floating `gitleaks` action rev changes ruleset behaviour | medium | mitigate | `.pre-commit-config.yaml` pins `rev: v8.21.2` |
| T-01-07 | Information Disclosure | gitleaks bypassed via `--no-verify` commit flag | medium | accept (this plan) | CI gitleaks job (plan 01-09) catches anything that bypassed the hook; CLAUDE.md prohibits `--no-verify` |
| T-01-08 | Tampering | `langchain` meta-package pulls unverified transitive deps (CLAUDE.md §10) | medium | mitigate | acceptance criterion greps that `"langchain"` (no suffix) is absent from `pyproject.toml`; only `langchain-core` allowed |
</threat_model>

<verification>
- `cd /root/Brain && uv lock --check` exits 0
- `uv run ruff --version` prints `0.15.14`
- `uv run pytest --version` prints `9.0.3`
- `grep -E '^\s*"langgraph(-checkpoint(-postgres)?)?==' pyproject.toml | wc -l` returns `3`
- `git check-ignore .env` exits 0
- `uvx pre-commit run --all-files` exits 0 on the committed files
- Canary leak in a staged file triggers `gitleaks protect --staged` failure
</verification>

<success_criteria>
- FOUND-01: Python 3.12 + uv-managed pinned deps — green
- FOUND-02: LangGraph + checkpointer triple exact-pinned (`==`) — green (acceptance grep)
- FOUND-12 (gitignore + gitleaks pre-commit layer): green (CI layer in plan 01-09)
- DEPLOY-07 (partial): `.gitignore` ignores `.env` — green (`.env.example` content lands in plan 01-03)
- Pre-commit chain installs and lint hooks run on a fresh clone
</success_criteria>

<output>
Create `.planning/phases/01-foundations-compose-skeleton/01-01-SUMMARY.md` documenting:
- Final dependency table (with exact versions resolved by `uv lock`)
- Ruff config decisions (selected rules + per-file-ignores)
- `.gitleaks.toml` rules and allowlist scope
- Pre-commit hook chain order (and which local hooks plan 01-04 will add)
- Any version drift surfaced during `uv lock` vs the planned versions
</output>
