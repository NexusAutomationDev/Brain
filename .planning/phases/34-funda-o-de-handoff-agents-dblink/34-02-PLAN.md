---
phase: 34-funda-o-de-handoff-agents-dblink
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - packages/database/src/agents.ts
  - packages/database/src/index.ts
  - packages/database/src/__tests__/unit/agents.test.ts
  - packages/database/src/__tests__/integration/agents.integration.test.ts
  - packages/database/src/__tests__/integration/migration-0012.test.ts
autonomous: true
requirements: [HANDOFF-04, HANDOFF-10]

must_haves:
  truths:
    - "Consultar `agents` por um nome desconhecido retorna { ok: false, reason: 'not_found' } — sem transferir, sem lançar exceção (HANDOFF-04)"
    - "Consultar `agents` por um nome existente com enabled=false retorna { ok: false, reason: 'disabled' } — sem transferir (HANDOFF-04)"
    - "Consultar `agents` por um nome válido e enabled=true retorna { ok: true, connectionString, brainType } (HANDOFF-04)"
    - "getAgentConnection() faz uma leitura sempre live (query direta a cada chamada) — nenhum snapshot em compile-time ou cache em memória (D-07)"
    - "getAgentConnection() usa exclusivamente eq(agents.name, name) do Drizzle — nunca interpolação de string/sql.unsafe() com o argumento name (Security Domain V5)"
    - "getAgentConnection()/seus testes nunca fazem console.log ou JSON.stringify da linha inteira de agents ou do AgentConnectionResult — apenas name/brainType são logáveis (Pitfall 2, T-34-01's logging corollary)"
    - "HANDOFF-10 (thread_id sempre do contexto de execução, nunca do argumento da tool/LLM) é N/A em código nesta fase — nenhuma tool ou call site que consome thread_id existe ainda (D-08); documentado aqui como constraint travada para a Phase 35 aplicar o mesmo padrão D-04 já usado por pause-session.ts/finish-conversation.ts (config.configurable.thread_id, nunca um argumento de tool)"
    - "As integration tests de getAgentConnection() (agents.integration.test.ts) rodam de verdade — não skipped — contra um Postgres real provisionado nesta mesma execução, fechando a mesma classe de gap (TEST_DATABASE_URL ausente) que a verificação da Phase 33 deixou como human_needed"
    - "migration-0012.test.ts confirma, via leitura de arquivo real (sem precisar de DB live), que o journal idx=12/tag, o arquivo SQL, e os exports de tables.ts existem exatamente como especificado na Plan 34-01 — mesmo padrão de migration-v14.test.ts"
  artifacts:
    - packages/database/src/agents.ts
    - packages/database/src/index.ts
    - packages/database/src/__tests__/unit/agents.test.ts
    - packages/database/src/__tests__/integration/agents.integration.test.ts
    - packages/database/src/__tests__/integration/migration-0012.test.ts
  key_links:
    - "getAgentConnection() importa `agents` de packages/database/src/schema/tables.ts (Plan 34-01) e usa eq(agents.name, name) — nunca sql.unsafe()"
    - "packages/database/src/index.ts exporta getAgentConnection + o tipo AgentConnectionResult — pronto para o futuro transfer_lead tool da Phase 35 importar diretamente, sem nenhum wiring de tool/LLM nesta fase"
    - "agents.integration.test.ts e migration-0012.test.ts rodam contra o mesmo runMigrations()/bun src/migrate.ts da Plan 34-01, provando a cadeia completa de migrations 0000-0012 aplicada do zero, de forma independente do container da Plan 34-01 (que já foi encerrado)"
  prohibitions: []
---

## Artifacts this phase produces

New files/symbols created by this plan (exclude these from drift/source-grounding checks —
they do not exist yet in the codebase):

- `packages/database/src/agents.ts` — new file, exports `getAgentConnection(sql, name)` and
  the `AgentConnectionResult` discriminated-union type
- New export lines in `packages/database/src/index.ts`:
  `export { getAgentConnection } from './agents.js';` and
  `export type { AgentConnectionResult } from './agents.js';`
- `packages/database/src/__tests__/unit/agents.test.ts` — new test file (mocked
  `drizzle-orm/postgres-js`)
- `packages/database/src/__tests__/integration/agents.integration.test.ts` — new test file
  (real Postgres, `TEST_DATABASE_URL`-gated)
- `packages/database/src/__tests__/integration/migration-0012.test.ts` — new test file
  (file-content assertions, no live DB needed)

Modified (not new) files: none beyond the above.

<flagged_assumptions>
## Spec-less probe note (edge coverage + prohibition recall — read before further work on this phase)

This phase has no `*-SPEC.md`. The deterministic edge-probe pre-classified 7 rows across the
phase's 4 requirements; 5 were resolved directly into Plan 34-01's `must_haves.truths`
(HANDOFF-01's empty/encoding/concurrency edges, HANDOFF-02's idempotency/concurrency edges —
all `covered`, backed by concrete evidence already in RESEARCH.md). The remaining 2 rows were
classified `unclassified`/`unresolved` by the deterministic engine and are surfaced here,
per protocol, rather than silently resolved or dropped:

- **HANDOFF-04 (unclassified):** not fully covered by the deterministic taxonomy is whether
  `agents.name` lookups are case/whitespace-sensitive (Postgres `text` equality is
  byte-exact — "Support" ≠ "support" — undocumented as a product requirement either way), and
  whether a TOCTOU race exists between this phase's live `getAgentConnection()` SELECT and
  Phase 35's later `dblink_exec` call (an agent could be disabled in the gap between the two).
  Not resolved by this phase — flagged for Phase 35's planner to decide whether the
  `transfer_lead` tool call site needs to re-validate immediately before writing.
- **HANDOFF-10 (unclassified):** no code in this phase consumes `thread_id` (D-08), so there
  is nothing to exercise the edge-probe's categories against yet. Flagged for Phase 35's
  planner/verifier to confirm the `transfer_lead` tool actually follows the
  `pause-session.ts`/`finish-conversation.ts` `config.configurable.thread_id` precedent once
  that tool exists — this phase only carries the constraint forward, per D-08.

**Prohibition recall (Stage 1 → Stage 2), all 4 requirements:** Stage 1 (adversarial recall)
produced candidates for each requirement (e.g. HANDOFF-01: `connection_string` becoming a
single point of multi-tenant credential blast-radius; HANDOFF-02: dblink's PUBLIC-executable
function grants as a privilege-escalation vector; HANDOFF-04: differentiating
not_found/disabled responses as a destination-name enumeration vector; HANDOFF-10: tool-arg/
thread_id confusion enabling cross-lead session access). Stage 2 (precision) dropped every
one of them for one of two reasons, never silently:
  1. **Canon-referral** — the credential-blast-radius and PUBLIC-executable-grant candidates
     are OWASP-canon (Access Control / Elevation of Privilege) already tracked as explicit,
     dispositioned rows in Plan 34-01's `<threat_model>` (T-34-01, T-34-02) — not re-minted
     here as a duplicate bespoke prohibition.
  2. **Already an explicit, locked decision — not an omission** — the not_found/disabled
     differentiation is D-06's literal, intentional contract (agent names are operator-
     configured, not attacker-controlled network input, so enumeration-prevention does not
     apply the way it would to a public login form); and the tool-arg/thread_id confusion
     concern IS HANDOFF-10's own requirement text verbatim ("thread_id nunca do argumento da
     tool/LLM") — there is no hidden must-NOT here to surface, the requirement already states
     it.

Net result: zero bespoke prohibitions survive Stage 2 for this phase — a defensible "clean
utility" outcome for a backend-only, zero end-user-facing infrastructure phase (per
CONTEXT.md's own `<specifics>` note). `must_haves.prohibitions: []` above reflects this
explicitly, not a silent omission.
</flagged_assumptions>

<objective>
Build and prove the isolated, testable destination-lookup function `getAgentConnection(sql,
name)` (HANDOFF-04) on top of Plan 34-01's `agents` table, and close the exact
"no real Postgres available" verification gap Phase 33 left as `human_needed` by actually
provisioning a scratch database and running the integration suite against it for real in this
same execution — not leaving it gated-and-skipped again.

Purpose: HANDOFF-04 is the mechanism that makes this phase "validable in isolation" per the
ROADMAP goal — a small, dependency-free function with a three-way rejection contract
(not_found / disabled / ok), with zero wiring into any tool or LLM yet (that's Phase 35).
HANDOFF-10 has no code to write in this phase (D-08) and is captured here purely as a
documented, locked constraint for Phase 35 to inherit.
Output: `packages/database/src/agents.ts` (`getAgentConnection` + `AgentConnectionResult`),
barrel exports from `index.ts`, a mocked unit test suite, a real-Postgres-gated integration
test suite that actually runs (not skips) in this execution, and a file-content test proving
migration 0012's shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-CONTEXT.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-RESEARCH.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-PATTERNS.md

@packages/database/src/seed.ts
@packages/database/src/schema/tables.ts
@packages/database/src/index.ts
@packages/database/src/__tests__/integration/seed-idempotency.test.ts
@packages/database/src/__tests__/integration/migration-v14.test.ts
@packages/core/src/tools/pause-session.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAgentConnection() lookup function + unit tests</name>
  <files>packages/database/src/agents.ts, packages/database/src/index.ts, packages/database/src/__tests__/unit/agents.test.ts</files>
  <read_first>
    packages/database/src/seed.ts (the "plain function, Sql injected, no class" shape and
    doc-comment style to mirror — note this new file deliberately diverges from seed.ts's
    throw-on-failure idiom in favor of a Result-object return, per D-06/CONTEXT.md's
    "Claude's Discretion" note),
    packages/database/src/schema/tables.ts (the `agents` export landed by Plan 34-01 — column
    names: name, brainType, connectionString, enabled, createdAt, updatedAt),
    packages/database/src/index.ts (the existing "export { runBrainSeed } from './seed.js';"
    line — add the new export directly below it),
    packages/core/src/leads/lead-service.ts (contrast only — its class-with-constructor shape
    is NOT the pattern to copy here; seed.ts's plain-function shape is the closer analog),
    .planning/phases/34-funda-o-de-handoff-agents-dblink/34-RESEARCH.md sections "Pattern 3"
    and "Code Examples → Unit test skeleton for getAgentConnection()" (the literal
    implementation and mock shape to reproduce)
  </read_first>
  <behavior>
    - getAgentConnection(sql, 'unknown-agent') with no matching row → resolves to
      { ok: false, reason: 'not_found' }
    - getAgentConnection(sql, 'support') where the row has enabled=false → resolves to
      { ok: false, reason: 'disabled' }
    - getAgentConnection(sql, 'support') where the row has enabled=true → resolves to
      { ok: true, connectionString: <row's value>, brainType: <row's value> }
  </behavior>
  <action>
    Create packages/database/src/agents.ts. Export a discriminated-union type
    AgentConnectionResult: either { ok: true; connectionString: string; brainType: string }
    or { ok: false; reason: 'not_found' | 'disabled' }. Export async function
    getAgentConnection(sql: Sql, name: string): Promise&lt;AgentConnectionResult&gt;. Import
    { drizzle } from 'drizzle-orm/postgres-js', { eq } from 'drizzle-orm', { agents } from
    './schema/tables.js', and type { Sql } from 'postgres'. Implementation: build
    `const db = drizzle(sql);`, query
    `db.select().from(agents).where(eq(agents.name, name)).limit(1)`, take the first row; if
    no row, return { ok: false, reason: 'not_found' }; if the row's enabled field is false,
    return { ok: false, reason: 'disabled' }; otherwise return { ok: true, connectionString:
    row.connectionString, brainType: row.brainType }. No class, no hidden connection
    creation, no retry/lock logic (this is a single read-only SELECT, unlike seed.ts's
    transactional writes). Never log or print the full row or the full result object anywhere
    in this file (Pitfall 2) — if any debug statement is ever needed, only `name`/`brainType`
    are safe fields.

    In packages/database/src/index.ts, add two lines directly below the existing
    "export { runBrainSeed } from './seed.js';" line: "export { getAgentConnection } from
    './agents.js';" and "export type { AgentConnectionResult } from './agents.js';" (the type
    needs its own explicit export — it is not covered by the existing "export * from
    './schema/tables.js'" line since AgentConnectionResult lives in agents.ts, not
    schema/tables.ts).

    Create packages/database/src/__tests__/unit/agents.test.ts (under __tests__/unit/, never
    a top-level src/agents.test.ts — Pitfall 4). Mock 'drizzle-orm/postgres-js' at the module
    level (mock.module) so that drizzle(sql).select().from(agents).where(...).limit(1)
    resolves to a controllable `selectResult` array, reset in beforeEach. Write three `it`
    blocks matching the &lt;behavior&gt; cases above exactly (not_found, disabled, ok),
    importing getAgentConnection AFTER the mock.module() call (bun's hoisting requirement,
    same convention as seed.test.ts).
  </action>
  <verify>
    <automated>cd packages/database && bun test src/__tests__/unit/agents.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/agents.ts exports both `getAgentConnection` and
      `AgentConnectionResult`
    - packages/database/src/agents.ts contains "eq(agents.name, name)" and contains no
      occurrence of "sql.unsafe" or a template-literal SQL string built from the `name`
      parameter
    - packages/database/src/agents.ts contains no `console.log`/`JSON.stringify` call on the
      full row or the full result object
    - packages/database/src/index.ts contains "export { getAgentConnection } from
      './agents.js';" and "export type { AgentConnectionResult } from './agents.js';"
    - `bun test src/__tests__/unit/agents.test.ts` (packages/database) exits 0 with 3/3 tests
      passing
    - the new test file's path is packages/database/src/__tests__/unit/agents.test.ts, not a
      top-level packages/database/src/agents.test.ts
  </acceptance_criteria>
  <done>
    getAgentConnection() is implemented matching the three-way not_found/disabled/ok contract
    exactly, exported from index.ts alongside its type, and unit-tested with a mocked
    drizzle-orm/postgres-js module (no live DB needed for this task).
  </done>
</task>

<task type="auto">
  <name>Task 2: Real-Postgres integration tests + migration-content test — close Phase 33's verification gap</name>
  <files>packages/database/src/__tests__/integration/agents.integration.test.ts, packages/database/src/__tests__/integration/migration-0012.test.ts</files>
  <precondition>Docker is reachable (`docker ps` exits 0) and the `pgvector/pgvector:pg17`
  image is available locally (present via `docker images` or pullable); host port 5434 is
  free (Plan 34-01's scratch container was already torn down at the end of that
  plan).</precondition>
  <read_first>
    packages/database/src/__tests__/integration/seed-idempotency.test.ts (the
    describeOrSkip/TEST_DB_URL gating idiom and the beforeAll/afterAll
    insert-fixture/cleanup-by-filter idiom to copy),
    packages/database/src/__tests__/integration/migration-v14.test.ts (the exact
    file-content-assertion idiom to copy in full for the new migration-0012.test.ts —
    MIGRATIONS_DIR/SQL_FILE/JOURNAL_FILE/TABLES_FILE constants, one `it` per assertion),
    .planning/phases/34-funda-o-de-handoff-agents-dblink/34-RESEARCH.md section "Code
    Examples → Integration test skeleton (real Postgres, gated)" (the literal test body to
    reproduce),
    packages/database/src/agents.ts (from Task 1 of this same plan — the function under test),
    packages/database/src/migrations/0012_agents_dblink_handoff_context.sql and
    packages/database/src/migrations/meta/_journal.json (from Plan 34-01 — the exact SQL
    content and journal entry this test's assertions must match)
  </read_first>
  <action>
    Create packages/database/src/__tests__/integration/agents.integration.test.ts. Gate all
    tests with `const TEST_DB_URL = process.env.POSTGRES_URL ?? process.env.TEST_DATABASE_URL;
    const describeOrSkip = TEST_DB_URL ? describe : describe.skip;`. In beforeAll, open a
    `postgres(TEST_DB_URL, { prepare: false })` connection and insert two fixture rows into
    `agents` with `ON CONFLICT (name) DO NOTHING`: one named 'test-agent-enabled' with
    enabled=true, one named 'test-agent-disabled' with enabled=false (both with a placeholder
    connection_string like 'host=x dbname=y' and brain_type 'support'). In afterAll, delete
    both fixture rows by name and close the connection. Write three test cases calling
    getAgentConnection(sql, ...): a nonexistent name resolves to
    { ok: false, reason: 'not_found' }; 'test-agent-disabled' resolves to
    { ok: false, reason: 'disabled' }; 'test-agent-enabled' resolves to
    { ok: true, connectionString: 'host=x dbname=y', brainType: 'support' }.

    Create packages/database/src/__tests__/integration/migration-0012.test.ts mirroring
    migration-v14.test.ts's structure exactly (no live DB needed — pure fs assertions):
    constants for MIGRATIONS_DIR, SQL_FILE (0012_agents_dblink_handoff_context.sql),
    JOURNAL_FILE, TABLES_FILE. Assert: the journal contains an entry with tag
    "0012_agents_dblink_handoff_context" and idx 12; the SQL file exists; the SQL file
    contains, in order, "CREATE EXTENSION IF NOT EXISTS dblink", 'CREATE TABLE "agents"',
    '"connection_string"', '"brain_type"', '"enabled"', and 'ALTER TABLE "leads" ADD COLUMN
    "handoff_context"'; tables.ts contains "export const agents" and "handoffContext".

    [Closing Phase 33's flagged gap] Provision a fresh, independent, throwaway Postgres 17
    container (same image/approach as Plan 34-01's tracer task — do not assume that plan's
    container is still running, it was torn down): remove any stale container, start a new
    one named brain-phase34-test-pg from pgvector/pgvector:pg17 on host port 5434 with
    POSTGRES_PASSWORD=postgres/POSTGRES_DB=brain_test, wait for readiness, then from
    packages/database run `bun src/migrate.ts` against it (DATABASE_URL exported) so agents /
    leads.handoff_context / dblink all exist for real. Then run the FULL packages/database
    test suite with TEST_DATABASE_URL exported to that same connection string
    (`TEST_DATABASE_URL=... bun test`), confirming agents.integration.test.ts's three cases
    and seed-idempotency.test.ts's cases actually PASS this run (not skip) — this is the exact
    real-Postgres gap Phase 33's VERIFICATION.md flagged as human_needed, now closed with a
    real, non-skipped run. Finally tear the container down
    (`docker rm -f brain-phase34-test-pg`).
  </action>
  <verify>
    <automated>
cd packages/database
docker rm -f brain-phase34-test-pg 2>/dev/null || true
docker run -d --name brain-phase34-test-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=brain_test -p 5434:5432 pgvector/pgvector:pg17
for i in $(seq 1 30); do docker exec brain-phase34-test-pg pg_isready -U postgres >/dev/null 2>&1 && break; sleep 1; done
export DATABASE_URL="postgresql://postgres:postgres@localhost:5434/brain_test"
bun src/migrate.ts
TEST_DATABASE_URL="$DATABASE_URL" bun test
docker rm -f brain-phase34-test-pg
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/__tests__/integration/agents.integration.test.ts exists under
      __tests__/integration/ and contains both "describeOrSkip" and "TEST_DATABASE_URL"
    - packages/database/src/__tests__/integration/migration-0012.test.ts exists and asserts
      idx 12, the tag, the SQL file's CREATE EXTENSION/CREATE TABLE/ALTER TABLE content, and
      tables.ts's `agents`/`handoffContext` exports
    - the full-suite run with TEST_DATABASE_URL set shows agents.integration.test.ts's 3 test
      cases PASSING (0 skipped) — not the "0 pass / 3 skip" outcome Phase 33 left as
      human_needed
    - migration-0012.test.ts passes with 0 failures
    - `docker ps -a` after the task shows no brain-phase34-test-pg container remaining
  </acceptance_criteria>
  <done>
    Both new test files exist and pass; HANDOFF-04's integration behavior and HANDOFF-01/02's
    migration-file content are proven against a real, freshly-provisioned Postgres 17
    instance in this same execution (not skipped for lack of a test database), with no
    residual container left running afterward.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `getAgentConnection()`'s `name` parameter → the `agents` table query | `name` is not yet attacker-controlled in this phase (no tool/LLM call site exists — Phase 35), but the function is written defensively as if it will be |
| `getAgentConnection()`'s return value → its (currently nonexistent) callers | The function intentionally returns `connectionString` to its in-process caller — this is by design (Phase 35 needs it to drive `dblink`), distinct from logging it, which remains forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-04 | Tampering | `getAgentConnection(sql, name)`'s `name` parameter | medium | mitigate | Parameterized query via Drizzle's `eq(agents.name, name)` exclusively — never raw string interpolation or `sql.unsafe()`; unit test (Task 1) asserts the source contains no `sql.unsafe` occurrence tied to `name`. |
| T-34-05 | Information Disclosure | `AgentConnectionResult`/the full `agents` row, if ever logged via `pino` | high | mitigate | Never log the full row or result object in `agents.ts` or its tests — only `name`/`brainType` are loggable fields (RESEARCH.md Pitfall 2); acceptance criteria for Task 1 assert no `console.log`/`JSON.stringify` of the full object. |
</threat_model>

<verification>
Run `bun test src/__tests__/unit/agents.test.ts` (mocked, no DB) — 3/3 pass. Then run Task
2's automated verify script: provisions a scratch Postgres 17 container independent of Plan
34-01's (already torn down), applies the full migration chain, runs the full
`packages/database` suite with `TEST_DATABASE_URL` set, confirms `agents.integration.test.ts`
and `migration-0012.test.ts` both pass with zero skips, then tears the container down.
</verification>

<success_criteria>
`getAgentConnection()` correctly resolves an unknown name, a disabled name, and a valid
enabled name against a real Postgres instance (not just a mocked one) in this same execution,
closing the exact "no real test DB" gap Phase 33's verification left as human_needed.
HANDOFF-10 is explicitly documented as N/A in this phase's code, carried forward as a locked
constraint for Phase 35.
</success_criteria>

<output>
Create `.planning/phases/34-funda-o-de-handoff-agents-dblink/34-02-SUMMARY.md` when done
</output>
