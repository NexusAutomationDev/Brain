---
phase: 34-funda-o-de-handoff-agents-dblink
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/tables.ts
  - packages/database/src/migrations/0012_agents_dblink_handoff_context.sql
  - packages/database/src/migrations/meta/_journal.json
  - packages/database/src/migrations/meta/0012_snapshot.json
autonomous: false
requirements: [HANDOFF-01, HANDOFF-02]

must_haves:
  truths:
    - "O schema de qualquer Brain inclui uma tabela `agents` com colunas name (PK), brain_type, connection_string, enabled (default true), created_at, updated_at — populável via INSERT SQL direto, sem redeploy (HANDOFF-01)"
    - "A migration compartilhada 0012 executa `CREATE EXTENSION IF NOT EXISTS dblink` automaticamente durante `runMigrations()` — um banco novo de cliente já tem dblink disponível sem qualquer ativação manual (HANDOFF-02)"
    - "Uma tabela `agents` vazia (zero linhas) é um estado válido, não um erro — SELECT * FROM agents não falha em um banco recém-migrado sem nenhum agente configurado ainda (edge: empty)"
    - "Igualdade de `agents.name` segue a collation text padrão do Postgres (comparação byte-a-byte, mesma semântica já usada por fupConfig.brainType/prompts.brainType) — nenhuma nova semântica de encoding introduzida por esta fase (edge: encoding)"
    - "Múltiplas instâncias do mesmo Brain subindo concorrentemente contra o mesmo banco novo: o `_schema_lock` row-lock existente de `runMigrations()` (FOR UPDATE NOWAIT, 3 tentativas com 200ms de backoff), inalterado por esta fase, garante que só uma instância aplica a migration 0012 por vez — mesmo mecanismo já usado pelas migrations 0000-0011 (edge: concurrency)"
    - "Re-rodar `runMigrations()` contra um banco onde a migration 0012 já foi aplicada é um no-op — o próprio Drizzle marca a migration como aplicada (hash em __drizzle_migrations) e não a reexecuta; `CREATE EXTENSION IF NOT EXISTS` é idempotente por definição mesmo em reexecução manual (edge: idempotency, HANDOFF-02)"
    - "A migration 0012 aplica com sucesso, do zero, contra uma instância Postgres 17 real e recém-provisionada nesta mesma execução (não mockada, não apenas type-checked) — confirmado via inspeção `psql` direta, fechando a mesma classe de gap que a verificação da Phase 33 deixou como human_needed por falta de Postgres real no sandbox daquela sessão"
  artifacts:
    - packages/database/src/schema/tables.ts
    - packages/database/src/migrations/0012_agents_dblink_handoff_context.sql
    - packages/database/src/migrations/meta/_journal.json
    - packages/database/src/migrations/meta/0012_snapshot.json
  key_links:
    - "runMigrations() (packages/database/src/migrate.ts), inalterado, aplica a migration 0012 dentro da mesma transação _schema_lock row-locked já existente — nenhum novo mecanismo de lock introduzido"
    - "A tabela `agents` e a coluna `leads.handoffContext` ficam definidas em tables.ts sem nenhum leitor/escritor nesta fase — Plan 34-02 conecta getAgentConnection(); Phase 35 conecta o fluxo de leitura/limpeza de handoff_context"
    - "BrainRunner.init() já invoca runMigrations() no boot de qualquer Brain (Phase 6) — nenhuma mudança de código em runner.ts é necessária para a migration 0012 ser aplicada automaticamente em todo Brain existente"
---

## Artifacts this phase produces

New files/symbols created by this plan (exclude these from drift/source-grounding checks —
they do not exist yet in the codebase):

- `packages/database/src/schema/tables.ts` — new export `agents` (pgTable), plus a new
  `handoffContext: text('handoff_context')` nullable column added to the existing `leads`
  pgTable definition
- `packages/database/src/migrations/0012_agents_dblink_handoff_context.sql` — new migration
  file (generated via `drizzle-kit generate`, hand-edited to prepend `CREATE EXTENSION IF NOT
  EXISTS dblink;`)
- New entry `{ idx: 12, tag: "0012_agents_dblink_handoff_context", ... }` in
  `packages/database/src/migrations/meta/_journal.json`
- `packages/database/src/migrations/meta/0012_snapshot.json` — new, auto-generated by
  `drizzle-kit generate`

Modified (not new) files: none beyond the above (`_journal.json` gains one entry; no existing
entries are altered).

<assumption_delta_decision>
Signal: the phase introduces the *first* `agents` table — a plausible pluralization signal
("multiple known Brain destinations" is a new concept) that the assumption-delta detector
flagged (matched on the unrelated term "configurable" inside HANDOFF-10's
`config.configurable` reference text — a false-positive term match, not a genuine
pluralization instance in that specific spot).

Decision: **promote**. `agents` becomes the primary, canonical registry of Brain destinations
for the DBLink-based handoff mechanism (HANDOFF-01/02/04/10 here; HANDOFF-03/05-09 in Phase
35). There is no pre-existing singular-destination field, table, or implicit assumption
anywhere else in this codebase to reconcile or keep "alongside" — before this phase, a Brain
instance has no concept of any other Brain as a transfer destination at all. This is a clean,
net-new registry, not a migration of an existing singular concept into a plural one, so
"promote" is the only applicable outcome (there is nothing to add this alongside).
</assumption_delta_decision>

<objective>
Build the shared, purely-additive PostgreSQL schema foundation for lead handoff: a new
`agents` table (the destination registry, HANDOFF-01), the `CREATE EXTENSION IF NOT EXISTS
dblink` bootstrap folded into the same shared migration (HANDOFF-02), and the nullable
`leads.handoff_context` column that Phase 35 will read/clear. All three land in ONE new
migration file (`0012_agents_dblink_handoff_context.sql`) applied by the existing, unmodified
`runMigrations()`/`_schema_lock` mechanism — and this plan proves the migration actually
applies, end-to-end, against a real, freshly-provisioned Postgres instance (not just
type-checked), closing the exact "no real test DB available" gap Phase 33's verification left
open.

This is the tracer for the whole phase: schema definition → migration generation → hand-edit
→ real-DB apply → psql-confirmed proof, all in one task, before Plan 34-02 builds
`getAgentConnection()` on top of it.

Purpose: HANDOFF-01/02 are the actual product requirement (a configurable, SQL-editable
destination registry + a working `dblink` extension in every client database); this plan's
real-Postgres proof is the safety rail that makes the migration safe to ship inside every
future Brain Docker image.
Output: `agents` pgTable + `leads.handoffContext` column in `tables.ts`, migration
`0012_agents_dblink_handoff_context.sql` + updated `meta/_journal.json`/`meta/0012_snapshot.json`,
and a real, non-mocked confirmation (via `psql`) that the table, column, and extension all
exist after a from-scratch migration run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-CONTEXT.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-RESEARCH.md
@.planning/phases/34-funda-o-de-handoff-agents-dblink/34-PATTERNS.md

@packages/database/src/schema/tables.ts
@packages/database/src/migrate.ts
@packages/database/src/migrations/0007_v1_4_foundation.sql
@packages/database/src/migrations/meta/_journal.json
@packages/database/drizzle.config.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm agents table shape + shared-migration placement before generating migration 0012 (D-01/D-04, one-way)</name>
  <decision>Proceed with the researched `agents` table shape (name text PK, brain_type, connection_string, enabled default true, created_at/updated_at) and bundle dblink + agents + leads.handoff_context into ONE new shared migration `0012_agents_dblink_handoff_context.sql`, applied by the existing `runMigrations()` Drizzle migrator?</decision>
  <context>
    D-01 (agents column shape, rated costly) and D-04 (migration placement/application, rated
    one-way) were recorded during a `/gsd-discuss-phase --auto` session — Claude's own
    recommended defaults, auto-selected without a live human confirming them. Once this
    migration is generated and applied to any real client database via the shared
    `runMigrations()`/`_schema_lock` mechanism baked into every Brain Docker image, reverting
    or reshaping it requires writing and shipping a brand-new follow-up migration — deleting
    the file after the fact does not undo an already-applied production migration. This is
    the phase's one one-way door; every other decision in this phase (D-02 connection-string
    format, D-06/D-07 lookup function shape, handled in Plan 34-02) is reversible/local by
    comparison.
  </context>
  <options>
    <option id="option-a">
      <name>Proceed with the researched shape (recommended)</name>
      <pros>Matches every existing table-shape idiom already in this codebase (fupConfig's
      text-PK precedent); zero new mechanism (reuses runMigrations()/_schema_lock unmodified);
      matches the locked DBLink architecture decision (D-05); unblocks Phase 35 immediately;
      HIGH-confidence research with a directly-read drizzle-orm source-code proof that dblink
      can safely share the migration's own transaction.</pros>
      <cons>One-way once shipped: changing agents' columns, splitting dblink into its own
      migration, or renaming handoff_context later all require a brand-new migration touching
      every deployed client database, not a file edit.</cons>
    </option>
    <option id="option-b">
      <name>Hold — revise the shape before generating any migration file</name>
      <pros>Avoids commitment if there is an unstated concern about the column set, PK choice,
      or migration placement.</pros>
      <cons>Contradicts CONTEXT.md's explicit D-01/D-02/D-04 decisions without any new
      information surfacing a problem with them; delays Phase 35, which depends on this schema
      existing; no concrete alternative shape has been proposed by research or context.</cons>
    </option>
  </options>
  <resume-signal>Reply "option-a" to proceed with the researched schema and generate/apply
  migration 0012, or "option-b" to pause and revise the shape first.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: agents table + leads.handoff_context column + dblink extension — migration 0012, applied and verified against a real Postgres</name>
  <reversibility rating="one-way">D-04: once this migration is applied to any production
  client database via the existing shared migrator, it cannot be undone by deleting the file
  — a new migration is required. D-01's column shape (name PK, brain_type, connection_string,
  enabled, timestamps) is bundled into this same one-way commit — changing it later also
  requires a coordinated follow-up migration across every deployed client database
  (costly).</reversibility>
  <precondition>Docker is reachable (`docker ps` exits 0) and the `pgvector/pgvector:pg17`
  image is available (present locally via `docker images` or pullable); host port 5434 is
  free.</precondition>
  <files>packages/database/src/schema/tables.ts, packages/database/src/migrations/0012_agents_dblink_handoff_context.sql, packages/database/src/migrations/meta/_journal.json, packages/database/src/migrations/meta/0012_snapshot.json</files>
  <read_first>
    packages/database/src/schema/tables.ts lines ~134-150 (fupConfig — the text-PK idiom to
    copy for agents.name) and lines ~77-110 (leads — the nullable-column idiom to copy for
    handoffContext, e.g. how fupNextAt/lastMessageAt are declared without .notNull()),
    packages/database/src/migrations/0007_v1_4_foundation.sql (the precedent for bundling
    multiple CREATE TABLE + ALTER TABLE statements in one file with `--> statement-breakpoint`
    separators),
    packages/database/src/migrations/meta/_journal.json (entry shape: idx, version, when, tag,
    breakpoints — confirm the new idx=12 entry drizzle-kit generates matches this shape),
    packages/database/src/migrate.ts lines 30-41 (the existing `CREATE EXTENSION IF NOT
    EXISTS vector` precedent, run OUTSIDE the migrator's own transaction because vector is a
    column TYPE referenced by table DDL — confirms why dblink, which provides only functions
    never referenced by this migration's own DDL, can safely live INSIDE the new migration
    file instead; do not move dblink into migrate.ts),
    packages/database/drizzle.config.ts (dbCredentials.url reads process.env.DATABASE_URL —
    must be exported before running `drizzle-kit generate`/`bun src/migrate.ts`),
    .planning/phases/34-funda-o-de-handoff-agents-dblink/34-RESEARCH.md sections "Pattern 1",
    "Pattern 2", and "Code Examples → Migration file (complete)" (the exact literal SQL/TS to
    reproduce),
    .planning/phases/34-funda-o-de-handoff-agents-dblink/34-PATTERNS.md sections on
    `tables.ts` and the migration file (literal code excerpts already extracted from this
    codebase)
  </read_first>
  <action>
    In packages/database/src/schema/tables.ts: add a new export `agents` as a pgTable named
    'agents', positioned after the existing `fupConfig` export, with exactly these columns
    (per D-01): `name` — text, primaryKey (no separate UUID, mirroring fupConfig.brainType's
    text-PK idiom); `brainType` — text('brain_type'), notNull; `connectionString` —
    text('connection_string'), notNull (D-02: libpq key=value format, e.g. "host=...
    dbname=... user=... password=...", NOT a postgres:// URI — no parsing/validation in this
    phase); `enabled` — boolean('enabled'), notNull, default(true); `createdAt` —
    timestamp('created_at'), defaultNow(), notNull; `updatedAt` — timestamp('updated_at'),
    defaultNow(), notNull. Add a comment above the export citing HANDOFF-01/D-01. Then add a
    new nullable column to the existing `leads` pgTable (do NOT create a new table, do NOT
    add `.notNull()`): `handoffContext: text('handoff_context')`, positioned near the other
    nullable columns (after fupFailureCount), with a comment citing HANDOFF-01/02 and noting
    it is read/cleared only in Phase 35.

    Provision a throwaway Postgres 17 instance to generate and apply the migration against
    (needed because drizzle.config.ts's dbCredentials.url reads process.env.DATABASE_URL, and
    because the [BLOCKING] step below requires a real database, not a mock): remove any stale
    container first, then start one named brain-phase34-test-pg from the pgvector/pgvector:pg17
    image, mapping host port 5434 to container port 5432, with POSTGRES_PASSWORD=postgres and
    POSTGRES_DB=brain_test, and wait (poll `docker exec brain-phase34-test-pg pg_isready -U
    postgres`) until it reports ready.

    With DATABASE_URL exported as
    postgresql://postgres:postgres@localhost:5434/brain_test, from the packages/database
    directory run `bunx drizzle-kit generate --name agents_dblink_handoff_context` (the
    `--name` flag is confirmed supported by the installed drizzle-kit 0.31.10 — this produces
    packages/database/src/migrations/0012_agents_dblink_handoff_context.sql,
    meta/0012_snapshot.json, and appends the idx=12 entry to meta/_journal.json
    automatically, with no manual renaming needed).

    Hand-edit the generated 0012_agents_dblink_handoff_context.sql: prepend
    "CREATE EXTENSION IF NOT EXISTS dblink;" followed by its own "--> statement-breakpoint"
    line as the very first lines of the file, before the auto-generated `CREATE TABLE
    "agents"` statement — per Pattern 1/D-04, this line must come first even though dblink
    provides no column type this migration's own DDL depends on. Do not alter anything else
    drizzle-kit generated (the CREATE TABLE "agents" and ALTER TABLE "leads" ADD COLUMN
    "handoff_context" statements should already match D-01's column list verbatim from the
    tables.ts diff).

    [BLOCKING] Apply the FULL migration chain (0000 through the new 0012) against the same
    fresh scratch database, proving the whole chain — including this new file — applies
    cleanly end-to-end via the project's own mechanism, not a mock: from packages/database,
    with the same DATABASE_URL exported, run `bun src/migrate.ts`. This must complete with
    exit code 0 and print "Migrations completed successfully".

    Then confirm via direct psql inspection against that same scratch database (never via
    TypeScript types compiling, never via drizzle-kit's dry diff alone): run `psql
    "$DATABASE_URL" -c '\d agents'` and confirm it lists all six columns (name, brain_type,
    connection_string, enabled, created_at, updated_at); run `psql "$DATABASE_URL" -c '\d
    leads'` and confirm "handoff_context" appears; run `psql "$DATABASE_URL" -c "SELECT
    extname FROM pg_extension WHERE extname='dblink';"` and confirm exactly one row is
    returned.

    Finally, tear down the scratch container (`docker rm -f brain-phase34-test-pg`) — leave
    no residual container running; Plan 34-02 provisions its own independent instance.
  </action>
  <verify>
    <automated>
cd packages/database
docker rm -f brain-phase34-test-pg 2>/dev/null || true
docker run -d --name brain-phase34-test-pg -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=brain_test -p 5434:5432 pgvector/pgvector:pg17
for i in $(seq 1 30); do docker exec brain-phase34-test-pg pg_isready -U postgres >/dev/null 2>&1 && break; sleep 1; done
export DATABASE_URL="postgresql://postgres:postgres@localhost:5434/brain_test"
bunx drizzle-kit generate --name agents_dblink_handoff_context
bun src/migrate.ts
psql "$DATABASE_URL" -c '\d agents' | grep -q connection_string && echo "AGENTS_TABLE_OK"
psql "$DATABASE_URL" -c '\d leads' | grep -q handoff_context && echo "HANDOFF_CONTEXT_OK"
psql "$DATABASE_URL" -c "SELECT extname FROM pg_extension WHERE extname='dblink';" | grep -q dblink && echo "DBLINK_EXT_OK"
docker rm -f brain-phase34-test-pg
    </automated>
  </verify>
  <acceptance_criteria>
    - packages/database/src/schema/tables.ts contains "export const agents = pgTable('agents'"
    - the agents table definition in tables.ts contains "connection_string", "brain_type", and
      "enabled: boolean('enabled').notNull().default(true)"
    - the leads pgTable definition in tables.ts contains a line
      "handoffContext: text('handoff_context')," with no ".notNull()" on that line
    - packages/database/src/migrations/0012_agents_dblink_handoff_context.sql exists and, in
      order, contains "CREATE EXTENSION IF NOT EXISTS dblink" before 'CREATE TABLE "agents"'
      before 'ALTER TABLE "leads" ADD COLUMN "handoff_context"'
    - packages/database/src/migrations/meta/_journal.json contains an entry with
      "tag": "0012_agents_dblink_handoff_context" and "idx": 12
    - the verify script's three echo markers (AGENTS_TABLE_OK, HANDOFF_CONTEXT_OK,
      DBLINK_EXT_OK) all print, proving real-Postgres confirmation rather than a type-check
    - `docker ps -a` after the task shows no brain-phase34-test-pg container remaining
  </acceptance_criteria>
  <done>
    agents table + leads.handoff_context column + dblink extension are all defined in a
    single new migration 0012_agents_dblink_handoff_context.sql, applied successfully (not
    skipped, not mocked) against a real freshly-provisioned Postgres 17 instance via the
    project's own runMigrations()/bun src/migrate.ts mechanism, and confirmed present via
    direct psql inspection; the scratch container is torn down afterward.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `agents.connection_string` (this phase's storage) → a destination Postgres, reached via `dblink` | Not exercised by this phase's code (no `dblink_exec` call site exists yet — Phase 35), but the credential is stored here for the first time |
| Migration batch (0012) → Brain process startup | A failure anywhere in the batch (e.g. `CREATE EXTENSION dblink` permission error) blocks the entire migration transaction, including the harmless `agents`/`handoff_context` DDL, and propagates to `BrainRunner.init()`'s throw-not-exit contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | `agents.connection_string` stored in plain text (no encryption-at-rest) | medium | accept | D-03 accepted risk — identical posture already accepted for `ADMIN_TOKEN`/`DATABASE_URL` (plain ENV) and `fup_config`/`prompts` (SQL-editable, no UI). Not a new gap introduced by this phase; documented here per D-03. |
| T-34-02 | Elevation of Privilege | `dblink` extension's functions are PUBLIC-executable by default once `CREATE EXTENSION dblink` runs | medium | accept | Not exploitable today — this project uses exactly one DB role per tenant (already effectively trusted with everything); no code in this phase or the next task calls `dblink_exec`. Forward-looking note for Phase 35/future role-separation hardening (RESEARCH.md Pitfall 3) — no `REVOKE` needed now. |
| T-34-03 | Denial of Service | `CREATE EXTENSION IF NOT EXISTS dblink` requires superuser (no `trusted = true` in dblink.control, confirmed via direct image inspection); migration 0012's single-transaction batch means a permission failure here also blocks the harmless `agents`/`handoff_context` DDL from applying, preventing Brain startup on a privilege-restricted deployment | low | accept | Not a new risk — identical to the pre-existing `vector` extension requirement already relied upon in production by every currently-deployed Brain (`migrate.ts`); document the superuser precondition once, generically, rather than solving narrowly for dblink (RESEARCH.md Pitfall 1). |
| T-34-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Not applicable — this phase installs no package-manager dependency; `dblink` is a native PostgreSQL contrib extension enabled via SQL, not an npm/pip/cargo package (RESEARCH.md's Package Legitimacy Audit confirms N/A). |
</threat_model>

<verification>
Run the tracer task's automated verify script end-to-end: it provisions a scratch Postgres
17 container, generates and hand-edits migration 0012, applies the full migration chain via
`bun src/migrate.ts`, confirms via `psql` that `agents`, `leads.handoff_context`, and the
`dblink` extension all exist, then tears the container down. All three echo markers
(AGENTS_TABLE_OK, HANDOFF_CONTEXT_OK, DBLINK_EXT_OK) must print.
</verification>

<success_criteria>
A fresh, from-scratch Postgres 17 database, after running the project's own
`runMigrations()`/`bun src/migrate.ts` mechanism through migration 0012, has: an `agents`
table with all six D-01 columns; a `leads.handoff_context` nullable text column; and the
`dblink` extension installed — all three confirmed via direct `psql` inspection, not by
inspection of TypeScript types or a dry `drizzle-kit generate` diff alone.
</success_criteria>

<output>
Create `.planning/phases/34-funda-o-de-handoff-agents-dblink/34-01-SUMMARY.md` when done
</output>
