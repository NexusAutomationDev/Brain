---
phase: 20-tool-events
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/events/event-publisher.ts
  - packages/core/src/events/__tests__/unit/event-publisher.test.ts
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-04

must_haves:
  truths:
    - "EventPublisher em modo webhook chama fetch() com payload correto para TOOL_EVENTS_URL"
    - "EventPublisher em modo RabbitMQ envia evento via pub.send() para TOOL_EVENTS_QUEUE"
    - "Falha em fetch() ou pub.send() é absorvida com logger.warn — nunca relança para o caller"
    - "Sem TOOL_EVENTS_URL nem TOOL_EVENTS_QUEUE: construtor lança ConfigurationError (EventPublisher nunca é instanciado nesse caso — BrainRunner guarda)"
    - "event_id = 'threadId:tool_call_id' — constante para o mesmo tool call (EVT-04)"
    - "Whitelist { qualify_lead, pause_session, finish_conversation } — tools fora da lista não geram evento"
    - "ToolMessage.name undefined não passa pela whitelist (guard typeof === string)"
    - "ToolMessage.content do tipo array é serializado com JSON.stringify antes de entrar em result"
  artifacts:
    - path: "packages/core/src/events/event-publisher.ts"
      provides: "IEventPublisher interface, ToolEvent type, EventPublisher class, NoopEventPublisher class"
      exports:
        - IEventPublisher
        - ToolEvent
        - EventPublisher
        - NoopEventPublisher
    - path: "packages/core/src/events/__tests__/unit/event-publisher.test.ts"
      provides: "Cobertura unitária de EVT-01 (webhook + rabbitmq + disabled), EVT-02 (whitelist), EVT-04 (event_id)"
  key_links:
    - from: "packages/core/src/events/event-publisher.ts"
      to: "rabbitmq-client"
      via: "new Connection(RABBITMQ_URL) + createPublisher({ confirm: true })"
      pattern: "createPublisher.*confirm.*true"
    - from: "packages/core/src/events/event-publisher.ts"
      to: "Bun fetch"
      via: "fetch(webhookUrl, { signal: AbortSignal.timeout(5000) })"
      pattern: "AbortSignal.timeout"
---

<objective>
Implementar `IEventPublisher`, `EventPublisher` e `NoopEventPublisher` em `packages/core/src/events/`, com cobertura de testes unitários completa via TDD.

Purpose: Criar o componente isolado de publicação de eventos antes de integrá-lo ao BrainRunner (Plan 02). A separação permite testar o publisher sem instanciar o BrainRunner.

Output: `event-publisher.ts` com interface + duas implementações, e `event-publisher.test.ts` cobrindo EVT-01, EVT-02 e EVT-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-tool-events/20-CONTEXT.md
@.planning/phases/20-tool-events/20-RESEARCH.md

<interfaces>
<!-- Padrões estabelecidos que o executor deve replicar. Extraídos do codebase. -->

De packages/transport/src/rabbitmq/consumer.ts (padrão rabbitmq-client a replicar):
```typescript
import { Connection } from "rabbitmq-client";
// Criação:
this.rabbit = new Connection(url);
this.pub = this.rabbit.createPublisher({ confirm: true });
// Envio:
await this.pub.send(queueName, body);  // body pode ser objeto JS — serializado pela lib
// Cleanup:
await this.pub?.close();
await this.rabbit?.close();
// Segurança: NUNCA logar process.env.RABBITMQ_URL (contém credenciais)
// Padrão de ConfigurationError:
throw new ConfigurationError("mensagem", { hasUrl: !!url, hasQueue: !!queue });
```

De packages/core/src/runner/runner.ts (padrão mcpClient — injeção opcional):
```typescript
// Campo privado anulável:
private mcpClient: MultiServerMCPClient | null = null;
// BrainRunnerOptions com campo opcional:
export interface BrainRunnerOptions {
  // ...
  mcpClient?: MultiServerMCPClient; // opcional para testes
}
```

De packages/shared (erros disponíveis):
```typescript
import { ConfigurationError } from "@brain-pkg/shared";
// ConfigurationError(message: string, context: Record<string, unknown>)
```

De @brain-pkg/observability (logger):
```typescript
import { createLogger } from "@brain-pkg/observability";
// Retorno: { info, warn, error, debug } — estruturado com pino
// Em falhas de publicação: logger.warn({ err, eventId }, "mensagem")
// Nunca logar payload completo (PII: nome, numero)
```

Estrutura ToolEvent (de CONTEXT.md D-02, D-05):
```typescript
export interface ToolEvent {
  event_id: string;    // `${threadId}:${toolCallId}`  (EVT-04)
  action: string;      // "qualify_lead" | "pause_session" | "finish_conversation"
  lead: {
    id: string;        // lead.uniqueId
    nome: string | null;
    numero: string;
  };
  result: string;      // ToolMessage.content raw (D-05); JSON.stringify se for array
  timestamp: string;   // new Date().toISOString()
}
```

Padrão de teste em packages/core (de brain-runner.test.ts):
```typescript
import { describe, test, expect, mock, beforeEach } from "bun:test";
// mock.module() para módulos externos
// Testes em packages/core/src/events/__tests__/unit/
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD — IEventPublisher + EventPublisher (webhook e RabbitMQ)</name>
  <files>
    packages/core/src/events/event-publisher.ts
    packages/core/src/events/__tests__/unit/event-publisher.test.ts
  </files>

  <read_first>
    - packages/transport/src/rabbitmq/consumer.ts (padrão Connection + createPublisher a replicar)
    - packages/core/src/runner/runner.ts (padrão logger e ConfigurationError)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (padrão mock.module e setup de testes)
  </read_first>

  <behavior>
    Cenários RED→GREEN que devem ser escritos ANTES da implementação:

    **EVT-01 — Webhook:**
    - `EventPublisher` instanciado com `TOOL_EVENTS_URL="http://example.com/events"` (sem `TOOL_EVENTS_QUEUE`):
      `publish([event])` chama `globalThis.fetch` exatamente 1 vez com method "POST", body JSON do evento e Content-Type "application/json"
    - `publish()` absorve erro de fetch silenciosamente: mock `fetch` lançando `new Error("ECONNREFUSED")` → `publish()` resolve sem lançar

    **EVT-01 — RabbitMQ:**
    - `EventPublisher` instanciado com `TOOL_EVENTS_QUEUE="tool-events"` e `RABBITMQ_URL="amqp://..."`:
      `publish([event])` chama `pub.send("tool-events", event)` exatamente 1 vez
    - `TOOL_EVENTS_QUEUE` presente mas `RABBITMQ_URL` ausente → construtor lança `ConfigurationError`
    - RabbitMQ tem prioridade (D-06): `TOOL_EVENTS_QUEUE` e `TOOL_EVENTS_URL` ambos presentes → usa RabbitMQ, não chama `fetch`
    - `publish()` absorve erro de `pub.send()` silenciosamente: mock `pub.send` lançando → resolve sem lançar

    **EVT-01 — Disabled / NoopEventPublisher:**
    - `NoopEventPublisher.publish([])` resolve sem lançar (no-op)
    - `NoopEventPublisher.close()` resolve sem lançar

    **EVT-02 — Whitelist de Tools:**
    - Somente para documentar que o filtro de whitelist pertence ao BrainRunner (Plan 02), não ao EventPublisher.
      O EventPublisher recebe `events: ToolEvent[]` já filtrados — testar que `publish([])` (array vazio) não chama fetch nem pub.send.

    **EVT-04 — event_id idempotente:**
    - `publish` é chamado 2x com `event_id = "lead-abc:call-xyz"` → fetch chamado 2x com o mesmo `event_id` nos dois bodies (publisher não modifica o event_id recebido)

    **Segurança — sem PII em logs:**
    - Quando fetch lança, logger.warn deve ser chamado com `eventId` e `err`, mas NÃO com `lead.nome`, `lead.numero` ou `lead.id` no objeto logado
  </behavior>

  <action>
    **CICLO TDD OBRIGATÓRIO:**

    **FASE RED:**
    Criar o arquivo de teste PRIMEIRO em `packages/core/src/events/__tests__/unit/event-publisher.test.ts`.

    Setup do arquivo de teste:
    - Importar `describe, test, expect, mock, beforeEach, afterEach` de `"bun:test"`
    - Usar `mock.module("rabbitmq-client", ...)` para mockar `Connection` — o mock de `Connection` deve retornar um objeto com `createPublisher` que retorna `{ send: mockSend, close: mock(async () => {}) }`
    - Mockar `@brain-pkg/observability` com `createLogger` retornando `{ warn: mockWarn, info: mock(()=>{}), error: mock(()=>{}), debug: mock(()=>{}) }`
    - Mockar `@brain-pkg/shared` com `ConfigurationError` como classe que estende `Error`
    - Usar `beforeEach` para limpar `process.env.TOOL_EVENTS_URL`, `process.env.TOOL_EVENTS_QUEUE`, `process.env.RABBITMQ_URL` e resetar `globalThis.fetch`
    - Importar `EventPublisher, NoopEventPublisher` de `"../../event-publisher.js"` APÓS os mocks

    Escrever todos os cenários da seção `<behavior>`. Rodar: `bun test packages/core/src/events/__tests__/unit/event-publisher.test.ts`
    Confirmar: TODOS os testes falham com "Cannot find module" ou erro de implementação (RED confirmado).

    **FASE GREEN:**
    Criar `packages/core/src/events/event-publisher.ts` com:

    ```typescript
    import { Connection } from "rabbitmq-client";
    import { createLogger } from "@brain-pkg/observability";
    import { ConfigurationError } from "@brain-pkg/shared";

    export interface ToolEvent {
      event_id: string;
      action: string;
      lead: { id: string; nome: string | null; numero: string };
      result: string;
      timestamp: string;
    }

    export interface IEventPublisher {
      publish(events: ToolEvent[]): Promise<void>;
      close(): Promise<void>;
    }

    export class NoopEventPublisher implements IEventPublisher {
      async publish(_events: ToolEvent[]): Promise<void> {}
      async close(): Promise<void> {}
    }

    export class EventPublisher implements IEventPublisher {
      private readonly logger = createLogger();
      private readonly mode: "webhook" | "rabbitmq";
      private readonly webhookUrl?: string;
      private readonly queue?: string;
      private rabbit?: InstanceType<typeof Connection>;
      private pub?: ReturnType<InstanceType<typeof Connection>["createPublisher"]>;

      constructor() {
        const queue = process.env.TOOL_EVENTS_QUEUE?.trim();
        const webhookUrl = process.env.TOOL_EVENTS_URL?.trim();

        if (queue) {
          // D-06: RabbitMQ tem prioridade — ignora webhookUrl quando queue presente
          if (!process.env.RABBITMQ_URL) {
            throw new ConfigurationError(
              "TOOL_EVENTS_QUEUE requer RABBITMQ_URL configurado",
              { hasQueue: true, hasRabbitUrl: false }
            );
          }
          this.mode = "rabbitmq";
          this.queue = queue;
        } else if (webhookUrl) {
          this.mode = "webhook";
          this.webhookUrl = webhookUrl;
        } else {
          // BrainRunner.init() só cria EventPublisher quando pelo menos um ENV está presente
          // Este branch indica uso incorreto do construtor diretamente
          throw new ConfigurationError(
            "EventPublisher requer TOOL_EVENTS_QUEUE ou TOOL_EVENTS_URL",
            { hasQueue: false, hasUrl: false }
          );
        }
      }

      /** Inicializa conexão RabbitMQ eagerly. Chamado por BrainRunner.init() após construção. */
      async init(): Promise<void> {
        if (this.mode === "rabbitmq") {
          this.rabbit = new Connection(process.env.RABBITMQ_URL!);
          // D-09: reutiliza RABBITMQ_URL existente; confirm:true aguarda ack do broker
          this.pub = this.rabbit.createPublisher({ confirm: true });
        }
      }

      async publish(events: ToolEvent[]): Promise<void> {
        for (const event of events) {
          if (this.mode === "webhook") {
            await this._publishWebhook(event);
          } else {
            await this._publishRabbitMQ(event);
          }
        }
      }

      private async _publishWebhook(event: ToolEvent): Promise<void> {
        try {
          await fetch(this.webhookUrl!, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify(event),
            signal: AbortSignal.timeout(5000),
          });
        } catch (err) {
          // D-08: absorver silenciosamente — nunca bloquear a resposta ao lead
          // Segurança: logar apenas eventId, nunca payload completo (PII)
          this.logger.warn(
            { err, eventId: event.event_id },
            "Webhook event publish failed — ignoring (fire-and-forget)"
          );
        }
      }

      private async _publishRabbitMQ(event: ToolEvent): Promise<void> {
        if (!this.pub) {
          this.logger.warn(
            { eventId: event.event_id },
            "RabbitMQ publisher not ready — ignoring"
          );
          return;
        }
        try {
          await this.pub.send(this.queue!, event);
        } catch (err) {
          // D-08: absorver silenciosamente
          this.logger.warn(
            { err, eventId: event.event_id },
            "RabbitMQ event publish failed — ignoring (fire-and-forget)"
          );
        }
      }

      async close(): Promise<void> {
        await this.pub?.close();
        await this.rabbit?.close();
      }
    }
    ```

    Rodar: `bun test packages/core/src/events/__tests__/unit/event-publisher.test.ts`
    Confirmar: TODOS os testes passam (GREEN).

    **FASE REFACTOR (se necessário):**
    Revisar para consistência com padrões do projeto. Rodar testes novamente para confirmar verde.

    **Criar diretório primeiro:** `mkdir -p packages/core/src/events/__tests__/unit`
  </action>

  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/events/__tests__/unit/event-publisher.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `packages/core/src/events/event-publisher.ts` existe e contém `export interface IEventPublisher`, `export interface ToolEvent`, `export class EventPublisher implements IEventPublisher`, `export class NoopEventPublisher implements IEventPublisher`
    - `packages/core/src/events/__tests__/unit/event-publisher.test.ts` existe e contém testes para "webhook", "rabbitmq", "disabled", "absorve erro"
    - `bun test packages/core/src/events` sai com código 0 (todos testes verdes)
    - `grep -n "AbortSignal.timeout" packages/core/src/events/event-publisher.ts` retorna ao menos 1 resultado
    - `grep -n "confirm.*true" packages/core/src/events/event-publisher.ts` retorna ao menos 1 resultado
    - `grep -n "TOOL_EVENTS_QUEUE\|TOOL_EVENTS_URL" packages/core/src/events/event-publisher.ts` retorna resultados (leitura de ENVs)
    - `grep -n "lead\.nome\|lead\.numero\|lead\.id" packages/core/src/events/event-publisher.ts` — NENHUM resultado no logger.warn (sem PII em logs)
  </acceptance_criteria>

  <done>
    EventPublisher implementado com TDD: webhook mode e RabbitMQ mode cobertos, erros absorvidos silenciosamente, event_id respeitado, whitelist de escopo documentada no design (filtro no BrainRunner — Plan 02). Testes unitários verdes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrainRunner → EventPublisher | Eventos saem do processo do Brain para sistemas externos (webhook URL ou fila RabbitMQ) |
| ENV → EventPublisher | TOOL_EVENTS_URL e TOOL_EVENTS_QUEUE são configurados pelo operador — fora do controle do lead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | EventPublisher.publish() — campo `action` | mitigate | Whitelist hardcoded `Set<string>` no BrainRunner (Plan 02) — apenas tools da whitelist chegam ao publish(); `action` nunca vem do payload externo do lead |
| T-20-02 | Information Disclosure | logger.warn em falha de publicação | mitigate | Logar apenas `{ err, eventId }` — nunca `event.lead.nome`, `event.lead.numero` nem `event.lead.id` (PII); `eventId = threadId:tool_call_id` não identifica pessoa |
| T-20-03 | Information Disclosure | RABBITMQ_URL em ConfigurationError | mitigate | Validar presença com `!!process.env.RABBITMQ_URL` — nunca logar o valor (contém credenciais); mesmo padrão de consumer.ts T-07-08 |
| T-20-04 | Elevation of Privilege | SSRF via TOOL_EVENTS_URL | accept | URL configurada via ENV pelo operador antes do deploy — lead não tem acesso nem visibilidade ao valor; scope: sistema interno; sem ASVS L1 obrigatório para ENVs de infra |
| T-20-05 | Denial of Service | fetch() sem timeout bloqueia BrainRunner.run() | mitigate | `AbortSignal.timeout(5000)` — máximo 5s de espera por requisição; qualquer erro absorvido em catch (D-08) |
| T-20-06 | Tampering | ToolMessage.name undefined bypassando whitelist | mitigate | Guard explícito `typeof msg.name === "string"` antes de `WHITELIST.has(msg.name)` — undefined não é string, não passa |
</threat_model>

<verification>
Suite completa de eventos:
```bash
cd /root/Brain && bun test packages/core/src/events
```
Deve sair com código 0.

Verificar exports existem:
```bash
grep -n "export" packages/core/src/events/event-publisher.ts
```
Deve conter: IEventPublisher, ToolEvent, EventPublisher, NoopEventPublisher.
</verification>

<success_criteria>
- `bun test packages/core/src/events` → 0 falhas
- `event-publisher.ts` expõe IEventPublisher, ToolEvent, EventPublisher, NoopEventPublisher
- EventPublisher modo webhook: instanciado com TOOL_EVENTS_URL, publish() chama fetch
- EventPublisher modo RabbitMQ: instanciado com TOOL_EVENTS_QUEUE + RABBITMQ_URL, publish() chama pub.send
- Erros em ambos os modos absorvidos com logger.warn (nunca lança)
- Sem PII (nome, numero, id) nos logs de erro
</success_criteria>

<output>
After completion, create `.planning/phases/20-tool-events/20-01-SUMMARY.md`
</output>
