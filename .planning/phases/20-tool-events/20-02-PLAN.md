---
phase: 20-tool-events
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01-PLAN.md
files_modified:
  - packages/core/src/runner/runner.ts
  - packages/core/src/index.ts
  - packages/core/src/runner/__tests__/brain-runner.test.ts
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-04

must_haves:
  truths:
    - "BrainRunner.init() cria EventPublisher a partir de ENVs quando TOOL_EVENTS_URL ou TOOL_EVENTS_QUEUE está presente"
    - "BrainRunner.run() filtra ToolMessages do resultado do invoke() pela whitelist e chama eventPublisher.publish() sem await no call site"
    - "Filtro respeita ToolMessage.isInstance() + typeof msg.name === 'string' + WHITELIST.has(msg.name)"
    - "event_id = threadId:msg.tool_call_id — construído no runner.ts (EVT-04)"
    - "Quando nenhum ENV de Tool Events está configurado, eventPublisher permanece null e BrainRunner funciona normalmente"
    - "BrainRunner.close() chama eventPublisher.close() se publisher estiver inicializado"
    - "IEventPublisher, ToolEvent, EventPublisher exportados pelo barrel packages/core/src/index.ts"
    - "BrainRunnerOptions aceita eventPublisher?: IEventPublisher para injeção em testes (D-11)"
  artifacts:
    - path: "packages/core/src/runner/runner.ts"
      provides: "BrainRunner com EventPublisher integrado em init/run/close + BrainRunnerOptions.eventPublisher"
      contains: "TOOL_EVENTS_WHITELIST"
    - path: "packages/core/src/index.ts"
      provides: "Barrel atualizado exportando IEventPublisher, ToolEvent, EventPublisher"
      exports:
        - IEventPublisher
        - ToolEvent
        - EventPublisher
    - path: "packages/core/src/runner/__tests__/brain-runner.test.ts"
      provides: "Testes de integração BrainRunner+EventPublisher: sem ENV (null), com injeção mock"
  key_links:
    - from: "packages/core/src/runner/runner.ts"
      to: "packages/core/src/events/event-publisher.ts"
      via: "import EventPublisher, IEventPublisher from '../events/event-publisher.js'"
      pattern: "events/event-publisher"
    - from: "packages/core/src/runner/runner.ts (run)"
      to: "this.eventPublisher?.publish(toolEvents)"
      via: "fire-and-forget com .catch() — sem await no call site"
      pattern: "eventPublisher.*publish.*catch"
---

<objective>
Integrar o `EventPublisher` (criado no Plan 01) ao `BrainRunner`: adicionar campo `eventPublisher` em `BrainRunnerOptions`, inicializar em `init()`, interceptar ToolMessages em `run()` e fechar em `close()`. Atualizar barrel export e adicionar casos de teste no `brain-runner.test.ts`.

Purpose: Completar o canal de eventos de tools — após este plan, todo Brain que configure `TOOL_EVENTS_URL` ou `TOOL_EVENTS_QUEUE` publicará automaticamente eventos de `qualify_lead`, `pause_session` e `finish_conversation`.

Output: `runner.ts` modificado, `index.ts` atualizado, `brain-runner.test.ts` com novos casos cobrindo EVT-01 (sem ENV e com injeção mock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-tool-events/20-CONTEXT.md
@.planning/phases/20-tool-events/20-RESEARCH.md
@.planning/phases/20-tool-events/20-01-SUMMARY.md

<interfaces>
<!-- Interfaces do EventPublisher criadas no Plan 01 -->

De packages/core/src/events/event-publisher.ts (criado no Plan 01):
```typescript
export interface ToolEvent {
  event_id: string;    // `${threadId}:${toolCallId}`
  action: string;
  lead: { id: string; nome: string | null; numero: string };
  result: string;      // ToolMessage.content raw ou JSON.stringify se array
  timestamp: string;   // ISO 8601
}

export interface IEventPublisher {
  publish(events: ToolEvent[]): Promise<void>;
  close(): Promise<void>;
}

export class EventPublisher implements IEventPublisher {
  constructor() { /* lê TOOL_EVENTS_QUEUE e TOOL_EVENTS_URL de process.env */ }
  async init(): Promise<void> { /* inicializa conexão RabbitMQ se modo rabbitmq */ }
  async publish(events: ToolEvent[]): Promise<void> { /* fire-and-forget por evento */ }
  async close(): Promise<void> { /* fecha pub e rabbit se abertos */ }
}

export class NoopEventPublisher implements IEventPublisher {
  async publish(_events: ToolEvent[]): Promise<void> {}
  async close(): Promise<void> {}
}
```

De @langchain/core/messages (instalado em packages/core):
```typescript
import { ToolMessage } from "@langchain/core/messages";
// Type guard oficial (verificado em tool.d.ts de @langchain/core@1.1.48):
ToolMessage.isInstance(msg): msg is ToolMessage
// Campos de ToolMessage:
msg.tool_call_id: string
msg.name: string | undefined   // ATENÇÃO: pode ser undefined
msg.content: string | Array<ContentBlock>
```

De packages/core/src/runner/runner.ts (atual — linhas relevantes):
```typescript
// BrainRunnerOptions (linha 29):
export interface BrainRunnerOptions {
  brain: IBrain;
  sql: Sql;
  toolsRegistry: ToolsRegistry;
  llmOptions?: LLMOptions;
  migrationsFolder?: string;
  // ADICIONAR: eventPublisher?: IEventPublisher;
}

// Campos privados da classe (linhas 51-70):
private mcpClient: MultiServerMCPClient | null = null;
// ADICIONAR: private eventPublisher: IEventPublisher | null = null;

// init() — após _compileGraph() (linha ~125):
// ADICIONAR: inicialização do EventPublisher

// run() — após invoke() retornar result (linha ~245):
// Inserir ANTES do "Step 3: Validate structured output"
// result.messages contém ToolMessages do turno atual

// close() — após fechar mcpClient (linha ~293):
// ADICIONAR: await this.eventPublisher?.close();
```

Estrutura atual do run() em runner.ts (linhas 234-248):
```typescript
const result = await this.compiledGraph.invoke(
  { messages: [...], userId: ..., sessionId: ..., leadName: ... },
  { configurable: { thread_id: threadId }, callbacks }
);

// Step 3: Validate structured output (SDK-06, D-12, D-14)
// INSERIR AQUI: filtro de ToolMessages + publish fire-and-forget
const rawOutput = result.brainOutput;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrar EventPublisher no BrainRunner (init, run, close)</name>
  <files>
    packages/core/src/runner/runner.ts
    packages/core/src/runner/__tests__/brain-runner.test.ts
  </files>

  <read_first>
    - packages/core/src/runner/runner.ts (arquivo COMPLETO — ler antes de qualquer edição)
    - packages/core/src/events/event-publisher.ts (interface e classe criadas no Plan 01)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (padrão de mocking existente)
  </read_first>

  <behavior>
    Alterações em brain-runner.test.ts (pré-requisito para Task 2):

    **MockToolMessage no mock de @langchain/core/messages:**
    - Localizar `mock.module("@langchain/core/messages", ...)` existente e adicionar classe `MockToolMessage`:
      ```typescript
      class MockToolMessage {
        constructor(public props: Record<string, unknown>) { Object.assign(this, props); }
        static isInstance(msg: unknown): msg is MockToolMessage {
          return msg instanceof MockToolMessage;
        }
      }
      ```
      Adicionar `ToolMessage: MockToolMessage` ao objeto retornado pelo mock.
    - Com `MockToolMessage` disponível no mock, `ToolMessage.isInstance()` em `runner.ts` reconhecerá
      instâncias de `MockToolMessage` nos testes — base para os testes de whitelist da Task 2.

    Testes de integração runner+publisher (EVT-01 básico):

    **EVT-01 — Sem ENV de Tool Events:**
    - `new BrainRunner({ ... })` sem `TOOL_EVENTS_URL` nem `TOOL_EVENTS_QUEUE` → `init()` completa sem erro;
      `run(event)` retorna `{ brainOutput, tokenUsage }` normalmente; nenhuma chamada de publish é feita

    **close() com publisher injetado:**
    - `new BrainRunner({ ..., eventPublisher: mockPublisher })` → `close()` chama `mockPublisher.close()` exatamente 1 vez
  </behavior>

  <action>
    Modificar `packages/core/src/runner/runner.ts` com as seguintes alterações EXATAS:

    **1. Adicionar import (após imports existentes):**
    ```typescript
    import { ToolMessage } from "@langchain/core/messages";
    import type { IEventPublisher, ToolEvent } from "../events/event-publisher.js";
    import { EventPublisher } from "../events/event-publisher.js";
    ```

    **2. Adicionar `eventPublisher?: IEventPublisher` em BrainRunnerOptions:**
    Localizar a interface `BrainRunnerOptions` e adicionar o campo após `migrationsFolder?`:
    ```typescript
    /** EventPublisher injetável para testes (D-11). Ausente = criado em init() a partir de ENVs. */
    eventPublisher?: IEventPublisher;
    ```

    **3. Adicionar campo privado na classe BrainRunner (após `mcpClient`):**
    ```typescript
    private eventPublisher: IEventPublisher | null = null;
    ```

    **4. Armazenar eventPublisher injetado no construtor (após mcpClient ou no final do construtor):**
    ```typescript
    // D-11: EventPublisher injetável para testes; null = criado em init() a partir de ENVs
    if (options.eventPublisher) {
      this.eventPublisher = options.eventPublisher;
    }
    ```

    **5. Em init(), após `await this._compileGraph()` e ANTES do registro SIGTERM:**
    ```typescript
    // EVT-01: Inicializar EventPublisher se ENVs configuradas (D-11: skip se já injetado)
    if (!this.eventPublisher) {
      const hasQueue = !!process.env.TOOL_EVENTS_QUEUE?.trim();
      const hasUrl = !!process.env.TOOL_EVENTS_URL?.trim();
      if (hasQueue || hasUrl) {
        const publisher = new EventPublisher();
        await publisher.init();
        this.eventPublisher = publisher;
      }
    }
    ```

    **6. Em run(), APÓS a linha `const result = await this.compiledGraph.invoke(...)` e ANTES de `// Step 3: Validate structured output`:**
    ```typescript
    // EVT-01, EVT-02, EVT-04: Publicar eventos de tools da whitelist (D-01, D-03)
    // Intercepção via result.messages (pós-invoke) — sem callbacks LangGraph, independente de MCP
    const TOOL_EVENTS_WHITELIST = new Set([
      "qualify_lead",
      "pause_session",
      "finish_conversation",
    ]);
    if (this.eventPublisher) {
      const toolEvents: ToolEvent[] = [];
      for (const msg of result.messages ?? []) {
        if (
          ToolMessage.isInstance(msg) &&
          typeof msg.name === "string" &&
          TOOL_EVENTS_WHITELIST.has(msg.name)
        ) {
          toolEvents.push({
            event_id: `${threadId}:${msg.tool_call_id}`,
            action: msg.name,
            lead: {
              id: lead.uniqueId,
              nome: lead.nome ?? null,
              numero: lead.numero,
            },
            result:
              typeof msg.content === "string"
                ? msg.content
                : JSON.stringify(msg.content),
            timestamp: new Date().toISOString(),
          });
        }
      }
      // D-08, EVT-01: fire-and-forget — NUNCA await aqui; não bloqueia resposta ao lead
      if (toolEvents.length > 0) {
        this.eventPublisher.publish(toolEvents).catch((err: unknown) => {
          this.logger.warn({ err }, "EventPublisher.publish failed — ignoring (fire-and-forget)");
        });
      }
    }
    ```

    **7. Em close(), APÓS fechar mcpClient:**
    ```typescript
    // EVT-01: Fechar EventPublisher (fecha conexão RabbitMQ se aberta)
    if (this.eventPublisher) {
      await this.eventPublisher.close();
      this.eventPublisher = null;
    }
    ```

    **8. Em brain-runner.test.ts — adicionar MockToolMessage ao mock de @langchain/core/messages:**

    Localizar o `mock.module("@langchain/core/messages", ...)` existente e adicionar a classe `MockToolMessage`
    ANTES da chamada de `mock.module`, depois adicioná-la ao objeto retornado:

    ```typescript
    class MockToolMessage {
      constructor(public props: Record<string, unknown>) {
        Object.assign(this, props);
      }
      static isInstance(msg: unknown): msg is MockToolMessage {
        return msg instanceof MockToolMessage;
      }
    }
    ```

    No objeto retornado por `mock.module("@langchain/core/messages", ...)`, adicionar:
    ```typescript
    ToolMessage: MockToolMessage,
    ```

    Isso garante que `ToolMessage.isInstance()` no `runner.ts` reconheça instâncias de `MockToolMessage`
    nos testes de whitelist da Task 2.

    **IMPORTANTE — TOOL_EVENTS_WHITELIST como constante de módulo:**
    Mover `TOOL_EVENTS_WHITELIST` para fora do método `run()` como constante de módulo (nível de arquivo) para evitar recriação a cada invocação:
    ```typescript
    // Constante de módulo — fora da classe BrainRunner
    const TOOL_EVENTS_WHITELIST = new Set([
      "qualify_lead",
      "pause_session",
      "finish_conversation",
    ]);
    ```
    E dentro do `run()`, remover a declaração e usar apenas a constante de módulo.
  </action>

  <verify>
    <automated>cd /root/Brain && bun test packages/core/src/runner/__tests__/brain-runner.test.ts 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "TOOL_EVENTS_WHITELIST" packages/core/src/runner/runner.ts` retorna ao menos 1 resultado (constante de módulo)
    - `grep -n "ToolMessage.isInstance" packages/core/src/runner/runner.ts` retorna 1 resultado
    - `grep -n "eventPublisher.*publish.*catch\|publish.*toolEvents.*catch" packages/core/src/runner/runner.ts` retorna 1 resultado (fire-and-forget sem await)
    - `grep -n "typeof msg.name.*string" packages/core/src/runner/runner.ts` retorna 1 resultado (guard de undefined)
    - `grep -n "eventPublisher.*IEventPublisher" packages/core/src/runner/runner.ts` retorna resultado em BrainRunnerOptions
    - `grep -n "await.*eventPublisher.*publish" packages/core/src/runner/runner.ts` NÃO retorna resultado (confirma que o call site em run() é fire-and-forget)
    - `grep -n "MockToolMessage\|ToolMessage: Mock" packages/core/src/runner/__tests__/brain-runner.test.ts` retorna ao menos 1 resultado (MockToolMessage adicionado ao mock de @langchain/core/messages)
    - `bun test packages/core/src/runner/__tests__/brain-runner.test.ts` sai com código 0
  </acceptance_criteria>

  <done>
    BrainRunner modificado: init() cria EventPublisher se ENVs presentes, run() filtra e publica ToolMessages da whitelist fire-and-forget, close() fecha o publisher. Campo eventPublisher em BrainRunnerOptions para injeção em testes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar barrel export e adicionar testes EVT no brain-runner.test.ts</name>
  <files>
    packages/core/src/index.ts
    packages/core/src/runner/__tests__/brain-runner.test.ts
  </files>

  <read_first>
    - packages/core/src/index.ts (arquivo completo — ver padrão de exportação existente)
    - packages/core/src/runner/__tests__/brain-runner.test.ts (arquivo completo — ver setup de mocks e padrão de describe)
    - packages/core/src/events/event-publisher.ts (para confirmar nomes exatos dos exports)
  </read_first>

  <action>
    **Parte 1: Atualizar packages/core/src/index.ts**

    Adicionar bloco de exportação ao final do arquivo (após a última linha de export existente), seguindo o padrão de comentários do arquivo:
    ```typescript
    // EVT-01: EventPublisher — canal de saída para eventos de tools
    export type { IEventPublisher, ToolEvent } from "./events/event-publisher.js";
    export { EventPublisher, NoopEventPublisher } from "./events/event-publisher.js";
    ```

    **Parte 2: Adicionar novos describe blocks em brain-runner.test.ts**

    Localizar o final do arquivo (após o último `describe` block fechado). Adicionar:

    ```typescript
    describe("EVT-01: EventPublisher — sem ENV configurada", () => {
      test("BrainRunner.init() e run() funcionam normalmente sem TOOL_EVENTS_URL nem TOOL_EVENTS_QUEUE", async () => {
        // Garantir que ENVs de eventos NÃO estão presentes
        const origUrl = process.env.TOOL_EVENTS_URL;
        const origQueue = process.env.TOOL_EVENTS_QUEUE;
        delete process.env.TOOL_EVENTS_URL;
        delete process.env.TOOL_EVENTS_QUEUE;

        const brain = makeBrain(["system"]);
        const runner = new BrainRunner({
          brain,
          sql: {} as Sql,
          toolsRegistry: registry,
        });
        await runner.init();
        const result = await runner.run(makeEvent());
        // run() retorna resultado normal — sem erro
        expect(result).not.toBeNull();
        expect(result?.brainOutput.fullResponse).toBe("test reply");

        // Restore
        if (origUrl) process.env.TOOL_EVENTS_URL = origUrl;
        if (origQueue) process.env.TOOL_EVENTS_QUEUE = origQueue;
      });
    });

    describe("EVT-02, EVT-04: Whitelist e event_id — injeção via BrainRunnerOptions (D-11)", () => {
      test("run() chama publish() com evento correto quando MockToolMessage da whitelist está em result.messages", async () => {
        const mockPublish = mock(async (_events: unknown[]) => {});
        const mockPublisherClose = mock(async () => {});
        const mockEventPublisher = {
          publish: mockPublish,
          close: mockPublisherClose,
        };

        // Usar MockToolMessage (adicionado ao mock em Task 1) — ToolMessage.isInstance() retornará true
        // porque o mock de @langchain/core/messages usa a classe MockToolMessage como ToolMessage
        const brain = makeBrain(["system"]);
        brain.buildGraph = mock(() => ({
          compile: mock(() => ({
            invoke: mock(async () => ({
              messages: [
                new MockHumanMessage("hello"),
                // qualify_lead na whitelist — deve gerar evento
                new ToolMessage({ name: "qualify_lead", tool_call_id: "call-abc-123", content: "Lead qualificado" }),
                // respond fora da whitelist — não deve gerar evento
                new ToolMessage({ name: "respond", tool_call_id: "call-xyz-999", content: "Respondendo" }),
                new MockAIMessage("test reply"),
              ],
              brainOutput: { fullResponse: "test reply", responseMode: "text" },
              tokenUsage: { inputTokens: 10, outputTokens: 5, totalTokens: 15 },
            })),
            getState: mock(async () => ({ values: { messages: [] } })),
          })),
        })) as unknown as IBrain["buildGraph"];

        const runner = new BrainRunner({
          brain,
          sql: {} as Sql,
          toolsRegistry: registry,
          eventPublisher: mockEventPublisher as unknown as IEventPublisher,
        });
        await runner.init();
        await runner.run(makeEvent());

        // EVT-02: somente qualify_lead (na whitelist) gera evento — respond é ignorado
        // EVT-04: event_id = threadId:tool_call_id
        expect(mockPublish).toHaveBeenCalledTimes(1);
        const [publishedEvents] = mockPublish.mock.calls[0] as [unknown[]];
        expect(publishedEvents).toHaveLength(1);
        expect((publishedEvents[0] as Record<string, unknown>).action).toBe("qualify_lead");
        expect((publishedEvents[0] as Record<string, unknown>).event_id).toMatch(/:call-abc-123$/);
      });

      test("run() não chama publish() quando ToolMessage.name é undefined", async () => {
        const mockPublish = mock(async (_events: unknown[]) => {});
        const mockEventPublisher = {
          publish: mockPublish,
          close: mock(async () => {}),
        };

        const brain = makeBrain(["system"]);
        brain.buildGraph = mock(() => ({
          compile: mock(() => ({
            invoke: mock(async () => ({
              messages: [
                new MockHumanMessage("hello"),
                // ToolMessage sem name — não deve passar o guard typeof === "string"
                new ToolMessage({ tool_call_id: "call-no-name", content: "sem nome" }),
                new MockAIMessage("test reply"),
              ],
              brainOutput: { fullResponse: "test reply", responseMode: "text" },
              tokenUsage: { inputTokens: 10, outputTokens: 5, totalTokens: 15 },
            })),
            getState: mock(async () => ({ values: { messages: [] } })),
          })),
        })) as unknown as IBrain["buildGraph"];

        const runner = new BrainRunner({
          brain,
          sql: {} as Sql,
          toolsRegistry: registry,
          eventPublisher: mockEventPublisher as unknown as IEventPublisher,
        });
        await runner.init();
        await runner.run(makeEvent());

        // name undefined não passa o guard typeof === "string" → publish não é chamado
        expect(mockPublish).not.toHaveBeenCalled();
      });
    });
    ```

    NOTA sobre imports no teste: `ToolMessage` já está disponível no mock de `@langchain/core/messages`
    (adicionado como `MockToolMessage` na Task 1). Use `new ToolMessage(...)` diretamente nos testes —
    o mock resolve para `MockToolMessage` e `ToolMessage.isInstance()` reconhecerá as instâncias.

    Para `IEventPublisher`: usar `as unknown as IEventPublisher` com tipo inline para evitar
    problemas de import circular, ou adicionar import após os existentes:
    ```typescript
    import type { IEventPublisher } from "../../events/event-publisher.js";
    ```
  </action>

  <verify>
    <automated>cd /root/Brain && bun test packages/core/src 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "IEventPublisher\|ToolEvent\|EventPublisher\|NoopEventPublisher" packages/core/src/index.ts` retorna 2 linhas de export (type e value)
    - `grep -n "EVT-01\|EVT-02\|EVT-04\|eventPublisher" packages/core/src/runner/__tests__/brain-runner.test.ts` retorna ao menos 5 resultados
    - `bun test packages/core/src` sai com código 0 (suite completa do core passa)
    - `grep -n "MockToolMessage\|ToolMessage" packages/core/src/runner/__tests__/brain-runner.test.ts` retorna ao menos 1 resultado (ToolMessage no mock de @langchain/core/messages)
    - `grep -n "eventPublisher.*IEventPublisher\|eventPublisher.*BrainRunnerOptions" packages/core/src/runner/runner.ts` retorna resultado (confirma field em BrainRunnerOptions)
  </acceptance_criteria>

  <done>
    Barrel export atualizado com IEventPublisher, ToolEvent, EventPublisher, NoopEventPublisher. brain-runner.test.ts com novos describes cobrindo EVT-01 (sem ENV e com injeção). Suite completa do packages/core passa com código 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiledGraph.invoke() → runner.ts | result.messages contém dados que vêm do LLM (potencial para alucinação de nome de tool) |
| runner.ts → EventPublisher | Eventos construídos internamente antes de saírem para canal externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | TOOL_EVENTS_WHITELIST — msg.name controlado pelo LLM | mitigate | Whitelist hardcoded como `Set<string>` de módulo — LLM não pode injetar novo nome na whitelist via prompt injection; Set.has() rejeita qualquer string fora das 3 permitidas (D-03) |
| T-20-08 | Denial of Service | EventPublisher.publish() bloqueando run() | mitigate | Call site usa `.catch()` sem `await` — Promise do publish corre independentemente; AbortSignal.timeout(5000) limita webhook; run() retorna ao caller sem esperar publish |
| T-20-09 | Information Disclosure | ToolMessage.content com PII em event.result | accept | D-05: result = content raw por design — operador configura o endpoint receptor e é responsável por tratar PII; TOOL_EVENTS_URL é endpoint privado interno (D-07) |
| T-20-10 | Elevation of Privilege | EventPublisher injetado via BrainRunnerOptions.eventPublisher | accept | Injeção apenas em contexto de teste (D-11); em produção, eventPublisher é criado internamente em init() a partir de ENVs controladas pelo operador; campo opcional não expõe superfície de ataque nova |
</threat_model>

<verification>
Suite completa do packages/core:
```bash
cd /root/Brain && bun test packages/core/src
```
Deve sair com código 0.

Verificar exports no barrel:
```bash
grep -n "IEventPublisher\|EventPublisher" /root/Brain/packages/core/src/index.ts
```

Verificar fire-and-forget (ausência de await no call site):
```bash
grep -n "await.*eventPublisher.*publish\|await this\.eventPublisher" /root/Brain/packages/core/src/runner/runner.ts
```
Deve retornar 0 resultados (confirma que não há await no call site de run()).
</verification>

<success_criteria>
- `bun test packages/core/src` → 0 falhas (suite completa verde)
- BrainRunner.init() inicializa EventPublisher a partir de ENVs quando configuradas
- BrainRunner.run() publica eventos fire-and-forget sem bloquear a resposta ao lead
- TOOL_EVENTS_WHITELIST hardcoded com exactly 3 tools: qualify_lead, pause_session, finish_conversation
- Sem await no call site de publish() em run() (confirmado por grep)
- IEventPublisher e ToolEvent exportados por packages/core/src/index.ts
- BrainRunnerOptions.eventPublisher (opcional) disponível para injeção em testes
</success_criteria>

<output>
After completion, create `.planning/phases/20-tool-events/20-02-SUMMARY.md`
</output>
