---
task: 260718-o64
type: quick
title: Atualizar deploy do brain-sdr para v1.6.1 (fix RabbitMQ passive-declare)
status: complete
commit: da1a632
completed: 2026-07-18
files_changed:
  - apps/brain-sdr/docker-compose.portainer.yml
  - apps/brain-sdr/docker-compose.local.yml
  - apps/brain-sdr/docker-compose.yml
---

# Task 260718-o64: Bump brain-sdr compose para v1.6.1 — Summary

Os 3 compose files do brain-sdr passam a referenciar `brain-sdr:1.6.1`, tag git
(`v1.6.1`, commit a1e6338) que carrega o fix `queueOptions: { passive: true }` no
consumer RabbitMQ — eliminando o erro `PRECONDITION_FAILED - inequivalent arg
'durable'` da v1.6. Mudança de config apenas; nenhuma lógica de aplicação tocada.

## Files Changed

| File | Change |
| ---- | ------ |
| `apps/brain-sdr/docker-compose.portainer.yml` | `image: brain-sdr:1.5` → `1.6.1` + cabeçalho atualizado |
| `apps/brain-sdr/docker-compose.local.yml` | `image: brain-sdr:1.5` → `1.6.1` + cabeçalho reescrito (não afirma mais que a imagem já está no host; aponta que precisa ser buildada/pullada antes, referenciando o build compose) |
| `apps/brain-sdr/docker-compose.yml` | `image: brain-sdr:latest` → `1.6.1` (seção `build:` intacta, para rastreabilidade) |

Env, ports, extra_hosts e networks permaneceram intactos em todos os arquivos.

## Commit

- `da1a632` — 🔧 chore(brain-sdr): bump compose image to v1.6.1 for RabbitMQ passive-declare fix

## Verification

- `grep -rn 'brain-sdr:1.5\|brain-sdr:latest' apps/brain-sdr/` → vazio (exit 1). PASSED
- `grep -rc 'brain-sdr:1.6.1' apps/brain-sdr/docker-compose*.yml` → local: 2, portainer: 2, yml: 1 (3 arquivos, ≥1 match cada). PASSED

## Deviations from Plan

None — plan executado como escrito. O ajuste de wording no cabeçalho do
`docker-compose.local.yml` (remover a afirmação de que 1.6.1 já está no host) foi
solicitado explicitamente na tarefa e está dentro do escopo da Task 1.

## Ops Handoff (fora do escopo de edição de repo — Docker indisponível aqui)

Executar no servidor, após merge desta mudança:

1. Buildar e taguear a imagem a partir da tag correta:
   ```
   git checkout v1.6.1
   docker build -t brain-sdr:1.6.1 -f apps/brain-sdr/Dockerfile .
   ```
2. Recriar o container:
   - Portainer: atualizar a stack (image `brain-sdr:1.6.1`) e redeploy.
   - CLI: `docker compose -f apps/brain-sdr/docker-compose.local.yml up -d --force-recreate`
3. Validar: logs do consumer RabbitMQ não devem mais exibir
   `PRECONDITION_FAILED - inequivalent arg 'durable'`.

## Self-Check: PASSED
- Files exist and updated: portainer, local, build compose — confirmed.
- Commit da1a632 exists in git log — confirmed.
