---
task: 260718-o64
type: quick
title: Atualizar deploy do brain-sdr para v1.6.1 (fix RabbitMQ passive-declare)
autonomous: true
files_modified:
  - apps/brain-sdr/docker-compose.portainer.yml
  - apps/brain-sdr/docker-compose.local.yml
  - apps/brain-sdr/docker-compose.yml
commit: "🔧 chore(brain-sdr): bump compose image to v1.6.1 for RabbitMQ passive-declare fix"
---

<objective>
Apontar os compose files do brain-sdr para a imagem `brain-sdr:1.6.1`, cuja
tag git (`v1.6.1`, commit a1e6338) contém o fix `queueOptions: { passive: true }`
no consumer RabbitMQ. Isso elimina o erro `PRECONDITION_FAILED - inequivalent
arg 'durable'` que ocorre com a v1.6 (commit fd4df4b), que NÃO tem o fix.

Purpose: destravar o deploy do brain-sdr com o consumer RabbitMQ funcional.
Output: 3 compose files referenciando a tag correta da imagem.

Root cause já diagnosticado — não re-investigar. Docker indisponível neste
ambiente: build/tag/push e recriação do container são ações de ops (ver Handoff).
</objective>

<context>
Estado atual verificado:
- `docker-compose.portainer.yml`: `image: brain-sdr:1.5` (linha 14) + cabeçalho cita `brain-sdr:1.5`
- `docker-compose.local.yml`: `image: brain-sdr:1.5` (linha 9) + cabeçalho cita `brain-sdr:1.5`
- `docker-compose.yml`: `build:` + `image: brain-sdr:latest` (linha 6)

Decisão para o compose de build (`docker-compose.yml`): trocar `:latest` por
`:1.6.1` explícito. Justificativa — rastreabilidade (a imagem buildada fica
amarrada à tag git v1.6.1) e consistência: os três compose files passam a citar
a mesma versão, evitando ambiguidade sobre qual artefato está rodando.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump portainer + local compose para 1.6.1</name>
  <files>apps/brain-sdr/docker-compose.portainer.yml, apps/brain-sdr/docker-compose.local.yml</files>
  <action>Em ambos os arquivos, substituir todas as ocorrências de `brain-sdr:1.5` por `brain-sdr:1.6.1` — tanto na linha `image:` quanto nas menções em comentário no cabeçalho. Não alterar mais nada (env, ports, extra_hosts permanecem iguais).</action>
  <verify>
    <automated>! grep -rn 'brain-sdr:1.5' apps/brain-sdr/docker-compose.portainer.yml apps/brain-sdr/docker-compose.local.yml && grep -c 'brain-sdr:1.6.1' apps/brain-sdr/docker-compose.portainer.yml apps/brain-sdr/docker-compose.local.yml</automated>
  </verify>
  <done>Nenhuma referência a `brain-sdr:1.5` resta nos dois arquivos; ambos citam `brain-sdr:1.6.1` na linha image e no cabeçalho.</done>
</task>

<task type="auto">
  <name>Task 2: Fixar tag explícita 1.6.1 no compose de build</name>
  <files>apps/brain-sdr/docker-compose.yml</files>
  <action>Trocar `image: brain-sdr:latest` por `image: brain-sdr:1.6.1` (linha 6). Manter a seção `build:` intacta — o build continua a partir do fonte; a tag explícita só dá rastreabilidade ao artefato produzido.</action>
  <verify>
    <automated>grep -q 'image: brain-sdr:1.6.1' apps/brain-sdr/docker-compose.yml && ! grep -q 'brain-sdr:latest' apps/brain-sdr/docker-compose.yml</automated>
  </verify>
  <done>`docker-compose.yml` referencia `brain-sdr:1.6.1`; nenhuma referência a `:latest` resta.</done>
</task>

</tasks>

<verification>
- `grep -rn 'brain-sdr:1.5\|brain-sdr:latest' apps/brain-sdr/` retorna vazio.
- `grep -rc 'brain-sdr:1.6.1' apps/brain-sdr/docker-compose*.yml` retorna 3 arquivos com ao menos 1 match cada.
- Commit único seguindo Conventional Commits:
  `🔧 chore(brain-sdr): bump compose image to v1.6.1 for RabbitMQ passive-declare fix`
</verification>

<success_criteria>
Os 3 compose files do brain-sdr apontam para `brain-sdr:1.6.1`. Mudança de config
apenas; nenhuma lógica de aplicação tocada.
</success_criteria>

<handoff>
## Ações de ops (fora do escopo de edição de repo — Docker indisponível aqui)

Executar no servidor, após merge desta mudança:

1. Buildar e taguear a imagem a partir da tag correta:
   ```
   git checkout v1.6.1
   docker build -t brain-sdr:1.6.1 -f apps/brain-sdr/Dockerfile .
   ```
2. Recriar o container:
   - Portainer: atualizar a stack (image `brain-sdr:1.6.1`) e redeploy.
   - CLI: `docker compose -f apps/brain-sdr/docker-compose.local.yml up -d --force-recreate`
3. Validar: logs do consumer RabbitMQ não devem mais exibir
   `PRECONDITION_FAILED - inequivalent arg 'durable'`.
</handoff>

<output>
Task de quick — sem SUMMARY dedicado. Registrar conclusão no commit.
</output>
