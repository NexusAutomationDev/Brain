---
phase: 260618-g2w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/tables.ts
  - packages/database/src/schema/tables.test.ts
  - packages/database/src/migrations/0006_leads_cols_remove_users.sql
  - packages/database/src/migrations/meta/_journal.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Export `users` removido de tables.ts — nenhum código importa a tabela users"
    - "Migration 0006 executa DROP TABLE IF EXISTS users e ADD COLUMN id_deal / id_contato em leads"
    - "Journal atualizado com entry idx 6 apontando para 0006_leads_cols_remove_users"
    - "Schema leads contém campos idDeal (id_deal, text, nullable) e idContato (id_contato, text, nullable)"
    - "Testes do schema passam sem referências a users, com cobertura dos novos campos"
  artifacts:
    - path: "packages/database/src/schema/tables.ts"
      provides: "Schema sem export users, leads com idDeal/idContato"
    - path: "packages/database/src/migrations/0006_leads_cols_remove_users.sql"
      provides: "Migration SQL: DROP TABLE users + ADD COLUMN id_deal/id_contato"
    - path: "packages/database/src/migrations/meta/_journal.json"
      provides: "Journal com entry idx=6 para nova migration"
    - path: "packages/database/src/schema/tables.test.ts"
      provides: "Testes sem users, com cobertura de idDeal/idContato"
  key_links:
    - from: "packages/database/src/migrations/meta/_journal.json"
      to: "packages/database/src/migrations/0006_leads_cols_remove_users.sql"
      via: "tag: 0006_leads_cols_remove_users"
      pattern: "0006_leads_cols_remove_users"
---

<objective>
Remover a tabela `users` do schema Drizzle e criar migration que limpa bancos existentes. Simultaneamente, adicionar colunas `id_deal` e `id_contato` (nullable) à tabela `leads` via schema + migration.

Purpose: A tabela users nunca foi utilizada e polui o schema. Os campos IDdeal/IDcontato são necessários para integração com CRM e serão consumidos pelo LeadService.
Output: Schema atualizado, migration 0006 aplicável via `runMigrations()`, testes verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar schema Drizzle — remover users, adicionar colunas em leads</name>
  <files>
    packages/database/src/schema/tables.ts
    packages/database/src/schema/tables.test.ts
  </files>
  <behavior>
    - Import: `users` NÃO deve mais ser exportado de tables.ts
    - Schema leads deve ter campo `idDeal: text('id_deal')` (nullable — sem `.notNull()`)
    - Schema leads deve ter campo `idContato: text('id_contato')` (nullable — sem `.notNull()`)
    - Test: `leads.idDeal.name` === `'id_deal'`, `leads.idDeal.notNull` === `false`
    - Test: `leads.idContato.name` === `'id_contato'`, `leads.idContato.notNull` === `false`
    - Test: import de `users` removido do arquivo de testes — o bloco `DB-01: exports users table...` deve ser deletado
  </behavior>
  <action>
**Em `packages/database/src/schema/tables.ts`:**

1. Remover o bloco `export const users = pgTable('users', { ... })` inteiro (linhas 14-21). O comentário `// DB-01: Users table...` também deve ser removido.

2. Na tabela `leads`, adicionar dois campos após `fullpp` e antes de `createdAt`:
```typescript
// IDdeal e IDcontato — integração CRM (nullable, sem regra de negócio em v1)
idDeal: text('id_deal'),
idContato: text('id_contato'),
```

**Em `packages/database/src/schema/tables.test.ts`:**

1. Remover `users` do import da linha 2. O import deve ficar:
```typescript
import { memories, agentState, embeddings, prompts, leads } from './tables.js';
```

2. Remover o bloco de teste completo `it('exports users table with uuid id and externalId unique constraint', ...)` (linhas 11-19).

3. Adicionar dois novos testes dentro do describe `LEAD-01`, após o teste `D-07: updatedAt`:
```typescript
it('idDeal maps to column id_deal and is nullable (no notNull)', () => {
  expect(leads.idDeal.name).toBe('id_deal');
  expect(leads.idDeal.notNull).toBe(false);
});

it('idContato maps to column id_contato and is nullable (no notNull)', () => {
  expect(leads.idContato.name).toBe('id_contato');
  expect(leads.idContato.notNull).toBe(false);
});
```
  </action>
  <verify>
    <automated>cd /root/Brain && bun test packages/database/src/schema/tables.test.ts 2>&1</automated>
  </verify>
  <done>Todos os testes passam; nenhuma referência a `users` existe em tables.ts ou tables.test.ts; `leads.idDeal` e `leads.idContato` estão definidos como text nullable.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration 0006 e atualizar journal</name>
  <files>
    packages/database/src/migrations/0006_leads_cols_remove_users.sql
    packages/database/src/migrations/meta/_journal.json
  </files>
  <action>
**Criar `packages/database/src/migrations/0006_leads_cols_remove_users.sql`** com o conteúdo exato:

```sql
DROP TABLE IF EXISTS "users";
--> statement-breakpoint
ALTER TABLE "leads" ADD COLUMN "id_deal" text;
--> statement-breakpoint
ALTER TABLE "leads" ADD COLUMN "id_contato" text;
```

**Atualizar `packages/database/src/migrations/meta/_journal.json`** — adicionar entry ao final do array `entries`:

```json
{
  "idx": 6,
  "version": "7",
  "when": 1781793280000,
  "tag": "0006_leads_cols_remove_users",
  "breakpoints": true
}
```

O JSON final do journal deve ter os campos na mesma ordem dos entries anteriores: `idx`, `version`, `when`, `tag`, `breakpoints`.
  </action>
  <verify>
    <automated>cd /root/Brain && cat packages/database/src/migrations/meta/_journal.json | bun -e "const j = JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const e = j.entries.find(x=>x.idx===6); if(!e) throw new Error('entry idx=6 not found'); if(e.tag !== '0006_leads_cols_remove_users') throw new Error('wrong tag: '+e.tag); console.log('OK: entry idx=6 found, tag='+e.tag);" 2>&1 && test -f packages/database/src/migrations/0006_leads_cols_remove_users.sql && grep -q 'DROP TABLE IF EXISTS "users"' packages/database/src/migrations/0006_leads_cols_remove_users.sql && grep -q '"id_deal"' packages/database/src/migrations/0006_leads_cols_remove_users.sql && grep -q '"id_contato"' packages/database/src/migrations/0006_leads_cols_remove_users.sql && echo "SQL OK"</automated>
  </verify>
  <done>Arquivo SQL existe com DROP TABLE users e dois ADD COLUMN; journal contém entry idx=6 com tag correta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runtime → PostgreSQL | SQL executado com privilégios de banco do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g2w-01 | Tampering | DROP TABLE IF EXISTS users | accept | IF EXISTS evita erro em bancos que já não têm a tabela; operação idempotente e reversível apenas via restore |
| T-g2w-02 | Denial of Service | ADD COLUMN em tabela leads com dados existentes | accept | Operação de ALTER TABLE em PG é de baixo custo para colunas nullable sem default computado; sem lock prolongado |
</threat_model>

<verification>
Após execução de ambas as tasks:

1. `bun test packages/database/src/schema/tables.test.ts` — todos os testes verdes, sem referência a `users`
2. `cat packages/database/src/migrations/meta/_journal.json` — deve conter 7 entries (idx 0–6)
3. `cat packages/database/src/migrations/0006_leads_cols_remove_users.sql` — deve conter DROP TABLE e dois ADD COLUMN
4. `grep -r "from.*tables" packages/ --include="*.ts" | grep users` — zero resultados (nenhum import de users restante)
</verification>

<success_criteria>
- Export `users` removido de `tables.ts` sem quebrar nenhum outro import no monorepo
- `leads` schema contém `idDeal` (id_deal, text, nullable) e `idContato` (id_contato, text, nullable)
- Migration `0006_leads_cols_remove_users.sql` criada com SQL correto
- Journal atualizado com entry idx=6
- Todos os testes de schema passam (sem testes de `users`, com testes de `idDeal`/`idContato`)
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260618-g2w-remover-tabela-users-das-migrations-e-ad/260618-g2w-SUMMARY.md` com:
- O que foi alterado em cada arquivo
- Verificações executadas e resultado
- Qualquer desvio do plano
</output>
