---
phase: quick-260717-uz8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/brain-sdr/docker-compose.local.yml
autonomous: true
requirements: [QUICK-01]

must_haves:
  truths:
    - "`docker compose -f apps/brain-sdr/docker-compose.local.yml up` sobe o serviço brain-sdr diretamente a partir da imagem local brain-sdr:1.5, sem executar nenhum build"
    - "O container brain-sdr recebe as mesmas variáveis de ambiente do compose de produção via `env_file: .env` e `MIGRATIONS_FOLDER: /app/migrations`"
    - "O container consegue resolver `host.docker.internal` (extra_hosts) para acessar o Postgres do host, igual ao compose de build"
    - "O serviço se conecta à rede externa `traefikNet`, igual aos demais Brains (brain-sdr build, brain-support, brain-echo)"
    - "O compose de build existente (apps/brain-sdr/docker-compose.yml) e o Dockerfile permanecem exatamente inalterados"
  artifacts:
    - path: "apps/brain-sdr/docker-compose.local.yml"
      provides: "Compose local do brain-sdr sem seção build, usando `image: brain-sdr:1.5` diretamente"
      contains: "image: brain-sdr:1.5"
  key_links:
    - "apps/brain-sdr/docker-compose.local.yml services.brain-sdr.networks -> apps/brain-sdr/docker-compose.local.yml networks.traefikNet (external: true, mesmo padrão do compose de build)"
    - "apps/brain-sdr/docker-compose.local.yml services.brain-sdr.env_file -> apps/brain-sdr/.env (deve existir no host em tempo de execução, mesma convenção do compose de build)"
---

<objective>
Criar `apps/brain-sdr/docker-compose.local.yml`: um compose alternativo para `apps/brain-sdr` que roda diretamente a partir da imagem local já existente `brain-sdr:1.5` (confirmada via `docker images` no host), sem seção `build:` — ou seja, `docker compose up` não tenta reconstruir a imagem a partir do Dockerfile.

Purpose: O usuário quer subir o brain-sdr rapidamente usando a imagem já buildada localmente, sem esperar um rebuild, mantendo o compose de build/produção (`apps/brain-sdr/docker-compose.yml`) intacto para quando o rebuild for realmente necessário.

Output: Novo arquivo `apps/brain-sdr/docker-compose.local.yml`, com env_file, environment, extra_hosts, restart e network (`traefikNet`, external) espelhando o compose de build existente e o padrão usado em `apps/brain-support/docker-compose.yml`. Nenhum outro arquivo é tocado.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/brain-sdr/docker-compose.yml
@apps/brain-support/docker-compose.yml

<interfaces>
Compose de build atual (`apps/brain-sdr/docker-compose.yml`) — usar como base de referência para env_file, ports, extra_hosts, restart e network (tudo exceto a seção `build:` e a tag da imagem, que mudam):

services:
  brain-sdr:
    build:
      context: ../..
      dockerfile: apps/brain-sdr/Dockerfile
    image: brain-sdr:latest
    ports:
      - "3002:${PORT:-3001}"
    env_file: .env
    environment:
      MIGRATIONS_FOLDER: /app/migrations
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    networks:
      - traefikNet

networks:
  traefikNet:
    external: true

Padrão irmão (`apps/brain-support/docker-compose.yml`) confirma o mesmo formato de header comment, mesma network `traefikNet` externa e mesma linha de uso (`docker compose -f apps/brain-{tipo}/docker-compose.yml up`), adaptada aqui para `docker-compose.local.yml`.

Dockerfile do brain-sdr (`apps/brain-sdr/Dockerfile`) confirma os defaults de runtime: `ENV PORT=3000`, `ENV MIGRATIONS_FOLDER=/app/migrations`, `USER bun` — consistentes com o `environment.MIGRATIONS_FOLDER` já usado no compose de build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar docker-compose.local.yml usando a imagem local brain-sdr:1.5 sem build</name>
  <files>apps/brain-sdr/docker-compose.local.yml</files>
  <action>
Criar o arquivo `apps/brain-sdr/docker-compose.local.yml`. NÃO modificar `apps/brain-sdr/docker-compose.yml` nem `apps/brain-sdr/Dockerfile` — ambos permanecem exatamente como estão.

Abrir o arquivo com um comentário de cabeçalho curto (3-4 linhas) explicando que esta é a variante local: roda diretamente a partir da imagem `brain-sdr:1.5` já presente no host (confirmada via `docker images`), sem etapa de build, e que o compose de build/produção continua sendo `apps/brain-sdr/docker-compose.yml`. Incluir a linha de uso: `docker compose -f apps/brain-sdr/docker-compose.local.yml up`.

Definir um único serviço chamado `brain-sdr` sob `services:`, espelhando `apps/brain-sdr/docker-compose.yml` para env_file, environment, extra_hosts, restart e ports, e espelhando `apps/brain-support/docker-compose.yml` para o bloco de rede externa. As chaves exatas do serviço:
- `image: brain-sdr:1.5` — referencia a tag local diretamente. NÃO incluir nenhuma chave `build:` (nem `context:` nem `dockerfile:` dentro dela) — este compose deve rodar contra a imagem já buildada, sem reconstruir.
- `ports:` mapeando a porta 3002 do host para `${PORT:-3001}` do container, escrito como `"3002:${PORT:-3001}"` — o mesmo mapeamento do `apps/brain-sdr/docker-compose.yml` atual.
- `env_file: .env` — lê variáveis de ambiente do arquivo `.env` no mesmo diretório de onde o `docker compose` é executado, mesma convenção dos dois composes de referência.
- `environment:` com `MIGRATIONS_FOLDER: /app/migrations` — igual ao `ENV MIGRATIONS_FOLDER=/app/migrations` do Dockerfile, mantido explícito por paridade com o compose de build.
- `extra_hosts:` com uma única entrada `"host.docker.internal:host-gateway"` — permite o container acessar o Postgres rodando no host.
- `restart: unless-stopped`.
- `networks:` listando `traefikNet`.

No final do arquivo, declarar o bloco `networks:` de nível raiz com `traefikNet:` marcado `external: true` — padrão idêntico aos dois composes de referência.

Manter indentação YAML de 2 espaços, igual aos dois arquivos de referência. Não adicionar nenhuma chave `context:` ou `dockerfile:` em nenhum lugar do arquivo novo.
  </action>
  <verify>
    <automated>test -f apps/brain-sdr/docker-compose.local.yml && grep -q 'image: brain-sdr:1.5' apps/brain-sdr/docker-compose.local.yml && ! grep -Eq '^[[:space:]]*(build:|context:|dockerfile:)' apps/brain-sdr/docker-compose.local.yml && grep -q 'env_file: .env' apps/brain-sdr/docker-compose.local.yml && grep -q 'MIGRATIONS_FOLDER: /app/migrations' apps/brain-sdr/docker-compose.local.yml && grep -q 'host.docker.internal:host-gateway' apps/brain-sdr/docker-compose.local.yml && grep -q 'restart: unless-stopped' apps/brain-sdr/docker-compose.local.yml && grep -q 'traefikNet' apps/brain-sdr/docker-compose.local.yml && grep -q 'external: true' apps/brain-sdr/docker-compose.local.yml && grep -q '"3002:${PORT:-3001}"' apps/brain-sdr/docker-compose.local.yml && git diff --quiet -- apps/brain-sdr/docker-compose.yml apps/brain-sdr/Dockerfile && echo PASS</automated>
  </verify>
  <done>`apps/brain-sdr/docker-compose.local.yml` existe, referencia `image: brain-sdr:1.5` sem nenhuma chave build/context/dockerfile, inclui `env_file: .env`, `MIGRATIONS_FOLDER: /app/migrations`, `extra_hosts` com `host.docker.internal:host-gateway`, `restart: unless-stopped`, e uma rede externa `traefikNet` com o mesmo mapeamento de porta `3002:${PORT:-3001}` do compose de build existente. `apps/brain-sdr/docker-compose.yml` e `apps/brain-sdr/Dockerfile` permanecem intocados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Arquivo de configuração docker-compose local, sem código executável novo; não introduz endpoint, parsing de input externo, nem lógica de aplicação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-quick260717uz8-01 | Tampering | apps/brain-sdr/docker-compose.local.yml | low | accept | Arquivo de config estático, sem instalação de pacotes npm/pip/cargo; imagem referenciada (`brain-sdr:1.5`) já é buildada e assinada pelo próprio pipeline de build do projeto, não é um pacote de terceiros baixado dinamicamente |
| T-quick260717uz8-02 | Information Disclosure | env_file .env | low | accept | Mesmo padrão de `apps/brain-sdr/docker-compose.yml` e `apps/brain-support/docker-compose.yml` — `.env` já está fora do controle de versão (gitignored) no restante do projeto; este plano não altera esse tratamento |
</threat_model>

<verification>
1. `test -f apps/brain-sdr/docker-compose.local.yml` confirma que o arquivo foi criado.
2. Grep positivo confirma presença de `image: brain-sdr:1.5`, `env_file: .env`, `MIGRATIONS_FOLDER: /app/migrations`, `host.docker.internal:host-gateway`, `restart: unless-stopped`, `traefikNet`, `external: true` e o mapeamento de porta `"3002:${PORT:-3001}"`.
3. Grep negativo (ancorado em início de linha) confirma ausência de qualquer chave `build:`, `context:` ou `dockerfile:` no arquivo novo.
4. `git diff --quiet -- apps/brain-sdr/docker-compose.yml apps/brain-sdr/Dockerfile` confirma que nenhum dos dois arquivos de referência foi modificado.
</verification>

<success_criteria>
- `apps/brain-sdr/docker-compose.local.yml` existe e permite `docker compose -f apps/brain-sdr/docker-compose.local.yml up` diretamente a partir da imagem `brain-sdr:1.5` local, sem build.
- Todas as configurações de runtime (env_file, MIGRATIONS_FOLDER, extra_hosts, restart, network traefikNet, porta) espelham o compose de build existente.
- `apps/brain-sdr/docker-compose.yml` (build/produção) e `apps/brain-sdr/Dockerfile` permanecem exatamente como estavam antes deste plano.
</success_criteria>

<output>
Create `.planning/quick/260717-uz8-docker-compose-yml-para-brain-sdr-usando/260717-uz8-SUMMARY.md` when done
</output>
