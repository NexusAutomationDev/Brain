---
phase: quick-260725-h17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish-brain-sdr.yml
  - .github/workflows/publish-brain-support.yml
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]
must_haves:
  truths:
    - "publish-brain-sdr.yml e publish-brain-support.yml podem ser disparados manualmente via workflow_dispatch com um input version obrigatório, sem precisar de push de tag"
    - "Um run manual calcula VERSION removendo um 'v' inicial do input version; um run por push de tag continua calculando VERSION via parse de GITHUB_REF exatamente como antes"
    - "O trigger push: tags: existente e todos os steps de Docker Hub (login, build-push-action, cache config) permanecem inalterados em ambos os arquivos"
  artifacts:
    - .github/workflows/publish-brain-sdr.yml
    - .github/workflows/publish-brain-support.yml
  key_links:
    - "workflow_dispatch.inputs.version -> env.RAW_VERSION -> steps.version.outputs.VERSION -> docker/build-push-action tags (biellil/IMAGE_NAME:VERSION)"
---

<objective>
Adicionar um trigger `workflow_dispatch` manual aos dois workflows de publish (Docker Hub) — `publish-brain-sdr.yml` e `publish-brain-support.yml` — para que possam ser disparados pela UI do GitHub Actions sem precisar de push de tag, mantendo o trigger `push: tags:` existente intocado.

Purpose: Hoje os dois workflows só disparam via `push: tags: ['v*.*.*', 'v*.*']` (migrados para Docker Hub push na quick task 260725-gme). Rodar manualmente hoje exige criar/mover uma tag git. Adicionar `workflow_dispatch` com um input `version` resolve isso sem tocar no fluxo de tag existente.

Output: `.github/workflows/publish-brain-sdr.yml` e `.github/workflows/publish-brain-support.yml` com trigger duplo (`push: tags:` + `workflow_dispatch:`) e o step "Extract version from tag" tratando ambos os casos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.github/workflows/publish-brain-sdr.yml
@.github/workflows/publish-brain-support.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: workflow_dispatch manual em publish-brain-sdr.yml</name>
  <files>.github/workflows/publish-brain-sdr.yml</files>
  <action>
Em `.github/workflows/publish-brain-sdr.yml`, adicionar `workflow_dispatch:` como irmão do bloco `push:` já existente dentro de `on:` (mesma indentação de 2 espaços de `push:`), per D-01. Não remover nem reformatar o bloco `push: tags: - 'v*.*.*' - 'v*.*'` existente. Sob `workflow_dispatch:`, adicionar `inputs:` (um nível de indentação abaixo) e, sob `inputs:`, `version:` (mais um nível abaixo) com três chaves no próximo nível de indentação: `description: 'Version to publish (e.g. 1.6.3) — used when running manually'`, `required: true` e `type: string` — per D-02, tornando `version` um input string obrigatório para runs manuais.

Em seguida, atualizar o step "Extract version from tag" (`id: version`) per D-03. Substituir a linha única atual `run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT` por: um mapeamento `env:` (mesmo nível de indentação de `run:`, ou seja um nível abaixo do step) contendo uma única chave `RAW_VERSION: ${{ inputs.version }}`, seguido de um `run: |` (block scalar, mesma indentação de `env:`) com um condicional shell if/else/fi: primeira linha `if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then`, depois indentado `echo "VERSION=${RAW_VERSION#v}" >> $GITHUB_OUTPUT` no branch workflow_dispatch (remove um `v` inicial do valor vindo do env var, produzindo semver puro como no fluxo de tag), uma linha `else`, depois indentado `echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT` (a linha exata já existente no arquivo, sem alteração) no branch de tag push, e um `fi` de fechamento. Passar o valor do run manual pelo mapeamento `env:` do step (`RAW_VERSION`) em vez de interpolar `${{ inputs.version }}` diretamente dentro do texto do `run:` — funcionalmente idêntico à intenção de D-03, mas evita colar um input não-sanitizado diretamente no texto do script shell (endurecimento contra script injection recomendado pela própria documentação do GitHub Actions), já que o `run:` é um shell script sem escaping automático.

Deixar os steps `Checkout`, `Set up Docker Buildx`, `Log in to Docker Hub` e `Build and push Docker image`, e o bloco `env:` de nível workflow (`APP_NAME`, `IMAGE_NAME`), completamente intocados — per D-04, nenhuma outra linha do arquivo muda. Não editar os comentários de cabeçalho do arquivo (linhas 1-5).
  </action>
  <verify>
    <automated>cd /root/Brain && python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" .github/workflows/publish-brain-sdr.yml && grep -q "workflow_dispatch:" .github/workflows/publish-brain-sdr.yml && grep -q "required: true" .github/workflows/publish-brain-sdr.yml && grep -Fq 'RAW_VERSION: ${{ inputs.version }}' .github/workflows/publish-brain-sdr.yml && grep -Fq '"${{ github.event_name }}" = "workflow_dispatch"' .github/workflows/publish-brain-sdr.yml && grep -Fq "v*.*.*" .github/workflows/publish-brain-sdr.yml && grep -Fq "v*.*'" .github/workflows/publish-brain-sdr.yml && grep -q "docker/login-action@v3" .github/workflows/publish-brain-sdr.yml && grep -q "docker/build-push-action@v6" .github/workflows/publish-brain-sdr.yml && grep -q "cache-from: type=gha,scope=brain-sdr" .github/workflows/publish-brain-sdr.yml && echo "OK: publish-brain-sdr.yml valid"</automated>
  </verify>
  <done>publish-brain-sdr.yml é YAML válido, tem workflow_dispatch com input version obrigatório, o step "Extract version from tag" trata os dois event_name via env RAW_VERSION, e o trigger de tag + todos os steps de Docker Hub permanecem exatamente como antes.</done>
</task>

<task type="auto">
  <name>Task 2: Espelhar a mudança em publish-brain-support.yml</name>
  <files>.github/workflows/publish-brain-support.yml</files>
  <action>
Aplicar em `.github/workflows/publish-brain-support.yml` a MESMA mudança da Task 1, per D-05 (os dois arquivos são near-mirrors e devem receber a mudança idêntica). Adicionar `workflow_dispatch:` como irmão de `push:` dentro de `on:` (per D-01), com `inputs: version: description: 'Version to publish (e.g. 1.6.3) — used when running manually'`, `required: true`, `type: string` (per D-02) — mesma estrutura e indentação da Task 1.

Atualizar o step "Extract version from tag" (`id: version`) exatamente como na Task 1 (per D-03): mapeamento `env: RAW_VERSION: ${{ inputs.version }}` seguido de `run: |` com `if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then` / `echo "VERSION=${RAW_VERSION#v}" >> $GITHUB_OUTPUT` / `else` / `echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT` (linha já existente, sem alteração) / `fi`.

Deixar intocados: o trigger `push: tags:` existente, os steps `Checkout`, `Set up Docker Buildx`, `Log in to Docker Hub`, `Build and push Docker image`, o bloco `env:` de nível workflow (`APP_NAME`, `IMAGE_NAME`) e os comentários de cabeçalho do arquivo (incluindo a menção a D-11/tags disparando ambos os workflows) — per D-04, nenhuma outra linha muda.

Ao commitar esta task (e o plano como um todo), usar o tipo `ci` com emoji 🤖 conforme o Git Commit Guidelines do CLAUDE.md — per D-06 — título em inglês, modo imperativo, <=72 caracteres, sem ponto final (ex.: "add manual workflow_dispatch trigger to publish workflows").
  </action>
  <verify>
    <automated>cd /root/Brain && python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" .github/workflows/publish-brain-support.yml && grep -q "workflow_dispatch:" .github/workflows/publish-brain-support.yml && grep -q "required: true" .github/workflows/publish-brain-support.yml && grep -Fq 'RAW_VERSION: ${{ inputs.version }}' .github/workflows/publish-brain-support.yml && grep -Fq '"${{ github.event_name }}" = "workflow_dispatch"' .github/workflows/publish-brain-support.yml && grep -Fq "v*.*.*" .github/workflows/publish-brain-support.yml && grep -Fq "v*.*'" .github/workflows/publish-brain-support.yml && grep -q "docker/login-action@v3" .github/workflows/publish-brain-support.yml && grep -q "docker/build-push-action@v6" .github/workflows/publish-brain-support.yml && grep -q "cache-from: type=gha,scope=brain-support" .github/workflows/publish-brain-support.yml && echo "OK: publish-brain-support.yml valid"</automated>
  </verify>
  <done>publish-brain-support.yml é YAML válido, espelha exatamente a mudança de publish-brain-sdr.yml (workflow_dispatch + input version obrigatório + step de extração tratando os dois event_name), e o trigger de tag + todos os steps de Docker Hub permanecem exatamente como antes.</done>
</task>

</tasks>

<verification>
- Ambos os arquivos passam `python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))"` (YAML bem formado)
- `grep -c "workflow_dispatch:"` retorna 1 ocorrência em cada arquivo (trigger adicionado uma única vez)
- `grep -Fc "v*.*.*"` e `grep -Fc "v*.*'"` continuam retornando 1 em cada arquivo (trigger de tag intocado)
- `docker/login-action@v3` e `docker/build-push-action@v6` presentes e inalterados em ambos os arquivos
- `git diff --stat` mostra apenas os dois arquivos `.github/workflows/publish-brain-*.yml` modificados — nenhum outro arquivo tocado
</verification>

<success_criteria>
- Disparar `publish-brain-sdr.yml` ou `publish-brain-support.yml` manualmente pela UI do GitHub Actions (workflow_dispatch) com um `version` (ex: "1.6.3" ou "v1.6.3") funciona sem precisar de tag git
- O step "Extract version from tag" produz `VERSION=1.6.3` em ambos os casos (manual com ou sem "v" prefixado, ou push de tag `v1.6.3`)
- Push de uma tag `v*.*.*` ou `v*.*` continua dispando os workflows exatamente como antes (nenhuma regressão no fluxo existente)
- Nenhum outro step (Docker Hub login, build-push-action, cache) foi alterado
- Commit segue a convenção do CLAUDE.md: tipo `ci`, emoji 🤖, título em inglês <=72 caracteres, modo imperativo, sem ponto final
</success_criteria>

<output>
Create `.planning/quick/260725-h17-adicionar-workflow-dispatch-manual-aos-w/260725-h17-SUMMARY.md` when done
</output>
