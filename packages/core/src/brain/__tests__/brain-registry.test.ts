// SDK-01: BrainRegistry — register and resolve IBrain instances by ID
// Wave 0 stub: tests are todo until implementation exists in brain/registry.ts

import { describe, test } from "bun:test";

describe("BrainRegistry", () => {
  test.todo("register() stores an IBrain and resolve() returns it by id");
  test.todo("resolve() throws ConfigurationError when brain id is not registered");
  test.todo("register() throws ConfigurationError when same brain id is registered twice");
});
